"""The curated tool surface exposed to the model (contracts/agent-tools.md).

Nothing here executes code, touches the file system outside the evidence package, or
writes to a SOLIDWORKS document. The only writes are to the current `ReviewSession`.
"""
