# Review backlog

Should-fix findings left unapplied by the adversarial review rounds of 2026-09-13. Every must-fix from the same rounds was applied and is in the history; these are quality and hardening items, confirmed by a reviewer but not required to pass the gates. Generated from the workflow outputs of the implementation session; file and line references are as of the commit each round reviewed and may have drifted.


Total items: 1200. Generated 2026-09-13; feature 003, 004 and 005 sections appended 2026-09-16, the handoff rounds the same day. Feature 006 spec review appended 2026-09-17. Feature 006 implementation rounds A-E appended 2026-09-17. The open follow-ups of features 008 and 009 appended 2026-09-23. Feature 011's open follow-ups appended the same day.


## Pre-test diagnostics (items 1, 3, 4, 8)


### 4-check-interference (11)

- **reviewer/src/swreview/cli.py:628** - Unreported deviation from the item text. The item's correction (1) says to use "_finding_lines per group (reaching group['result']['finding'])", but the implementation calls `_finding_lines(group["result"])`. I verified the implementation is the correct one -- `check_interference_group` returns a `CheckResult` (reviewer/src/swreview/checks/result.py:50-65: check/status/severity/observed/requirement/inputs/calculation/coverage_limits/recommended_action), so there is no `finding` key and the item's parenthetical would have raised KeyError. The implementer's report nonetheless says "Deviations from the item text: none in behavior", so the wrong parenthetical is now unflagged and a later reader could "fix" the code back into a crash.
  - Fix: Record the deviation explicitly for the orchestrator: the item's `group['result']['finding']` is factually wrong; `group['result']` IS the finding-shaped CheckResult dict. No code change.
- **reviewer/tests/unit/test_cli.py:883** - DRY violation against Principle V ("repeated logic across checks, parsers, or scripts MUST be extracted once it appears twice with the same intent"). `INTERFERENCE_FIXTURE = Path(__file__).resolve().parents[1] / "golden" / "fixtures" / "bracket-assy-interference"` is a verbatim copy of `FIXTURE_DIR` at reviewer/tests/unit/test_checks_interference.py:38-40. test_cli.py already imports fixtures across test modules (`from tests.unit.test_tools_checks_fastener import joint_package`, line 32), so the duplication is also inconsistent with the file's own convention. If the golden fixture is ever renamed or moved, two files must change and only one is obvious.
  - Fix: Import the existing constant (`from tests.unit.test_checks_interference import FIXTURE_DIR`) or move it to tests/support/, and keep the new docstring on the local alias.
- **specs/001-agentic-design-review/contracts/cli.md:18** - Contract drift: the row still reads ``swreview check fit\|stack\|fastener\|alignment`` and does not list the new `interference` subcommand, so the CLI contract no longer describes the CLI. Not a scope violation by this item -- the orchestrator restricted spec edits to files the item names, and item 4 names none -- but the drift is real and nothing in the test suite catches it (no test reads contracts/cli.md).
  - Fix: Route to the item that owns contracts/cli.md (rank 9): add `interference` to the check row, noting `--package <dir>`, `--json`, and that it is read-only (never writes exceptions.json).
- **reviewer/tests/unit/test_cli.py:957** - The read-only guarantee is asserted only for `exceptions.json`. The item's sentence is broader ("it must stay read-only"); `interference_case` also calls `load_package(directory)`, and nothing pins that the command leaves the rest of the package directory (package.json, case.json, answer key) untouched.
  - Fix: In test_check_interference_leaves_the_exceptions_file_byte_for_byte, snapshot every file under the copied directory (path -> sha256 or read_bytes) before the run and compare the whole mapping afterwards, instead of one file.
- **reviewer/src/swreview/cli.py:613** - No test covers the empty/boundary package: a package with zero interferences and no exceptions.json beside it. I confirmed the code path is safe (ExceptionStore.load() treats an absent file as "none yet", reviewer/src/swreview/exceptions.py:221-229, and the text builder degrades to "0 interference condition(s)" / "0 exception(s) after refresh" / "unresolved coverage: 0 item(s)"), but Principle III asks for boundary-condition tests and this is the shape every freshly dumped package will have on test day -- the very scenario the item's "why" is written for.
  - Fix: Add one test over a minimal package (e.g. the existing joint_package fixture directory, which has no exceptions.json) asserting exit 0 and the three zero-count lines.
- **reviewer/src/swreview/cli.py:627** - Undisclosed deviation from the item text. The item says to use "_finding_lines per group (reaching group['result']['finding'])"; the code passes `group["result"]` directly. The code is right and the item text is wrong - `check_interference_group` returns a `CheckResult` (src/swreview/checks/result.py:50), which IS the finding-shaped record, so `group['result']['finding']` would KeyError exactly the way the item warned `_emit_check` would. But the implementer's report says "Deviations from the item text: none in behavior" and lists only two unrelated notes, so the next reader has no record that the plan's parenthetical is wrong.
  - Fix: Record the deviation in the handoff: "item says group['result']['finding']; CheckResult is itself the finding record, so _finding_lines(group['result']) is the correct call and the item's parenthetical is an error." No code change.
- **reviewer/tests/unit/test_cli.py:883** - DRY: INTERFERENCE_FIXTURE duplicates, character for character, the path expression already defined as FIXTURE_DIR at tests/unit/test_checks_interference.py:38-40 (`Path(__file__).resolve().parents[1] / "golden" / "fixtures" / "bracket-assy-interference"`). Constitution Principle V: "repeated logic across checks, parsers, or scripts MUST be extracted once it appears twice with the same intent", and CLAUDE.md says to flag repetition aggressively. test_cli.py already imports fixtures across test modules (lines 32-33, `from tests.unit.test_tools_checks_fastener import joint_package`), so the convention for sharing exists. I left this in should_fix rather than must_fix because a path constant is data, not logic, and the duplicate is inert.
  - Fix: `from tests.unit.test_checks_interference import FIXTURE_DIR as INTERFERENCE_FIXTURE` and keep the explanatory docstring as a comment, or drop the local constant entirely.
- **reviewer/tests/unit/test_cli.py:897** - test_check_interference_json_is_the_library_case asserts `body == interference_case(interference_package_dir)` against a command whose body is one call to interference_case - a delegation identity that cannot fail while the wrapper compiles, and that pins none of the numbers the item's "why" cares about ("the unverified interference volume unit"). It is what the verification field asked for, and the content is genuinely pinned elsewhere (tests/golden/test_golden/bracket-assy-interference.yml lines 49-53 carry the mm3 volumes through the inline-kwargs branch, with the directory/inline equivalence proved by test_checks_interference.py:414-431), but nothing in this module says so, so a future reader may mistake it for content coverage.
  - Fix: Either add the cross-reference to the test's docstring ("content is pinned by tests/golden/test_golden/bracket-assy-interference.yml; this asserts only that the CLI emits the library case verbatim"), or add one substantive assertion here, e.g. `assert "largest overlap volume 4.1 mm3" in body["groups"][0]["result"]["observed"]`, so a unit regression fails at the CLI too.
- **reviewer/tests/unit/test_cli.py:1005** - No coverage of the empty case: a package with zero interferences (every dump before the console `interference` command has merged rows) prints the bare header `0 interference condition(s)` / `0 exception(s) after refresh` / `unresolved coverage: 0 item(s)`. That is the correct, loud output, but it is the state most real invocations will hit first and no test fixes it.
  - Fix: Reuse an existing zero-interference package (e.g. the joint_package_dir fixture already in this module) and assert exit 0 plus the three zero-count lines.
- **reviewer/src/swreview/cli.py:631** - Cosmetic inconsistency in the text block separation: a blank line is emitted before each group (line 620) and before the exceptions header (line 628), but not before `unresolved coverage:`, so the unresolved-coverage block runs straight on from the exception list. Principle I makes unresolved coverage the thing that must stay visible; it is the one block with no visual break.
  - Fix: `lines.append("")` before the `unresolved coverage:` line, and extend test_check_interference_human_output_lists_exceptions_and_unresolved_coverage if you want the separation asserted.
- **reviewer/tests/unit/test_cli.py:883** - Stale gate line in the handoff report, not in the code: the report quotes `Passed! - Failed: 0, Passed: 419` for `dotnet test extractor/SwReview.sln -c Release`. On the tree as it stands now that command reports `Failed: 0, Passed: 495, Skipped: 0, Total: 495` (other items in the shared tree added C# tests). Still green, and this item touches no C#, but the quoted line does not describe the tree it was reported against.
  - Fix: Re-quote the C# gate from a run on the current tree, or state that the C# gates were run before the other items landed.

### 1-attach-only (8)

- **extractor/SwReview.Extractor.Console/Program.cs:68** - The started-session log line asserts '--allow-start was given and nothing was running.' The second clause is not established: `started` only means `Marshal.GetActiveObject` threw, which the whole item argues does NOT mean nothing was running. With `--allow-start` set, an elevation mismatch now starts a second seat AND writes a false cause into extract.log - reintroducing, one layer up, the exact mis-statement the item was raised to delete from `TryGetRunning`'s comment. The `DescribeAttachFailure()` machinery that knows the truth is not consulted on this path.
  - Fix: Reword to what is known, e.g. 'Started a new SOLIDWORKS session: --allow-start was given and no instance was visible to COM from this process.' Better: on the allowStart path, log `DescribeAttachFailure()` as a warning line before creating the seat, so an engineer who finds an unexpected second seat in extract.log can see the elevated one it failed to attach to. No new test needed beyond an assertion on the wording if one is added.
- **extractor/SwReview.Extractor.Tests/CommandLineOptionsTests.cs:305** - `AllowStart_IsAcceptedByEveryCommandThatAttaches` cannot fail for any change to Program.cs. Its comment claims 'Program.cs calls SwAttach.Connect from all five commands, so all five must accept the flag; an option missing from one command's list is a usage error at run time', but the test builds its own local `InterferenceOptions`/`CaptureOptions`/`ServeOptions` copies and feeds them to `CommandLine.Parse` - it never touches `Program.KnownOptions` or `Program.*OptionNames`. Deleting `KnownOptions(...)` from one command leaves the suite green. It also covers only 3 of the 5 commands: `resolve` is absent entirely (no `ResolveOptions` array exists) and `dump` is only covered incidentally by the neighbouring facts. Separately, the four test-local arrays are now verbatim copies of Program.cs's five arrays plus 'allow-start' - Principle V DRY: the five-way repetition the implementer's deviation #2 claims to have removed has moved into the test file.
  - Fix: Make `Program.KnownOptions` (or the five `*OptionNames` fields) `internal` and add `[assembly: InternalsVisibleTo("SwReview.Extractor.Tests")]` to the Console project, then drive the theory from the real arrays: `[InlineData("dump")] [InlineData("resolve")] [InlineData("interference")] [InlineData("capture")] [InlineData("serve")]`, parsing with `Program.KnownOptions(<the real array>)`. That deletes the four duplicated arrays and makes the test able to fail. If exposing internals is judged too invasive, at minimum add `ResolveOptions`, include `dump` and `resolve` in the theory, and rewrite the comment so it does not claim wiring coverage it does not have.
- **extractor/SwReview.Extractor/Sw/SwSession.cs:169** - `FileLoadErrors.Describe` is wired only into the `document == null` failure message. OpenDoc6 sets the `warnings` out-param almost exclusively on a SUCCESSFUL open, so the entire `DescribeWarnings` half of the new, well-tested decoder is dead in production: a document that opens carrying `swFileLoadWarning_MissingExternalReferences` or `swFileLoadWarning_ModelOutOfDate` means the dump describes an incomplete or stale model, and that fact is currently dropped with no log line and no gap. Principle I: 'Unsupported, missing, truncated, and failed checks MUST remain visible in the report as unresolved coverage, never silently dropped.' The implementer flagged this honestly as deviation #4 and out of scope, which I agree with for THIS item - the item's 'printing' target was the one failure message.
  - Fix: Do not widen this item. Raise a follow-up item: surface a non-zero `warnings` from a successful `OpenDoc6` as a `GapCollector` gap (the decode function it needs already exists and is fully tested), which needs a gap sink plumbed into `SwSession.Attach`/`OpenReadOnly`.
- **extractor/SwReview.Extractor/Sw/SwSession.cs:163** - The decode is applied only when OpenDoc6 returns null. On a SUCCESSFUL open the warnings int is read into a local and then discarded: a document that opens carrying swFileLoadWarning_ModelOutOfDate (0x2000) or swFileLoadWarning_MissingExternalReferences (0x100000) is dumped with no record anywhere that the IR describes a stale or incomplete model. Principle I: "Unsupported, missing, truncated, and failed checks MUST remain visible in the report as unresolved coverage, never silently dropped." The item says "Decode OpenDoc6's error/warning ints" without restricting to the failure branch. The implementer flagged this honestly as a follow-up; the loss is pre-existing rather than introduced here, which is why this is should_fix and not must_fix.
  - Fix: At minimum, when warnings != 0 on a successful open, surface FileLoadErrors.DescribeWarnings(warnings) rather than dropping it - e.g. expose it on SwSession as a read-only property the console host logs, so the wording stays testable and no new sink is invented. A gap entry is the right long-term home but GapCollector.cs belongs to another in-flight item, so keep it out of this change.
- **extractor/SwReview.Extractor.Console/AttachFailure.cs:116** - The unreadable branch states an inference as fact, and can state a wrong one. It is reached whenever EITHER the session id or the integrity level is null, but the text says "cannot read its Windows session or token, which itself means it runs elevated or as another user." For SwProcessFacts(pid, sessionId: null, integrityLevel: "medium") - covered by Describe_SessionUnreadable_IsTreatedAsUnreadableNotAsAMatch at AttachFailureTests.cs:104 - the token WAS readable and read the same integrity level as ours, so "runs elevated" is contradicted by a fact already in hand. Principle I forbids inferring a favourable/definite value from a missing input. The existing test only asserts Contains("cannot read"), so it passes on the wrong sentence.
  - Fix: Split the branch: name only the fact that was actually unreadable, and assert elevation only when IntegrityLevel is the null one ("this process cannot open its token, which happens exactly when it runs elevated or as another user"); for an unreadable session id with a matching integrity level, say the session could not be read and stop. Strengthen the two tests to assert the specific clause rather than the shared substring.
- **extractor/SwReview.Extractor.Console/SwAttach.cs:106** - Process.GetProcessesByName can itself throw (Win32Exception/InvalidOperationException on a machine with broken performance counters or a restricted token), and Process.GetCurrentProcess() at line 101 is never disposed. DescribeAttachFailure has no guard, so a secondary failure while GATHERING the diagnosis replaces the item's whole deliverable - the cause sentence - with an unrelated exception message on the one path that matters most. Every other read in this file is individually guarded (SessionIdOf, IntegrityLevelOf), so the omission is inconsistent with its own style.
  - Fix: Wrap the enumeration in try/catch for Win32Exception and InvalidOperationException and fall back to AttachFailure.Describe with an empty process list plus a note that the process list could not be read, so the engineer always gets a message naming the ProgID and --allow-start. Put `self` in a using. Add an AttachFailureTests case for the empty-list-because-unreadable wording.
- **extractor/SwReview.Extractor/Sw/SwSession.cs:144** - The item dictates this message's wording verbatim ('not open in SOLIDWORKS and not found on disk from this process') and the item's verification prescribes the method: "Extract the pure pieces and test those." The FileLoadErrors decode next to it was extracted and has 20 tests; this string was left inline in a class the implementer correctly notes no test touches, so the one piece of wording the item specifies letter-for-letter is verified by reading only. It is a pure function of a path.
  - Fix: Move it to a one-line pure helper beside FileLoadErrors (e.g. FileLoadErrors.DescribeMissingDocument(string path)) and add two tests: one asserting the mandated clause appears verbatim, one asserting the mapped-drive/EPDM/UNC guidance is present. SwSession then calls the helper, and the contract sentence is regression-guarded at near-zero cost.
- **extractor/SwReview.Extractor.Tests/AttachFailureTests.cs:46** - Describe_NoSolidWorksProcess_SaysSoWithoutGuessing asserts DoesNotContain("session") and DoesNotContain("integrity") over the ENTIRE message, including the fixed prologue and the --allow-start advice that are not under test. It passes today only because that boilerplate happens to avoid both words; any later rewording of the --allow-start sentence that mentions a session breaks a test whose subject is the diagnosis clause. Separately, SwProcessFacts_KeepsWhatItWasGiven at line 152 asserts only that a three-property constructor assigns its three properties - no behaviour, no failure mode.
  - Fix: Have Diagnose's output be assertable on its own (make it internal + InternalsVisibleTo, or have Describe take the diagnosis as a seam) and scope the negative assertions to that clause. Delete SwProcessFacts_KeepsWhatItWasGiven - the constructor is already exercised by every other test in the file.

### 3-typename-census (9)

- **extractor/SwReview.Extractor/Dump/HoleDumper.cs:131** - All three sites record the pass only AFTER the walk loop completes (HoleDumper.cs:131, MateDumper.cs:70, ComponentTreeDumper.cs:383). The `gate.Call("GetTypeName2", ...)` and `gate.Call("GetNextFeature", ...)` calls inside the loops are not wrapped in `TryStep`, so a COM failure mid-walk propagates to `RunPhase`, which records a phase gap and discards every sighting collected so far. That is precisely the moment the engineer most needs to know which type names were reached before the tree read died — the census exists to make exactly this class of failure diagnosable (Principle I: unsupported coverage stays visible).
  - Fix: Wrap each walk in try/finally and call `AddPass` in the `finally`, so a partial pass is still censused. Add a unit test at the census level is not needed; a single comment plus the finally at all three sites is the whole change.
- **extractor/SwReview.Extractor.Tests/PackageWriterTests.cs:117** - `Build_FeatureTypesNoDumperRead_BecomeOneUnsupportedGapPerDocument` asserts `Assert.Single(package.Gaps, g => g.EntityKind == "feature")` but `FakeSources.SeeFeatureTypes` (PackageWriterTests.cs:433) is hard-wired to a single `HousingPath` pass, so the "per document" half of the name is never exercised. The emit loop in `PackageWriter.AddSkippedFeatureTypes` (PackageWriter.cs:289-300) — one `Add` per `DocumentTypeNames`, each with its own `scope.DocumentId(document.DocumentPath)` — is only covered for n=1. A bug that emitted the first document's id on every gap would pass every test in the suite.
  - Fix: Let `SeeFeatureTypes` take the document path (or add a second setter for the assembly document) and add a test asserting two gaps, each with the entity_id of its own document and only its own type names in the reason.
- **extractor/SwReview.Extractor/Dump/PackageWriter.cs:297** - The gap reason names the document with `Path.GetFileName(document.DocumentPath)` only. Two distinct files with the same name in different folders (routine in a vault with revised/copied parts) produce two gap lines that read identically in `swreview validate` output, distinguishable only by the opaque `doc:<12 hex>` entity_id. Principle VI requires a finding an engineer can reproduce; here they must cross-reference the hash against package.documents by hand.
  - Fix: Either include the full path in the reason, or keep the file name and append the parent folder (e.g. `bracket-assy\\housing.SLDPRT`). One-line change; the existing `Assert.Contains("housing.SLDPRT", ...)` assertions still hold.
- **extractor/SwReview.Extractor.Tests/GapCollectorTests.cs:229** - `TypeNames_IsOneCensusEveryHolderOfTheCollectorSharesAndStartsEmpty` claims in its name to prove that every holder of the collector shares one census, but the body constructs one `GapCollector` and calls it directly — nothing in it demonstrates sharing. The property it actually pins (starts empty; a censused name is not itself a gap) is real and worth keeping; the name over-claims and will mislead the next reader into thinking sharing is covered.
  - Fix: Rename to what it tests (e.g. `TypeNames_StartsEmptyAndACensusedNameIsNotItselfAGap`), or make it earn the name by passing the same collector to two holders and asserting both see the other's pass.
- **extractor/SwReview.Extractor/Dump/PackageWriter.cs:298** - The gap reason ends "SOLIDWORKS reported these GetTypeName2 names in the feature tree and no dumper reads them." - a present-tense causal claim about the code. It is false whenever the dump aborted: `aborted` is set by a `CircuitOpenError` in any earlier phase (PackageWriter.cs:140-180), the mate and hole phases are then skipped, and `AddSkippedFeatureTypes` still runs at line 188. The root assembly's pattern-walk pass has already censused `MateGroup` as unconsumed, so the gap will state that no dumper reads MateGroup (or HoleWzd, for a part root where the hole phase never ran). On test day that sentence points the engineer at a non-existent type-name constant bug - the precise misdiagnosis this item was written to prevent.
  - Fix: Either skip the census gap when the dump aborted (pass the `aborted` flag into `AddSkippedFeatureTypes`, and let the abort gap carry the coverage loss), or reword to the claim the data actually supports: "...and no dumper that ran in this dump read them." Add a PackageWriterTests case where a phase raises `CircuitOpenError` and assert whichever behaviour is chosen.
- **extractor/SwReview.Extractor.Tests/TypeNameCensusTests.cs:194** - `Unconsumed_IsStableAcrossCalls` asserts `census.Unconsumed().Single().Describe()` equals itself across two calls, with a census holding exactly one name and no count ties. `Unconsumed()` is a pure function over a `Dictionary` + `OrderByDescending/ThenBy`, so with one entry there is no ordering left to be unstable - the assertion cannot fail for any implementation, including a broken one. Tie-break determinism is already covered properly by `Unconsumed_OrdersByCountThenName` (line 179), so this test adds no signal.
  - Fix: Either delete it, or give it teeth: seed at least two names with equal counts plus a third, and assert the full ordered `Names.Select(n => n.TypeName)` sequence is identical across two separate `Unconsumed()` calls (and that a call does not mutate the census - e.g. calling it twice still yields the same counts).
- **extractor/SwReview.Extractor.Tests/PackageWriterTests.cs:118** - `Build_FeatureTypesNoDumperRead_BecomeOneUnsupportedGapPerDocument` names the per-document rule but only ever drives one document: `FakeSources.SeeFeatureTypes` appends to a single `_holePass` list that is always recorded against `HousingPath` (line 539). Every assertion in the file is `Assert.Single(package.Gaps, g => g.EntityKind == "feature")`, so the `foreach` in `AddSkippedFeatureTypes` and the per-document `scope.DocumentId(document.DocumentPath)` mapping are never exercised with more than one entry - an implementation that emitted one merged gap, or that reused the first document's id for every gap, would keep the suite green. The item's headline requirement is 'one gap per document'.
  - Fix: Let the fake record passes for two documents (e.g. `SeeFeatureTypes(path, ...)` recording `HousingPath` and `ScrewPath`), then assert two `entity_kind == "feature"` gaps, with distinct `EntityId`s matching each document's id and each reason naming its own file name and only its own type names.
- **extractor/SwReview.Extractor/Dump/TypeNameCensus.cs:91** - The comment at line 87 claims the census is "Keyed the way DocumentIds hashes a path", but `StringComparer.OrdinalIgnoreCase` only folds case, while `DocumentIds.Hash` also trims and replaces '/' with '\\' before lowering. Two spellings of one file that differ by separator or surrounding whitespace therefore become two `DocumentEntry` rows but one document id, and `AddSkippedFeatureTypes` emits two gaps carrying the *same* `entity_id` with split counts. `AddPass_TheSameFileSpeltDifferently_IsOneDocument` only tests the casing half, so the comment's claim is asserted only where it happens to hold.
  - Fix: Either normalize the key exactly as DocumentIds does (`path.Trim().Replace('/', '\\')` before the ignore-case lookup, keeping the first-seen spelling for display) and extend the test with a '/'-separated and a whitespace-padded spelling, or narrow the comment to what the code does ('case-insensitive, matching how SOLIDWORKS varies case') so it stops asserting agreement it does not have.
- **extractor/SwReview.Extractor/Dump/PackageWriter.cs:293** - The gap pairs `entity_kind = "feature"` with `entity_id = scope.DocumentId(...)`, i.e. a `doc:` id. `Gap.EntityKind` is documented as "The IR entity type the gap is about" (Ir/DrawingSheet.cs:90) and every other gap in the extractor either carries a null id or an id of the kind it names (`"component"` + `cmp:`, `"hole"` + `hol:`). On the Python side `tools/context.py:126 entity_kind("doc:...")` answers "document", directly contradicting the gap's own label, and `swreview validate` prints the pair verbatim as `unsupported feature doc:0a1b...`. The schema does not catch it (`entity_kind` is a free string), so nothing else will.
  - Fix: Use `entity_kind = "document"` - the reason text already says these are feature type names, so no information is lost - or keep "feature" and set `entity_id` to null. Update the `g.EntityKind == "feature"` filters in PackageWriterTests accordingly.

### 8-pattern-names (7)

- **extractor/SwReview.Extractor.Tests/PatternFeatureTypesTests.cs:36** - Command (1) omits `moLocalLPattern_c`, which the item's "what" text explicitly names as part of the evidence set: "string extraction from ... sldasmu.dll shows internal feature classes moLocalLPattern_c, moLocalChainPattern_c, moDerivedCirPattern_c and moMirrorCompFeat_c". The recorded array searches the other three plus the negatives, so the anchor name — the one already-trusted entry that validates the whole mo<X>_c -> "<X>" inference — is the one name the recorded command does not cover.
  - Fix: Add 'moLocalLPattern_c' to the name array in the command at line 36 and add its output row (I reproduced it: `moLocalLPattern_c ascii=3 utf16=0`).
- **extractor/SwReview.Extractor.Tests/PatternFeatureTypesTests.cs:60** - The strongest single piece of evidence in the file — that the LocalChainPattern hit sits in a contiguous UTF-16 run reproducing swFeatureNameID_e 103..113 in order — is asserted in prose only, with no recorded command that regenerates it. Every other claim in the block is backed by a pasteable command. I verified the claim is true (the run reads `CurvePattern SketchPattern APattern TablePattern DimPattern LocalLPattern LocalCirPattern LocalCurvePattern LocalSketchPattern LocalChainPattern GroundPlane`), but an auditor cannot re-derive it from what is checked in, which is the failure mode the item was trying to avoid by rejecting a hand-transcribed table.
  - Fix: Add the one-line command that prints the run, e.g. `powershell -NoProfile -Command "$b=[IO.File]::ReadAllBytes('...sldapiu.dll'); $u=[Text.Encoding]::Unicode.GetString($b); ([regex]::Match($u,'.{0,160}LocalChainPattern.{0,60}').Value -replace '[^\x20-\x7E]','|')"` above the quoted run.
- **extractor/SwReview.Extractor.Tests/PatternFeatureTypesTests.cs:128** - Reading the private `PatternFeatureTypes` field by reflection is a convention this test project does not otherwise use — no other source file under SwReview.Extractor.Tests/ references System.Reflection or BindingFlags — and it trades Principle V's "explicit beats clever" for keeping the production surface unchanged. It is also a weaker pin than it looks: it verifies the contents of a field, not that ReadPatternMembership consults that field, so a future change that inlines or bypasses the set leaves the test green. The tradeoff was disclosed and is defensible (widening to internal needs an InternalsVisibleTo in the csproj, which is outside this item's scope, and ComponentTreeDumper has no COM-free test seam today), so "do nothing" is a reasonable answer here.
  - Fix: Either accept as-is and add one sentence to the class doc noting that the pin covers the constant only, not its use at ReadPatternMembership; or, if the csproj is opened for another reason, make the field `internal static readonly`, add `[assembly: InternalsVisibleTo("SwReview.Extractor.Tests")]`, and drop the reflection helper.
- **extractor/SwReview.Extractor/Dump/ComponentTreeDumper.cs:57** - The TablePattern justification comment states the component patterns are "the Local* family 108-112 plus the Derived* pair and MirrorComponent 116". The file's own cited evidence refutes "the Derived* pair" under both readings: swFeatureNameID_e contains only swFmDerivedLPattern=28 (no swFmDerivedCirPattern at all, as the test file itself notes), and an exhaustive ASCII scan of sldasmu.dll for mo*(Pattern|Mirror)*_c returns moDerivedLPattern_c(2), moDerivedCirPattern_c(2), moDerivedCurvePattern_c(2), moDerivedSketchPattern_c(2), moDerivedTablePattern_c(2), moDerivedAPattern_c(2), moDerivedHolePattern_c(1) — a family of at least seven, not a pair. Since the set carries only DerivedLPattern/DerivedCirPattern, the pattern-driven ("derived") component patterns built from a part's curve, sketch or table pattern are plausibly the same silent-miss this item exists to fix, and the comment currently asserts they do not exist. This is the file's evidence trail, which the implementer's own deviation 1 argues must stay true.
  - Fix: Correct the sentence to what the evidence shows (e.g. "the Local* family 108-112, the Derived* family — of which only DerivedLPattern and DerivedCirPattern are in this set — and MirrorComponent 116"). Do NOT add the other Derived* names: the item's "what" authorizes exactly two additions. Report the gap to the orchestrator as a candidate follow-up item, and note in the comment that the rank 3 census is what would surface DerivedCurvePattern / DerivedSketchPattern / DerivedTablePattern on a real model.
- **extractor/SwReview.Extractor.Tests/PatternFeatureTypesTests.cs:61** - The provenance comment claims the contiguous UTF-16 run in sldapiu.dll "follows swFeatureNameID_e exactly - \"CurvePattern SketchPattern APattern TablePattern DimPattern ...\", i.e. ids 103..113 in order". The run reproduces verbatim, but it is contradicted by the enum the same file prints three paragraphs later: id 105 is swFmFillPattern, while the run has APattern. "Exactly" is refuted by the file's own two commands; a reader auditing the evidence hits the mismatch and loses confidence in the rest. (Checked further: the run also stops at GroundPlane 113 then continues SMGusset/Belt/StrctSys*, so it does not track the enum past 113 either — 114/115/116 are absent.)
  - Fix: Downgrade the claim to what is true: the run is positionally aligned with ids 103..113 except position 105, which the API table spells APattern where swFeatureNameID_e spells swFmFillPattern, and the run does not continue in enum order past GroundPlane 113. The load-bearing part of the evidence — LocalChainPattern sitting at the 112 position — survives that correction intact.
- **extractor/SwReview.Extractor.Tests/PatternFeatureTypesTests.cs:119** - Test name PatternFeatureTypes_IsExactlyTheSetTheProvenanceSupports overclaims relative to the file's own findings. The provenance does not support "ChainPatternFeat" at all — moChainPatternFeat_c ascii=0 in sldasmu.dll and 0 boundary hits in sldapiu.dll (I also confirmed 0 raw UTF-16 occurrences of the substring anywhere in sldapiu.dll) — and does not support "DerivedCirPattern" from the enum. Those names are kept deliberately, as the body text honestly explains, so the set is "provenance plus names kept because removing a possibly-live name regresses grouping", not "the set the provenance supports". Under Constitution I (evidence before conclusions) the assertion name is the first thing a reader takes as the claim.
  - Fix: Rename to something that states the actual guarantee, e.g. PatternFeatureTypes_MatchesThePinnedSet or PatternFeatureTypes_IsThePinnedSetWithRecordedProvenance, and leave the per-name reasoning where it already is.
- **extractor/SwReview.Extractor.Tests/PatternFeatureTypesTests.cs:36** - Provenance command (1) checks six class names and omits moLocalLPattern_c, which the item's "what" names as part of the citable extraction ("string extraction from ... sldasmu.dll shows internal feature classes moLocalLPattern_c, moLocalChainPattern_c, moDerivedCirPattern_c and moMirrorCompFeat_c"). The recorded evidence therefore does not cover the one name in the set that the pilot fixture will actually exercise, so the mo<X>_c -> "<X>" correspondence the comment relies on is never demonstrated for a name known to work.
  - Fix: Add 'moLocalLPattern_c' (and, cheaply, the rest of the Local* family) to the name list in command (1) and record the counts — moLocalLPattern_c is present with ascii=3, so the paste-and-compare stays green and the "Local* family" claim becomes self-verifying.

## Feature 002 spec package review


### spec, plan, contracts, tasks (44)

- **specs/002-task-pane-assistant/contracts/cli-profiles.md** - The generated Codex profile deliberately enables the CLI's shell tool. That hands the model generic command execution on the workstation (read-only filesystem, but arbitrary processes), which the constitution bans outright, and the plan's Constitution Check marks that same gate PASS with an empty Complexity Tracking table. Nothing in the feature discloses or justifies the exception.
  - Fix: see the finding id constitution:F2 in the review; the fix agent applied only must-fix and local should-fix items
- **specs/002-task-pane-assistant/contracts/cli-profiles.md** - The per-launch tool-service secret is written in cleartext into a file inside the run folder, and the process that can read that folder is exactly the sandboxed CLI the secret is meant to constrain. That contradicts the plan's own rule and defeats FR-019/FR-022: with the secret plus the pipe name (also in the same file, `--bridge-pipe <pipe>`), the general-chat model can talk to the in-process dispatcher directly and issue any command the dispatcher accepts — including `interference`, which mcp-toolset.md deliberately withholds from general chat.
  - Fix: see the finding id constitution:F3 in the review; the fix agent applied only must-fix and local should-fix items
- **specs/002-task-pane-assistant/contracts/pane-host-messages.md** - Saving settings restarts the backend process unconditionally, which kills any chat session it is hosting: the SSE stream drops, the in-flight review turn dies, and the session on disk is left without an ended time — exactly what FR-008 and US2 Acceptance 3 forbid. Nothing in the contract or tasks defines the interaction between a settings save and a running turn.
  - Fix: see the finding id constitution:F9 in the review; the fix agent applied only must-fix and local should-fix items
- **specs/002-task-pane-assistant/contracts/settings.schema.json** - The quickstart's primary US1 validation and T044 both require selecting the `fake` provider in the Settings UI and pressing Review, but the settings contract cannot store `fake`, and T026 tests the schema round trip. The demonstration path the plan relies on (deliver US2 and US1 'with the fake provider so the pane is demonstrable without keys') is therefore blocked by its own contract.
  - Fix: see the finding id constitution:F10 in the review; the fix agent applied only must-fix and local should-fix items
- **specs/002-task-pane-assistant/tasks.md** - The 16000 token ceiling is copied from an Anthropic-specific constraint that no longer applies, and no event, coverage bucket, or task covers the failure mode it creates. On a reasoning model, reasoning tokens count against `max_output_tokens`, so a turn can end with `status: incomplete` / `incomplete_details.reason = max_output_tokens` — a truncated answer that is not an exception and not a tool error. `turn.ended` has no reason for it (`end | max_steps | error | stopped`), so the runner would report a normal end and the report would show a review that simply stopped, violating Principle I's rule that truncated work stays visible.
  - Fix: see the finding id constitution:F12 in the review; the fix agent applied only must-fix and local should-fix items
- **specs/002-task-pane-assistant/data-model.md** - FR-014 requires effort to be mapped to each provider's own control and the mapping recorded, but no artifact states the mapping table, and the four inherited levels include `xhigh`, which research itself flags as model-dependent for OpenAI and which has no counterpart in Gemini's thinking level. T017/T019 assert only that 'effort mapping is recorded', so a mapping that silently collapses xhigh to high, or that sends a value the provider rejects mid-run, would pass the tests.
  - Fix: see the finding id constitution:F13 in the review; the fix agent applied only must-fix and local should-fix items
- **specs/002-task-pane-assistant/contracts/mcp-toolset.md** - The general-chat path gets a second implementation of the call choke point with nothing specifying that it reuses the first. RecordedTool is where argument validation, step recording, the `{"error": ...}`-to-error-result conversion, and the never-raise-past-the-runner rule all live; the MCP server needs the same four behaviours against a different sink. T059 simply says 'implement', so the two paths can drift — and a divergence here means a failed tool call in general chat is dropped rather than surfaced, which FR-022/SC-003 forbid.
  - Fix: see the finding id constitution:F14 in the review; the fix agent applied only must-fix and local should-fix items
- **specs/002-task-pane-assistant/contracts/settings.schema.json** - The settings schema forbids the `fake` provider, but quickstart Scenario 1 and task T044 require saving `fake` in Settings, and T026 validates the written settings file against this schema. The two cannot both hold.
  - Fix: see the finding id consistency:F02 in the review; the fix agent applied only must-fix and local should-fix items
- **specs/002-task-pane-assistant/contracts/chat-api.md** - The stop endpoint and its corresponding `turn.ended` reason have no producer: no FR requires a stop control, no page-host message carries one, and no task builds one.
  - Fix: see the finding id consistency:F08 in the review; the fix agent applied only must-fix and local should-fix items
- **specs/002-task-pane-assistant/data-model.md** - The data-model event table disagrees with the JSON Schema it describes, so whichever an implementer reads first produces events the other rejects.
  - Fix: see the finding id consistency:F09 in the review; the fix agent applied only must-fix and local should-fix items
- **specs/002-task-pane-assistant/contracts/cli-profiles.md** - The generated Codex profile hands the general-chat model a shell, which the constitution's technical constraints prohibit, while the plan's Constitution Check records that gate as PASS with an empty Complexity Tracking table. The Gemini profile denies shell, so the two modes also enforce different boundaries for the same FR.
  - Fix: see the finding id consistency:F11 in the review; the fix agent applied only must-fix and local should-fix items
- **specs/002-task-pane-assistant/tasks.md** - The terminal's working directory is "the run folder", but the only task that creates a run folder is part of the review flow, so a Terminal-first session has no defined working directory and no task defines how one is chosen or created.
  - Fix: see the finding id consistency:F13 in the review; the fix agent applied only must-fix and local should-fix items
- **specs/002-task-pane-assistant/spec.md** - An acceptance scenario requires re-evaluating findings after an evidence answer, and no task implements or tests that behaviour.
  - Fix: see the finding id consistency:F16 in the review; the fix agent applied only must-fix and local should-fix items
- **specs/002-task-pane-assistant/spec.md** - Two named edge cases have no implementing task and, for the retry case, no data field to carry what the spec promises.
  - Fix: see the finding id consistency:F17 in the review; the fix agent applied only must-fix and local should-fix items
- **specs/002-task-pane-assistant/spec.md** - Five of the nine measurable outcomes have no task that measures them, and SC-004 names a "guard log" artifact that no contract, data-model entry, or task defines.
  - Fix: see the finding id consistency:F18 in the review; the fix agent applied only must-fix and local should-fix items
- **specs/002-task-pane-assistant/contracts/cli-profiles.md** - The per-launch bridge secret and the bridge pipe name are written in cleartext into files under `<run_dir>/.swreview-cli/`, which is the sandboxed CLI's own working directory and is readable by it. The restriction profile grants the CLI read access to the run folder on purpose (Codex `[features] shell_tool = true` "for reading the run folder"; the Gemini policy allows `read_file` and `list_directory` at priority 50). The general-chat model can therefore read the credential that authenticates to the in-process SOLIDWORKS dispatcher. Because the dispatcher's vocabulary is `ping|capture|measure|interference` and authorization is a single shared secret, anything holding that secret can issue `interference` — the exact command mcp-toolset.md says is excluded "by construction" (FR-025, FR-022). The exclusion lives only in the MCP server layer, not at the trust boundary. `chat-api.md` line 16-17 compounds it: `POST /sessions` carries `bridge: {pipe, secret}` and `GET /sessions/{chat_id}` is specified to withhold only `token`, so the bridge secret is echoed back to the WebView2 page as part of session state.
  - Fix: see the finding id security:SEC-001 in the review; the fix agent applied only must-fix and local should-fix items
- **specs/002-task-pane-assistant/contracts/cli-profiles.md** - The Gemini restriction rests on an environment variable the contract itself will not name ("or the documented equivalent for a settings directory override"), and nothing in the task list proves the restriction is actually in force. If the override name is wrong, Gemini loads the engineer's `~/.gemini/settings.json` and `~/.gemini/policies/`, our deny-all policy is absent, and the CLI runs with its full core toolset (`write_file`, `run_shell_command`, `web_fetch`) against the customer's machine — a silent fail-open with no detection. The specified first-launch verification only compares the *MCP tool listing* to the allowlist, so it detects a missing `swreview` server but not the presence of unrestricted core tools. Across both CLIs, the sole check that a write is refused is a manual quickstart sentence; there is no automated or scripted negative test anywhere in tasks.md.
  - Fix: see the finding id security:SEC-004 in the review; the fix agent applied only must-fix and local should-fix items
- **specs/002-task-pane-assistant/contracts/chat-api.md** - The SSE contract cannot be implemented by the page as specified, and the obvious workaround leaks the token. The browser `EventSource` API cannot set an `Authorization` header and cannot set `Last-Event-ID` (the browser sends it only on its own automatic reconnect), yet chat-api.md requires the header on every request and T041 requires "SSE client with `Last-Event-ID` reconnect". The implementer's shortest path is `?token=...` in the URL, which puts the per-launch bearer token into the server's access log, WebView2 history and any crash dump. Separately, the page is served from a virtual host name while the backend is `http://127.0.0.1:<port>` — a different origin — and no CORS/`Origin` policy is defined anywhere in the package, so cross-origin `fetch`/`EventSource` behaviour is unspecified; a permissive `Access-Control-Allow-Origin: *` added later would let any page in any browser on the workstation probe the port.
  - Fix: see the finding id security:SEC-005 in the review; the fix agent applied only must-fix and local should-fix items
- **specs/002-task-pane-assistant/tasks.md** - The automated scan that is supposed to prove SC-006 ("API keys never appear in any file under the run folder or the log folder … an automated scan confirms it") reads the keys to search for from its own environment, but on the workstation the key lives DPAPI-protected in `%APPDATA%\SwReview\settings.json` and reaches only the backend child process's environment. When the engineer runs `uv run swreview audit-secrets <run> <logs>` from a separate shell, that shell has no key, so the scan has nothing to look for and reports "none" regardless of what is in the files. The check passes vacuously exactly in the scenario it exists to cover.
  - Fix: see the finding id security:SEC-006 in the review; the fix agent applied only must-fix and local should-fix items
- **specs/002-task-pane-assistant/spec.md** - SC-004 makes "zero mutating SOLIDWORKS calls … verified by the guard log" the acceptance evidence for the read-only boundary, but no guard log exists and no task creates one. `SwGate.Call` performs `ReadOnlyGuard.Assert` and then executes; it writes nothing anywhere. T047 adds a tool-service request log (per-request, at the dispatcher level), which records commands, not the interop members underneath them — so it cannot distinguish an allowed getter from a refused writer, and cannot substantiate a 30-minute general-chat claim. The claim is currently unverifiable. Compounding it, `ReadOnlyGuard` is a denylist of ten member names plus four prefixes, documented as needing manual extension "the moment a phase touches an API family that can write", and feature 002 adds no task to re-review it even though it adds two new untrusted callers driving a live `ISldWorks` in-process.
  - Fix: see the finding id security:SEC-007 in the review; the fix agent applied only must-fix and local should-fix items
- **specs/002-task-pane-assistant/contracts/README.md** - Two gaps in the tool-service transport's authentication. (a) The `secret` is optional in the protocol and the version stays 1.0, while the console host is specified to ignore it — so a client has no way to tell whether the host it is talking to enforces authentication, and the README's rationale is stated backwards ("clients that omit the field simply fail on the in-process host and still work against the console host" describes the harmless direction; the risky direction is a client that believes it is authenticated talking to a host that never checks). `ping` reports `protocol: "1.0"` either way. (b) No access control is specified for the named pipe itself. T047 creates `swreview-<guid>` with no `PipeSecurity`; .NET Framework 4.8 has no `PipeOptions.CurrentUserOnly`, so the pipe is created with the process token's default DACL and nothing in the design states who may connect, states that a server-side identity check (`NamedPipeServerStream.GetImpersonationUserName`) is performed, or prevents another process from creating the same pipe name first and harvesting the secret a client sends it.
  - Fix: see the finding id security:SEC-009 in the review; the fix agent applied only must-fix and local should-fix items
- **specs/002-task-pane-assistant/contracts/chat-api.md** - `run_dir` is accepted as a caller-supplied path at three boundaries with no constraint that it lie under the configured `run_root` and no normalization against traversal or UNC paths. `POST /sessions` takes any `run_dir` and the server then writes `session.json`, `report.md` and `events.jsonl` into it — anyone who reaches the loopback port with the token (including an injected page script, SEC-002) can point a session at an arbitrary directory. `folder.open` and `report.open` take a payload-supplied path/id and ShellExecute it, so a page message can launch an arbitrary file or directory (including a UNC path) with the engineer's credentials; `report.open` is specified as "Open `report.md` in the default app", which is a shell-association launch of a path the caller chose.
  - Fix: see the finding id security:SEC-010 in the review; the fix agent applied only must-fix and local should-fix items
- **specs/002-task-pane-assistant/contracts/chat-api.md** - With the chosen stack (`starlette` + `sse-starlette` + `uvicorn`, plan.md Primary Dependencies) the contract is violated by default and the port is not discoverable. uvicorn's default logging configuration sends the access logger to stdout, so access log lines will interleave with the handshake line the add-in parses. Separately, `--port 0` binds an ephemeral port whose number is only knowable after startup from the bound socket; `uvicorn.run(port=0)` gives the caller no way to print it, and uvicorn's `fd=` option is not usable on Windows.
  - Fix: see the finding id feasibility:F05 in the review; the fix agent applied only must-fix and local should-fix items
- **specs/002-task-pane-assistant/contracts/cli-profiles.md** - The Gemini half of the contract asserts six configuration facts that research never established, and the contract itself hedges on the most load-bearing one. `GEMINI_CLI_HOME` as a settings-directory override, `mcp.allowed`, `security.disableYoloMode`, `general.defaultApprovalMode`, `context.fileName`, the per-server `trust` key, and a `toolName = "*"` wildcard deny rule appear nowhere in research R4. If the home override is wrong, the generated files are never read and Gemini runs under the engineer's own global settings — the exact failure FR-021 exists to prevent, and it would fail open (unrestricted), not closed.
  - Fix: see the finding id feasibility:F08 in the review; the fix agent applied only must-fix and local should-fix items
- **specs/002-task-pane-assistant/tasks.md** - The locator is specified to resolve npm shims, but the launch path has no provision for executing one. A ConPTY child must be started with `CreateProcess` plus `PROC_THREAD_ATTRIBUTE_PSEUDOCONSOLE` (so `System.Diagnostics.Process` cannot be used), and `CreateProcess` cannot execute a `.cmd`/`.bat` at all — it requires `cmd.exe /c "<shim>"` — while a `.ps1` requires `powershell.exe -File`. Consequently the Gemini CLI (`gemini.cmd`) would fail to start, and T050's job-object assignment has to be done by hand against the raw process handle rather than through the managed Process class.
  - Fix: see the finding id feasibility:F11 in the review; the fix agent applied only must-fix and local should-fix items
- **specs/002-task-pane-assistant/research.md** - The marshalling design has no timeout and no story for a blocked application thread. The pipe reader thread hands each request to the SOLIDWORKS thread and must wait for the result, but that thread is routinely blocked by modal dialogs, rebuilds, and long API calls. With no bound on the wait, a single modal dialog hangs every review tool call and every general-chat tool call indefinitely, and the Python client's own timeout fires while the C# side still holds the request — producing exactly the kind of unaccounted-for state Principle I forbids. `Control.BeginInvoke` also throws if the control's handle is not created or has been destroyed during unload.
  - Fix: see the finding id feasibility:F15 in the review; the fix agent applied only must-fix and local should-fix items
- **specs/002-task-pane-assistant/contracts/pane-host-messages.md** - Every chunk of pseudo-console output becomes one `PostWebMessageAsJson` call, and those calls must be made on the SOLIDWORKS application thread. A CLI that repaints a spinner or streams a long answer produces hundreds of small writes per second; each one posts a window message into SOLIDWORKS' pump and allocates a base64 string. Nothing in the contract or the tasks bounds the rate, so the realistic outcome is a visibly sluggish SOLIDWORKS UI during general chat — and the 50 ms keystroke-echo goal has no headroom left.
  - Fix: see the finding id feasibility:F16 in the review; the fix agent applied only must-fix and local should-fix items
- **specs/002-task-pane-assistant/data-model.md** - The data model and the event schema disagree on two event bodies, and T024 asserts that every event the runner emits validates against the schema — so an implementation written from the table will fail the test it is paired with. `session.started` is documented with four fields but the schema requires a fifth, `effort_mapping`, and lists it in `required` with `additionalProperties: false`. `turn.ended` is documented with three reasons but the schema allows four; the missing one, `stopped`, is the reason the `POST /sessions/{chat_id}/stop` route must produce.
  - Fix: see the finding id feasibility:F17 in the review; the fix agent applied only must-fix and local should-fix items
- **specs/002-task-pane-assistant/tasks.md** - No task removes the hard-coded Claude model id from product code, and eight feature-001 tests assert that id. FR-026 ("No Claude or Anthropic path MAY exist in product code, configuration, or documentation") cannot be satisfied by the task list as written, and the assertions will fail once the default becomes `gpt-5.6`.
  - Fix: see the finding id tests:F2 in the review; the fix agent applied only must-fix and local should-fix items
- **specs/002-task-pane-assistant/plan.md** - The stated mitigation for the top provider risk is unverifiable by the test it names. A `respx`-recorded exchange replays a response we author ourselves, so it can never show that OpenAI's strict-schema validator accepts the generated `SourceRef`/nullable schemas — the exact thing research flags as unverified. Every test in T017 will pass while the real call 400s.
  - Fix: see the finding id tests:F3 in the review; the fix agent applied only must-fix and local should-fix items
- **specs/002-task-pane-assistant/tasks.md** - T038 implements the process contract the whole pane depends on — first stdout line is exactly `{port, token}`, nothing else on stdout, bind 127.0.0.1 only, `--fail-bridge N` — with no preceding test task. T036 uses Starlette's in-process test client, which never exercises `__main__`, and the only test of the handshake (T033) runs against a *stub script* that prints the line by construction, so both sides are tested against assumptions and neither against the real program.
  - Fix: see the finding id tests:F4 in the review; the fix agent applied only must-fix and local should-fix items
- **specs/002-task-pane-assistant/data-model.md** - The two normative descriptions of the event stream disagree, so T024 ("every event the runner and tools can emit validates against `contracts/chat-events.schema.json`") and T035/T014 cannot both be satisfied: a `session.started` event built from data-model.md is invalid under the schema, and a `/stop` producing `turn.ended` is valid under the schema but not under data-model.md.
  - Fix: see the finding id tests:F5 in the review; the fix agent applied only must-fix and local should-fix items
- **specs/002-task-pane-assistant/tasks.md** - Four test tasks are ordered after the code they cover, and one behavior has no test at all, which breaks the file's own rule ("Test tasks precede implementation in every phase") and constitution Principle III. Written after the fact these tests are calibrated to the implementation's actual output rather than to the contract, which is exactly how a schema or message mismatch survives.
  - Fix: see the finding id tests:F8 in the review; the fix agent applied only must-fix and local should-fix items
- **specs/002-task-pane-assistant/tasks.md** - The only tests for either WebView2 page are string scans of the source file, which is close to tautological: a scan passes if a message name appears in a comment and cannot fail for a page that sends nothing, sends the right name with the wrong payload, or mishandles a reply. Meanwhile the page behaviors with real failure modes — `Last-Event-ID` reconnect, the 500-event memory cap, input disabled while a turn runs, error-card actions — have no test at all, so spec Edge Case "Very long sessions" and FR-008's "next action" are unverified.
  - Fix: see the finding id tests:F9 in the review; the fix agent applied only must-fix and local should-fix items
- **specs/002-task-pane-assistant/spec.md** - Four named failure modes have no test task anywhere in tasks.md, so the spec's Edge Cases section is not a verified contract: (a) the document closed mid-review, (b) a tool call naming a tool that does not exist, (c) a model id the provider no longer offers, (d) a persistent reference that no longer resolves offering the component's full path.
  - Fix: see the finding id tests:F10 in the review; the fix agent applied only must-fix and local should-fix items
- **specs/002-task-pane-assistant/spec.md** - Nothing in the data model, the contracts, or tasks.md can express the link this edge case requires, so the task list cannot be executed as written. `ReviewSession` gains only `provider_info` (T016), no chat-api endpoint takes a predecessor id, and no test covers the retry path or the requirement that the aborted session still carries an ended time.
  - Fix: see the finding id tests:F11 in the review; the fix agent applied only must-fix and local should-fix items
- **specs/002-task-pane-assistant/spec.md** - The WebView2-missing fallback has neither an implementation task nor a test, yet it is the failure mode that takes down both tabs at once. T043 builds the tab host assuming WebView2 initializes; nothing shows the download link or the plain-text run-folder fallback the spec promises.
  - Fix: see the finding id tests:F12 in the review; the fix agent applied only must-fix and local should-fix items
- **specs/002-task-pane-assistant/data-model.md** - The ChatSession state machine is incomplete: `ended` is terminal, but FR-006 and US1 Acceptance Scenario 5 require a follow-up message on a *completed* session to run in that same session. There is also no state or transition for `POST /stop` or for the failure-then-Retry path. T035 tests "transitions from data-model section 3", so the incomplete machine would be locked in by test and follow-ups would be rejected.
  - Fix: see the finding id spec-quality:F2 in the review; the fix agent applied only must-fix and local should-fix items
- **specs/002-task-pane-assistant/quickstart.md** - Every workstation scenario and SC-007 are anchored on "the bracket fixture from feature 001", which does not exist in the repository and is not created by any task. `benchmarks/native/` is empty and `benchmarks/packages/` contains only `cover-blind-tap`. Nothing is gitignored, so this is genuinely absent, and the three SOLIDWORKS tasks (T044, T051, T063) cannot be run as written.
  - Fix: see the finding id spec-quality:F5 in the review; the fix agent applied only must-fix and local should-fix items
- **specs/002-task-pane-assistant/spec.md** - SC-004 promises "Zero mutating SOLIDWORKS calls ... verified by the guard log", but no guard log exists anywhere in the codebase or in feature 001, and no task in tasks.md creates or asserts one. Worse, the existing guard is a denylist that throws rather than records, so even if a log were added it could only prove that none of ten named members were called — not that zero mutating calls occurred. The success criterion is unverifiable as stated.
  - Fix: see the finding id spec-quality:F6 in the review; the fix agent applied only must-fix and local should-fix items
- **specs/002-task-pane-assistant/data-model.md** - The event catalogue is stated three times and the three copies disagree. data-model §2 says `session.started` carries `session_id, package_id, provider, model` while the schema *requires* `effort_mapping` as well; data-model says `turn.ended` reason is `end | max_steps | error` while the schema enum also contains `stopped` (which `POST /stop` produces). FR-013 lists only 8 of the 13 event types. An implementer following data-model.md will emit events that fail the schema T024 validates against.
  - Fix: see the finding id spec-quality:F8 in the review; the fix agent applied only must-fix and local should-fix items
- **specs/002-task-pane-assistant/contracts/cli-profiles.md** - The bridge secret — the sole authentication on the in-process tool service — is written in cleartext into `<run_dir>/.swreview-cli/codex.toml` and `<run_dir>/.swreview-cli/gemini/settings.json`, inside the very directory the sandboxed CLI is launched in and explicitly permitted to read. The general-chat model can read its own profile, recover the secret and the pipe name, and address the tool service outside the MCP allowlist, which defeats FR-022's "MUST record every call in a chat log" and SC-003's "100% of tool calls are recorded". The contract's own claim that the secret is "never on the command line" understates the exposure rather than removing it.
  - Fix: see the finding id spec-quality:F9 in the review; the fix agent applied only must-fix and local should-fix items
- **specs/002-task-pane-assistant/spec.md** - The Retry-after-provider-failure behaviour — "starts a new session that references the previous one" — has no functional requirement, no field anywhere in data-model.md or the contracts to hold the reference, and no endpoint or task that implements it. T041 renders a Retry button on error cards with nothing behind it.
  - Fix: see the finding id spec-quality:F11 in the review; the fix agent applied only must-fix and local should-fix items
- **specs/002-task-pane-assistant/spec.md** - Three of the nine success criteria are timing thresholds, and nothing in tasks.md or quickstart.md measures any of them. The quickstart scenarios list expected behaviours but no stopwatch, no instrumentation, and no recorded number; the workstation tasks (T044, T051, T063) say only "record results". These criteria cannot be shown met or unmet at the end of the feature.
  - Fix: see the finding id spec-quality:F14 in the review; the fix agent applied only must-fix and local should-fix items

## Feature 002 Phases 1-2 (provider layer)


### schema-protocol (16)

- **reviewer/tests/unit/test_provider_schema.py:96** - `enumerate_tools()` wraps `from swreview.tools import registry` in `except ModuleNotFoundError` and falls back to `FALLBACK_TOOL_FUNCTIONS` / `FALLBACK_BRIDGE_TOOL_FUNCTIONS` (lines 55-91), a hand-maintained second copy of the registration list. T013 has landed, `registry.py` imports cleanly and the fallback is now dead code - but it is a live silent-failure trap: any future `ModuleNotFoundError` anywhere in registry's import graph (a renamed module, a dropped dependency) makes 29 parametrized tests silently re-target a stale hardcoded list and still report green. It is also the DRY violation the constitution's Principle V calls out - the tool list now exists in two places.
  - Fix: Import `swreview.tools.registry` unconditionally at module top and delete `enumerate_tools`, `FALLBACK_TOOL_FUNCTIONS` and `FALLBACK_BRIDGE_TOOL_FUNCTIONS`. The agent-tools.md golden (line 201) already keeps the registry honest against the contract.
- **reviewer/tests/unit/test_provider_schema.py:370** - `test_strictify_makes_a_nested_optional_field_nullable_too` asserts the exact behaviour that produces must_fix #1 - it pins `scope.properties.component_ids.type == ["array","null"]` as correct. Nothing in the suite closes the loop: there is no test that an arguments object which conforms to a strictified schema is actually accepted by `RecordedTool`/`validate_call`. That missing round trip is the coverage gap that let the defect through, and it is exactly the class of test the constitution calls non-negotiable.
  - Fix: Add a per-tool test that builds the maximal strict-conforming instance from `strictify(canonical_schema(fn))` (every property present, optionals set to null, required scalars given a valid value) and asserts `RecordedTool.call(...)` does not come back with an argument-validation `is_error`. Then update this test to whatever the corrected `strictify` emits.
- **reviewer/src/swreview/agent/providers/fake.py:187** - `error = str(result.payload.get("error")) if result.is_error else None` fabricates the literal string `"None"` for the contract's `tool.finished.error` field whenever a `ProviderTool` returns `is_error=True` with a payload that has no `error` key. The schema declares `error` as `["string","null"]`, so `"None"` validates and the pane would show an error whose message is the word None. `RecordedTool` happens to always set the key, so this is latent - but the fake is the thing every runner, server and pane test drives, and an invented message in an error path is the opposite of "a missing input must stay unknown".
  - Fix: `raw = result.payload.get("error"); error = str(raw) if raw is not None else (summarize_result(result.payload) if result.is_error else None)` - or assert the invariant explicitly. Add a test with a `FakeTool(is_error=True, payload={"detail": "..."})`.
- **reviewer/tests/unit/test_provider_schema.py:106** - T006's sentence is absolute: "none contains an object with zero properties". The implementation carves out `NO_ARGUMENT_TOOLS` (`get_package_summary`, `list_gaps`, `get_review_checklist`) and exempts the root parameters object (line 341). The exemption is substantively right - a tool that takes nothing has no other strict-mode form, and `{"type":"object","properties":{},"required":[],"additionalProperties":false}` is accepted - but it is a literal departure from the task text that has not been surfaced as a deviation.
  - Fix: Keep the exemption (it is correct) and record it explicitly in the task's deviation note, so the narrowing of a stated constraint is a decision on the record rather than a detail buried in a test docstring.
- **reviewer/src/swreview/agent/providers/schema.py:113** - `parse_docstring` returns only the text before the FIRST section header, so any `Returns:`, `Note:` or `Examples:` prose - whether it sits before or after `Args:` - is silently dropped from the description the model actually reads, with no error. No tool has such a section today (verified across all 29), so this is latent, but the module raises loudly for an undocumented parameter and says nothing at all for discarded prose - an inconsistent contract for the same failure class.
  - Fix: Either append the non-Args sections to the description (they are prose the model benefits from), or raise for a section the parser does not consume, so a future docstring cannot lose text without anyone noticing.
- **reviewer/src/swreview/tools/session.py:112** - `ToolContext.session` became `ReviewSession | None` (for general chat), but `mark_coverage` (line 112), `request_evidence` (line 73), `record_drawing_finding` (line 179) and `get_review_checklist` (line 205) all dereference it unguarded, and `ToolRegistry.functions_for` still hands the full curated list - session tools included - to a sessionless context. A general-chat call to `mark_coverage` therefore surfaces as `AttributeError: 'NoneType' object has no attribute 'coverage'` converted to a tool error, rather than a result that says there is no review session in this chat. This most likely belongs to T059, but it lands in a file T005a edited.
  - Fix: Either filter the four session-writing tools out of `functions_for` when `context.session is None`, or add one shared guard (`context.require_session("mark_coverage")`) that returns the standard `{"error": ...}` shape. Cover it with a test that builds a sessionless context and calls each of the four.
- **reviewer/src/swreview/agent/providers/__init__.py:270** - `available()` returns only the adapters that happen to have been imported already, so before the first `get()` call it can legitimately return `()`. The docstring says so, but the name reads like "the providers this build supports" and any caller that validates a user's `--provider` against it would reject a perfectly legal value depending on import order - an order-dependent failure that no test would reproduce.
  - Fix: Rename to `registered()`, or have `available()` return `tuple(ProviderName)` and keep the import-state question behind a separate `registered()`. Either way state in the docstring which one a CLI choice should be validated against.
- **reviewer/src/swreview/agent/providers/schema.py:338** - `gemini_adapt`'s docstring claims "only `additionalProperties` and `$defs` come out; everything else ... goes across as standard JSON Schema", but it leaves in the constraint-only `anyOf` branches that `strictify` is careful to drop (`SourceRef`'s `{"required": ["sheet"]}` alternatives) and the pydantic `title` keys. The installed `google-genai` types `parameters_json_schema` as `Optional[Any]` with no client-side validation (types.py:4814), so whether the Gemini backend accepts a bare-constraint `anyOf` branch is untested and unverifiable from the SDK source - the claim rests on nothing.
  - Fix: Either assert the claim in a test against a real Gemini `FunctionDeclaration` round trip (T017/T018's live test is the natural home), or drop the constraint-only branches and `title` in `gemini_adapt` too - reusing `_drop_constraint_only_branches` costs one line and removes the open question. Soften the docstring to what is actually known either way.
- **reviewer/src/swreview/agent/providers/fake.py:187** - `error = str(result.payload.get("error")) if result.is_error else None` emits the literal string "None" in the `tool.finished` body when a ProviderTool returns is_error=True with a payload that has no `error` key. It validates against chat-events.schema.json (string is allowed), so the contract test cannot catch it, and no test covers that payload shape. Today's RecordedTool always sets `error` (registry.py:439), but T059's MCP path and any other ProviderTool implementation are not bound by that.
  - Fix: Use an explicit fallback, e.g. `payload.get("error")` with `summarize_result(payload)` (or a fixed "the tool reported a failure" string) when the key is absent, and add a test case with `FakeTool(payload={"detail": "..."}, is_error=True)` asserting the event carries a meaningful error string.
- **reviewer/tests/support/contracts.py:64** - `contract_validator` passes `format_checker=Draft202012Validator.FORMAT_CHECKER`, which reads as 'formats are enforced', but `rfc3339-validator` is not installed in the venv, so `date-time` is silently NOT checked (I verified: `FORMAT_CHECKER.check('not-a-timestamp','date-time')` passes; `uuid` is checked). Every `format: date-time` field in chat-events and review-session — event `at`, session.ended `ended_at`, disposition `at`, `answered_at` — is validated as a plain string, so T008's and T010's contract assertions are weaker than they look.
  - Fix: Add `rfc3339-validator` (or the `jsonschema[format-nongpl]` extra) to the dev dependencies in pyproject.toml, plus a one-line guard test asserting `contract_validator(...)` rejects `{"at": "not-a-timestamp"}`, so the assurance is real rather than assumed.
- **reviewer/src/swreview/agent/providers/fake.py:45** - `_DELTA = re.compile(r"\S+\s*")` drops leading whitespace: for `text="  Checking\nthe joints"` the deltas rejoin to "Checking\nthe joints", so the stream the pane renders differs from `TurnResult.text` and from the `text.done` body. The invariant is only spot-checked on one whitespace-free string (test_text_only_turn_streams_deltas_then_one_done), so nothing pins it.
  - Fix: Split with a pattern that keeps every character (e.g. `re.findall(r"\s*\S+\s*", text)` or `re.split` keeping separators) and add a parametrized test over several texts (leading spaces, embedded newline, trailing space, single token, unicode) asserting `''.join(deltas) == turn.text == text.done body`.
- **reviewer/tests/unit/test_provider_protocol.py:322** - `test_stub_satisfies_the_provider_protocol` (and test_fake_provider.py:105) assert `isinstance(x, AgentProvider)`. For a runtime_checkable Protocol, isinstance only checks that the attribute names exist and are callable — it does not check signatures. Both tests would still pass if `run()` lost `on_event` or renamed `max_steps`, which is exactly the drift the protocol exists to prevent.
  - Fix: Add a signature assertion alongside the isinstance check, e.g. compare `inspect.signature(FakeProvider.run).parameters` against `inspect.signature(AgentProvider.run).parameters` (names and keyword-only-ness), for both the stub and FakeProvider.
- **reviewer/tests/unit/test_provider_schema.py:324** - The strict-mode assertions cover additionalProperties, required, nullability, $defs and $refs, but nothing asserts the absence of the keywords OpenAI rejects outright under `strict: true` for all models — allOf, not, if/then/else, dependentRequired, dependentSchemas, patternProperties. I verified none is present today, but pydantic can emit `allOf` when a field carries a $ref plus metadata, so a future model change (a Field(description=...) on SourceRef, an Annotated constraint) could reintroduce one and the only signal would be a 400 in T018a's live call. Research risk 4 designates T006 as the local evidence for exactly this.
  - Fix: Add one parametrized test over ALL_TOOL_FUNCTIONS walking `strictify(canonical_schema(fn))` and asserting no node contains any key in a named UNSUPPORTED_KEYWORDS frozenset, with the OpenAI doc reference in the docstring (fine-tuned-model-only restrictions such as minLength/minItems deliberately excluded, since they are supported on gpt-5.x).
- **reviewer/src/swreview/ir/models.py:382** - Nested model fields reach the model with no description at all: every property of InterferenceSettings, CoverageScope and SourceRef comes out as a bare name plus a pydantic-generated `title` (verified on check_fit.bore_dimension_ref: document_id/sheet/view/annotation/persist_ref/page/bbox all lack `description`). T006's test_every_parameter_carries_its_docstring_description only walks top-level parameters, so the gap is invisible. The docstring prose on mark_coverage/bridge_interference partly compensates, but `pairs`, `positions`, `ignore_hidden` and `fastener_folder_treatment` are exactly the fields where a guess silently becomes recorded engineering data.
  - Fix: Add `Field(description=...)` to InterferenceSettings, CoverageScope and SourceRef fields, and extend the T006 test to require a description on every property of every object subschema (not just the root's), so the requirement is enforced rather than assumed. Note this touches models shared with feature 001 output — check the golden baselines stay byte-identical (descriptions live in schemas, not in serialized sessions, so they should).
- **reviewer/tests/unit/test_tools_session.py:137** - In test_mark_coverage_refuses_a_scope_it_cannot_read, `with pytest.raises(ValidationError): CoverageScope(widgets=[])` asserts pydantic's own extra="forbid" behaviour, not any code in this repo — it is tautological and would pass even if mark_coverage were deleted. The behaviour that actually replaced the old error path (a model-invented scope key becomes an error ToolCallResult plus `failed` coverage rather than a raise) is only asserted in T012's registry tests, and the docstring here says so.
  - Fix: Either drop those two lines and keep only the unknown-ids assertion (which does exercise the tool), or make the case real by going through `ToolRegistry().build(context)` and asserting `tools['mark_coverage'].call({... 'scope': {'widgets': []} ...}).is_error is True` plus a `tool.mark_coverage` entry in `context.session.coverage.failed`.
- **reviewer/src/swreview/agent/providers/schema.py:233** - `_make_nullable` adds `null` to every non-required property, and schema.py's own docstring records the other half of the bargain: RecordedTool must drop a null for a parameter whose annotation does not admit one, so the default applies. For `scope: CoverageScope` that means a model sending `{"component_ids": null, "pairs": null, "configuration": null, "positions": null, "document_ids": null}` re-creates precisely the silent empty-scope outcome T005a set out to eliminate — it validates, and mark_coverage records an empty scope with no error. No test in T005a/T006 covers an all-null scope.
  - Fix: Decide explicitly and pin it with a test: either have mark_coverage reject a scope with no field set (`if scope == CoverageScope(): return error_result(...)`, matching 'never write a default for engineering data'), or add a test asserting the all-null scope is accepted deliberately. Either way, add the case to test_tools_session.py so the choice is visible.

### registry-runner (17)

- **reviewer/src/swreview/agent/runner.py:391** - `ReviewRun.start` writes defaults into the `session.started` body when `provider_info` is missing: `"provider": mapping.provider if mapping is not None else ""` and `effort_mapping` falling back to `{}` (line 396). Both are contract-invalid - the schema pins `provider` to the enum `[openai, gemini, fake]` and requires `effort_mapping.{requested,provider_param,provider_value}` - so the branch writes a silently malformed event into `events.jsonl` rather than failing. It is also error handling for a scenario `start_review` makes impossible, which the constitution's Principle V rules out.
  - Fix: Drop the conditionals and read `self.session.provider_info` directly; if the invariant needs stating, `raise ValueError("a review run must record its provider before the first turn")` when it is None. Same shape at line 444, where `session.ended` emits `"ended_at": None` if `ended_at` were null, against a schema that requires a date-time string.
- **reviewer/src/swreview/report/session.py:38** - `ProviderInfo.provider` is typed `str`, but `review-session.schema.json` constrains it to `["openai", "gemini", "fake"]`. Pydantic will happily accept `ProviderInfo(provider="anthropic", ...)` and `save_session` will write a `session.json` that fails its own contract, with nothing in the write path validating against the schema. `tests/unit/test_session.py` covers the equivalent hole for `key_source` (`test_provider_info_rejects_an_unknown_key_source`) but has no counterpart for `provider` - because there is nothing to assert.
  - Fix: Type it `ProviderName` (already imported alongside `EffortMapping` from `swreview.agent.providers`) or a local `Literal["openai", "gemini", "fake"]`, and add the mirror of the `key_source` rejection test.
- **reviewer/src/swreview/agent/runner.py:359** - data-model.md section 3 rule 4 reads "the cumulative step count is recorded separately **on the session**"; the implementation keeps it on the in-memory run (`self.total_steps`, incremented at line 502) and the runner docstring restates it as "on the run". Nothing persists it, so a `session.json` reloaded after a pane restart cannot tell how much budget the session has already spent. `len(session.steps)` is a near-equivalent but counts recorded calls, not provider-reported steps, and diverges the moment an adapter reports a step it did not record.
  - Fix: Either add an optional `cumulative_steps` (or fold it into `Timing`) on `ReviewSession` and write it on each finalize, or - if the field is genuinely not wanted - amend data-model rule 4 to say "on the run" so the artifact and the spec agree. My recommendation is the session field: it is one integer, it survives a restart, and the resume rule exists precisely for the restart case.
- **reviewer/src/swreview/tools/registry.py:413** - `ToolDispatch.by_name` is a `@property` that rebuilds the whole name dict on every access. `ToolDispatch.call` hits it once through `get()` and, on the unknown-name path, a second time for `sorted(self.by_name)` in the error message - so every dispatched tool call rebuilds a ~21-entry dict, twice for hallucinated names. `ToolDispatch` is a frozen dataclass over an immutable tuple, so the mapping is invariant for its whole life.
  - Fix: Build it once: `functools.cached_property`, or compute it in `ToolRegistry.dispatch` and store it as a field. Small, but this is the per-call hot path for every provider turn.
- **reviewer/src/swreview/tools/registry.py:388** - The trace summary regressed for list-returning tools. Feature 001's `summarize` rendered a list as `f"{len(payload)} items: " + json.dumps(...)`, so the count survived the 200-character truncation; `summarize_result` (agent/providers/__init__.py) has no list branch, and because `_as_object` wraps lists as `{"result": [...]}` first, a long `list_components` result now records `{"result":[{"id":"cmp:0001",...` truncated at 200 chars with the count gone. `InvestigationStep.result_summary` is the trace line an engineer reads in `session.json`, and "how many did it get back" was its most useful fact.
  - Fix: Restore the count in `summarize_result` for the wrapped-list case - e.g. when the payload is exactly `{RESULT_KEY: [...]}`, emit `f"{len(items)} items: " + json.dumps(items, ...)` before truncating. Keeps the one shared summarizer (the reason it was moved to `providers`) and restores the feature 001 trace.
- **reviewer/src/swreview/tools/registry.py:154** - `RESULT_KEY` wrapping changes the payload the model actually receives for every list-returning query tool (`list_components`, `list_mates`, `list_holes`, `list_fasteners`, `list_interferences`, `list_gaps`, `find_components`, `find_dimensions`): feature 001 handed the model a bare JSON array, it now gets `{"result": [...]}`. The change is necessary (Gemini's `function_response` takes an object) and is documented in the module docstring, but `specs/001-agentic-design-review/contracts/agent-tools.md` still documents the bare-array return shapes, and T064's doc-update sentence only covers the schema-generation prose and the `mark_coverage`/`bridge_interference` rows - it does not mention the result envelope.
  - Fix: Add the result-envelope rule to the T064 doc edit (one sentence in agent-tools.md: a non-object tool result is delivered as `{"result": <value>}`), and consider asserting it once in `test_registry_provider_neutral.py` against the contract text so the two cannot drift.
- **reviewer/tests/unit/test_runner_provider.py:707** - Two bridge tests from the deleted `test_agent_runner.py` have no replacement: `test_the_pipe_name_reaches_the_factory` (a non-default `pipe_name` actually reaches `bridge_factory`) and `test_the_default_factory_builds_a_bridge_client_without_connecting` (the default factory is `BridgeClient` and constructing it does not connect). The new `test_bridge_true_wires_a_client_into_the_context_and_the_tools` only asserts the default pipe name `"swreview"` via an injected factory, so `pipe_name` is now a parameter nothing exercises and the production default factory is untested. T014 says "replace the anthropic-based fake", not "drop coverage", and the project's stated preference is too many tests over too few.
  - Fix: Port both cases: one asserting `start_review(..., bridge=True, pipe_name="custom", bridge_factory=f)` passes `"custom"` through, and one asserting `start_review(..., bridge=True)` with no factory builds a `BridgeClient` without opening the pipe.
- **reviewer/src/swreview/agent/runner.py:173** - `finalize_session` does `review = context.session` and immediately dereferences `review.coverage` / `review.evidence_requests`, but T013 made `ToolContext.session` optional (`ReviewSession | None`). A sessionless context - the general-chat shape the same task introduced - reaches here as a bare `AttributeError: 'NoneType' object has no attribute 'coverage'` rather than a named error. `ReviewRun.session` (line 368) already has the explicit guard; the module-level function does not.
  - Fix: Add the same explicit check at the top: `review = context.session; if review is None: raise ValueError("finalize_session needs a context with a review session")`. Three lines, and it keeps the type checker honest now that `session` is optional.
- **reviewer/src/swreview/report/session.py:38** - `ProviderInfo.provider` is typed `str`, but the contract (review-session.schema.json `$defs/ProviderInfo`) constrains it to `enum: ["openai", "gemini", "fake"]`. Nothing on the Python side rejects any other value, so a session.json that fails its own contract can be written without a single test failing. test_session.py:289 proves `key_source` is rejected for a bad value but there is no equivalent for `provider` - the one field where an enum actually exists in the schema.
  - Fix: Type it `provider: ProviderName` (it is a StrEnum, so it serializes to the bare value and the golden/contract output is unchanged), and add a `ProviderInfo(provider="anthropic", ...)` -> ValidationError case next to the existing key_source test.
- **reviewer/src/swreview/agent/runner.py:391** - Two defensive fallbacks in the event emission produce events that violate chat-events.schema.json. `"provider": mapping.provider if mapping is not None else ""` emits `""`, which fails the `session.started` body's `provider` enum; and at runner.py:444 `"ended_at": ... else None` emits null, which fails `session.ended`'s `"ended_at": {"type": "string"}`. Both are unreachable through `start_review` (it always sets provider_info; finalize always sets ended_at), so no test covers them - the fallback silently trades a loud failure for a contract-invalid line in events.jsonl that the pane will choke on.
  - Fix: Drop the fallbacks and read the values off the session directly, or raise/assert if they are absent. `explicit over clever`: if the state is impossible, say so rather than writing an invalid event. If the branch is kept, it needs a test that the emitted event still validates.
- **reviewer/tests/unit/test_runner_provider.py:707** - The port from test_agent_runner.py lost two bridge cases. (1) `test_the_default_factory_builds_a_bridge_client_without_connecting` is gone - every new test injects `bridge_factory`, so `factory = bridge_factory if bridge_factory is not None else BridgeClient` (runner.py) is now entirely untested; breaking the default wiring would ship green. (2) `test_the_pipe_name_reaches_the_factory` used a custom `pipe_name="review-42"`; the new test only asserts the default `"swreview"`, so a runner that ignored the caller's pipe_name would still pass.
  - Fix: Restore both: one test passing `pipe_name="review-42"` with an injected factory asserting the name reaches it, and one with `bridge=True` and no factory asserting `isinstance(run.context.bridge, BridgeClient)` and `pipe_name == "swreview"` (the old test proved BridgeClient does not connect on construction, so it stays offline).
- **reviewer/src/swreview/tools/registry.py:262** - `_admits_null` (the recursive anyOf/oneOf walk) and the nullable-keeps-its-null half of `_prepared` are currently unobservable: no tool in the registry has a parameter whose annotation admits None and whose default is not None (verified by grep over tools/*.py), so dropping a null and keeping it produce identical results for every tool that exists. The one test aimed at this (test_registry_provider_neutral.py:139) passes `parent_id: None` where the default is also None, so it cannot distinguish the branches - forcing `_admits_null` to return False unconditionally would break nothing. That is ~16 lines of recursion with no test that can fail.
  - Fix: Either pin it directly - a small test that builds a `RecordedTool` over a synthetic `ToolSpec` with `flag: bool | None = True` and asserts a null arrives as `None` rather than `True`, and that a non-nullable `bool = True` gets the default - or, if no real tool will ever need it, delete the recursion and drop nulls for anything without an explicit `"null"` in `type`.
- **reviewer/tests/unit/test_runner_provider.py:541** - Resume rule (d) asserts the `max_steps` closeout item exists (`len(closeout) == 1`) only before the resumed turn. After `continue_session` - which finalizes a second time - the test checks steps, total_steps and turn.ended reasons but never re-checks the closeout item. So the interaction of rule (d) with rule (a) is unpinned in both directions: a finalize that withdrew the item, or one that duplicated it, both stay green here. (The sibling case at line ~455 covers the same mechanism for a `mark_coverage` item, so the mechanism is not wholly untested - but the max_steps item is the one this test exists for.)
  - Fix: Add the two assertions after `run.continue_session("Keep going.")`: the closeout list is still length 1 and its reason still names `max_steps reached (2 tool calls)`.
- **reviewer/tests/unit/test_registry_provider_neutral.py:299** - The FR-026 guard exempts all of `agent/providers/__init__.py` from the `(?i)anthropic|claude` scan, justified by `RETIRED_PROVIDERS` needing the bare names in order to reject them. But the exemption is wider than the justification: that file also carries prose - `"Feature 002 removed the Anthropic SDK entirely (FR-026)"` and the error text `f"provider {text!r} was removed with the Anthropic SDK in feature 002"` - which is a vendor string under reviewer/src/ that the product constraint forbids and that `MODEL_OR_PACKAGE` (`claude-|anthropic[.\-/]|@anthropic`) deliberately permits. It is the one hole the scan leaves. Separately, the scan only walks `*.py`; non-Python files under src/ (checklist_v1.yaml, prompts/system_v1.md, bridge/PROTOCOL.md) are unscanned - clean today, but nothing keeps them so.
  - Fix: Reword the two prose mentions in providers/__init__.py to avoid the vendor name (e.g. "the retired provider SDK"), keeping only `RETIRED_PROVIDERS = frozenset({"anthropic", "claude"})`, then narrow `RETIREMENT_GUARD` to permit just that one literal line. While there, widen `source_files()` to every text file under src/ (T068 will want this anyway).
- **reviewer/src/swreview/tools/registry.py:431** - `RecordedTool.call` guards a non-Mapping `arguments` and converts it to an error result, but `ToolDispatch.call`'s unknown-name path does not: it goes straight to `record_call(..., arguments=arguments, ...)` which does `to_jsonable_python(dict(arguments))` and raises TypeError for a list or string - inside the layer whose documented invariant is "nothing raises. Ever." The asymmetry is unreachable in practice today (openai_provider raises MalformedArgumentsError for non-object arguments before dispatch, gemini gets a dict from the SDK), which is also why neither the guard at registry.py:302 nor this gap has a test.
  - Fix: Move the non-Mapping coercion above the name lookup so both paths share it, and add one test passing `[1, 2]` as arguments to both a known and an unknown tool name, asserting an error result rather than a raise.
- **reviewer/tests/unit/test_report.py:117** - The shared `build_session` fixture still writes `"model": "claude-opus-5"` (same at test_session.py:74, and in test_scorecard.py / test_timing.py). The FR-026 scan is deliberately scoped to reviewer/src/, so this is not a violation - but these are precisely the two files T016 just touched, and the new tests now render a `ProviderInfo(provider="openai", model="gpt-test")` header directly above a `- Model: claude-opus-5` line in the same report. It reads as an oversight and will be re-flagged at T068.
  - Fix: Swap the fixture model literal to a neutral `"gpt-test"` (or the provider-neutral default T023 lands). One-line change per file, no assertion depends on the value.
- **reviewer/tests/unit/test_registry_provider_neutral.py:99** - `test_the_built_names_are_the_registered_groups_in_order` derives `expected` from `REGISTRATIONS` - the same constant `TOOL_FUNCTIONS` is derived from - so it can only catch a `build` that reorders or drops entries, not a wrong tool surface. Mild tautology. It is not a real hole (test_provider_schema.py:202 holds the golden against contracts/agent-tools.md, which is the assertion that has teeth), so this is a note rather than a defect.
  - Fix: Either leave it and add a one-line comment pointing at the agent-tools.md golden as the real guard, or fold the ordering claim into that golden test so one test owns the tool surface.

### adapters-settings (14)

- **reviewer/src/swreview/agent/providers/gemini_provider.py:309** - `TurnResult.text` is inconsistent between the two adapters for the same provider-neutral contract. OpenAI accumulates every round's text and returns the join (openai_provider.py:250-252, 362). Gemini returns only the last round's `round_.text` (line 312) and hard-codes `text=""` on the `max_steps` return (line 309), so anything the model said before its tool calls, or in the round the budget cut short, is dropped from the turn result the runner records.
  - Fix: Accumulate round texts in `run` the way openai_provider does and return the join on every exit path, including `max_steps`. Add a two-round test where both rounds emit text and assert `result.text` holds both.
- **reviewer/src/swreview/agent/providers/gemini_provider.py:267** - `text.done` cardinality differs between adapters: Gemini emits one per model round (line 267-268), OpenAI one per turn carrying the whole turn's text (openai_provider.py:361-365). FR-013 requires a uniform event stream that both the pane and the CLI consume; a consumer that treats `text.done` as "the turn's answer is complete" will be wrong on one of the two providers, and the difference is pinned by tests on both sides rather than reconciled.
  - Fix: Pick one semantic, write it into contracts/chat-events.schema.json's description (or data-model section 2), and make both adapters follow it. One `text.done` per turn is the reading that matches `turn.ended` and the existing OpenAI tests.
- **reviewer/src/swreview/agent/providers/gemini_provider.py:208** - `max_output_tokens` defaults to `None` and is then omitted from `GenerateContentConfig` (line 388), so `OUTPUT_CEILINGS[ProviderName.GEMINI]` in agent/settings.py:97 is dead unless a caller remembers to pass it, and the `MAX_TOKENS` -> "truncated" path depends on the model's own default rather than on a budget we own - the settings module's own docstring (settings.py:100-108) says the opposite ("Both are *our* budget for one turn ... which it can only do if the ceiling is ours to set"). The OpenAI adapter defaults from `output_ceiling` (openai_provider.py:182-186) and rejects a non-positive value (line 187-188); Gemini does neither. tests/unit/test_gemini_provider.py:275-287 pins the omission as intended behaviour.
  - Fix: Default to `output_ceiling(ProviderName.GEMINI, model)` when `max_output_tokens is None`, and validate `> 0`, exactly as openai_provider.py:182-188 does. Update the test to assert the settings ceiling is sent when nothing is passed.
- **reviewer/src/swreview/agent/providers/gemini_provider.py:349** - `_stream` catches only `errors.APIError`. google-genai raises transport failures (httpx connect/read/timeout errors) unwrapped from `_api_client`, so a dropped connection or DNS failure escapes `run` with no `error` event, no `retryable` classification and no redaction - the pane gets an unclassified crash instead of the retry card FR-013 and FR-028 assume. The OpenAI adapter has no such hole because `openai.OpenAIError` covers `APIConnectionError` (openai_provider.py:323, and the mapping at 412).
  - Fix: Widen the except to cover the transport exceptions the installed client can raise (`httpx.HTTPError`/`httpx2` equivalents alongside `errors.APIError`) and map them to a retryable `GeminiProviderError` through `_report`. Add a test whose stub `generate_content_stream` raises a connect error.
- **reviewer/src/swreview/agent/settings.py:389** - `redact(text, secrets)` and `RedactingFilter(secrets)` take `Iterable[str | SecretStr | None]`. A bare `str` satisfies that annotation at runtime, so `redact(msg, settings.api_key.get_secret_value())` - an easy and silent mistake - iterates the key's characters and masks every occurrence of every character in the text, producing unreadable output while looking like it worked. `openai_provider._redact` (line 406) gets it right by wrapping in a list, but nothing enforces it at the boundary; "explicit over clever" argues for rejecting the ambiguous input.
  - Fix: Reject a bare `str`/`SecretStr` argument in `_secret_values` with a `TypeError` naming the mistake ("pass a sequence of secrets, not one secret"), and add a test for it.
- **reviewer/tests/conftest.py:26** - T018a gives OpenAI a key-gated proof that its strictified schemas survive the real API, on the stated grounds that "a respx exchange replays a body we authored and cannot retire this risk". Gemini has exactly the same risk and no such test: `LIVE_KEYS` is `("OPENAI_API_KEY",)` with a TODO-style docstring ("Gemini's key names join this tuple with its own live test"), and every Gemini assertion runs against a hand-rolled `StubClient` (tests/unit/test_gemini_provider.py:97-116) that never serializes a request. `gemini_adapt` output under `parameters_json_schema`, `thinking_level`/`thinking_budget` acceptance per model, and the content role are all unproved against the API - and the role defect above is precisely what that gap let through.
  - Fix: Add `tests/live/test_gemini_live_schemas.py` mirroring the OpenAI one (post each tool group's `gemini_adapt`ed declarations plus one function-response round trip, fail on 400, skip on 401/429/5xx) and add `GOOGLE_API_KEY`/`GEMINI_API_KEY` to `LIVE_KEYS`. Note that `LIVE_KEYS` is currently an `any()` check, so once it holds both providers' names an OpenAI-only workstation would try to run the Gemini live test - gate each live module on its own key.
- **reviewer/tests/unit/test_gemini_provider.py:355** - No Gemini test scripts a round that contains BOTH text and a function call - every tool-call test uses a call-only first round (test_a_function_call_runs_through_the_tool_and_is_answered_with_its_id, test_parallel_calls_in_one_round..., test_max_steps_is_a_per_turn_budget) and every text test is single-round. That is precisely the shape where the text.done/TurnResult.text defect above is visible, and it is the common real shape for Gemini (the model narrates before calling a tool). The suite passes while the turn text is being lost.
  - Fix: Add a test scripting [text_part("Checking the bracket. "), call_part(...)] then [text_part("It interferes."), finish_reason=STOP] and assert exactly one `text.done` whose text is the concatenation, and `result.text == "Checking the bracket. It interferes."`.
- **reviewer/tests/conftest.py:38** - `live` tests are gated on `any(os.environ.get(name) for name in LIVE_KEYS)`, i.e. ANY provider key un-skips EVERY live test. Today LIVE_KEYS holds only OPENAI_API_KEY so it is inert, but the docstring at line 28 states "Gemini's key names join this tuple with its own live test (feature 002, Phase 2)". The moment they do, a workstation with only GOOGLE_API_KEY set runs tests/live/test_openai_live_schemas.py, whose module-scoped `client` fixture does `os.environ["OPENAI_API_KEY"]` and raises KeyError - a red suite instead of a skip, which is exactly what the skip convention exists to prevent.
  - Fix: Gate per test rather than globally: give each live test its own required key (e.g. a `requires_key("OPENAI_API_KEY")` marker or a fixture that calls `pytest.skip` when its key is absent) so a Gemini-only or OpenAI-only environment skips the other provider's live test instead of erroring.
- **reviewer/src/swreview/agent/settings.py:151** - `output_ceiling` consults `MODEL_OUTPUT_CEILINGS` before `OUTPUT_CEILINGS`, and the docstring says the model is consulted first "so a model with a smaller output window can be given its own number". `MODEL_OUTPUT_CEILINGS` is empty and no test ever populates it, so the model-first precedence - the entire reason the table exists - is unproven: swapping the two lookups, or dropping the per-model table altogether, leaves the whole suite green. The one test that touches it (test_output_ceiling_falls_back_to_the_provider_budget_for_an_unlisted_model) only exercises the fallback.
  - Fix: Add a test that monkeypatches a per-model entry (e.g. `monkeypatch.setitem(settings.MODEL_OUTPUT_CEILINGS, "gpt-5-nano", 4096)`) and asserts `output_ceiling("openai", "gpt-5-nano") == 4096` while the provider budget still applies to other models.
- **reviewer/src/swreview/agent/providers/openai_provider.py:473** - `_encode_assistant`'s fallback branch - rebuilding `function_call` input items from the neutral `tool_calls` list when the message carries no `openai` key - is untested. Its stated purpose is resuming a session on a provider it did not start on (module docstring, line 445-449), and every test either replays an adapter-written history (which takes the `HISTORY_KEY` branch) or a text-only assistant message. A mistake here (a missing `call_id`, `arguments` left as a dict instead of a JSON string) produces a 400 on the first resumed request, and nothing in the suite would catch it.
  - Fix: Add a respx test whose `messages` contain an assistant message with `tool_calls` and no `openai` key plus the matching `role: tool` message, and assert the encoded `input` carries `{"type": "function_call", "call_id": ..., "name": ..., "arguments": "<json string>"}` followed by the `function_call_output`.
- **reviewer/src/swreview/agent/providers/gemini_provider.py:310** - The truncation check is asymmetric with the OpenAI adapter. `_end_reason(round_.finish_reason)` is reached only when the round made no calls, so a round that comes back with `finish_reason=MAX_TOKENS` AND a function_call runs that call and continues the loop, and the turn can end `end` rather than `truncated`. The OpenAI adapter deliberately does the opposite (openai_provider.py:263-267: the ceiling is checked before dispatch because "running those calls would spend the session on a request we already know is incomplete"), and the runner turns `truncated` into unresolved coverage - so the same cut-short turn is recorded as finished on Gemini and unresolved on OpenAI. Untested in either direction on Gemini.
  - Fix: Check `round_.finish_reason == MAX_TOKENS` before dispatching the round's calls and return `truncated` without running them (mirroring `_hit_ceiling`), and add a test scripting a chunk with a function_call and `finish_reason=MAX_TOKENS` that asserts `reason == "truncated"`, `steps == 0` and no `tool.started`.
- **reviewer/src/swreview/agent/providers/openai_provider.py:286** - `max_steps` means something different in each adapter. OpenAI returns `max_steps` the moment `steps >= max_steps` after running the round's calls, so with max_steps=1 the model never sees the result of the call it just made (test_max_steps_is_a_per_turn_budget asserts `route.call_count == 1`). Gemini instead runs the calls, loops, and issues one more request so the model can summarize, only ending `max_steps` if that next round asks for more calls. Same flag, same budget, observably different turns - and the runner records unresolved coverage in one case and a finished turn in the other.
  - Fix: Pick one semantic and state it on `AgentProvider.run`'s docstring in providers/__init__.py (the Gemini behaviour - spend the budget, then let the model answer once - wastes less), then make both adapters match and pin it with the same test in both suites.
- **reviewer/src/swreview/agent/providers/openai_provider.py:322** - Three error paths in the OpenAI adapter have no test at all: the `response.failed` stream event (`_failed`, line 425, which produces error_class `ResponseFailedError` and reads `response.error.message`), the `final is None` guard producing `IncompleteStreamError` (line 326), and both constructor `ValueError`s (a non-positive ceiling, and neither api_key nor client, lines 188-190). `response.failed` is a routine Responses-API outcome (a mid-generation model failure), and the `response.error` attribute access is exactly the kind of shape a stub never validates.
  - Fix: Add respx exchanges for a `response.failed` event (with and without an `error` object) and for a stream that ends with neither `response.completed` nor `response.incomplete`, asserting the error_class, `retryable`, and that the `error` event is emitted; add two one-line pytest.raises tests for the constructor guards.
- **reviewer/src/swreview/agent/providers/gemini_provider.py:452** - `_request` documents that "A model that sends no `id` gets no id back (`""` here, omitted on the response part): inventing one would pair a response to a call the model never made", and `_response_part` (line 471) guards on `if call_id`. Neither branch is tested - every `call_part` helper in the suite passes an explicit id. The documented compatibility behaviour for pre-3.5 models is unproven, and `GeminiRequestError` for a 403 (`code in (401, 403)`, line 526) is likewise only exercised at 401.
  - Fix: Add a test with `types.FunctionCall(name=..., args=...)` and no `id`, asserting the tool is still called and the emitted `function_response` part has `id is None`; add 403 to the error-mapping parametrize list.

### cli-gates (13)

- **reviewer/tests/unit/test_events_schema.py:14** - The module docstring states: "the assertion is one-way, so wiring a producer for one of them does not break this test." Line 221 is `assert emitted == SCHEMA_EVENT_TYPES - NOT_YET_EMITTED` - strict set equality. The first commit that wires a `finding`, `coverage`, `evidence.requested` or `disposition` producer into the runner will break this test in exactly the way the docstring promises it will not, and the next agent will read the docstring and be misled about why.
  - Fix: Either make the assertion match the prose - `assert emitted >= SCHEMA_EVENT_TYPES - NOT_YET_EMITTED` plus `assert emitted <= SCHEMA_EVENT_TYPES` - or delete the "one-way" sentence and say plainly that NOT_YET_EMITTED must be shrunk in the same commit that wires a producer.
- **reviewer/tests/unit/test_events_schema.py:58** - `NOT_YET_EMITTED` excuses `finding`, `evidence.requested`, `disposition` and `coverage` on the grounds that they "belong to the US1 chat server". FR-013 requires the *provider layer* to emit "finding recorded, evidence request opened, coverage changed" as part of the uniform event stream, and this module's own point 2 argues `events.jsonl` is what the pane replays after a crash. If those four are pushed by the chat server over SSE only, they never reach `events.jsonl` and a post-crash replay loses every finding and evidence card. I found no task between T035 and T041 that names the emission explicitly (T041 only consumes them on the page), so the allow-list currently legitimizes a gap that nobody owns.
  - Fix: Confirm with the plan author whether the runner (T015) or the chat server (T037) emits these four. If the runner, the emission belongs in Phase 2 and NOT_YET_EMITTED should be empty. If the chat server, add a one-line comment naming the task id that will shrink the set, so the exemption is traceable rather than open-ended.
- **reviewer/src/swreview/benchmark/runner.py:45** - Three problems in one function. (a) Layering: `from swreview.cli import provider_factory` makes the benchmark library depend on the CLI, which already imports `benchmark.runner` at module scope - a deliberate circular edge that only survives because the import is deferred to call time, and that drags typer and the fake provider into any library-only consumer. (b) DRY: `_default_review_fn` (lines 32-56) is a line-for-line duplicate of `cli._review_fn` (cli.py:837-855) - same `ProviderSettings.from_env`, same `run_review(provider=provider_factory(settings), model=, effort=, key_source=)`. CLAUDE.md asks for aggressive DRY flagging. (c) Coverage: grep across `reviewer/` shows `_default_review_fn` is referenced only by its own definition and docstring - every CLI path injects `review_fn`, and no test exercises it, so it is untested code whose only job is to duplicate tested code.
  - Fix: Delete `_default_review_fn` and make `review_fn` a required argument of `run_benchmark` (every real caller already passes one), or keep one default that is literally `from swreview.cli import _review_fn as _default_review_fn` with a test that calls `run_benchmark` without `review_fn` and asserts it reaches `provider_factory`.
- **reviewer/src/swreview/cli.py:170** - `provider_factory`'s two real branches are never executed by any test. `tests/unit/test_cli.py` monkeypatches the whole function for every review test; the only two tests that use the real factory are `--provider fake` (line 388, the FAKE branch) and the no-key case (line 413), which raises inside `OpenAIProvider.__init__` before any client is built. So `genai.Client(**settings.client_kwargs())`, the `redact=` wiring, the `max_output_tokens=output_ceiling(...)` argument and the OpenAI `adapter(model=..., **client_kwargs())` spread are all unasserted. I exercised all three by hand and they construct correctly today, but a renamed SDK kwarg or a dropped `redact=` would surface only on the workstation - and a dropped `redact=` is silent (the Gemini adapter falls back to `_verbatim`). **Resolved 2026-09-23:** the Gemini branch was already broken - it passed `redact=` to an adapter whose constructor takes `secrets=`, so every Gemini review, pane review and remodel raised `TypeError` before its first request. It now passes `secrets=settings.secrets`, and `tests/unit/test_gemini_provider_construction.py` builds the real adapter through `provider_factory`, `chat.server.build_provider` and `remodel.runner.build_provider` with the client stubbed and the network closed.
  - Fix: Add two tests that monkeypatch `openai.OpenAI` and `google.genai.Client` with recording stubs and assert the exact kwargs `provider_factory` passes, including that the Gemini adapter receives a non-default `redact` that masks the configured key and the per-provider `max_output_tokens`.
- **reviewer/src/swreview/cli.py:208** - `class Effort(StrEnum)` (line 133) restates `providers.EffortLevel = Literal["low", "medium", "high", "xhigh"]` value for value, and line 208 bridges them with `effort_level: EffortLevel = effort.value  # type: ignore[assignment]`. The `type: ignore` is precisely what would hide the drift if either list changes - add a fifth level to `EffortLevel` and nothing fails; add one to `Effort` and the ignore swallows it. This is a DRY violation on a small, exactly-duplicated enumeration, and it is masked rather than surfaced.
  - Fix: Build the Typer choice from the single source: `Effort = StrEnum("Effort", {level: level for level in get_args(EffortLevel)})`, or keep the class and replace the ignore with `typing.cast(EffortLevel, effort.value)` plus an assertion (in a test) that `{m.value for m in Effort} == set(get_args(EffortLevel))`.
- **reviewer/src/swreview/tools/context.py:202** - T023 says the default must be taken "from `agent/settings.py` per provider at every use (`new_session`, both `build_context` overloads, `context_for`, ...)". `new_session` instead hardcodes one provider: `default_model(DEFAULT_PROVIDER)`, i.e. always `gpt-5.6`. It has no provider parameter, so the per-provider part of that sentence is not implemented. Every caller that omits `model` records OpenAI's model id regardless of what is running: `cli._context_for` (cli.py:537, used by all four `check` subcommands), `tools/golden.py:49`, and any future general-chat or MCP context that builds a session. Today `start_review` always passes `chosen_model`, so no review is mis-stamped - but the guard is a convention, not the type.
  - Fix: Either give `new_session`/`build_context`/`context_for` a `provider: ProviderName` parameter alongside `model` and resolve `default_model(provider)`, or make `model` required and let the two or three callers that genuinely have no model pass `default_model(DEFAULT_PROVIDER)` explicitly at the call site, so the OpenAI assumption is visible where it is made rather than buried in a shared helper.
- **reviewer/src/swreview/cli.py:173** - The missing-key path is correct (exit 1, one stderr line, verified) but the message is the adapter's internal wording: `error: ValueError: an OpenAI adapter needs either an api_key or a ready-made client`. It names neither `OPENAI_API_KEY` nor the settings pane, so the engineer whose key expired gets no route to the fix. The same will be true for Gemini once a keyless Gemini run reaches the SDK.
  - Fix: In `provider_factory`, check `settings.api_key is None` for OPENAI and GEMINI before constructing the adapter and raise a `ValueError` naming the provider, the environment variable it looked at (`OPENAI_API_KEY`, or `GOOGLE_API_KEY`/`GEMINI_API_KEY`), and that the pane's settings file takes precedence - with `key_source` in the text. Assert the variable name appears in stderr in `test_review_on_openai_without_a_key_exits_1`.
- **reviewer/tests/support/contracts.py:20** - Two near-identical loaders now coexist: `load_contract` (line 20, hardcoded to the feature 001 directory) and `load_any_contract` (line 33, searches `CONTRACT_DIRS`). `load_any_contract` is a strict superset for every name 001 holds, so the older one is duplication kept only to avoid touching callers. Separately, `contract_registry()` (line 40) re-reads and re-parses every `*.schema.json` in both feature directories on every call, and `contract_validator` calls it each time - `test_events_schema.py` alone builds it once per test plus once per parametrize case.
  - Fix: Make `load_contract` an alias of `load_any_contract` (or delete it and update the callers), and wrap `contract_registry` in `functools.lru_cache` so the registry is built once per session. Both are pure reads of committed files, so caching is safe.
- **reviewer/tests/unit/test_events_schema.py:14** - The module docstring states that the NOT_YET_EMITTED assertion 'is one-way, so wiring a producer for one of them does not break this test', but line 221 asserts strict equality: `assert emitted == SCHEMA_EVENT_TYPES - NOT_YET_EMITTED`. Wiring the US1 chat server's `finding`/`evidence.requested`/`disposition`/`coverage` producers will fail this test, contradicting the documented contract and sending a future agent hunting a non-bug.
  - Fix: Keep the strict equality (it is the stronger assertion and matches the 'too many tests than too few' preference) and correct the docstring to say the set is exact and NOT_YET_EMITTED must be shrunk as producers are wired. Only weaken to `SCHEMA_EVENT_TYPES - emitted <= NOT_YET_EMITTED` if the one-way behaviour is actually wanted.
- **reviewer/tests/unit/test_cli.py:1283** - `test_benchmark_run_passes_the_provider_and_model_to_every_package` runs against the `benchmark_set` fixture (test_cli.py:1219), which is documented as 'A one-package benchmark set'. With one package, `[provider.settings.provider for provider in built] == [ProviderName.FAKE]` cannot verify the property both `cli._review_fn` and `benchmark/runner.py`'s new docstring claim is the point: a fresh adapter per package, because adapters carry per-turn state (step indices, a client). A regression that hoisted provider_factory out of the per-package loop would still pass, and the test's own name overclaims.
  - Fix: Add a two-package fixture (the same tmp_package_dir referenced under two package_ids) and assert `len(built) == 2`, that the two RecordingProvider objects are distinct (`built[0] is not built[1]`), and that both carry the same resolved settings - or rename the test to what it actually proves.
- **reviewer/tests/unit/test_cli.py:457** - `test_review_with_an_unknown_provider_is_a_usage_error` asserts only `result.exit_code == 2` and its docstring invokes FR-026, but Typer's `ProviderName` enum validation rejects 'anthropic' before `providers.get` is ever reached, so the test proves Typer's enum conversion works and says nothing about the RETIRED_PROVIDERS guard at agent/providers/__init__.py:248. It is also the weakest assertion in the rewritten file - exit code only, no message.
  - Fix: Assert the usage error names the accepted spellings (`'openai' in result.stderr + result.stdout` and `'gemini'`), so the test fails if the enum ever silently gains a member; and leave the retirement-guard assertion where it belongs, in test_registry_provider_neutral.py, rather than implying it here.
- **reviewer/src/swreview/cli.py:170** - `provider_factory`'s gemini branch (the `genai.Client(**settings.client_kwargs())` construction, including the enterprise kwargs) is never executed by any test. Two tests use the real factory - fake (test_cli.py:388) and the openai missing-key path (test_cli.py:414) - but the gemini branch, which is the only place `output_ceiling(...)` and the `redact` lambda are wired into an adapter, has no coverage. A wrong kwarg name there surfaces only at runtime on a real Gemini run.
  - Fix: Add a test that monkeypatches `google.genai.Client` (or `swreview.agent.providers.get`) and calls `cli.provider_factory(ProviderSettings(provider=gemini, model=..., api_key=...))` directly, asserting the client kwargs, the model, `max_output_tokens == output_ceiling('gemini', model)`, and that the injected `redact` masks the key.
- **reviewer/tests/support/contracts.py:59** - `contract_validator` calls `contract_registry()` on every invocation, which re-globs both contract directories and re-parses every *.schema.json from disk each time (test_events_schema.py alone calls it 8+ times, and the parametrized turn-reason test calls it once per case). Separately, `load_contract` and `load_any_contract` are now near-duplicate readers differing only in the directory they search.
  - Fix: Wrap `contract_registry` in `functools.cache` (the files are read-only fixtures for the life of a test session) and make `load_contract` delegate to `load_any_contract`, or drop `load_contract` in favour of the one loader once its remaining callers are updated. Low urgency - the suite runs in 13s - but it is the kind of duplication the DRY preference asks to be flagged.

## Feature 002 Phases 3-4 (settings and review chat)


### chat-backend (15)

- **reviewer/src/swreview/chat/server.py:955** - `worker.shutdown(wait=False)` does not stop `concurrent.futures.thread._python_exit`, which joins every non-daemon pool thread at interpreter exit. A shutdown signal that arrives while a turn is blocked in a provider HTTP call finalizes the session (good) and then hangs the process on that join until the call returns. Worse, when it does return the stranded worker runs `_play`'s `else`/`finally`: a second `finalize()` writes another `session.ended` and re-saves `session.json` *after* the ones shutdown wrote. test_chat_server.py:1080 shows the shape - it has to defer `provider_control.release()` until after its assertions precisely because the stranded worker would rewrite the files.
  - Fix: Mark the chat's executor threads as ones the process need not wait for, or set a per-chat `shutting_down` flag that `_play` checks before finalizing/settling so a stranded turn writes nothing after shutdown, and give `shutdown()` a bounded join (e.g. `worker.shutdown(wait=True, cancel_futures=True)` with a timeout guard) rather than an unbounded implicit one at exit.
- **reviewer/src/swreview/chat/server.py:725** - `disposition` calls `_require_idle` and then `await run_in_threadpool(record_disposition, ...)`. The chat's state stays non-RUNNING across that await, so a concurrent `POST /messages` passes `_require_idle`, sets RUNNING and submits a turn on the chat worker while `record_disposition` is still mutating `run.session` and writing `session.json` + `report.md` from a different thread. The contract's Concurrency clause promises the server serializes per chat; the message and evidence endpoints do (no await between check and submit), this one does not.
  - Fix: Run `record_disposition` on the chat's own single-threaded executor (submit and await the future via `anyio.to_thread`/a wrapper around `_submit`) so it serializes with turns, or hold the chat in a short-lived `RUNNING`-equivalent guard across the await. Add a test that posts a disposition and a message concurrently and asserts one of them is 409.
- **reviewer/src/swreview/chat/server.py:837** - `PACKAGE_ERRORS` includes `ValueError`, `KeyError`, `LookupError` and `OSError`, and wraps everything `start_review` raises as `InvalidPackage` with the message "<run_dir> is not a readable evidence package". `start_review` also calls `provider.effort_mapping(effort)`, which raises `EffortNotSupportedError(ValueError)` (openai_provider.py:206) - the deliberately named fail-fast error of data-model s1 ("fails fast with a named error; it never silently downgrades"). The pane is then told the package is unreadable when the real cause is that the chosen model cannot express the requested effort, and FR-008 requires the error's real class plus a next action.
  - Fix: Catch the provider-side errors before the package ones: wrap only the package load in `PACKAGE_ERRORS` (or re-raise `EffortNotSupportedError` and the adapter's own error classes as `ProviderFailed`/a dedicated `InvalidEffort` `ChatError` carrying the real class name). Add a test posting an effort the fake adapter cannot map and asserting the error_class is not `InvalidPackage`.
- **reviewer/src/swreview/chat/server.py:598** - `development` only changes what `GET /health` lists. A release build (no `--dev`) advertises `providers: ["openai", "gemini"]` and then still accepts `POST /sessions {provider: "fake"}` and runs the scripted script, writing `Dry run...` text into a real run folder. FR-027's stated purpose is "so a shipped configuration can never produce fabricated findings"; today only the C# settings loader enforces that, and the component that actually produces the findings does not.
  - Fix: Make `_settings`/`create_session` refuse `ProviderName.FAKE` with `UnknownProvider` (400) when `self.development` is False, so `/health` and `/sessions` agree. Same for `GET /models?provider=fake`. Add tests for both on a non-development app.
- **reviewer/src/swreview/chat/server.py:965** - `run.close()` - which releases the live SOLIDWORKS bridge named pipe - is only reached from `_finalize_on_exit`, and that method returns early for any chat where `chat.finished` is True. So the ordinary paths (turn ends, `/stop`, a failed chat) never close the bridge: a US3 session's pipe handle is held for the life of the backend process. FR-019's "MUST stop both when the add-in unloads" is satisfied only because the process dies.
  - Fix: Close the run when the chat reaches a terminal state: call `chat.run.close()` from `_play`'s terminal branches (or from a small `_close_run(chat)` invoked wherever the chat settles ENDED/FAILED), guarded so a follow-up that reopens the session re-acquires a client. Assert in a test that the injected bridge factory's client saw `close()` after the session ended.
- **reviewer/src/swreview/chat/sessions.py:345** - `record_disposition` emits `{"finding_id": ..., "disposition": disposition.model_dump(...) if disposition else None}`. `chat-events.schema.json` requires `disposition` to be a `Disposition` object (`required: [finding_id, disposition]`, `$ref`), so the `None` branch would write a line that fails the contract the events-schema test enforces. It is unreachable today (`set_disposition` always sets one), which makes it a defensive branch that can only ever produce an invalid event.
  - Fix: Drop the conditional and emit `finding.disposition.model_dump(mode="json")` directly, or assert `finding.disposition is not None` first so a future regression fails loudly instead of writing a schema-invalid event.
- **reviewer/src/swreview/cli.py:1170** - `_scan_file` opens every file as UTF-8 with `errors="replace"`. A key written into a UTF-16LE file - the default encoding for `>` / `Out-File` redirection in Windows PowerShell 5.1, which is this project's primary shell - decodes to interleaved replacement characters, so neither the verbatim env value nor the `sk-`/`AIza` detectors match. `audit-secrets` reports "none" over a file that does contain the key. That is exactly the false green the command's own docstring says it exists to prevent, and T029 pins the loud-rather-than-green behaviour.
  - Fix: Sniff a BOM (or try UTF-8, then UTF-16LE/BE) per file, or scan the raw bytes for both the UTF-8 and UTF-16LE encodings of each secret and each detector pattern. Add a test that plants a key in a UTF-16LE file and asserts exit 1.
- **reviewer/src/swreview/chat/server.py:623** - `bridge.secret` from the `POST /sessions` body is stored on the chat and then never used: `_start_review` passes only `bridge.get("pipe")` (line 832) and `bridge/client.py` has no notion of a secret at all, so the named-pipe handshake is unauthenticated despite the contract carrying the field. It is also absent from the redaction set - `ChatServer.secrets` is built from `KEY_VARIABLES` only (line 587/104) - so an error or log line that ever quotes the request body would carry it in the clear. `public()` correctly withholds it, so nothing leaks today, but the guarantee is accidental rather than enforced.
  - Fix: Decide one way: either have the bridge client present the secret on connect (and add a contract line saying how), or remove `secret` from the `POST /sessions` body in `chat-api.md`. Either way, add the supplied bridge secret to the per-chat redaction set the moment it is accepted so `self.redact` masks it, and add a test asserting an error body built from a request carrying a bridge secret does not contain it.
- **reviewer/src/swreview/chat/server.py:958** - `_finalize_on_exit` finalizes a mid-flight chat but does nothing to stop the turn, so the stranded worker can rewrite what shutdown just wrote. It emits `turn.ended {reason: "error"}`, finalizes, and calls `chat.run.close()` -- which only closes the bridge client (runner.py:472) -- then `shutdown()` calls `worker.shutdown(wait=False)`, which does not cancel work in progress. ThreadPoolExecutor threads are non-daemon and joined at interpreter exit, so the process lingers until the provider call returns, `_play` then runs to completion and appends a second `turn.ended`/`session.ended` and re-saves `session.json` after the stream's terminator. Closing the bridge out from under a running turn also turns its next bridge tool call into a closed-pipe error. test_chat_server.py:1080 acknowledges this in a comment ("letting it run any earlier would rewrite the files just asserted on") rather than preventing it.
  - Fix: Set `chat.stop_requested.set()` at the top of `_finalize_on_exit` so the stranded turn stops at its next tool boundary, and mark the chat closed (e.g. a `ChatSession.closed` flag checked at the top of `_play`'s `else`/`except TurnStopped` branches) so a turn that finishes after shutdown does not emit a second `session.ended` or re-save the session. Then have test_chat_server.py:1080 release the gate BEFORE its assertions and assert the file is unchanged -- that is the version that actually pins the behaviour.
- **reviewer/src/swreview/cli.py:1081** - The set of provider key environment variables is written out four times, and two of the copies have a correctness consequence. `cli.py:1081 KEY_ENV_VARS`, `chat/server.py:104 KEY_VARIABLES`, `tests/conftest.py:26 LIVE_KEYS` and `tests/unit/test_cli_audit_secrets.py:48` all spell `("OPENAI_API_KEY", "GOOGLE_API_KEY", "GEMINI_API_KEY")`, while `agent/settings.py:330-332` hardcodes the same names a fifth time inside `_key_from_env`. If a fourth variable is ever supported, `audit-secrets` silently stops scanning for it and the backend silently stops redacting it -- a DRY violation that degrades into a secret leak, not a style issue.
  - Fix: Put one `KEY_ENV_VARS` in `agent/settings.py` beside `_key_from_env` (which should read from it), and import it in `cli.py` and `chat/server.py`. Leave the copies in `tests/conftest.py` and `test_cli_audit_secrets.py` as they are -- the latter already carries a comment saying the duplication is deliberate so a rename has to be made twice, which is a defensible test-side choice.
- **reviewer/tests/unit/test_chat_server.py:60** - The three chat test modules each re-declare the same helpers rather than sharing them: `EVIDENCE_ARGUMENTS` and `DRAWING_FINDING_ARGUMENTS` appear verbatim in test_chat_server.py:60-73, test_chat_sessions.py:51-62 and (the evidence one) test_events_schema.py; `call`/`turn` appear in test_chat_server.py:76-83 and test_chat_sessions.py:65-72; `wait_until` in test_chat_server.py:88 and test_chat_main.py:151; `events_of`/`start_session` in test_chat_server.py:210/240 and test_chat_main.py:172/187. The tool-argument dicts are the risky pair -- when a tool signature changes, three separate literals have to be edited and a missed one fails as an opaque validation error in an unrelated module.
  - Fix: Move the scripted-turn helpers and the tool-argument fixtures into `reviewer/tests/support/` (a `scripts.py` beside the existing `contracts.py` and `packages.py`), and import them from the three modules. Keep `wait_until` per-module if the timeouts genuinely differ; the argument dicts and `call`/`turn` should be single-sourced.
- **reviewer/tests/unit/test_chat_server.py:1162** - `test_a_turn_with_no_tool_call_left_to_make_finishes_normally` does not test what its docstring claims. The docstring says "The stop flag is read at a tool boundary, so a turn that reaches none just ends", but the test never calls `POST /stop` -- it only asserts that an ordinary tool-free turn ends with reason `end`. The behaviour the docstring describes (Stop pressed on a turn that will reach no further tool boundary, so the 202 is honoured but `turn.ended` carries `end` rather than `stopped`) is the one real edge of FR-030's "next tool boundary" wording and is untested.
  - Fix: Hold the gate, `POST /stop`, release, and assert the documented outcome for a tool-free turn: 202 from `/stop`, the chat settles in `ended`, `session.json` has a non-null `ended_at`, and record what `turn.ended.reason` actually is. If the contract wants `stopped` even in that case, `_play` needs to consult `chat.stop_requested` after a normally-completed turn; if `end` is acceptable, say so in the test so the next reader is not misled.
- **reviewer/src/swreview/chat/server.py:821** - The `bridge.secret` the contract has the pane send is accepted and then dropped. `_start_review` reads `bridge = chat.bridge or {}` and forwards only `str(bridge.get("pipe") ...)`; the secret is stored on `ChatSession.bridge` (correctly never echoed, per the tested `public()`) and otherwise unused, so the named pipe is currently opened unauthenticated. T049 is the task that wires `secret` through `bridge/client.py`, so this is deferred rather than wrong -- but there is no marker in the code saying so, and `fake_chat_script`'s bridge probes already exercise the path.
  - Fix: Add a one-line comment at server.py:821 naming T049 as the task that will forward `bridge['secret']` to `BridgeClient`, so the dropped field is visibly deferred rather than looking like an oversight to whoever picks that task up.
- **reviewer/tests/unit/test_chat_sessions.py:496** - `assert isinstance(UUID(str(run_with_a_finding.session.session_id)), UUID)` is a tautology -- `UUID(str(x))` returns a `UUID` or raises, so the assertion can never be false in a way the preceding line has not already caught. It is the only non-load-bearing assertion I found in the module.
  - Fix: Delete the line, or replace it with the assertion the test's name implies is missing: that the emitted `disposition` body's `finding_id` matches a finding actually on `run.session` (the earlier `test_a_disposition_is_written_to_...` already does this, so deleting is the honest option).
- **reviewer/tests/unit/test_chat_server.py:480** - Three contract-adjacent cases have no test. (a) `POST /sessions` with an unknown `provider` -- `_settings` maps the `ValueError` from `from_env` to a 400 `UnknownProvider`, but only the `/models` route is tested for that (test at line ~455). (b) `POST .../disposition` while a turn is running returns 409 `TurnRunning` via `_require_idle`; T036's "disposition 200/409" is satisfied by the `IllegalTransition` case, but the concurrency refusal on this route is unexercised. (c) `--dev` is untested through either entry point -- `test_a_development_build_also_lists_the_scripted_provider` constructs the app directly, so neither the Typer `--dev` flag nor `__main__`'s `--dev` is proven to reach `create_app(development=True)`.
  - Fix: Add three short cases: `start_session(..., provider="acme-llm")` asserting 400/`UnknownProvider`; a gated turn plus a disposition POST asserting 409/`TurnRunning`; and one subprocess case in test_chat_main.py launching with `--dev` and asserting `GET /health` lists `fake`.

### settings-backend (12)

- **extractor/SwReview.AddIn/Review/ReviewHost.cs:201** - AnyTurnRunning() swallows every exception from IBackendClient.IsTurnRunning and treats "cannot ask" as "no turn running". A single slow or failed GET /sessions/{chat_id} (30 s HttpWebRequest timeout in BackendClient) therefore lets settings.save restart the backend under a live turn — and because of the must_fix above that restart is a TerminateProcess, so the turn dies without finalization. The comment justifies the choice for a backend that is down, but a transient failure is indistinguishable from a dead one here.
  - Fix: Distinguish BackendUnavailable (endpoint null / connection refused) from every other failure: treat only the former as "no turn", and answer anything else with error {error_class: "TurnRunning"} (or a distinct class) so the save is retried rather than silently destructive. Add a PageMessageTests case whose FakeBackend throws a transient exception from IsTurnRunning.
- **extractor/SwReview.AddIn/SwReviewAddIn.cs:449** - Report() appends the raw `failure` (Exception.ToString(), i.e. message plus every inner exception and stack) to %LOCALAPPDATA%\SwReview\logs\addin.log with no redaction. BackendProcess wraps failures as `new BackendStartException(Redaction.Redact(...), processId, failure)` keeping the *unredacted* original as InnerException, so ToString() re-prints whatever the redacted message just masked. This is the only log path in the add-in that is not redacted, against FR-015's "MUST be redacted from any error text".
  - Fix: Route Report through Redaction.Redact(failure, secrets) using the resolved key (the same list ReviewHost.Redact builds), and add a test that an exception carrying the key reaches addin.log masked.
- **extractor/SwReview.AddIn/Review/ReviewHost.cs:983** - Half-configured Gemini Enterprise input is discarded silently: Enterprise() returns null when project or location is blank, the save succeeds, and the page is answered `settings.saved` as if everything was stored. app.js:964 does the same on its side. An engineer who fills in Project and tabs past Location presses Save, sees "Saved.", and watches the field blank itself with no explanation.
  - Fix: Refuse the save with error {error_class: "InvalidSettings"} naming the missing field (the Python side already raises exactly that error for the half-set environment case, settings.py:360), and keep the page from clearing the field it refused. Add the case to PageMessageTests beside the other InvalidSettings rows.
- **extractor/SwReview.AddIn.Tests/ProcessLifetimeTests.cs:146** - T050's sentence "two hosts in one process get distinct pipe names and ports" is only half covered, and the covered half is tautological: both stub backends are told which port and token to print by the test itself, so Assert.NotEqual(a.Port, b.Port) proves nothing about the system. Pipe names are not tested at all (the class docstring at line 30 acknowledges the deferral to T047/US3).
  - Fix: Either narrow the assertion to what it really proves (two live BackendProcess instances keep independent endpoints and process ids) and leave a named TODO referencing T047 for the pipe-name half, or drive the real `--port 0` binding in the T037a subprocess test. Make sure the pipe-name clause is actually honoured when T047 lands rather than left behind a checked-off T050.
- **extractor/SwReview.AddIn/Settings/UserSettings.cs:234** - A settings file that cannot be read or carries an unsupported version yields Defaults() in memory; ReviewHost then clones *those defaults* on the next settings.save (ReviewHost.cs:434) and overwrites the file. The stored api_key_protected, terminal_cli, python and run_root of a v2 file (or of a file that was momentarily locked) are destroyed by an ordinary Save, with only the earlier `status` banner as warning. PageMessageTests.SettingsSavePreservesFieldsThePageDoesNotOwn covers the healthy path only.
  - Fix: Record on SettingsLoadResult whether the file was *unusable* as opposed to merely missing/normalized, and have ReviewHost refuse settings.save in that state with an error naming the file (or back the original up before overwriting). Add a PageMessageTests case for the v2 file.
- **extractor/SwReview.AddIn/Review/BackendLocator.cs:98** - FindOnPath will happily return a .cmd or .bat launcher, and FromOverride accepts one too, but ChildProcess.StartSuspended passes the path as CreateProcess' lpApplicationName — which cannot execute batch files (it requires cmd.exe as the application). A workstation whose `uv` or `swreview` on PATH is a shim gets ERROR_BAD_EXE_FORMAT surfaced as "the backend could not be started: ...", pointing at Python rather than at the shim, which is exactly the failure mode the locator's own docstring says it exists to prevent.
  - Fix: Either restrict the search to .exe, or detect .cmd/.bat in BackendProcess and launch them through %COMSPEC% /c with correct quoting. A unit test over BuildCommandLine plus a .cmd stub would pin it.
- **extractor/SwReview.AddIn/Review/BackendProcess.cs:296** - When the first stdout line is not a handshake, the line is never written anywhere — not even to the redacted log. The reply and the log both say only "the backend's first stdout line was not the {port, token} handshake", so the engineer has no evidence of what the backend actually printed. The stated reason (it might contain the key) does not apply to the log, which is written through Redaction.Redact with the key registered as a secret, exactly as every stderr line already is.
  - Fix: Write the offending line to the log via the existing redacted Write() (keeping it out of the exception message and out of the page), and assert in BackendProcessTests.AnUnparseableFirstLineIsReportedWithoutEchoingIt that the log contains the line with the key masked.
- **extractor/SwReview.AddIn/Review/BackendClient.cs:180** - StartOptions never passes `--dev`, which reviewer/src/swreview/chat/__main__.py:151 defines and server.health (server.py:598) uses to list `fake` among the providers (FR-027). A development-build pane therefore offers and saves `fake` while the backend it launched reports a provider set that excludes it — the two halves of the FR-027 affordance disagree, and any future consumer of /health's provider list (or a diagnostic) will be misled.
  - Fix: Pass `--dev` when ReviewHostOptions.BuildMode is Development (thread the mode into BackendClient), and assert the flag in BackendProcessTests.TheAllowedOriginAndRunRootAreOnTheCommandLine alongside --allow-origin/--run-root.
- **extractor/SwReview.AddIn.Tests/BackendProcessTests.cs:89** - Assert.Contains("3", failure.Message) is near-vacuous in both the timeout test (line 89) and the early-exit test (line 110): the message always embeds stub.LogPath, a temp path containing a 32-hex GUID, so a "3" is present with ~87% probability regardless of whether the timeout seconds or the exit code were ever formatted in. The clause of T033 being checked ("times out with a clear error", "reports its exit code") is effectively unasserted.
  - Fix: Assert the surrounding phrase instead: Contains("within 3", ...) / Contains("3 seconds", ...) for the timeout, and Contains("exited with code 3", ...) for the early exit.
- **extractor/SwReview.AddIn.Tests/UserSettingsTests.cs:190** - T026 asks for the key to be "protected with DPAPI CurrentUser". The tests prove only round-trip and tamper-detection: an implementation using DataProtectionScope.LocalMachine (readable by every account on the box) or one passing no entropy would pass every assertion here, including SavedFileNeverContainsTheKeyInTheClear. The entropy binding at UserSettings.cs:292/322 is likewise unasserted.
  - Fix: Add a test that a blob protected with different entropy (ProtectedData.Protect(plain, otherEntropy, CurrentUser), base64'd into ApiKeyProtected) resolves to Key==null with a non-null Error - that pins the application-specific entropy. The CurrentUser-vs-LocalMachine scope is not reliably observable in-process (CryptUnprotectData ignores the flag), so note it in the test doc comment rather than pretending to test it.
- **extractor/SwReview.AddIn/Review/BackendProcess.cs:560** - DefaultHealthProbe is the code that actually runs in production, and every test injects options.HealthProbe instead, so it has no coverage at all. Untested properties that matter: the token goes in the Authorization header and never in the URL (chat-api.md), request.Proxy = null so a corporate proxy is not consulted for 127.0.0.1, a non-200 counts as unhealthy, and a connection refusal is retried rather than thrown.
  - Fix: Add a BackendProcessTests case using a System.Net.HttpListener bound to http://127.0.0.1:<free port>/ : assert the received request path is /health with no query string, that the Authorization header is "Bearer <token>", and that a 503 response makes WaitForHealth keep polling until it times out.
- **extractor/SwReview.AddIn/Review/ReviewHost.cs:455** - Save() catches TurnRunningException and BackendStartException around Backend.Restart and degrades gracefully, but any other exception from Restart (e.g. Win32Exception from CreateProcess, ObjectDisposedException from a disposed supervisor) escapes into Receive's generic catch. The settings file has already been written and _settings already swapped, yet the page gets only error{HostError} and never settings.saved, so the pane keeps rendering the previous settings while the store and the backend hold the new ones. PageMessageTests exercises only the TurnRunning and BackendStart paths.
  - Fix: Catch Exception around the Restart call (redacted into a status error) so settings.saved is always sent once the file is written, and add a PageMessageTests case whose FakeBackend.Restart throws an arbitrary exception, asserting settings.saved is still replied and a status{stage:"error"} explains the backend did not come back.

### host-page-control (13)

- **extractor/SwReview.AddIn/Review/ReviewPage/app.css:103** - `.field { display: block; }` is an author-origin declaration, so it beats the user-agent `[hidden] { display: none }` rule regardless of specificity. `#enterprise` is `<fieldset id="enterprise" class="field">` and `renderProviders()`/the provider `change` handler hide it with `ui.enterprise.hidden = currentProvider() !== 'gemini'` (app.js:876, 1102). The Gemini Enterprise project/location fieldset is therefore permanently visible for every provider, and the page's only defence for `hidden` elsewhere (`.banner`, `.panel`, `.details`) is the accident that those rules happen not to set `display`.
  - Fix: Add `[hidden] { display: none !important; }` near the top of app.css (and to the Terminal page's CSS when T060 lands), so the `hidden` property the scripts use is authoritative regardless of what a class rule sets.
- **extractor/SwReview.AddIn/Review/ReviewHost.cs:421** - `Save` pushes an unsolicited `models` message (`Send("models", null, ...)`) before an `UnknownModel` refusal. `contracts/pane-host-messages.md`'s "Host → review page (unsolicited)" table is exhaustive and lists only `status`, `document.changed` and `backend.stopped`; `models` appears only as the *reply* to `models.list`. The page relies on the undocumented push (app.js:136-141 `case 'models'` with a `payload.provider` guard the reply shape does not carry). ReviewPageContractTests only passes it because its HostToPage set is harvested from backticked spans in the reply column.
  - Fix: Either add a `models` row to the unsolicited table in `contracts/pane-host-messages.md` (documenting the `{provider, models}` payload) or drop the push and let the page call `models.list` itself when it sees `UnknownModel`. Pick one before T060 copies the pattern into the Terminal page.
- **extractor/SwReview.AddIn/Review/ReviewPage/app.js:344** - `scheduleReconnect` never gives up. After a `settings.save` the host restarts the backend (new port, new token); the page learns the new endpoint through `requestInit(false)` but `state.chatId` still names a chat that died with the old child, and the open stream is never closed. The reconnect loop then re-issues `GET /sessions/<dead chat>/events`, gets a 404, throws, and reschedules forever at the 15 s ceiling, repainting "…Reconnecting…" indefinitely with no way out but a pane reload. The same loop runs if the backend exits without a `backend.stopped` message.
  - Fix: Treat a 4xx on the events stream as terminal (404/401 in particular): stop the loop, show an error card with Retry/Open Settings/View log, and clear `state.chatId`. Also `closeStream()` and clear the chat in `saveSettings`'s success path, since the chat is known to be gone once the backend is restarted.
- **extractor/SwReview.AddIn/Review/ReviewPage/app.js:145** - FR-008 requires every error to be shown with its class and a next action. Several paths show only the bare message on the banner or the card status line, dropping `error_class` and offering nothing: `handleUnsolicited`'s `case 'error'` (`showBanner(asError(payload).message)` - `asError` computes `errorClass` and it is discarded), the `report.open`/`folder.open`/`log.open` catches (app.js:1092, 1097, 1102) and `requestInit`'s catch (app.js:1030). Only `startReview`, `sendFollowUp` and `stopTurn` build a proper `render.errorCard`.
  - Fix: Route these through `render.errorCard({error_class, message, retryable})` (or a banner variant that prints the class and offers Open Settings / View log), so the three next actions FR-008 names are present for host-side failures too - `NotFound` on report.open and `BackendUnavailable` on init are exactly the cases where "View log" / "Open Settings" is the engineer's next move.
- **extractor/SwReview.AddIn.Tests/ReviewPageContractTests.cs:110** - T042 asks that "every message type it sends **and handles**" be checked against the contract. Rule 1 covers what the page sends, but rule 3 - the only check over handled types - filters literals through `DottedType` (`^[a-z][a-z0-9_]*(\.[a-z0-9_]+)+$`), so every non-dotted type the page handles is invisible to the scan: `case 'models'`, `case 'error'`, `case 'status'` in `handleUnsolicited`, and the `'init'`/`'ok'`/`'settings'`/`'settings.saved'` replies it consumes. A typo in `case 'modles'` would pass all four tests.
  - Fix: Add a rule that harvests the `case '<type>':` labels out of `handleUnsolicited`/`onChatEvent` (a `case\s*'([a-z][a-z0-9_.]*)'` scan is enough given the page's shape) and asserts each is in `Contract.HostToPage` or `Contract.ChatEvents`, so the "handles" half of the task sentence is actually enforced.
- **extractor/SwReview.AddIn/Review/ReviewPage/app.js:330** - `onFrame` calls `onChatEvent(event)` inside the `ReadableStream` reader's promise chain with no guard. A throw from any renderer - e.g. `entry.card.parentNode.replaceChild(...)` at app.js:485 and 520 when the card was removed by an intervening `resetTranscript()`, or a malformed body from a future event type - propagates out of `read()` into `openStream`'s `.catch`, which tears down a perfectly healthy live stream and drops into the reconnect backoff. One unrenderable event costs the rest of the turn's streaming.
  - Fix: Wrap the per-event dispatch: `try { onChatEvent(event); } catch (error) { appendCard(render.errorCard({error_class: 'RenderError', message: String(error && error.message || error), retryable: false})); }`, and null-guard the two `parentNode.replaceChild` calls (fall back to `appendCard`).
- **extractor/SwReview.AddIn.Tests/ReviewPageContractTests.cs:246** - T041 names two rules the static scan is perfectly able to check and does not: the stream must be read with `fetch`+`ReadableStream` and "never with `EventSource`", and "the token never appears in a URL". Today the only thing standing behind either is a prose comment in app.js (lines 18-23); a future edit that switches to `new EventSource(url + '?token=' + token)` passes every test in this file, and the CSP would not stop it because the origin is the same.
  - Fix: Add two scans beside the key-field scan: reject `\bEventSource\b` in any page script, and reject a token reaching a URL (e.g. any string concatenation or `URLSearchParams` naming `token`/`access_token` used in a `fetch`/`Navigate` argument - the cheap version is to reject the literal substring `token=` and `EventSource` outright, both of which the page legitimately never needs).
- **extractor/SwReview.AddIn/TaskPaneControl.cs:260** - T043's origin guard has no test. `OnNavigationStarting`/`OnNewWindowRequested` cancel anything outside `https://swreview.invalid/`, and this is the control that stops a link in model-authored text from navigating the pane with the backend token live in the renderer - yet nothing asserts it, and the predicate `IsOwnPage` (line 299) is a private static that would be trivial to cover.
  - Fix: Make `IsOwnPage` internal (with `InternalsVisibleTo`) or public static and assert the table directly: the page URL and `about:blank` allowed; `https://swreview.invalid.evil.com/`, `http://swreview.invalid/`, `file:///c:/`, `https://example.com`, null and empty refused. The `OffscreenReviewPage` harness already exists if you would rather drive a real cancelled navigation end-to-end.
- **extractor/SwReview.AddIn.Tests/ReviewPageContractTests.cs:124** - The class doc claims the three page behaviours it cannot prove - reconnect sends the highest seen `seq`, event 501 evicts event 1, the follow-up box is disabled between `tool.started` and `turn.ended` - "are covered by the host-side tests plus quickstart Scenario 1". They are not: all three live entirely inside app.js's closure and no host-side test in this assembly touches them. tasks.md leaves this as an open question ("Behavioural tests for the two pages"), and the comment reads as if the question had been answered. A false coverage claim in a test file is worse than an acknowledged gap, because the next reader stops looking.
  - Fix: Reword the comment to state the gap plainly, and reconsider the trade-off now that T042a has built a real in-page harness: exposing the three values app.js already holds (`state.lastSeq`, `state.events.length`, `ui.followupText.disabled`) on a debug-only accessor would let `OffscreenReviewPage.Evaluate` drive `onFrame`-shaped input and assert all three for a few dozen lines.
- **extractor/SwReview.AddIn.Tests/ReviewHostTests.cs:578** - The `ReviewWorld` harness duplicates `PageMessageTests`' harness: `FakeChannel` (line 722), `FakeBackend` (line 798), and the whole `Receive`/`Reply`/`AllPosted`/`LastPosted` plumbing (lines 655-693) all exist twice, ~120 lines. The file's comment defends the split ("they fake different seams"), which is fair for the fakes - but the JSON envelope plumbing is mechanical, identical, and is the part that will silently drift when the envelope shape changes: the two copies would then disagree about what a reply is and one suite would keep passing on a stale contract.
  - Fix: Pull only the transport plumbing into a shared base or helper (`PageWorld`: `Posted`, `Receive(type,id,payload)`, `Reply`, `AllPosted`, `LastPosted`, `AssertNothingPostedContains`, `FakeChannel`) and let each file keep its own fakes and its own `Open()`. That keeps the stated independence of the fixtures while removing the duplication the project's DRY rule targets.
- **specs/002-task-pane-assistant/contracts/pane-host-messages.md:46** - Contract drift the T042 scan structurally cannot see. The `review.start` row documents the payload as `{}`, but T041/FR-028 require Retry to carry `retry_of` - app.js sends `{retry_of: chatId}` (app.js:572) and ReviewHost reads it (ReviewHost.cs:652) and `ARetryCarriesTheChatItIsRetrying` tests it. The scan compares message *types* only, so every payload-field disagreement between page and host is invisible; this one is real today.
  - Fix: Update the `review.start` row to `{retry_of: chat_id | null}` (and note the host defaults it to null). If payload drift is worth catching generally, the same table parser already in `PaneContract.Load` could be extended to pull the backticked field names out of the payload column and check that the page names no field the row does not list.
- **extractor/SwReview.AddIn/TaskPaneControl.cs:215** - `_initializing` is set to true on entry and never reset, so `InitializeAsync` is a one-shot. After the fallback path runs (missing runtime, a transient `EnsureCoreWebView2Async` failure), there is no way to retry in that SOLIDWORKS session even though the cached faulted environment task is the only thing that would need replacing - the engineer must restart SOLIDWORKS after installing the runtime the fallback panel just told them to install.
  - Fix: Reset `_initializing = false` in a `finally`, and/or give the fallback panel a "Try again" button that clears `_environment` and re-runs `InitializeAsync`. If one-shot is deliberate, say so in the comment - the current code reads like an in-flight guard rather than a permanent latch.
- **extractor/SwReview.AddIn/TaskPaneControl.cs:313** - `ShowFallback` drops the failed `WebView2` control without disposing it: `AttachReviewPageAsync` adds the view to `_reviewTab` and assigns `_reviewView` before awaiting `EnsureCoreWebView2Async`, and on failure `ShowFallback` sets `_reviewView = null` and `Fill` calls `page.Controls.Clear()`, which removes but does not dispose. A half-initialized WebView2 can already own a browser process, and `Dispose(bool)` can no longer reach it because the field was nulled.
  - Fix: Capture the view in a local, and dispose it on the failure path (or add the control to the tab only after `EnsureCoreWebView2Async` succeeds, keeping the "Starting the review page..." note in place until then).

## Feature 002 Phases 5-6 (tool service, terminal, MCP)


### tool-service (16)

- **extractor/SwReview.AddIn/ToolService/InProcPipeServer.cs:502** - `Answer()` queues every syntactically valid request onto the application-thread pump before the secret is looked at: the `ISecretPolicy` check lives in `SwBridgeDispatcher.Dispatch`, which runs inside the `Control.BeginInvoke` delegate. So a caller holding no secret at all (or the general-chat CLI spamming `interference`) still occupies the SOLIDWORKS UI thread once per refused request, serialized behind real work. Serve/PROTOCOL.md's claim that 'a refused request never reaches SOLIDWORKS' is true of SOLIDWORKS *calls* but not of the SOLIDWORKS *thread*, and the refusal is a pure ordinal string compare that needs neither.
  - Fix: Give `InProcPipeServerOptions` the `ISecretPolicy` (or a `Func<string?, string, bool>`) and refuse in `Answer()` on the reader thread, returning `BridgeResponse.Failed(request.Id, SwBridgeDispatcher.UnauthorizedError)` before `_queue.Add`. Keep the dispatcher's own check as defence in depth so `BridgeSecretPolicyTests` still pin it.
- **extractor/SwReview.AddIn/SwReviewAddIn.cs:350** - `reviewOptions.Bridge` is only set once `ToolServiceHost.Start` returns, and that start walks the whole component tree on the application thread. The window is not 'before the first document' as the comment says — it is 'until the attach finishes', which on a large assembly is tens of seconds starting exactly when the engineer opens a document and clicks Review. A review begun in that window posts `POST /sessions` with no `bridge`, so `chat/server.py` passes `bridge=False` and the three live tools are never registered at all. Nothing tells the engineer: the run simply has no capture/measure/interference, and those checks are absent from coverage rather than recorded as failed, which is weaker than the `document no longer open` path this same task took care to make visible.
  - Fix: Either post a `status` message to the Review page while the tool service is attaching and keep `review.start` disabled until `ToolService` answers non-null, or have `ReviewHost` wait briefly on the gate before building `NewSessionRequest` and, if the attach has not finished, say so in the status line it already sends.
- **reviewer/src/swreview/chat/server.py:675** - `bridge = body.get("bridge") or None` accepts anything truthy. A body with `"bridge": "swreview-abc"` (a string) reaches line 942 as `bridge.get("secret")` and raises `AttributeError`, which surfaces as an unexplained 500 on `POST /sessions` rather than the 400 every other malformed field gets (`retry_of`, `provider`, `run_dir` are all validated). A well-formed object carrying `pipe` but no `secret` is worse: `bridge_secret` becomes `None`, the client omits the field, and the in-process host answers `unauthorized` on the very first call — a misconfiguration that only shows up as a dead bridge partway into the run.
  - Fix: Validate alongside the other fields: require `bridge` to be a Mapping with a non-empty string `pipe`, reject anything else with the same 400 path used for `provider`, and reject a `bridge` without a non-empty `secret` (the console host is reached through the CLI, never through `POST /sessions`, so a secret-less bridge over HTTP is always a mistake). Add a `test_chat_server.py` case per rejection.
- **reviewer/src/swreview/chat/server.py:942** - The bridge secret arrives in the request body but is never added to the run's redaction set — `self.secrets` is `environment_secrets()` plus the HTTP token, both established at process start. `self.redact` therefore masks the provider key and the bearer token but not the bridge secret, so any path that puts request state into text (a `bridge_factory` that raises with its arguments in the message, a future traceback log of the session body) writes it to the backend log in the clear. The C# side is explicit that the secret must never be logged; the Python side only guarantees it by the client never formatting it.
  - Fix: Extend the run's redactor with the session's bridge secret where `start_review` is called — pass a `redact` that wraps `self.redact` with `redact(text, (*self.secrets, bridge_secret))` for that chat — and assert in `test_chat_server.py` that the secret cannot appear in a reported error.
- **extractor/SwReview.AddIn/ToolService/InProcPipeServer.cs:509** - A line that fails `BridgeCodec.ReadRequest` is answered and returned before the request ever reaches `ToolServiceRequestLogger`, so nothing about it is written to the tool-service log. That contradicts the logger's own stated property ('Every request produces a line, including refusals and errors, because an audit of what did *not* happen is only as good as its record of what was attempted') and leaves the SC-004 artifact with no trace of a client sending garbage at the pipe — the one thing an audit most wants to see.
  - Fix: Log the protocol error from `Answer()` through the same `Action<string>? Log` the timeout and unavailable paths already use (`request malformed: <BridgeProtocolError.Message>`), which carries no client-supplied value, and add a case to `AMalformedLineIsAnsweredWithAnEmptyIdAndTheListenerSurvives` asserting the line is written.
- **extractor/SwReview.AddIn/ToolService/ToolServiceHost.cs:331** - `ToolServiceLog.Write` swallows `IOException` and `UnauthorizedAccessException` with empty catch blocks. This file is the artifact SC-004 is audited against (T047), so a log folder that is read-only, full, or held by another process produces a review that looks entirely normal and an audit with nothing in it — the reviewer cannot distinguish 'no gated member outside the reader set' from 'no records at all'. `ToolServiceLog`'s constructor calls `Directory.CreateDirectory` and will throw for a wholly unusable folder, but a mid-run failure is silent.
  - Fix: Keep the swallow (a log must not fail a review) but make it loud once: latch a `bool _failed` plus the first exception on `ToolServiceLog`, expose it, and have `ToolServiceHost.Start`/`ToolServiceGate` report it through the same `Report` delegate a failed start uses, so the SC-004 audit in T051 is told the log is incomplete rather than reading an empty file as a pass.
- **reviewer/src/swreview/bridge/client.py:249** - The `BridgeOpenError` text is hard-coded to 'the bridge circuit is open after three consecutive failures', but the circuit now has three ways to open and two of them are not three failures: the host's own `circuit_open`, and a single `BridgeUnauthorizedError` (line 271, `opened=isinstance(exc, BridgeOpenError | BridgeUnauthorizedError)`). An engineer reading the coverage report after a wrong-secret run is told SOLIDWORKS failed three times when in fact nothing ever ran. The `(last: ...)` clause carries the real cause, so the sentence in front of it is actively misleading rather than merely vague.
  - Fix: Record why the circuit opened in `_fail` (a small enum or the opening exception type) and phrase the message from it: 'after three consecutive failures', 'because the host reported circuit_open', 'because the tool service refused this run's secret'. Add a `test_bridge_client.py` assertion on the unauthorized wording.
- **extractor/SwReview.AddIn/SwReviewAddIn.cs:296** - `entity.show` now prefers `_toolService?.Session`, which is bound to whichever document was active when the tool service attached and is never re-attached (`ToolServiceGate` starts exactly once per add-in instance, by design). The previous code attached per Show with `documentPath: null`, i.e. against whatever is active now. After the engineer opens a second assembly, Show resolves and selects inside the first — still-open, possibly hidden — document, and `SelectByID2` may well report success, so the engineer sees 'shown' with nothing highlighted. T048 does say `entity.show` uses the same scope, so the direction is right; what is missing is the check that the scope still matches what the engineer is looking at.
  - Fix: Before using the tool service's session, compare `_toolService.DocumentPath` with `CurrentDocument()?.Path` (ordinal, case-insensitive) and fall back to the per-Show `SwSession.Attach` when they differ — or keep the scope and have `entity.shown` report `ok: false` with a message naming the document the reference belongs to, which the `full_path` field from T039 already has room for.
- **extractor/SwReview.AddIn/ToolService/InProcPipeServer.cs:611** - The accept loop `return`s permanently when `CreateInstance()` throws. With `MaxServerInstances = 4` (line 325), the listening instance counts toward the limit, so the 4th simultaneous client connection makes the creation of the next listener throw IOException ("All pipe instances are busy"), the accept thread exits for good, and the tool service accepts no client ever again - even after every client disconnects. Only a log line records it; `Dispose` is the only way out, i.e. unload the add-in. The comment at line 320-324 claims the consequence is "a refused connection is an opaque failure at the client", which is materially milder than what the code does. The same `return` also makes any transient creation failure terminal.
  - Fix: On a creation failure, `continue` after a short backoff (and re-try when a connection closes) instead of returning; keep the hard `return` only for `_stopping`. Either raise `MaxServerInstances` or correct the comment to say the ceiling is 3 concurrent clients plus the listener. Add a test that opens `MaxServerInstances` clients at once, closes them, and proves a later client still connects.
- **extractor/SwReview.AddIn.Tests/InProcPipeServerTests.cs:60** - No test ever holds two pipe connections open at the same time - every test uses a single `Connect(...)`. Yet "two clients exist by design - the review backend holds the review secret and the CLI's MCP server holds the general-chat one" is the stated premise of the whole two-secret design (class docstring, lines 33-36) and of `MaxServerInstances`. The T046 clause "concurrent requests serialize in arrival order" is proved only through direct `server.Answer(...)` calls on three threads, which bypasses the accept loop, the per-connection reader/writer threads and the multi-instance pipe creation entirely. The one part of the transport the two-client design depends on is untested.
  - Fix: Add a test that opens two `PipeClient`s simultaneously, sends `interference` on the review-secret connection and on the general-chat connection, asserts one `ok`/one `unauthorized`, and (with a manual `FakeAppThread`) asserts the two connections' requests serialize through the single pump in arrival order.
- **extractor/SwReview.AddIn/ToolService/ToolServiceHost.cs:526** - `ToolServiceHost.Start` mints two secrets and hands them to a private constructor as two adjacent `string` positional parameters (`reviewSecret, generalChatSecret`, lines 416-417 / 524-527), which then populate `ReviewBridge` and `GeneralChatBridge`. Swapping those two arguments would give the CLI's generated profile the secret that authorizes `interference` - exactly the failure the scoping exists to prevent - and no test would fail. `Start` needs a live `ISldWorks`, so the composition is untestable as written, and `ToolServiceWiringTests`' `FakeToolService` only proves the *gate* routes whatever the host says. `NewSecret` (entropy, base64url, distinctness) is likewise untested.
  - Fix: Extract the composition below the attach (mint two secrets -> `ScopedSecretPolicy` -> dispatcher chain -> `InProcPipeServer`) into an `internal static` method taking a `BridgeServices` and an `IAppThreadInvoker`, and test it with the existing fakes: assert `ReviewBridge.Secret != GeneralChatBridge.Secret`, that a request carrying `ReviewBridge.Secret` gets `interference` through, and that one carrying `GeneralChatBridge.Secret` gets `unauthorized`.
- **extractor/SwReview.AddIn.Tests/InProcPipeServerTests.cs:258** - `TheLateDelegateCannotWriteAStaleResponse` does not test what its name claims. I deleted the `if (_abandoned) { return; }` guard from `AppThreadCall<T>.Publish` and this test stayed green - only `AnAbandonedCallNeverPublishesItsResult` (line 285) went red. The property it asserts (a later request gets its own answer) is structurally guaranteed by one `AppThreadCall` per request plus one queue, not by `Abandon`, so the test would pass against an implementation with no abandon semantics at all. The T046 clause is still covered, but by the unit test next door, and the misnamed test invites someone to delete the real coverage believing this one has it.
  - Fix: Either rename it to what it proves (a timed-out request does not desynchronise the connection) or strengthen it: expose the in-flight `AppThreadCall` (or count publishes) and assert `Completed == false` / `Abandoned == true` for the stale call after `ReleaseOne()`, so the guard's removal turns this test red too.
- **extractor/SwReview.AddIn/ToolService/InProcPipeServer.cs:276** - The comment justifying the 120s default says it is "deliberately longer than the Python client's own 60s (reviewer/src/swreview/bridge/client.py, DEFAULT_TIMEOUT_S), so the two sides cannot disagree about whether a call is still running". `NamedPipeTransport.__init__` stores `timeout_s` but never applies it - `open(self.path, "r+b", buffering=0)` and the byte-at-a-time `_read_line` block forever - so the client has no timeout at all and the stated invariant is imaginary. (The desync it worries about is actually prevented by the request-id check in `_exchange`, which is real.) A false invariant in a comment is worse than none: the next person will reason from it.
  - Fix: Either say what is actually true ("the client blocks until the host answers; the host's bounded wait is what guarantees an answer arrives, and the id check in `_exchange` is what guarantees it is the right one"), or make `NamedPipeTransport` honour `timeout_s` - the latter is a feature-001 change and probably out of scope here.
- **extractor/SwReview.AddIn/SwReviewAddIn.cs:296** - `() => _toolService?.Session ?? SwSession.Attach(...)` pins `entity.show` to the tool service's session for the rest of the SOLIDWORKS session. That satisfies T048's "entity.show uses the same scope", but it silently drops the adaptive behaviour the fallback comment still describes: `SwEntityResolver` calls `session.Document` (SwEntityResolver.cs:77), so once the tool service has attached to document A, every Show acts on A even after the engineer closes it or switches to B. With a closed document that is a dead COM pointer on the application thread. No test covers it - `ToolServiceWiringTests.EntityShowSeesTheToolServiceScopeOnceItIsListening` asserts reference identity only, never what happens when that scope's document is gone.
  - Fix: Confirm the intended behaviour with the spec owner, then pin it: a test that the resolver reports a clean failure (not an unhandled COM exception on the pane thread) when the tool service's document has been closed, and - if switching documents should re-target Show - either restart the gate on `ActiveDocChangeNotify` or keep the per-Show attach when the run's package document differs from `IToolServiceAccess.DocumentPath`.
- **extractor/SwReview.AddIn/ToolService/ToolServiceHost.cs:108** - The logger's contract says "Every request produces a line, including refusals and errors, because an audit of what did *not* happen is only as good as its record of what was attempted" - but a line that fails `BridgeCodec.ReadRequest` is answered and returned inside `InProcPipeServer.Answer` before the queue, so it never reaches `ToolServiceRequestLogger` and leaves no trace in the SC-004 artifact. `AMalformedLineIsAnsweredWithAnEmptyIdAndTheListenerSurvives` asserts the wire answer but nothing about the log, so the gap is invisible. For an audit log whose job is recording attempts against the tool service, "someone sent 10,000 unparseable lines" is exactly the thing you want recorded.
  - Fix: Either log the parse failure in `InProcPipeServer.Answer` (id and error text only - never the offending line, which could carry a secret-shaped string), or soften the claim in the docstring to "every parsed request". Add the corresponding assertion to the malformed-line test.
- **extractor/SwReview.AddIn.Tests/ProcessLifetimeTests.cs:221** - `AConPtyChildIsInsideTheJobBeforeItsFirstThreadRuns` is still `[Fact(Skip = "T047/US3: no ConPTY child exists to start yet...")]` and throws `NotImplementedException`. It is the single skipped test in the C# run. The premise is now stale: `Terminal/ConPty.cs` and `Terminal/TerminalSession.cs` exist in the tree, so a ConPTY child does exist, and T050's explicitly named clause - the CreateProcess-suspended window in which a fast-exiting child escapes the job - is therefore unproven for the one child process it was written about. (Terminal-branch file; flagging, not touching.)
  - Fix: Hand this to the terminal branch: start the ConPTY child through `ChildProcess.StartSuspended` into the host's `JobObject`, assert `job.Contains(pid)` and that the marker file is still absent before `Resume()`, and un-skip. If the ConPTY path genuinely cannot use the job yet, update the skip reason to name the task that will, so it is not read as already-done work.

### mcp-server (12)

- **reviewer/tests/unit/test_mcp_server.py:273** - The bridge is tested only through the tool *list*. No test calls a `bridge_*` tool through the server, and the `bridge_factory` seam that `create_server` advertises ("a test passes its own", server.py:335) is never exercised - `create_server(run_dir, bridge_pipe="swreview-test")` in these tests builds a real `BridgeClient` against a pipe name that does not exist and then never calls it. That gap is exactly why the dropped secret above is invisible, and it also leaves untested that a bridge failure comes back as an error result with a `status: "error"` chat-log line (FR-022/SC-003 for the one tool group that can actually fail at runtime).
  - Fix: Add tests that pass a fake `bridge_factory`: (a) assert the factory is called with the pipe name and the secret; (b) call `bridge_capture` with a fake bridge that returns a capture and assert the result plus one `status: "ok"` chat-log line; (c) call it with a fake bridge raising `BridgeUnauthorizedError` and assert an error result plus a `status: "error"` line.
- **reviewer/tests/unit/test_mcp_server.py:556** - `test_swreview_mcp_never_takes_the_secret_itself` proves the rule with a substring check on typer help text (`assert "--bridge-secret " not in output`). Help text is wrapped and reflowed by typer/rich, so a future `--bridge-secret` option rendered at a line break, in a different column, or truncated would leave this assertion green while a secret became passable on the command line - the one thing contracts/cli-profiles.md and T056 forbid.
  - Fix: Assert behaviour rather than formatting: invoke `cli.app` with `["mcp", "--run-dir", str(run_dir), "--bridge-secret", "s3cret"]` and assert a non-zero exit (typer reports exit 2 for an unknown option), keeping the help-text check only as a secondary assertion.
- **reviewer/src/swreview/cli.py:1115** - `--bridge-secret-env` given without `--bridge-pipe` is accepted silently: the variable is resolved (and hard-fails if unset, which is confusing on its own) and then discarded, because `create_server` only builds a bridge when `bridge_pipe` is truthy (server.py:346). The CLI then runs with no `bridge_*` tools at all and says nothing, so a profile-generation bug that emitted the secret variable but lost the pipe name would look like a working session with a mysteriously smaller toolset.
  - Fix: In the `mcp` command, raise a `ValueError` (already in `HANDLED_ERRORS`, so it becomes one stderr line and exit 1) when `bridge_secret_env` is given and `bridge_pipe` is not, naming both flags; add a CLI test for it beside the existing unset-variable test.
- **reviewer/src/swreview/mcp/server.py:362** - `toolset.sink.cli = _client_name(context) or toolset.sink.cli` is sticky and never resets. Since `Toolset`/`ChatLogSink` are created once per `create_server`, a client that identified itself once and a later request whose `client_params` are absent both write the earlier name; more relevantly, the fallback quietly relabels rather than falling back to `UNKNOWN_CLI`. The chat log is what the SC-003 audit reads to say which CLI made a call, so a wrong `cli` is worse than `"unknown"`.
  - Fix: Set the field per call to `_client_name(context) or UNKNOWN_CLI`, or pass the name into `record`/`ToolCallRecord` for that call instead of mutating shared state; add a test for a client that sends no `clientInfo` asserting the line reads `"unknown"`.
- **reviewer/tests/unit/test_mcp_server.py:426** - SURVIVING MUTANT. `test_the_report_resource_appears_only_once_a_report_exists` calls `create_server(run_dir)` a *second* time (line 437) after writing `report.md`, so it only proves a freshly started server sees the report. The documented behaviour is stronger and is the whole point of the resource — server.py:265-268: "The report is checked per request rather than at startup because a review running in the Review tab writes one while the CLI is connected." I verified this is untested by patching `build_toolset` to snapshot `report.md`'s presence once per server instance and having `_resources` read that snapshot: all 26 tests still pass. A server that froze the decision at startup ships green.
  - Fix: Use ONE server for the whole test: inside a single `talk_to` scenario, `list_resources` (assert `[PACKAGE_SUMMARY_URI]`), then write `report.md` from inside the scenario (or via an `anyio.to_thread.run_sync`), then `list_resources` again and `read_resource(REPORT_URI)` on the same session.
- **reviewer/tests/unit/test_mcp_server.py:245** - Vacuously passable. `test_schemas_and_descriptions_are_the_canonical_ones` is a bare `for tool in talk_to(...)` with no non-empty guard, so a server offering zero tools passes it. Confirmed with an `emptylist` mutant (forced `Toolset.specs = ()`): only `test_bridge_tools_appear_with_a_bridge_pipe` and `test_missing_package_json_still_offers_every_tool` go red; this test and line 255 stay green. T058 names "schemas are the canonical schemas" as its own case, so it should fail on its own rather than lean on the list tests as a backstop.
  - Fix: Bind the list first and assert its size: `tools = talk_to(create_server(run_dir, bridge_pipe="swreview-test"), scenario); assert len(tools) == len(OFFLINE_TOOLS) + len(BRIDGE_TOOLS)`, then loop. Same one-line guard at line 261 with `len(OFFLINE_TOOLS)`.
- **reviewer/src/swreview/mcp/server.py:424** - `serve()` is never executed by any test. `_invoke_mcp` (test_mcp_server.py:489) monkeypatches `mcp_server.serve` away, so `configure_logging_redaction((bridge_secret,))` on line 424 has zero coverage in this feature. The redactor itself is well tested (test_provider_settings.py:623-758), but nothing proves the MCP process installs it — and this is precisely the process whose stderr carries the bridge secret and whose logs the branch notes say are collected off the workstation.
  - Fix: Add a test that runs `serve` with `mcp_server.configure_logging_redaction` and `mcp_server.stdio_server`/`anyio.run` monkeypatched, asserting the redactor was called with a tuple containing the secret before the server was created.
- **reviewer/tests/unit/test_mcp_server.py:305** - `UNKNOWN_CLI` is never asserted. chat_log.py:53-54 states "the `cli` of a client that did not say who it is; a line is never written without one", and `_client_name` (server.py:237-242) has an explicit two-branch `None` path, but `test_the_chat_log_names_the_cli_that_called` only covers a client that *did* introduce itself. An implementation that wrote `cli: null` or omitted the field for an anonymous client would pass. (A `nocli` mutant forcing `_client_name -> None` is killed by this test only because the fallback happens to be correct today; the fallback's own value is unpinned.)
  - Fix: One test: drive a session whose `client_info` is omitted (or monkeypatch `mcp_server._client_name` to return `None`) and assert `[e["cli"] for e in chat_log(run_dir)] == [UNKNOWN_CLI]`, importing `UNKNOWN_CLI` from `swreview.mcp.chat_log`.
- **reviewer/tests/unit/test_mcp_server.py:358** - `test_a_withheld_tool_called_by_name_is_refused` asserts the error result but never reads `chat-log.jsonl`, while its immediate neighbour `test_an_unknown_tool_name_is_a_result_and_is_logged` (line 345) does. SC-003 ("no missing call, including failures") applies to a refused withheld tool identically — an engineer reading the log should see that the CLI tried to call `check_interference_group`. Asymmetric coverage on the same code path.
  - Fix: Append one line: `assert [(e["tool"], e["status"]) for e in chat_log(run_dir)] == [("check_interference_group", "error")]`.
- **reviewer/tests/unit/test_mcp_server.py:301** - Two near-tautological assertions in the flagship chat-log test. `assert entry["elapsed_s"] >= 0.0` cannot fail: `record_call` clamps with `max(elapsed_s, 0.0)` at registry.py:403. And `assert entry["at"].endswith("Z") or "+00:00" in entry["at"]` accepts two shapes when chat_log.py:72 pins exactly one (`.isoformat().replace("+00:00", "Z")`) — the `or` branch is dead and would silently accept a regression to the non-`Z` spelling that a CLI or the Terminal tab might parse differently.
  - Fix: `assert isinstance(entry["elapsed_s"], float)` (plus `< TIMEOUT_S` if you want a bound), and `assert entry["at"].endswith("Z")` with a `datetime.fromisoformat(entry["at"].replace("Z", "+00:00"))` round-trip to prove it parses.
- **reviewer/src/swreview/mcp/server.py:361** - The `calling` lock and the shared-sink `cli` mutation on line 362 are untested: no test ever issues two overlapping `tools/call` requests on one session. The comment says the lock exists because "the tools are blocking and the sink is shared" — but `ChatLogSink.cli` is a mutable field on a single shared `Toolset`, so if the lock were dropped, two concurrent calls could attribute a log line to the wrong CLI, and two `handle.write` calls could interleave. Low blast radius (stdio, one client) but it is an explicit correctness claim with no test.
  - Fix: One test: in a single `talk_to` scenario, `anyio.create_task_group()` firing two `client.call_tool` calls for different tools, then assert `chat-log.jsonl` has exactly two well-formed lines with the two expected tool names and the right `cli`.
- **reviewer/tests/unit/test_mcp_server.py:408** - The contract's closing sentence — `contracts/mcp-toolset.md`: "Prompts: none in v1" — has no assertion. `create_server` (server.py:379-391) registers no `on_list_prompts`, so a CLI issuing `prompts/list` gets a method-not-found error rather than an empty list. Whichever is intended, nothing pins it, and a future hand-off of prompt support would not be noticed by this suite.
  - Fix: Add one test in the resources section asserting the intended behaviour for `client.list_prompts()` — either `pytest.raises(MCPError)` (method not registered) or `== []` — with a comment citing the contract line.

### terminal (11)

- **extractor/SwReview.AddIn/Terminal/TerminalPage/term.js:214** - The page consumes `init` CLI fields the contract does not define. contracts/pane-host-messages.md's `ready` row is `init {clis: [{name, found, version, path, minimum}], last_choice}`, but term.js reads `cli.status` (line 214), `cli.display_name` (222), `cli.message` (289) and `cli.install_steps` (301). The page degrades gracefully to `found`, but the install-steps view that FR-023 requires only has content when the host sends `install_steps`/`message`, which no contract row obliges it to send — and the host that will answer this message does not exist yet (finding 1), so the shape is unpinned in both directions.
  - Fix: Update the `init` row in contracts/pane-host-messages.md to the row the page actually needs (`{name, display_name, status, found, version, path, minimum, install_steps, message}`), and extend the TerminalPageContractTests payload check to assert the page reads only documented fields, so page and host cannot drift.
- **extractor/SwReview.AddIn/Terminal/TerminalPage/term.js:83** - `request()` registers a pending entry and never times out, and `start()` (line 441) shows a banner only on rejection. If the host throws before replying, or replies without echoing the id, the promise never settles: the dropdown stays empty, Start stays disabled and the engineer is given no message at all — a silent failure of exactly the kind the install-steps view exists to avoid.
  - Fix: Give `request()` a timeout (a few seconds for `ready`, longer for `terminal.start`) that rejects with a message naming the message type, and delete the pending entry when it fires.
- **extractor/SwReview.AddIn/Terminal/TerminalSession.cs:231** - `ExitCommand` defaults to `"exit\r"` for every session, and `Stop()` writes it before the grace period. For `cmd.exe` that is right; for the Codex TUI, typing `exit` at the prompt submits a chat turn to the model rather than quitting — so the last act of the stop path is to send the provider a prompt and pay for a turn, and the session then ends by `Kill()` plus the job three seconds later. T053's sentence is 'stop with exit command then process-tree termination'; the exit command has to be the CLI's.
  - Fix: Carry the exit command on `CliDefinition` (Codex: `/quit\r`, or Ctrl+C twice if that is what 0.115 honours — verify on the workstation in T063) and have the host set `TerminalSessionOptions.ExitCommand` from the definition, leaving `"exit\r"` only as the raw-process default the ConPTY tests use.
- **extractor/SwReview.AddIn.Tests/ProcessLifetimeTests.cs:221** - `AConPtyChildIsInsideTheJobBeforeItsFirstThreadRuns` is still `[Fact(Skip = "T047/US3: no ConPTY child exists to start yet...")]`, but T053 landed exactly that child: `TerminalSession.Start` calls `ChildProcess.StartSuspended(info, options.Job, tree)` with `PseudoConsole` set (TerminalSession.cs:347-360). The skip reason is now false, so T050's 'assigned to a job with kill-on-close before their initial thread is resumed' is asserted for the backend child and left unproven for the terminal child — the one the task singles out as the risky case.
  - Fix: Unskip it and implement the body against `TerminalSession`/`ConPty` (assert `job.Contains(pid)` and that the child's marker file is still absent before `Resume()`). The file belongs to the tool-service branch's task, so coordinate before editing, or raise it there.
- **extractor/SwReview.AddIn/Terminal/CliProfileWriter.cs:454** - `<pipe>` and `<secret>` are substituted into TOML basic strings by plain `String.Replace` with no escaping (`.Replace("<pipe>", bridge.Pipe).Replace("<secret>", bridge.Secret)`), unlike every path, which goes through `PathText`. Today both values are safe by construction (`swreview-<guid>` and base64url from `ToolServiceHost.NewSecret`), so the byte-for-byte tests pass; a future secret alphabet or pipe spelling containing `"` or `\` would silently emit a config.toml Codex cannot parse, which surfaces as a CLI that ignored the generated profile.
  - Fix: Escape the two values for a TOML basic string (or assert their alphabet at the boundary with a clear exception), and add a test that a secret containing a quote and a backslash round-trips through the generated file.
- **extractor/SwReview.AddIn/Terminal/ToolListingCheck.cs:357** - `CodexBuiltIns` is `{ "apply_patch" }` and deliberately omits `shell`, while contracts/cli-profiles.md's 'Verification on first launch' paragraph — which T062 names as the source of the intended built-in set — still says 'Codex: the read-only shell and apply_patch'. The code is the stricter and correct reading under the 2026-09-13 `features.shell_tool = false` decision, but an authoritative contract and the gate it governs now disagree in writing, with only a code comment recording it.
  - Fix: Correct that sentence in specs/002-task-pane-assistant/contracts/cli-profiles.md to drop the shell from Codex's intended built-ins (it is a one-line documentation edit in a file this branch may touch, or fold it into the T064 documentation task), so the contract and the gate state the same set.
- **extractor/SwReview.AddIn.Tests/CliProfileWriterTests.cs:289** - Tautological half of `ShipsTheContractToolAllowlist`: the loop `foreach (string tool in CliProfileWriter.EnabledTools) Assert.Contains("\"" + tool + "\"", config)` cannot fail, because `BuildConfig` substitutes `<enabled-tools>` from that same `EnabledTools` list. It reads like a check that the profile ships the contract's allowlist, but it only checks that string interpolation works. (The `withheld` loop and the byte-for-byte config test are the assertions actually carrying that guarantee.)
  - Fix: Either delete the first loop, or make it non-circular by reading the expected tool names out of `contracts/mcp-toolset.md` (as the fixture README and T058 do) and asserting `CliProfileWriter.EnabledTools` equals that list in order.
- **extractor/SwReview.AddIn.Tests/CliLocatorTests.cs:211** - `TheProbeGivesUpOnACliThatDoesNotAnswer` states in its summary that a timed-out probe 'must not leave a process behind when it is given up on', but asserts only `output.Failure != null` and that the text does not contain 'too late'. The `ping -n 30` grandchild that `CliLocator.Probe`'s `finally` is supposed to kill via `tree.Dispose()` is never observed, so a regression that dropped the JobObject teardown (leaving a 30-second orphan per dropdown refresh) would keep this test green.
  - Fix: Capture the pid before the timeout (or enumerate `cmd.exe`/`ping.exe` children of the probe) and assert the tree is gone within a bounded wait after `Probe` returns - e.g. expose the JobObject or assert `Process.GetProcessById(pid)` has exited.
- **extractor/SwReview.AddIn.Tests/CliLocatorTests.cs:305** - `AShortVersionIsNotOlderThanTheSameLongOne` computes its own expected value from the data under test: `Assert.Equal(minimum.Build == 0 && minimum.Revision <= 0 ? CliStatus.Ready : CliStatus.TooOld, found.Status)`. With today's 0.115.0 minimum it asserts Ready; if the minimum ever becomes 0.116.1 the test flips its expectation and keeps passing whatever `ParseVersion` does with the missing component - which is the exact bug the test exists to prevent.
  - Fix: Assert against a fixed CliDefinition/minimum constructed in the test (or parametrize `ParseVersion("0.115") >= new Version(0,115,0,0)` directly) so the expectation does not track the production constant.
- **extractor/SwReview.AddIn.Tests/TerminalPageContractTests.cs:394** - T053 requires the `terminal.output` post to be marshalled onto the UI thread (the read loop is a background thread and `PostWebMessageAsJson` has UI-thread affinity). The only channel test, `TheTerminalChannelSwallowsAPostWhenThereIsNoPage`, posts from the STA test thread onto a pane with no page, so it exercises neither `InvokeRequired` nor the `BeginInvoke` branch in `TaskPaneControl.PostToPage` (line 474). A change to `Invoke` - which would deadlock against `TerminalSession.Stop`'s `_watcher.Join` if Stop is ever called on the UI thread - or a dropped marshal would not be caught.
  - Fix: Add a test that, on a pane with a created handle, posts from a `new Thread(...)` and asserts the call returns without blocking and that the work is queued to the UI thread (e.g. record the thread id inside a stubbed `PostOnUiThread` seam, or assert the posting thread is not the one that runs the queued action).
- **extractor/SwReview.AddIn/Terminal/TerminalSession.cs:414** - `Stop()` blocks its caller for `StopGrace` (3 s by default) plus `_watcher.Join(WatchdogMilliseconds)` (15 s), and the contract puts it on the host's `terminal.stop` handler, which is raised on the SOLIDWORKS UI thread. Nothing in the suite pins down where Stop runs; the ConPTY tests call it from a test thread with a 750 ms grace. As written, the first real `terminal.stop` on a CLI that ignores `exit\r` freezes SOLIDWORKS for at least 3 s, and up to 18 s if the last read wedges.
  - Fix: Decide and test the threading rule: either run `Stop()` off the UI thread and reply `terminal.stopped` from the completion (with a test that the handler returns promptly), or document a much shorter watchdog for the interactive path. Also catch/ignore `Win32Exception` in `TerminalSession.Resize` (line 407) - `ConPty.Resize` throws on any non-zero HRESULT and that would travel out of the page-message handler on the UI thread, contradicting the 'a resize arriving while the CLI is exiting is ordinary' comment.

## Feature 001 DRY review (T104), report-only findings

- The metre-to-millimetre factor is hand-written at nine sites (`geometry/axis.py`, `tools/measure.py`, `tools/checks_fastener.py`, `checks/hole_alignment.py`, `checks/fastener.py`); one `units.metres_to_mm()` should replace them, with a golden re-run.
- `tools/checks_fastener.py` `_bbox_extent_mm` computes an engineering number in the tool layer; move it to `geometry/`.
- `tools/checks_fastener.py` loses why a clamped thickness is unknown (unparseable vs absent `Thickness` property); distinguish in the layer source.
- `SourceRef` is rendered twice (`checks/result.py:cite()` and `report/markdown.py:_fmt_source_ref()`); one formatter.
- `{component.id: component}` maps are built in five modules; one `components_by_id(package)` in `ir/`.
- `cli.py` hard-codes the five coverage bucket names already declared by `report.session.Coverage`.
- `checks/engagement_rules.py` and `checks/tool_envelopes.py` are near-identical cached YAML loaders and disagree on `.resolve()`; unify path handling.
- `checks/golden.find_dimension` is a single-use alias for `resolve_dimension`; drop it.
- `checks/golden_fastener.py` re-implements fastener/hole/material lookup that `ToolContext` already owns.
- C#: `Dump/GapCollector.cs` `TryStep(Action)` and `TryStep<T>` repeat the same three-catch block.

## Polish leftovers (T064-T068 agents)

- `specs/001-agentic-design-review/contracts/cli.md`: the closing paragraph still describes the add-in as three Task Pane buttons; stale after the two-tab pane.
- `swreview-extract --help` lists options the CLI contract does not (`capture --note`, `serve --doc/--config/--out`); align the contract.
- `.github/workflows/reviewer.yml` `paths` filters do not include `specs/002-task-pane-assistant/contracts/**`, so a contract-only change does not trigger the job.
- `extractor/SwReview.Extractor.Console/Serve/PROTOCOL.md` stayed in the console folder after the dispatcher moved to `SwReview.Extractor/Bridge/`; move it and update the four Python docstrings that cite the path.
- Workstation tasks still open: feature 001 T061 and T076; feature 002 T044, T051, T063, and the SOLIDWORKS/credential half of T067; Gemini spike T055a deferred.

## Feature 003 implementation reviews (2026-09-16)

Should-fix findings from the adversarial reviews of each feature 003 phase, the add-in load fix, the pane cleanup, and the feature 004 spec review. Must-fix findings were applied in the phase they were found; these were not. Grouped by review round; each names the file and line as reviewed, the problem, and the proposed fix.

## Phase 1-2 (2026-09-16)

- **reviewer/src/swreview/ir/models.py:249** (python-ir-builder) - Regenerating `ir.schema.json` from the models dropped documentation from the feature 001 contract. Diffing every `description` string in the old committed contract against the new one, exactly one is lost: `components[].transform` -> "Row-major 4x4, translation in meters". That string only ever lived in the hand-written contract; `Transform` is a bare `Annotated[list[...]]` alias (models.py:90) and `transform: Transform` carries no `Field(description=...)`, so the generator has nothing to emit. The C# DTO still documents the convention (extractor/SwReview.Extractor/Ir/ComponentInstance.cs:42), so the contract is now the least informative of the three copies about a unit-and-ordering convention a reader must not guess. Fix: Give the field the description it used to have - `transform: Transform = Field(description="Row-major 4x4 relative to the root assembly, translation in meters")` - and re-run `python -m swreview.ir.schema --write ../specs/001-agentic-design-review/contracts/ir.schema.json`. (Everything else about the regeneration checks out: the file is byte-identical to the generator output, and a canonicalised old-vs-new diff shows exactly four additions - `features`, `equations`, `rms_suppress_test`, `components[].constrained_status_raw` - and nothing else changed.)
- **reviewer/tests/unit/test_ir_features.py:113** (python-ir-builder) - DRY: `build_run` (line 113) and `build_row` (line 95) are second builders for `SuppressTestRun`/`SuppressTestRow`, duplicating `suppress_run` (tests/support/features.py:365) and `suppress_row` (features.py:338) added by T003 in the same change set. `build_run` is a near field-for-field copy of `suppress_run` with the same defaults (acknowledged True, baseline 0, restore_verified True, features_present defaulted from the rows). `component_fields` (line 134) is likewise a third copy of the `ComponentInstance` field set that already exists inline in tests/support/packages.py and in features.py. The constraint is explicit: never add a second mechanism for something the repo already has. Fix: Build the happy-path run/row through `suppress_run`/`suppress_row` from `tests.support.features` (`suppress_row(build_feature(), "ok")` works - the helper takes a built `Feature` and both helpers already expose outcome, messages, messages_truncated, limit, group and plan_file as kwargs), and keep a local constructor only for the cases that must pass values the IR should reject (`outcome="restored"`, 21 messages, `messages_truncated=-1`, `constrained_status_raw="over_defined"`), with a one-line comment saying why those cannot go through the shared builder.
- **reviewer/src/swreview/ir/models.py:534** (python-ir-builder) - Two invariants that data-model.md section 1 states as rules are only written in `Field(description=...)` prose, not enforced: `features_present` "equals len(rows)" (line 534 / 539) and `unrestored_feature_ids` "Empty when restore_verified" (line 538). A `SuppressTestRun` that drops rows, or claims a verified restore while naming unrestored features, loads silently and the T056 rule then computes `<tested>/<present>` off a number nothing checked. The IR already has precedent for exactly this kind of cross-field check (`SourceRef._require_a_locator`, models.py:160), so this is not a new mechanism. Fix: Add one `@model_validator(mode="after")` on `SuppressTestRun` asserting `len(self.rows) == self.features_present` and `not (self.restore_verified and self.unrestored_feature_ids)`, and pin both in `test_ir_features.py`. If you would rather keep the model permissive so a partially-written run still parses for diagnosis, say so explicitly in the docstring rather than leaving the rule as prose - but then T054's C# assertions become the only enforcement.
- **reviewer/tests/unit/test_ir_features.py:417** (python-ir-builder) - `test_the_new_gap_entity_kinds_validate` is tautological: `Gap.entity_kind` is a plain `str` (models.py:579), so the parametrized test passes for any string at all - "banana" included - and asserts nothing about the ten names data-model section 1 adds. T005 asks that "the new gap `entity_kind` values validate per data-model section 1"; as written the test documents the names but cannot fail if a producer misspells one. (Widening the field to a `Literal` is the wrong fix - feature 001 emits free-form kinds from `ingest/bom.py`, `package_builder.py`, `pdf_drawing.py`, `tools/bridge.py`, so a closed enum would be the breaking change the constraints forbid.) Fix: Make the tuple load-bearing instead of decorative: assert that the kinds the T003 builder actually emits (`feature_tree_unavailable`, `mate_suppression` in tests/support/features.py) are drawn from `NEW_GAP_ENTITY_KINDS`, and have the later dumper/rule tasks assert the same against their producers. Failing that, keep the test but say in its docstring that it pins the vocabulary as documentation only, so a later reader does not mistake it for validation.
- **reviewer/src/swreview/ir/models.py:311** (python-ir-builder) - Inconsistent bounds on the new integer fields. `messages_truncated` gets `ge=0` (line 536) but `Feature.index`, `Feature.depth` (lines 311-312), `Equation.index` (line 334), `SuppressTestRun.baseline_whats_wrong_count`, `limit`, `timeout_seconds`, `features_present` and `SuppressTestRow.whats_wrong_count`/`elapsed_ms` accept negatives. None of these can be negative in the domain (an index, a tree depth, a count of errors, an elapsed time), so a -1 from a mis-wired dumper would round-trip as valid engineering data. Fix: Add `ge=0` to the counts and indices that cannot be negative (`index`, `depth`, `features_present`, `baseline_whats_wrong_count`, `whats_wrong_count`, `elapsed_ms`) and `ge=1` to `limit`/`timeout_seconds` if zero is meaningless there, then regenerate the contract so the C# side validates against the same bounds. Low effort, no behaviour change for valid data; the only risk is a fixture that happens to use a sentinel negative, and there is none today.
- **reviewer/tests/unit/test_ir_features.py:417** (python-ir-builder) - `test_the_new_gap_entity_kinds_validate` is tautological: `Gap.entity_kind` is a bare `str` and `git diff reviewer/src/swreview/ir/models.py` shows `Gap` is untouched by this change, so all ten parametrizations pass unmodified against pre-T006 code. The test asserts `gap.entity_kind == entity_kind` - i.e. that pydantic stores a string it was given. It is the one case in the T005 list ("the new gap `entity_kind` values validate per data-model section 1") that pins nothing about the implementation. The NEW_GAP_ENTITY_KINDS constant does match data-model section 1 exactly (all ten, same spelling), so the value here is documentary, not behavioural. Fix: Option A (recommended): turn the constant into a real cross-check by asserting the ten kinds are exactly the ones the repo emits - `tests/support/features.py` already emits `feature_tree_unavailable` and `mate_suppression`, and the C# dumpers emit the rest, so a cheap version is to assert the list is a superset of `{gap.entity_kind for gap in <built rms_package>.gaps}` plus keep the per-kind validation. Effort ~6 lines, catches a future typo in a builder or dumper. Option B: leave it as an executable copy of the data-model list and say so in the docstring ("documents the vocabulary; `Gap.entity_kind` is intentionally unconstrained so a new extractor can name a gap this build has not heard of"). Zero effort, honest about what it is. Option C: constrain `Gap.entity_kind` to a Literal - this I would NOT do: it breaks forward compatibility with a newer extractor and would be a schema-breaking change to a feature 001 contract. I lean B given the constraint that the IR bump adds optional members only, with A as a nice-to-have if you want the list load-bearing.
- **reviewer/tests/unit/test_support_features.py:66** (python-ir-builder) - `assert_round_trips` only round-trips through the pydantic models (`EvidencePackage.model_validate(package.model_dump())` in python and json mode). Nothing on the Python side validates a *populated* 1.1.0 package against `specs/001-agentic-design-review/contracts/ir.schema.json`. `test_schema_sync.py` validates only `build_package()`, which has no features, equations or suppress-test run; the only populated-1.1.0 contract check lives in C# `IrSerializerTests`. Since the T030/T036/T043/T046 golden `package.json` files are produced by this builder and are the artefacts the extractor contract governs, a divergence between the models and the committed contract on a new member would not be caught on the Python side. (I verified the builder's output does validate cleanly today - errors: [] - so this is a missing assertion, not a live bug.) Fix: Option A (recommended): reuse the existing helper - `from tests.support.contracts import contract_validator`, and in `assert_round_trips` add `assert not list(contract_validator("ir.schema.json").iter_errors(package.model_dump(mode="json")))` with the error messages in the assertion message. ~4 lines, uses the repo's existing mechanism (no second validator), and every package the builder emits is then held to the same contract as the C# writer. Option B: a single dedicated test that validates one fully-loaded `rms_package(...)` against the contract, leaving `assert_round_trips` alone. Cheaper and faster, but only covers the one shape you remember to build. Option C (do nothing): rely on `test_schema_sync` plus the C# side. I recommend A - it is the DRY option, it costs nothing per call, and it puts the check on the path every future fixture already takes.
- **reviewer/src/swreview/ir/models.py:534** (python-ir-builder) - `SuppressTestRun.features_present` carries the invariant `len(rows) == features_present` in its own Field description and in data-model section 2, but nothing enforces or tests it. `tests/support/features.py::suppress_run` lets a caller pass a `features_present` that disagrees with `rows`, and `test_ir_features.py::test_suppress_test_run_records_the_audit_fields` asserts the equality only for a run that happens to satisfy it. T054 asserts `len(rows) == features_present` on the C# producer side, so a Python-side fixture that violates it would sail through and then feed T056's rule tests a run shape the real command can never produce. Same class of looseness: `limit`, `timeout_seconds`, `features_present`, `baseline_whats_wrong_count` and `Feature.index`/`depth` are unconstrained ints (negatives validate), while `SuppressTestRow.messages_truncated` does carry `ge=0`. Fix: Option A (recommended): add a `@model_validator(mode="after")` on `SuppressTestRun` raising when `len(self.rows) != self.features_present`, and a test asserting the mismatch is refused. ~6 lines, makes the documented invariant real for both producers, and is the kind of edge case the repo's preferences say to catch rather than trust. Risk: if the C# command ever legitimately writes a partial run it would now be rejected - but T054's own assertion says it never does. Option B: enforce it only in the test builder (`suppress_run` raises on a mismatch). Keeps the IR permissive toward a future extractor, but leaves the invariant unenforced for real packages read off disk. Option C: add `ge=0` to the int fields and leave the row-count invariant to the C# side. Partial, and it leaves the one invariant a rule actually reads unguarded. I recommend A plus the `ge=0` constraints on `features_present`/`limit`/`timeout_seconds`/`baseline_whats_wrong_count`; I would leave `Feature.index`/`depth` alone since the indexer owns them and the C# DTO is already the authority.
- **reviewer/tests/unit/test_ir_features.py:113** (python-ir-builder) - DRY: `build_row`/`build_run` here duplicate `suppress_row`/`suppress_run` in `tests/support/features.py` (T003), field for field, and the two sets of defaults have already drifted - `build_run` defaults `group="Detail"`, `suppress_run` defaults `group="4-Detail"` (only the latter is an actual RMS group name from `rms_types.yaml`). `component_fields` likewise re-states the `ComponentInstance` field set that `tests/support/packages.py::build_package` already owns. Two parallel definitions of the same fixture shape is exactly the repetition the project rules say to flag aggressively; the concrete risk is a later reader taking `group="Detail"` for a real group name. Fix: Option A (recommended): keep the local builders (the T005 module deliberately exercises the models without going through a builder layer, and it needs to pass invalid kwargs like `is_folder=True` that the specs layer cannot express) but remove the drift - make `build_run`'s `group` default `"4-Detail"`, and add a one-line comment saying why this module does not import `tests.support.features`. Effort ~2 lines, zero risk, keeps the layering argument explicit rather than accidental. Option B: have `build_row`/`build_run` delegate to `suppress_row`/`suppress_run`. Genuinely DRY, but it couples the model-contract tests to the fixture layer built on top of those models, so a bug in the builder reads as a bug in the IR. Option C (do nothing): the duplication is ~35 lines of flat literals and the divergent default is harmless. I recommend A: it gets the honesty of B's intent without inverting the dependency, and it matches "explicit over clever".
- **reviewer/tests/unit/test_support_features.py:533** (python-ir-builder) - Several builder options that downstream tasks will depend on are never exercised, so their first use will be inside a rule test where a silent misbehaviour reads as a rule bug: `InstanceSpec.referenced_configuration` (the override path - only the default `spec.referenced_configuration or configuration` branch is covered), `PartSpec.material`, `MateSpec.alignment`, `suppress_run`'s `limit`/`timeout_seconds`/`plan_file`/`features_present` overrides, `end_tag(**kwargs)` pass-through, and `rms_package(assembly=..., parts=())` - the assembly with no child components that T032 needs for "no children skips". I probed the last two by hand and both behave correctly today (assembly-only yields components `['cmp:0001']`; `referenced_configuration='Machined'` overrides a `Default` part), so this is coverage, not a defect. Fix: Option A (recommended): add two small tests - one asserting `rms_package(assembly=AssemblySpec())` yields exactly the root instance and no part documents (the T032 precondition), and one asserting the `InstanceSpec.referenced_configuration` override wins over the part's configuration. ~12 lines, and they cover the two options a later task actually depends on. Leave `material`, `alignment` and the `suppress_run` numeric overrides uncovered - they are plain pass-throughs with no branching. Option B: cover every option exhaustively, one test per parameter. Matches "too many tests rather than too few", but most of them would assert a dataclass field reached a pydantic field unchanged, which is the tautology class flagged above. Option C (do nothing): the rule tests will exercise these in a few tasks' time. I recommend A - it targets the two options with real branching logic behind them and leaves the pass-throughs to their first real use.
- **reviewer/src/swreview/checks/rms/part.py:26** (table-groups-registry) - T012's scope is `checks/rms/registry.py` and `checks/rms/__init__.py`. This change also creates three source modules owned by later tasks - part.py (T020), assembly.py (T033), equations.py (T040) - which other agents are editing concurrently, and each ends with `for _rule in evaluable(): if _rule.scope == SCOPE: bind(_rule.id)(_not_implemented)`. That loop binds whatever the registry happens to list, so T011's required assertion 'every evaluable rule ... has a function' (test_rms_registry.py TestEvaluableRules / TestInvariant) can never fail for these scopes: an id added to the registry with no evaluator is auto-stubbed rather than reported. It also hides the T057 sequencing problem (rms.detail.individually_suppressible is bound by the loop today, and T020 will have to keep stubbing it until T057). Fix: Either drop the three modules and record the red T011 invariant as a declared deviation until T020/T033/T040 land, or keep them but bind each rule id by an explicit literal list (`@bind('rms.folders.present')` ... one line per id) so the file names exactly what it owes and the registry/evaluator drift the test is meant to catch stays catchable. Flag the out-of-scope file creation to the orchestrator either way.
- **reviewer/src/swreview/checks/rms_types.py:193** (table-groups-registry) - `_parse_classes` enforces that the eleven class sets are pairwise disjoint, but nothing enforces that `ambiguous` is disjoint from them, and `classify` (line 117) checks the class sets FIRST. So a table that lists ICE in both `ambiguous` and `cut` silently classifies it `cut`. The shipped YAML's own comment records that upstream keeps ICE in both its cut and hole sets, so this is the exact mis-edit an engineer is likely to make, and it turns 'unknown stays unknown' into a definite class: rms.detail.holes_last would grade an ICE feature instead of reporting it unresolved. Only a test (test_the_ambiguous_set_is_disjoint_from_every_class) guards it, and only for the shipped file. Fix: In `_parse`, after `_parse_classes`, raise ValueError naming the offending type name(s) when `ambiguous` intersects any class set (same message shape as the existing disjointness error), and add a loader test writing such a table.
- **reviewer/src/swreview/checks/rms/registry.py:51** (table-groups-registry) - `CoverageBucket = Literal['unresolved', 'out_of_scope']` shadows the existing public `swreview.report.session.CoverageBucket = Literal['checked','skipped','unresolved','failed','out_of_scope']` with the same name and a different value set, and re-exports it from `checks/rms/__init__.py`. Two same-named bucket types in one codebase is exactly the 'second mechanism for something the repo already has' the constraints forbid; a report-layer author importing the wrong one gets no type error until a bucket is missing. Fix: Rename to `RuleCoverageBucket` (or `CoverageOnlyBucket`) and derive it from the session type so the subset relationship is explicit, e.g. keep the Literal but add a module-level assertion that its members are a subset of `swreview.report.session.CoverageBucket`'s.
- **reviewer/src/swreview/checks/rms_types.py:265** (table-groups-registry) - DRY: `_parse(...)` + `@lru_cache(maxsize=4) def _load_cached(path) -> T: return _parse(yaml.safe_load(path.read_text(encoding='utf-8')), path)` + `load_x(path=None)` is now the third verbatim copy of the same loader trio (engagement_rules.py:91-101, tool_envelopes.py:91-99). The constitution requires extraction 'once it appears twice with the same intent'; it has now appeared three times, and the copies have already drifted - tool_envelopes.load_envelopes does NOT call .resolve() while the other two do, so its 'cached by resolved path' claim is false. Fix: Extract one helper (e.g. `checks/_yaml_table.py` with `cached_loader(parse)` returning a `load(path=None)` that resolves and caches), and have all three tables use it. Keep each module's `_parse` as-is. Out of T008's stated scope, so raise it with the orchestrator rather than editing engagement_rules.py/tool_envelopes.py unasked.
- **reviewer/src/swreview/checks/rms_types.py:104** (table-groups-registry) - `RmsTypeTable` is `frozen=True` but two of its fields are plain mutable dicts (`classes`, `constrained_status_map`), and `load_table` hands every caller the SAME cached instance. One rule module doing `table.classes['cut'] |= {...}` or `table.constrained_status_map[7] = 'fully_defined'` mutates the table for the whole process, which is precisely how a favourable default for engineering data gets introduced without a file change. Fix: Wrap both in `types.MappingProxyType` at construction (the values are already frozensets), or store them as tuples of pairs and expose lookups only through `classify`/`constrained_status`.
- **reviewer/src/swreview/checks/rms_types.py:238** (table-groups-registry) - `_parse` reaches for `document['assembly']`, `document['version']`, `document['groups']`, `document['folder_type']`, `document['ambiguous']`, `document['tolerated_loose']`, `document['default_names_excluded']` and `assembly['reference_entity_kinds']` with bare subscripts, so a table missing a key raises a bare KeyError with no path in the message, while every other malformed-table case raises `ValueError(f'{path}: ...')`. An engineer editing rms_types.yaml gets two different failure shapes for the same class of mistake, and `groups` is never checked for the six unique names `assign_groups` then indexes into. Fix: Add one `_require(document, key, path)` helper raising `ValueError(f'{path}: missing key {key!r}')`, use it for every top-level read, and validate that `groups` is a non-empty list of unique names.
- **reviewer/tests/unit/test_rms_groups.py:223** (table-groups-registry) - Every derived-subfolder test goes through `build_features`, whose 'flat' shape flattens a feature's sub-features as well as a folder's contents, so no test in the module ever produces a row whose `folder_id` points at a non-folder, nor a document that mixes flat folders with sub-feature nesting. Both must_fix defects above sit entirely in that blind spot and the module is otherwise thorough, so the coverage gap is what let them through. Fix: Add two tests that construct `Feature` rows directly (as test_rms_type_table.py's `build_feature` already does) rather than through the builder: (a) nested shape, a sketch at depth 1 under an extrusion that itself sits in a non-group subfolder - assert the sketch's derived subfolder is the SUBFOLDER, not the extrusion; (b) flat folders with end tags plus that same sketch nesting - assert the flat folder's members still get its id. Separately, consider teaching `build_features` a per-spec 'sub_feature' flag so the builder can express the mixed shape (T003's owner).
- **reviewer/src/swreview/checks/rms/groups.py:75** (table-groups-registry) - The traversal shape is decided once per document by `any(row.folder_id is not None or row.depth > 0)`, and since `folder_id`/`depth` come from sub-feature structure of ANY feature (not just folders), a mixed tree — folders reported flat with end-tag markers while features list their absorbed sketches as sub-features, which is the ordinary SOLIDWORKS tree shape — is classified `nested`. End-tag markers are then ignored, so every flat folder's membership is silently lost and `subfolder_by_feature_id` is null for all of its members (and misattributed for the nested sketches). research.md's open question 1 says the probe (T062) has not yet shown which shape 2024 reports, so this is not hypothetical. Fix: Decide membership per folder rather than per document: a folder whose id appears as some row's `folder_id` owns those rows; otherwise fall back to the end-tag span. Failing that, restrict the `nested` test to rows whose enclosing row is a folder (`table.is_folder`), which removes the sketch-under-feature false positive, and record the decision in the docstring.
- **reviewer/src/swreview/checks/rms/groups.py:75** (table-groups-registry) - A document with no nesting anywhere is ambiguous between the two shapes and is read as flat, so an empty non-group folder captures every following feature as its derived subfolder. Verified: nested specs [folder('Misc'), A, B] yields subfolder 'Misc' for A and B although in the nested shape they are outside it. That is the unsafe direction for a `fail` rule — it manufactures a coupled pair — and it contradicts the module's own reasoning elsewhere ("a fail rule must not be excused by a missing tag"). The neighbouring test test_a_flat_folder_left_unclosed_keeps_its_members_to_the_end pins the same ambiguous input to the flat reading, so the choice is deliberate but undocumented as a risk. Fix: Either treat an ambiguous document (no nesting, no end-tag rows anywhere) as nested, so a folder with no listed contents owns nothing, or keep the flat reading and state the trade-off in the docstring plus a test named for it. Note the current unclosed-folder test would need revisiting under the first option.
- **reviewer/src/swreview/checks/rms_types.py:227** (table-groups-registry) - `_parse` indexes required top-level keys directly (`document["assembly"]`, `document["groups"]`, `document["folder_type"]`, `document["version"]`, `document["ambiguous"]`, …), so a table missing one raises a bare `KeyError('assembly')` with no file path, while every other malformed-table path raises a `ValueError` naming the path. The "a table that cannot be trusted" block (tests 375-430) covers overlapping classes, an unknown class, a missing class, an unavailable status value, non-int status keys and a non-mapping document, but no missing required key — so the inconsistency is untested. Fix: Validate the required top-level keys once and raise `ValueError(f"{path}: missing ...")`, and add a parametrized test that deletes each required key and asserts the message names the path and the key.
- **reviewer/tests/unit/test_rms_registry.py:280** (table-groups-registry) - `test_binding_returns_the_function_unchanged` inserts a synthetic rule into the module-global `RULES` dict and removes it in `finally`. pytest-randomly is installed (the suite randomises order), and any future parallelism or a failure between insert and delete leaves a 35th rule visible to `test_exactly_the_contract_ids_are_registered`, `by_scope` and the count assertions. The mutation is invisible from the other tests' point of view only by luck of single-threaded execution. Fix: Exercise `bind` against a local dict (e.g. give `bind` the registry to bind into, or patch `registry.RULES` with `monkeypatch.setitem`/`monkeypatch.setattr` so pytest restores it) rather than mutating the shared catalogue by hand.
- **reviewer/src/swreview/exceptions.py:268** (exceptions-recording) - `fingerprint(..., "feature_tree")` is chosen for every check id starting with `rms.` (fingerprint_kind_for, line ~88), but it only hashes `package.features`/`package.equations` for the components' documents. Assembly documents carry no `features[]` rows (data-model.md §1: `document_id` is "the part document that owns the tree"), so an exception accepted for a fail-severity assembly rule - `rms.assembly.mates_to_reference_geometry` or `rms.assembly.first_component_fixed` (contracts/rules.md:80-81), both acceptable through the import per data-model.md §3 - hashes `{"document_id":"doc:1","features":[],"equations":[]}`, a digest that can never change. `refresh` will never move such a waiver to `needs_review` no matter how the assembly is re-mated or re-fixed: a silent permanent waiver. tests/unit/test_exceptions_feature_tree.py:406 asserts this as intended behaviour without noting the consequence. (The digest does at least differ per document, since `document_id` is in the payload.) Fix: data-model.md §3 only contemplates part documents here, so this needs the owner's call rather than an unilateral redesign. Options: (A) refuse it loudly - raise from `fingerprint` when `kind == "feature_tree"` and the selected documents contribute zero feature and zero equation rows, so an un-bindable waiver fails at `accept` instead of silently freezing (small, matches "no silent failure"); (B) extend the feature-tree payload for assembly documents with the mate rows and the instances' `constrained_status_raw`/`is_fixed`/Toolbox identity (correct, but is new contract surface beyond T015/T016); (C) do nothing and record the limitation in the module docstring plus a backlog entry. Recommend (A) now and raise (B) with the owner before T033/T035 land assembly findings that can be accepted.
- **reviewer/src/swreview/exceptions.py:210** (exceptions-recording) - `_feature_row` collapses absence with unknown in two slots, against the constitution's "unknown stays unknown, absence is null plus a Gap": `None if sketch is None else sketch.raw_status` gives `None` both for a feature that is not a sketch and for a sketch whose `GetConstrainedStatus` failed (the `sketch_status` gap), and line 201/212 gives `None` both for a feature with no `fillet` object and for a simple fillet whose `default_radius` could not be read (the `fillet_radius` gap, which is also how a variable fillet is recorded). A feature that stops being a fillet and a fillet whose radius became unreadable are different facts for `rms.quarantine.largest_fillet_first` (contracts/rules.md:35) but hash identically. In practice `type_name` usually separates the sketch case, and nothing today separates the fillet case. Fix: Make the slots two-valued instead of one: emit a presence flag next to the value, e.g. `[sketch is not None, None if sketch is None else sketch.raw_status, ...]` and `[fillet is not None, None if radius is None else [...]]`. Cheap, explicit, and it keeps "no fillet", "variable/unreadable fillet" and "0.002 m fillet" as three distinct digests. Add a test pairing `fillet=None` against `FilletInfo(default_radius=None)` on the same row.
- **reviewer/src/swreview/tools/context.py:186** (exceptions-recording) - `replace_coverage` drops the check's items only from the bucket being written, which is exactly what data-model.md §2 says, but it means a rule that changes bucket between two runs of the same tool leaves a stale item behind: run 1 writes `rms.detail.holes_last` into `skipped`, the engineer adds a hole and re-runs, run 2 writes it into `checked` via `replace_coverage(check, "checked", ...)` and the `skipped` item from run 1 survives. The session then reports the same rule in two buckets, and the `modeling.resilience` summary ("in `checked` when no rule was unresolved for any document") is computed over a coverage set that contradicts itself. Nothing in T017/T018 is wrong; the hazard lands on T022's report layer, which is being written now. Fix: Leave the per-bucket contract as specified and make the obligation explicit: note in the `replace_coverage` docstring that the caller must call it for every bucket a rule can land in (passing an empty/absent item is not possible, so the report layer must clear the other buckets), or add a sibling `replace_coverage_across_buckets(check, bucket, item)` that filters `check` out of all buckets before recording. Recommend the docstring note plus a T022 test asserting that a rule which moves from `skipped` to `checked` appears exactly once in the session - flag it to the T022 owner rather than changing the contract here.
- **reviewer/tests/unit/test_exceptions_feature_tree.py:585** (exceptions-recording) - test_a_feature_tree_exception_ignores_the_geometry is the one test here that can go green while asserting nothing. It mutates package.components[0].transform[3][0] to 0.001 and asserts store.refresh(package) == [], but never asserts that this mutation actually moves the geometry digest. If _component_token ever stopped hashing that row (or the transform shape changed), the test would keep passing while proving nothing about kind isolation. Its mirror image, test_a_geometry_exception_ignores_the_feature_tree, is safe because CHANGED_TREES['inserted'] is independently proven to move the tree digest. Fix: Add one line before the refresh assertion: `assert fingerprint(package, BOTH) != GEOMETRY_DIGEST_BOTH` (the mutated package's geometry digest must differ from the pinned constant), so the test proves the mutation is one a geometry exception would have noticed.
- **reviewer/src/swreview/exceptions.py:190** (exceptions-recording) - Nothing pins that the feature-tree hash is LIMITED to data-model section 3's field list. Every listed field is proven to move the digest, but no test proves that a field outside the list does not. Feature.error_code in particular is rebuild-volatile: if it (or parent_ids, persist_ref, configuration, or id) ever crept into _feature_row, every RMS exception in the repo would silently flip to needs_review on an unrelated rebuild and not one test would fail. This is the whole point of excluding the description text, and that exclusion IS tested - the other exclusions are not. Fix: Add a parametrized negative case beside test_the_description_text_itself_is_not_hashed, e.g. UNHASHED_CHANGES over _with(base_features(), 1, error_code=2), parent_ids=[], persist_ref=persist_ref('other'), configuration='Cold', id='feat:0099', plus an Equation change to `text`/`value`, each asserting the feature_tree digest is unchanged.
- **reviewer/src/swreview/exceptions.py:224** (exceptions-recording) - _document_rows sorts features and equations by `index` so the digest is independent of the order rows happen to sit in package.features. Nothing tests that. build_package's helper emits rows in index order, so deleting both `sorted(...)` calls would leave the whole suite green while quietly making the digest depend on extractor emission order - exactly the class of bug the geometry path's sorting was written to avoid and DOES have a test for (test_matching_is_independent_of_the_order_of_the_component_ids). Fix: Add test_the_feature_tree_fingerprint_is_independent_of_the_row_order: `assert fingerprint(rms_package(features=list(reversed(base_features())), equations=list(reversed(base_equations()))), BOTH, 'feature_tree') == fingerprint(rms_package(), BOTH, 'feature_tree')`.
- **reviewer/src/swreview/tools/context.py:186** (exceptions-recording) - T017 says replace_coverage 'removes the session's itemS with that check in that bucket' (plural). The implementation's `items[:] = [existing for existing in items if existing.check != check]` correctly removes all of them, but every test only ever seeds ONE item with the target check, so a naive single-removal implementation (e.g. a first-match `items.pop(i)`) would pass. Duplicates are reachable in practice: check_rms_part appending before the tool was switched to replace_coverage, or a partial run. Fix: In test_replace_coverage_replaces_that_check_in_that_bucket_only, record 'rms.part.grouping' twice (reasons 'first pass' and 'stale duplicate') before the replace, and assert the bucket afterwards holds exactly two items - the untouched holes_last one and the single new grouping one.
- **reviewer/src/swreview/tools/recording.py:103** (exceptions-recording) - record_result forwards six arguments positionally: `result_to_finding(context, result, component_ids, drawing_locations, exception_id, tool_result_ids)`. exception_id and tool_result_ids are now adjacent trailing optionals, and no test in test_recording.py passes both together - yet that combination is exactly what T022 needs for a waived RMS finding (checked_within_scope is a NUMERIC_STATUS, so it needs a step id, AND it needs the exception_id). Reordering those two params in result_to_finding would be caught only by pydantic type coercion, not by an intent-level assertion. Against the repo's 'explicit over clever' rule this is also the wrong shape for a 6-arg call. Fix: Pass both by keyword in record_result (`exception_id=exception_id, tool_result_ids=tool_result_ids`) and add test_record_result_carries_an_exception_id_and_a_step_id_together asserting finding.exception_id and finding.tool_result_ids on a checked_within_scope result.
- **reviewer/tests/unit/test_exceptions.py:269** (exceptions-recording) - DRY: the literal "interference.static" is now hardcoded in five places in test_exceptions.py (lines 269, 279, 287, 296, 307) and again as INTERFERENCE_CHECK in test_exceptions_feature_tree.py, while the implementation deliberately passes the module constant swreview.checks.interference.CHECK. The tests therefore no longer track the single source of truth the implementation uses - the same mistake test_tools_context.py's own docstring calls out ('imported rather than spelled out, so these assertions track the single source of truth'). Related nit: T016 asked for `check=interference.CHECK`; both call sites (checks/interference.py:286, tools/checks_interference.py:97-102) pass it positionally - behaviourally identical, but the keyword would make the fourth argument self-describing at the call site. Fix: Import `from swreview.checks.interference import CHECK as INTERFERENCE_CHECK` in both test modules and use it everywhere instead of the literal; optionally spell the argument as `check=CHECK` at the two call sites.
- **reviewer/src/swreview/exceptions.py:199** (exceptions-recording) - _feature_row collapses two distinguishable states into one digest: `sketch=None` (this feature has no sketch at all) hashes identically to `SketchInfo(raw_status=None, consumer_ids=None)` (the extractor could not read the sketch - a gap), and `fillet=None` hashes identically to `FilletInfo(default_radius=None)`. Data-model section 3 only names sketch.raw_status / len(consumer_ids) / fillet.default_radius, so this is arguably within spec, but it sits against the brief's rule that unknown must stay distinguishable from absent. Neither state is tested, so the collapse is currently undocumented behaviour. Lowest priority of the seven - flag it, do not necessarily change it. Fix: Either add the presence flags (`sketch is not None`, `fillet is not None`) to the row, or - the cheaper option given type_name already changes when a feature stops being a sketch - add a test that pins the collapse as deliberate with a comment citing data-model section 3.
- **extractor/SwReview.Extractor/Dump/FeatureTreeIndexer.cs:103** (csharp-ir-indexer) - `if (node == null) { continue; }` silently drops a feature from the tree: no row, no id, no gap, and the sub-features under that node vanish with it. Every downstream RMS rule then reads a tree with a hole and reports pass/skip instead of unresolved, which is exactly the silent-absence failure Principle I exists to prevent. It is also untested - no test in FeatureTreeIndexerTests.cs passes a null node - so the branch is unexercised in the 554-test run. Fix: Decide and be explicit. Either the input can never hold a null (it is built by the T024 dumper), in which case throw `new ArgumentException("A feature walk must not contain a null node.", nameof(features))` so a dumper bug is loud, or nulls are a real interop outcome, in which case the indexer must not be the place that swallows them: have the dumper record a `feature_tree_unavailable`-style gap before it ever reaches the indexer. Add the matching test either way.
- **extractor/SwReview.Extractor/Ir/SuppressTestRun.cs:30** (csharp-ir-indexer) - `Outcome` defaults to `SuppressTestOutcome.Truncated`. Per contracts/rules.md's outcome table, `truncated` maps to *skip* - so a row that T055 forgets to fill in silently drops that Detail feature out of `rms.detail.individually_suppressible` rather than surfacing it. This is a default standing in for engineering data on the permissive side. Fix: Default to `SuppressTestOutcome.Aborted`, which the same table maps to *unresolved*: an unfilled row then shows up as "not proven" instead of "deliberately not tested". Update the XML doc to say why the default is the unresolved outcome.
- **extractor/SwReview.Extractor/Dump/MateDumper.cs:195** (csharp-ir-indexer) - `FirstFlag` has four branches (bool, bool[], object[] of boxed bool, and the null fallback) and `ReadSuppressed` has three outcomes (read value, TryStep caught, null-answer gap). None of it is covered: the new PackageWriterTests case pins only PackageWriter's pass-through of a value the fake IMateSource fabricates, so MateDumper's VARIANT unwrapping and both of its gap paths run for the first time on a seat. T006 asks for `false` plus a `mate_suppression` gap on failure and nothing proves that happens. Fix: `FirstFlag` is already static and interop-free - make it `internal static` (with InternalsVisibleTo, which the test assembly likely already has) or lift it next to the other pure helpers, and add a small `MateDumperTests`/`FirstFlagTests` covering `true`, `new[]{true}`, `new object[]{false}`, `new bool[0]`, and `"nonsense"` -> null. If the gap paths cannot be reached without a seat, say so in the return note; the unwrapping itself has no excuse.
- **extractor/SwReview.Extractor/Dump/FeatureTreeIndexer.cs:13** (csharp-ir-indexer) - `FeatureTreeNode` and `FeatureTreeRow` are dump contracts shaped exactly like `ComponentNode`/`ScopedComponent` (same `Handle` as `object?` with the same verbatim comment, same id-assigned-later pattern), but they live in the algorithm file instead of Dump/DumpContracts.cs, where every other dump contract type is declared (DumpOptions, ComponentNode, ComponentTreeResult, ScopedComponent, FaceRequest, HoleDumpResult, DumpScope and the eight I*Source interfaces). T024's FeatureDumper and its `IFeatureSource` will look for them in the contracts file; two homes for dump contracts is the start of the second mechanism the constraints forbid. Fix: Move `FeatureTreeNode` and `FeatureTreeRow` into Dump/DumpContracts.cs next to `ComponentNode`/`ScopedComponent` and leave FeatureTreeIndexer.cs holding only the static indexer. No behaviour change; the test file needs no edit.
- **extractor/SwReview.Extractor/Dump/ComponentTreeDumper.cs:140** (csharp-ir-indexer) - `RootNode` sets every single ComponentNode member explicitly - including the ones whose C# default would already be right (`ParentKey = null`, `PatternId = null`, `IsToolbox = false`) - but the new `ConstrainedStatusRaw` is omitted and silently takes the `null` default. The root assembly genuinely has no constrained status, so `null` is the correct value, but the omission reads as an oversight rather than a decision and breaks the file's own explicit-over-clever convention. Fix: Add `ConstrainedStatusRaw = null,` after `IsToolbox = false,` with a one-line comment: the root assembly is not constrained by anything, so Python's table maps null to `unavailable` and the first-component rule never asks about it.
- **extractor/SwReview.Extractor/Dump/MateDumper.cs:102** (csharp-ir-indexer) - `ReadSuppressed` records its `mate_suppression` gap against `mateId` before the mate is known to be complete. If any later step in `ReadMate` throws, the outer `scope.Gaps.TryStep("mate", ...)` at MateDumper.cs:85 swallows it and the mate is never added to `mates` - leaving a gap whose `entity_id` names a `mate:NNNN` that does not exist in the package. (The id hole in the allocator predates this change; the dangling entity_id does not.) Fix: Low cost, low frequency - reasonable to do nothing and note it. If you want it closed, read suppression into a local in `ReadMate` and only flush the gap once the IrMate has been appended to `mates`, or have the outer catch drop gaps whose entity_id is the abandoned mate id.
- **extractor/SwReview.Extractor.Tests/PackageWriterTests.cs:71** (csharp-ir-indexer) - `Build_CarriesMateSuppressionThroughToThePackage` is tautological with respect to the change T006 made. PackageWriter.cs line 155 is `package.Mates.AddRange(_mates.Dump(scope))` - the fake IMateSource constructs a finished `IrMate` with `Suppressed = MateSuppressed` (PackageWriterTests.cs line 600) and the writer appends that same object. The test asserts a field the fake itself set on the object under assertion; no production line T006 touched is executed. Reverting MateDumper.cs to the previous hardcoded `Suppressed = false` leaves this test green. The task's "pinned through the same writer tests" is satisfied only in letter: unlike the component path, where the fake yields a `ComponentNode` the writer maps (so Build_CarriesTheComponentsRawConstrainedStatusThroughToThePackage at line 56 genuinely fails if `ConstrainedStatusRaw = node.ConstrainedStatusRaw` is deleted), the mate fake bypasses every mapping line. Fix: Keep the test as cheap regression cover for the AddRange path, but add a comment stating what it does and does not pin, and make the real coverage exist via the must_fix. Do not let this test stand as the evidence that the IsSuppressed2 read works.
- **extractor/SwReview.Extractor.Tests/IrSerializerTests.cs:261** (csharp-ir-indexer) - `Serialize_WritesEnumsAsTheSchemaStrings` is the repo's single DRY home for pinning enum wire strings and was not extended for the new `SuppressTestOutcome`. The sample package only ever serializes `Ok` and `Aborted`, so only "ok" and "aborted" are validated against the schema's enum. `RebuildErrors`, `AlreadySuppressed`, `NotApplied` and `Truncated` have no test producing their wire form. This is a C#-writes / Python-reads boundary (the Python T005 task explicitly covers "all six outcomes"), so a naming mismatch on those four would surface only on a live seat. I hand-checked SnakeCaseLowerNamingPolicy and all six are correct today, so this is a coverage gap rather than a live bug - but nothing stops a future rename from breaking it silently. Fix: Add the four missing SuppressTestOutcome values to Serialize_WritesEnumsAsTheSchemaStrings alongside the existing enum assertions, asserting the exact strings "rebuild_errors", "already_suppressed", "not_applied", "truncated".
- **extractor/SwReview.Extractor/Dump/FeatureTreeIndexer.cs:103** (csharp-ir-indexer) - `if (node == null) { continue; }` inside `Visit` is defensive handling for a scenario that cannot occur - FeatureTreeNode lists are built by the indexer's own caller and by `SubFeatures`, a private-set List<FeatureTreeNode> - and it is the only branch in the file with no test. It also fails silently rather than loudly: a null would be dropped from the tree with no gap and no exception, the opposite of the "unknown stays unknown, absence is a Gap" discipline this feature follows everywhere else. The two ArgumentNullException guards on `Index` (lines 78, 83) are likewise untested, though those are defensible as public-API guards. Fix: Delete the `node == null` branch - the loop variable cannot be null from any caller, and a null-bearing sequence should throw rather than quietly shrink the tree. If you prefer to keep a guard, make it `throw new ArgumentException` and add a test for it; either way, no untested silent skip.
- **specs/001-agentic-design-review/contracts/ir.schema.json:1** (csharp-ir-indexer) - The `--write` regeneration rewrote the entire feature-001 contract file, not just the 1.1.0 additions: previously-inline $defs are hoisted (Design, ExtractorInfo, Manifest, MateEntity, PlaneFace, CylinderFace, BBox3D, SheetView, InterferenceSettings added; PersistRef and Transform removed), `title` annotations are added throughout, and the annotative "default": [] is dropped from mates/holes/interferences/captures/drawings. I verified this is validation-neutral - every shared $def keeps identical `required` and `additionalProperties: false`, top-level `required` and `additionalProperties: false` are unchanged, and the only new property on an existing def is the optional ComponentInstance.constrained_status_raw - so no 1.0.0 package stops validating. But the sole negative test guarding the contract, SchemaValidation_RejectsAPackageThatBreaksTheContract (IrSerializerTests.cs line 36), only checks one id pattern, so a regeneration that HAD loosened `required` or `additionalProperties` would not have been caught by the suite. Fix: No schema change needed - it is faithful. Strengthen the guard instead: extend SchemaValidation_RejectsAPackageThatBreaksTheContract with one case that removes a required field and one that adds an unknown property, so future regenerations cannot quietly loosen the contract. Separately, confirm with the owner that an annotation-only rewrite of the 001 contract is acceptable, since it is broader than "the IR bump adds optional members with defaults only" literally implies.

## Phase 3 (2026-09-16)

- **reviewer/src/swreview/checks/rms/part.py:226** (part-rules) - `_verdict` emits a zero-subject `pass` whenever a rule had neither a violation nor an unresolved subject ("a rule with nothing to look at at all passes vacuously"). Data-model section 2 aggregates `pass` into the `checked` bucket, so a rule that compared nothing is advertised to the engineer as checked. For the sketch rules on a part with no sketches this is defensible (the contract lists no skip), but for `rms.refs.direction` on an ungrouped part, and for `rms.groups.no_solids_in_ref_or_construction` when the group holds only non-content rows, it reports coverage that was never obtained - constitution Principle I, "a healthy feature tree is NOT an engineering acceptance result". Fix: Keep the vacuous pass only where the contract leaves no other bucket, and make the rules that exclude subjects (refs.direction, no_solids) return `skipped(...)` with an explicit reason (e.g. `no grouped content feature`) instead of a bare pass when their comparable set is empty; T021/T022 then record it as skipped coverage rather than checked.
- **reviewer/src/swreview/checks/rms/results.py:188** (part-rules) - `finding()` reads `row.suppressed` as a boolean, so `suppressed is None` - which `data-model.md` section 1 defines as "unreadable" - is silently treated as "not suppressed": the finding carries neither the `suppressed in <configuration>` marker nor any note that the suppression state could not be read. That is a default for engineering data on the evidence path (constitution Principle I: a missing input must stay unknown). Fix: Split the three states: `row.suppressed is True` keeps the current marker; `row.suppressed is None` adds a coverage limit such as `<id> <name>: suppression unreadable` so the unknown is visible in the finding. Add a case to `TestEvaluatePart.test_a_suppressed_feature_is_evaluated_as_present_and_marked` for `suppressed=None`.
- **reviewer/src/swreview/checks/rms/part.py:1004** (part-rules) - `individually_suppressible` raises `NotImplementedError` when the package carries a suppress-test run for this document. Because `evaluate_part` dispatches every evaluable part rule, that raise propagates out of `evaluate_part`, and therefore out of `check_rms_part` (T028) and `swreview check rms` (T029), for any package produced after the T055 `suppress-test` command appended a run - a window that stays open until T057. A whole-review crash is a harsher failure mode than the unresolved item the constitution asks for, and the test at test_rms_part_rules.py:1295 pins the crash rather than a report. Fix: Until T057 lands, return `unresolved(rule, tree.document_id, "suppress-test run present; grading not implemented", rows)` instead of raising, so the review still completes and the gap is visible as unresolved coverage. T057 then replaces the branch wholesale.
- **reviewer/src/swreview/checks/rms/part.py:966** (part-rules) - The observed text of `rms.sketches.one_sketch_per_feature` counts raw ids (`len(row.sketch.consumer_ids)`) but names only the consumers `_consumers()` could resolve to rows of this document, so a consumer id that is not an indexed row yields evidence like "consumed by 2 features: Boss1". A finding whose count and name list disagree is hard to reproduce (constitution Principle VI). Fix: Either name the count of resolved consumers, or append the unresolved consumer ids verbatim (e.g. `... : Boss1, feat:0042 (not in this document's rows)`).
- **reviewer/src/swreview/checks/rms/part.py:336** (part-rules) - `folders_ordered` recomputes the group ordering from `groups_seen` although `GroupAssignment.order_ok` (groups.py:125) already answers exactly that question and, after this change, nothing reads it. Two implementations of one predicate, with slightly different definitions (`order_ok` is strictly-increasing-pairwise; here it is every out-of-order pair), is the DRY violation the constitution calls out, and the dead field invites the two to drift. Fix: Use `tree.assignment.order_ok` as the verdict and keep the pair scan only to name the offending folders (`assert not offenders or not tree.assignment.order_ok or tree.assignment.duplicates`), or drop `order_ok` from `GroupAssignment` if the rule is its only consumer - whichever the T009/T010 owner prefers, but not both.
- **reviewer/src/swreview/checks/rms/results.py:188** (part-rules) - `finding()` decides suppression with `if row.suppressed`, so `suppressed=None` — which the IR defines as "unreadable" (ir/models.py: `suppressed: bool | None`, "null when unreadable") — is treated exactly like `False`: no `suppressed in <configuration>` marker, no coverage limit, no trace that the answer was unknown. That is a default written for engineering data, the one thing the feature constraint rules out, and it is inside the module whose docstring promises "no rule can name a suppressed feature and stay silent about it". No test exercises `suppressed=None`. Fix: Split the three states: `is True` → the existing marker and limit; `is None` → a `coverage_limits` entry such as `<id> <name>: suppression unreadable`; `is False` → nothing. Add a test alongside test_rms_part_rules.py:1366 asserting the null case carries the limit.
- **reviewer/src/swreview/checks/rms/part.py:812** (part-rules) - The sketch carve-out in `rms.detail.no_internal_references` tests `len(row.child_ids or ()) == 1`, but the contract says "a sketch consumed by exactly one feature", i.e. `row.sketch.consumer_ids`. The two are equal today only by accident of the extractor (FeatureDumper.cs:453 sets `ConsumerIds = childIds`) and of the fixture builder (tests/support/features.py:188 defaults `child_names` to `consumers`), so no test can tell the two readings apart; I had to override `child_names` by hand to see the difference. It also counts non-content children (folders, end tags), whereas `_dependents` filters them out, so the exemption and the comparison read the same field through two different filters. Fix: Read the field the contract names: `row.sketch is not None and len(row.sketch.consumer_ids or ()) == 1`, and add one test with a sketch whose `consumer_ids` and `child_ids` differ, so the choice is pinned rather than accidental.
- **reviewer/src/swreview/checks/rms/part.py:1063** (part-rules) - `_tree_not_read` invents a second mechanism and a second wording for "this document has no component instance": it returns `no component instance for <doc>; tree not read`, while report.py:129 already converts that exact condition into an unresolved coverage item reading `no component instance for <doc>` (data-model.md:126-130 assigns the condition to the report layer). Two phrasings for one condition is the "never add a second mechanism" case, and the branch has no test — I had to call `evaluate_part` with an empty `instances=[]` to reach it. Fix: Either drop the branch (return `None` and let report.py own the no-instance case — the 18 rules then evaluate an empty tree and pass vacuously, which report.py converts anyway), or keep it and add a test plus a comment saying why the wording differs from report.py's. My recommendation is to drop it: one condition, one owner.
- **reviewer/src/swreview/checks/rms/results.py:106** (part-rules) - `RuleResult.__post_init__` carries four invariant guards (result-without-outcome, outcome-without-result, missing reason, reason where none is allowed, mismatched `CheckResult.check`) and `finding()` carries a coverage-only guard, and not one of them is exercised by any test: no `pytest.raises` touches this module anywhere in reviewer/tests. Since T019 owns the part rules and T021 owns the report layer, no task currently owns results.py's own contract, so the module that exists precisely to enforce the outcome invariant is the untested one. Given the "rather too many tests than too few" standard this is the largest coverage hole in the change. Fix: Add a short `tests/unit/test_rms_results.py` (or a class in the part-rules file) asserting each guard raises with its message, plus `severity_of` for one warn / one medium / both high rules and `subject_input`'s exact format string.
- **reviewer/src/swreview/checks/rms/part.py:1004** (part-rules) - `individually_suppressible` raises `NotImplementedError` when the package does carry a run for this document. Until T057 lands, any package with an `rms_suppress_test` for a graded part will crash `evaluate_part`, and therefore `check_rms_part` / `swreview check rms` — an uncaught exception out of a check tool rather than a degraded result. The test at tests/unit/test_rms_part_rules.py:1295 pins the crash as intended behaviour, so the risk is deliberate, but it is a live landmine for whoever runs US4's suppress-plan before T057. Fix: Return `unresolved(rule, doc, "suppress-test outcome table not implemented", rows)` instead of raising, and keep the test asserting that reason (T057 then replaces the reason with the real table). If you prefer the loud failure, at least confirm no US4 path can produce a run before T057 merges.
- **reviewer/src/swreview/checks/rms/part.py:493** (part-rules) - Two smaller gaps in `holes_last`. (a) The no-hole-plus-unclassified branch (lines 493-503) hand-rolls the `unresolved(...)` construction that `_verdict` already owns — the one duplication of that shape in the module — and it is untested: I had to run a Detail group of only `Frobnicate`+`ICE` to see it emit `skip` + `unresolved` together. (b) On the pass path the subjects are `block` (the graded rows from the first hole onward), so Detail features examined *before* the first hole are named in no result at all; the test at line 366 asserts exactly `["Hole1", "Hole2"]` and so pins the narrower set without flagging it. Fix: For (a) route the branch through a small helper (or call `_verdict` with `violation=None, passing=(), unknown=unknown` after emitting the skip) and add the Detail-with-no-hole-but-unclassified test. For (b) decide whether a `checked` result should name every graded Detail feature or only the trailing block, and assert that choice.
- **reviewer/src/swreview/checks/rms/part.py:333** (part-rules) - `folders_ordered` skips on `len(seen) < 2`, where `groups_seen` counts group-folder *occurrences*, not distinct groups. A part with a single group folder duplicated (e.g. `3-Core` twice) therefore does not skip — I verified it reports `fail` naming both occurrences — although the contract's skip condition reads "fewer than two groups". I think the current behaviour is the right one (a duplicate must be reported, and skipping would hide it), but it is an unrecorded reading of the contract and nothing tests it: the existing duplicate test at line 190 uses two occurrences of one group only in combination with content, and no test asserts the one-group-duplicated shape skips-or-fails deliberately. Fix: Add a test for `[folder(CORE), folder(CORE)]` asserting `fail` with the duplicate observed text, and one comment line in the rule saying that occurrences, not distinct names, decide the skip — so a future reader does not "fix" it toward the literal contract wording.
- **reviewer/src/swreview/cli.py:873** (report-tools-cli-golden) - DRY / drift: `check interference` gets its whole payload from the golden adapter (`golden_interference.interference_case`, cli.py:754), so what the CLI prints and what the 001 baseline pins are literally the same code path. `check rms` does not: it re-implements the package+exceptions wiring (`_context_for(..., exceptions=True)`, cli.py:575) and re-implements coverage flattening in `_coverage_rows`/`_coverage_counts` (cli.py:873-888), which is a copy of `checks/golden_rms.py:_coverage` (cli.py derives the bucket list from `get_args(CoverageBucket)`, golden_rms.py:38 hard-codes the same five names). Two mechanisms for the same answer, one of them a hand-maintained bucket list, and the CLI can now drift from the baseline that is supposed to pin it. Fix: Have `check_rms_command` call `golden_rms.part_case(package)` for `--scope part` (it already accepts a directory and reads `exceptions.json` from it) and delete `_context_for(..., exceptions=True)`, `_coverage_rows` and `_coverage_counts`; move the one shared bucket tuple into `report/session.py` (or derive it from `get_args(CoverageBucket)` in golden_rms too). If `--document` filtering makes `part_case` unsuitable, give `part_case` the optional `document_ids` argument rather than keeping a second path.
- **reviewer/src/swreview/tools/rms_query.py:155** (report-tools-cli-golden) - `list_features(document_id, folder=<group name>)` returns `[]` when that group does not exist in that document: `folder in table.groups` succeeds against the table, then the comprehension at line 169 matches nothing. The module's own docstring (line 32) states the opposite rule - "an argument that names nothing in the package comes back as an error result, so the model sees the mistake as a tool result instead of as an empty list it reads as 'nothing there'" - and the tests cover a non-group name (`7-Nowhere`), a non-folder id and a foreign folder id but not this case. A model asking "what is in 6-Quarantine?" on a part that has no Quarantine folder is told the group is empty, which is the answer `rms.quarantine.*` skips on, not the answer the tree gives. Fix: In `_in_folder`, when `folder` is a group name, return `None` (error result) unless the group is in `assignment.groups_seen`, with a message that distinguishes the two states, e.g. `document 'doc:3' has no '6-Quarantine' group folder; its groups are ['3-Core', '4-Detail']`. Add a test for it next to `test_list_features_refuses_a_folder_that_names_nothing`.
- **reviewer/src/swreview/checks/rms/report.py:415** (report-tools-cli-golden) - `_write_summary` reaches into the session's coverage lists directly (`items[:] = [...]`, line 418) to pull the `modeling.resilience` item out of whichever of `checked`/`unresolved` it was in before. That is a second write path into `session.coverage` outside `ToolContext.record_coverage`/`replace_coverage`, and unlike `replace_coverage` it is done from the report layer, so nothing is emitted on the run's event stream: the pane sees the summary appear in `checked` and is never told it left `unresolved` (FR-013's whole point is that the session and the event stream cannot drift). The comment at line 66-68 acknowledges `replace_coverage` cannot express the move but solves it by bypassing the context rather than extending it. Fix: Add the cross-bucket case to the context - e.g. `ToolContext.replace_coverage(check, bucket, item, drop_from=other_buckets)` or a `move_coverage(check, bucket, item)` that removes the check from every bucket and records it in one - and call that from `_write_summary`. Keeps one path into the session and one event per change.
- **reviewer/src/swreview/checks/rms/report.py:397** (report-tools-cli-golden) - The `modeling.resilience` summary counts only items whose check is a dispatched rule (`RULES.get(item.check)` with `rule.coverage is None`). Excluding the four data-gap rules is defensible and documented; silently excluding `rms.types.unknown` is not. A package whose tree carries type names the calibrated table does not know, none of which any class-dependent rule happens to read (an unknown type sitting in Core, say), ends the review with `rms.types.unknown` in `unresolved` and `modeling.resilience` in `checked` - the summary line the engineer reads says the method was graded while the grader could not name part of the tree. Fix: Count `UNKNOWN_TYPES_CHECK` toward the summary's unresolved bucket (it is already written through `replace_coverage`, so it is in the session when `_write_summary` runs), and name it in the reason, e.g. `... ; 2 unknown type name(s)`. Add a test for the case where an unknown type produces no unresolved rule result.
- **reviewer/src/swreview/checks/rms/report.py:86** (report-tools-cli-golden) - `report_results` returns the first finding-build error immediately, after earlier findings have already been appended to the session and before any coverage, census, data-gap or summary item is written. The session is then left with N findings, no coverage for any rule, and no `modeling.resilience` item - a half-written review that reads like a run that found violations and graded nothing. It is reported (not silent), but the partial state survives in `session.json`. Fix: Either write coverage before bailing out (compute and write the aggregated items, then return the error), or collect the per-result errors and report them all in the returned envelope while still writing the coverage for everything that did report. Recommend the first: it is two lines and it keeps the invariant that every call either writes both halves or neither. Add a test with a result whose `CheckResult` cannot build a finding.
- **reviewer/src/swreview/checks/rms/report.py:213** (report-tools-cli-golden) - DRY: `_waived` (213) and `_needs_review` (236) duplicate `checks/interference.py:_excepted` (314) and `_needs_review` (336) almost sentence for sentence - same status change, same 'accepted by X on Y for this <geometry|feature tree> and configuration' observed text, same 'Re-review <id>' recommended action. The only real differences are the noun and the coverage-limit format (`exception:<id>: <note>` here, `exception:<id>` there, per data-model section 2). Two copies of "how an accepted exception rewrites a CheckResult" will drift the next time the exception wording changes. Fix: Extract one helper - e.g. `checks/exception_text.py: apply_exception(result, exception, *, subject_noun, limit_note: bool)` - and call it from both. Low risk: both call sites are covered by tests and by a golden baseline (bracket-assy-interference, rms-part), so a wording regression would show up as a diff.
- **reviewer/src/swreview/cli.py:830** (report-tools-cli-golden) - Two smaller gaps in `check rms`. (1) For `--scope assembly|equations` the command validates `--document` through `part_documents` and then throws the result away (`documents = []`, line 830), so `check rms --scope assembly --document doc:2` silently reports `0 part document(s)`; `test_check_rms_scope_assembly_reports_that_it_is_not_in_this_build` bakes that in. (2) There is no test for a corrupt `exceptions.json` on this command, although `_context_for(..., exceptions=True)` newly reads one and `check interference` has exactly that test (`test_check_interference_with_a_corrupt_exceptions_file_exits_1`) - the path where a waiver file that cannot be parsed must exit 1 rather than grading as if nothing were accepted. Fix: (1) Either reject `--document` with a scope that does not use it, or keep reporting the documents the run would have graded; do not validate then discard. (2) Add the corrupt-exceptions test for `check rms` mirroring the interference one, and one asserting the file is left byte-for-byte after a run that has an exception file with real records (the existing byte-for-byte test uses an empty file, so `refresh` never rewrites anything).
- **reviewer/src/swreview/tools/rms_query.py:155** (report-tools-cli-golden) - `_in_folder` accepts any name in `table.groups`, so `list_features(document_id, folder="6-Quarantine")` on a document that has no Quarantine folder returns `[]` (verified). The module docstring, lines 20-26, states the opposite rule for this tool: "an argument that names nothing in the package comes back as an error result, so the model sees the mistake as a tool result instead of as an empty list it reads as 'nothing there'". The existing negative tests only cover a name that is not a group at all (`7-Nowhere`), a non-folder feature id, and another document's folder id, so the case the docstring is about is the one case not covered. Fix: Recommended: when `folder` is a group name that `assignment.groups_seen` does not contain for this document, return `error_result(f"document {document_id!r} has no {folder} group")`, and add the test beside `test_list_features_refuses_a_folder_that_names_nothing`. Alternative (do nothing): defensible only if an empty list is deliberately the answer for "the group exists in the method but not in this tree" - if so, say that in the docstring and pin it with a test, because right now code and docstring disagree.
- **reviewer/src/swreview/agent/prompts/system_v1.md:30** (report-tools-cli-golden) - The new step 4 instructs the model to run `check_rms_assembly` and `check_rms_equations`. Neither is registered in this build: `tools/registry.py::check_tools()` adds only `rms_checks.check_rms_part` (they arrive in T035 and T042). A review run on this build will emit two unknown-tool errors and two `tool.*` entries in the `failed` coverage bucket. Nothing guards prompt-vs-registry agreement - no test references `prompts/system_v1.md` at all. Fix: Option A (recommended): word step 4 for what exists now (`check_rms_part`, plus one sentence that the assembly and equation rules arrive with their own tools) and re-expand it in T035/T042, which already touch this area. Option B (do nothing): acceptable only if no review run happens between now and T042 - it costs two failed tool calls per run until then. Either way, a test asserting every backticked `check_*`/`list_*` name in the prompt is a registered tool name would be cheap and would have caught this.
- **reviewer/src/swreview/checks/golden_rms.py:57** (report-tools-cli-golden) - The directory branch of `part_case` is dead: nothing calls `part_case` with a path. `golden_interference.interference_case` has the same branch and the CLI reuses it (`cli.py` line 753), but `check_rms_command` re-implements the load/refresh sequence itself in `_context_for(..., exceptions=True)` (cli.py lines 575-587). So the repo now has two code paths that load a package, load `exceptions.json`, refresh it in memory and run the RMS part rules - the DRY violation the file's own docstring says it exists to prevent. Related: `tests/golden/fixtures/rms-part/exceptions.json` is a second copy of the exception the harness never reads (the harness passes `case.json` `kwargs.exceptions` inline), so the two copies can drift. Fix: Recommended: keep the CLI as it is (it needs per-document dispatch and scope reporting that `part_case` does not offer) and delete the unused directory branch from `part_case`, or keep the branch and drop `exceptions.json` from the fixture - not both. My preference is to delete the directory branch and the fixture's `exceptions.json`: one input, one path, nothing to drift. If you would rather mirror `interference_case` exactly for symmetry, then keep the branch and add the test that exercises it (`part_case(fixture_dir)` equals the inline result), which is what makes a branch real.
- **reviewer/src/swreview/checks/rms/report.py:318** (report-tools-cli-golden) - `_write_unknown_types` rebuilds the `rms.types.unknown` item from only the documents in the current call's results and writes it with `replace_coverage`, so a narrow second call erases a wider first call's census. After `check_rms_part()` reports `NoSuchType x2` for doc:4, `check_rms_part(document_id="doc:3")` on a package where doc:3 has a different unknown type replaces the item and doc:4's unknown types vanish from the report. Per-rule coverage does not have this problem because it is keyed per rule and only the re-evaluated rules are replaced. The early `return []` at line 318 means the erase happens only when the narrow call has its own unknowns, which makes it intermittent rather than absent. No test covers a narrow re-run's effect on the census. Fix: Recommended: merge rather than replace - read the existing `rms.types.unknown` item off the session, union its documents and counts with this call's, then `replace_coverage` the merged item; add a test for `check_rms_part()` followed by `check_rms_part(document_id=<other part>)`. Alternative (cheaper, also defensible): compute the census over every document in the package rather than over the call's scope, since it is a package-wide census and not a per-call answer. Do nothing is tenable only because the agent normally calls the tool once with no argument.
- **reviewer/src/swreview/checks/rms/report.py:418** (report-tools-cli-golden) - `_write_summary` drops the summary from the bucket it is leaving by mutating the session list in place (`items[:] = [...]`) instead of going through `replace_coverage`/`record_coverage`. `ToolContext.record_coverage`'s docstring (context.py) says the `record_*` methods are "the only places a finding, a coverage item or an evidence request reaches the session, so 'written to the session' and 'on the event stream' cannot drift apart"; this is a second write path, and a streaming pane sees the summary appear in `unresolved` while never being told it left `checked`. Fix: Recommended: add the one primitive this needs - e.g. `ToolContext.move_coverage(check, from_bucket, to_bucket, item)` or let `replace_coverage` take the buckets to clear - so the removal emits an event like every other coverage write. Low effort, keeps one write path. Alternative (do nothing): the only consumer that would notice is the live pane, and the session file ends up correct either way - but then say so in a comment at line 415, because the code currently reads as an accident.
- **reviewer/tests/golden/fixtures/rms-part/generate_package.py:193** (report-tools-cli-golden) - The `widget()` docstring - which is the hand-check artifact T030 asks for - says `Boss-Extrude1`'s null `child_ids` is "for the three reference rules". The baseline shows it reaches only one: `rms.refs.direction` is unresolved for doc:4, while `rms.refs.quarantine_has_no_children` and `rms.detail.no_internal_references` are `checked` for doc:4, because those two only report a null `child_ids` on a feature that is in Quarantine or in Detail, and `Boss-Extrude1` is in Core (contracts/rules.md, unresolved conditions for those two rows). The fixture is right; its stated intent is not. Fix: Recommended: correct the sentence (name `rms.refs.direction` alone), or - better, and what I would choose - move the null-child feature into Detail as well as keeping one in Core so the fixture actually seeds all three unresolved conditions and the golden covers `rms.detail.no_internal_references`'s unresolved branch, which nothing in the baseline currently does. Regenerating the baseline is one command and this is the moment to do it, before more tasks pin it.
- **reviewer/tests/golden/test_golden/rms-part.yml:1** (report-tools-cli-golden) - The baseline pins an interpretation of contracts/rules.md that the contract does not obviously give: `part.py::_verdict` (line 224, "A violation replaces the pass rather than joining it") means a document that fails a rule for one subject never appears in that rule's `checked` item, even when most subjects comply. In this baseline doc:3 appears in no `checked` row at all, although e.g. `rms.sketches.fully_defined` has three compliant sketches there. rules.md's preamble says a rule "produces one RuleResult per outcome it reaches on that document" and that it "may therefore appear in more than one bucket (finding, checked, skipped, unresolved) for a single document" - which reads as allowing finding+checked together, and the implementation does emit checked+unresolved together (doc:4, `rms.detail.holes_last`). Fix: This is T020's decision, but the golden is what freezes it, so it should be settled before more baselines are generated. Recommended: keep the current behaviour (a document that fails a rule is not also 'checked' for it - that is the more useful report and the coverage counts stay readable) and write the interpretation down in part.py beside `_verdict` plus a line in the golden's generator docstring, so the next reader does not read the preamble and 'fix' it. Alternative: emit the pass alongside the violation and regenerate rms-part.yml - closer to a literal reading of the preamble, but it makes every failing document also 'checked', which reads badly in the coverage summary.
- **extractor/SwReview.Extractor/Dump/SwFeatureReader.cs:218** (csharp-dumper-probe) - `Features()` turns anything that is not an `object[]` into an empty list, and drops non-IFeature items from an array that is one, with no gap either way. The comment says 'the dumper's id mapping is what notices a feature it cannot name', but the dumper only ever sees the filtered list, so both cases are invisible. This matters because `[]` is a pass-producing value: rules.md rows 37, 38, 39 and 43 treat a non-null `child_ids`/`consumer_ids` as an answer, and 43 says outright 'a sketch with no consumer passes'. research.md R5 open question 2 ('whether GetChildren and GetParents return values for ordinary features on 2024') is exactly the case where every feature would get `[]` and three fail rules would pass on every part. Fix: At minimum, record a `feature_children`/`feature_parents` gap when the answer is a non-empty array none of whose items is an IFeature (an answer that came back and could not be read is not 'no dependents'). Keep `null -> []` only if T062 confirms 2024 returns an empty VARIANT for a childless feature, and say so in the comment instead of the current claim that the dumper notices; make T062 sign-off on question 2 a gate for US1 rather than a later calibration.
- **extractor/SwReview.Extractor.Tests/FeatureDumperTests.cs:588** (csharp-dumper-probe) - `Dump_FeedsTheTypeNameCensusNothing` asserts `Unconsumed()` is empty, which is weaker than the T023 sentence 'TypeNameCensus receives no pass from this dumper'. A pass with `Consumed: false` would fail the test (good), but a pass with `Consumed: true` also leaves `Unconsumed()` empty - and that is the more damaging mistake, because marking every walked type consumed would suppress the unsupported-type gap that HoleDumper's and MateDumper's passes exist to raise for the same document. Fix: Assert that no pass was recorded at all rather than that no name is unconsumed: seed the census with a pass of the document's own types marked unconsumed before calling Dump and assert those names are still reported afterwards, or expose a pass count / document count on TypeNameCensus for the test to assert is zero.
- **extractor/SwReview.Extractor/Dump/FeatureDumper.cs:462** (csharp-dumper-probe) - ReadFillet (line 462) and ReadSketch (line 424) raise `fillet_radius` and `sketch_status` gaps for features that have neither. When `GetDefinition` fails on a folder or an extrusion the gap says the radius is unknown for a feature that never had one, and the same for `GetSpecificFeature2` on a non-sketch. Those gaps inflate the unresolved coverage the report aggregates per document (data-model section 2, coverage aggregation) with subjects no rule grades. Fix: The sketch case is genuinely unknowable (a failed GetSpecificFeature2 means we do not know whether it is a sketch) and should stay. For the fillet case, the failure is only meaningful once the feature is known to be a fillet - consider raising the `fillet_radius` gap only for the classified Simple/Variable paths and recording a `feature`-level gap for a GetDefinition that failed outright, so the fillet rule's unresolved count means fillets.
- **extractor/SwReview.Extractor/Dump/FeatureDumper.cs:273** (csharp-dumper-probe) - The `feature_tree_configuration` gap is written from `other.Node.ReferencedConfiguration` (the configurations the document is actually instanced under), but the Python consumer deliberately does not parse the reason and instead names `document.configurations` minus the one the tree was read in (reviewer/src/swreview/checks/rms/report.py:180-186). For a document that has configurations it is not instanced under, the coverage limit 'other configurations not read: <names>' will name configurations no instance uses, and the two layers disagree about what the gap means. Fix: Pick one definition and state it in both places. Either C# writes the gap for every other configuration the document owns (matching what Python prints), or Python derives the names from the instances' referenced configurations. Cross-layer, so agree it with the owner of report.py before changing either side.
- **extractor/SwReview.Extractor.Console/Program.cs:655** (csharp-dumper-probe) - RunProbe's own parsing has no test. CommandLineOptionsTests covers `--features`, the probe's option list and `--allow-start`, but nothing exercises `probe` with no subject (UsageError), `probe --doc x` (subject that starts with `--`), or `probe notrms` (unknown subject), and nothing asserts the new usage lines. Constitution III asks for the invalid-input tests alongside the implementation, and these three branches are the ones a typo hits. Fix: Extract the subject check into an internal static helper on Program (as KnownOptions already is) and add three CommandLineOptionsTests cases for it, plus one asserting the usage text lists `probe rms`.
- **extractor/SwReview.Extractor/Dump/FeatureDumper.cs:415** (csharp-dumper-probe) - Every feature of every resolved part gets GetChildren, GetParents, Description, GetErrorCode2, IsSuppressed2, GetSpecificFeature2 and GetDefinition - seven out-of-process COM round trips each, GetChildren and GetParents being the slow ones. research.md R7 forbids filtering by type name (that is the whole point of the design), so there is no cheap filter available, but the constitution's technical constraint that out-of-process calls stay coarse makes this the phase most likely to dominate dump time on a real assembly. Fix: Do not change the design, but make T062 measure it: have `probe rms` print elapsed time for the walk and for the per-feature reads so the workstation run produces a number, and decide there whether the feature phase needs a per-document budget or a documented expectation in cli.md.
- **extractor/SwReview.Extractor.Tests/FeatureDumperTests.cs:299** (csharp-dumper-probe) - T023 names "description null plus gap", and FeatureDumper.cs:368 has a dedicated branch for it - Description READ successfully and answered null - which writes the feature_description gap so that blank ("") and unknown (null) stay different answers. No test exercises it: FakeFeature.Description defaults to string.Empty and no test assigns null (only DescriptionThrows, which reaches the gap through TryStep instead). Deleting the whole `if (descriptionRead && description == null)` block leaves the suite green, and a null-returning interop property is the likelier real-world shape than a throwing one. Fix: Add a case that sets `boss.Description = null` with DescriptionThrows false and asserts row.Description is null, one gap with EntityKind feature_description and EntityId feat:0001, and GapKind.NotExtracted (not ToolError) so the two routes to the gap stay distinguishable.
- **extractor/SwReview.Extractor/Dump/FeatureDumper.cs:200** (csharp-dumper-probe) - The "active configuration could not be read -> drop the document's rows" branch in DumpDocument is untested. FakeDocument has a WalkFailure switch (used by Dump_WalkThatFailed_...) but no ActiveConfiguration failure switch, so neither the TryStep-false path nor the `configuration == null` path is covered; the comment says a tree labelled with the wrong configuration is worse than no tree, and nothing pins that decision. Fix: Add `ActiveConfigurationFailure`/null support to FakeDocument and a test asserting no rows for that document, a `feature` gap naming the document id, and that another document still dumps - mirroring Dump_WalkThatFailed_IsAGapAndTheOtherDocumentsAreStillRead.
- **extractor/SwReview.Extractor/Dump/FeatureDumper.cs:228** (csharp-dumper-probe) - FeatureTreeIndexer was changed (Phase 2 file) from skipping a null node to throwing ArgumentException, and its new comment says "the caller turns this into a gap rather than a quietly shorter tree". The caller does not: Index is called bare here, outside any TryStep, so the exception escapes Dump() to PackageWriter.RunPhase, which records one generic "Failed to read the part feature trees" gap with a null entity id and discards the rows of EVERY document, including the ones already collected. probe rms (Program.cs:718) calls Index bare too and simply exits 1. The claim in the comment is only true at the FeatureTreeIndexerTests level, where the test asserts the throw, not the recovery. Fix: Wrap the Index call in scope.Gaps.TryStep("feature", documentId, ...) and return Array.Empty<Feature>() on failure - same shape as the walk failure - plus a FeatureDumperTests case with a null node in the walk asserting one document is lost and the other still dumps. Otherwise correct the indexer comment to say the caller does not localise it.
- **extractor/SwReview.Extractor/Dump/FeatureDumper.cs:311** (csharp-dumper-probe) - When row.Node.Handle is null, ReadFeature returns null and the caller writes the gap reason "'<name>' in <file> has no persistent reference" - which is not what happened; the walk produced a node with no live feature. An engineer chasing that gap looks for a persistent-reference problem that does not exist. The path is also untested (Node() always sets Handle). Fix: Either distinguish the two causes (return a small reason from ReadFeature, or write the handle gap at the point of detection) or drop the handle null-check and let the PersistRef read fail naturally; add a test for whichever you choose.
- **extractor/SwReview.Extractor/Ir/SuppressTestRun.cs:28** (csharp-dumper-probe) - SuppressTestRow.Outcome's default was changed from Truncated to Aborted, in a Phase 2 file that is not in T023/T024/T031's scope. data-model.md line 74 says the opposite for the contract's own meaning of a row nobody attempted: "One per planned feature, in plan order (len(rows) == features_present; untested ones are `truncated`)", and T054 requires --limit/--timeout-seconds rows to be `truncated` (a skip) rather than `aborted` (unresolved). The change is safe only while every row's outcome is set explicitly; T055 is the task that will construct those rows and is not written yet. Fix: Confirm the deviation with the user. If it stays, say in the XML comment that T055 must set Truncated explicitly for limit/timeout rows (the default is a last-resort, not the truncation mechanism), so the next agent does not inherit `aborted` for deliberately untested features.
- **reviewer/tests/golden/test_golden/cover-blind-tap.yml:46** (csharp-dumper-probe) - The feature 001 golden baseline is NOT byte-identical any more: +10 lines adding the `modeling.resilience` checklist item to the rendered checklist. The stated constraint for this feature is that feature 001 and 002 goldens stay byte-identical. The change comes from T028's checklist_v1.yaml edit (another agent, outside this group), not from T023/T024/T031, but nothing in the suite flags it as a constraint breach - the golden was simply regenerated. Fix: Reconcile explicitly before T061: either accept that adding a checklist item necessarily re-renders every feature-001 golden and record that decision, or keep the RMS checklist item out of the rendered baseline. Do not let it pass as an unremarked regeneration.
- **extractor/SwReview.Extractor.Console/Program.cs:738** (csharp-dumper-probe) - ProbeFeatures takes `configuration` from Describe(() => reader.ActiveConfiguration(...)), so when that read fails the variable holds the error text ("!InvalidOperationException: ...") and is then passed to reader.Suppressed(handle, configuration) as a configuration NAME. IsSuppressed2 with a name that does not exist answers false for every feature, and the probe prints suppressed=False for a whole tree - a calibration run reading that would record the wrong answer for R5 item 6. Fix: Keep the configuration as string? from a non-Describe read; when it is unavailable print `suppressed=(no configuration)` and skip the IsSuppressed2 call rather than asking with a bogus name.
- **extractor/SwReview.Extractor/Dump/FeatureDumper.cs:420** (csharp-dumper-probe) - SketchInfo.ConsumerIds is assigned the SAME List<string> instance as Feature.ChildIds (ReadSketch(..., childIds) stores the reference). Serialisation is unaffected today, but any later code that mutates one list silently mutates the other, and the aliasing is invisible at the call site. Fix: Assign a copy: `ConsumerIds = childIds == null ? null : new List<string>(childIds)`, keeping the null-means-unavailable distinction the test pins.
- **reviewer/src/swreview/exceptions.py:223** (carry-over-python) - `_feature_row` hashes `feature.child_ids` as a raw list, so the feature-tree digest is sensitive to the order `GetChildren` happened to return dependents in. The module docstring (line ~15) promises the opposite - "It is order-independent, so the order SOLIDWORKS happened to report components and faces in cannot flip an exception" - and the geometry path keeps that promise by sorting face tokens. Every rule that reads `child_ids` (`rms.refs.direction`, `rms.refs.quarantine_has_no_children`, `rms.detail.no_internal_references`) treats it as a set, and SOLIDWORKS promises no ordering from `GetChildren` (research.md R5 leaves it to be confirmed on the workstation). A rebuild that reorders the children re-opens every RMS exception on that document as `needs_review` although nothing a rule reads has changed - churn that pushes engineers to stop trusting the mechanism. Fix: Hash `None if feature.child_ids is None else sorted(feature.child_ids)` (keeping null distinct from empty), and add a test alongside `test_the_feature_tree_fingerprint_is_independent_of_row_order` that reverses one row's `child_ids` and asserts the digest is unchanged.
- **reviewer/src/swreview/checks/rms_types.py:265** (carry-over-python) - The new `REQUIRED_KEYS`/`_require` guard covers only top-level keys, but `_parse` still indexes the nested assembly keys directly: `assembly["reference_entity_kinds"]`, `assembly["geometry_entity_kinds"]`, `assembly["mate_chain_depth_limit"]`. A table whose `assembly:` block is missing a key raises a bare `KeyError('reference_entity_kinds')` with no file path, and one whose `assembly:` is a list raises `TypeError`, which is exactly the failure mode the `_require` docstring says was being removed ("refused by name rather than surfacing a bare KeyError from wherever it happened to be read"). `document["version"]` has the same hole: a non-numeric version surfaces `invalid literal for int()` with no path. The parametrized test only walks `REQUIRED_KEYS`, so the gap is invisible. Fix: Either extend the guard to the nested block (an `isinstance(assembly, dict)` check plus a `_require`-style loop over the three assembly keys, with `ASSEMBLY_REQUIRED_KEYS` parametrized in the test the same way), or narrow the `REQUIRED_KEYS` docstring to say it covers the top level only and state why the nested block is left bare. The first is the honest option given the rest of this loader's error handling.
- **reviewer/src/swreview/checks/rms/registry.py:59** (carry-over-python) - A module-level `assert` is used to enforce a production invariant (`RuleCoverageBucket` is a subset of `report.session.CoverageBucket`). Python strips asserts under `-O`, so the guard is not reliable; and it is already asserted twice in `test_rms_registry.py::TestCoverageBuckets`, so it is a third copy of the same fact (DRY: "never add a second mechanism for something the repo already has"). Its only effect at runtime is to pull `swreview.report.session` into the rule catalogue module, coupling the one list of rules to the report layer for no behavioural reason. Fix: Drop the `assert` and the `from swreview.report.session import CoverageBucket` import (and the now-unused `get_args`), leaving the two existing tests as the guard; or, if the coupling is wanted, make it a real `raise ValueError` at import so `-O` cannot remove it. Prefer the first - the tests already run on every change.
- **reviewer/src/swreview/tools/recording.py:52** (carry-over-python) - The carry-over fix passes `exception_id` and `tool_result_ids` by keyword at the `record_result` call site, and the new test's docstring states the guarantee as fact - "The two optional arguments are passed by keyword, so an added parameter cannot silently slide one into the other's place" - but the signatures of `result_to_finding` (line 52), `record_result` (line 97) and `record_results` still accept them positionally. The convention is enforced nowhere; the next caller (T022's `checks/rms/report.py` is already one) can still pass them positionally and the same latent swap returns. All six current call sites already use keywords, so the enforcement is free. Fix: Insert a bare `*` before `exception_id` in `result_to_finding` and `record_result` and before `tool_result_ids` in `record_results`, making the optional provenance arguments keyword-only. No call site changes.
- **reviewer/tests/unit/test_ir_features.py:490** (carry-over-python) - `test_the_fixture_builder_emits_only_documented_gap_entity_kinds` does not do what its docstring claims. The docstring says it "anchors `NEW_GAP_ENTITY_KINDS` to something that actually writes a gap" because "the parametrized test above would pass for any tuple at all", but the assertion is `emitted <= set(NEW_GAP_ENTITY_KINDS)` over a package that emits only two kinds (`feature_tree_unavailable`, `mate_suppression`). An invented eleventh entry in the tuple, or a documented kind no producer ever writes, still passes; only `assert emitted` (non-empty) and the subset direction are actually checked. The claim in the docstring is stronger than the test, which is the kind of assertion that gets trusted later. Fix: Assert the exact set for that package (`assert emitted == {"feature_tree_unavailable", "mate_suppression"}`), or build the union over builder scenarios that exercise each documented kind and assert equality with `set(NEW_GAP_ENTITY_KINDS)`. Failing that, reword the docstring to claim only what is checked.
- **reviewer/src/swreview/ir/models.py:551** (carry-over-python) - The two new `SuppressTestRun` invariants (`len(rows) == features_present`, and `unrestored_feature_ids` empty when `restore_verified`) plus `limit`/`timeout_seconds` `ge=1` are enforced only on the Python side, where a violation is a `ValidationError` raised while loading `package.json`. `rms_suppress_test` is an optional, audit-only member, but a bad run makes the whole package unloadable - every check dies, not just `rms.detail.individually_suppressible`. The C# producer carries none of these: `extractor/SwReview.Extractor/Ir/SuppressTestRun.cs` defaults `Limit` and `TimeoutSeconds` to `0` (which Python now rejects) and has no row/count guard, so `AppendSuppressTest` can write a file the reviewer refuses to open. Fix: Mirror the invariants at write time in T055's `PackageAppender.AppendSuppressTest` (throw naming the mismatch, with `PackageAppenderTests` cases for a short row list, a non-zero `unrestored_feature_ids` with `restore_verified`, and a zero limit/timeout), so a malformed run is refused where it is produced rather than bricking the package for every other check.
- **reviewer/src/swreview/exceptions.py:218** (carry-over-python) - `_feature_row` hashes two fields beyond the list data-model.md section 3 enumerates: `sketch is not None` (line 218) and `fillet is not None` (line 221). The reasoning in the docstring is sound (absent sketch vs unreadable sketch are different facts and drive different rule outcomes), and T015's own test pins it - but section 3 spells the hashed fields out one by one and has not been updated, so the implementation and the normative contract now disagree. specs/003 is off-limits to the implementer, which is exactly why the deviation has to be surfaced rather than left in a docstring. Fix: Report the deviation explicitly to the feature owner so section 3's field list gains the two presence flags (or the flags are removed if the owner disagrees). Until then, add a one-line comment at the call site pointing at the open contract question rather than leaving the divergence implicit.
- **reviewer/src/swreview/exceptions.py:419** (carry-over-python) - `ExceptionStore.accept` binds any check whose id starts with `rms.` to a feature-tree fingerprint without checking that the rule is `fail`-severity. `contracts/rules.md` ("Waivable rules") is normative: "Only `fail`-severity rules are acceptable as exceptions; a waiver naming a `warn`, data-gap, or out-of-scope rule is reported invalid and changes nothing", and data-model section 3 repeats it. Nothing in the module, and no test in `test_exceptions_feature_tree.py`, prevents a `warn` rule (`rms.folders.present`) or a coverage-only one (`rms.advisory.avoid_multibody`) from being accepted today. Fix: Confirm the guard lands in T045's `exceptions accept-rms` (which owns the four per-id statuses including `invalid (warn rule)`), and add a test there that a `warn` and a coverage-only rule id are both refused. If the store is ever reachable by another path, the cheapest durable guard is for `accept` to consult `RULES[check].severity` for `rms.*` ids and refuse a non-`fail` rule outright.
- **reviewer/src/swreview/checks/rms_types.py:264** (carry-over-python) - `REQUIRED_KEYS`/`_require` was added so a malformed table is "refused by name rather than surfacing a bare KeyError", but the guard only covers top-level keys. Two lines later `assembly["reference_entity_kinds"]`, `assembly["geometry_entity_kinds"]` and `assembly["mate_chain_depth_limit"]` are indexed unguarded, and `assembly` is never checked to be a mapping: a table with `assembly: {}` still raises a bare `KeyError`, and `assembly: []` raises `TypeError: list indices must be integers`. `test_a_table_missing_a_required_key_is_refused` is parametrized over top-level keys only, so nothing covers this. Fix: Check the three assembly sub-keys the same way (an `_require`-style call against the `assembly` mapping, plus an `isinstance(assembly, dict)` refusal with the same `f"{path}: ..."` shape), and add the matching parametrized test beside `test_a_table_missing_a_required_key_is_refused`. Same mechanism, no second one - it is ~6 lines and closes the only remaining raw-exception path in the loader.
- **reviewer/src/swreview/checks/rms/registry.py:59** (carry-over-python) - A module-level `assert` enforces the `RuleCoverageBucket <= CoverageBucket` invariant in library code. `assert` is stripped under `python -O`, so the invariant it claims to enforce is not enforced in an optimized interpreter; and when it does fire it raises at import time, which surfaces as a pytest collection error rather than a named test failure. It is also duplicated verbatim by `tests/unit/test_rms_registry.py::TestCoverageBuckets::test_the_rule_buckets_are_session_buckets`, which means the same assertion is now maintained in two places - a DRY violation in exactly the direction the project instructions call out. Fix: Drop the module-level assert and keep the test, which already covers it and fails cleanly by name. If a runtime guarantee is genuinely wanted, raise explicitly (`if not set(...) <= set(...): raise ValueError(...)`) rather than asserting - the rest of this package refuses bad state with `ValueError` and a message naming the thing, and an `assert` is the one place that convention is broken.
- **reviewer/tests/unit/test_ir_features.py:490** (carry-over-python) - `test_the_fixture_builder_emits_only_documented_gap_entity_kinds` does not do what its docstring claims. The docstring says "a kind it emits and data-model.md section 1 does not document fails here", but `NEW_GAP_ENTITY_KINDS` (line 48) is a hand-written literal in the test file, not parsed from data-model.md - so the test is anchored to the test's own list, exactly the weakness it says it is fixing. The assertion is also a subset check (`emitted <= set(NEW_GAP_ENTITY_KINDS)`) over a fixture that emits only 2 of the 10 documented kinds, so 8 kinds remain pinned by nothing but `test_the_new_gap_entity_kinds_validate`, which the docstring itself correctly describes as vacuous (`Gap.entity_kind` is a free string). Fix: Parse the ten kinds out of data-model.md section 1 the way `test_rms_registry.py` already parses `contracts/rules.md` (`tests.support.contracts.REPO_ROOT` plus a small regex), assert the parsed set equals `NEW_GAP_ENTITY_KINDS`, and make the builder test assert set *equality* against the kinds that fixture is built to produce (`{"feature_tree_unavailable", "mate_suppression"}`) rather than a subset. That makes a doc/code drift in either direction fail, and stops the builder silently dropping a gap.
- **reviewer/src/swreview/ir/models.py:250** (carry-over-python) - The `description="Row-major 4x4 relative to the root assembly, translation in meters"` added to `ComponentInstance.transform` (and regenerated into `specs/001-agentic-design-review/contracts/ir.schema.json` line 209) is feature 001 surface with no bearing on feature 003 - no RMS rule, dumper or tool reads `transform`. Its test, `test_component_transform_documents_the_frame_and_the_unit` (test_ir_features.py:453), asserts the exact wording, so it is a pure change detector: any reword breaks it with no semantic change, and it cannot fail for any reason a reader would care about. Fix: Either revert both the field description and its test (surgical-change rule: every changed line should trace to feature 003), or - if documenting the frame is deliberate and wanted - keep the description and delete the test, since the schema-sync test (`test_schema_sync`) already guarantees the regenerated contract matches the model. Do not keep both.
- **reviewer/src/swreview/exceptions.py:218** (carry-over-python) - `_feature_row` now hashes two fields data-model.md section 3 does not list: `sketch is not None` (line 218) and `fillet is not None` (line 221). The behaviour is right - I confirmed `sketch_lost` and `SketchInfo(raw_status=None, consumer_ids=None)` collided without the flag, and both new tests go red if the flags are removed - but the contract enumerates the hashed fields exactly ("index, type_name, name, depth, folder_id, suppressed, description present or not, sketch.raw_status, len(sketch.consumer_ids), fillet.default_radius, child_ids"), and the code now exceeds that list with no contract change and no note. Fix: Since specs/003 is off-limits to the implementing agent, flag it for the spec owner: data-model.md section 3's field list needs "whether a sketch/fillet block is present at all" added, or the flags need removing. Leaving the code ahead of the contract is the one thing not to do - the next reader reconciling the two will not know which is authoritative. Worth recording alongside it that the digest shape is now load-bearing for `tests/golden/fixtures/rms-part/case.json`'s stored hash (I verified it still recomputes to 5bdb638b…), so any further row-shape change must regenerate that fixture.
- **reviewer/tests/unit/test_exceptions_feature_tree.py:165** (carry-over-python) - This file defines local `feature(index, name, type_name, ...) -> Feature` (line 75) and `rms_package(features=..., equations=...) -> EvidencePackage` (line 165) that shadow the shared T003 builders of the same names in `tests/support/features.py` (`feature(name, type_name, **kwargs) -> FeatureSpec` at line 172, `rms_package(parts=..., assembly=...)` at line 415) with different signatures and different return types. `test_ir_features.py` now imports the shared `rms_package` while its neighbour defines a different one - two functions, one name, one test suite, one feature. Fix: Rename the local helpers to something that cannot be confused with the shared builders (`tree_package(...)`/`row(...)`, say). Do not try to merge them: the local one is legitimately different - it wraps `build_package` so the geometry digests pinned at lines 59 and 66 stay computable, and the fingerprint tests need per-field `model_copy` mutation that the spec-driven builder does not offer. The collision is a naming problem, not a duplication problem.
- **reviewer/src/swreview/ir/models.py:551** (carry-over-python) - `_rows_account_for_every_planned_feature` enforces `len(rows) == features_present` and `restore_verified => not unrestored_feature_ids` in Python only. Neither invariant is expressible in (or present in) the regenerated `ir.schema.json`, and `extractor/SwReview.Extractor/Ir/SuppressTestRun.cs` is a plain property bag with no equivalent check. So a T055 `SuppressTest`/`PackageAppender` writer can emit a run that passes `IrSerializerTests`' contract validation on the C# side and then makes the *entire* package unloadable on the Python side - the reviewer loses every check on the package, not just the suppressibility rule. Fix: Pin the invariant on the writing side too: assert both in `PackageAppender.AppendSuppressTest` (T055 already has a `PackageAppenderTests` case to extend), so a violation fails at the point of writing with a message naming the mismatch rather than as a pydantic error on someone else's machine a day later. Worth deciding at the same time whether a violating run should really take the whole package down or whether `rms.detail.individually_suppressible` should simply report unresolved - the latter matches this project's "unknown stays unknown" rule better for a field that only the C# side can get wrong.

## Feature 004 spec review should-fix (2026-09-16)

- **specs/004-resilient-remodeler/tasks.md** - The payload T130 calls identical is not identical: contracts/pane-remodel-messages.md and the US6 addendum both specify `entity.shown {ok, state_code, message, full_path | null}` (us6-tasks T074 asserts `full_path` explicitly). Three more Phase 8 tasks restate pane payloads that the contract has sin Fix: Bring T122, T126, T128 and T130 to the payloads in contracts/pane-remodel-messages.md (add `full_path`, `latest_run`, `saving`, `attestation`, `state`), or have them cite the contract instead of restating the payloads.
- **specs/004-resilient-remodeler/data-model.md** - The run-folder table in section 7 has no `exceptions.json` row, although FR-042 requires the carried-forward exceptions in every run folder, plan.md's Storage line lists `exceptions.json`, contracts/run-artifacts.md gives it a row plus an "Exceptions carry-forward" section, and plan.md key point 15  Fix: Add an `exceptions.json` row to data-model section 7 (written by the carry-forward before the plan) and add it to quickstart step 11's list of files that survive Discard.
- **specs/004-resilient-remodeler/data-model.md** - The row makes "copy present: no" apply to every cause of `failed`, including target verification failure, but contracts/guard-allowlist.md says the opposite for that cause: "Any failure throws `RemodelTargetError` naming which check failed; the run aborts with the change log intact and the copy left Fix: Split the `failed` row's copy column by cause: the copy is deleted on a scope/open/tag refusal and on a geometry-verification failure, and is left on disk on a `target_mismatch` abort, matching guard-allowlist.md and the bridge error table.
- **specs/004-resilient-remodeler/quickstart.md** - The single probe ledger is assigned to two owners in places. PROBE-14, 16 and 17 belong to feature 003 US6 (us6-tasks.md T088 runs them and records them in `benchmarks/native/rms-part/notes.md`), and 004's Phase 2 tasks (T031–T039) implement none of them, so `probe remodel` cannot produce the rows t Fix: In quickstart Scenario 4, drop 14, 16, 17 from the `probe remodel` pass and mark the PROBE-15 row as "answered by 003 US6 T088; recorded here as a precondition"; add PROBE-21 to plan.md's Phase 2 probe list so the three lists name the same set.
- **specs/004-resilient-remodeler/tasks.md** - T141 writes the workstation results into feature 003's notes file, while quickstart.md names a different file for the same numbers in four places: "record the observations in `benchmarks/native/remodel/notes.md`", "record the PROBE-8 numbers in `benchmarks/native/remodel/tolerance-calibration.md`",  Fix: Point T141 (and plan.md's Phase 9 row) at `benchmarks/native/remodel/notes.md` and `benchmarks/native/remodel/tolerance-calibration.md`, the paths quickstart.md and T039's calibration ledger already use.
- **specs/004-resilient-remodeler/contracts/tools.md** - The tool takes `evidence` as a list of feature-id strings but writes `plan.globals[]`, whose normative `GlobalEvidence` (data-model 1.9) requires `feature_id`, `parameter`, `value_m`, `document_length_unit`, `value_document_units` and `equation_text` per entry — as the run-artifacts plan.json exampl Fix: Make the argument `evidence: list[{feature_id, parameter}]` so the tool can build a complete `GlobalEvidence` deterministically, state that the tool fills `value_m`, `document_length_unit`, `value_document_units` and `equation_text` from the package and `remodel.open`'s `document_length_unit`, and s
- **specs/004-resilient-remodeler/plan.md** - Subclassing alone cannot add a command. `reviewer/src/swreview/bridge/client.py:68` defines `COMMANDS: tuple[str, ...] = ("ping", "capture", "measure", "interference")` as a *module-level* constant, and `BridgeClient.call` reads it directly: `if command not in COMMANDS: raise BridgeError(f"{command! Fix: Say what the one edit to `client.py` is: promote `COMMANDS` to a class attribute (`class BridgeClient: COMMANDS = ("ping", "capture", "measure", "interference")`) read as `self.COMMANDS` inside `call`, keeping the module-level name as an alias, and have `RemodelClient` override it with `("ping", *RE
- **specs/004-resilient-remodeler/contracts/guard-allowlist.md** - Qualified keys already exist across the reviewer, in a *different* convention than the allowlist's. `extractor/SwReview.AddIn/Review/SwEntityResolver.cs:141-153` uses `"Component.Select4"`, `"Feature.Select2"`, `"Entity.Select4"`; `Capture/SwCaptureView.cs`, `Measure/SwMeasureSource.cs`, `Interferen Fix: Correct the sentence to say the codebase already uses `Type.Member` keys in several non-remodel call sites, and state the disambiguation rule `CallKey` must implement: the allowlist is keyed on the full interop interface name (`IFeature.Select2`) and `QualifiedKey`/`BareName` must treat an unprefixe
- **specs/004-resilient-remodeler/tasks.md** - Phase 0 is declared seat-free ("| **0. Decide** | Run the Phase 1 planner over the benchmark packages and 3 to 5 real parts ... | No | n/a |", plan.md line 316) but its named inputs do not exist and cannot be produced without SOLIDWORKS. `benchmarks/native/rms-part/` contains only `RECIPE.md`; the r Fix: Name the real Phase 0 inputs: the `rms-part` golden fixtures under `reviewer/tests/golden/fixtures/rms-part` plus the T005 builder packages (both seat-free), and state that `benchmarks/packages/rms-part/native/` is a feature 003 workstation output that Phase 0 uses only if it already exists. Exclude
- **specs/004-resilient-remodeler/contracts/pane-remodel-messages.md** - Two of the five names do not exist in the code. `extractor/SwReview.AddIn/TaskPaneControl.cs:160-167` creates exactly `new TabPage("Review")`, `new TabPage("Terminal")` and `new TabPage("Actions")`, and no feature 002/003 artifact renames them (002's `contracts/pane-host-messages.md` line 23 still s Fix: Write the row as the tabs actually are: "Review, Terminal (Ask), Actions, Model check, Remodel", or state explicitly that this feature also renames the `TabPage` labels and add that rename to the task list and to `WebViewFallbackTests`/pane contract expectations.
- **specs/004-resilient-remodeler/plan.md** - `EventSink` is not private to the review run today: `reviewer/src/swreview/chat/sessions.py:46` is `from swreview.agent.runner import EVENTS_FILE_NAME, EventSink, ReviewRun`, and line 297 constructs one (`self.fallback_sink = EventSink(self.events_path, listeners=[self.publish])`). A plain move to ` Fix: Add to plan point 8 and to the Phase 7 task that `agent/runner.py` re-exports `EventSink` and `EVENTS_FILE_NAME` from `agent/events.py` (or that `chat/sessions.py:46` is updated to import from the new module), and name `reviewer/tests/unit/test_chat_sessions.py` passing unedited as the regression pr
- **specs/004-resilient-remodeler/contracts/guard-allowlist.md** - `ISldWorks.OpenDoc7` is pinned here for its option integers but is absent from the allowlist ("Exactly this list"), so it takes the delegation branch to `ReadOnlyGuard`. It is also the only call in the flow that names a file path and opens it read-write, and the only check on which file it opened is Fix: Add `ISldWorks.OpenDoc7` to the allowlist, and specify an `AssertOpenTarget(spec.FileName)` that runs the same refusal table as `AssertSaveTarget` **before** `OpenDoc7` is called (inside the run folder, `.SLDPRT`, not the source, not another run's copy), plus a rule that `CloseDoc`'s argument is rea
- **specs/004-resilient-remodeler/contracts/bridge-remodel.md** - This is the only stated restore site, and it sits in `remodel.close`. The toggles are set at `remodel.open` step 6, which is **before** steps 7 to 12, and six documented error codes fire after that point and end the attempt without any document being opened or any `remodel.close` being issued: `copy Fix: State in step 6 that the set is armed in the same `try/finally` as steps 7 to 12, so every `remodel.open` error path restores the previous values before it answers, and add that case ("an error result from any step after the toggles are set restores them") to T052.
- **specs/004-resilient-remodeler/contracts/pane-remodel-messages.md** - Two preconditions that 003's own analysis identified are missing here, and without them Show fails silently on every change row. (1) The subject's persist ref belongs to the **copy**, and `remodel.open_copy` in the row below says the copy may be closed ("Activate the copy, re-opening it if it was cl Fix: Add to the `remodel.show_change` row: the host activates the copy (re-opening it if closed) before resolving, and answers `ok:false` with `CopyDiscarded` when it cannot; and state in run-artifacts.md section 7 / the `remodel.planned` host action that `RunFolders.CreateForRemodel` registers the run f
- **specs/004-resilient-remodeler/contracts/run-artifacts.md** - Tier 2 is the only recovery for a failed folder creation (FR-024, data-model section 2.3, research R8.3), but no document names the bridge sequence that performs it, and the sequence is not obviously available. Deleting the copy requires the document to be closed first, and `remodel.close` is specif Fix: Write the replay's command sequence into contracts/bridge-remodel.md (`remodel.close {discard_copy:true}` → filesystem re-copy → `remodel.open` with the same `run_id` and the run's existing `probe_id` → re-apply the `applied` prefix), state which refusals are re-evaluated on that path and which are 

## Phase 4-5 + add-in + pane (2026-09-16)

- **reviewer/src/swreview/checks/rms/assembly.py:315** (assembly) - `_verdict` is a verbatim copy of `part._verdict` (part.py:211-239) — same docstring shape, same three branches, same body; the only difference is that the part copy takes `tree: PartTree` and reads `tree.document_id` while this one takes `document_id: str` directly. The docstring concedes it ("A sha Fix: Move the function to `checks/rms/results.py` (its module docstring already claims to be "the only place [RuleResult] is built") as `verdict(rule, document_id, *, violation, passing, unknown)`, change `part._verdict` to a call passing `tree.document_id`, and delete both copies. It is a mechanical cha
- **reviewer/src/swreview/checks/rms/assembly.py:614** (assembly) - `_toolbox_unreadable` links a Toolbox-identity gap back to a component by substring-matching the gap's English prose: `entity_kind == "component"` plus `"Toolbox identity" in gap.reason` plus `f"'{row.full_path}'" in reason`. That is correct today only because `ComponentTreeDumper.ReadIsToolbox` (Co Fix: Prefer the mechanism the same file already uses for the same problem: `ReadConstrainedStatus` defers its gap so `PackageWriter` can write it with the allocated `cmp:NNNN` as `entity_id` (ComponentTreeDumper.cs:355-375). Do the same for `ReadIsToolbox` — a dedicated `component_toolbox` entity kind ca
- **reviewer/src/swreview/checks/rms/assembly.py:387** (assembly) - Engineer-facing `observed` text carries raw API identifiers where every other RMS rule carries names an engineer can search for. The golden baseline reads `mate:0001 (COINCIDENT) references swSelFACES on base-1, swSelFACES on plate-1` (rms-assembly.yml:163) and `Toolbox document doc:6 is inserted as Fix: Render the entity kind through a small display map ("a face", "an edge", "a vertex") kept next to `geometry_entity_kinds` in `rms_types.yaml` so the table stays the single source, falling back to the raw name for an unlisted kind (which is unresolved anyway, and there the raw name is the point). For
- **reviewer/tests/golden/test_golden/rms-assembly.yml:3** (assembly) - The assembly golden pins no `checked` and no `skipped` coverage row at all — every coverage entry is `unresolved` or `out_of_scope`, and `modeling.resilience` reads "0 checked, 0 skipped, 2 unresolved rule result(s)". So the baseline never records what a passing or skipping assembly rule looks like  Fix: Either add a second fixture (or extend this one) that reaches a clean `pass` and a clean `skip` for at least one assembly rule — e.g. a second package with no Toolbox component and a fully compliant mate set — or correct generate_package.py's docstring to say the six plane mates are absorbed by the 
- **reviewer/src/swreview/checks/rms/assembly.py:508** (assembly) - `deep = [node for node in graph.nodes if depths.get(node.id, 0) > limit]` defaults an unmeasured depth to 0. The result is right today only because the same nodes are separately collected into `unknown`, so the default never reaches a finding — but "a component whose distance from the fixed root cou Fix: Iterate the measured set explicitly instead: `deep = [node for node in graph.nodes if node.id in depths and depths[node.id] > limit]`, matching the `passing` comprehension two lines below which already spells the membership test out. Behaviour-identical, one line, no baseline change.
- **reviewer/src/swreview/cli.py:845** (assembly) - `--document` interacts badly with `--scope assembly`. `part_documents` is called unconditionally before the scope is consulted, so (a) `--scope assembly --document <part id>` validates the id and then silently ignores it — `graded` stays empty and the JSON reports `"documents": []` with no word that Fix: Resolve part documents only when the run actually includes a document-scoped family (`if set(runs) & set(RMS_SCOPE_CHECKS)`), and reject `--document` with an explicit message when the chosen scope has nothing to narrow, rather than accepting and discarding it. For the dead scope-gating code, either 
- **reviewer/src/swreview/checks/rms/assembly.py:276** (assembly) - `joined_by` silently drops any mate entity whose `component_id` is not in `by_id`. `MateDumper.ReadEntities` writes `ComponentId = component?.Id ?? string.Empty` when the entity's `ReferenceComponent` could not be read or was not found in the component scope, so a real mate can arrive with an entity Fix: Count the entities whose `component_id` is empty or unresolved and either add a `coverage_limits` entry on the finding ("<n> mate entity/entities name no component") or fold the mate into the unresolved bucket via the same mechanism as the must-fix above. A `coverage_limits` note is the smaller chan
- **reviewer/src/swreview/checks/rms/registry.py:265** (assembly) - `rms.assembly.mate_chain_depth`'s `statement` is copied from the contract with the placeholder intact: "No component is more than limit mates from the fixed root (default 3)." That string is the `requirement` field of the finding an engineer reads (rms-assembly.yml:242), where the literal word "limi Fix: Either reword the statement so it reads as a sentence without the placeholder ("No component is more than the configured number of mates from the fixed root — currently 3.") or have the rule substitute `tree.table.assembly.mate_chain_depth_limit` into the requirement when it builds the finding. regi
- **reviewer/src/swreview/checks/rms/assembly.py:315** (assembly) - `_verdict` is a near-verbatim copy of `part._verdict` (reviewer/src/swreview/checks/rms/part.py:211-240): same body, same semantics, same docstring argument, differing only in taking `document_id: str` instead of `tree: PartTree` and in the element type of `passing`/`unknown`. The module docstring a Fix: Move the function to `checks/rms/results.py` as `verdict(rule, document_id, *, violation, passing, unknown)` (it already builds `passed`/`unresolved`/`subject_reasons`, so it has no new dependency), have `part._verdict` become a one-line call passing `tree.document_id`, and delete the assembly copy.
- **reviewer/src/swreview/checks/rms/results.py:118** (assembly) - `subject_input`, `subject_reasons`, `passed`, `skipped`, `unresolved` and `finding` are all annotated `Feature`, and assembly.py now hands them a `Subject` dataclass (assembly.py:102-119) that is structurally compatible but nominally unrelated. The annotations are therefore false, and the only thing Fix: Declare a `RuleSubject` Protocol in results.py with the seven members (`id`, `name`, `type_name`, `persist_ref`, `persist_ref_scope`, `suppressed`, `configuration`) and annotate the six constructors with it; `Feature` and `Subject` both satisfy it structurally, so no call site changes.
- **reviewer/src/swreview/cli.py:786** (assembly) - `RMS_SCOPE_NOT_BUILT` and the `unavailable_scopes` payload key are now unreachable. Every member of `RmsScope` that can appear in `runs` is in `RMS_SCOPES_BUILT` (cli.py:805-809), so the comprehension at cli.py:874-878 always yields `[]` and the `scope <x>: <reason>` lines at cli.py:899 can never pr Fix: Either delete `RMS_SCOPE_NOT_BUILT`, `RMS_SCOPES_BUILT`, the `unavailable` comprehension, the payload key and the trailing output lines (keeping one test that the JSON shape no longer carries the key), or keep the key as a permanently-empty list and say in its docstring that it exists for forward co
- **reviewer/src/swreview/checks/rms/assembly.py:494** (assembly) - Branch coverage of assembly.py over the assembly, tool, CLI and golden suites is 99% with three gaps, all of them real cases the extractor can produce: line 494 (`no fixed child of the root assembly` *and* at least one mate whose suppression could not be read - the joined reason is never rendered),  Fix: Add three cases: (a) `TestMateChainDepth` with no fixed child plus a `suppression_gap=True` mate, asserting both the `no fixed child` clause and the per-mate reason appear in the one unresolved result; (b) `TestMateGraph` with a mate whose two entities name the same instance, asserting no self-edge 
- **reviewer/src/swreview/checks/rms/assembly.py:179** (assembly) - `MateGraph` drops `cmp:0001` from its nodes, so a mate on the root assembly's own reference geometry contributes no edge. That is what the extractor actually writes for such a mate: MateDumper.cs:210-220 resolves the entity's `ReferenceComponent`, gets null for assembly-owned geometry, and records ` Fix: Add a `MateSpec` case whose entity names no instance (empty `component_id`) and pin the two consequences: the mate still grades on its entity kinds and renders the unnamed entity by kind alone, and the component it half-joins is unresolved rather than deep. Then say in the `MateGraph` docstring that
- **reviewer/tests/golden/test_golden/rms-assembly.yml:1** (assembly) - Every one of the four evaluable rules in the new baseline lands in `fail`/`warn`/`unresolved`; the summary reads `0 checked, 0 skipped, 2 unresolved rule result(s)`. Because `_verdict` suppresses the pass when a violation exists, the six compliant plane mates and the reachable components in the fixt Fix: Either add a second assembly fixture (or a second case over the same package with the seeded violations removed) so one baseline carries the `checked` and `skipped` wording for all four rules, or accept the gap explicitly and note in generate_package.py why no assembly rule can reach `checked` in th
- **reviewer/src/swreview/checks/rms/assembly.py:434** (assembly) - A package whose components carry no instance of `design.root_assembly_document_id` makes `_root_instance` return None, and the two rules that care then disagree: `assembly_tree` yields `children=()` so `first_component_fixed` reports `skip` with reason `the root assembly has no child components` - a Fix: Make the missing root instance one decision in `assembly_tree`: either assert it (`# pragma: no cover` plus a RuntimeError naming the document, matching how `evaluate_assembly` treats an unbound rule at line 645) or return `unresolved` for all four rules with the document in the reason. Do not leave
- **reviewer/tests/unit/test_rms_assembly_rules.py:727** (assembly) - `test_every_evaluable_assembly_rule_is_dispatched_once_per_outcome` asserts only `{result.rule_id for result in results} == set(ASSEMBLY_RULE_IDS)` - a set comparison that says nothing about "once per outcome". The property the name claims is enforced elsewhere (the `run` helper at line 128 rejects  Fix: Rename it to `test_every_evaluable_assembly_rule_is_dispatched`, or add the assertion it promises (`Counter((r.rule_id, r.outcome) for r in results)` has no value above 1). Drop the duplicated registry assertions or reduce them to the one line each test actually needs.
- **extractor/SwReview.Extractor/Dump/EquationDumper.cs:226** (equations) - A failed `Value(i)` records a document-scoped `equations` gap. Neither rule reads `value`, but equations.py:101 treats any `equations` gap naming the document as the document-wide unresolved condition, so one unreadable value turns both rules unresolved for the whole part and silences a genuine "no  Fix: Stop recording an `equations` gap for a failed Value(i) - the null value is already the honest record the data model asks for - or record it under an entity kind the rules do not consume. Update Dump_ValueThatThrows_IsNullPlusAnEquationsGap (EquationDumperTests.cs:170) to assert the null value and n
- **reviewer/src/swreview/checks/rms/equations.py:211** (equations) - evaluate_equations gates on part.tree_not_read, which also returns the document-level `feature_tree_unavailable` gap. With `--features none --equations on` (a combination Program.cs and contracts/cli.md both allow) PackageWriter.cs:168 writes that gap with entity_id null, so every equation-scope rul Fix: Split the predicate: share the instance-state half (no resolved instance -> "component <path> <suppression>; tree not read") with part.py, and keep the feature_tree_unavailable half for the part scope only. The equation scope's own missing-data condition is already the `equations` gap it consults at
- **reviewer/src/swreview/cli.py:875** (equations) - With the equations family landing, every member of RmsScope is now in RMS_SCOPES_BUILT, so `unavailable` is always empty and RMS_SCOPE_NOT_BUILT (line 786) and RMS_SCOPES_BUILT (line 805) are unreachable - 20 lines of constants, docstrings and a comprehension that can never fire, plus an `unavailabl Fix: Delete RMS_SCOPE_NOT_BUILT, RMS_SCOPES_BUILT and the `unavailable` comprehension, and drop the `unavailable_scopes` key (or keep the key as a documented always-empty literal if a consumer reads it). RMS_SCOPE_CHECKS plus the assembly branch is then the whole dispatch. This is a deletion the owner sh
- **reviewer/src/swreview/cli.py:866** (equations) - `--document` is validated by part_documents (line 845) and then silently ignored for the assembly family: `swreview check rms --scope assembly --document doc:2` grades the whole root assembly and prints "0 part document(s)" with nothing saying the filter had no effect. The reasoning is sound and doc Fix: Either reject `--scope assembly` combined with `--document` as a usage error, or add one line to the human output and a key to the payload saying the assembly family takes no document selection, so the ignored argument is stated rather than inferred.
- **specs/001-agentic-design-review/contracts/agent-tools.md:48** (equations) - The `list_equations` row promises `index`, `text`, `lhs`, `is_global`, `value`, but the tool returns the full row including `document_id` - reviewer/tests/unit/test_tools_rms_query.py pins exactly six keys through EQUATION_KEYS, with document_id first. The contract and the pinned shape disagree, and Fix: Add `document_id` to the returned-fields cell in the agent-tools.md row (specs/003-resilient-modeling/contracts/tools.md:14 already says "the document's `Equation` list", which is consistent with returning the whole row).
- **extractor/SwReview.Extractor/Dump/EquationDumper.cs:261** (equations) - `lhs` is parsed twice by two different rules: the production Lhs() strips every double quote in the left side (text.Replace("\"", "")), while the fixture builder at reviewer/tests/support/features.py:229 strips only leading and trailing quotes (.strip('"')). For any left side with an interior quote  Fix: Make the builder's helper use the extractor's rule (drop every quote, then trim) so the one parse is stated once and the fixtures cannot drift from what SwEquationReader will actually yield; add the interior-quote case to the Dump_LhsIsLeftOfTheFirstEquals theory.
- **extractor/SwReview.Extractor/Dump/EquationDumper.cs:128** (equations) - The two component-scoped `equations` gaps - the not-resolved branch (line 105) and the `GetModelDoc2` throw/null branches (lines 116, 128) - are recorded under `component.Id` (`cmp:0002`), but `checks/rms/part.py:document_gap` only matches `gap.entity_id in (None, document_id)`, so the rule layer ne Fix: Either record these gaps under `component.DocumentId` when the document has no other resolved instance to fall back on, or teach `document_gap` to resolve a component-scoped gap to that component's `document_id` via `package.components`. Add a Python test with an `equations` gap whose `entity_id` is
- **reviewer/src/swreview/cli.py:805** (equations) - `RMS_SCOPE_NOT_BUILT` (786), `RMS_SCOPES_BUILT` (805) and the `unavailable` comprehension (875-879) are now dead: every member of `RmsScope` except `all` is in `RMS_SCOPES_BUILT`, and every member of `RMS_SCOPE_RUNS[all]` is too, so `unavailable_scopes` is unconditionally `[]` - which is exactly wha Fix: Drop `RMS_SCOPE_NOT_BUILT`, `RMS_SCOPES_BUILT` and the `unavailable` list plus the `unavailable_scopes` payload key and its stdout line, and delete the now-vacuous assertions. If you want the key kept for payload stability, say so and keep it as a literal `[]` with a comment - don't keep the dead te
- **reviewer/src/swreview/cli.py:863** (equations) - `graded = documents` is assigned inside the per-scope loop, so it is rewritten once per document-scoped family and means nothing about which family actually ran; it is also the only thing distinguishing "no document family ran" from "a document family ran over zero documents". Related: for `--scope  Fix: Hoist the decision out of the loop - e.g. `ran_documents = any(item in RMS_SCOPE_CHECKS for item in runs)` and `graded = documents if ran_documents else []` - and add a test pinning what `--scope assembly --document <id>` does (either an explicit error, or `documents: []` with the flag documented as
- **reviewer/src/swreview/tools/rms_query.py:155** (equations) - DRY: three independent copies of "this document's equation rows, in index order" now exist - `list_equations` (rms_query.py:153-159), `run_equation_checks` (rms_checks.py:196) and `document_equations` (equations.py:90). The features side already solved this with the shared `rms_query._rows_of` helpe Fix: Add one `equations_of(package, document_id) -> list[Equation]` (sorted by `index`) next to `_rows_of` and call it from all three sites; `document_equations` then keeps only its cross-document `ValueError` guard.
- **extractor/SwReview.Extractor.Tests/CommandLineOptionsTests.cs:1** (equations) - The only change to line 1 is a newly added UTF-8 BOM (`efbbbf` before `using System;`); the file had none before this task. That is unrelated encoding churn in a file the task otherwise only appends to, and it makes the diff of an existing file show a spurious first-line change. Fix: Rewrite the file without the BOM so line 1 is unchanged, keeping only the new `--equations` test block.
- **reviewer/tests/unit/test_tools_rms_query.py:370** (equations) - Test-coverage gaps, none of them failures today but all of them claims the tests assert without exercising: (a) `test_list_equations_returns_every_field_of_every_row_in_index_order` never exercises the sort - `rms_package` emits rows already in index order, so removing `sorted(...)` from `list_equat Fix: (a) give one fixture document out-of-order `EquationSpec`s, or assert the sort directly against a shuffled `package.equations`; (b) add `test_check_rms_scope_equations_reports_no_assembly_document`; (c) extend `test_check_rms_scope_all_runs_every_family` with the per-(bucket, check) duplicate check 
- **reviewer/src/swreview/checks/rms/equations.py:179** (equations) - The two rules in the same contract table decide membership by two different mechanisms: `global_variables_present` reads the manager's `is_global` flag, while `dimensions_driven_by_equations` uses the text heuristic `DIMENSION_MARKER in row.lhs`. Nothing excludes a row the manager flagged `is_global Fix: Add a one-line note in `dimensions_driven_by_equations` saying the `@` test is research R2's own condition and is deliberately independent of `is_global`, plus a test with a row that is both (`is_global=True`, lhs containing `@`) pinning the intended answer for both rules.
- **extractor/SwReview.AddIn.Tests/AddInComCallbackTests.cs:32** (addin-load-fix) - `new SwReviewAddIn()` runs the type initializer, which does two things to the test process that no other test in this suite does. First, it appends to the engineer's real `%LOCALAPPDATA%\SwReview\logs\addin.log` — I confirmed three new "AssemblyRedirect installed." lines were written there by my `do Fix: Give the log folder the same seam `ReviewHostOptions.Environment` has (an injectable `Func<string>` or a `SWREVIEW_LOG_FOLDER` override the tests point at a temp directory), and either have the marshalling test avoid the type initializer or accept the resolver and document the ordering hazard. Cheap
- **extractor/SwReview.AddIn/SwReviewAddIn.cs:157** (addin-load-fix) - The new third guard — the change the owner document calls "the change that would have made all three diagnosable in minutes" and "recommended upstream regardless of the other fixes" — has no test. The existing rationale for not testing this class ("every path in that class needs a live ISldWorks", T Fix: Add one case: `ConnectToSW` with a non-ISldWorks argument returns `false` and does not throw. With the log seam from the finding above in place, assert it also wrote the failure line, which pins "no failure may leave ConnectToSW" and "the failure reports itself" in one test. Alternative: leave it un
- **extractor/tools/register-addin.ps1:56** (addin-load-fix) - `$SolidWorksRoot` hard-codes `C:\Program Files\SOLIDWORKS Corp\SOLIDWORKS` as a second, independent copy of a location the build already owns: `extractor/Directory.Build.props:13` defines `SwRedist` with the same default and documents overriding it (`-p:SwRedist="D:\SW\api\redist"`) for a seat insta Fix: Default `$SolidWorksRoot`/the redist path from `$env:SwRedist` when it is set, falling back to the current literal, and say so in the .PARAMETER block — one line, and it makes the script agree with the build by construction. Alternative: read the property out of Directory.Build.props (more faithful,
- **extractor/tools/register-addin.ps1:122** (addin-load-fix) - `throw "regasm failed with exit code $LASTEXITCODE; nothing was registered."` asserts a fact the script did not check. RA0000 (the failure this script exists to prevent) does fail before writing, but regasm can also fail after `RegisterFunction` has already created the HKLM AddIns key and the HKCU s Fix: Say only what is known: "regasm exited with code $LASTEXITCODE. Registration may be incomplete - run this script with -Unregister before retrying." Alternative: verify by reading back `HKLM\SOFTWARE\SOLIDWORKS\AddIns\{GUID}` after a failure and reporting what is actually there; more thorough, and ch
- **extractor/README.md:44** (addin-load-fix) - The "regasm must load swpublished / the build sets Private=false / a plain regasm fails with RA0000" rationale is now written out in full in five places: the script's .DESCRIPTION (register-addin.ps1:9-22), the class comment (SwReviewAddIn.cs:54-61), extractor/README.md:44-50, specs/002-task-pane-as Fix: Keep the full explanation in exactly two places — docs/addin-load-fix.md (the diagnosis) and the script's own .DESCRIPTION (where someone reading the script needs it) — and reduce README, quickstart and the class comment to the command plus a pointer ("stages the seat's interops then runs regasm /co
- **docs/addin-load-fix.md:357** (addin-load-fix) - "Add-in suite after the changes: **372** tests pass (361 baseline + 3 + 8)" is already false in this working tree — the add-in suite is at 387 passed / 0 failed, because sibling work landed more tests. An absolute count in a diagnosis document is stale the moment any other feature lands, and a reade Fix: State the delta rather than the total ("+11 add-in cases: 3 for cause 1, 8 for cause 2") and restore the commit SHA in the first paragraph. Alternative: keep the total and accept that it is a point-in-time note — then say so explicitly ("at the time of writing").
- **extractor/SwReview.AddIn/SwReviewAddIn.cs:153** (addin-load-fix) - The guard comment this change rewrote still calls the tab "the pane's Actions tab", while the class comment 130 lines above — rewritten in the same working tree — now calls it the Extract tab and the button "Extract evidence". One comment block, two names for the same tab. (The tab rename itself is  Fix: Change "the pane's Actions tab" to "the pane's Extract tab" when the sibling rename settles, so the load-fix comment does not reintroduce the old vocabulary. Alternative: leave it to whoever owns the rename — fine, provided someone sweeps it; it is the kind of line that survives a rename precisely b
- **extractor/SwReview.AddIn/AssemblyRedirect.cs:92** (addin-load-fix) - Matching on simple name alone means the handler will serve *our* copy of a shared assembly to another add-in whose bind for that name failed, which is the opposite of what the code comment and the doc claim. AssemblyRedirect.cs:31-33 says the handler "answers only names that exist as a file beside t Fix: Narrow the promise or narrow the code. Preferred: a `private static readonly string[] Shims` listing the simple names the add-in actually needs redirected, checked before the File.Exists probe - explicit over clever, and it makes the doc's claim literally true. Add a test that a name beside the add-
- **extractor/tools/register-addin.ps1:1** (addin-load-fix) - A 131-line tracked script that is now the documented registration entry point in three places (extractor/README.md, specs/002-task-pane-assistant/quickstart.md, and the SwReviewAddIn class comment) has no test of any kind - not even a parse check. A typo in a Join-Path or a renamed interop ships sil Fix: Add a small contract test beside those: assert the script file exists at the path the three documents cite; assert it parses (`[System.Management.Automation.PSParser]::Tokenize(text, out errors)` with zero errors, or shell out to `powershell -NoProfile -Command "[scriptblock]::Create(...)"`); and as
- **extractor/SwReview.AddIn.Tests/AssemblyRedirectTests.cs:38** (addin-load-fix) - DRY: three of the eight cases assert the same thing against three display names. `AnOlderVersionRequestIsServedByTheFileBesideTheAddIn` (line 38), `ANewerVersionRequestIsServedByTheFileBesideTheAddIn` (line 57) and `ARequestWithNoVersionIsServedByTheFileBesideTheAddIn` (line 74) each write the same  Fix: Fold into one `[Theory]` with three `[InlineData]` rows ("Version=4.0.4.1", "Version=9.9.9.9", no version), keeping the explanatory XML comment on the theory. Same coverage, one body. The remaining five cases each assert a genuinely different branch and should stay as they are.
- **extractor/SwReview.AddIn.Tests/AddInComCallbackTests.cs:68** (addin-load-fix) - `TheAddInIsComVisible` is characterization, not regression: `[ComVisible(true)]` was already on the class before this change, so the case passes identically with and without the fix (the owner document confirms it: "2 of 3 failed, ComVisible passed"). It is justified in the doc as "the precondition  Fix: Either leave it and let its XML comment carry the weight (it already says "precondition"), or make the comment explicit that this case did not go red for the fix and exists to pin a pre-existing precondition. No code change needed; this is a naming/expectation nit, raised because the review lens ask
- **extractor/tools/register-addin.ps1:122** (addin-load-fix) - On the `-Unregister` path the failure message is wrong: `throw "regasm failed with exit code $LASTEXITCODE; nothing was registered."` fires for both branches, so a failed unregister tells the operator "nothing was registered", which is both confusing and the opposite of the state they are trying to  Fix: Branch the message the same way the two Write-Host calls above it already branch, e.g. `$verb = if ($Unregister) { 'unregistered' } else { 'registered' }` and `throw "regasm failed with exit code $LASTEXITCODE; nothing was $verb."`.
- **extractor/SwReview.Extractor/Dump/PackageWriter.cs:109** (pane-cleanup) - `File.WriteAllText(path, PackageSerializer.Serialize(package))` truncates then streams. Three new readers now observe that file while it is being written: `RunFolders.HasEvidence` (File.Exists -> the step strip says step 2 is done mid-write, and says it again after a dump that failed halfway and lef Fix: Write to `package.json.tmp` in the same folder and `File.Replace`/`File.Move(overwrite)` onto `package.json`, so the file is either the previous package or the new one and never neither. That makes the stamp-based reload and `HasEvidence` correct by construction rather than by luck. Add a test that 
- **extractor/SwReview.AddIn/Terminal/TerminalPage/term.js:265** (pane-cleanup) - `counted()` guards the component count (`typeof counts.components !== 'number'` -> "the evidence package") but then substitutes a literal zero for a missing gap count: `(counts.gaps || 0) + ' gaps'`. A host that could not count gaps, or a future payload that omits the key, is reported to the enginee Fix: Apply the same guard to gaps: build the phrase from the counts that are numbers and omit the ones that are not (e.g. `7 components` when gaps is absent), never defaulting to 0.
- **extractor/SwReview.AddIn/Terminal/TerminalPage/term.js:521** (pane-cleanup) - `state.evidence` is set once, from the `init` reply at page load, and thereafter only by a successful `evidence.extract`. Nothing refreshes it when the *pane's* answer changes underneath: a review run in the Review tab creates a new session folder and writes a package into it, and `TaskPaneControl.R Fix: Add an unsolicited host -> terminal page message (`evidence.changed {present, run_dir}`) posted from the same place `Extracted`/`RefreshSteps` fires, document it in the Host -> terminal page table of contracts/pane-host-messages.md, and have `renderEvidence` follow it. The contract test counts the u
- **extractor/SwReview.AddIn/TaskPaneControl.cs:182** (pane-cleanup) - `AskPurpose` promises "Read-only: it can query the extracted evidence, measure, and capture, but cannot change or save anything." `bridge_capture` reaches the live document through CaptureService, which calls `IModelDoc2.ViewZoomToSelection` (SwCaptureView.cs:110) and `IModelDoc2.ShowNamedView2` (Sw Fix: Make the promise precise: "...but cannot edit or save your model (a capture may rotate or zoom the view)." One clause, and it stops the first surprise from reading as a broken promise.
- **extractor/SwReview.AddIn.Tests/TaskPaneGuidanceTests.cs:277** (pane-cleanup) - `NothingThePaneShowsCallsTheEvidencePackageAnIr` sweeps only `Control.Text`, `TabPage.ToolTipText` and the three ActionsPanel button tooltips. The class comment claims it "walks every caption, banner, status line and tooltip the pane can show" — but the engineer-facing strings that actually still sa Fix: Either narrow the comment to what the test checks, or better, add a second test that greps the shipped text assets — Terminal/Profiles/*.md, TerminalPage/index.html, TerminalPage/term.js, Review/ReviewPage/*.js user-visible strings — for `\bIR\b`, excluding code comments. That test would have caught
- **extractor/SwReview.AddIn/SwReviewAddIn.cs:72** (pane-cleanup) - `[ClassInterface(ClassInterfaceType.None)]` -> `AutoDispatch` is justified and tested (SetAddinCallbackInfo2 needs IDispatch), but it also publishes every public member of this class late-bound to any in-process COM caller that can reach the add-in object via `ISldWorks.GetAddInObject` — including ` Fix: Keep AutoDispatch and shrink the surface: mark `SwApp` and `ToolService` `[ComVisible(false)]` (they exist for the tests and for in-assembly wiring, not for COM), and add `[assembly: ComVisible(false)]` with `[ComVisible(true)]` left on `SwReviewAddIn` alone. Add an assertion to AddInComCallbackTest
- **extractor/SwReview.AddIn/TaskPaneControl.cs:204** (pane-cleanup) - `_terminalRunFolder` is written from the TerminalHost pump thread (`TerminalRunFolder()` is called from `Start`/`Extract`, both on that thread) and read from the WinForms UI thread by `SessionRunDirectory` during `RefreshOnUiThread`, with no synchronization and no `volatile`. Reference assignment is Fix: Make the field `volatile` (or guard it with the same lock pattern used elsewhere in the pane) and correct the `RefreshSteps` comment to say it is called from background threads and marshals for itself — which is what the `InvokeRequired` branch already does.
- **reviewer/src/swreview/mcp/server.py:186** (pane-cleanup) - `refresh`'s try/except is the interesting branch and nothing tests it. The three new tests cover appear, rewrite, and read-once. Untested: a `package.json` that becomes unparseable mid-session (does the CLI get an error result naming it, and does it recover when the file is rewritten?), a package th Fix: Add three tests around `Toolset.refresh`: unparseable-then-fixed, deleted-then-rewritten, and unreadable exceptions.json after startup — each asserting the error result's text and that the next call recovers.
- **extractor/SwReview.AddIn/TaskPaneControl.cs:833** (pane-cleanup) - `private readonly ToolTip _tips = new ToolTip();` is a Component that owns a native window and is never disposed: it is not added to a `components` container and `ActionsPanel` overrides no `Dispose`. The pane is created per SOLIDWORKS session so the leak is bounded, but the add-in already disposes  Fix: Dispose `_tips` in an overridden `ActionsPanel.Dispose(bool disposing)`, or hold it in the standard `IContainer components` the rest of the control tree uses.
- **specs/002-task-pane-assistant/contracts/pane-host-messages.md:86** (pane-cleanup) - The "Terminal run folder" paragraph still reads "on `terminal.start` the host uses the current chat session's run folder when one exists; otherwise it creates ...". `evidence.extract` now resolves a folder through the same rule (its row points at "rule below"), and — per must_fix 2 — it should *not* Fix: Name both messages in the run-folder paragraph and state the running-session exception explicitly. Separately, either relax FR-001 to "a short statement" or shorten the Extract banner to one line with the detail left in the button tooltips, where it already is.
- **reviewer/src/swreview/mcp/server.py:244** (pane-cleanup) - build_toolset stamps the file *after* reading it (loaded,... = _bind(...) on line 236, then stamp=_stamp(run_dir) on line 244), which is the opposite of the order Toolset.refresh deliberately uses (self.stamp = stamp on line 189, before the load) and the one ordering that can pin a stale package per Fix: Take the stamp before the bind in build_toolset (stamp = _stamp(run_dir) on the line above _bind), or simpler and DRYer: construct the Toolset with package=None, dispatch=None, stamp=None and call refresh() once, so there is a single load-and-stamp path. Add a test that monkeypatches _bind (or load_
- **reviewer/tests/unit/test_mcp_server.py:568** (pane-cleanup) - Two named cases in the new reload docstrings are untested. (a) _stamp's justification is explicitly the *size* half - "a rewrite inside one filesystem tick is exactly what a second extraction into the same folder looks like" - but test_a_package_rewritten_while_the_cli_runs_is_reloaded changes both  Fix: Add three tests: rewrite the package and os.utime it back to the previous st_mtime_ns with a different size, asserting the new design name is served; write invalid JSON over a loaded package and assert the next call is an error result naming the file (and that a valid rewrite afterwards recovers); d
- **extractor/SwReview.AddIn/Terminal/TerminalPage/term.js:221** (pane-cleanup) - renderEvidence hides the Extract evidence button entirely once evidence.present is true, so the Ask tab offers exactly one extraction per session folder. That contradicts the rationale written into the code it feeds: Toolset.refresh's docstring says "the Review tab writes a second package.json over  Fix: Keep the button visible when evidence is present and change only its label/sentence (e.g. "This session folder holds an evidence package." + a "Re-extract" button), disabled only while extracting. Cover the re-extract state in the behavioural page test proposed above.
- **specs/002-task-pane-assistant/contracts/mcp-toolset.md:35** (pane-cleanup) - Contract text left behind by this change. mcp-toolset.md line 34-35 still reads "so the CLI can tell the engineer to press Review or Dump IR first" - the button name is gone and the server's own message was rewritten in this change to "press Review or Extract evidence ... and ask again - this CLI pi Fix: Update mcp-toolset.md line 35 to quote the server's current sentence and state that the package is re-read lazily on every call. For the specs/001 files, ask the owner first - the standing constraint is that feature 001 contracts change only where a task says so - and either get that permission or r
- **extractor/SwReview.AddIn.Tests/TaskPaneGuidanceTests.cs:187** (pane-cleanup) - Tautological assertion in ThePaneNeverThrowsBecauseAStepCallbackDid. Assert.Equal(3, pane.Steps.Lines.Count) cannot fail: StepStrip.Lines is a three-element array built from three fields in the constructor, whatever the callbacks did. The test proves RefreshSteps swallowed the exception but not that Fix: Replace the count with the three literal pending lines already used by WithNothingOpenEveryStepIsPendingAndSaysWhy, and add a case where only one of the two callbacks throws, asserting the other step still renders its true answer.
- **extractor/SwReview.AddIn/TaskPaneControl.cs:180** (pane-cleanup) - The Ask banner overclaims on the one sentence it exists for: "Read-only: it can query the extracted evidence, measure, and capture, but cannot change or save anything." With the bridge configured, request_capture/bridge_capture act on the live document - per specs/001 contracts/cli.md the capture pa Fix: Tighten to the claim that is true and still reassuring, e.g. "Read-only on your model: it can query the extracted evidence, measure and take screenshots, but it cannot edit, rebuild or save your document." Update the matching literal in TaskPaneGuidanceTests.cs:41.
- **extractor/SwReview.AddIn/TaskPaneControl.cs:440** (pane-cleanup) - RefreshSteps marshals only when the handle exists: `if (IsHandleCreated && InvokeRequired)`. When the pane has been constructed but not yet shown - the handle is not created - a call from TerminalHost's pump thread (Extracted = pane.RefreshSteps) falls through to RefreshOnUiThread() on the pump thre Fix: Either skip the repaint when the handle does not exist yet (and repaint once from OnHandleCreated), or keep a captured SynchronizationContext from the constructor and post through it. A test can assert that calling RefreshSteps from a background thread on a pane whose handle is not created does not 
- **specs/002-task-pane-assistant/spec.md:109** (pane-cleanup) - The approved wording has no source of truth in the repository. I could not find an owner document for this change anywhere under docs/ or specs/, so the three banner sentences exist only as constants in TaskPaneControl.cs and as hand-copied duplicates in TaskPaneGuidanceTests.cs:37-49 - the duplicat Fix: Put the three sentences verbatim in spec.md FR-001 (or in contracts/pane-host-messages.md) and have TaskPaneGuidanceTests read them from that file the way TerminalPageContractTests reads the contract tables, keeping the literal copies only if the owner prefers the belt-and-braces. Either way, reword
- **reviewer/src/swreview/mcp/server.py:176** (pane-cleanup) - refresh's "what it deliberately does not re-do" paragraph names only the specs, but the reload also silently skips exceptions.json: _bind re-reads it, yet refresh is triggered solely by package.json's stamp, so an exception the engineer accepts in the Review tab while the CLI is connected is invisib Fix: Either add exceptions.json to the stamp (a tuple of both files' (mtime_ns, size), which costs one extra stat), or say plainly in the docstring that exceptions are bound once and a newly recorded exception needs a restart - and add the test for whichever answer is chosen.

## Phase 6-7 (2026-09-16)

- **reviewer/src/swreview/cli.py:1106** (exceptions-cli) - `RMS_WAIVER_INVALID` emits six distinct status strings (`invalid (warn rule)`, `invalid (data-gap rule)`, `invalid (out-of-scope rule)`, `invalid (unknown rule)`, plus `accepted <n>`/`would accept <n>`/`unused`), but contracts/cli.md line 10 states "The `--json` `status` field carries the same four  Fix: Keep the honest labels and raise the wording gap so contracts/cli.md line 10 can be reconciled (it is the spec that is abbreviating, not the code that is wrong); flag it explicitly in the task hand-back rather than leaving the deviation silent.
- **reviewer/src/swreview/cli.py:954** (exceptions-cli) - `rms types` surveys feature `type_name` only. contracts/cli.md line 8 and FR-020 both say "type names ... the tables do not classify" (plural), and the second table is `assembly.reference_entity_kinds` / `geometry_entity_kinds` in rms_types.yaml:48-53, whose own comment says an unlisted kind is unre Fix: Extend `unknown_types` (or add a sibling census in rms_types.py, reused by both callers) to also report mate entity kinds in neither assembly list, with their counts and the documents the mates belong to, and print them as a second section of `rms types`; add a test over a package carrying an unlist
- **reviewer/src/swreview/cli.py:1250** (exceptions-cli) - On a refused import (any invalid id) and on an import where no listed rule had a finding, the `--json` payload still reports `"exceptions_file": <path>` for a file that was never created, while `report_file` is correctly `None`. test_accept_rms_writes_nothing_when_no_listed_rule_has_a_finding assert Fix: Mirror `report_file`: emit `exceptions_file` as `None` when `store.save()` was not called, or rename the key to make it clearly the target path rather than a written file.
- **reviewer/tests/golden/fixtures/rms-exceptions/generate_package.py:59** (exceptions-cli) - A committed golden-fixture generator imports `AcceptedFinding` from `tests.unit.test_tools_rms_checks` - a unit-test module - so regenerating the fixture depends on a private class in a test file that nothing guarantees is stable. There are now four copies of the same two-line `FingerprintTarget` st Fix: Move `AcceptedFinding` into tests/support/ (next to features.py) and have all four sites import it from there; move `invoke`/`payload` into tests/support/ as well and re-import them in test_cli.py. One helper per concern, per the feature's DRY constraint.
- **reviewer/tests/unit/test_cli_exceptions_accept_rms.py:259** (exceptions-cli) - The suite covers `demonstrated`, `checked_within_scope` (already waived -> unused), warn, data-gap, out-of-scope, unknown, empty file, no-reason, non-object file, missing file and missing session - but not a finding carrying a `needs_review` exception, which is the one state the rms-exceptions fixtu Fix: Add a case that runs accept_rms against tests/golden/fixtures/rms-exceptions for `rms.quarantine.chamfers_before_fillets` and asserts on the state AFTER a second `check rms`: the rule must come back `checked_within_scope`, and the store must not hold two live exceptions for the same check and bindin
- **reviewer/src/swreview/cli.py:1230** (exceptions-cli) - `store.save()` runs before `_save_run` (cli.py:1230-1232, and the same order in exceptions_accept at 1079-1081). If `render_report` or the session write fails, exceptions.json is on disk while no finding records an `exception_id` and the report still shows the old text - the half-applied state the c Fix: Write the session and the report first and save the store last, or write all three through a temp-file-then-rename so the group is atomic; at minimum note the ordering in the docstring so the guarantee it claims is the guarantee it gives.
- **reviewer/tests/unit/test_cli_exceptions_accept_rms.py:177** (exceptions-cli) - The binding assertions are weak where the task is most specific. `assert stored[0]["component_persist_refs"]` is a truthiness check: an exception bound to the wrong component instance, with the wrong `persist_ref_scopes`, or with the wrong `configuration` passes every test in the file (I had to deco Fix: Assert the exact values: `component_persist_refs` equal to the persist ref of the finding's component instance, `persist_ref_scopes == ['doc:1']`, and `configuration == 'Default'` - and assert `fingerprint_kind == 'feature_tree'` for the second accepted rule too, not only the first.
- **reviewer/src/swreview/checks/rms_types.py:350** (exceptions-cli) - `unknown_types` is new public API (added to `__all__`) with no direct unit test in `tests/unit/test_rms_type_table.py`; it is covered only through `swreview rms types` over a fixture with a single unknown type in a single document. The two pieces of logic that can actually be wrong - the first-seen  Fix: Add unit tests in test_rms_type_table.py for: two unknown type names returned in first-seen order; one type name seen in two documents returning both ids once each in first-seen order; an `ambiguous` type absent from the census; and an explicit non-default `table`.
- **reviewer/tests/golden/fixtures/rms-exceptions/generate_package.py:59** (exceptions-cli) - The fixture generator imports `AcceptedFinding` from `tests.unit.test_tools_rms_checks`, a unit-test module, while `fixtures/rms-part/generate_package.py:287` defines its own `_AcceptedFinding` with the same three fields. That is three copies of one shape, and a golden fixture whose reproducibility  Fix: Move the shape to `tests/support/features.py` (next to `rms_package`/`suppress_run`, which both generators already import) and have the unit test and both generators use that one definition.
- **reviewer/tests/unit/test_cli_exceptions_accept_rms.py:55** (exceptions-cli) - The whole file is pinned to properties of the rms-part golden fixture that a sibling task regenerates: exactly one demonstrated `rms.core.shell_last` finding, exactly one `rms.refs.direction`, `rms.quarantine.only_fillets_and_chamfers` already waived, and `rms.params.global_variables_present` report Fix: Build the graded package for the status tests with `tests.support.features.rms_package`, seeding exactly the rules each test needs (one fail rule demonstrated, one warn rule, one pre-waived condition), and keep the golden fixture only where the test is genuinely about the shipped fixture.
- **reviewer/src/swreview/cli.py:1190** (exceptions-cli) - The `unused` docstring names three causes - 'it passed, or it was waived already, or it never ran' - and the tests cover only two: `test_accept_rms_reports_an_already_waived_finding_unused` and `test_accept_rms_reports_a_fail_rule_with_no_finding_unused` (a rule of another scope, i.e. never ran). A  Fix: Add one case listing a fail rule the graded package satisfies (e.g. a compliant package plus `rms.folders.present`) and assert `unused` with an empty `finding_ids`.
- **reviewer/tests/unit/test_cli.py:2092** (exceptions-cli) - T045 adds a new top-level command group (`rms`), but `test_the_top_level_help_lists_every_command_group` still lists only the eight pre-existing groups, so the new group's registration is unpinned - `app.add_typer(rms_app, name='rms')` could be dropped and only the two new test files would notice. Fix: Add `"rms"` (and, while there, the equally missing `"chat"`) to that test's tuple.
- **reviewer/src/swreview/checks/rms/part.py:1084** (suppress-plan-and-rule) - A `rebuild_errors` row is turned into a `demonstrated` finding on the outcome string alone. The outcome table defines that row as 'post-rebuild count above the run's baseline', and the code already refuses to fail on a missing count (line 1087), but it does not check `whats_wrong_count > run.baselin Fix: In the `rebuild_errors` branch, treat `tried.whats_wrong_count <= run.baseline_whats_wrong_count` the same way as a missing count: unresolved with a reason naming both numbers, never a fail. One extra T056 case.
- **reviewer/src/swreview/checks/rms/part.py:1052** (suppress-plan-and-rule) - `by_feature_id = {row.feature_id: row for row in run.rows}` silently keeps the last row when a run carries two rows for one feature: the earlier outcome (a `rebuild_errors`, say) disappears from the report entirely — it is neither graded nor named as unused, and `tested` still counts the feature onc Fix: Detect duplicate `feature_id`s while building the index and report them — either as an unresolved reason for that feature ('two run rows: ok, rebuild_errors') or in the coverage line alongside the unused rows. A run with duplicate rows is a corrupt run, and saying so is cheap.
- **reviewer/src/swreview/checks/rms/part.py:1052** (suppress-plan-and-rule) - Rows are matched to features by `feature_id` alone; `SuppressTestRow.persist_ref` / `persist_ref_scope` (carried in every row for exactly this purpose) are never compared with the package feature's. A run appended to a package other than the one it was planned against — `suppress-test --out <other p Fix: When a row's `feature_id` matches a Detail content feature but its `persist_ref`/`persist_ref_scope` (and, for the run, `group` vs `tree.group_name(DETAIL)`) disagree with the package, report that feature unresolved ('run row identifies another feature') instead of grading it, and add the T056 case.
- **reviewer/tests/golden/fixtures/rms-part/generate_package.py:257** (suppress-plan-and-rule) - The golden run sets `features_present = 6` (len(rows)) while `swreview rms suppress-plan --document doc:3` over the same package plans 7 features, so the fixture encodes a run the product cannot produce (the extractor writes one row per planned feature, `truncated` for the ones it never reached — da Fix: Add a test (unit or golden-adjacent) that `build_plan(package, 'doc:3', load_table())` names exactly the 7 features the rule grades for doc:3, and that the golden run's 6 rows are a prefix of the plan in plan order. Consider having the rule name `run.features_present` versus the number of Detail con
- **reviewer/src/swreview/cli.py:1009** (suppress-plan-and-rule) - The plan file is resolved and written outside `_errors_as_exit_1()` (lines 1009-1012), unlike every other write in this module (compare `exceptions_accept`, line 1073 onwards). A `--out` that is a directory or an unwritable path exits with a Python traceback instead of `error: ...` and exit 1. Separ Fix: Move the `mkdir`/`write_text` inside the `with _errors_as_exit_1():` block (adding OSError to the handled set if needed), and refuse an `--out` whose name is `package.json` or which resolves to the package file, with a message.
- **reviewer/src/swreview/checks/rms/part.py:1138** (suppress-plan-and-rule) - When nothing was skipped and nothing is unresolved, the rule appends a subject-less `skip` result purely to carry the coverage line, so a document whose every Detail feature passed appears in the report's `skipped` coverage bucket for this rule with no subject and no skipped condition (test asserts  Fix: Either keep it and say so in the reason (prefix the line with something like 'nothing skipped; '), or carry the coverage line on the `pass` path instead by giving the rule a coverage hook the report layer reads. A one-line reason prefix is the cheaper and more explicit of the two.
- **reviewer/src/swreview/checks/rms/part.py:1087** (suppress-plan-and-rule) - The rule's trust in a row is inconsistent. `rebuild_errors` with `whats_wrong_count is None` is demoted to unresolved (a deviation from the outcome table, which says `rebuild_errors` -> fail), but `rebuild_errors` with `whats_wrong_count == 0` or any count <= `run.baseline_whats_wrong_count` still b Fix: Pick one policy and pin it. Recommended: grade from the numbers the run recorded — fail when `whats_wrong_count > run.baseline_whats_wrong_count`, unresolved with a reason naming both numbers when the outcome says `rebuild_errors` but the count does not exceed the baseline (contradictory evidence is
- **reviewer/src/swreview/checks/rms/part.py:1138** (suppress-plan-and-rule) - When a run has no skipped and no unresolved features, the rule appends a subject-less `skipped` RuleResult whose only content is the coverage line. `report.py::_aggregate` then puts that document into the `skipped` coverage bucket for this rule alongside the `checked` one, and `_write_summary` count Fix: Confirm the intent. Option A (recommended, small): emit the subject-less skip only when the run is incomplete or has unused rows — i.e. when the coverage line says something other than `<n>/<n>` with no unused rows — so a fully clean document lands only in `checked`. Option B: teach the report layer
- **reviewer/src/swreview/checks/rms/part.py:1055** (suppress-plan-and-rule) - `tested` excludes only `truncated`, so `already_suppressed`, `not_applied` and `aborted` rows all count as tested. In the regenerated golden this is why doc:3 reads `tested 6/7` even though Sketch4 (already_suppressed) was never actually suppressed and nothing was learned about it — five of the seve Fix: Decide what `tested` means and say it in the docstring plus a test. Recommended: count a feature as tested only when the run reached a verdict about it — `ok` and `rebuild_errors` — and let the reason carry the rest (it already lists every skipped and unresolved feature by name). That would make the
- **reviewer/tests/golden/fixtures/rms-part/generate_package.py:252** (suppress-plan-and-rule) - The seeded run has six rows and `features_present` defaults to 6, but doc:3's Detail group holds seven content features (Sketch4, Cut-Extrude2, Cut-Extrude3, Sketch5, Hole1, Cut-Extrude5, Cut-Extrude4), which is exactly what `build_plan` would write. SuppressTestRun's own validator documents the inv Fix: Confirm the intended reading with me before changing anything, since this is spec-directed. Options: (A) leave it and add a comment in `suppress_test()` saying the missing row models a stale plan file, not a run the current command can emit; (B) keep the six rows but set `features_present=7` so the 
- **reviewer/tests/unit/test_rms_suppress_rule.py:151** (suppress-plan-and-rule) - Three cases are now spelled twice, near-verbatim: `test_without_a_run_every_detail_content_feature_is_unresolved` and `test_a_run_for_another_document_is_no_run` here (lines 151, 159) duplicate test_rms_part_rules.py:1316 and :1329, and `test_no_detail_group_skips_before_the_run_is_read` (line 167)  Fix: Keep the outcome-table cases in test_rms_suppress_rule.py and reduce test_rms_part_rules.py::TestIndividuallySuppressible to the seam test it already documents (`test_a_run_for_this_document_is_graded`) plus the no-Detail-group skip that the per-rule class contract requires. That keeps one assertion
- **reviewer/tests/unit/test_rms_suppress_plan.py:428** (suppress-plan-and-rule) - `test_suppress_plan_without_a_package_is_a_usage_error` and `test_suppress_plan_without_a_document_is_a_usage_error` (line 424) assert only `exit_code == 2`. Typer also exits 2 for an unknown command, so both tests would still pass if `rms suppress-plan` did not exist at all — they cannot go red for Fix: Assert on the message as well, e.g. that stderr names the missing option (`--document` / `--package`), which is only produced once the command is registered with those options. That also justifies keeping the `plain_dir` fixture.
- **reviewer/src/swreview/checks/rms/part.py:1108** (suppress-plan-and-rule) - `observed` puts `run baseline N rebuild error(s).` immediately after the feature names, and the report derives the finding title from the first ~70 characters, so the golden's title is `Cut-Extrude2 cannot be suppressed alone; run baseline 0 rebuild error(s)` — the whole title is spent on a number t Fix: Move the baseline clause after the per-feature detail: `<names> cannot be suppressed alone; <name>: N rebuild error(s) after suppression: <messages> (run baseline N)`. This changes one golden line, which is inside T057's allowance, and the existing assertions on `observed` are substring-based so the
- **reviewer/src/swreview/checks/rms/plan.py:91** (suppress-plan-and-rule) - Two small duplications worth noting rather than a defect. (a) `rows = [row for row in package.features if row.document_id == document_id]` is now the fourth copy of the same filter (tools/rms_checks.py:123, tools/rms_query.py `_rows_of` used twice), where a shared `rows_of(package, document_id)` wou Fix: For (a), promote `_rows_of` out of tools/rms_query.py into a shared home (checks/rms/part.py already owns `part_tree`) and call it from plan.py and rms_checks.py — this is pre-existing duplication that plan.py extends, so it is your call whether it belongs in this task. For (b), either compare `run.
- **extractor/SwReview.Extractor.Tests/SuppressTestGuardTests.cs:29** (csharp-guard-suppress) - `StillRefused` is a hand-copied duplicate of `ReadOnlyGuard.DeniedMembers` + `DeniedPrefixes`. `Assert_DiffersFromTheReadOnlyGuardOnExactlyTheExemptMembers` (line 98) therefore proves "exactly two" only over the members this array happens to list. Adding a member to `ReadOnlyGuard.DeniedMembers` and Fix: Expose the denylist read-only from `ReadOnlyGuard` (e.g. `public static IReadOnlyCollection<string> DeniedMembers => _denied;`) and rewrite the 'exactly' test to iterate it: assert `SuppressTestGuard` refuses every member in it except `SetSuppression2` and `ForceRebuild3`, and assert the set-differe
- **extractor/SwReview.Extractor.Tests/SwGateTests.cs:108** (csharp-guard-suppress) - T048 requires that 'every existing construction site' still refuses `ForceRebuild3` and `SetSuppression2`. The test's own XML doc claims coverage of 'the add-in's tool service, the bridge's session, SwSession.Attach's fallback, every dumper', but the body only exercises `new SwGate()` and `new SwGat Fix: Either add assertions for the named sites (a `SwSession.Attach` test over the existing fake `ISldWorks` that asserts `session.Gate.Call("SetSuppression2", ...)` throws `MutatingCallError`, plus the equivalent for the add-in's gate), or narrow the XML doc to what is actually asserted. Prefer the form
- **extractor/SwReview.Extractor/Rms/SuppressTest.cs:400** (csharp-guard-suppress) - The final `Restore(...)` in `Execute` is the only call not inside a try, and `Restore`'s per-feature handler catches exactly four types (CircuitOpenError, COMException, InvalidOperationException, MutatingCallError; lines 504-519). Anything else from a stale RCW — `TargetInvocationException`, `NullRe Fix: Broaden the per-feature handler in `Restore` to `catch (Exception)` and name the feature in `unrestored` (one dead feature must not stop the rest, and a swallowed unknown type must still be reported as unrestored rather than lost). Optionally also wrap the final `Restore` call in a try so that an ex
- **extractor/SwReview.Extractor.Console/Program.cs:1069** (csharp-guard-suppress) - Nothing ties the attached document to `plan.document_id`. `RowsFor` (SuppressTest.cs:655) selects the package rows by `plan.DocumentId`, and the walk/persistent-id comparison is against those rows, but no check compares `session.DocumentPath` with the package's `documents[plan.document_id].path`. Ru Fix: Before building the target, resolve the plan's document in the loaded `EvidencePackage` and refuse when its path does not match `session.DocumentPath` (normalized compare, as `PersistRefService.IsSame` does for refs): 'the plan is for <package path> but this session has <session path> open'. Add a `
- **extractor/SwReview.Extractor/Rms/SuppressTest.cs:655** (csharp-guard-suppress) - `RowsFor` filters `packageFeatures` on `DocumentId` only and ignores `IrFeature.Configuration`, while the whole run is pinned to one configuration (`plan.Configuration`, re-checked against the live active configuration at line 243). If a package ever carries `features[]` rows for the same document u Fix: Add `&& string.Equals(row.Configuration, plan.Configuration, StringComparison.Ordinal)` to the `Where` in `RowsFor` (pass the plan's configuration in), and give the empty-result refusal a message that names the configuration as well as the document.
- **extractor/SwReview.Extractor/Rms/SuppressTest.cs:369** (csharp-guard-suppress) - `attempted++` runs before `TestOne`, so an `already_suppressed` row — which performs no suppression and no rebuild (line 415) — consumes one unit of the `--limit` budget. contracts/cli.md describes `--limit <n>` as bounding features to attempt, and the cost the limit exists to bound is the rebuild.  Fix: Move the already-suppressed decision out of `TestOne` into `Execute` (or have `TestOne` report whether it attempted anything) and increment `attempted` only for rows that reach `SetSuppression2`. Add a `SuppressTestTests` case: three planned features, the first already suppressed, `--limit 2`, and a
- **extractor/SwReview.Extractor.Tests/SuppressTestTests.cs:577** (csharp-guard-suppress) - `Run_NamesEveryInteropMemberToTheGateAndNeverASaveMember` — the test T054 names for 'never calls a save member (asserted on recorded member names)' — asserts only on `test.Observer.Members`, which under the fake sees just the seven members `SuppressTest` names itself. The three multi-call members (` Fix: Add `foreach (string save in new[] { "Save3", "SaveAs3", "SetSaveFlag", "ForceRebuildAll", "EditRollback" }) Assert.DoesNotContain(save, test.Target.Calls);` to the same test, so the assertion covers every call the seam makes, not only the ones named one layer up.
- **extractor/SwReview.Extractor/Rms/SuppressTest.cs:378** (csharp-guard-suppress) - `Execute` restores the whole tree after every row (line 378) and then restores the whole tree again unconditionally at line 400. On a clean run the last two restores are back-to-back full walks — 2N `IsSuppressed2` gated calls with nothing between them that could change a state. On a 400-feature par Fix: Track whether the last per-row `Restore` completed cleanly and, when the loop finished with `failure == null` and that restore verified, set `RestoreVerified = true` from it instead of walking again. Keep the unconditional final restore for the `failure != null` path, which is the one that needs the
- **extractor/SwReview.Extractor.Console/Program.cs:1095** (csharp-guard-suppress) - The generic catch prints `SuppressTest.ModifiedInMemoryMessage` on stdout unconditionally, but three of the exceptions that reach it are raised BEFORE SOLIDWORKS is touched at all: `SuppressPlanError`/`FileNotFoundException` from `SuppressPlan.Load` (line 1061), a bad package from `PackageAppender.L Fix: Track whether the run actually started (set a flag immediately before `new SuppressTest(...).Run(...)` on line 1073) and print `ModifiedInMemoryMessage` in the generic catch only when it is set; otherwise print the same "nothing was changed" wording the refusal branch uses. Cheap, and it keeps the m
- **extractor/SwReview.Extractor.Console/Program.cs:1069** (csharp-guard-suppress) - `SwSession.Attach(swApp, documentPath, null, gate)` falls through to `SwSession.OpenReadOnly`, which opens the document with `swOpenDocOptions_ReadOnly | swOpenDocOptions_Silent` (SwSession.cs:155-156) when it is not already open. So `suppress-test --doc C:\some\part.SLDPRT` on a part nobody opened  Fix: In `ExecuteSuppressTest`, resolve the document with `swApp.GetOpenDocumentByName(documentPath)` directly (or add an `openOnly` overload to `SwSession.Attach`) and refuse with a `SuppressTestRefusedError` naming the document when it is not already open: "open <path> in SOLIDWORKS first; this command 
- **extractor/SwReview.Extractor.Tests/SuppressTestTests.cs:458** (csharp-guard-suppress) - T054 asks for "snapshots the whole tree", and no test ever exercises a restore that has to put a feature back to SUPPRESSED. In every restore case the snapshot is all-false: `Run_SuppressionAlsoSuppressedDependents` starts with nothing suppressed, and in `Run_FeatureAlreadySuppressed` the pre-suppre Fix: Add a case where a NON-planned feature starts suppressed (`test.Target.Features[0].Suppressed = true; test.Rows[0].Suppressed = true;`) and is listed as a dependent of a planned feature, then assert after the run that it is still suppressed, that `RestoreVerified` is true, and that `SetSuppression2`
- **extractor/SwReview.Extractor.Tests/SuppressTestGuardTests.cs:98** (csharp-guard-suppress) - `Assert_DiffersFromTheReadOnlyGuardOnExactlyTheExemptMembers` claims to prove the "exactly two members" property but does it over three hand-maintained string arrays (lines 18-62). It catches a THIRD exemption added to `SuppressTestGuard` only while that member also happens to appear in `StillRefuse Fix: Reflect over `ReadOnlyGuard`'s private static `DeniedMembers` and `DeniedPrefixes` (the test assembly already has `InternalsVisibleTo`; a `BindingFlags.NonPublic | BindingFlags.Static` field read is three lines) and assert that the set of members `ReadOnlyGuard` refuses but `SuppressTestGuard` allow
- **extractor/SwReview.Extractor/Rms/SuppressTest.cs:658** (csharp-guard-suppress) - `RowsFor` selects the package rows by `DocumentId` alone and ignores `IrFeature.Configuration`, even though `Compare` (line 594) then compares live suppression - read in ONE configuration - against `row.Suppressed`, which is documented in data-model.md:27 as "In `configuration`". Today `dump` writes Fix: Add `&& string.Equals(row.Configuration, plan.Configuration, StringComparison.Ordinal)` to the `Where` in `RowsFor` (pass the configuration in), and keep the existing empty-rows refusal - it then also covers "rows exist but none for this configuration" with an accurate message. One line, no behaviou
- **extractor/SwReview.Extractor/Sw/RecordingGateObserver.cs:20** (csharp-guard-suppress) - `RecordingGateObserver` is a near-copy of `SwGateRecorder` in extractor/SwReview.AddIn/ToolService/ToolServiceHost.cs:45: the same `HashSet<string>(OrdinalIgnoreCase)` + `List<string>` first-seen dedup, the same null/whitespace guard, the same not-thread-safe rationale in the docstring. They differ  Fix: Not in T048-T055's scope to change ToolServiceHost.cs, so the call here is yours: either (a) leave both and add a one-line "see also" comment in each so a future edit to one is prompted to check the other, or (b) a separate follow-up task that reimplements `SwGateRecorder` as a thin wrapper over `Re

## Phase 8-9 (2026-09-16)

- **extractor/SwReview.Extractor.Tests/PackageWriterTests.cs:540** (extractor-fast-path) - `Build_ModelCheckProfile_RecordsNothingButTheProfile` compares the full and thin gap lists, but the fake's `_holePass` is empty unless a test explicitly seeds it (line 1031: `if (_holePass.Count > 0)`), so the one mechanism by which the two profiles can differ is never exercised. In the real dump th Fix: Seed the fake's hole census pass (a `TypeNameSighting("AdvHoleWzd", consumed: true)` for a name the traversal pass censuses as unconsumed) and re-run the gap-equality assertion. Then decide the real question deliberately: either the skipped phases still register their consumption under `ModelCheck`,
- **extractor/SwReview.Extractor.Tests/PackageWriterTests.cs:565** (extractor-fast-path) - All three part-root tests (`Build_PartRootTree_YieldsOneComponentInstanceForTheDocumentItself`, `..._CarriesThatDocumentsFeaturesAndEquations`, `..._ProducesAPackageThatValidatesAgainstTheContract`) call `ModelCheckOptions()`. Nothing pins the part-root tree under the `Full` profile, yet that is the Fix: Add a `Build_PartRootTree_UnderTheFullProfile_...` test over the same one-node tree asserting the hole, fastener, face and mesh sources were called with that single component and that the package validates - so the change to the full dump of a standalone part is stated rather than incidental.
- **extractor/SwReview.Extractor/Ir/Manifest.cs:30** (extractor-fast-path) - `ExtractorInfo.Profile` is typed as `SwReview.Extractor.Dump.DumpProfile`, and the `using SwReview.Extractor.Dump;` at Manifest.cs:3 is the only `Ir -> Dump` reference in the whole extractor; `Dump` already depends on `Ir` (DumpContracts.cs has `using SwReview.Extractor.Ir;`), so this creates a name Fix: Move the enum to `Ir/ExtractorProfile.cs` next to `DocumentKind`, and have `DumpOptions.Profile` use it (or keep `DumpProfile` in `Dump` as a thin mapping). The serializer's snake_case policy gives the same `"full"`/`"model_check"` strings either way, so no wire change - `Ir` stops depending on `Dum
- **extractor/SwReview.Extractor.Console/Program.cs:257** (extractor-fast-path) - The dump log line echoes the invocation so an engineer can re-run it, and every other option is printed in its own CLI spelling (`--meshes glb`, `--features tree`, `--equations on`). The new profile line prints `PackageSerializer.EnumToJsonName(options.Profile)`, which is the *IR* spelling: it logs  Fix: Print the CLI spelling: `options.Profile == DumpProfile.ModelCheck ? "model-check" : "full"`, or a small `CliName(DumpProfile)` helper shared with `DumpProfile()` so the two spellings have exactly one place each.
- **extractor/SwReview.Extractor/Dump/PackageWriter.cs:206** (extractor-fast-path) - `bool geometry = options.Profile == DumpProfile.Full;` silently discards `--faces` and `--meshes`. `dump --profile model-check --faces all --meshes stl` parses, logs both options, and then reads no faces and writes no meshes, with no gap and no usage error - the comment at line 199 deliberately reco Fix: Reject the combination in `Program.cs` as a `UsageError` when `--faces` or `--meshes` was explicitly passed alongside `--profile model-check` (CommandLine already knows whether an option was supplied via `Value(name) != null`), with a sentence naming which option the profile overrides. That keeps th
- **extractor/SwReview.Extractor.Console/CommandLine.cs:222** (extractor-fast-path) - `DumpProfile()` is the fifth copy of an identical shape in this file - `MeshFormat()` (line ~155), `FaceScope()` (169), `FeatureScope()` (184), `EquationScope()` (198), `DumpProfile()` (222) - each being `Value(name) ?? default`, `switch (value.ToLowerInvariant())`, two-or-three literal cases, `defa Fix: Extract one `private T EnumOption<T>(string option, string fallback, params (string Spelling, T Value)[] allowed)` that builds the `must be a, b or c; got 'x'` message from `allowed`, and have all five call it. It is five lines of helper replacing ~60, needs no new abstraction beyond what is already
- **specs/001-agentic-design-review/contracts/cli.md:69** (extractor-fast-path) - The `dump` row was rewritten to document `--profile`, `--features` and `--equations`, and a closing paragraph about the Model check tab was added. T063, T064 and T065 authorize exactly one contract edit between them - T065's regeneration of `ir.schema.json` - and the review constraint is that contra Fix: Confirm which task owns this row (T063-T065 do not) and either move the edit there or get the sentence added to the task text before it lands. If it stays, note it explicitly as a deviation in the stage report rather than leaving it to be discovered in the diff.
- **extractor/SwReview.Extractor/Dump/ComponentTreeDumper.cs:200** (extractor-fast-path) - The part-root node -- the headline of T064 -- has no test over production code. `PackageWriterTests.UsePartRootTree` (PackageWriterTests.cs:825) hand-builds a look-alike node and the three `Build_PartRootTree_*` tests assert on that fake, so deleting `PartRootNode` entirely, or changing `Suppression Fix: Split the field set out as a pure factory the test can call without COM, e.g. `internal static ComponentNode PartRoot(string designName, string documentPath, string configuration, ScopedPersistRef? reference, bool isToolbox)`, leave the three COM reads in `PartRootNode`, and have `UsePartRootTree` b
- **extractor/SwReview.Extractor/Dump/ComponentTreeDumper.cs:205** (extractor-fast-path) - The part root asks `_refs.TryGet(document, document)` -- `GetPersistReference3` handed a whole `IModelDoc2` rather than an entity. If 2024 answers null or throws (plausible; the comment at line 192-197 admits it is the one thing that cannot be had for certain), `PackageWriter.AddComponentInstances`  Fix: No code change now -- the risk is correctly parked on PROBE-15, and `ProbeComponentTree` already prints `persist_ref=present|null` per node, which is the right instrumentation. Recommend instead: state this consequence explicitly in the T088 probe text or in the RK-15 risk note ("if the part root ha
- **extractor/SwReview.Extractor/Sw/SwSession.cs:99** (extractor-fast-path) - `KindOf` classifies every document type that is not `swDocASSEMBLY` or `swDocDRAWING` as `DocumentKind.Part` -- including `swDocNONE`, `swDocSDM`, `swDocLAYOUT`, `swDocIMPORTED_PART` and `swDocIMPORTED_ASSEMBLY`. That default was harmless when it only labelled a node; T064 makes it load-bearing at C Fix: Make the part branch demand the value rather than accept the fallback: either have `KindOf` return `DocumentKind?` with `swDocPART` as the only mapping to `Part` (and a gap for anything else), or, minimally invasive, gate line 126 on `swDocumentTypes_e.swDocPART` read directly instead of on `rootKin
- **extractor/SwReview.Extractor/Dump/DumpContracts.cs:156** (extractor-fast-path) - `ComponentTreeResult.RootDocumentKind` keeps its `= DocumentKind.Assembly` initializer, and T064 changed the read into a `TryStep` that leaves the property at that default when `GetType()` throws (ComponentTreeDumper.cs:91-108). Before T064 a failed read threw out of `Traverse`; now it is a gap plus Fix: Make the field `DocumentKind?` and have `RootNode` refuse (or gap) rather than read a default. Roughly ten lines across the dumper and the three tests that set it. Alternative (do nothing): acceptable given it is unreachable today, but then add a one-line comment at line 156 saying the default is on
- **extractor/SwReview.Extractor.Tests/PackageWriterTests.cs:552** (extractor-fast-path) - `Build_ModelCheckProfile_RecordsNothingButTheProfile` compares `full.Gaps` to `thin.Gaps` as sequences, but the default `FakeSources` full build produces no gaps at all (every gap test in this file configures one first: lines 160, 186, 275, 300, 386, 447), so the assertion is empty-equals-empty. It  Fix: Either assert the claim directly -- `Assert.Empty(thin.Gaps)` plus the existing profile assertion -- or seed a gap that both paths must still produce (`sources.TraversalGap = "..."`) so the equality is between two non-empty sequences and carries its weight.
- **extractor/SwReview.Extractor/Dump/PackageAppender.cs:63** (extractor-fast-path) - `PackageAppender` round-trips through `PackageSerializer`, and `ExtractorInfo.Profile` now has a non-ignored default, so appending `interference`, a `capture` or `rms_suppress_test` to an existing 1.0.0/1.1.0 package rewrites it with `"profile": "full"` while leaving `schema_version` at its original Fix: Decide it explicitly rather than inherit it: either add an `IrSerializerTests`/`PackageAppenderTests` case pinning "appending to a 1.1.0 package leaves schema_version alone and may add profile" with a sentence saying why that is safe, or have the appender raise `schema_version` to `CurrentSchemaVers
- **reviewer/tests/unit/test_ir_profile.py:106** (extractor-fast-path) - `test_every_shipped_golden_fixture_predates_the_profile_and_loads_as_full` parametrizes over every directory under `tests/golden/fixtures/` and asserts each one's `extractor` block has no `profile` key. That is a rule against future fixtures, not a property of the code: the first deliberately-added  Fix: Narrow it to what it means: keep the backward-compatibility claim ("a package written before 1.2.0 loads as full") over a frozen list of the pre-existing fixture names, or assert `package.extractor.profile in {"full", "model_check"}` and drop the `"profile" not in ...` half -- the 1.1.0 load path is
- **reviewer/tests/golden/fixtures/rms-part/session.json:1** (no-llm-core-and-route) - Two untracked artifacts were left inside the tracked golden fixture tree by a manual `swreview check rms --package reviewer/tests/golden/fixtures/rms-part` run: `session.json` (started_at 2026-09-16T10:42:57, model `gpt-5.6`) and `report.md` (31 KB). The golden harness (reviewer/tests/golden/test_go Fix: Delete reviewer/tests/golden/fixtures/rms-part/session.json and reviewer/tests/golden/fixtures/rms-part/report.md. Do not gitignore them - the right answer is not to run the writing command against the fixture tree.
- **reviewer/src/swreview/checks/rms/run.py:456** (no-llm-core-and-route) - `_carry_forward` treats `directory.parent` as the run root unconditionally, and T067 wires this into `swreview check rms --package <dir>` too. `--package` can name any directory, so a bare `design_id` match against an arbitrary sibling directory is enough to copy a foreign `exceptions.json` in and s Fix: Either require the carry-forward's candidate to look like a run folder (a sibling holding both `package.json` and a `session.json`, or a folder matching the `<yyyyMMdd-HHmmss>-<doc>` run-folder shape), or take the run root as an explicit argument that the route passes and the CLI leaves unset unless
- **reviewer/tests/unit/test_cli_exceptions_accept_rms.py:84** (no-llm-core-and-route) - `rms_run` still builds a session by calling `cli._context_for` + `rms_checks.run_part_checks` directly and `save_session`-ing the result, and its docstring still says "`check rms` prints its findings and saves nothing - grading a package must not edit it". That is a second evaluation path, which T06 Fix: Replace the body with `run_rms_check(package_dir, scope=RmsScope.part)` and point the fixture at the package directory it writes into (or copy the written `session.json` to `run_dir`), then delete the stale docstring sentence. Keep the assertions as they are; they should still pass, and they will th
- **reviewer/tests/unit/test_rms_run.py:315** (no-llm-core-and-route) - `assert run.findings == [] or run.findings` is a tautology - it is true for every possible value - so `test_no_api_key_is_read` asserts nothing about the findings after the keys are deleted. Given the CLAUDE.md preference for strong assertions, this is a silent coverage hole in exactly the SC-011 te Fix: Assert what the run must actually produce with no key present, e.g. `assert run.session.model` (already there) plus `assert run.grade.total > 0` and `assert [step.tool for step in run.session.steps] == ["check_rms_part"]`, and drop the tautology.
- **reviewer/src/swreview/checks/rms/report.py:235** (no-llm-core-and-route) - `_subjects` emits a `reason` key that contracts/model-check.md's `CheckResult.subjects` example does not list ({feature_id, name, type_name, group, persist_ref, persist_ref_scope, component_ids}). The key is required by T070's sentence ("is reported in the subject entry with the reason"), so the cod Fix: No code change. Leave a note for T085 to add the `reason` field (null when the reference was read) to the `subjects` block in specs/003-resilient-modeling/contracts/model-check.md so the shipped shape and the contract agree.
- **reviewer/tests/unit/test_chat_checks_routes.py:540** (no-llm-core-and-route) - The `check_id` path rule is claimed in chat-api.md ("It is caller-supplied too and goes through the same rule, resolved under the run root; a path the rule refuses is answered `404 UnknownCheck`") but is only tested with an ordinary unknown name. The POST route has the UNC/device/parent-segment para Fix: Parametrize the existing UNC/device/parent-segment cases over `GET /checks/{check_id}` and `POST /checks/{check_id}/exceptions/{finding_id}` as well, asserting 404 `UnknownCheck` (not 400), so the "refused paths read as unknown checks" rule cannot regress into a probe.
- **reviewer/tests/unit/test_rms_run.py:315** (no-llm-core-and-route) - `assert run.findings == [] or run.findings` is a tautology — true for every possible list. It is the only assertion about findings in test_no_api_key_is_read, and the test's other assertion (`assert run.session.model`) does not test key reading either. The test deletes three env vars and then assert Fix: Assert what the name claims. Monkeypatch the key lookup in swreview.agent.settings (or os.environ access for key-shaped names) to raise and assert the run still completes, mirroring how TestNoLanguageModel patches providers.get. Drop the tautology.
- **reviewer/tests/unit/test_rms_run.py:283** (no-llm-core-and-route) - test_a_scope_this_build_does_not_run_is_reported asserts `run.unavailable_scopes == []` — it pins that nothing is ever reported, the opposite of its name. The underlying code is unreachable: RMS_SCOPES_BUILT == frozenset(RMS_SCOPE_TOOLS) == {part, assembly, equations} and every RMS_SCOPE_RUNS value  Fix: Delete RMS_SCOPE_NOT_BUILT, RMS_SCOPES_BUILT's filtering role in run_rms_check, the `continue` branch and the unavailable_scopes field (plus the CLI's `scope {item['scope']}: {item['reason']}` lines), then delete this test. If instead the route stops offering assembly (must-fix #2), reuse this machi
- **reviewer/tests/unit/test_rms_run.py:187** (no-llm-core-and-route) - The D1 regression test asserts only `set(recorded.tool_result_ids) <= steps`. With three recorded steps, a regression in which every finding cited step 0 — essentially the off-by-one D1 describes — would still pass. I verified the implementation is correct today (part findings cite 0, equation findi Fix: Assert the attribution, not just membership: build `{step.index: step.tool for step in session.steps}` and assert every finding whose check starts with `rms.assembly.` cites the step whose tool is check_rms_assembly, and likewise for part and equations. That is the assertion that would have failed b
- **reviewer/src/swreview/chat/server.py:732** (no-llm-core-and-route) - `"acceptable": rule is not None and rule.severity == "fail"` is a second expression of "which rules may be waived". The authoritative one is _rms_waiver_invalidity in cli.py, which _accept_exception imports and uses in this same file a few hundred lines down. They agree today (both reduce to severit Fix: Call the one predicate: `"acceptable": _rms_waiver_invalidity(str(finding["check"])) is None`, importing it the same lazy way _accept_exception already does. Then add a test that the route's `acceptable` flag and a real POST to the accept route agree for every rule severity in the catalogue, so the 
- **reviewer/tests/golden/fixtures/rms-part/session.json:1** (no-llm-core-and-route) - reviewer/tests/golden/fixtures/rms-part/report.md and session.json are untracked artefacts left inside the committed golden fixture directory — written at 06:42:57 by a manual run_rms_check against the fixture (the session's own started_at matches the file mtime exactly). I confirmed the test suite  Fix: Delete both files. If hand-checking the T071 diff needs a real run, copy the fixture to a temp directory first — which is what tests/unit/test_cli_rms_types.py already does ("copied so a test can never edit the fixture"). Optionally add session.json/report.md under tests/golden/fixtures/ to .gitigno
- **reviewer/src/swreview/chat/server.py:1091** (no-llm-core-and-route) - `document_id=document_id or None` silently turns an empty-string document_id into None, i.e. "grade every part document". The validation two lines above only rejects a non-string, so `{"document_id": ""}` — a plausible thing for a page to send when no feature is selected — quietly changes the scope  Fix: Treat a blank string as the error it is: `if document_id is not None and (not isinstance(document_id, str) or not document_id.strip())` -> raise ChatError naming that document_id is one part document id or null. Add the case to TestRunCheckRefusals.
- **reviewer/src/swreview/checks/rms/run.py:319** (no-llm-core-and-route) - `coverage_rows` is a third copy of "flatten session.coverage into rows with a bucket key": it duplicates `_coverage` in reviewer/src/swreview/checks/golden_rms.py, which hardcodes the bucket tuple that run.py derives from get_args(CoverageBucket). cli.py's `_coverage_rows` was correctly deleted in t Fix: Keep run.py's `coverage_rows` as the single public helper (it derives the bucket order from the Literal, so it cannot drift from the model) and have golden_rms._coverage delegate to it, deleting its hardcoded BUCKETS tuple. Low risk: the two orders are already identical, so no golden baseline moves.
- **reviewer/tests/unit/test_rms_grade.py:341** (no-llm-core-and-route) - `assert isinstance(moved.summary(), str)` is tautological — summary() is annotated `-> str` and unconditionally returns an f-string. The preceding assertion (`"nothing evaluated" in moved.summary()`) is the one carrying the test, so this adds noise rather than coverage, and it does not test the "nev Fix: Replace it with an assertion that tests the name — e.g. that no whitespace-delimited token in the summary is a single alphabetic character, reusing the shape of test_no_rendered_value_is_a_single_letter — or drop the line and rename the test to what it actually checks.
- **reviewer/src/swreview/checks/rms/report.py:233** (no-llm-core-and-route) - The subject entry carries an eighth key, `reason`, that specs/003-resilient-modeling/contracts/model-check.md's CheckResult sample does not list ({feature_id, name, type_name, group, persist_ref, persist_ref_scope, component_ids}). The same is true of `exceptions_carried_forward.reason`, which the c Fix: Out of T070/T071's edit scope, so flag it to whoever runs T085: add `reason` to the subjects entry and to exceptions_carried_forward in model-check.md's CheckResult sample. Worth calling out explicitly because T085's job is to keep model-check.md in step with what T073 shipped, and it will need this
- **extractor/SwReview.AddIn/Model/ModelCheckPage/check.js:528** (addin-host-page-strip) - bucketOfFinding coerces anything that is not `severity === 'warn'` into 'failed'. A row whose severity the backend reported as null (chat/server.py `_finding_row` sets `severity: None` when RULES.get(check) misses) renders as a failure, and a finding whose status is 'suspected' or 'unresolved' rende Fix: Map severity explicitly - 'warn' -> warned, 'fail' -> failed - and give anything else its own visible state (an 'unknown' row saying the rule's severity was not reported) rather than falling through to 'failed'. Likewise branch on finding.status for 'unresolved'/'suspected' instead of letting them l
- **extractor/SwReview.AddIn/SwReviewAddIn.cs:467** (addin-host-page-strip) - RegisterLatestRun runs on the swreview-model-check-messages thread and calls _reviewHost.TrackCheck (ReviewHost.cs:284), which does `_sessions.Add(record)` on a plain List<SessionRecord>. That list is otherwise only touched by the review pump thread, which enumerates it in AnyTurnRunning() and FindS Fix: Marshal the registration the way TaskPaneControl.RefreshSteps already marshals itself (BeginInvoke onto the pane), or guard _sessions with a lock in TrackSession/TrackCheck/FindSession/AnyTurnRunning. Add a test that calls TrackCheck concurrently with AnyTurnRunning.
- **extractor/SwReview.AddIn.Tests/ReviewPageFiles.cs:40** (addin-host-page-strip) - Scripts() still globs only web/Review/ReviewPage, so after T080 moved el/write/clear/append/button/field/list/scalar/compact/seconds into web/shared/dom.js, the Review page's own scans (ReviewPageInjectionTests' markup-sink scan and the contract scan) no longer cover the helpers that build every nod Fix: Include web/shared/*.js in ReviewPageFiles.Scripts() (the Review page loads it from the same virtual host, so it is one of that page's scripts), or add a shared-files scan both page test classes call, so dom.js is held to the rule by whichever page is present.
- **extractor/SwReview.AddIn/Review/ReviewPage/render.js:38** (addin-host-page-strip) - `var dom = window.SwReviewDom;` followed by `var el = dom.el;` throws a TypeError at script-parse time if shared/dom.js did not load (a Content item that stops copying, a rename under web/shared, a mapping change). render.js is loaded before app.js, so the whole Review page dies with a blank tab and Fix: Guard the binding once and fail loudly: if window.SwReviewDom is missing, write a plain-text line into the page saying shared/dom.js did not load. The same guard belongs at the top of check.js.
- **extractor/SwReview.AddIn/Model/ModelCheckPage/check.js:292** (addin-host-page-strip) - `replaceFinding(findingId, answer && answer.finding)` silently does nothing when the route returns `finding: null`. chat/server.py's accept_check_exception builds that field with `next((item for item in result['findings'] if ...), None)`, so null is reachable. The page then re-renders unchanged cont Fix: Treat a null `finding` as an outcome, not a no-op: keep the accept-state span and say the exception was written (naming answer.exception_id) but the check could not be re-rendered, then refresh from GET /checks/{check_id}. Add a page test for the null-finding response.
- **extractor/SwReview.AddIn/Model/ModelCheckPage/check.js:253** (addin-host-page-strip) - loadLatestCheck's catch swallows every failure of GET /checks/{check_id}, not just the 404 the comment justifies. BackendUnavailable, a 401, an UnknownCheck from an unreadable session.json, or a 500 all end as setCheckState('') - the tab looks empty and the engineer cannot tell "nothing to show" fro Fix: Branch on error.errorClass: stay silent only for UnknownCheck/404, and show the banner for everything else.
- **extractor/SwReview.AddIn.Tests/ModelCheckPageInjectionTests.cs:76** (addin-host-page-strip) - T081 asks for a CSP meta tag "byte-identical to the one in pane-host-messages.md", but the assertion compares Normalize(contract) with Normalize(page), which collapses inter-directive whitespace and drops empty directives. The page and the contract could differ in spacing or a trailing `;` and still Fix: Assert string equality on the raw captured attribute values and keep Normalize only for the failure message. The two strings are already identical today, so this costs nothing.
- **extractor/SwReview.AddIn/TaskPaneControl.cs:1162** (addin-host-page-strip) - ActionsPanel.SuggestOutputDirectory's contract was changed - a null/blank folder now clears the box instead of leaving the last suggestion standing. T084 asks only for the strip's notice and the Extract full evidence action; this changes pre-existing Extract tab behaviour for every caller, including Fix: Confirm with the owner that the Extract tab's suggestion semantics were meant to change. If the only requirement is "never suggest a check folder", keep the old early-return for blank and have RefreshSteps pass the previous non-check suggestion instead; and if the step-strip paragraph is to stay in 
- **extractor/SwReview.AddIn.Tests/ReviewHostTests.cs:1** (addin-host-page-strip) - The file gained a UTF-8 BOM (`-using System;` / `+<BOM>using System;`), which is an encoding change to the one file whose untouched state is T074/T075's stated regression proof. Harmless to the compiler, but it makes "this file was not edited" harder to assert at a glance and adds churn to a file tw Fix: Restore the original encoding so the only diff in ReviewHostTests.cs is T076's added tests and the IReviewDump fake's new profile parameter.
- **specs/002-task-pane-assistant/contracts/pane-host-messages.md:133** (addin-host-page-strip) - `backend.stopped` is a row in the tab-4 unsolicited table and check.js:129-133 handles it, but nothing in the add-in ever posts it to `ModelCheckChannel` (grep for `backend.stopped` over extractor/SwReview.AddIn finds only the two page handlers and a doc comment on ModelCheckHost.cs:218). It is a do Fix: Either post it - the supervisor already knows when the child exits, and the page's recovery from a dead backend is the same problem as the must-fix above - or drop the row and the handler. If you post it, consider extending the contract scan with the mirror rule: every host-to-page row is sent by so
- **extractor/SwReview.AddIn/Review/SwEntityResolver.cs:242** (addin-host-page-strip) - SelectComponent returns null when `SelectByID2("COMPONENT")` fails, and FeatureSelection.Plan cannot tell that from "the page named no component": it falls to FeatureSelectionKind.None and tells the engineer "...the finding names no component instance to select it through", which is false - the find Fix: Extract the component-selection block that `Select` and `SelectComponent` share into one private helper that returns the selected IComponent2 (or null) and keeps the Select4 fallback, and have both call it. Then give FeatureSelectionRequest a way to distinguish "no component id was given" from "the 
- **extractor/SwReview.AddIn.Tests/ModelCheckPageTests.cs:285** (addin-host-page-strip) - AFailRuleCarriesTheAcceptControlWithARequiredNoteAndAWarnRuleCarriesNone does not test what its name claims. The page's acceptRow() is gated on `row.acceptable && !row.exception` (check.js) and never reads severity for that decision; the test passes only because CheckResultSample sets `acceptable =  Fix: Add a sample row with `severity: "warn"` and `acceptable: true` and assert the page still renders no Accept control, with the page deriving it as `row.acceptable && row.severity !== 'warn'`. If you would rather keep one decision point in the rule layer (defensible under DRY), rename the test to say 
- **extractor/SwReview.AddIn/Model/ModelCheckHost.cs:305** (addin-host-page-strip) - StartCheck ignores the `scope` field the page sends and the contract documents (`check.start | {scope: "part"}`). A page that posted `{scope: "assembly"}` silently gets a part check with no refusal. The class's own doctrine says the page is the least trusted thing in the process, and every other pay Fix: Either read `scope` and refuse anything but `part` with a `ScopeNotAvailable` error (matching the backend's own class for the same condition in chat/server.py), with a ModelCheckHostTests case, or drop `scope` from the payload column in both contracts/model-check.md and pane-host-messages.md so the 
- **extractor/SwReview.AddIn.Tests/PaneActionsTests.cs:24** (addin-host-page-strip) - The class doc states the T074 regression proof as "`ReviewHostTests.cs` needs no edits" and "it is not touched by the extraction", and line 373 repeats "needs no edits for this feature". The second half is now false: ReviewHostTests.cs carries +150 lines in this tree, from T064's LastProfile asserti Fix: Narrow both sentences to the true claim: ReviewHostTests' assertions about the four shared rows are unchanged by the extraction (T064 and T076 add tests elsewhere in the file). Consider making the claim mechanical rather than documentary, e.g. a test that asserts the four shared-row test methods sti
- **extractor/SwReview.AddIn.Tests/ModelCheckPageInjectionTests.cs:78** (addin-host-page-strip) - The test named IndexHtmlCarriesTheContractCspMetaTag asserts equality only after Normalize(), which splits on `;`, trims each directive and collapses internal whitespace. T081 asks for a CSP meta tag "byte-identical to the one in pane-host-messages.md". The tag is in fact byte-identical today, so th Fix: Assert the raw strings match first (`Assert.Equal(ContractCsp(), meta.Groups[1].Value)`) and keep the normalized comparison only as the failure message's explanation, or drop "byte-identical" from the comment and say "semantically identical".
- **extractor/SwReview.AddIn.Tests/ReviewPageFiles.cs:44** (addin-host-page-strip) - ReviewPageFiles.Scripts() enumerates only `web/Review/ReviewPage`, so after the T080 extraction the Review page's own markup-sink and injection scans no longer cover `web/shared/dom.js` - the file that is now the single enforcement point for the Review page's "insert every untrusted string with crea Fix: Union SharedFolder into ReviewPageFiles.Scripts() the way ModelCheckPageFiles.Scripts() does. This edits a T080-protected file, so verify the Review contract and injection tests stay green afterwards and record it as a deliberate follow-up rather than folding it into T080. The cheaper alternative - 
- **specs/001-agentic-design-review/contracts/cli.md:42** (polish) - The `check rms` row contradicts itself about the refusal path: it states earlier in the same cell that an empty `features` array is refused and that an unparseable carry-forward candidate "refuses the run", then closes with "this exits 1 only when the package cannot be read or an argument names some Fix: Close the row with the full set, matching the docstring: "...exits 1 only when the package cannot be read, its feature array is empty, a carried-forward exception store cannot be parsed, or an argument names something the package does not carry."
- **specs/001-agentic-design-review/contracts/cli.md:39** (polish) - The `exceptions accept-rms` row lists five status spellings (`accepted <n>`, `would accept <n>`, `unused`, `invalid (warn rule)`, `invalid (unknown rule)`) and then says `--json` "carries the same four values in `status`". `cli.py::_rms_waiver_status` returns five distinct spellings, and the JSON `r Fix: Change "the same four values" to "the same five values" (or drop the count: "carries the same spellings in `status`").
- **reviewer/src/swreview/checks/rms/report.py:257** (polish) - The group census is computed twice per check run. `tools/rms_checks.py:124` already builds `assign_groups(rows, table)` for every part document before evaluating, and `report.py::_groups_by_document` re-filters `package.features` per document and re-runs `assign_groups` to answer the same question f Fix: Thread the `GroupAssignment` the dispatcher already built into `report_results` (one extra parameter on the runner seam in `tools/rms_checks.py::_reported`), or cache the assignment per (document_id, package identity), so group membership is derived exactly once per check.
- **reviewer/src/swreview/checks/rms/part.py:212** (polish) - The DRY move left two spellings of the one shared helper: `assembly.py` now calls `verdict(rule, tree.document_id, ...)` directly at its three sites, while `part.py` keeps a private `_verdict(rule, tree, ...)` wrapper whose whole body is a re-spelling of the same call, used at ~20 sites. A reader of Fix: Pick one spelling: either delete the wrapper and call `verdict(rule, tree.document_id, ...)` in part.py (mechanical, covered by the existing rule tests), or keep a tree-taking wrapper and give assembly.py the matching one. Dropping the wrapper is the more explicit of the two.
- **reviewer/tests/perf/test_model_check_perf.py:163** (polish) - The two perf modules repeat the whole timing harness with the same intent: `pytestmark`, `BUDGET_SECONDS = 2.0` (73 vs test_rms_perf.py:54), `VIOLATION_EVERY`, the `started = time.perf_counter()` / `print(...)` / `assert elapsed < BUDGET_SECONDS` block with near-identical message text, and the seede Fix: Put the harness in one place under `reviewer/tests/perf/` (e.g. `timed(label)` context manager that prints the measured seconds and asserts the shared `BUDGET_SECONDS`), and leave each module only its workload builder and its workload assertions.
- **reviewer/src/swreview/checks/rms/results.py:98** (polish) - `subject_rows` carries no invariant tying it to `subjects`, although the report layer now depends on the two being the same rows in the same order (report.py `_source_refs` and `_subjects` build the Show payload and the structured entries from `subject_rows`, while the finding's own `inputs` come fr Fix: Add one line to `__post_init__`: `if self.subject_rows and len(self.subject_rows) != len(self.subjects): raise ValueError(...)` (and, since both are built together in this module, assert the ids line up).
- **specs/003-resilient-modeling/contracts/model-check.md:72** (polish) - Contract/implementation drift on the subjects shape that landed alongside this work: `report.py::_subjects` emits `reason` (the `NO_PERSIST_REF` sentence) and nullable `group`/`persist_ref`/`persist_ref_scope`, none of which the contract's `subjects` example or prose mentions. A page written to the  Fix: Add the `reason` field and the null cases to the `subjects` entry in contracts/model-check.md, saying explicitly that a null `persist_ref` always comes with a `reason` and that `group` is null for a subject that is not a feature of a graded part.
- **specs/003-resilient-modeling/quickstart.md:138** (polish) - T059's mandate is Scenarios 1 and 2, but the edit also rewrote Scenario 5's command paths and added a new BOM warning. The path rewrite is a necessary consequence of the shared `$runs` setup (leaving S5 pointing at the fixture in place would contradict the new preamble), but the BOM note is an unreq Fix: Keep the path rewrite; confirm with the task owner that the Scenario 5 BOM sentence is wanted here rather than in the T047 scenario's own follow-up, and say so in the T059 report as a deviation.
- **README.md:88** (polish) - The Model check tab paragraph is written in the shipped present tense ("dumps the active document with `--profile model-check` ... shows the grade, the findings and an Accept control") while Phase 9 is mid-flight: T063-T089 are unchecked in tasks.md and the workstation validations (T088, T089) have  Fix: Either leave it and make sure T088/T089 gate the branch, or qualify the tab paragraph until Phase 9 closes (one clause: "shipping with feature 003 Phase 9"). T058's own mandate - README "with the family" - is satisfied either way.
- **specs/001-agentic-design-review/contracts/cli.md:42** (polish) - The `check rms` row closes with "this exits 1 only when the package cannot be read or an argument names something the package does not carry", contradicting two refusals the same row states earlier: an empty `features` array is refused, and a carried-forward exception store that cannot be parsed ref Fix: Extend the closing sentence to the four conditions cli.py names: package unreadable, empty features array, an argument naming something the package does not carry, and an unparseable carried-forward exception store.
- **reviewer/src/swreview/checks/rms/results.py:98** (polish) - `subject_rows` is added to RuleResult with a docstring making a load-bearing claim -- "in the same order" as `subjects` -- that report.py `_source_refs` (line 202) and `_subjects` (line 224) both depend on to pair a SourceRef with its subject id. No test asserts it: test_rms_results.py checks only ` Fix: In test_rms_results.py, assert `[row.id for row in result.subject_rows] == result.subjects` in the passing and violation cases (and once through `unresolved`), so the pairing is pinned where the field is defined rather than only where it is consumed.
- **reviewer/src/swreview/checks/rms/part.py:212** (polish) - The DRY move left two shapes for one helper. part.py keeps a three-line `_verdict` pass-through around `results.verdict` across 15 call sites, differing only in taking `tree` rather than `tree.document_id`, while assembly.py deleted its wrapper and calls `verdict(rule, tree.document_id, ...)` direct Fix: Pick one spelling: either drop part.py's `_verdict` and pass `tree.document_id` at its 15 sites, or keep the tree-taking wrapper and give assembly.py the same one. Dropping it is the explicit-over-clever choice and removes an indirection.
- **reviewer/tests/conftest.py:57** (polish) - `"perf" not in config.getoption("markexpr")` is substring matching on the marker expression. `-m "not perf"` collects the whole perf directory and then deselects it (harmless today but the opposite of the intent), and any future marker whose name contains the substring -- `perf_slow`, `imperfect` -- Fix: Match the marker as a word: `re.search(r"\\bperf\\b", config.getoption("markexpr"))`.
- **reviewer/tests/perf/test_rms_perf.py:56** (polish) - The 2.0 s budget is about 16x the measured time on this machine (0.120 s), so the assertion cannot catch anything short of a catastrophic regression; the printed line is doing all the real work. The 2 s figure comes from the task text so it is spec-compliant, but a reader has no way to tell whether  Fix: Record the observed figure in the module docstring (e.g. "0.12 s on the development workstation, 2026-09") so the headroom is visible and a future drift toward the budget is readable without re-deriving it.
- **README.md:88** (polish) - The new 'Model check tab' paragraph documents Phase 9 as shipped ('dumps the active document with --profile model-check ... runs the same run_rms_check ... shows the grade, the findings and an Accept control'). Phase 9 is in flight in this same working tree (run.py, grade.py, extractor/SwReview.AddI Fix: Either move that paragraph to the task that lands the tab, or mark it as the shipping target rather than present behavior until Phase 9 is green.


## Feature 004 offline part 1 (2026-09-16)

- **reviewer/src/swreview/cli.py:1052** (planner) - `_remodel_refusals` builds the folder-refusal sentence from member *counts*, so when the counts happen to be equal it reads as a contradiction. On the shipped golden fixture it prints "the folder 3-Core (feat:0002) holds 3 feature(s) where the method puts 3" while the actual members are feat:0003, f Fix: Name the set difference: the unexpected members (`actual - expected`) and the missing members (`expected - actual`), by id and name, instead of the two counts.
- **reviewer/src/swreview/remodel/intent.py:296** (planner) - `GlobalCandidate.value_m` is set to `radius.value` raw, but `Quantity.unit` is `LengthUnit = Literal["mm", "in", "m"]`, not always metres. `rank.py` converts the same field properly through `units.as_mm`. A package whose `default_radius` carried "mm" would produce a `value_m` wrong by a factor of 10 Fix: Convert through `swreview.units` (`to_length(radius).to("m")`) as `rank.py` does, or refuse a non-metre quantity by name rather than reading its magnitude.
- **reviewer/src/swreview/remodel/scope.py:436** (planner) - `_configurations` tests `if not configuration_names`, collapsing `None` (IConfigurationManager could not be read) and `()` (read and empty), and then emits the note "the configuration names could not be read" for both. Everywhere else this feature treats the distinction as load-bearing and says so i Fix: Split the two: `None` keeps the current "could not be read" note; `()` gets its own note ("the configuration listing was read and is empty, which no part is, so which_configs is unknown"). Split the parametrised test accordingly.
- **reviewer/src/swreview/remodel/plan.py:708** (planner) - `_CORE_ROLE = 2` re-spells `swreview.checks.rms.part.CORE`, which `rank.py` already imports by name from the same module for exactly the same purpose. Two names for one constant is how the planner and the checker start to disagree, and the binding constraint for this feature is aggressive DRY / cons Fix: `from swreview.checks.rms.part import CORE` and delete `_CORE_ROLE`.
- **reviewer/tests/golden/fixtures/remodel-plan/remodel-unplaceable/case.json:1** (planner) - T030 asks for the four golden plans "plus one fixture per refusal category of T016". Seven of the eight FR-014 rebuild reasons appear across the remodel-plan baselines (backward_reference, cycle, graph_unreadable, radius_unreadable, shared_sketch, splits_group, unclassified); `ambiguous_name` appear Fix: Add a fixture whose duplicate name cannot be repaired (both rows unrenameable - e.g. two folder rows sharing a group name, or a name an equation references) so a baseline pins an `ambiguous_name` rebuild entry with its detail sentence.
- **reviewer/src/swreview/remodel/scope.py:336** (planner) - `ScopeGate.evaluate` calls `load_table()` itself, while `target_group`, `rank_features`, `plan_renames`, `plan_folders`, `description_gaps` and `global_candidates` all take the `RmsTypeTable` as an explicit argument and `plan_reorganize` threads one through. The gate is the only planner entry point  Fix: Add a `table: RmsTypeTable | None = None` parameter to `evaluate` (and thread it from `scope_report` / `plan_reorganize`), defaulting to `load_table()` only when the caller passes none.
- **reviewer/src/swreview/remodel/folders.py:118** (planner) - `FolderAction.op` admits `"rename"`, and `plan.py::_changes` carries a `folder.rename` branch plus a `raise ValueError` for a rename with no existing folder - but nothing in the module ever emits an action with `op="rename"`, so both the branch and its guard are unreachable in stage 1. That is specu Fix: Either narrow `FolderAction.op` to `Literal["create"]` for v1 and drop the dead `_changes` branch, or, if `data-model.md` section 1.6 requires the two-value name space for the bridge, add a test that constructs a rename action and asserts both the emitted `folder.rename` change and the refusal when 
- **reviewer/src/swreview/cli.py:1100** (planner) - `_remodel_lines` prints the reorganizable fraction with no marker of the part's state, so a refused part prints "5 of 5 content features reach their target group (1.000)" immediately above "0 change(s)" and its refusal. phase0-decision.md section 3.2 documents this exact reading hazard ("a refused p Fix: When `row['state'] == 'failed'`, annotate the fraction line (e.g. "... (1.000, hypothetical: this part is refused)") and/or carry a `refused` flag inside the `reorganizable_fraction` object so the JSON consumer cannot read it as work done.
- **specs/004-resilient-remodeler/phase0-decision.md:5** (planner) - T004 requires "the numbers from T002 and T003 in one table ... and an explicit written go / no-go on staged versus rebuild-only, signed by the owner". The file correctly and honestly marks itself incomplete (T003 deferred by owner decision, sections 4 and 5 PENDING, no signature), which is the right Fix: No code change. Carry T004 forward as open work: it closes only once T003 runs on the workstation over the owner's nominated parts and the owner signs section 5. Note that Phases 2 to 9 are gated on that signature, while extractor/add-in work for those phases already exists in the working tree.
- **reviewer/src/swreview/cli.py:1052** (planner) - The folder refusal message is built from member COUNTS, so when the counts happen to match it prints a contradiction. `swreview remodel plan --package tests/golden/fixtures/remodel-plan/remodel-refusal-rms-folder` prints "the folder 3-Core (feat:0002) holds 3 feature(s) where the method puts 3" whil Fix: Name the set difference: the unexpected members (actual minus expected) and the missing ones (expected minus actual), by id and name, rather than two counts.
- **reviewer/tests/unit/test_cli_remodel_plan.py:236** (planner) - Two of T028's named cases are not exercised. (a) "exit 1 with a named reason on a scope refusal" is untested and in fact unreachable from the command: `remodel_plan_command` never passes `signals`, so `scope.verdict` is always `unresolved` and `scope.refused` is never true -- the CLI's scope-refusal Fix: Add a CLI case over a cycle package (`remodel-cycle`'s builder) asserting exit 1 and `cycle` in the refusals, and either decide the scope-refusal path is unreachable from the dry run and say so in the command docstring and cli.md, or give the command a way to supply a probe's signals so T028's case 
- **reviewer/tests/golden/fixtures/remodel-plan/remodel-duplicate-names/case.json:1** (planner) - T030 asks for the four golden plans plus "one fixture per refusal category of T016". Seven of the eight rebuild reasons appear across the baselines (backward_reference, cycle, graph_unreadable, radius_unreadable, shared_sketch, splits_group, unclassified) but `ambiguous_name` appears in none -- `rem Fix: Add one fixture whose duplicate is unrepairable (two rows sharing a name where both are unrenameable -- e.g. two group-folder rows, or a name an equation references) and hand-check that its baseline carries reason `ambiguous_name`.
- **reviewer/tests/unit/test_remodel_plan_compose.py:455** (planner) - Tautological assertion: `assert all("@" not in item.item for item in plan.coverage if item.item == "dimension")` filters on a coverage item that does not exist (the real one is "sketch dimensions"), so the generator is empty and `all()` is vacuously True. It can never fail and adds nothing to the FR Fix: Either drop the line or make it the real assertion, e.g. assert the coverage item "sketch dimensions" is present and that no coverage item, deviation or global carries an "@".
- **reviewer/tests/unit/test_cli_remodel_plan.py:311** (planner) - `test_the_command_constructs_no_provider` ends with `assert "provider" not in json.loads(result.stdout)` -- the payload's top-level keys are `package` and `parts`, so this can never fail -- and the test takes an unused `monkeypatch` parameter. All the real work is done by the `no_provider` fixture,  Fix: Drop the vacuous assertion and the unused parameter; the fixture's `AssertionError` on `providers.get` / `swreview.cli.provider_factory` is the assertion. If a distinct test is wanted, assert that `swreview.remodel` imports construct no adapter, e.g. by patching before import.
- **reviewer/src/swreview/cli.py:1176** (planner) - A refused part prints work it will never do. `remodel-refusal-rms-folder` prints "1 move(s), 0 rename(s), 1 folder(s) to create, 0 change(s)" although `changes` is suppressed to `()` on any refusal. phase0-decision.md section 3.2 warns the reader about the fraction on a failed row but not about the  Fix: On a row whose `state` is `failed`, either suppress the move/rename/folder counts or label them ("would have been"), the same way the fraction is qualified, so the only number an engineer can act on is `chg`.
- **reviewer/src/swreview/remodel/order.py:99** (planner) - `OrderResult.kept` is documented as "the features that are not moved, in tree order", but `plan_order` builds it by walking `achievable` (`tuple(feature for feature in order if feature in kept)`). The two coincide today only because kept features have strictly increasing current positions; the docst Fix: Say "in achievable order, which for kept features is also tree order", or build it from `current` so the docstring is what the code does. `test_the_kept_features_are_the_longest_increasing_subsequence` already asserts the equality, so either spelling stays honest.
- **reviewer/src/swreview/remodel/geometry.py:425** (geometry) - `Tolerances.body_count` is dead. `_tier_1_deltas` passes the literal `"exact"` for `solid_body_count` instead of `tolerances.body_count`, so the field declared in both profiles (and asserted by test_remodel_tolerances.py) is never read by the gate. Changing a profile's `body_count` would change noth Fix: Pass `tolerances.body_count` at line 425. The `Literal["exact"]` type already prevents it from being widened, so behaviour is unchanged and the field stops being a decoration.
- **reviewer/src/swreview/remodel/geometry.py:427** (geometry) - `edge_count` is gated under `tolerances.face_count`. No profile field in data-model 3.2, bridge-remodel.md or T096 covers edge count, so the gate is silently inventing a rule: under `IDENTITY` it adds a gated quantity the profile table does not list, and under `EQUIVALENCE` an edge-count difference  Fix: Either add an explicit `edge_count: CountRule` field to `Tolerances` (exact/exact for IDENTITY, warn for EQUIVALENCE) so the artifact states the rule it used, or document the reuse in one sentence on `_tier_1_deltas` and assert it in the tolerances test. Prefer the explicit field - it is one line pe
- **reviewer/src/swreview/remodel/geometry.py:429** (geometry) - `mass_kg` borrows `tolerances.volume_rel` as its bound. Mass has no declared tolerance anywhere in the spec, and `_material_changed` (line 477) decides `material_changed` from `mass.within is False` - i.e. from a 1e-9 relative comparison of a quantity the mass-property engine has not been calibrated Fix: Name the constant: add `mass_rel` to `Tolerances` (or a module-level `MASS_REL` with a comment saying it tracks the volume bound because mass at constant density is volume), and include it in PROBE-8's ledger row. At minimum, say in the `_tier_1_deltas` docstring that the mass bound is borrowed and 
- **reviewer/src/swreview/remodel/geometry.py:555** (geometry) - `_largest_residual_min_dimension_m` selects the residual body with the largest bounding-box *volume*, so a wide thin sliver hides a genuinely thick small body. Verified: `per_body_bbox=((0.1, 0.1, 1e-7), (1e-4, 1e-4, 1e-4))` with a total volume under the threshold returns `pass`, even though the sec Fix: Report the maximum over all residual bodies of each body's minimum bounding-box dimension (`max(min(box) for box in boxes)`) rather than `min(largest_box)`, and keep the largest-body figure in the reason text if it is useful for the report. Add a T100 case `small_thick_residual_beside_a_sliver` asse
- **reviewer/src/swreview/remodel/geometry.py:639** (geometry) - `_combined_verdict` demotes a definite tier-1 failure to `unresolved` when tier 2 did not run: tier 1 `fail` + tier 2 `unresolved` returns `unresolved` (verified). A measured volume change outside its bound is a fact tier 2's failure to run does not erase, and the report then says "the gate did not  Fix: Return `"fail"` when `tier_1.verdict == "fail"` and tier 2 is `unresolved`, carrying tier 2's reason into the diagnosis so the reader sees the corroboration was unavailable; or, if the demotion is deliberate, say so in `_combined_verdict`'s docstring and cite the sentence it comes from. Either way a
- **reviewer/tests/unit/test_remodel_geometry.py:394** (geometry) - Two residual assertions are satisfied by the error code rather than by the value they claim to check. At line 394 `assert "1" in result.tier_2.reason` is meant to prove the body count is recorded, but the reason begins "swBodyOperationPartialCoincidence (1040)..." so the substring matches unconditio Fix: Assert the discriminating phrase, as the neighbouring tests already do: `assert "1 residual bodies" in result.tier_2.reason` and `assert "residual volume 0 m3" in empty.tier_2.reason` (or assert on `f"{value:.3g}"` in the segment after the prefix).
- **reviewer/tests/unit/test_remodel_geometry_artifact.py:236** (geometry) - `test_the_writer_opens_nothing_but_the_artifact_it_writes` monkeypatches `Path.open`, `Path.read_text` and `Path.read_bytes`, but `write_geometry_json` calls `Path.write_text`, which is not patched. It only reaches the guard because CPython 3.12's `write_text` happens to delegate to `self.open`. If  Fix: Record the calls (`opened: list[Path]`) inside `guarded_open` and assert `opened == [artifact]` after the write, so the test fails if the guard was never reached. Patching `builtins.open` as well costs one line and closes the other escape.
- **reviewer/src/swreview/remodel/geometry.py:155** (geometry) - `GeometryReading.accuracy_level` is an unconstrained `int` and no code path checks it. data-model.md 3.1 states `swMassPropertyAccuracyLevel_Higher = 2` on every reading, and T094 has the C# handler set it, but the Python gate will happily compare two readings taken at a lower accuracy level against Fix: Either type the field `Literal[2]` on `GeometryReading`, or add a blocking reason in `_blocking_reasons` when either reading's `accuracy_level != 2` (unresolved, naming the level that was used). Add the case to the T098 table.
- **reviewer/src/swreview/remodel/geometry.py:777** (geometry) - contracts/run-artifacts.md line 32 names `remodel/report.py` as the writer of `geometry.json`; the writer is implemented in `remodel/geometry.py` instead (and `remodel/report.py` does not exist yet). Nothing is broken, but the contract table and the code now disagree about where the artifact comes f Fix: Either leave `write_geometry_json` here and have `remodel/report.py` call it when that module lands (and note that in the geometry.py docstring), or move the writer to `report.py` beside the other artifact writers. State the choice in the return so the report task does not re-implement it.
- **reviewer/src/swreview/remodel/geometry.py:290** (geometry) - quickstart.md Scenario 3's `translated 0.1 mm` row says the report states "the centre-of-mass delta over the diagonal"; `_com_delta` divides by `volume_m3 ** (1/3)` instead. The choice is well reasoned and documented (the reading carries no bounding box, and `|com|` is zero for a symmetric part), bu Fix: Reconcile the two: either add a bounding-box diagonal to `GeometryReading` (a data-model 3.1 change, not worth it for stage 1) or, preferably, put the denominator in the delta so the report is self-describing - e.g. name the quantity `center_of_mass_m` with the reason string stating "distance over t
- **reviewer/tests/unit/test_remodel_geometry_artifact.py:274** (geometry) - `test_no_reference_body_can_enter_any_tree_from_this_module` is tautological in its load-bearing half: it reads geometry.py and tolerances.py as text and asserts `"InsertPart3" not in source`. No Python module would ever contain a C# interop method name, so that assertion passes against an empty fil Fix: Drop the source-text grep from the Python suite and let the C# side carry the claim - RemodelInteropManifestTests.cs / RemodelGeometryTests.cs are where `IPartDoc.InsertPart3` could actually be called and where an allowlist assertion has teeth. If you want a Python-side guard, assert it over the rec
- **reviewer/src/swreview/remodel/geometry.py:477** (geometry) - `_material_changed` reports `material_changed: True` from a mass-only delta even when both material names are readable and equal. The run then writes an artifact that says `before.material_name == after.material_name == "1060 Alloy"` next to `material_changed: true` - a positive claim about engineer Fix: Keep the mass-only rule but make it honest: when the names are readable and equal, either report `material_changed=True` *and* append a coverage line saying the material name did not change so the density moved for an unexplained reason, or route that combination to a coverage note rather than a bar
- **reviewer/src/swreview/remodel/geometry.py:555** (geometry) - `_largest_residual_min_dimension_m` picks the residual body with the largest bounding-box *volume* and then takes that one body's minimum dimension. A wide thin sliver shadows a smaller genuinely solid residual: boxes ((1.0, 1.0, 1e-9), (2e-5, 2e-5, 2e-5)) select the first (bbox volume 1e-9 vs 8e-15 Fix: Read the signal as `max(min(box) for box in boxes)` - the thickest residual, not the biggest-boxed one - which is strictly safer and still one number. That is a research.md R4.6 wording change, so I will not make it without your say-so. Alternative: keep the letter of the spec and add the shadowing 
- **reviewer/tests/unit/test_remodel_geometry.py:394** (geometry) - Two vacuous substring assertions in the tier-2 rows. `assert "1" in result.tier_2.reason` (line 394, `one_sliver_residual`) and `assert "0" in empty.tier_2.reason` (line 527, `error_code_1067_no_intersect`) are satisfied by any reason string containing "1e-09" or "1067" - they cannot fail for the re Fix: Assert the formatted signal the way the neighbouring assertions already do: `f"{residual.body_count} residual bodies" in reason` at :394, and `f"{0.0:.3g} m3" in reason` plus `"1067" in reason` at :527.
- **reviewer/tests/unit/test_remodel_geometry_artifact.py:179** (geometry) - `test_the_tolerance_block_carries_the_profile_with_its_calibration` is the only block test that does not assert a closed key set. `before`, `after`, `gate` and each `Delta` all assert `set(...) == {...}`; the `tolerances` block only spot-checks values. The consequence is that `Tolerances.name` - the Fix: Add `assert set(body["tolerances"]) == {"name", "volume_rel", "area_rel", "com_rel", "moment_rel", "face_count", "body_count", "calibrated", "calibration_ref"}` and `assert body["tolerances"]["name"] == "IDENTITY"`.
- **reviewer/tests/unit/test_remodel_geometry_artifact.py:236** (geometry) - `test_the_writer_opens_nothing_but_the_artifact_it_writes` claims in its docstring that "the whole gate is covered by this one guard", but it only monkeypatches `pathlib.Path.open`, `read_text` and `read_bytes`. A `builtins.open(str(path))`, `os.open`, or `json.load(open(...))` added to this module  Fix: Also patch `builtins.open` and `os.open` with the same path predicate, or soften the docstring to say the guard covers pathlib access only. I recommend patching both - FR-037 is the one claim in this feature where a false negative is unrecoverable, and the extra four lines cost nothing.
- **reviewer/tests/unit/test_remodel_tolerances.py:68** (geometry) - `test_a_profile_is_a_frozen_constant_and_cannot_be_edited_at_a_call_site` uses `pytest.raises(Exception)` with a `# noqa: B017`. An `AttributeError` from a renamed field, or a `TypeError` from a wrong assignment target, passes this test just as happily as the pydantic frozen-model `ValidationError`  Fix: `from pydantic import ValidationError` and use `pytest.raises(ValidationError)`, dropping the noqa.
- **specs/004-resilient-remodeler/contracts/run-artifacts.md:267** (geometry) - `geometry.json` now carries a `Delta.gates` field that the contract's own example (lines 267-269) does not show. The module docstring documents it as a deliberate addition (it is a good one - it is what lets a reader see why `within: false` on mass or a warned face count did not fail the run), but t Fix: Add `gates` to the `Delta` line of the run-artifacts.md example and to data-model.md §3.3's `Delta` row. Neither T102 nor T103 authorises a spec edit, so I need your go-ahead before making it - the alternative is to leave the drift and record it as a known deviation.
- **extractor/SwReview.Extractor/Rms/RemodelSystemToggles.cs:169** (csharp-guard-scope) - `Restore()` sets `_restored = true` before it writes anything. If the first `SetUserPreferenceToggle` throws, toggles 77 and 329 and `CommandInProgress` are never put back and every later `Restore()` -- including the host's own finally -- returns immediately, so a partial restore is permanently reco Fix: Restore every setting before giving up: attempt all four, collect failures, set `_restored = true` only after the last attempt, and throw an aggregate naming the settings that could not be put back so the run report can say which of the engineer's settings are still changed.
- **extractor/SwReview.Extractor.Tests/RemodelSystemTogglesTests.cs:99** (csharp-guard-scope) - `ToggleHostFake.SetCommandInProgress` cannot be made to throw, so `Suppress()`'s most important rollback branch is untested: the three dialog toggles have landed and the fourth set fails. `ASetThatFailsHalfwayRestoresTheOnesThatAlreadyLanded` only covers a failing `SetUserPreferenceToggle`. Given th Fix: Add a `CommandInProgressUnsettable` flag to the fake and a test asserting all three toggles are back at their originals and `CommandInProgress` is unchanged when the fourth set throws.
- **extractor/SwReview.Extractor/Rms/RemodelInteropSurface.cs:220** (csharp-guard-scope) - `RemodelInteropSurface.Calls` is consumed only by the tests -- no product call site reads it (grep shows only RemodelInteropManifestTests and RemodelGeometryTests). `contracts/interop-manifest.md` Test A rule 1 is "every member a builder calls has a manifest row", but since the "builders" are this p Fix: Make the table the single source: expose `RemodelInteropSurface.Key("IModelDoc2", "Save3")` (or public consts derived from the rows) and have every remodel gate call site use it, so a call with no row does not compile. Failing that, add a test that reflects over the `private const string *Key` field
- **extractor/SwReview.Extractor/Rms/RemodelScope.cs:145** (csharp-guard-scope) - The constructor accepts any `SwGate`. Layer 2 is documented as pairing with a `RemodelGuard` ("`gate` is a `SwGate` built with a `RemodelGuard`"), but nothing checks it, so a scope built with a review gate would happily run every write through `ReadOnlyGuard` bare-name rules -- where `FeatureFillet3 Fix: Either take the `RemodelGuard` and build the `SwGate` inside `RemodelScope`, or expose `SwGate.Guard` and assert it is a `RemodelGuard` in the constructor, with a test that a scope built over a non-remodel gate is refused.
- **extractor/SwReview.Extractor/Guard/RemodelGuard.cs:176** (csharp-guard-scope) - A malformed key such as `"IModelDoc2."` is not on the allowlist, has a `BareName` of `""` (so it misses `ExcludedMemberSet`), and `IsQualified` returns false for it -- so it falls through to `ReadOnlyGuard.Assert("IModelDoc2.")`, which denies nothing, and the guard passes it. `RemodelGuardTests` cov Fix: Refuse a key that contains the separator but is not well-formed (`key.IndexOf('.') >= 0 && !CallKey.IsQualified(key)` throws), and add the `"IModelDoc2."`, `".Save3"` and `"."` rows to `Assert_ExcludedMember_IsRefused`.
- **extractor/SwReview.Extractor/Bridge/BridgeDispatcher.cs:1410** (csharp-guard-scope) - Both production call sites pass the same value as `path` and `copyPath` (`AssertSaveTarget(scope.CopyPath, scope.CopyPath, ...)` here, and `AssertSaveTarget(copy, copy, ...)` at line 576). The final exact-match branch -- the one that refuses another case or an 8.3 short name -- therefore compares a  Fix: Pass the value that actually came from outside as `path` -- at line 576 the raw `parameters.CopyPath` before `Path.GetFullPath`, so the `..` and case checks see what the client sent -- and keep the canonicalized run-created copy as `copyPath`. At line 1410 the self-comparison is harmless once #1 is 
- **specs/004-resilient-remodeler/contracts/guard-allowlist.md:46** (csharp-guard-scope) - The contract says `RemodelGuard.Assert` "returns if the qualified key is on the allowlist below; else it delegates to `ReadOnlyGuard.Assert(BareName(qualifiedKey))`", and T042 repeats it. The implementation instead refuses every unlisted interface-qualified key outright (rule 3) and delegates only b Fix: Owner decision: amend the "Keys are interface-qualified" paragraph to state the three-answer rule the code implements (allowlisted -> pass; excluded member -> refuse; any other qualified key -> refuse; bare name -> `ReadOnlyGuard`). I did not edit it -- no task in this range authorizes changing that
- **extractor/SwReview.Extractor/Bridge/BridgeDispatcher.cs:685** (csharp-guard-scope) - The open-rollback `seat.CloseDocument(copy)` bypasses `_remodelGate` entirely, unlike the `Close()` path which goes through `session.Scope.Write(CloseKey, ...)` at line 1502. No scope exists yet, so `VerifyTarget` cannot run, but the gate and its observer can -- and as it stands the one close that h Fix: Route it through `_remodelGate.Call(CloseKey, () => seat.CloseDocument(copy))`, which is already how the tag write at line 609 handles the same before-a-scope-exists situation.
- **extractor/SwReview.Extractor/Rms/RemodelGeometry.cs:222** (csharp-guard-scope) - `properties.SetAccuracyLevel(...)` and `properties.SetSelectedItems(...)` are gated with bare member names, so they take `RemodelGuard`'s delegation branch to `ReadOnlyGuard`. They are property-sets on a transient `IMassProperty2` rather than document writes, and the manifest records them honestly a Fix: Leave the behaviour, but state the classification where a reader will look: a one-line comment at the call site (and a row in the manifest's note field) saying these are measurement-setup property-sets on a transient object, not document writes, which is why they are bare-named and not allowlisted.
- **extractor/SwReview.Extractor/Rms/RemodelInteropSurface.cs:120** (csharp-guard-scope) - IModelDocExtension.SelectByID2 is allowlisted ('selection where Select2 is not enough') but has no call path either: IRemodelDocument declares no SelectByID member, and the only SelectByID2 call sites in the tree are the add-in's SwEntityResolver (lines 202, 254, 283), which use the bare name on the Fix: Confirm with the owner whether folder-wrap selection can ever need SelectByID2 in stage 1. If not, drop the key from RemodelGuard.AllowedKeySet, from RemodelGuardTests.Stage1Allowlist and from the manifest's allowlisted rows (keeping the row with allowlisted: false, as EditDelete is kept), and note 
- **extractor/SwReview.Extractor.Tests/RemodelInteropManifestTests.cs:76** (csharp-guard-scope) - EveryBuilderHasARow is the check cited for 'an allowlist entry without a call path is the accidental widening the allowlist exists to prevent', but it compares Loaded.Members against RemodelInteropSurface.Calls - a hand-written declarative table in the product, not anything derived from call sites.  Fix: Bind the table to reality: give each RemodelInteropCall the seam member that reaches it (e.g. a nameof(IRemodelDocument.SelectFeature)-style reference) and assert by reflection that every allowlisted row names a member that exists on IRemodelDocument/IRemodelSeat/IEquationTarget; or assert that ever
- **extractor/SwReview.Extractor/Rms/RemodelCopy.cs:288** (csharp-guard-scope) - The IOException -> StreamCopy fallback branch never returns RemodelCopyRoute.SharedReadStream in any test. RemodelCopyTests exercises the fallback only by calling CopyThroughSharedStream directly, and ASourceHeldOpenForWritingIsStillCopiedByteIdentically deliberately does not assert which route ran. Fix: Add a narrow seam: an internal overload CreateCopy(source, copy, Action primaryCopy) defaulting to File.Copy, so a test can inject a throwing IOException and assert the result is RemodelCopyRoute.SharedReadStream, that the bytes match, and that a destination appearing mid-flight still yields copy_ex
- **extractor/SwReview.Extractor/Bridge/BridgeDispatcher.cs:808** (csharp-guard-scope) - contracts/guard-allowlist.md's 'Option composition, asserted as exact integers' table says each row is one unit test asserting the integer the code composes, from one source rather than two. EveryComposedOptionEqualsTheManifestsEnumValue covers only OpenOptions, SaveOptions, the two custom-property  Fix: Extend EveryComposedOptionEqualsTheManifestsEnumValue to read MoveBefore/MoveAfter/ContainingFolder against Loaded.Enum("swMoveLocation_e", ...) and Loaded.Enum("swFeatureTreeFolderType_e", ...), exposing those dispatcher constants as internal or moving them beside the other option constants. Give E
- **extractor/SwReview.Extractor/Rms/RemodelScope.cs:186** (csharp-guard-scope) - VerifyTarget check 1 compares Canonicalize(GetPathName()) to CopyPath with OrdinalIgnoreCase, while AssertSaveTarget's final comparison is Ordinal and its test asserts a differently-cased spelling of the very same file is refused ('the comparison is exact'). The two halves of the same 'is this the c Fix: Pick one comparer for 'is this path this run's copy', state why in one line, and use it in both places. OrdinalIgnoreCase is the defensible choice on Windows for a value SOLIDWORKS hands back; then AssertSaveTarget's short-name and case cases should be refused by containment/canonicalization rather 
- **extractor/SwReview.Extractor/Guard/RemodelGuard.cs:74** (bridge-and-client) - IModelDocExtension.SelectByID2 is on the allowlist ('selection where Select2 is not enough') with no call site anywhere in the product - CreateFolder uses IFeature.Select2 exclusively. By the guard's own doc comment and guard-allowlist.md's 'Every member below is ... Used for' framing, an entry with Fix: Remove it from AllowedKeys, RemodelGuardTests.Stage1Allowlist (dropping the count from 20 to 19) and guard-allowlist.md's table, and let stage 2 add it back with a call site. Consider adding a test that every key in AllowedKeys is named by a const in product code, so this class of drift fails the bu
- **extractor/SwReview.Extractor/Bridge/BridgeDispatcher.cs:875** (bridge-and-client) - Reorder computes previousIndex = IndexOf(before, parameters.FeaturePersistRef), which returns -1 when the resolved feature is not in the GetFeaturesInOrder walk (plausible for a feature inside a folder, depending on how the walk recurses). The previousIndex > 0 branch then falls through to else if ( Fix: Refuse with persist_ref_unresolved when previousIndex < 0: a feature the tree walk cannot place is a feature whose inverse cannot be derived, and unknown must stay unknown rather than becoming a fabricated anchor. Add a test case.
- **extractor/SwReview.Extractor/Bridge/BridgeDispatcher.cs:1511** (bridge-and-client) - contracts/bridge-remodel.md (remodel.close): 'The system toggles are restored in the finally that wraps the run, including on recovery from a previous run that died.' RemodelSystemToggles.Within - the helper written for exactly this, whose doc says 'the finally is written once, here, so no caller ca Fix: Restore the toggles in a finally around the whole run - at minimum move session.Toggles.Restore() into a finally in Close(), and restore from the host's session teardown as well (Restore is already idempotent). Add a test that a throwing CloseDocument still restores.
- **extractor/SwReview.Extractor.Tests/RemodelFinishHandlerTests.cs:207** (bridge-and-client) - Close_RestoresTheSystemTogglesItSet asserts new[] { "10=False", "77=False", "329=False", "CommandInProgress=False" }. FakeRemodelSeat._toggles starts empty, so GetUserPreferenceToggle returns false for all three (RemodelFakes.cs:863-866) - the three originals equal the suppressed values, and the ass Fix: Seed the fake with non-default originals (e.g. toggle 10 and 77 true) before Open(), so the restore line reads 10=True, 77=True, 329=False, CommandInProgress=False and a restore that writes the suppressed values fails.
- **extractor/SwReview.Extractor/Bridge/BridgeDispatcher.cs:715** (bridge-and-client) - T063 says each of the five mutating commands 'resolves its persist ref through IModelDocExtension.GetObjectByPersistReference3 (VERIFIED, ByRef error code) and reads that error code on every resolve'. CreateFolder does not: it addresses its members by string-matching persist refs produced by RefsInO Fix: Either resolve each member ref through session.Resolve before selecting it (keeping the walk only for the contiguity check), or write the deviation and its justification into contracts/bridge-remodel.md's remodel.folder section under T073 so the reason the folder path differs is on the page rather t
- **extractor/SwReview.Extractor/Rms/RemodelEquations.cs:76** (bridge-and-client) - Two smaller issues in the add path plus a DRY problem below it. (1) int at = index < 0 || index > before ? before : index; silently clamps an out-of-range explicit insertion index to before; the contract gives null the 'append' meaning, so an explicit index past the end is a caller bug that should r Fix: Refuse an explicit index > before with the reconciled bad-request token instead of clamping; attach count_before/count_after to the equation_unverified detail so the executor can invert the right number of rows; replace the raw token literals in RemodelCopy.cs with the RemodelErrorCodes constants.
- **extractor/SwReview.Extractor/Bridge/BridgeProtocol.cs:486** (bridge-and-client) - RemodelErrorCodes.BadRequest ("bad_request") is a 24th token the host puts on the wire — every RequiredString/RequiredInt/RequiredChoice failure, plus RequiredEquationText, RequiredEquationIndex and RequiredConfigurations, raise it — but it is in neither contracts/bridge-remodel.md's error table nor Fix: Add a bad_request row to the contract table (mapping to RemodelContractError — a malformed request is a caller bug, not a condition) and to ERROR_CLASSES, and add one C# test asserting RemodelErrorCodes.All is exactly the contract's token list, so the two tables cannot drift apart silently.
- **extractor/SwReview.Extractor/Rms/RemodelCopy.cs:232** (bridge-and-client) - copy_exists, copy_failed and tag_failed are raised as bare string literals (lines 232, 273, 295, 302, 311, 376 among others) rather than through the RemodelErrorCodes constants that exist in the same assembly and are used everywhere else in the family. A typo in one of those literals ships a token n Fix: Replace each literal with the corresponding RemodelErrorCodes constant. (RemodelCopy is T042/T043's file, so this is a small cross-task edit, but the token agreement is the property under review here.)
- **extractor/SwReview.Extractor/Rms/RemodelSystemToggles.cs:190** (bridge-and-client) - Apply() and Restore() call _host.SetUserPreferenceToggle(...) and _host.SetCommandInProgress(...) directly on IRemodelToggleHost, never through SwGate. So the two allowlist keys RemodelGuard names for them — ISldWorks.SetUserPreferenceToggle and ISldWorks.set_CommandInProgress — are never exercised  Fix: Pass the SwGate into RemodelSystemToggles.Apply/Restore and wrap each write as gate.Call("ISldWorks.SetUserPreferenceToggle", ...) / gate.Call("ISldWorks.set_CommandInProgress", ...), then assert in RemodelOpenHandlerTests.Open_SetsTheThreeTogglesAndTheCommandFlag that the observer saw both keys. Th
- **extractor/SwReview.Extractor/Bridge/BridgeDispatcher.cs:1380** (bridge-and-client) - The geometry subject advances on the first remodel.geometry call (session.BaselineGeometryTaken), not on the first change. contracts/bridge-remodel.md says copy_at_open is "the reading taken immediately after remodel.open returns and before the first change". A caller that applies a reorder and only Fix: Set a HasWritten flag on RemodelSession the first time RemodelScope.Write runs, and stamp copy_at_open only when nothing has been written yet (refusing or stamping copy_at_end otherwise). Add a test that applies a rename and then takes the first reading, asserting it is not copy_at_open.
- **extractor/SwReview.Extractor/Bridge/BridgeDispatcher.cs:1197** (bridge-and-client) - DeleteEquation deletes whatever index it is handed. The contract says delete "is only ever the inverse of an `add` this run made. It never deletes a global the part already had, and never one that any equation still references." That property is enforced nowhere — not in the host, and not by any tes Fix: Have RemodelEquations.AddEquationVerified record the index it added on the session, and have DeleteEquation refuse an index the run did not add. Test: delete an index the run never added and assert nothing was written.
- **extractor/SwReview.Extractor/Bridge/BridgeDispatcher.cs:1322** (bridge-and-client) - Rebuild() times the ForceRebuild3 call and reports elapsed_ms, but compares it against nothing, and RemodelErrorCodes.RebuildTimeout is raised nowhere in the product. The contract's remodel.rebuild section states "A rebuild that exceeds max_rebuild_seconds is reported as rebuild_timeout", which read Fix: Either state in the contract that the executor derives rebuild_timeout from the reply's elapsed_ms against its own max_rebuild_seconds (and say so in remodel_client.py's RemodelLimitError docstring), or carry the bound on the request and have the handler raise it. Either way, one test should exercis
- **extractor/SwReview.Extractor/Rms/RemodelEquations.cs:88** (bridge-and-client) - If Add3 adds a row whose text does not round-trip (count rose by one, wrong text), Landed() is false and the helper immediately calls Add2, which adds a second row. The change then fails with equation_unverified, but two spurious rows are on the copy while the recorded inverse is a single Delete. Ad Fix: Before falling back, check whether the count already moved; if it did, fail with equation_unverified naming the orphan row rather than adding a second one (or delete the mis-added row first). Add a FakeEquationManager knob (Add3AddsWrongText) and a test asserting the equation list length after the f
- **reviewer/src/swreview/bridge/remodel_client.py:427** (bridge-and-client) - _classify passes result.get("detail"), so an exception raised from a reply that omits detail carries detail = None. The contract section added by T073 promises "result.detail travels beside it and is always an object, {} when the host has nothing to add, never null; the client carries it onto the ex Fix: Either normalise on the client (result.get("detail") or {}) and add a case for a reply whose detail is null, or default RemodelErrorResult.Detail to an empty dictionary so the host never emits null. Pin whichever is chosen with one test on each side.

## Feature 004 offline part 2 (2026-09-16)

- **reviewer/src/swreview/remodel/apply.py:1013** (executor) - `_settle` computes `ceiling = baseline + int(change.expect.get("rebuild_errors_delta", 0))`, so a plan can raise the threshold above the run's baseline. contracts/run-artifacts.md and the constitution both say flatly "a count above the run's baseline means the change is inverted" — no per-change all Fix: Either compare against `baseline` alone and keep `rebuild_errors_delta` purely as the `expect` key `_assertable` validates, or assert in `_assertable` that every `rebuild_errors_delta` is 0 so a non-zero value is refused at the door rather than honoured silently.
- **reviewer/src/swreview/remodel/apply.py:1131** (executor) - `_unmet` docstring claims "`folder_location` is the membership the host verified with IFeatureManager.FeatureFolderLocation per member; asserting it here is what keeps a folder that wrapped the wrong run from reading as a success", but the code only compares `landed.after["name"]` against the expect Fix: Compare the reported `member_persist_refs` with the requested ones inside `_unmet` (order-insensitively or exactly, whichever the host guarantees) and fail `expect_unmet` on a mismatch, or correct the docstring to say the name is the only thing asserted on this side. The first is the one that matche
- **reviewer/src/swreview/remodel/apply.py:1082** (executor) - `_undo_of` catches bare `ValueError` from `derive_undo`, conflating three different things onto one `None`: "v1 has no in-place inverse for this kind" (folder.create, save — a deliberate value), "the record carries no reading to invert from" (an unreadable previous description or location), and "pyd Fix: Let `derive_undo` return `None` for the two kinds `_INVERSE` maps to `None` and let its ValueErrors propagate, or catch a narrow, purpose-built exception type so a malformed record fails loudly instead of being reported as an un-undoable change. Keep one test per branch.
- **reviewer/src/swreview/remodel/replay.py:97** (executor) - `applied_changes` matches `record.seq` (the executor's own counter, seeded from `_next_seq(log)` and incremented once per attempted change) against `PlannedChange.seq` (the plan's 1..N, validated at plan.py:700). The two coincide only because a fresh run's log starts at 1 and the loop increments in  Fix: Record the plan's own `seq` on the `ChangeRecord` (or match on `kind` + `subject.persist_ref` + params) rather than relying on the two counters agreeing, or add an explicit test that pins `record.seq == change.seq` for every line of a first pass so the coupling fails loudly if it ever drifts.
- **reviewer/src/swreview/remodel/apply.py:1059** (executor) - `_invert` recovers the client method with `undo.command.removeprefix("remodel.")`, a second, implicit copy of the command-to-method translation that `BRIDGE_METHOD` (line 651) already owns. The two disagree in shape (`remodel.close` would resolve to a nonexistent `close`, `remodel.folder` happens to Fix: Resolve the inverse's method through one mapping shared with the forward path (a `command -> client method` dict that `BRIDGE_METHOD` is derived from), so a command name that does not map is a refusal rather than an AttributeError.
- **reviewer/tests/support/remodel.py:414** (executor) - `STAGE_1_SOURCE_DIRS` scans `reviewer/src/swreview/remodel/*.py`, `extractor/.../Rms/*.cs` and `.../Guard/*.cs`, but not `reviewer/src/swreview/bridge/` (where `remodel_client.py` composes every command) nor `extractor/SwReview.Extractor/Bridge/` (where `BridgeDispatcher.cs` carries the `remodel.*`  Fix: Add `reviewer/src/swreview/bridge` (`*.py`) and `extractor/SwReview.Extractor/Bridge` (`*.cs`) to `STAGE_1_SOURCE_DIRS`. The existing "matched no pattern" assertion keeps the new entries from passing vacuously.
- **reviewer/tests/unit/test_remodel_apply.py:624** (executor) - `test_a_truncated_run_is_never_a_silent_partial_success` asserts `len(result.applied) + len(result.not_attempted) <= len(changes())`, which holds for almost any wrong answer — including one where the executor loses track of changes entirely. The real invariant is a partition: applied + rolled_back + Fix: Assert the partition as an equality over `result.planned`, and assert it in every branch of the loop (not just the truncated ones). It is the one assertion that makes ApplyResult's "never a silent partial success" docstring checkable.
- **reviewer/tests/unit/test_remodel_apply.py:631** (executor) - `test_the_limits_are_neither_readable_nor_settable_by_the_model` scans only `swreview/agent/**/*.py` and `remodel/intent.py`, but `Limits` is a field on `RemodelPlan` (plan.py:655, `limits: Limits = Limits()`) and is therefore serialized into `plan.json` and into any plan-shaped payload. The test as Fix: Widen the scanned surface to every module that composes a payload for the model (`swreview/tools/`, `swreview/mcp/`, anything that renders a plan into a prompt), or assert directly that no model-facing tool payload contains the `limits` key — which is the property T082's sentence actually claims.
- **reviewer/tests/unit/test_remodel_apply.py:160** (executor) - The module docstring makes "a count compared against the **run's baseline** - not against the reading before the change" a headline property (lines 12-14), and `run()` takes a `baseline` parameter to drive it, but no test ever passes a non-zero value (grep confirms `baseline` appears only in the hel Fix: Add a test with `baseline=2` plus `Script(errors_after={2: 2})` asserting the change records `applied` (the count is at, not above, the baseline) and a second with `errors_after={2: 3}` asserting `rolled_back` and that the error string names the baseline of 2. That is the one assertion that would fa
- **reviewer/src/swreview/remodel/replay.py:158** (executor) - `replay_run` hands the same `Limits` object to a fresh `apply_changes` call, which resets both the change counter (`taken` restarts at 0 in `_bound_reached`) and the wall clock (`started_at = now()` at apply.py:855). The bounds are therefore per-pass, not per-run. Probe with the replay fixture and ` Fix: Either carry the remaining budget into the replay - subtract the first pass's applied count and elapsed wall clock and pass a narrowed `Limits` - or, if a 2x aggregate is the owner's intent, say so explicitly in both docstrings and add a test that pins the aggregate (`first pass + replay <= 2 * max_
- **reviewer/src/swreview/remodel/apply.py:1166** (executor) - `_rebuild_bound` receives `rebuild_ms=None` for every equation change, because `_perform` builds `_Landed(..., None)` on the `apply_equation_change` path (apply.py:975) and `apply_equation_change` never reads `rebuild["elapsed_ms"]`. So the executor-enforced `max_rebuild_seconds` bound applies to fi Fix: Have `apply_equation_change` carry `int(rebuilt["elapsed_ms"])` on `EquationOutcome` and let `_perform` pass it into `_Landed`, so one bound is enforced in one place for all seven kinds. Add a test with `Script(rebuild_ms_after={5: 121_000})` (the `equation.edit` ordinal) asserting `status == "trunc
- **reviewer/src/swreview/remodel/replay.py:106** (executor) - `applied_changes` matches `record.seq` against `change.seq`, but these are two different numbering schemes: `ChangeRecord.seq` comes from `_next_seq(log)` (a log-line ordinal that continues an existing file), while `PlannedChange.seq` is the plan ordinal validated as 1..N at plan.py:700. They coinci Fix: Carry the plan ordinal explicitly on the record - add a `planned_seq` field, or assert the invariant at the top of `applied_changes` (`records[i].seq == i + 1` for the pass being replayed) with a raise naming the mismatch. At minimum, document on `ChangeRecord.seq` that it is a log ordinal and on `a
- **reviewer/tests/unit/test_remodel_verify.py:300** (executor) - T104 says "any one of the three failing discards the copy, writes the report and does not save". `TestTheDiscard` and `TestTheReport` only exercise the gate-failure branch (`reading(volume_m3=1.3e-3)`). Neither the `rebuild_errors > 0` case (`test_a_rebuild_error_count_above_zero_does_not_save`) nor Fix: Parametrize the discard and report assertions over all three failing readings, or add `assert result.copy_discarded is True` and `assert result.report_path.is_file()` to the two existing check tests.
- **reviewer/tests/unit/test_remodel_apply.py:629** (executor) - `test_the_limits_are_neither_readable_nor_settable_by_the_model` scans only `src/swreview/agent/**` and `remodel/intent.py`. The model's actual callable surface is `src/swreview/tools/` (registry, payload builders, the MCP server), none of which is scanned, so a limit name leaking into a tool schema Fix: Extend `surface` to include `sorted((root / "tools").rglob("*.py"))` and `root / "mcp" / "server.py"`, keeping the same three names. Coordinate with the feature 005 workflow, which is editing those files concurrently.
- **reviewer/tests/unit/test_remodel_apply.py:497** (executor) - `test_an_open_circuit_aborts_and_the_call_was_never_sent` proves "never sent" only by `bridge.tree.order == [SKETCH, BOSS, CUT, FILLET]`. The fake's `_begin` appends the call to `bridge.calls` and increments `mutations` *before* it returns `CircuitOpen` (remodel_bridge.py:511-516), so the call does  Fix: Assert on the fake's own state instead: `assert ("remodel.reorder", ...) in bridge.calls` is expected, so pin the thing that matters - `bridge.tree.order` unchanged *and* `bridge.rebuilds == 2` (no rebuild followed the unsent change) - or rename the test to "the change did not land". Either way, mak
- **reviewer/src/swreview/remodel/report.py:662** (artifacts) - Section 8 prints only the recorded half. contracts/run-artifacts.md requires 'the recorded and re-checked path, length, last-write time and hash, and the verdict', but render_report is handed a bare SourceAttestation, which carries no observed values, so a run that failed on the attestation says the Fix: Pass the AttestationCheck (or its differences) into render_report and print recorded vs observed per differing field in section 8.
- **reviewer/src/swreview/remodel/report.py:276** (artifacts) - _headline puts 'elif plan.rebuild or plan.pins' ahead of 'elif plan.state == "failed"', so a run that aborted on rollback_failed, failed its save or was discarded leads with 'this part needs rebuilding: ... and the geometry gate reported pass' and the first line never says the run failed or that not Fix: Order the leads by severity: attestation, then state == 'failed', then the gate, then rebuild/pins. Add a headline test for a failed run that also carries a rebuild list.
- **reviewer/src/swreview/remodel/report.py:637** (artifacts) - Section 7's internal order deviates from the contract, which fixes it as 'Per accepted proposal, the provider and the model ...; then rejected_proposals with the rule that refused each one ...; then the sentence that the added globals drive nothing yet (FR-030)'. The implementation emits GLOBALS_DRI Fix: Move the FR-030 sentence after the rejected-proposal list and assert the three blocks' order by index, the way the nine sections are asserted.
- **reviewer/src/swreview/remodel/apply.py:1690** (artifacts) - _report returns None and writes no report.md at all when the geometry reading never came back. The report is the product of the run and the one artifact an engineer reads; a run that failed because remodel.geometry did not answer leaves the failure only in Verification.reasons, which is in-process s Fix: Make the geometry argument optional in render_report and have section 4 state which reading did not come back, so report.md is written for every terminal state as _report's own docstring claims.
- **reviewer/src/swreview/remodel/artifacts.py:102** (artifacts) - ATTESTATION_FILE_NAME = "source-attestation.json" is declared and exported here as well as in attestation.py:65. Two spellings of one filename that must agree is the drift the surrounding docstrings argue against (PACKAGE_BEFORE is imported from plan.py for exactly that reason), and there is no impo Fix: Import ATTESTATION_FILE_NAME from swreview.remodel.attestation in artifacts.py and drop the local constant from __all__.
- **reviewer/tests/unit/test_remodel_apply_log.py:497** (artifacts) - test_target_path_agrees_with_the_remodel_log_line_for_the_same_request composes both halves of the comparison from the same COPY_PATH constant and parses the log line with a second, locally written regex (logged_target, line 133) rather than with report.read_log_targets. T074's 'one case asserts it  Fix: Assert through read_log_targets over a written remodel.log file, and keep one parser: either the test uses the production function or both use one shared regex.
- **reviewer/tests/unit/test_remodel_units.py:296** (artifacts) - T088's step 6 ('assert the geometry snapshot is identical to the pre-change one ... any delta here is a defect and not a tolerance question') is replaced by an inspect.signature purity argument, and the add is driven through a locally written FakeEquationManager plus a hand-rolled add_verified rathe Fix: Add one case in test_remodel_apply_globals.py that reads remodel.geometry before and after an equation.add over FakeRemodelBridge and asserts the two GeometryReadings are equal field for field; keep the purity assertion as the explanation rather than as the evidence.
- **reviewer/src/swreview/remodel/units.py:173** (artifacts) - _literal falls back to repr(value), which emits Python scientific notation ('1e-05') for magnitudes below 1e-4. The docstring's guarantee is 'the shortest spelling that parses back to the same float', but the consumer of the text is the SOLIDWORKS equation parser, not Python's float(), and a row rea Fix: Refuse, or format in plain decimal, any converted value whose shortest repr is not plain decimal, so an unverifiable literal is a refusal rather than a row written into the document.
- **reviewer/src/swreview/remodel/report.py:378** (artifacts) - FR-041's count is taken over every `ChangeRecord`, including `attempting` and `failed` lines, and then printed as "{records} of {total_records} change record(s) in `changes.jsonl` name it as the target `VerifyTarget` confirmed". `apply.py:1204` stamps `target_path` from the caller's constant (`apply Fix: Count only records that actually wrote — `status in {applied, rolled_back, rollback_failed}` — and say so in the sentence, or carry the host's VerifyTarget-confirmed path back in each mutating reply and record that value instead of the caller's constant.
- **reviewer/tests/unit/test_remodel_apply_log.py:497** (artifacts) - `test_target_path_agrees_with_the_remodel_log_line_for_the_same_request` is tautological: it calls `remodel_log_line("17", "remodel.reorder", COPY_PATH)` and then asserts `logged_target(line) == record.target_path`, where `record.target_path` is the same `COPY_PATH` module constant. Nothing but the  Fix: Drive the two halves from different sources: write a `remodel.log` the way tests/support/remodel_bridge or an existing fixture produces it, run a change through `apply_changes`, and assert `read_log_targets(run_dir)` and the `ChangeRecord.target_path` written by the executor name the same file.
- **reviewer/tests/unit/test_remodel_report.py:365** (artifacts) - The section order is asserted against `report.SECTIONS`, the implementation's own constant (`assert headings[1:] == list(SECTIONS[1:])`), not against the nine-row table in contracts/run-artifacts.md. Reordering or renaming SECTIONS keeps this test green; only the `file_regression` golden catches it, Fix: Parse the nine-row `report.md` section table out of specs/004-resilient-remodeler/contracts/run-artifacts.md and assert the rendered `##` headings equal that list in order, keeping SECTIONS as the implementation detail it is.
- **reviewer/tests/unit/test_remodel_report.py:437** (artifacts) - `test_a_gate_that_did_not_pass_is_in_the_headline` asserts `"geometry" in first`, but `_headline` (report.py) unconditionally appends "and the geometry gate reported {verdict}" to every headline, so the assertion passes for a passing gate too. The test would not fail if the `GATE_DID_NOT_PASS` lead  Fix: Assert on the distinguishing text — e.g. `GATE_DID_NOT_PASS`'s "could not be shown unchanged" and `"fail"` — and add the negative: a passing run's first line does not carry that lead.
- **reviewer/tests/unit/test_remodel_units.py:276** (artifacts) - Section 5 of T088 ("add the global through AddEquationVerified and assert it landed and evaluated", "rebuild and assert the text and value round-trip") is driven by a local `FakeEquationManager` plus a local `add_verified` whose assertions restate exactly what the fake does (append, count+1, read ba Fix: Drive these two cases through `tests.support.remodel_bridge.FakeRemodelBridge` and `apply.apply_equation_change`, as test_remodel_apply_globals.py does, and delete the local fake; the round-trip then goes through real code.
- **reviewer/tests/unit/test_remodel_units.py:320** (artifacts) - T088's step 6 — "assert the geometry snapshot is **identical** to the pre-change one, because a global that drives nothing cannot move geometry" — is not asserted anywhere. The file substitutes a purity argument (`test_seeding_a_global_is_a_pure_function_of_the_value_the_unit_and_the_name`) and says Fix: Add one case that takes a `copy_at_open` reading off the fake bridge, applies the `equation.add`, takes a `copy_at_end` reading and asserts `evaluate(before, after, IDENTITY).verdict == "pass"` with every delta exactly 0.0 — an identity, not a tolerance.
- **reviewer/src/swreview/remodel/apply_log.py:128** (artifacts) - `rebuild_errors_after: int | None`, but data-model.md section 2 and the restated table in contracts/run-artifacts.md both type it `int` (only `elapsed_ms` is documented `int | null`). Nulling it on the attempting line is the right call — writing 0 into a reading nobody took is exactly the default Pr Fix: Out of this workflow's scope to edit the spec: raise the one-line change to data-model.md section 2 (and its verbatim restatement) with the owner, and until then have the test assert the documented optionality per field rather than only the name set.
- **reviewer/src/swreview/remodel/units.py:166** (artifacts) - `_literal` falls back to `repr(value)` for a non-integral value, which emits scientific notation below 1e-4 (`repr(1e-05) == '1e-05'`), so a seeded global could reach `IEquationMgr` as `"w" = 1e-05`. `test_the_literal_parses_back_to_exactly_the_value_it_was_written_from` round-trips with Python's `f Fix: Either refuse a value whose literal would not be plain decimal with a `DocumentUnitError` naming it, or format without an exponent and assert the no-`e` property in the parametrized literal test.
- **reviewer/src/swreview/remodel/apply.py:1072** (artifacts) - `_undo_of` catches every `ValueError` from `derive_undo` and returns `None`, which the artifact defines as a deliberate value meaning "no in-place inverse exists" (apply_log.py `derive_undo` docstring, run-artifacts.md). A `rename` whose `before.name` did not read, or an `equation.edit` with no reco Fix: Keep returning `None` (the control flow is right) but carry the refusal sentence into the terminal record's `error`, so `changes.jsonl` distinguishes "this kind has no inverse in v1" from "the reading this inverse needed was not there".

## Feature 004 offline part 3 (2026-09-16)

- **reviewer/src/swreview/remodel/runner.py:247** (judgement) - `build_provider` (lines 231-262) is a near-verbatim copy of `cli.provider_factory` (cli.py:243-279). The Gemini branch is character-for-character identical (deferred `from google import genai`, `client=genai.Client(**settings.client_kwargs())`, `redact=lambda text: redact_secrets(text, settings.secrets)`, `max_output_tokens=output_ceiling(...)`). The brief called out the EventSink copy as the sin to avoid and this is the same sin one module over: the next change to the Gemini enterprise path, the output ceiling, or a new SDK kwarg lands in one of the two sites and the remodel run silently diverges. contracts/tools.md's "exactly one place" is satisfied for the remodel run, but the product now has two adapter-construction bodies. **Still open (2026-09-23), and now proven costly:** the copy carried the same wrong `redact=` keyword as the original, so the Gemini bug had to be fixed twice. Delegating `build_provider` to `cli.provider_factory` after its scripted-provider refusal would leave one construction body, but `tests/unit/test_remodel_prompt_policy.py::test_the_remodel_runner_is_the_one_construction_site` and feature 004's contracts/tools.md require `remodel/runner.py` to reach the registry itself, so it is a contract change for the owner to decide. **Resolved 2026-09-23 (owner decision 4A):** the owner took the contract change. `build_provider` keeps its refusal of the scripted provider and delegates to `cli.provider_factory` through a deferred import, so one body builds the review's, the pane's and the remodel run's adapters; feature 004's contracts/tools.md and research R7 record the amendment and the reason, and `test_the_remodel_runner_is_the_one_construction_site` now asserts the delegation, with two behavioural tests beside it (the settings reach the factory untouched and with no levers; the scripted provider is refused before the factory is asked). The `build_adapter` extraction below was not needed: `cli.provider_factory` already is the one body.
  - Fix: Extract the shared body into agent/providers (or agent/settings) as one `build_adapter(settings, *, parallel_tool_calls=None, script=None)` and have both cli.provider_factory and remodel.build_provider call it, each keeping only its own policy (the CLI's efficiency lever, the remodel run's refusal of ProviderName.FAKE). Keep the T118 AST assertion pointed at remodel/runner.py by keeping the `providers.get` call, or retarget it at the shared function.
- **reviewer/src/swreview/remodel/runner.py:422** (judgement) - `judge` hardcodes `key_source="none"` in the `ProviderInfo` it writes into session.json. The review path threads the real value (cli.py:535 and :1695 pass `key_source=settings.key_source`), and `ProviderSettings._key_source_matches_the_key` (settings.py:214) makes "none" mean specifically "no api_key is set". So a remodel run that used an env key records in its run artifact that it used no key at all. That is not "unknown stays unknown" - the enum has no unknown member, so the runner is asserting something false rather than declining to answer.
  - Fix: `run_remodel` already holds `settings`; thread `settings.key_source` through to `judge` (a keyword defaulting to "none" for the provider-injected test paths) and record it. Assert it in test_remodel_runner.py alongside test_session_json_records_every_tool_call_the_turn_made.
- **reviewer/src/swreview/remodel/runner.py:734** (judgement) - `run_remodel(redact=no_redaction)` defaults to no redaction even though the same call already receives `settings`, whose `.secrets` carries the key. Every provider error text that reaches `events.jsonl` (line 450-453) and, through `JUDGEMENT_FAILED`, the plan's coverage row and report.md, is unmasked unless the not-yet-written host caller remembers to pass a redactor. FR-015 puts the key out of the run folder. `start_review` has the same default, but it does not receive the settings object, so it has no choice; `run_remodel` does.
  - Fix: Default the redactor from the settings inside `run_remodel` - when `redact` is not given and `settings is not None`, use `lambda text: redact_secrets(text, settings.secrets)` - and keep the explicit parameter for callers that install a process-wide redactor the way cli._redacting does. Add a test that a provider error carrying a fake key never appears in events.jsonl or plan.json.
- **reviewer/src/swreview/remodel/runner.py:633** (judgement) - `_equation_changes` hardcodes `repairs=()`, and it is the only caller of `apply.equation_changes` in the product. `_changes` in plan.py:1060-1063 states that C5 is "only the judgement phase proposes", but no proposal tool produces a repair (`propose_global` rejects an existing name outright, remodel_plan.py:363). The result: the whole FR-029 / C5 `equation.edit` path built and tested by T089a/T089b (test_remodel_equation_edit.py, SetEquationVerified, the derive_undo inverse, the guard allowlist entries for `set_Equation` and `SetEquationAndConfigurationOption`) is unreachable from any run, and contracts/tools.md's v1 scope note ("v1 can name and repair globals that already exist") has no implementation behind it. The allowlist rationale "so no allowlist entry is without a call path" (T042) is also no longer true in practice.
  - Fix: Decide with the owner which it is: either compose the deterministic repairs in `revision_two` from whatever `remodel/intent.py` identifies as a repairable existing global and pass them as `repairs=`, or record explicitly (a `PlanCoverage` row and a line in the report) that v1 composes no repair, so the gap is visible rather than inferred from an empty tuple. Do not change contracts/tools.md here - flag it for the owner.
- **reviewer/src/swreview/remodel/runner.py:601** (judgement) - The deviations merge keeps the planner's stale `fillet_default_core` deviation after the model overrides that fillet. In the T116 fixture Fillet1 is defaulted to 3-Core by the planner with `report_line="reviewed as structural; move to Quarantine if cosmetic"` (plan.py:1158), the valid script then quarantines it, `_merged_targets` correctly replaces the target - and revision 2 still carries the Core deviation. report.py prints both, so one fillet appears in report.md as reviewed-as-structural-in-Core and as a model-decided Quarantine target at the same time. FR-020's point is that a reader can tell where a group came from; two contradictory provenance rows defeat it.
  - Fix: In `revision_two`, drop any planner deviation of kind `fillet_default_core` whose `feature_id` appears in `remodel.targets` (the same substitution rule `_merged_targets` already applies to targets), and add the assertion to test_remodel_runner.py's `judged` fixture: after a model Quarantine, no `fillet_default_core` deviation names that feature.
- **reviewer/src/swreview/remodel/runner.py:679** (judgement) - `_merged_targets` (line 679) and `tools/remodel_plan._targets` (line 642) both substitute model targets into `plan.targets` by feature id and silently DROP any model target whose feature is not already in `plan.targets`. Today the partition is total so nothing is lost, but the drop is silent: a future planner that leaves a feature out of `targets` (or a tool rule that is relaxed) would have `decide_fillet`/`classify_unknown` return success to the model, write a `RejectedProposal`-free accepted target, and have it vanish from revision 2 and from `get_remodel_plan("targets")`. That is an accepted proposal with no record anywhere, which is the exact failure FR-016 and the "a rejection is also written down" rule exist to prevent, only for acceptances.
  - Fix: Make the invariant explicit rather than implicit: in `_merged_targets`, raise (the way `describe_changes` already raises for an unknown feature, runner.py:534) when a model target names a feature the plan's partition does not carry, or append it. Add the assertion that len(revision-2 targets) == len(revision-1 targets) and that every model target id survives.
- **reviewer/src/swreview/tools/remodel_plan.py:508** (judgement) - Three rejection rules exist in code that contracts/tools.md's rule tables do not list: `decide_fillet.already_decided` (line 508), `classify_unknown.already_decided` (line 566) and `decide_fillet.dependents_unreadable` (line 490). All three are defensible - two targets for one feature is not a partition, and an unreadable dependency list is not a known-empty one, which is "unknown stays unknown" - but the contract's tables are presented as exhaustive ("Every rule below is a pure function ... has its own unit test") and the report will print rule ids no reader can look up. The tasks correctly forbid editing the contract here, so this needs an owner decision rather than a silent divergence.
  - Fix: Flag to the owner for a follow-up contract edit adding the three rows (the contract already carries the equivalent `at most one proposal per feature_id` row for propose_description, so this is the same rule generalised). No code change; do not edit contracts/tools.md under T106-T119.
- **reviewer/src/swreview/tools/remodel_plan.py:374** (judgement) - `propose_global` accepts an expression identifier that is an existing document global (`proposed | existing`, line 374), while the contract row reads "every identifier in `expression` is a global declared earlier in the plan | `expression references an undeclared name`". The implementation is the more useful reading and is consistent with the sibling rule ("`evidence` is non-empty unless every identifier is a global declared earlier in this plan" only makes sense if other identifiers are permitted), and `referenced_globals` plus `seed_order` handle the case correctly, so this is a wording divergence rather than a defect - but it is undocumented and untested: no case in test_tools_remodel_plan.py proposes an expression over an existing package global.
  - Fix: Add the missing accept case to test_tools_remodel_plan.py (an expression naming an existing package global with non-empty evidence) so the intended reading is pinned by a test, and flag the contract wording to the owner.
- **reviewer/src/swreview/remodel/runner.py:667** (judgement) - DRY: `_merged_targets` (remodel/runner.py:667) and `_targets` (tools/remodel_plan.py:634) are the same four-line merge - `decided = {item.feature_id: item for item in remodel.targets}` then `tuple(decided.get(item.feature_id, item) for item in plan.targets)` - carrying near-identical comments ("two targets for one feature is not a partition"). They must agree by construction, because one is what `get_remodel_plan("targets")` shows the model and the other is what revision 2 records; the day one changes, the model sees a partition the plan does not carry. `_rows` is likewise defined twice (runner.py:659 returning a list, remodel_plan.py:159 returning a dict) over the same `row.document_id == plan.document_id` filter.
  - Fix: Define the merge once - `merged_targets(plan_targets, model_targets)` in `tools/remodel_plan.py`, beside the dataclass whose lists it merges - and have `remodel/runner.py::_merged_targets` call it. Consider the same for the document-row filter (one helper returning the list; the dict is `{row.id: row for row in ...}` at the call site).
- **reviewer/src/swreview/remodel/runner.py:74** (judgement) - `from swreview.agent.runner import SESSION_FILE_NAME` makes the re-modeler import the whole review runner - checklist, findings, evidence requests, coverage buckets - for one filename constant. T107 says explicitly that `ReviewRun` is not reused by the re-modeler, and T106 says the shared pieces are extracted to `agent/events.py`; `EVENTS_FILE_NAME` moved and its sibling did not. `test_agent_events.py`'s `SHARED` tuple does not list it either, so nothing pins the asymmetry.
  - Fix: Move `SESSION_FILE_NAME` into `agent/events.py` beside `EVENTS_FILE_NAME`, re-export it from `agent/runner.py` the way the other six are re-exported, add it to `SHARED` in `test_agent_events.py`, and re-point `remodel/runner.py` so it imports nothing from `agent/runner.py`.
- **reviewer/tests/unit/test_remodel_runner.py:767** (judgement) - `test_the_judgement_phase_sends_no_remodel_command` is tautological. Its docstring claims "asserted against a bridge fake that fails any call: `judge` never receives one", but `bridge = NoBridge()` is constructed and never passed to `judge` (which has no client parameter at all); the only assertion about it is `with pytest.raises(AssertionError): bridge.rename`, which proves the fake raises and nothing about the code under test. The test would pass unchanged if `judge` grew a bridge parameter and used it, as long as the local `NoBridge` stayed unwired. The real proof lives in the sibling `test_the_fake_seat_is_the_only_sink_of_remodel_commands` (line 785).
  - Fix: Either delete the `NoBridge` half and keep only the `after.plan_revision == 2` assertion with an honest docstring, or make the fake load-bearing: assert on `inspect.signature(runner.judge).parameters` that no parameter is a client/bridge, and assert that `runner.judgement_context(...)[0].bridge is None` so no bridge tool can be registered for the turn.
- **reviewer/src/swreview/tools/remodel_plan.py:490** (judgement) - Three rejection sentences are not in the normative table of `specs/004-resilient-remodeler/contracts/tools.md`. The contract's `decide_fillet` table has one row, "`feature_id` exists and classifies as a fillet -> `not a fillet`"; the code splits it into `unknown feature id` (line 490) and `not a fillet` (line 492). `classify_unknown`'s contract row "`feature_id` exists and classifies as `unknown` or `ambiguous` -> `this feature already has a class`" is likewise split into `unknown feature id` (line 546) and `not a content feature` (line 548). The code also adds a rule the contract does not declare at all: `decide_fillet.dependents_unreadable` (line 503). The tests codify the implementation, not the contract, so nothing catches the drift - and no task in this stage authorises editing tools.md.
  - Fix: Pick one side and make it explicit. Recommended: keep the code (the split sentences are more actionable and `dependents_unreadable` is correct under "unknown stays unknown"), and raise the three extra rows with the owner as a contract amendment under whichever task owns `contracts/tools.md`, rather than leaving the code and the normative table disagreeing silently.
- **reviewer/src/swreview/tools/registry.py:177** (judgement) - Stale prose introduced by this change: the `REGISTRATIONS` docstring still says "The bridge tools are the one group that is not here: they depend on the run, not on the build, so `ToolRegistry.build` adds them when the context carries a bridge." There are now two such groups, and `_offered` (not `build`) is what adds them - `remodel_tools`'s own docstring at line 152 already says `_offered`. A reader adding a third conditional group is told there is only one.
  - Fix: Reword to name both conditional groups and point at `ToolRegistry._offered` as the single place that decides them.
- **reviewer/src/swreview/agent/events.py:55** (judgement) - `utc_now` is now defined twice in the product: here, and in `remodel/apply.py:831` (where it is also in that module's `__all__` and is the default `now=` for `apply_changes` and the replay). The two bodies are identical (`datetime.now(UTC)`), and `remodel/runner.py` imports the `agent/events.py` one while handing it to `apply_changes`, whose default is the other. Two names for one clock in one feature is exactly the repetition the stage brief says to extract rather than copy.
  - Fix: Have `remodel/apply.py` import `utc_now` from `swreview.agent.events` and re-export it (keeping its `__all__` entry so `replay.py` and the existing tests are unedited), leaving one definition.
- **reviewer/src/swreview/agent/runner.py:128** (judgement) - Deviation from T106's literal text, worth recording rather than fixing blind: T106 names `build_system_prompt` among the pieces "extracted the same way and shared rather than copied", and it stayed in `agent/runner.py`. The implementation is defensible - `build_system_prompt` takes a `Checklist` and an `EvidencePackage` and renders a review census, and `test_agent_events.py::REVIEW_SHAPED` deliberately forbids `swreview.agent.checklist` and `swreview.ir.models` from ever appearing in `events.py` - and `remodel/runner.py::system_prompt()` does not copy it, it reads a different prompt file with no checklist and no census. But nothing in the tests or the code says the task item was considered and declined, so the next reader will re-open it.
  - Fix: No code change recommended. Record the decision where it will be read: one sentence in `agent/events.py`'s module docstring (beside the existing note on `DEFAULT_MAX_STEPS` and `no_redaction`) saying `build_system_prompt` stays with `ReviewRun` because it is checklist- and package-shaped, and that the remodel run's prompt is a plain file read. Confirm with the owner that this satisfies T106 before the task is checked off.
- **extractor/SwReview.AddIn/SwReviewAddIn.cs:446** (pane) - `RemodelHost` is never constructed and `TaskPaneControl.RemodelPageMessageReceived` has no subscriber, unlike `StartModelCheckHost` which wires tab 4. There is also no production `IRemodelPipeline` (the only implementation is `FakePipeline` in the tests). As shipped, opening the Remodel tab loads the page, the page posts `ready`, nothing answers, the promise never settles, and every button stays disabled with "Backend starting" forever. No task in T120-T134 names the wiring explicitly, but T140 ("run the Remodel tab manually") cannot be reached without it.
  - Fix: Confirm with the owner whether the wiring and a real pipeline are deliberately deferred to a later stage. If so, record it as a known-undone item; if not, add a `StartRemodelHost(reviewOptions)` beside `StartModelCheckHost` that subscribes to `RemodelPageMessageReceived` and hands the message to `RemodelHost.Receive` off the UI thread, one at a time, exactly as the Model check host does.
- **extractor/SwReview.AddIn/TaskPaneControl.cs:853** (pane) - The WebView2 fallback panel still says "the Review, Ask and Model check tabs cannot be shown". There are now four WebView2 tabs, and the Remodel tab is equally unshowable without the runtime; the engineer reading that panel from the Remodel tab is told about three tabs that are not the one they are looking at. T120 says "the WebView2 fallback path still renders the existing message", and `WebViewFallbackTests` asserts that exact substring three times (:88, :127, :178), so changing it is a deliberate deviation from the task text rather than a free fix.
  - Fix: Ask the owner: either leave it and record the staleness, or change the sentence to name Remodel as well and update the three assertions in the same commit. My recommendation is to change it - the panel exists to tell the engineer what is broken, and naming three of four tabs is the kind of half-true message Principle I is about.
- **extractor/SwReview.AddIn/Review/PaneActions.cs:198** (pane) - The `IEntityResolver? resolver = _options.EntityResolver()` null check plus its `NotAttached` error is now written twice, verbatim including the message string: once in `ShowEntity` (:198-206) and once in the extracted `ShowPersistRef` (:233-241). `ShowEntity`'s copy is dead - it always falls through to `ShowPersistRef`, which repeats the check - so the two copies can drift and one of them is already redundant.
  - Fix: Delete the resolver lookup and null check from `ShowEntity` and let `ShowPersistRef` be the single place that decides what "not attached" means and says it.
- **extractor/SwReview.AddIn.Tests/RemodelPageContractTests.cs:775** (pane) - `ContractCsp()` (:775-784) and `Normalize(string policy)` (:799-804) are copied verbatim from `ModelCheckPageInjectionTests.cs:132` and `:143`. This is a duplicated *security* assertion - the same rule the page tests elsewhere insist must have exactly one copy (`ThePageLoadsTheSharedDomHelpersInsteadOfCopyingThem`). Separately, `Normalize` collapses whitespace and re-joins directives, so what is asserted is "equivalent modulo whitespace", not the "byte-identical" T132 asks for; the two strings do in fact match byte for byte today, so the weaker assertion is hiding nothing yet.
  - Fix: Lift `ContractCsp()` and the comparison into one shared helper in the test assembly (beside `ReviewPageFiles.ReadContract`) that both suites call, and have it assert ordinal string equality on the raw `content="..."` value so "byte-identical" is what is actually checked. If whitespace tolerance was deliberate, say why in the helper's doc comment instead of in two places.
- **extractor/SwReview.AddIn/Remodel/RemodelPage/remodel.js:789** (pane) - The judgement section cannot be filled after a pane restart. `renderPlan` is only called from `planRun` with `remodel.planned.plan_summary`; `renderResult` does not touch `ui.judgement`, and the `remodel.result` reply carries no plan summary. So on `applyInit` with a `latest_run`, the page calls `renderRun` + `refreshResult` and the judgement region permanently reads "No plan has been read yet" for a finished run. `contracts/pane-remodel-messages.md` region 3 requires the judgement section (accepted proposals with provider and model, and every rejected proposal with the rule that refused it, "listed rather than omitted"), and the same contract's stated reason for reading `remodel.result` from the run folder is "so the tab answers after a restart".
  - Fix: Either have the page fetch the plan on `applyInit` (the host already reads `plan.json` in `StateOf`, so a `plan_summary` member on the `remodel.result` reply is a small additive change), or have `renderResult` render the judgement from members carried on the result. This touches the contract's `remodel.result` payload, so confirm the direction with the owner before changing it.
- **extractor/SwReview.AddIn/Remodel/RemodelHost.cs:715** (pane) - When the copy goes away mid-run, `DocumentChanged` sets the stop flag with no pending stop id, and `Execute` then sends `remodel.stopped` with `id: null` because `run.StopRequested` is true. `remodel.stopped` is a reply to `remodel.stop` in the contract's page-to-host table, not a row of the host-to-page unsolicited table, so the page's `handleUnsolicited` has no case for it and drops it silently. The engineer does get the `status {stage: "error"}` banner, so nothing is invisible, but the host is posting a message type the contract does not define in that direction - which is exactly what `EveryUnsolicitedHostToPageRowIsHandledByThePage` exists to prevent in the other direction.
  - Fix: Send `remodel.stopped` only when a `remodel.stop` is actually pending (`stopId != null`), and let the abort path report itself through the `status` it already posts. If the page should learn `changes_applied` on the abort path too, that needs a new unsolicited row in the contract.
- **extractor/SwReview.AddIn/Remodel/RemodelHost.cs:226** (pane) - T126 says "`document.changed` naming the **copy** aborts the run". The implementation does the opposite by design: `DocumentChanged` aborts only when `CopyExists(run)` is false, and `RemodelHostTests.ADocumentChangedNamingTheCopyItselfDoesNotAbortTheRun` locks that in. The contract row is on the implementation's side ("If the copy goes away mid-run, the run aborts with the change log intact"), and aborting on a `document.changed` that names the copy would make `remodel.open_copy` abort its own run - so the behaviour looks right and the task sentence looks like loose shorthand. It is still an unrecorded deviation from a task sentence, and tasks.md may not be edited.
  - Fix: Record the deviation explicitly for the owner (task sentence vs contract row, with the `remodel.open_copy` self-abort as the reason the contract wins), and get an explicit yes before this ships, rather than leaving the contradiction only in a test name.
- **extractor/SwReview.AddIn/Remodel/RemodelHost.cs:504** (pane) - `Track(runDirectory, copy.CopyPath)` runs only after `pipeline.Plan` returns. If `OpenCopy` or `Plan` throws after the copy file exists, the exception propagates to `Receive`'s catch, `_busy` is reset, and the run is never added to `_runs`. The copy `.SLDPRT` is then orphaned in the run folder and unreachable: `FindRun` answers `RunNotFound` for that `run_dir`, so `remodel.discard_copy`, `remodel.open_copy` and `folder.open` all refuse it, and the engineer has to delete it from Explorer. The copy is the one artifact this feature promises the engineer can get rid of on demand.
  - Fix: Track the run as soon as `OpenCopy` returns a copy path (before `Plan`), so a failed plan still leaves a discardable, openable run record, and let the run's `plan.json` state - or its absence - carry the fact that planning failed.
- **extractor/SwReview.AddIn.Tests/RemodelHostTests.cs:814** (pane) - The `RunInProgress` branch of `DiscardCopy` (`RemodelHost.cs:851-855`, discard requested while that run is the active one) has no test. Every other `RunInProgress` path is covered (`ASecondPlanWhileARunIsInFlightIsRefusedAsRunInProgress`, `ASecondStartWhileTheRunIsInFlightIsRefusedAsRunInProgress`), and this is the one whose failure mode is deleting a file out from under a running executor.
  - Fix: Add a case that starts a run on the blocking scheduler, sends `remodel.discard_copy` for the active run, and asserts `error {error_class: "RunInProgress"}` and that the copy folder still exists.
- **extractor/SwReview.AddIn/TaskPaneControl.cs:853** (pane) - The WebView2 fallback panel still says 'the Review, Ask and Model check tabs cannot be shown'. The Remodel tab now renders that same panel (LoadRemodelAsync -> BuildFallback), so on a runtime-less workstation an engineer looking at the Remodel tab reads a message that does not name the tab they are on. Three tests pin the stale wording (WebViewFallbackTests.cs:88, :127, :178) - :127 is the new Remodel case, so the new test actively locks in the inaccuracy. T120 did say 'the existing message', so this is a faithful reading of the task, but the message is now wrong.
  - Fix: Name all four WebView2 tabs: '...the Review, Ask, Model check and Remodel tabs cannot be shown. The Extract tab still works.' Update the three assertions to the new sentence (they are the only consumers).
- **extractor/SwReview.AddIn/Remodel/RemodelHost.cs:718** (pane) - When a run is aborted by `DocumentChanged` (the copy went away, RemodelHost.cs:230-240) there is no `_pendingStopId`, but `Execute` still sends `remodel.stopped` because `run.StopRequested` is true - with `id: null`, i.e. as an unsolicited message. `remodel.stopped` is not a row of the contract's 'Host to page (unsolicited)' table, and remodel.js `handleUnsolicited` (remodel.js:134-160) has no case for it, so it falls through `default` and is silently dropped: the engineer never learns how many changes were applied before the abort. The contract test cannot catch this because `RemodelContract.Unsolicited` is parsed from the table, which does not list it.
  - Fix: Pick one: (a) on the abort path post `status {stage:'error', message: '... N change(s) applied'}` instead of an unsolicited `remodel.stopped`, leaving `remodel.stopped` strictly a reply; or (b) add `remodel.stopped` to the unsolicited table in contracts/pane-remodel-messages.md and a `case 'remodel.stopped'` in `handleUnsolicited` - the existing `EveryUnsolicitedHostToPageRowIsHandledByThePage` test then covers it for free.
- **extractor/SwReview.AddIn.Tests/RemodelHostTests.cs:404** (pane) - Nothing pins the `status` stage vocabulary. `PlanPostsTheStagesThePageShows` only asserts that `copying`, `planning` and `ready` appear; there is no assertion that the host never posts a stage outside the contract's nine (`copying|dumping|planning|judging|applying|verifying|saving|ready|error`). `PostStatus` takes an arbitrary string, and the page only branches on `ready` and `error` (remodel.js:857-871), so a typo ('copy' for 'copying', or the host's `judging`/`applying` drifting) is invisible at runtime and invisible in the suite. Note the host currently never emits `dumping`, `verifying` or `saving` - those would come from the (non-existent) real pipeline, so they are untested in both directions.
  - Fix: Add a case to RemodelHostTests that reads the stage enum out of the `status` row of contracts/pane-remodel-messages.md (the RemodelPageContractTests parser already reads that file as data) and asserts every stage the host posted during plan + start + stop is a member of it.
- **extractor/SwReview.AddIn.Tests/ReviewPageUsageLineTests.cs:88** (pane) - 005 T016a says the test must assert 'that `GET /sessions/{chat_id}` is unchanged'. `TheNumbersComeOffTheStreamAndNotOffTheSessionRoute` only regex-scans the page's own scripts for any `<x>.usage` read that is not `state.usage`/`ui.usage`. That proves the page does not read a usage total off a reply, but it says nothing about the route: if `ChatSession.public()` later grew a `usage` member (the third copy the task exists to prevent), this test stays green. The named half of the task is therefore uncovered.
  - Fix: Add the missing half on the Python side: a test over `GET /sessions/{chat_id}` asserting the response body's key set is unchanged (no `usage`, no `round_trips`), pinned against the documented shape rather than a snapshot of the current dict.
- **extractor/SwReview.AddIn/Review/ReviewPage/app.js:335** (pane) - `state.usage.push(body)` (app.js:411) accumulates with no de-duplication, and the SSE frame loop dispatches every parsed frame regardless of `seq` (app.js:330-340 updates `state.lastSeq` but never skips a frame whose seq it has already seen). The page reconnects with `Last-Event-ID` (app.js:241-242), so a server that resumes inclusively, or any redelivery, double-counts tokens and round trips in a line whose whole value is that the arithmetic is trustworthy. The other accumulators tolerate this - findings/evidence/tools are keyed maps - but `usage` is the first one that is pure arithmetic, so the existing pattern is no longer safe here.
  - Fix: Guard the dispatch on `seq > state.lastSeq` (or de-duplicate `state.usage` on `round_index` within a turn), and add a case to ReviewPageUsageLineTests that feeds the same `usage` event twice and asserts the total does not change.
- **extractor/SwReview.Extractor/Dump/PackageWriter.cs:57** (csharp-leftovers) - PhaseOrder restates the nine phase names that are already spelled as the `entityKind` argument of each RunPhase call (lines 171, 179, 190, 208, 227, 241, 251, 257, 263) - nine string literals in two places, which is the DRY violation CLAUDE.md asks to be flagged aggressively. Worse, PhaseLog.Rows() at line 558 projects over PhaseOrder only, so a row recorded under a name PhaseOrder does not contain is timed and then silently discarded. The comment at lines 52-55 names that exact failure mode ('a phase that runs and is not named here would be timed and then dropped') and then does nothing to prevent it. Nothing in PackageWriterTests would fail: the assertions compare Rows(package) against a fixed nine-element array, which is precisely what a dropped tenth row still satisfies.
  - Fix: Make one list the source of truth: declare a constant per phase (or build a HashSet from PhaseOrder), pass those constants to RunPhase instead of inline literals, and have PhaseLog.Record throw InvalidOperationException when the name is not one PhaseOrder knows - explicit over clever, and it turns a silent data loss into a visible test failure. Add one PackageWriterTests case that records an unnamed phase and asserts it throws.
- **extractor/SwReview.Extractor/Dump/PackageWriter.cs:145** (csharp-leftovers) - `ComponentTreeResult tree = _components.Traverse(gaps, options);` runs before any phase and is not timed, and neither is ScopeFor() at line 158 nor the Finish() tail (AddComponentInstances, the reuse-key digest). On an assembly the component-tree walk is one of the most expensive parts of a dump - it is the COM traversal 003 US6 extended with the part-root node - so the sum of the nine recorded rows is not the dump. PhaseOrder's own doc comment claims it lists 'The phases of a dump, in the order Build runs them', which is not true of the first thing Build runs. T033's stated payoff is that 'which phase actually costs the time' becomes answerable rather than guessed; with the traversal missing it is still guessed for the largest slice, and lever 9 (package reuse skips the entire dump, traversal included) will under-report its own saving.
  - Fix: Either (recommended) wrap the traversal in RunPhase as a first phase named e.g. "component_tree" at the head of PhaseOrder - one call site, its gaps already flow through GapCollector, and it makes the row set actually equal the dump - or, if the owner prefers RunPhase's abort semantics untouched, time it with its own Stopwatch and record it via phases.Record. Either way state in Manifest.cs's XML doc and the pydantic description which parts of Build the rows do and do not cover.
- **reviewer/src/swreview/ir/models.py:672** (csharp-leftovers) - `DumpPhase.name` is a bare `str` with no constraint, and the C# `DumpPhase.Name` (extractor/SwReview.Extractor/Ir/Manifest.cs:55) is a bare string defaulting to "". The nine legal names exist only in prose - in this field description, in the XML doc at Manifest.cs:53-54, and in PhaseOrder. That is inconsistent with the closed-set discipline used everywhere else in the same object: `status` right below is a Literal of four, `profile` is an enum, GapKind is an enum. A typo'd or renamed phase validates cleanly on both sides and against the contract, and an empty name validates too (no minLength). Combined with the silent drop in PhaseLog.Rows(), a renamed phase yields a package that says nothing is wrong.
  - Fix: Make the name a closed set the way `status` is: `name: Literal["document", "manifest", "mate", "feature", "equation", "hole", "fastener", "face", "body"]` in models.py, a DumpPhaseName enum (or a validated lookup against PhaseOrder) in Manifest.cs, regenerate the contract, and add the negative cases to test_ir_phases.py and IrSerializerTests beside the existing UnknownPhaseStatus case. If the owner would rather keep the name open so a future phase needs no schema change, say so explicitly in the description - but then the PhaseLog guard above becomes mandatory, not optional.
- **extractor/SwReview.Extractor/Ir/Manifest.cs:42** (csharp-leftovers) - `public List<DumpPhase> Phases { get; set; } = new List<DumpPhase>();` has no omit-when-empty treatment, and PackageSerializer sets DefaultIgnoreCondition = JsonIgnoreCondition.Never, so C# always writes `"phases": []`. The implementer's own test had to strip it with a regex (Regex.Replace(json, ",\\s*\"phases\": \\[\\]", ...) in IrSerializerTests) to build the pre-T033 case, which is the proof. That contradicts the guarantee stated at reviewer/src/swreview/ir/models.py:724-726 - 'a package written before the member existed round-trips to the bytes that build wrote' - because two C# paths do exactly that round trip: Dump/PackageAppender.cs:50 deserializes a package and :63 re-serializes it (the 003 suppress-test append), and Dump/PackageReuse.cs's Copy() deserializes a candidate and re-writes it. A pre-T033 package.json passed through either now gains a member it did not have. No checked-in golden actually moves (they are files, and the suite is green), so this is a latent byte shift, not a break today.
  - Fix: Pick one and say so in both docs. Option A (recommended): make the C# omit it too - back Phases with a nullable list or add a small JsonConverter so an empty list is not written - and add an IrSerializerTests case asserting a package with no rows serializes without the member, which also removes the regex from the existing test. Option B: narrow the Python claim at models.py:724 to 'the Python writer' and add a line to Manifest.cs:33-42 saying the C# always writes the member (empty when nothing was timed), so the asymmetry is stated rather than discovered. A recommends itself for the same reason ReuseKey is kept byte-identical across the two languages.
- **extractor/SwReview.Extractor/Ir/Manifest.cs:33** (csharp-leftovers) - The XML doc says `phases` is 'Empty in a package written by a build that timed nothing, and in one assembled from exported files rather than dumped', and the pydantic description at reviewer/src/swreview/ir/models.py:713-719 says the same two cases. Neither mentions the third and most consequential case: Dump/PackageReuse.cs's Copy() re-serializes the earlier candidate verbatim, so a reused run's package.json carries the ORIGINAL dump's nine rows - real millisecond numbers, all `ok` - for a run in which no dump happened. Every other field in a reused package is likewise the earlier dump's and reused_from/reused_at say so, which makes it defensible; but `phases` is the one member whose whole purpose is measurement, and 005 T085a plans to read 'the per-phase dump wall clock from T033' into the lever-9 scorecard. A harness that reads phases without first reading reused_from scores the reuse arm as costing what the cold arm cost - the opposite of the lever's finding.
  - Fix: Add one sentence to both descriptions naming the case - e.g. 'In a reused package (reused_from is set) these rows are the dump it was copied from, not this run; a reader measuring this run must check reused_from first' - regenerate the contract, and add a C# test asserting a reused package's rows are the candidate's. If the owner would rather the file never state a timing this run did not make, the alternative is to clear candidate.Extractor.Phases in PackageReuse's Copy() beside the ReusedFrom/ReusedAt assignment; that is stricter against Principle I but discards a fact the earlier run legitimately recorded, so my recommendation is the documented option plus the test.
- **specs/005-llm-efficiency/tasks.md:251** (csharp-leftovers) - T093's pass/fail precondition reads: 'the reused package must be byte-identical to a fresh dump modulo `package_id`, `created_at`, `reuse_key` and the new reuse fields'. T033 has just added extractor.phases[].elapsed_ms, a wall-clock measurement that differs between any two dumps of the same document, so that precondition can never pass again as written - and T093 is explicitly blocked on T033, so the two tasks now contradict each other. Nothing in the diff notes this. (I am not proposing an edit to tasks.md - the implementer is forbidden from touching it - only that the collision be raised before T093 is attempted, since it is a gate that would otherwise fail for a reason unrelated to lever 9.)
  - Fix: Raise it with the owner now and record the answer where T093's implementer will find it (the T033 field descriptions are the natural place). Two workable directions: (A) add extractor.phases to the modulo set, i.e. the harness strips it from both packages before comparing - simplest, keeps the timing honest, and matches how package_id and created_at are already handled; or (B) have the harness compare [{name, status}] for equality and treat the elapsed values only as a reported delta, which is stronger because it still proves the reused package ran the same phases. A is the smaller change and the one I would take; B is worth it only if the owner wants the phase set itself under the gate.
- **extractor/SwReview.Extractor/Dump/PackageWriter.cs:558** (csharp-leftovers) - `PhaseLog.Rows()` projects over the fixed `PhaseOrder` array and `PhaseLog.Record` accepts any name, so a phase whose `entityKind` is not in `PhaseOrder` is timed and then silently dropped. The doc-comment names the hazard ("a phase that runs and is not named here would be timed and then dropped") but nothing enforces it: `Build_TimesEveryPhaseItRan_InTheOrderItRanThem` asserts the nine expected rows, and a tenth phase added to `Build` with a new entityKind would still produce exactly those nine, so the test stays green while its timing is lost. Every current `RunPhase` call site is in the list, so this is a latent, not a live, defect.
  - Fix: Make `Record` refuse a name that is not in `PhaseOrder` (throw, or record it and let `Rows()` emit it at the end), and add a PackageWriterTests case that a phase name outside the list is not silently dropped. Cheapest explicit form: `if (Array.IndexOf(PhaseOrder, name) < 0) throw new InvalidOperationException(...)`, which turns the hazard into a build-time-visible test failure.
- **reviewer/tests/unit/test_ir_phases.py:78** (csharp-leftovers) - 005 T033 says to implement "with the schema bump and a `test_schema_sync` regeneration". The regeneration was done (specs/001-agentic-design-review/contracts/ir.schema.json gained `$defs/DumpPhase` and the optional `ExtractorInfo.phases`), but no bump happened: SCHEMA_VERSION and EvidencePackage.CurrentSchemaVersion both stay "1.3.0", and `test_the_phases_member_needs_no_new_schema_version` pins that. The argument in the docstring is sound (1.3.0 landed in the immediately preceding commit f1c2bd2 as part of this same feature, the member is optional and additive, and a bump would move the version string inside the 001/002/003 goldens and break SC-007 byte-identity) — but it is a deviation from binding task text and should be the owner's call, not the implementer's.
  - Fix: Get explicit owner sign-off that 1.3.0 absorbs `extractor.phases` because it is unreleased and additive, and record that decision where a later reader will find it (a line in specs/005-llm-efficiency/probe-log.md or the T033 task note). Do not bump to 1.4.0 without first checking what it does to the goldens.
- **extractor/SwReview.Extractor/Ir/Manifest.cs:42** (csharp-leftovers) - The omission-when-empty rule is implemented on the Python side only. `PackageSerializer` sets `DefaultIgnoreCondition = JsonIgnoreCondition.Never`, so C# always writes `"phases": []` for an untimed `ExtractorInfo`, while `ExtractorInfo._omit_empty_phases` (models.py:723) drops the member entirely. `IrSerializerTests.PackageWithoutThePhasesMember_StillLoadsAndStillValidates` documents the asymmetry by regex-stripping `,\s*"phases": \[\]` from C# output rather than by producing it. Impact today is nil — `PackageWriter.Finish` always adds nine rows, so C# never emits an empty one, and `remodel/artifacts.write_package` only re-serializes C#-produced packages that already carry rows — but a C#-written package read and rewritten by Python is no longer byte-identical to the C# original, which is the property `save_package`/`load_package` round-trips elsewhere rely on.
  - Fix: Either state the asymmetry in the `Phases` doc-comment ("C# writes `[]`; the Python writer omits the member when empty, and both read either form"), or put `[JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingDefault)]`-equivalent behaviour on this one property via a small converter so both sides omit. Stating it is the cheaper and more honest option; silently differing writers is the thing that bites later.
- **extractor/SwReview.Extractor.Tests/RemodelFinishHandlerTests.cs:282** (csharp-leftovers) - `Close_RemovesTheSessionTagThroughTheGateBeforeItClosesTheDocument` pins removal-after-verification (`removed > verified` over `Copy.Members`) and that the gate saw `ICustomPropertyManager.Delete2` with no refusals, but nothing pins removal-BEFORE-`CloseDoc`. The close goes to `_harness.Seat.Closed` and the untag to `_harness.Copy.Members`, two separate fakes, so swapping lines 1704 and 1705 of BridgeDispatcher.cs leaves every test green — while on a real seat writing a custom property to an already-closed document is the failure the ordering comment at BridgeDispatcher.cs:1697-1701 says the design depends on avoiding.
  - Fix: Give the harness one shared ordered log both fakes append to (or have `FakeRemodelSeat.CloseDocument` record into `Copy.Members`), then assert `IndexOf(RemoveSessionTag) < IndexOf(CloseDocument)` alongside the existing verify-then-remove assertion. One extra assertion in the test that already exists.
- **extractor/SwReview.Extractor/Rms/RemodelCopy.cs:381** (csharp-leftovers) - Both `Tag` and `Untag` document their return value as "handed back for `remodel.log`, so the run report can still say what the seat did", but neither call site keeps it: BridgeDispatcher.cs:737 is `_remodelGate.Call(TagMember, () => RemodelCopy.Tag(tagged, parameters.RunId));` with the int discarded (the reply's `tag` field at line 801 is `parameters.RunId`, not Add3's answer), and line 1704 discards Untag's the same way. `SwGate.Call<T>` returns the value but records only the member name through the observer, so the Add3/Delete2 answers reach no artifact. Pre-existing (committed in f1c2bd2), not introduced by this working tree.
  - Fix: Either record the answer where the comment claims it goes (add it to the `remodel.log` row or to the close/open result) or correct both doc-comments to say the answer is deliberately not used because the read-back (Tag) and the imminent close (Untag) are the evidence. The second is less code and matches what the design actually relies on.
- **reviewer/src/swreview/ir/models.py:723** (csharp-leftovers) - DRY: `_omit_empty_phases` re-implements inline (`data = handler(self); if not data.get("phases"): data.pop(...)`) what the sibling wrap serializer at line 240 does through the shared `omit_when_null` helper (line 92). Two wrap serializers in one module, one using a helper and one open-coding the same pattern, and the next additive-optional member makes it three.
  - Fix: Add an `omit_when_empty(handler, self, *names)` beside `omit_when_null` and call it, so the two omission rules read as one family and the next one has an obvious home.
- **reviewer/tests/unit/test_ir_phases.py:78** (csharp-leftovers) - Two near-tautological cases in an otherwise strong file. `test_the_phases_member_needs_no_new_schema_version` asserts `SCHEMA_VERSION == "1.3.0"`, which restates the constant it imports and would need editing by the same hand that bumped it; `test_every_status_the_writer_can_record_is_accepted` parametrizes over the same four strings the `Literal` declares and asserts `row.status == status`. Neither can fail for a reason anyone cares about. The rest of the file (the negative cases, the golden-fixture sweep, the committed-contract validation) is genuinely load-bearing.
  - Fix: Keep them if they are wanted as executable documentation, but consider pointing the first at what it actually guards — e.g. assert the shipped golden fixtures' `schema_version` still equals `SCHEMA_VERSION`, which fails if someone bumps without regenerating the goldens.

## Feature 004 wiring round (2026-09-16)

- **reviewer/src/swreview/chat/remodel.py:905** (python-routes) - POST /remodel/runs/{job_id}/package-after is accepted whether or not the run has reached the rendezvous. _deliver validates only the path and the profile, then calls job.deliver(...), which sets job.package and job.arrived. A package-after posted before apply_changes finishes is returned by wait_for_package_after immediately and graded as the state after the last change — the exact outcome the wait_for_package_after docstring says must not happen ("returning a stale or empty one would grade the wrong tree"). backend-remodel.md calls this route "Hands the run the dump the add-in just wrote, ending the dump_after rendezvous". The pipeline only dumps on awaiting == "package_after", so this is not reachable in the normal flow, but the route is the enforcement point and nothing enforces it. The author appears to have considered this: test_stop_is_idempotent_and_sets_the_runs_flag asserts `status_code in {200, 409}`, a status the code never returns.
  - Fix: Refuse (409, or PackageAfterRefused) when job.state != JOB_AWAITING, and tighten the test's `in {200, 409}` to the single status that is actually contracted.
- **reviewer/src/swreview/chat/remodel.py:117** (python-routes) - DRY: PACKAGE_AFTER = "package-after.json" is redefined here while swreview.remodel.artifacts already exports the identical constant (artifacts.py:94) — and this module already imports NOT_MODEL_CHECK and carry_forward_exceptions from that very module. Both new test files import PACKAGE_AFTER from artifacts, so the product and its tests now read the name from two places. The stated constraint is "DRY ... move rather than copy". Same for MODEL_CHECK_PROFILE = "model_check" (line 120), a fourth spelling of a literal that artifacts.py:235, agent/runner.py:220 and plan.py:888 each spell for themselves.
  - Fix: Import PACKAGE_AFTER from swreview.remodel.artifacts alongside the two names already imported from it, and drop the local definition. Optionally hoist the "model_check" literal into one exported constant in artifacts.py and import it in all four places.
- **reviewer/src/swreview/chat/remodel.py:803** (python-routes) - Two issues in _plan. (a) DRY: plan_path(run_dir).write_text(plan.model_dump_json(indent=2) + "\n", encoding="utf-8") is a third copy of the plan-writing lines that already exist in remodel/runner.py:290-292 (plan_run) and remodel/plan.py:1416-1418 (record_state); plan_run then writes plan.json once and this immediately overwrites it. (b) POST /remodel/plan has no guard at all: re-planning a folder whose run is live rewrites plan.json back to `planned` with a fresh change list while apply_changes is running, so the poll's plan_state and changes_total flip mid-run and the run's own record_state then writes over the re-plan. Nothing in the routes refuses it.
  - Fix: (a) Give plan_run the run_id, copy_path and source arguments (or add one exported writer in plan.py) so the plan is written once by one function. (b) Refuse POST /remodel/plan when a live job holds the folder, and when changes.jsonl already exists, reusing the same RunInProgress / ResumeRefused classes; add the two route tests.
- **reviewer/src/swreview/chat/remodel.py:384** (python-routes) - RemodelJob.abandon() (called from shutdown(), line 986) sets `arrived` without a package, so wait_for_package_after falls into its timeout branch and the run dies with "package-after.json never arrived: the add-in had 600s to dump the copy after the last change and the run cannot be graded without it". At shutdown the add-in had whatever fraction of 600 s had elapsed, and the reason was the backend going away, not an expiry. That sentence reaches job.error, the pane and the log, and test_a_waiting_job_is_released_when_the_backend_shuts_down asserts only `state == "failed"`, so the wrong sentence is untested.
  - Fix: Have abandon() set a flag (or deliver a sentinel) that makes wait_for_package_after raise a distinct message naming the shutdown, and assert that sentence in the shutdown test.
- **reviewer/src/swreview/chat/remodel.py:695** (python-routes) - _probe coerces a missing probe_id to the empty string: "probe_id": str(_field(reply, "probe_id") or ""). The stated constraint is "unknown stays unknown", and this module already has _optional_text for exactly that (line 1006, "Unknown is not None, never ''"). An absent probe_id then travels to POST /remodel/open, where _text refuses it with a generic "probe_id is required" 400 instead of the route that actually saw the bridge answer nothing saying so.
  - Fix: Use _optional_text and refuse at the probe route (RunFolderFailed) when the bridge answered no probe_id, the way _signals already refuses a missing scope_signals block.
- **reviewer/src/swreview/chat/remodel.py:849** (python-routes) - Neither self.jobs nor self._workers is ever pruned. Every run leaves its RemodelJob and a live ThreadPoolExecutor (one OS thread each until it is shut down) in the dicts for the process's lifetime; _submit adds to _workers (line 976) and only shutdown() clears it. The backend is long-lived beside the add-in, so a session of many runs accumulates both. It also means the RunInProgress scan (once reordered per must-fix 1) walks a list that only grows.
  - Fix: In _play's finally, shut down and drop that job's executor from _workers; keep the job record (the pane still polls a finished run) but consider capping or evicting finished jobs, e.g. keeping the last N.
- **reviewer/tests/unit/test_chat_remodel_routes.py:1306** (python-routes) - test_the_run_route_builds_its_provider_only_through_the_runner proves only the negative half of its name. It patches swreview.cli.provider_factory to raise and asserts the run finished — which is the FR-045 "a run whose adapter cannot be built applies the plan anyway" property, not "the run reaches a model through remodel/runner.py::build_provider". Because the test bodies always pass provider: "fake" and build_provider deliberately refuses ProviderName.FAKE (runner.py:318), no test in either new file ever exercises the route actually constructing an adapter, so a regression that routed /remodel/runs through some other factory would still pass as long as the run finished.
  - Fix: Add one test that monkeypatches swreview.remodel.runner.build_provider with a recorder returning a scripted adapter, starts a run, and asserts the recorder was called exactly once with the settings the body named — the positive half the task text asks for.
- **reviewer/tests/unit/test_chat_remodel_routes.py:1259** (python-routes) - TestTheDoor asserts the missing-token 401 and the foreign-origin 403 for POST /remodel/probe only. The Guard is app-wide middleware so the property holds structurally today, but the feature 002 pane rule is that every route is behind the token, and the contract's own Tests section asks this file to hold the route table as data. test_every_route_of_the_contract_answers_something already iterates DOCUMENTED_ROUTES with a token; the unauthenticated case is not iterated.
  - Fix: Parametrize the 401 (and ideally the 403) over DOCUMENTED_ROUTES the way the smoke test already iterates them, so a route later mounted outside the Guard fails here.
- **reviewer/src/swreview/chat/remodel.py:741** (python-routes) - _open records only the copy path this module derived; it never compares it against the `document_path` the bridge answered. bridge-remodel.md:77 lists document_path in remodel.open's reply and step 11 of that contract has the host assert GetPathName() == copy_path, so the check exists one layer down — but open.json, plan.copy_path and every later VerifyTarget comparison are all built from the string this route composed rather than from the path the seat reports it actually opened. If the two ever diverge (normalization, a long-path form), nothing on the Python side notices.
  - Fix: Read document_path out of the reply and refuse with RunFolderFailed when it does not resolve equal to copy_path, the way _deliver compares resolved paths for the after-dump; it is two lines and it closes the loop the constitution's "re-verifies the target document's path" rule is about.
- **reviewer/src/swreview/chat/remodel.py:967** (python-routes) - `_submit` creates a `ThreadPoolExecutor(max_workers=1)` per job and stores it in `self._workers`, which is only ever cleared in `shutdown()`. `_play` never shuts its executor down, so every completed remodel run leaves an idle non-daemon worker thread and its executor alive for the life of the backend process. The `ChatServer._submit` pattern this reuses is not the same case: a chat session's executor is looked up and reused across many turns, whereas a remodel job is one-shot, so the remodel copy leaks one thread per run rather than per session.
  - Fix: In `_play`, wrap the body in try/finally and in the finally pop the job's executor out of `self._workers` and call `worker.shutdown(wait=False)` - the run is over by definition at that point. Keep `shutdown()` as it is for jobs still live at process exit.
- **reviewer/src/swreview/chat/remodel.py:371** (python-routes) - `shutdown()` calls `job.abandon()`, which sets `arrived` with `package` still `None`. `wait_for_package_after` then falls into the same branch as a real expiry and raises `PackageAfterTimeout` saying "package-after.json never arrived: the add-in had 600s to dump the copy after the last change" - which is false: the add-in had whatever fraction of a second elapsed before the backend was told to stop. That sentence is redacted onto `job.error` and read by the pane, so a shutdown is reported to the engineer as an add-in that was too slow. This is exactly the "unknown stays unknown" rule pointed the wrong way.
  - Fix: Give the job an `abandoned` flag (or have `abandon()` set a sentinel) and branch in `wait_for_package_after`: if the wait ended because the backend is shutting down, raise with a sentence that says so ("the backend shut down while this run was waiting for package-after.json"); keep the 600 s sentence for a genuine `arrived.wait()` expiry. `test_a_waiting_job_is_released_when_the_backend_shuts_down` already drives this path and can assert the new sentence.
- **reviewer/tests/unit/test_chat_remodel_routes.py:1306** (python-routes) - `test_the_run_route_builds_its_provider_only_through_the_runner` cannot fail. It monkeypatches `swreview.cli.provider_factory` (which no route touches), calls `run_to_completion` and asserts `state["state"] == "finished"`. Even if the route built a provider through some other path and it raised, `run_remodel` catches provider-construction exceptions (runner.py:888) and finishes the run anyway, so the assertion holds either way; it is a duplicate of `test_the_run_reaches_its_terminal_state_with_every_artifact` wearing a stronger docstring. The property it claims is only actually asserted in the integration file (`scripted == []`).
  - Fix: Spy rather than assert an outcome: monkeypatch `swreview.remodel.runner.build_provider` (and/or `swreview.agent.providers.get`) with a recorder, run to completion, and assert it was called exactly once with the settings the body named and that no other construction site fired. My review used precisely this spy and it is cheap: a plugin that wraps `providers.get` showed 15 calls, all `ProviderName.FAKE`.
- **reviewer/tests/unit/test_chat_remodel_routes.py:1284** (python-routes) - `test_the_eight_other_routes_run_with_every_provider_factory_raising` is named for eight routes and drives four (probe, open, plan, close). The four job-scoped routes - `GET /remodel/runs/{id}`, `GET .../events`, `POST .../package-after`, `POST .../stop` - are never exercised under `providers.get = refuse_provider`, so the task's "no provider is constructed on any route but /remodel/runs" is asserted for half the surface.
  - Fix: Either rename it to what it does, or extend it: start a run with the patched registry in place, then hit the four job routes and assert each answers 200 without the factory firing. Cheap, and it closes the enumeration the task text asks for.
- **reviewer/tests/unit/test_remodel_apply.py:801** (python-routes) - The wiring brief specifies the stop is read "after the change in flight has landed **or been inverted** and recorded", and `pane-remodel-messages.md` says `remodel.stop` "finishes the change in flight, inverts it if it failed". Every stop test drives the landed case only: `run(tmp_path, stop_after=N)` over the default script, where each change applies cleanly. There is no test where the change in flight fails, is rolled back, and only then does the stop end the loop - so the interaction between the rollback path and the new `stop_requested()` check at apply.py:1012 is unverified.
  - Fix: Add one case to `test_remodel_apply.py`: a script whose change N fails and inverts, with `stop_requested` raised during N. Assert the run is `truncated` with `stop.reason == "stopped"`, that change N's rollback line is on disk and terminal, that `in_flight is None`, and that nothing after N was attempted.
- **reviewer/src/swreview/chat/remodel.py:604** (python-routes) - `configuration` is documented in the body of both `POST /remodel/probe` and `POST /remodel/open` (backend-remodel.md, Routes table) and is dead on both. `probe` (line 592) never reads it at all. `open` reads it at 604 and writes it into `open.json` at 756, but nothing ever reads that key back: `_plan` pulls `scope_signals` and `probe_id`, `_start` pulls `rebuild_error_count`, `document_length_unit`, `which_configs`, `geometry_before` and `copy_path`, and the plan's own `configuration` comes from the package. A documented field that is accepted and discarded is surface nobody can rely on and drift the contract tests cannot see (they parse route paths and error classes, not body keys).
  - Fix: Pick one and be explicit: either pass it where it belongs (probe's configuration to `client.probe_scope`, open.json's to `plan_run`, if the planner should honour a named configuration), or drop it from the two body columns in `backend-remodel.md` and from `_open`'s signature. Given the owner decision that the plan's configuration is the copy's as measured, I'd drop it - and say in the contract that the active configuration is read from the copy, never posted.
- **reviewer/src/swreview/chat/remodel.py:283** (python-routes) - `_refusal` builds every bridge refusal's message from `str(exc)`, the host's own sentence, and the door's handler redacts it with `server.redact` - which masks only the secrets `create_app` was given (the process environment / `secrets=`). The per-request bridge secret arrives in the body and is in no redaction list, so a bridge error sentence that quoted it would be echoed verbatim in a `RunFolderFailed`/`ScopeRefused` body. The three secret tests all cover happy paths and artifacts; none covers an error body. The likelihood is low (the host composes those sentences and has no reason to quote the secret), but "the secret is never echoed" is currently a property of the host's good manners rather than of this module.
  - Fix: Redact the request's own bridge secret on the way out: capture it in `_client` and mask it in the message of any `ChatError` raised while serving that request (a small context-local redactor composed with `self.redact` is enough). Add one test: a `RemodelError` whose sentence contains `SECRET`, asserting `SECRET not in response.text`.
- **extractor/SwReview.AddIn/Remodel/RemodelBackendClient.cs:273** (csharp-pipeline) - Call, Failed, Read, Parse, Text and Flag (lines 273-403) are a byte-for-byte copy of Review/BackendClient.cs:277-398, including the 30 s timeout pair, `request.Proxy = null`, the bearer header, the BackendUnavailable/HttpError/BadResponse vocabulary and the comment wording. That is ~130 duplicated lines of the transport that carries the token, in a task whose constraints say "DRY ... move rather than copy". The brief's "exactly the way Review/BackendClient.cs does it" names the conventions to keep, not a mandate to paste them. Two copies means the next change to loopback auth or timeout handling has to be made twice and a divergence is invisible to both test suites.
  - Fix: Extract the six members into one internal helper (e.g. AddIn/Review/BackendTransport.cs: `JsonElement Call(Func<BackendEndpoint?>, method, path, body)` plus the Text/Flag/Count readers) and have both BackendClient and RemodelBackendClient call it. BackendClient keeps its supervisor, log-folder and job-object duties; its existing tests are the regression proof that the extraction changed nothing. If the owner prefers not to touch feature 002 code, the honest alternative is to leave the copy and say so in a comment - but flag it now rather than after a third client appears.
- **extractor/SwReview.AddIn/Remodel/SwRemodelSeat.cs:83** (csharp-pipeline) - The brief's IRemodelSeat row ends "Offline test over a fake" and there is no test of SwRemodelSeat at all - the class documents itself as untested like SwReviewDump. The consequence is that the very property this review is asked to verify (OpenDoc7 with Silent|LoadModel = 17, never ReadOnly/ViewOnly) is asserted only by a comment. It is also stated twice in the codebase now: as booleans here and as the integer RemodelCopy.OpenOptions in the extractor, with nothing tying them together, so a future edit that sets ReadOnly or LightWeight here would be caught by nothing.
  - Fix: Extract `internal static void Configure(IDocumentSpecification spec)` (DocumentType, Silent, LoadModel, ReadOnly=false, ViewOnly=false) and test it over a small fake IDocumentSpecification - the interface is ~27 plain properties, so a fake is a few lines: assert Silent && LoadModel && !ReadOnly && !ViewOnly, and assert the equivalence to RemodelCopy.OpenOptions by composing the flags back into an int and comparing to 17. The ActivateDoc3-vs-open branch can be covered the same way over a tiny seam if the owner wants it; the options are the part that matters.
- **extractor/SwReview.AddIn/Remodel/BackendRemodelPipeline.cs:185** (csharp-pipeline) - OpenCopy drops reply.CopyPresent on the floor: RemodelCopyReading carries only the path and the count, so the host decides the PreexistingRebuildErrors path from the count alone. backend-remodel.md makes copy_present the field that says whether a copy exists on disk. If the bridge refusal carries a detail count of 0 (or any build answers copy_present:false with a zero count), the host sees "0 rebuild errors" and proceeds to plan and run against a run folder with no copy in it - the dump then reads whatever is active, which is the source (see must_fix 1). Parsing a contract field and discarding it is also the kind of silent drift the rest of this class is careful to avoid.
  - Fix: Carry copy_present into RemodelCopyReading (or refuse in the pipeline when copy_present is false: throw RemodelRefusal("PreexistingRebuildErrors", ...) naming the count, unknown when the count is null), and test that copy_present:false with rebuild_error_count:0 refuses rather than planning.
- **extractor/SwReview.AddIn/Remodel/RemodelBackendClient.cs:156** (csharp-pipeline) - changes_total and changes_applied use `Count(...) ?? 0`, so a field that is absent, null or not an int becomes 0, while rebuild_error_count on the very same client is carefully kept null-as-unknown with a comment explaining why. After a stop, that 0 is what reaches remodel.stopped {changes_applied} through RemodelRunOutcome, so a malformed or truncated status body tells the engineer that nothing was applied to a copy that may hold a dozen applied changes. The task's "unknown stays unknown" constraint is the same rule the rebuild count follows.
  - Fix: Either make the counts nullable through RemodelRunStatus and report unknown (the host can say "the run applied an unknown number of changes; the change log says which"), or treat a status body missing either count as BadResponse the way a missing copy_path and a missing job_id already are. Add the test beside StatusGetsTheJobAndReadsEveryFieldOfIt.
- **extractor/SwReview.AddIn/Remodel/BackendRemodelPipeline.cs:78** (csharp-pipeline) - StageForPlanState maps the terminal plan state `truncated` to the stage `saving`, so a run the engineer just stopped posts "Saving the copy..." - and `saved` maps to `saving` too, so a run that has already saved says it is saving. The brief lists the mapping as judging/applying/verifying/saving with terminal states going to ready or error, and the class's own comment says terminal states post no stage at all, which `failed` and `discarded` honour. truncated is terminal in data-model.md section 11.
  - Fix: Drop the `truncated` row so a truncated run posts no stage and the host's closing ready/error is the last word (its message already names the state), or keep it and reword the sentence so it is true of both ("Finalizing the copy..."). Extend EveryStageTheRunPostsIsOnTheContractsClosedList with a truncated poll so the choice is pinned.
- **extractor/SwReview.AddIn/Remodel/BackendRemodelPipeline.cs:173** (csharp-pipeline) - Two different "copying" statuses are posted for one moment: RemodelHost.cs:467-469 posts `Copying {RunFolders.DocumentName(document.Path)} into the run folder...` and then OpenCopy posts `Copying {Path.GetFileName(request.SourcePath)} into the run folder...`. The page shows the same stage twice with two spellings of the file name from two different helpers. The brief does ask OpenCopy for a copying status, so the duplicate is the host's or the pipeline's, not both.
  - Fix: Pick one owner. The pipeline's is the one the brief names, so delete the host's pre-copy PostStatus (it is one line and RemodelHostTests is the place to record the change), or keep the host's and drop the pipeline's. Whichever survives should use one helper for the file name.
- **extractor/SwReview.AddIn/Remodel/BackendRemodelPipeline.cs:470** (csharp-pipeline) - The comment says the profile is read back "out of the file that was written", but the code calls PackageIndex.HeadOf(Path.GetDirectoryName(written)), which re-derives <dir>/package.json by name and ignores `written` entirely. Today they are the same file only because PackageWriter happens to name it package.json; if a dump ever returns a differently named or nested package, this silently validates the profile of a stale neighbour file and then File.Move's the real one into place unchecked.
  - Fix: Assert that Path.GetFileName(written) == PackageWriter.PackageFileName before the HeadOf call, or add a HeadOf overload that takes the file path, so the code does what the comment claims.
- **extractor/SwReview.AddIn/Remodel/BackendRemodelPipeline.cs:105** (csharp-pipeline) - _probeId is set by ProbeScope and never cleared. If OpenCopy is ever reached without a fresh probe in this process - a host path added later, or a probe that threw after an earlier run succeeded - a probe id minted for a different source travels in the open body. The bridge refuses it (scope_not_probed), so the part is safe, but the refusal the engineer reads is a scope error rather than the real cause, and the property is currently guaranteed by the host's call order rather than by this class.
  - Fix: Clear _probeId at the start of ProbeScope and again after a successful OpenCopy, and add a test that OpenCopy with no preceding probe sends probe_id: null (which the bridge refuses by name) rather than a stale id.
- **extractor/SwReview.AddIn.Tests/RemodelBackendClientTests.cs:62** (csharp-pipeline) - T134d asks for 'method, path, token header and body per route'. Method, path and body are asserted on all nine routes, but the bearer header is asserted on only three: probe (:62), status (:281) and probe+close inside NeitherTheTokenNorTheBridgeSecretEverReachesAUrl (:502). /remodel/open, /remodel/plan, /remodel/runs, the events GET, package-after and stop have no token-header assertion. Failure scenario: a future edit that builds one of those six requests without `request.Headers["Authorization"]` (or that puts the token in the query string for the GET routes, which is exactly the mistake the Guard middleware and chat-api.md warn about) passes the whole suite green, and the regression only shows up as a 401 against the live backend.
  - Fix: Cheapest DRY fix: in FakeBackend.Serve, assert the header centrally — record it as today but add `Assert.Equal("Bearer " + Token, context.Request.Headers["Authorization"])` at the top of each per-route test's `RecordedRequest` check via a shared helper, or extend NeitherTheTokenNorTheBridgeSecretEverReachesAUrl to drive all nine routes in one pass and assert header-present + token-absent-from-URL for each. The second is one test and covers every route.
- **extractor/SwReview.AddIn/Remodel/IRemodelBackend.cs:266** (csharp-pipeline) - `RemodelEventPage Events(string jobId, int after)` is declared on the interface, implemented in RemodelBackendClient.cs:162, faked in BackendRemodelPipelineTests.cs:784 and covered by RemodelBackendClientTests.cs:322 — but `grep -rn '\.Events(' extractor/SwReview.AddIn --include=*.cs` returns nothing. Neither BackendRemodelPipeline nor RemodelHost ever calls it. The wiring brief and backend-remodel.md are explicit that the page reads the event stream itself with the token and origin from `init`, so there is no caller by design. This is speculative surface (CLAUDE.md section 2: no abstractions for single-use code, nothing beyond what was asked) carrying its own tests, which makes it look load-bearing to the next reader.
  - Fix: Either delete the member, its implementation, its fake and its test (the page owns the stream), or name the one concrete add-in caller that needs it and wire that caller now. If it is kept deliberately as the add-in's escape hatch, say so in the XML doc in one sentence that states no production caller exists yet — but the DRY/YAGNI-consistent choice here is to remove it.
- **extractor/SwReview.AddIn/Remodel/SwRemodelSeat.cs:94** (csharp-pipeline) - The open options are hard-coded as four independent booleans (`Silent = true; LoadModel = true; ReadOnly = false; ViewOnly = false`) with a comment asserting they are 'the two halves of RemodelCopy.OpenOptions (Silent | LoadModel = 17)'. Nothing in the code references `RemodelCopy.OpenOptions`, and SwRemodelSeat has zero tests, so the equivalence is a comment. The extractor pins 17 and the absence of the ReadOnly(2)/ViewOnly(4) bits (RemodelCopyTests.cs:462-466, RemodelOpenHandlerTests.cs:284), but the add-in's own OpenDoc7 path is outside that guard. Failure scenario: someone changes RemodelCopy.OpenOptions (say, adds RapidDraft) or flips `ReadOnly = true` while chasing a file-lock bug; the extractor tests stay green, the add-in tests stay green, and the engineer gets a copy they cannot Save As — the one thing the run exists to let them do. The brief asked for an 'Offline test over a fake' for this class and none was written.
  - Fix: Derive rather than restate: `specification.Silent = (RemodelCopy.OpenOptions & 1) != 0; specification.LoadModel = (RemodelCopy.OpenOptions & 16) != 0; specification.ReadOnly = (RemodelCopy.OpenOptions & 2) != 0; specification.ViewOnly = (RemodelCopy.OpenOptions & 4) != 0;` so the extractor's existing `TheOpenOptionsComposeToSeventeenAndNeverCarryReadOnlyOrViewOnly` becomes the guard for the seat too, and the seat cannot drift from the bridge. I would not build a full `ISldWorks` fake for this — the codebase's stated convention (SwReviewDump, cited in this file's own doc comment) is that interop-only classes are compiled, not unit tested — so the derivation is the whole fix.
- **extractor/SwReview.AddIn.Tests/BackendRemodelPipelineTests.cs:553** (csharp-pipeline) - TheCallsGoInTheOrderTheContractStatesThem asserts `world.Backend.Calls.Select(call => call.Route).Distinct()` equals `{probe, open, plan, runs, status, close}`. `.Distinct()` collapses repeats, so the test proves only the order of first occurrences. Failure scenario: a change that re-posts `open` after `plan`, or that calls `probe` a second time inside Run (re-probing the source mid-run — precisely the thing the 'source named once' rule forbids), still passes this test because the duplicate route is distinct-ed away; only the separate TheSourceIsNamedInNoCall... test would catch the probe case, and nothing catches a duplicate open.
  - Fix: Assert the raw sequence with only the expected repetition collapsed — e.g. build the expected list from the actual poll count, or assert `Assert.Equal(1, Calls.Count(c => c.Route == "open"))` and the same for probe/plan/runs/close alongside the Distinct order check. One extra line per route and the test then means what its name says.
- **extractor/SwReview.AddIn/Remodel/BackendRemodelPipeline.cs:543** (csharp-pipeline) - `RequireBackend()` raises `RemodelRefusal("BackendUnavailable", ...)`, which travels to the page as `error {error_class: "BackendUnavailable"}`. That class appears in no spec file at all (`grep -rn BackendUnavailable specs/` is empty): it is on neither pane-remodel-messages.md's 'Refusal classes' table nor backend-remodel.md's 'Error classes' table, both of which the contracts describe as closed lists, and RemodelPageContractTests has no error-class closed-list check to catch it. It is consistent with feature 002 practice (Review/BackendClient.cs:283, ReviewHost.cs:654), so this is a documentation gap rather than a behavioural bug — but the page renders whatever class arrives and the tab's refusal vocabulary is meant to be enumerable.
  - Fix: Add a `BackendUnavailable` row to pane-remodel-messages.md's refusal table alongside the other host-owned refusals (NoDocument, RunNotFound, CopyDiscarded), since that is where 'the host's own refusals' live per backend-remodel.md's closing paragraph. Note that pane-remodel-messages.md is an existing contract file and the brief says 'no existing contract file changes', so this needs your call: either amend the contract, or reuse `BridgeUnavailable` (already on backend-remodel.md at 502, 'the call was never sent'), which fits a dead backend exactly and needs no contract edit. I recommend the latter given the brief's constraint, but flag it rather than assume.
- **extractor/SwReview.AddIn/Remodel/RemodelHost.cs:1143** (wiring) - `DeleteCopyFolder` tolerates a missing folder but not an undeletable one, and the `PreexistingRebuildErrors` callers do not wrap it. `Directory.Delete(folder, recursive: true)` is unguarded; the two Plan callers (lines 497 and 510) call it outside any try, so an `IOException`/`UnauthorizedAccessException` escapes `Plan`, is caught by `Receive`'s generic handler (line 229) and answers `HostError` with a Win32 sentence. The contract-named refusal the engineer needs never arrives. This is reachable: the preceding `pipeline.CloseCopy` failure is deliberately swallowed (lines 1134-1139), so a tool service that is not listening leaves SOLIDWORKS holding `copy\<part>-RMS.SLDPRT` open and the delete fails. `DiscardCopy` (line 906) does wrap it and answers `DiscardFailed`, so the two callers of one method behave differently on the same failure - the asymmetry is the tell.
  - Fix: Wrap the delete (or the call at 497/510) so the named refusal still goes out: catch the exception in `DeleteCopyFolder`, return whether the folder went, and have the `PreexistingRebuildErrors` handlers still send `PreexistingRebuildErrors` with a sentence that adds "the copy could not be deleted: <reason>". Add a test with a delete failure (a read-only/held file, or a seam on the delete) asserting the class is still `PreexistingRebuildErrors` and not `HostError`.
- **extractor/SwReview.AddIn/SwReviewAddIn.cs:280** (wiring) - Dispose symmetry with the Model check host is claimed but not achieved for a run in flight. The Model check host does its work on the pump thread, so `StopModelCheckPump`'s 2 s join really does bound it. The remodel run is handed to `RemodelHostOptions.Schedule` (`Task.Run`, RemodelHost.cs:75) precisely so it is off the pump - so `StopRemodelPump` bounds nothing, and `RemodelHost.Dispose` (RemodelHost.cs:267-272) only clears `_runs`, `LatestRun` and `_active`; it never calls `run.RequestStop()`. `DisconnectFromSW` then continues to `_applicationThread = null`, `_pane.Dispose()`, `Marshal.ReleaseComObject(_taskPane)`, `_swApp = null` and `GC.Collect()` while the worker is still inside `pipeline.Run` - still polling the backend, still calling `SwReviewDump.Run` on the application thread it just dropped, and still able to drive a save of the copy through the bridge. Failure scenario: engineer disables the add-in (or SOLIDWORKS unloads it) during a twenty-minute run; the run keeps touching a session whose pointers have been released.
  - Fix: Have `RemodelHost.Dispose` request the stop on `_active` before it clears it (the flag already exists and the executor already finishes the change in flight, inverts it if it failed and finalizes - exactly the shutdown behaviour wanted), and give the add-in a bounded wait for the executor to finish, the way `StopRemodelPump` bounds the pump, before `_applicationThread`/`_swApp` are nulled. Pin it with a test: dispose while a run is in flight, assert the run saw `StopRequested`.
- **extractor/SwReview.AddIn.Tests/RemodelHostTests.cs:1647** (wiring) - The `CloseCopy` half of `DeleteCopyFolder`'s tolerance is asserted only by a comment. `FakePipeline.CloseCopy` can never throw (it just records "close"), so the swallow at RemodelHost.cs:1134-1139 - whose comment says "A copy that is not open, or a SOLIDWORKS that will not answer, must not stop the folder being deleted" - is untested in both callers. T134g names "DeleteCopyFolder tolerating a copy the bridge already deleted", and the missing-folder half is covered (`ACopyTheBridgeHasAlreadyDeletedIsStillRefusedByNameAndThrowsNothing`, line 380) but the won't-close half is not.
  - Fix: Add `CloseFailure` to `FakePipeline` and two cases: a `PreexistingRebuildErrors` plan where `CloseCopy` throws still deletes `copy/` and still answers `PreexistingRebuildErrors`; a `remodel.discard_copy` where `CloseCopy` throws still deletes and still replies `ok {kept}`.
- **extractor/SwReview.AddIn/SwReviewAddIn.cs:626** (wiring) - The new secret in the wiring is never pinned by a test. `Secrets` now returns the API key and `_toolService?.RemodelBridge?.Secret`, which is the one genuinely new redaction input in T134f (the remodel bridge secret authorizes every write to the copy). `RemodelWorld.Open` (RemodelHostTests.cs:1420) configures `Secrets = () => new[] { Endpoint?.Token }` only, so `TheTokenNeverReachesThePageInAStatusOrAnError` (line 143) proves one secret is masked and nothing proves a second one in the list is, nor that a null entry (the ordinary state before the tool service listens) does not break redaction. `SwReviewAddIn` itself is untestable headless by the project's own precedent (ToolServiceWiringTests.cs:22), so the host-side test is the only place this can be held.
  - Fix: Give `RemodelWorld` a second secret (`Secrets = () => new[] { Endpoint?.Token, BridgeSecret }` with `BridgeSecret` nullable by default) and assert that a pipeline failure message quoting the bridge secret reaches the page masked, with one case where the second entry is null.
- **specs/004-resilient-remodeler/contracts/backend-remodel.md:129** (wiring) - Two refusal classes the Remodel page can receive are in no contract. `backend-remodel.md` line 129 enumerates the host's own classes as `NoDocument`, `NotAttached`, `DocumentReadOnly`, `RunNotFound`, `CopyDiscarded` plus `PreexistingRebuildErrors`, and `pane-remodel-messages.md`'s refusal table lists eleven - but `RemodelHost` also sends `OpenFailed` (line 875) and `DiscardFailed` (lines 908, 924), and `RemodelHostTests` asserts `OpenFailed` at line 552. Neither string appears anywhere under specs/. Since T134a/T134g's whole mechanism is "an error class on either side alone is a failure", the gap is invisible to exactly the test built to catch it. (`HostError` is the same shape but is repo-wide and pre-existing across ReviewHost, ModelCheckHost and TerminalHost, so I am not counting it against this change.)
  - Fix: Add `OpenFailed` and `DiscardFailed` to the sentence at backend-remodel.md line 129 (or to pane-remodel-messages.md's table if you prefer them beside the rest, which reopens the "no existing contract file changes" question), so the contract-parsing test covers the host's full set. No code change.
- **extractor/SwReview.AddIn/SwReviewAddIn.cs:760** (wiring) - The new `_remodelHost?.PostStatus(stage, message)` line puts the Remodel page on a fan-out that emits an off-contract stage. `PostStatusToPages("backend_starting", ...)` at line 731 sends `status {stage: "backend_starting"}`, but `specs/004-resilient-remodeler/contracts/pane-remodel-messages.md:67` defines a CLOSED list of nine stages (`copying|dumping|planning|judging|applying|verifying|saving|ready|error`) that does not contain `backend_starting` — unlike `specs/003-resilient-modeling/contracts/model-check.md:122` and `specs/002-task-pane-assistant/contracts/pane-host-messages.md:71`, which both list it explicitly. `BackendRemodelPipelineTests.EveryStageTheRunPostsIsOnTheContractsClosedList` (RemodelHostTests' sibling, line 374) enforces that list for the pipeline and asserts `stages.Count == 9`, so the project already treats the list as closed; the wiring is the one place that breaks it. Not a crash — `remodel.js:882` only text-renders an unrecognised stage — but the contract and the product now disagree, and nothing tests it.
  - Fix: Keep the fan-out but do not relay `backend_starting` to the Remodel host (the page only needs the follow-up `ready` at line 733, which `remodel.js:896` already uses to re-request `init`), e.g. `if (stage != "backend_starting") _remodelHost?.PostStatus(...)` with a one-line comment naming the contract row. The brief's 'What stays out' forbids changing existing contract files, so amending pane-remodel-messages.md is not the option here. Add one RemodelHostTests case asserting every stage the host itself posts is on the contract's closed list, read out of the contract the way BackendRemodelPipelineTests.ContractStages() (line 650) reads it.
- **extractor/SwReview.AddIn/Remodel/RemodelHost.cs:267** (wiring) - `Dispose()` clears `_runs`, `LatestRun` and `_active` but never calls `run.RequestStop()` on the run in flight, and this is the one place the Model check symmetry does not hold. `ModelCheckHost.Dispose` (ModelCheck.cs:228) is equally minimal, but a check runs synchronously on its pump thread, so stopping the pump stops the work. A remodel run runs on `RemodelHostOptions.Schedule` (default `Task.Run`, line 75), so `DisconnectFromSW` — which stops the remodel pump with a 2 s bounded join (SwReviewAddIn.cs:686), then disposes the tool service, the backend and the hosts — leaves a run the feature bounds at twenty minutes executing against a SOLIDWORKS session, a bridge and a backend child process that are all being torn down underneath it. `Dispose` setting `_active = null` makes it worse: the run's own `RemodelHost.DocumentChanged` abort path (line 250) and `Stop` (line 762) can no longer reach it, so the executor never gets the flag that makes it finish the change in flight, invert it if it failed, and finalize the artifacts — exactly the RK-14 guarantee the host implements everywhere else. Untested: no test in RemodelHostTests.cs exercises Dispose with a run in flight.
  - Fix: In `Dispose()`, before clearing state: `_active?.RequestStop();` (one line, same call the RK-14 path at line 256 makes). Add a RemodelHostTests case using a `DuringRun` hook that disposes the host mid-run and asserts the reporter saw the stop flag set, so the change is pinned rather than commented.
- **extractor/SwReview.AddIn.Tests/RemodelHostTests.cs:536** (wiring) - Test-strength gap on T134g's first named case, the `RemodelRefusal` mapping. `SendRefusal` (RemodelHost.cs:1292) passes three fields through — class, message and `Retryable` — but only the class and message are pinned in both directions. `retryable` is asserted exactly once for a refusal, at line 447, and there the expected value is `true`. The two tests that construct `RemodelRefusal(..., retryable: false)` (line 514 for the executor, line 543 for `open_copy`) assert only `ErrorClass`. So a `SendRefusal` that hardcoded `retryable: true` — or dropped the parameter and let PaneActions default it to true — passes the entire suite. The `retryable` flag is what the page uses to decide whether the button is pressable again, so a stuck-true value is a user-visible wrong answer, not cosmetic.
  - Fix: Add `Assert.False(world.Reply("error", "o1").GetProperty("retryable").GetBoolean());` to `AnRefusalRaisedWhileOpeningTheCopyIsAnsweredWithItsOwnClass` (line 536) — one line, no new fixture, and it makes the pair at 447/536 pin the field in both directions.
- **extractor/SwReview.AddIn/Remodel/BackendRemodelPipeline.cs:464** (wiring) - Out of my scope (T134e), but it is the one place the 'nothing reaches the source document' property is enforced by assumption rather than by check, so reporting it rather than letting it pass. `DumpInto` calls `_dump.Run(runDirectory, ..., DumpProfile.ModelCheck)`, which dumps whatever SOLIDWORKS says is the ACTIVE document, and then verifies only the PROFILE of the file that came back (`PackageIndex.HeadOf(...).Profile != DumpProfile.ModelCheck`). It never verifies the dumped document's path is the copy. If the active document is the source at that moment — the engineer clicks the source's window between `remodel.open` and the dump, or the bridge's open did not activate — `package-before.json` is a reading of the SOURCE, the plan is built against the source's feature tree, and it is then applied to the copy. No write reaches the source, so the constitution's hard rule holds, but the run is silently wrong and the artifacts claim otherwise. `OpenCopy` already knows the copy's path (`RemodelCopyReading.CopyPath`), so the check is cheap.
  - Fix: Hand `DumpInto` the expected copy path and assert the written package's document path (or `DumpSummary`'s own document path) matches it, throwing the same shaped `InvalidOperationException` the profile mismatch throws. Add the case to BackendRemodelPipelineTests beside the existing profile-mismatch test.
- **extractor/SwReview.AddIn/SwReviewAddIn.cs:585** (wiring) - T134f itself has no test. `StartRemodelHost`, `OnRemodelPageMessage`, `StartRemodelPump`, `StopRemodelPump`, the `-=` at line 248, the `Dispose` at line 280 and the fan-out at line 760 are referenced by nothing in SwReview.AddIn.Tests (grep confirms: the only test-project references to `SwReviewAddIn` are the COM-attribute and assembly-redirect tests). Every behaviour the task names is therefore verified only by reading. This is precedent-consistent — `StartModelCheckHost` and the `ModelCheckPageMessageReceived` subscription are equally untested, and ToolServiceWiringTests.cs:22 states the reason ('None of this can be tested through SwReviewAddIn itself') — so I am not calling it a defect of this task. But it means the subscribe/unsubscribe/dispose symmetry I verified by inspection has no regression guard, and the `backend_starting` finding above is exactly the kind of thing such a guard would have caught.
  - Fix: Either accept it explicitly as the established pattern, or do what ToolServiceWiringTests did for the tool-service delegates: extract the three or four delegates `StartRemodelHost` composes (the endpoint func, the bridge func, the `RegisterLatestRun` action, the `Secrets` func) into a small testable factory and assert their behaviour there. I would not do this now — it is scope the task did not ask for — but it should be an owner decision rather than an omission.
- **extractor/SwReview.AddIn/Remodel/RemodelHost.cs** (wiring, fixer note) - `Execute`'s `catch (RemodelRefusal)` synthesizes `RemodelRunOutcome("failed", 0)`, so `remodel.stopped {changes_applied}` reports 0 after a refusal late in a run even though changes landed; the pipeline cannot hand the applied count out through an exception.
  - Fix: carry the applied count on `RemodelRefusal` (or read it from `changes.jsonl` in the catch) so the closing message states what landed.
- **reviewer/src/swreview/chat/remodel.py** (python-routes, author note) - `copy_path_for` restates the C# `RemodelCopy.CopyPathFor` convention (`<run_dir>/copy/<stem>-RMS.SLDPRT`) in a second language; the two can drift silently.
  - Fix: have `remodel.open`'s reply carry the copy path the bridge actually created and drop the Python derivation, or pin both with one shared fixture.

## Feature 005 spec review should-fix (2026-09-16)

- **specs/005-llm-efficiency/data-model.md** - The "Read when" column of the flag table says `coverage_stop` is read "per round" and `prompt_cache_key` "per request", contradicting contracts/levers.md section 1 ("**Read once at `start_review`, never per round.** A flag read mid-session changes the request shape mid-session"), levers.md's own lev Fix: Change those two cells to "once at `start_review`; applied per request" and "once at `start_review`; applied per round" so the read point matches levers.md section 1 for all ten flags.
- **specs/005-llm-efficiency/contracts/README.md** - The contract-edit register does not match data-model.md section 12, which is the same register. data-model.md schedules `Finding.carried_over_from`, `carried_over_at`, `carry_over_key` and `reused_from` onto `review-session.schema.json` in **Phase 4** and `extractor.phases` onto `ir.schema.json` in  Fix: Rewrite the README register rows to mirror data-model.md section 12 exactly, with a phase column per edit (session: usage + efficiency in Phase 1, the three Finding fields and `reused_from` in Phase 4; IR: `extractor.phases` in Phase 1b, the reuse fields in Phase 4; scorecard: the full section 8.1/8
- **specs/005-llm-efficiency/tasks.md** - FR-054 ("The unresolved count MUST be reported as two numbers, unresolved-because-withheld and unresolved-for-any-other-reason") and SC-013 have no implementing task. data-model.md section 8.1, contracts/usage.md section 9 and contracts/ab-harness.md sections 4 and 6 all require `unresolved_because_ Fix: Add the two fields to T023/T024 (or add a lever 4 task beside T062) that writes `unresolved_because_withheld`/`unresolved_other` in `benchmark/scorecard.py` from the `WithheldTool`-written coverage items, and add both columns to T039's asserted raw-row list.
- **specs/005-llm-efficiency/tasks.md** - The phase label `1b` names two different things. plan.md's Delivery order table defines "**1b. Standalone fixes**" (the zero-bodies guard and dump phase timing) as a phase, and data-model.md section 12 schedules the `extractor.phases` IR edit into "1b (phases)"; in tasks.md, section 1b is the usage  Fix: Rename the tasks.md subsections so the standalone fixes carry the 1b label (or rename plan.md's phase to "1-fixes"), and make data-model.md section 12's phase cell point at the task ids (T032, T033) rather than at a label that means two things.
- **specs/005-llm-efficiency/quickstart.md** - Scenarios 3c, 3d, 4a, 4b and 4c run the six arms but never call `swreview benchmark score`, although each then states an "Expected decision row". contracts/ab-harness.md section 1 and section 3 make `scorecard.json` an input `compare` reads per run, and section 3 lists "the scorecard exists" among t Fix: Add `uv run swreview benchmark score "$runs\lever0N-<arm>-$rep" --answer-keys $keys` after each `benchmark run` in Scenarios 3c, 3d, 4a, 4b and 4c, matching Scenario 3a, and add the `benchmark compare` invocation those scenarios' decision rows come from.
- **specs/005-llm-efficiency/contracts/ab-harness.md** - The ledger's lever identifier has two spellings. data-model.md section 8.3 defines the `lever` column as "Which lever this arm is measuring, e.g. `lever06`", and section 8.4's decision row plus the checkpoints in tasks.md refer to levers by number; this example writes the flag name instead. data-mod Fix: Pick one identifier for the `lever` column (the flag name is the one the CLI already uses) and make data-model.md section 8.3, section 8.4 and the ab-harness.md example agree, stating once how the baseline study's `lever: "none"` fits that vocabulary.
- **specs/005-llm-efficiency/data-model.md** - `cached_input_share` has two declared single homes. Section 2.1 makes it a derived property of `TokenUsage` defined "once, here and in code, and nowhere else", and section 8.1 sources `PackageScore.cached_input_share` from "`usage.cached_input_share` (section 2.1)"; but section 8.3 says `cached_shar Fix: Choose one owner (the `TokenUsage` property is the one that also serves the report and the pane) and make usage.md section 2 declare `cached_input_share` alongside `uncached_input_tokens`, with data-model.md section 8.3 and ab-harness.md section 4 saying the scorecard reads that property rather than
- **specs/005-llm-efficiency/contracts/levers.md** - The cited precedent does not exist on the OpenAI path. `cli.provider_factory` (cli.py:202-228) passes `max_output_tokens=output_ceiling(...)` only on the Gemini branch (:224); the OpenAI branch is `return adapter(model=settings.model, **settings.client_kwargs())` (:228), and `ProviderSettings.client Fix: State the real seam: `provider_factory` must gain an `efficiency: EfficiencySettings | None = None` parameter (and the `Callable[[ProviderSettings], AgentProvider]` alias in `chat/server.py` and `benchmark/runner.py` must widen with it), and `OpenAIProvider.__init__` gains `parallel_tool_calls: bool
- **specs/005-llm-efficiency/contracts/tool-tiers.md** - The package states this same VERIFIED measurement as two different numbers. tool-tiers.md §3, spec.md:74 ("the saving is 8,087 bytes or 24 percent of the payload (VERIFIED, measured on this tree)") and tasks.md T060/T063 say 8,087 / 24 percent; data-model.md:688 and :690 and research.md:749 say "**8 Fix: Change 8,087/24 percent to 8,093/23.8 percent in tool-tiers.md §3 and §8, spec.md:74, and tasks.md T060 and T063, so all five documents quote the number the tree produces.
- **specs/005-llm-efficiency/tasks.md** - The Gemini and with-bridge byte totals do not reproduce against the tree, and T001 pins them as test assertions. Measured here: OpenAI compact array 34,065 (exact match), tool descriptions 14,685 (exact), every description blanked 15,274 (exact), per-group 10,410 / 3,706 / 14,182 / 5,770 / 3,648 (al Fix: Re-run the regenerating script and replace 34,248 with the value the chosen Gemini encoding actually produces (34,281 for the adapter's own `FunctionDeclaration` dump) and 37,709 with 37,712, in T001, plan.md key point 15, spec.md:62, tool-tiers.md §2 and quickstart.md §1a; and state the exact seria
- **specs/005-llm-efficiency/plan.md** - Wrong location and wrong docstring. The sentence "every `function_call` in the echoed history needs a matching `function_call_output` or the next request is rejected" is at `reviewer/src/swreview/agent/providers/openai_provider.py:107-108`, and it is the attached docstring of the `BUDGET_EXHAUSTED`  Fix: Change both citations to `openai_provider.py:107-108` and drop "module" — write "the `BUDGET_EXHAUSTED` constant's docstring says plainly ...".
- **specs/005-llm-efficiency/plan.md** - The cited range does not contain `comparison_response_id`. `openai/types/responses/response_create_params.py:158-177` is the request TypedDict's `prompt_cache_key` and `prompt_cache_options` fields with their docstrings; the docstring at :165-176 never mentions `comparison_response_id`. The field is Fix: Change both citations to `response_create_params.py:385-396` (field at `:391`), which is where `PromptCacheOptions`, its `comparison_response_id`, `mode` and the `gpt-5.6` sentence actually live.
- **specs/005-llm-efficiency/contracts/ab-harness.md** - `ir/models.py:487` is `)` closing `Interference.volume`'s `Field(...)` — line 480-495 is `class Interference`. `SuppressTestRow` is at `ir/models.py:498-515` and its `elapsed_ms: int | None = Field(ge=0)` is at `:515`. The same wrong citation appears in research.md:1477 and tasks.md T033, which is t Fix: Replace `ir/models.py:487` with `ir/models.py:515` in ab-harness.md §7, research.md:1477 and tasks.md T033; `ir/models.py:245` with `:248` in data-model.md:537; `ir/models.py:508-560` with `:518-570` in data-model.md:634, levers.md §6 and tasks.md T106.
- **specs/005-llm-efficiency/plan.md** - Contradicted by this feature's own documents. data-model.md §12 lists `001/contracts/ir.schema.json` as gaining `ManifestEntry.file_modified_utc`, `file_size_bytes`, `EvidencePackage.reuse_key`, `reused_from`, `reused_at`, `extractor.completed` **and** `extractor.phases` (dump timing), with the Phas Fix: Rewrite the Principle IV row as: "The IR moves twice: `extractor.phases` in Phase 1b (standalone, ungated) and `ManifestEntry.file_modified_utc`, `file_size_bytes`, `EvidencePackage.reuse_key`, `reused_from`, `reused_at` and `extractor.completed` for Tier 3 lever 9; one minor bump to 1.3.0 covers bo
- **specs/005-llm-efficiency/contracts/levers.md** - The two rows above it cite the no-selection enumerators (`run_part_checks` at `tools/rms_checks.py:103`, `run_assembly_checks` at `:157`, both correct), but `:186` is `def check_rms_equations(document_id: str | None = None)` — the model-facing tool, not the enumerator. The function that is "the same Fix: Change `(:186)` to `(:210)` and name the function: "| RMS equations | **Yes, already** | `run_equation_checks(context, None)` grades every part document (`:210`) |".
- **specs/005-llm-efficiency/contracts/ab-harness.md** - Nothing stops a decision resting on that median. If two of the three on-runs report `total_tokens` as null (a provider that returned no usage object, which the edge cases explicitly allow and FR-003 propagates to the session total), the on-arm median is a single run, the 20 percent threshold is appl Fix: Add a fifth mechanical rule in ab-harness.md section 5 and in FR-028: the computed decision is `re-measure`, with the printed reason naming the metric and the runs, whenever any of the six runs contributes a null to a metric the decision rests on (total tokens, wall clock, or the defect and false-al
- **specs/005-llm-efficiency/spec.md** - The normative text never names the mechanism levers.md analysed and chose. Read literally, "withdrawing the tools" means handing the adapter a shorter tool array for that round, which (a) varies the tool list within one session and so fails the prefix-stability regression test FR-042 demands and cos Fix: Name the mechanism in FR-072: the tool array sent is unchanged and the model is prevented from calling by `tool_choice: "none"` on OpenAI and `FunctionCallingConfig(mode=NONE)` on Gemini, so the prompt prefix is not disturbed and FR-042 still holds.
- **specs/005-llm-efficiency/contracts/usage.md** - The assertion cannot settle what it claims. Under a delta stream the last chunk's total can equally be greater than or equal to the first, so the probe passes in both worlds and the "last chunk that carries usage wins" rule (T009) stays unverified while being recorded as answered. This matters becau Fix: Replace G3's assertion with one that discriminates: record every chunk's usage and assert the `total_token_count` sequence is non-decreasing AND that the final chunk's `total_token_count` equals its own `prompt_token_count + candidates_token_count + tool_use_prompt_token_count + thoughts_token_count
- **specs/005-llm-efficiency/spec.md** - The only guard on the cached branch is that it is chosen once at `start_review` and tested both ways (FR-045). Nothing covers the cache disappearing while the branch stays cached: the TTL is bounded at 1800s (levers.md) and the cached config shape sends no `system_instruction` and no `tools`, so a r Fix: Add to FR-045/FR-046 that a request failure naming the cached content falls back once to the uncached config shape (system instruction and tools sent) for the remainder of the session, records the fallback on the usage record so the ledger row shows it, and never aborts the review; add the fallback 

## Feature 005 offline part 1 (2026-09-16)

- **reviewer/src/swreview/cli.py:1688** (measurement) - T018 states the refusal unconditionally: "a set with no held_out: true package is refused unless --i-know-the-set-is-too-small is passed". `_refuse_a_set_too_small_to_gate` is only called when `declares_a_study` is true (`bool(lever) or study != NO_STUDY or arm is not None`, cli.py:1742/1752), so `s Fix: Either take the narrowing to the owner and record it (a one-line note in probe-log.md or contracts/ab-harness.md section 2), or make the refusal unconditional and pass --i-know-the-set-is-too-small in the smoke-run call sites, so the run folder always carries the override flag that tells a reader it
- **specs/001-agentic-design-review/contracts/cli.md:46** (measurement) - The CLI contract still documents `swreview benchmark run` as taking only `--set`, `--out`, `--provider`, `--model`, `--effort`, and `swreview review` (line 34) without `--lever`. T018 added five options to `benchmark run` (`--lever`, `--study`, `--arm`, `--rep`, `--i-know-the-set-is-too-small`) and  Fix: Add the new options to the `swreview review` and `swreview benchmark run` rows of cli.md, naming the five refusals in one clause each. It is an additive contract edit of the same kind T020 already made to three schemas.
- **reviewer/src/swreview/cli.py:1728** (measurement) - `--rep` is optional and nothing refuses a study run that omits it: `check_study_arm` validates only study/arm/lever consistency, so `benchmark run --study prompt_cache_key --arm on --lever prompt_cache_key` (no `--rep`) writes a provenance record with `rep: null`. T037 requires the repetition index  Fix: Refuse `--arm off|on` without `--rep` as a fifth consistency check in `check_study_arm` (a baseline or a no-study smoke run may keep rep null), with the message naming the rule, so the refusal lands before the paid run rather than at `compare`.
- **reviewer/src/swreview/report/markdown.py:51** (measurement) - contracts/usage.md section 8 says of the `## Tokens` section: "A `None` renders as `unknown`, never as `0` and never as a dash that could be read as zero." The report renders `not reported` (`_NOT_REPORTED`), while benchmark/scorecard.py:41 renders the same concept as `unknown` (`UNKNOWN`). The subs Fix: Pick one word across report.md, scorecard.md and the ledger - `unknown` is the contract's - and keep the L1 case as the qualified variant (`unknown (probe L1 not recorded)`). If `not reported` is genuinely the better distinction, amend contracts/usage.md section 8 in the same change so the contract 
- **specs/001-agentic-design-review/contracts/scorecard.schema.json:62** (measurement) - `TokenUsage` is now defined in full in both scorecard.schema.json ($defs, line 62) and review-session.schema.json ($defs), with two hand-written copies of the same eight properties and their descriptions. chat-events.schema.json already demonstrates the cross-file reference style (`https://smart-sw. Fix: Replace scorecard.schema.json's `$defs/TokenUsage` with a `$ref` to the review-session contract's definition, as chat-events.schema.json already does; failing that, extend the equality test to compare the whole `$defs/TokenUsage` object rather than only `required`.
- **reviewer/src/swreview/report/markdown.py:508** (measurement) - `_render_cached_share` gates on the module-global `CACHED_SHARE_PUBLISHABLE`, so a Gemini session also renders "Cached input share: unknown (probe L1 not recorded)" even though the code's own docstring (agent/providers/__init__.py, `cached_input_share` and `CACHED_SHARE_PUBLISHABLE`) states containm Fix: Either gate per provider (publish the share on Gemini now, hold OpenAI until L1) and say so in FR-047, or leave the blanket gate and add one sentence to the `CACHED_SHARE_PUBLISHABLE` docstring recording that the Gemini half is being withheld deliberately for a single publication rule rather than fo
- **reviewer/src/swreview/benchmark/scorecard.py:50** (measurement) - `scorecard.py` imports `EVENTS_FILE_NAME` from `swreview.agent.runner` for one string constant, which drags the whole agent stack (providers, bridge client, tool registry, exception store) into the import graph of `swreview benchmark score` and of every test that scores a run. Scoring contacts no pr Fix: Move `EVENTS_FILE_NAME` (and, if it helps, `SESSION_FILE_NAME`) to a small leaf module both `agent/runner.py` and `benchmark/scorecard.py` import - `report/session.py` is the natural home, since it already owns the session file's identity - and import it from there.
- **reviewer/src/swreview/report/markdown.py:542** (measurement) - T021 asks the section to name each provider's own fields "rather than a single cross-provider 'output tokens' column". `_render_reasoning` does that for reasoning/thoughts, but the output line is still a bare `- Output tokens: N`. That single label is precisely the cross-provider column the task war Fix: Render the output line through the same provider-aware helper, e.g. `- Output tokens (OpenAI, includes the reasoning tokens)` / `- Candidate tokens (Gemini, excludes the thoughts tokens)` / `- Output tokens` when the session names no provider, and extend the two provider tests to assert it.
- **reviewer/src/swreview/agent/providers/gemini_provider.py:496** (measurement) - `_stream` accumulates `usage` into a local and returns it only on the normal path; an `errors.APIError` raised part-way through the stream discards whatever usage the chunks already carried and `run()` never reaches its `on_event("usage", ...)` call. That is the same failure T013 argues against one  Fix: Carry the partially collected `usage_metadata` out on the error path (e.g. attach it to the raised `GeminiProviderError`, or emit the `usage` event from a `finally`/`except` before re-raising) so a round that reported usage before failing still lands on the stream. Add a test whose stream yields a u
- **reviewer/src/swreview/benchmark/scorecard.py:253** (measurement) - `seconds_to_first_finding` promises `None` "whenever the number cannot be had", and catches `OSError` and `json.JSONDecodeError`, but `event["at"]` and `datetime.fromisoformat(...)` are unguarded. A line that is valid JSON yet lacks `at`, or carries a malformed timestamp, raises `KeyError`/`ValueErr Fix: Read the stamp with `event.get("at")` and wrap the two `fromisoformat` calls in the existing failure path (return `None`). Add cases to `test_scorecard_usage.py` for an event line missing `at` and one with an unparseable `at`, beside the existing truncated-stream test.
- **reviewer/src/swreview/report/session.py:210** (measurement) - T014 specifies "`turns` counts `turn.ended` events". The implementation sets `turns=len(by_turn)`, which drops any turn that ended without producing a round and adds one for trailing rounds that never ended a turn. It is a defensible reading (and the schema description and docstring both say "turns  Fix: Either confirm the divergence with the owner and note it in the task/contract, or count `turn.ended` events directly and keep `by_turn` as the per-segment breakdown. Whichever way, make `test_a_turn_that_produced_no_round_is_not_counted_as_a_turn` state which rule it is pinning.
- **reviewer/tests/unit/test_tool_payload.py:255** (measurement) - T001 says to pin the Gemini compact-JSON byte total at 34,248; the test pins 34,217. The deviation is deliberate and reasoned (`gemini_objects` dumps `types.FunctionDeclaration` with `by_alias=True, exclude_none=True`, which I confirmed matches what `GeminiProvider._config` at `gemini_provider.py:54 Fix: Add one sentence to the module docstring (and ideally to T001's own text when the owner next touches tasks.md) recording that 34,248 was measured from the snake_case dict rather than the aliased wire form, so the next reader does not 'correct' the pin back.
- **reviewer/src/swreview/report/session.py:238** (measurement) - `SessionUsage.summed([], [])` returns an all-zero `TokenUsage` (`any(...)` over an empty list is False, `sum([])` is 0), and `test_a_session_with_no_rounds_sums_to_zero_and_not_to_unknown` blesses it. Today only `UsageLedger.usage()` calls it and that guards with `if not self._rounds: return None`,  Fix: Move the guard into the rule: have `summed` refuse (or return `None` from a `summed_or_none` sibling) on an empty round sequence, so the ledger's guard is the rule rather than a convention the caller has to remember.
- **reviewer/src/swreview/benchmark/scorecard.py:47** (measurement) - `scorecard.py` now imports `from swreview.agent.runner import EVENTS_FILE_NAME`, pulling the whole agent runner (bridge client, tool registry, exception store, provider registry) into a module whose docstring is about answer-key isolation and whose only other imports are models and loaders. It works Fix: Move `EVENTS_FILE_NAME` (with `SESSION_FILE_NAME` if it lives there too) to a small neutral module — `swreview/report/session.py` already owns `save_session`/`load_session` — and import it from there in both `agent/runner.py` and `benchmark/scorecard.py`.
- **reviewer/src/swreview/benchmark/compare.py:413** (harness) - `_check_one_study` compares only `commit`, `effort`, `set_digest` and `checklist_digest`. `max_steps` is recorded in the provenance record and is listed by FR-022 and ab-harness section 2 ('Fixed across every run of a study: the set file ..., the model id, the effort, `max_steps`, the checklist, and Fix: Add `"max_steps"` to the field tuple at compare.py:413 and a parametrized case to `test_two_runs_of_one_study_that_differ_are_refused_naming_the_field`.
- **reviewer/src/swreview/benchmark/compare.py:396** (harness) - T041 says compare 'refuses to place two runs in one comparison when the commit, model, effort, set file or checklist differ, with no declared exception, and it says which field differed'. `_study_key` puts provider and model in the study key instead, so an off run on gpt-5.6 and an on run on gpt-5.5 Fix: Either refuse a study whose run set spans more than one provider/model unless every arm is complete on each (naming `model`), or keep the split and add a test plus an explicit note in the decision-row output that a one-armed study usually means a mis-typed `--model`/`--provider`. Confirm the directi
- **reviewer/src/swreview/benchmark/compare.py:502** (harness) - `run_dirs` is rendered as `str(run.run_dir)`, i.e. exactly as typed on the command line, and `RunRow.run` is only `run_dir.name`. Once a real study lands under `benchmarks/studies/`, the committed `## Results ledger` in docs/llm-efficiency-options.md will embed absolute machine paths (verified: `C:\ Fix: Render `run_dirs` and `run` relative to `REPO_ROOT` (already imported) with a POSIX separator, falling back to the absolute path only when the folder lies outside the tree, and add a test that the rendered cell is identical for a relative and an absolute invocation.
- **reviewer/src/swreview/benchmark/compare.py:118** (harness) - Alternation is neither enforced nor reportable. ab-harness section 2 says 'The run order is normative: alternated (off, on, off, on, off, on) ... the provenance record's start time is what lets a reader check it afterwards', and FR-022 requires the start time to be recorded per run. `RunProvenance.s Fix: Add `started_at` to `RunRow` and to the raw table (additive to FR-025's column list), sourced only from the provenance record, and state in ab-harness 10.8 that alternation stays a discipline but is now auditable from the ledger. Alternatively, have `compare` print a note when the recorded start tim
- **reviewer/src/swreview/benchmark/compare.py:719** (harness) - ab-harness 10.8 claims 'Reasoning and thoughts are named per provider in the cell, not in the header: one header serves rows of both providers, so the row says which field it is quoting'. The rendered cell is plain `_off_on(...)` numbers with no field name (verified: `16000 (16000-16000, n=3) -> 160 Fix: Either prefix the cell with the provider's own field name (`reasoning_tokens` for OpenAI, `thoughts_token_count` for Gemini, taken from the row's provider) or correct ab-harness 10.8 to say the `Provider and model` column is what identifies the field.
- **reviewer/src/swreview/cli.py:1938** (harness) - `--check` is only consulted inside `if into is not None`. `swreview benchmark compare --check` with no `--into` prints the run/lever counts and exits 0, which reads as 'the committed ledger is current' when nothing was checked at all - the one failure mode a drift guard must not have. Fix: Raise `typer.BadParameter` when `check` is passed without `into` (exit 2, beside the other usage errors), and add a CLI test.
- **reviewer/src/swreview/cli.py:1768** (harness) - The provenance record is written only after `run_benchmark` returns, so a study arm that crashes or is interrupted part-way leaves a run directory with sessions and no record. `compare` then renders those packages with `lever`, `arm`, `rep` and `commit` null (the 'a run made before the convention ex Fix: Write the record before the first package is reviewed (every field except nothing is known only afterwards - `started_at` is already captured at cli.py:1755), or write it in a `finally`, so a partial run is attributable and can be recognized as incomplete.
- **reviewer/src/swreview/benchmark/runner.py:63** (harness) - The RK-20 independence claim is overstated for the settings check. `cli.benchmark_run` resolves one `EfficiencySettings` object (cli.py:1743) and passes that same object both to `run_benchmark` (which threads it into `session.efficiency`) and into `RunProvenance(efficiency=...)` (cli.py:1783). The c Fix: Record the raw `--lever` names on `RunProvenance` (additive) and have `_check_session` compare `session.efficiency` against the settings re-derived from those typed names, so the resolution step is covered too; or soften the docstrings in runner.py, compare.py and ab-harness section 3 to say what th
- **specs/005-llm-efficiency/contracts/ab-harness.md:150** (harness) - T046 says 'extend' the contract with what the implementation settled. Two existing rows of the section 3 cross-check table were rewritten rather than extended: the answer-keys path is now 'not cross-checked' and the checklist digest is now compared across a study instead of against `session.json`. B Fix: Flag these two rows explicitly to the owner (or keep the original rows with a struck-through note pointing at 10.3/10.4) so the weakening is signed off rather than absorbed; the replacement answer-key guarantee is already asserted by `test_compare_reads_no_file_under_the_answer_keys_directory`.
- **reviewer/src/swreview/benchmark/adoption.py:217** (harness) - `_defects_lost` iterates the packages present in each ON run's scorecard, so a package that is missing from an on-run's `per_package` (a package the lever made fail or skip entirely) contributes no loss at all: the worst-case rule sees nothing. `_check_one_study` does not compare package sets across Fix: Iterate the off arm's package ids and treat a package absent from an on run as every one of its off-found defects lost; or refuse a study whose runs do not cover the same package ids, naming the package. Add a test with an on run whose scorecard is missing a package.
- **reviewer/src/swreview/benchmark/compare.py:401** (harness) - T041 says compare 'refuses to place two runs in one comparison when the commit, model, effort, set file or checklist differ'. `_check_one_study` checks only commit, effort, set_digest and checklist_digest; `model` is handled by making it part of the study key instead (documented as a deliberate subs Fix: Add a test that two studies differing only in model (and one differing only in provider) produce two decision rows rather than a refusal, so the contract claim in 10.5 is pinned. Confirm with the owner that swapping the refusal for a second study key is acceptable against T041's literal text.
- **reviewer/src/swreview/benchmark/compare.py:641** (harness) - ab-harness.md 10.8, added by T046, states 'Reasoning and thoughts are named per provider in the cell, not in the header ... the row says which field it is quoting'. Nothing implements that: the header is the generic 'Reasoning or thoughts off -> on' and `_lever_line` renders `_off_on(row.reasoning_o Fix: Either render the provider-specific field name in the cell (e.g. 'reasoning 16000 (...) -> ...' for OpenAI, 'thoughts ...' for Gemini, driven off `LeverDecision.provider`) with a test, or correct 10.8 to say the 'Provider and model' column is what disambiguates.
- **reviewer/tests/unit/test_benchmark_compare.py:8** (harness) - Two module docstrings state sources the code does not use. The test docstring and compare.py's own say 'uncached in and the cached share are derived in the scorecard only and read through', but compare.py:366 reads `uncached_input_tokens` from `session.usage.totals` (the TokenUsage property) while ` Fix: Correct both docstrings to name the actual source of each column (uncached from TokenUsage, s-to-first-finding from PackageScore via the scorer's read of events.jsonl), or move uncached into PackageScore so the pair really does share one source.
- **reviewer/src/swreview/benchmark/compare.py:556** (harness) - `refused_decisions` excludes baseline studies by `row.decision_reason.startswith(BASELINE)` - a string-prefix match on free-text prose is the only thing keeping a baseline-only compare from exiting non-zero, and nothing tests it. `test_a_baseline_study_renders_a_distribution_and_no_decision` (line 4 Fix: Carry the refusal as a structured flag on LeverDecision (e.g. `decision_refused: bool`) rather than re-deriving it from the reason string, and add a test asserting `benchmark compare` over a baseline-only study exits 0.
- **reviewer/tests/unit/test_adoption_rule.py:268** (harness) - T042 asks for the SC-011 assertion 'over a study built on today's single-package benchmarks/sets/pilot.json (no held_out: true, recall null)'. Both SC-011 tests build a hand-made spec with `held_out=False` instead of reading the real set file, and nothing anywhere pins that `benchmarks/sets/pilot.js Fix: Add one assertion that loads `benchmarks/sets/pilot.json` and pins its shape (one package, no `held_out: true`), so the SC-011 tests go red when the premise changes - which is exactly when FR-029 stops biting.
- **reviewer/tests/unit/test_adoption_rule.py:112** (harness) - `test_deciding_writes_no_flag_default` is tautological: `assert EfficiencySettings() == EfficiencySettings(**{})` compares two identical expressions and can never fail, and the second assertion re-states the defaults that `test_efficiency_settings.py` already pins. The test would pass unchanged if ` Fix: Snapshot `EfficiencySettings().model_dump()` before calling `decide` and assert it is unchanged after, and assert `decide` returns a Verdict with no settings object on it - or drop the test and rely on the settings test.
- **reviewer/src/swreview/benchmark/compare.py:620** (harness) - The normative alternated run order (off, on, off, on, off, on - FR-022, ab-harness section 2) is, as the contract says, not enforced by code; but it is not reported either. `RunProvenance.started_at` is the thing the contract nominates as 'what lets a reader check it afterwards', and it appears in n Fix: Add `started_at` to RunRow and a 'started' column to RUN_COLUMNS (cheap, one source, no new recording), or - stronger - have `compare` print a warning row when the start times of a study are not alternated. Ask the owner which; the contract currently promises the reader something the ledger does not
- **reviewer/tests/support/studies.py:293** (harness) - `write_events` writes an events.jsonl into every package folder, but `compare` never reads it (`seconds_to_first_finding` is taken from the hand-built PackageScore). The compare tests therefore pass identically with the events files deleted, so the 's to 1st finding comes from events.jsonl' claim in Fix: Either drop `write_events` from the compare fixtures and state in the docstring that the column arrives through PackageScore, or build the PackageScore's `seconds_to_first_finding` by calling the scorer's `seconds_to_first_finding` over the written events file so the chain is actually exercised once

## Feature 005 offline part 2 (2026-09-16)

- **reviewer/src/swreview/tools/registry.py:612** (tier1) - The `ToolDispatch.withheld` docstring describes a design the code does not have: "`tools` then holds two kinds of entry and **`__iter__` filters while `by_name` does not**: a small explicit split inside one dataclass". In the code `tools: tuple[RecordedTool, ...]` holds one kind of entry, `withheld` Fix: Reword both comments to say what the code does: two fields, `__iter__` yields only the offered tools and `by_name` merges both, so no schema reaches the wire and a withheld name still resolves.
- **reviewer/src/swreview/benchmark/compare.py:172** (tier1) - `LeverCounter` gained `dropped_tools` and `_counter_cell` (line 797) now appends "; no longer called: ..." to the rendered counter column, but `contracts/ab-harness.md:520` states "**`lever_counter` is `{name, off, on}`**". No task in this batch authorizes a contract edit (T046 already landed; T057  Fix: Add a short section to `specs/005-llm-efficiency/probe-log.md` naming both shape changes and the contract lines they contradict (ab-harness.md:515-522), flagged for the owner, exactly as the lever 2 byte deviation is. Do not edit the contract without a task that says to.
- **reviewer/tests/unit/test_prefix_stability.py:166** (tier1) - The first scripted turn calls `record_coverage`, which is not a registered tool - `tools/session.py:78` defines `mark_coverage`, and `test_runner_provider.py:311` uses that name with the same `COVERAGE_ARGUMENTS`. The call therefore falls through `ToolDispatch.call`'s "no tool named" branch, writes  Fix: Rename the scripted call to `mark_coverage`, and assert the played session records no `failed` coverage, so a future typo in this script cannot pass again.
- **reviewer/src/swreview/benchmark/scorecard.py:169** (tier1) - `tool_calls_by_name` and `coverage_bucket_mix` default to `{}` on the model while `specs/001-agentic-design-review/contracts/scorecard.schema.json` now lists them in `required`. A scorecard.json written before this commit loads with `{}`, and `_repertoire` (compare.py:495) then reports `0.0` distinc Fix: Make both fields `dict[str, int] | None = None`, render `None` as unknown in the ledger, and have `_histogram_counter` return `off=None`/`on=None` and an empty `dropped_tools` when any run of an arm carries `None`.
- **reviewer/src/swreview/benchmark/scorecard.py:78** (tier1) - `tool_histogram` counts every `session.steps` entry, so a call the lever 4 tier refused appears in `tool_calls_by_name` as a call to a tool that was never on the wire. `ToolCallRecord.withheld` already distinguishes the two cases but `SessionSink.record` (registry.py:246-256) does not carry it onto  Fix: Carry `withheld` onto `InvestigationStep` (additive, and the same field must_fix #2 needs) and either exclude withheld steps from the histogram or report them as a separate key, with a one-line note saying which choice was made and why.
- **reviewer/tests/unit/test_tool_notes_prompt.py:289** (tier1) - T053's exception provision is written for "a docstring that cannot be split under the cap without rewording" and says such an exception is "recorded in the ledger row". The nine `CAP_EXCEPTIONS` are all `Args:` entries rather than descriptions, and they are recorded in probe-log.md and enforced by a Fix: Confirm with the owner that probe-log.md is the intended home for the nine, and soften the `MAX_PARAMETER_DESCRIPTION_LENGTH` docstring in schema.py so it does not claim a universal enforcement the exception list contradicts.
- **reviewer/tests/unit/test_openai_prompt_cache.py:211** (tier1) - `test_the_cache_key_survives_a_process_restart_because_it_is_read_off_disk` does not exercise any production restart path. `new_session` (tools/context.py) always mints `session_id=uuid4()`, and `chat/server.py:_start_review` always calls `start_review` fresh after `_rotate_previous`, so no restarte Fix: Either add the resume path the claim needs (a `start_review` that adopts an existing session.json's `session_id` when the run folder already holds one) and assert the key through it, or narrow the test name and docstring to what it proves: the key is a persisted value rather than a process-local ran
- **reviewer/src/swreview/agent/providers/openai_provider.py:351** (tier1) - `self._last_response_id = None` at the top of every turn means the first round of turns 2..n never sends `prompt_cache_options.comparison_response_id`, so its `cache_diagnostic` is always `None`. That is precisely the round where the misses `test_prefix_stability.py` ranks as most likely - `reasonin Fix: Carry `_last_response_id` across turns on the adapter (the `OpenAIProvider` instance outlives the turn on `ReviewRun`), so turn N round 1 compares against turn N-1's last response, and add a test pinning that a second turn's first request carries `comparison_response_id`. If the per-turn reset is ke
- **reviewer/tests/unit/test_tool_notes_prompt.py:288** (tier1) - Only the 160-character description cap is enforced over all 35 tools; the 90-character `Args:` cap has 9 named exceptions (`bridge_interference.settings` at 299 chars down to `check_axial_stack.dimension_refs` at 94), which I confirmed against the tree. The reasoning is sound - an `Args:` entry is t Fix: Owner decision before the lever 2 ledger row is written (T058/T059): either accept 160/unbounded-for-nine as the enforced cap and get the six spec-package files corrected in the one change allowed to edit them, or re-scope the 90 cap to 'descriptions only' in contracts/levers.md so the cap the build
- **reviewer/src/swreview/tools/registry.py:572** (tier1) - `WithheldTool.call` writes its coverage with `ToolContext.replace_coverage`, not the `record_coverage` T062 names. Idempotence is the right call and the docstring argues it well, but `replace_coverage` drops *every* existing `unresolved` item whose `check` equals `modeling.resilience`, including one Fix: Add a test that calls `mark_coverage` on `modeling.resilience` and then a withheld tool, and pin the intended behaviour (the tier sentence wins, which is probably right since the tier is the harder fact). If the model's own item should survive, scope the replacement to items this refusal wrote.
- **reviewer/tests/unit/test_tool_tiers_withheld.py:63** (tier1) - The new lever 4 tests never close the runs they start. `review()` here returns `run.start()` and drops the `ReviewRun`, and `test_tool_tiers.py:281` starts a two-turn run with no `close()`. Every other new test file in this change set (`test_prefix_stability.py`, `test_openai_prompt_cache.py`) uses  Fix: Wrap both in try/finally with `run.close()`, matching the pattern the two other new test modules already use, or give `tests/support/tiers.py` a context-manager helper that does it once for both modules.
- **reviewer/src/swreview/chat/server.py:355** (tier2) - `StoppableTools` wraps the run's `ToolSet` (server.py:1435 `run.tools = StoppableTools(run.tools, ...)`) and implements only `__iter__`, `__len__` and `call`. `providers.tools_withdrawn` asks `isinstance(tools, WithdrawableTools)`, which is a runtime_checkable protocol keyed on the `tools_withdrawn` Fix: Delegate through the wrapper: add `def tools_withdrawn(self) -> bool: return providers.tools_withdrawn(self.tools)` to `StoppableTools` (one method, no behaviour change when the inner set is a plain `ToolDispatch`), and add a unit test that `tools_withdrawn(StoppableTools(CoverageStopTools(...), sto
- **reviewer/src/swreview/benchmark/compare.py:107** (tier2) - Lever 7 has landed and leaves no machine-readable record that the stop fired. `CoverageStopTools.call` annotates only the returned payload; the `InvestigationStep` was already recorded by `ToolDispatch._finish` with the tool's own output, and `tool.finished.result_summary` is `summarize_result`, tru Fix: Record the fire where the scorecard can read it, then wire the counter. Cheapest explicit option: emit a `coverage.stop` event (or set a `coverage_stop_fired: bool` / `stopped_at_step: int | None` on `ReviewSession`, additive and null when the lever is off) from `CoverageStopTools.call` when it latc
- **Resolved 2026-09-19** (feature 007, research R3): `session.started` is now emitted before the pre-run runs, so the order this entry describes no longer occurs; the gate's same-session test pins `session.started` first on the stream. Kept for the record: **reviewer/src/swreview/agent/runner.py:989** (tier2) - `prerun_checks` runs inside `start_review`, before `ReviewRun.start()` emits `session.started`, so with lever 5 on the whole pre-run - 4 `tool.started`, 4 `tool.finished`, every `finding` and every `coverage` event - is written to `events.jsonl` ahead of `session.started` (verified by replay: the fi Fix: Either emit the pre-run after `session.started` (move `prerun_checks` into `ReviewRun.start()` before `_say(self.opening_message)`, keeping it out of `_run_turn` so it is still not a turn), or state in the contract that the pre-run's events precede `session.started` and add a test pinning that order
- **reviewer/src/swreview/agent/runner.py:353** (tier2) - FR-070 says the stop predicate "MUST be the same function finalization uses to enumerate open checklist items and open evidence requests", and T080 says the predicate "is **used by** [finalize_session], not copied". Only half of that is literally true: `open_evidence_requests` is shared (good, and ` Fix: Confirm the reading, then make it explicit in one place: either have `finalize_session` call `coverage_complete` for its own (unused-today) purposes, or - my recommendation - add a sentence to `coverage_complete`'s docstring and to FR-070's record stating that what is shared is the two enumerations 
- **reviewer/tests/integration/test_coverage_stop.py:321** (tier2) - `test_with_the_flag_off_the_event_stream_is_the_one_a_lever_free_run_writes` is close to vacuous: it compares a run with `efficiency=None` against one with `efficiency=EfficiencySettings()`, and `start_review` normalises the first into the second one line later (`session.efficiency = efficiency if e Fix: Keep the test (it is a useful determinism guard) but rename it, and add the assertion that carries the sentence: play the same script with `coverage_stop=True` on a session whose checklist never closes and assert its stream equals the off-arm stream event for event - i.e. the lever changes nothing u
- **reviewer/src/swreview/prerun.py:120** (tier2) - `coaxial_hole_pairs` is O(n^2) in `package.holes` and calls `axis_distance` on every pair, on every lever-5 run, purely to put a count in one digest line. The docstring calls the quadratic deliberate ("it runs once per review over a list the extractor already loaded"), which is fine for the fixtures Fix: Bound it explicitly rather than optimise it: count pairs only while `len(holes)` is under a named cap (say 1,000 holes), and above it render the line as the hole count with "candidate pairs not counted: too many holes to enumerate" - unknown staying unknown, which the digest's own discipline already
- **specs/001-agentic-design-review/contracts/scorecard.schema.json:20** (tier2) - T083's additive field was added to `PackageScore.required` alongside `carried_findings` and `tool_calls_by_name`. Adding to `required` is not an additive schema change for readers: any scorecard.json written before this commit now fails `contract_validator("scorecard.schema.json")`. Nothing in-tree  Fix: Confirm the intent. If old scorecards are never re-validated, leave it and say so in the schema's description. If they might be (a study folder from an earlier run), move the three new names out of `required` and keep them as optional properties with defaults, which is what the pydantic models alrea
- **reviewer/src/swreview/prerun.py:1** (tier2) - Deviation from T077's literal wording, flagged for acknowledgement rather than as a defect: the task says "Implement as a **sibling of `record_partial_evidence`** (`runner.py:157-183`), called from the same place in `start_review` (`runner.py:684`)". The call site is right (runner.py:989, beside `re Fix: No code change recommended; `runner.py` is already large and the digest rendering is a real unit worth its own module and docstring. I recommend the owner ratify the deviation so the next reader does not treat the task text as unmet - or, if the literal sibling placement matters, move `prerun_checks
- **reviewer/src/swreview/chat/server.py:1435** (tier2) - `run.tools = StoppableTools(run.tools, chat.stop_requested)` wraps the CoverageStopTools, and StoppableTools (server.py:355-379) implements only __iter__/__len__/call. `providers.tools_withdrawn` is `isinstance(tools, WithdrawableTools) and tools.tools_withdrawn()`, so through the pane's wrapper it  Fix: Give StoppableTools a `tools_withdrawn(self) -> bool: return providers.tools_withdrawn(self.tools)` (three lines, delegating through the same helper so the question is still asked in one place), and add one test that StoppableTools(CoverageStopTools(...)) reports the withdrawal. The alternative - ma
- **reviewer/tests/integration/test_coverage_stop.py:321** (tier2) - T081 names "flag off produces byte-identical events.jsonl" as a case. test_with_the_flag_off_the_event_stream_is_the_one_a_lever_free_run_writes plays the same script with efficiency=None and efficiency=EfficiencySettings() and compares the streams - but runner.py:964 normalizes `None` to `Efficienc Fix: Keep it (the determinism check is worth having) but rename it to say what it checks, and add the case that can fail: play one script twice, once OFF and once ON, and assert the OFF stream is a strict prefix-plus-tail of what a lever-free run writes - concretely, that the two streams are equal up to 
- **reviewer/src/swreview/prerun.py:251** (tier2) - The lever-4 interaction that T076 explicitly specifies is implemented and entirely untested. `planned_calls` filters `tools.withheld` (prerun.py:251-256), `not_evaluated_families` takes a `withheld: Sequence[tuple[str, str]]` argument (prerun.py:262, 276-279) and `prerun_checks` builds it (prerun.py Fix: Add one test in test_prerun_digest.py driving a run with EfficiencySettings(prerun_checks=True, tool_tiers=True) over a package whose tier withholds an RMS tool, asserting (a) the withheld tool produces no step, (b) its NOT-evaluated line carries the tier's own reason string verbatim rather than a s
- **reviewer/tests/golden/test_golden/rms-part.yml:1** (tier2) - The lens asked whether goldens are untouched: they are not. Five feature 001-003 session goldens have changed in the working tree (cover-blind-tap.yml +5, rms-assembly.yml +20, rms-equations.yml +10, rms-exceptions.yml +10, rms-part.yml +90) with `carried_over_at`/`carried_over_from`/`carry_over_key Fix: Reporting rather than fixing, per the scope rule. OQ-6's answer arguably authorizes the two manifest fields (the lever is impossible without them) and OQ-7's answer implies the carry-over provenance fields, so this is most likely an intended consequence of two answered open questions colliding with 
- **reviewer/src/swreview/prerun.py:388** (tier2) - `error=str(result.payload["error"]) if result.is_error else None` indexes `"error"` on any error payload. Every error result the dispatch builds today carries that key, so this is safe now, but it is an unguarded index on a Mapping the pre-run does not own, and the failure mode is a KeyError raised  Fix: `result.payload.get("error", result.payload)` or a small helper, and one test that a tool returning an error payload without an `error` key still produces a digest line rather than an exception. Two lines; I would take it, since the whole argument for this code path is that a failing check never sto
- **reviewer/tests/unit/test_reuse_key.py:104** (tier3) - The cross-language agreement on the reuse key is pinned in one direction only. `ReuseKeyTests.cs:29-44` hard-codes `PythonCanonicalForm` and `PythonDigest` as literals. Nothing on the Python side pins those literals - `grep` for the digest 35753b68... finds exactly one hit, in the C# test. A change  Fix: Add two asserts in test_reuse_key.py pinning the same canonical-form string and the same digest for `reuse_package()` / `DumpOptions()`, with a comment naming ReuseKeyTests.cs as the other copy. Then a change on either side fails on that side.
- **reviewer/src/swreview/benchmark/reuse.py:366** (tier3) - `_aborted_dump_refusals` reads `getattr(package.extractor, "completed", None) is False`, and the docstring claims `extractor.completed` "arrives with schema 1.3.0". It did not: `ExtractorInfo` in ir/models.py has no `completed`, the C# `ExtractorInfo` has none either, and `IRModel` is `extra="forbid Fix: Pick one half of T088's "or" and finish it: either add `completed: bool | None = None` to `ExtractorInfo` on both sides (and set it in `PackageWriter.Finish`), or delete the branch, the docstring claim and the two `ExtractorInfo13` tests and let the phase-level `tool_error` gap rule carry the case a
- **extractor/SwReview.Extractor.Console/Program.cs:309** (tier3) - On a `--reuse` miss or refusal, `Extract` has already run `writer.BuildReuseProbe(options)` - the component traversal, the property phase, the document phase and the manifest build, all on the SOLIDWORKS application thread - and then throws the result away and calls `writer.Write(options)`, which ru Fix: Have `Build` keep the probe's scope/package and continue the remaining phases from it on a miss (e.g. `BuildReuseProbe` returns the partially built package plus its scope, and `Write` accepts it), so the traversal is paid once whether reuse hits or misses. Record the before/after in the T092/T093 nu
- **extractor/SwReview.Extractor.Console/Program.cs:393** (tier3) - `RecordInIndex` runs for a reused package too and stamps `WrittenAt = DateTimeOffset.Now`. A second `--reuse` then finds the copy, and `PackageReuse.StatusLine` prints "Reusing the extraction from <copy> (under a minute old)" for evidence that was actually dumped days ago. `reused_from` on the copy  Fix: Carry the original extraction time forward: when the result came from a reuse, index the row with the source row's `WrittenAt` (and keep the source folder in `reused_from` rather than the intermediate copy), so the age the pane prints is the age of the extraction and not of the copy.
- **reviewer/src/swreview/tools/bridge.py:260** (tier3) - Two bounded-resource defects in the lazy fetch, both against the single STA worker. (1) `fetched` is seeded only from bodies already on the package, so a component that answers with no body - the `"the bridge fetched no body for it"` path - is never marked as attempted and is re-fetched on every lat Fix: Track attempted components, not just fetched bodies: add the component id to a per-context `attempted` set in `_fetch_component` regardless of outcome and skip it thereafter (its unresolved reason is already recorded once). For the limit, count the attempt before the call and stop issuing calls the 
- **reviewer/src/swreview/report/markdown.py:351** (tier3) - A carried finding keeps `tool_result_ids` from the originating session (carry_over.py's module docstring records this deliberately), and the report renders them unqualified as "- Tool result steps: 3" inside a report whose own step 3 is a different call. The heading says "carried over from session X Fix: Qualify the line when `finding.carried_over_from is not None`: "- Tool result steps (in session <id>): 3". One conditional in `_render_finding`, and a test asserting a carried finding's provenance line names the originating session.
- **specs/001-agentic-design-review/contracts/scorecard.schema.json:20** (tier3) - `carried_findings` (with `coverage_bucket_mix` and `tool_calls_by_name`) was added to `required`, not just to `properties`. That is not an additive contract change: a `scorecard.json` written by any earlier build no longer validates against the committed schema, which cuts across SC-004's "every fea Fix: Leave the three new members in `properties` and out of `required`, matching how `reused_from`, `carried_over_from`, `carried_over_at` and `carry_over_key` were added to review-session.schema.json in this same batch.
- **reviewer/src/swreview/benchmark/package_index.py:141** (tier3) - Two things here need an owner decision rather than a quiet landing. (1) `reusable_for` and `append_row` have no production caller: the product's index is written and read entirely by the C# `PackageIndex`, so this module is a second implementation of a shared on-disk format kept in step by nothing b Fix: For (1), decide with the owner whether the Python index reader stays (then pin one shared fixture line and the field order against the C# writer, as test_reuse_key should pin the key) or whether it collapses to the read side the tests actually need. For (2), confirm that T085a is the intended only c
- **extractor/SwReview.AddIn/Review/RunFolders.cs:147** (tier3) - T091 requires the reuse to be stated in the pane: "Reusing the extraction from <folder> (<age>)" in the status line instead of "Extracting evidence from ...". `ReviewHost.cs:767` still prints "Extracting evidence from ..." unconditionally and nothing in the add-in ever calls reuse. `RunFolders.FindR Fix: Pick one and say so: either wire the pane's extraction step through `PackageReuse.Reuse` so the status line and `reused_from` are real there, or delete `RunFolders.FindReusable` and `RunFoldersReuseTests`'s first three cases (keep `TheStepStripStillReadsTheProfileOutOfTheHead`, which covers the real
- **extractor/SwReview.Extractor/Dump/PackageIndex.cs:445** (tier3) - `PackageOf`'s containment check is `full.Length <= root.Length || !full.StartsWith(root, OrdinalIgnoreCase)`, a prefix test with no separator boundary. With run root `C:\runs`, a row naming `../runs-evil` resolves to `C:\runs-evil`, which is longer than the root and starts with it, so the guard pass Fix: Compare against the root with a trailing separator: build `root` as `Path.GetFullPath(runRoot).TrimEnd(sep) + Path.DirectorySeparatorChar` and require `full.StartsWith(root, OrdinalIgnoreCase)`. Add the sibling-prefix case to `AFolderThatLeavesTheRunRoot_IsRefused` as an `[InlineData]` row (`"../els
- **extractor/SwReview.Extractor.Tests/BridgeTessellateTests.cs:180** (tier3) - `Tessellate_ReportsTheElapsedMilliseconds` asserts `response.ElapsedMs >= 0`. `ElapsedMs` is a stopwatch reading the dispatcher sets for every command, so the assertion holds for any command, for a no-op handler, and for a handler that was never written - it is tautological. Its comment carries the  Fix: Replace the `>= 0` assertion with the behaviour: add a row to `InProcPipeServerTests` (or a new case there) where an `ITessellateSource` blocks on a gate while a `ping` and a `measure` are submitted, and assert the three responses come back in arrival order with the tessellate first. If that fixture
- **reviewer/src/swreview/tools/bridge.py:265** (tier3) - `extraction.lazy_fetch_body_limit` bounds bodies fetched, not round trips: the limit is checked against `context.lazy_bodies_fetched`, which only increments by `len(bodies)` after a successful fetch. A component that answers with zero bodies (suppressed, lightweight, or a gap) never increments it, s Fix: Count round trips as well as bodies: increment a `lazy_fetches_attempted` counter in `_fetch_component` before the call and trip the limit on `max(bodies, attempts)`, or simply increment `lazy_bodies_fetched` by `max(1, len(bodies))` so a zero-body answer still costs one unit. Add a test with `bodie
- **reviewer/src/swreview/benchmark/reuse.py:366** (tier3) - The `extractor.completed` branch is speculative and unreachable. `IRModel` is `ConfigDict(strict=True, extra="forbid")` (ir/models.py:104) and T089 did not add `completed` to `ExtractorInfo`, so no package this build can parse will ever carry the member; `getattr(package.extractor, "completed", None Fix: Delete the `completed` branch, the two tests and `ExtractorInfo13`, and correct the docstrings in reuse.py and ReuseRefusals.cs to say the phase-level `tool_error` gap is the route this build takes (ReuseRefusals.cs already says this correctly). Keep `test_an_absent_completed_field_leaves_the_gap_ru
- **reviewer/tests/golden/test_golden/cover-blind-tap.yml:66** (tier3) - Five feature 001/003 golden baselines changed in this working tree: cover-blind-tap.yml (+5), rms-assembly.yml (+20), rms-equations.yml (+10), rms-exceptions.yml (+10), rms-part.yml (+90). The diffs are all additive nulls from this batch's new optional members (`carried_over_at`, `carried_over_from` Fix: Raise it with the owner explicitly rather than shipping the regeneration unremarked. Two directions: (a) accept additive-null churn and amend the rule in tasks.md/the commit message to say so - but tasks.md is off-limits to this agent, so it needs the owner; or (b) make the golden dumper drop member

## Handoff round: proxy, event stream, check rms --out, tool service (2026-09-16)

- **extractor/SwReview.AddIn.Tests/BackendProxyPageTests.cs:118** (proxy) - None of the page tests the brief names for item 1 exist, and the wired path has no test at all. The brief asks for an offscreen page fetching `/__backend/health` with a fake `ProxySend` seeing the fake's status and body, the fake seeing `Authorization` and no `Origin`, a fetch to `/__backend/sessions/x/events` getting the 501 `StreamNotProxyable` body, and a page resource still loading. What landed is the opposite assertion - `ThePagesOwnResourcesStillLoadWithTheProxyFilterInstalled` ends `Assert.Empty(fake.Requests)`. So `ServeProxied` (reading headers and `Content` before the deferral goes async, `TryServe` off the UI thread, `CreateWebResourceResponse` and `Complete()` back on it) is exercised by nothing: the claims my review was asked to verify are true by inspection only.
  - Fix: Use the doc's own evidence that the mechanism works against a host that is NOT folder-mapped (section 4, "The same wiring against a host that is not mapped sees the request and serves it from C#"). Add a test-only fixture that maps a second virtual host with no folder mapping, points `TaskPaneOptions.Backend`/`ProxySend` at the fake, and asserts the brief's four properties through the real `ServeProxied`. That test survives whichever way out the owner picks, and it is the only way the ArgumentException in must_fix 2 would have been caught.
- **extractor/SwReview.AddIn/TaskPaneControl.cs:683** (proxy) - `ConfigureAwait(true)` makes the UI-thread hop implicit: it resumes on whatever `SynchronizationContext` was current when the handler ran. In the pane that is the WinForms context, so it is correct today, but if the event ever fires with no ambient context the continuation sets `args.Response` and completes the deferral from a thread-pool thread - a COM wrong-thread failure that the blanket `catch (Exception)` at line 693 swallows silently, leaving the request unanswered with no trace. "Explicit over clever" argues against leaving the most load-bearing thread hop in the method to an ambient.
  - Fix: Marshal explicitly - capture the control and resume with `BeginInvoke`, or assert `SynchronizationContext.Current != null` at registration time with a comment naming the WinForms context as the requirement. Either way the reader sees which thread each of the three steps runs on.
- **extractor/SwReview.AddIn.Tests/BackendProxyPageTests.cs:119** (proxy) - `ThePagesOwnResourcesStillLoadWithTheProxyFilterInstalled` is close to vacuous. The filter installed in `AttachPageAsync` is `BackendProxy.ResourceFilter` = `https://swreview.invalid/__backend/*`, so the page's own `index.html`, `app.js`, `render.js` and stylesheet were never in its scope; and on a folder-mapped host the event is not raised for anything at all. The test would pass with `TryServe` hard-coded to return a 500 for every URL. It reads as coverage of "a null answer leaves page resources alone" and is not.
  - Fix: Either narrow the docstring to what it actually proves (installing the filter and the handler does not break page loading), or move the real property to the non-mapped-host fixture above, where a request outside the prefix can actually reach `TryServe` and be seen to be left alone.
- **specs/002-task-pane-assistant/contracts/chat-api.md:82** (proxy) - Brief item 4 also asks that the "Origin and CORS" section note that the proxy drops `Origin` (the backend's origin guard and CORS headers remaining for the CLI and any direct caller). The diff rewrites only "Reading the event stream"; the Origin and CORS section is untouched, so the one behaviour a future reader is most likely to trip over - the proxy deliberately stripping the header the backend's guard checks, BackendProxyHandler.Forwardable line ~160 - is documented only in a C# docstring and a doc under docs/.
  - Fix: Add the sentence the brief names to the "Origin and CORS" section, phrased for the state that actually shipped (the proxy drops Origin when it serves a call; today it serves none, because the folder-mapped virtual host never raises the event).
- **extractor/SwReview.AddIn/Review/ReviewHost.cs:479** (proxy) - Brief Part A items 2 and 3 are not implemented, and the round's stated goal - "no Task Pane page ever makes an HTTP request to 127.0.0.1" - is not met. `init.backend.origin` is still `endpoint.Origin` (here and Model/ModelCheckHost.cs:279), and the CSP is still `connect-src http://127.0.0.1:*` in all four index.html files and in specs/002-task-pane-assistant/contracts/pane-host-messages.md:39. Only the event stream moved off loopback. The deviation is documented and the byte-identity constraint is intact (Remodel page CSP == contract CSP, both unchanged, RemodelPageContractTests green), and the brief-named tests for these items - the page-level fake status/body, Authorization-and-no-Origin at page level, the 501 through the page, and the `fetch('http://127.0.0.1:1/')`-refused-by-CSP test - are correspondingly absent. This is an owner decision, not something to patch quietly.
  - Fix: Do not let this round close as "Part A done". Re-run the unconfounded limitation test first (must-fix 1). If interception still cannot be had, put doc section 4's two ways out to the owner: (A) a second, non-folder-mapped virtual host - smallest change, but re-opens CORS and adds OPTIONS handling to a class that currently strips every Access-Control-* header; (B) serve the pages themselves through WebResourceRequested and drop the folder mapping - this is the design section 2 chose, `connect-src 'self'` holds and no CORS anywhere, but it changes how every page loads and the offscreen page tests map the host today. My recommendation is (B): it is the explicit option, it closes CORS rather than reopening it, and it makes items 2 and 3 land as written; (A) trades a one-line origin change for permanent CORS surface on the exact boundary this round exists to remove.
- **extractor/SwReview.AddIn/TaskPaneControl.cs:713** (proxy) - DRY: `ReadRequestContent` is a byte-for-byte duplicate of the private `Read` in BackendProxyHandler.cs (copy to MemoryStream, ToArray, null when empty) - the same rule stated twice, in two files, with two docstrings that must stay in agreement. It also costs two extra copies of every request body: byte[] on the UI thread, wrapped in a new MemoryStream at line 683, re-read into another byte[] by the handler.
  - Fix: Give BackendProxyHandler a `TryServe(string uri, string method, headers, byte[]? body)` overload (the Stream overload delegating to it) and have ServeProxied pass the bytes straight through; delete ReadRequestContent.
- **extractor/SwReview.AddIn/Review/BackendProxyHandler.cs:290** (proxy) - `FormatResponseHeaders` has no test. docs/pane-backend-proxy.md section 3 lists "No CORS header is copied back" among the decisions "each with a test", but the stripping of Access-Control-*, Transfer-Encoding, Content-Length and Connection is private and only reachable through the real `Send`, which no test exercises (every handler test injects a transport that returns a hand-built ProxiedResponse). A regression here would put `Access-Control-Allow-Origin` back on a same-origin response - re-opening precisely the question the proxy closes - with nothing to catch it.
  - Fix: Make FormatResponseHeaders internal (or move it to a small static helper) and pin it with a WebHeaderCollection unit test covering each stripped name, case-insensitivity, and the CRLF join shape WebView2 expects.
- **extractor/SwReview.AddIn/TaskPaneControl.cs:696** (proxy) - ServeProxied swallows every exception with no record anywhere. The two catch blocks are silent by design, but the pane already owns a redacting report channel, and the failure mode this whole class exists for was diagnosed on a pilot workstation by reading logs. A proxy call that throws leaves the engineer with the same three words (`Failed to fetch`) and nothing to distinguish cause from cause - which is exactly the confusion docs/pane-backend-proxy.md section 4 warns about.
  - Fix: Report one redacted line per failure through the existing choke point (URL path only, never headers - the Authorization header is in scope on that stack), rate-limited if need be. The token must not appear; today it does not reach any log, which is correct and should stay pinned by a test once ServeProxied is testable.
- **extractor/SwReview.AddIn/Review/ReviewHost.cs:258** (events) - `Redact(frame)` runs per SSE frame, and `Redact` resolves the secret every call: `PaneActions.Redact` -> `_options.Secrets()` -> `_settings.ResolveApiKey(...)` -> `Convert.FromBase64String` + `ProtectedData.Unprotect` (UserSettings.cs:322) + a fresh `Encoding.UTF8.GetString` of the plaintext key. The event stream is the highest-volume path in the product - `text.delta` is one frame per token - so a review now performs one DPAPI decryption per token and materialises the plaintext API key as a new, uncleared managed string per token. `Array.Clear(plain, ...)` clears the byte array but not the string, so the key is duplicated across the managed heap thousands of times per review, which is exactly the crash-dump exposure chat-api.md's token rule exists to prevent, and it is on the reader thread that is supposed to keep up with the stream.
  - Fix: Memoise the resolved secret in `ReviewHost` - resolve once per settings load and on every successful `settings.save`, hand `PaneActions` a `Secrets` lambda that returns the cached value - so `Redact` stays the single choke point but stops decrypting. Add a test that N frames cause one key resolution.
- **extractor/SwReview.AddIn/Review/EventStreamPump.cs:197** (events) - DRY: the loopback URL is rebuilt by hand as `string.Format("http://127.0.0.1:{0}/sessions/{1}/events", endpoint.Port, ...)` even though `BackendEndpoint.Origin` (BackendProcess.cs:52) is that exact string and is what `BackendClient.Send` uses (BackendClient.cs:286). Two places now encode "the backend is at 127.0.0.1 on this port"; if the endpoint ever gains a host or a scheme, this one will not follow.
  - Fix: Build the URI from `endpoint.Origin + "/sessions/" + Uri.EscapeDataString(chatId) + "/events"`. The `Uri.EscapeDataString` on the chat id is right and should stay.
- **extractor/SwReview.AddIn/Review/EventStreamPump.cs:210** (events) - Connection starvation on net48. The project targets net48 (extractor/Directory.Build.props), where `ServicePointManager.DefaultConnectionLimit` is 2 per ServicePoint, and nothing in the tree raises it (no `ServicePoint` reference anywhere under extractor/). This request holds a connection to 127.0.0.1:<port> open for the whole life of a review, leaving exactly one for every other `HttpWebRequest` to the same endpoint - `BackendClient` (`IsTurnRunning`, `ListModels`, `CreateSession`), `BackendProxyHandler.Send` on the WebView2 thread, and `RemodelBackendClient`. Two of those overlapping while the stream is open means the third blocks until its `Timeout` and surfaces as a spurious backend failure. Nothing in the current tests can see this: they open one stream and make no other call.
  - Fix: Set `request.ServicePoint.ConnectionLimit` (8 is ample) in `Build`, with a comment saying the stream pins one connection for the life of a review and net48 hands out two by default. One explicit line beats a process-wide `ServicePointManager` mutation from inside an add-in.
- **extractor/SwReview.AddIn.Tests/EventStreamPumpTests.cs:48** (events) - `FramesArriveInOrderAndVerbatim` and `AMultiLineDataFrameIsNotSplit` only ever feed `\n`-terminated frames (`SseConnection.Pump` writes `frame + "\n\n"`). The real backend is sse-starlette, whose `DEFAULT_SEPARATOR` is `\r\n` (reviewer/.venv/Lib/site-packages/sse_starlette/event.py:13), so every frame in production takes the CRLF path through `Drain` - the one path with no coverage at all. A regression that mishandled a bare `\r`, or that stopped normalising and shipped `\r` into the page, would pass the whole suite.
  - Fix: Give `SseConnection.Send` an optional separator and add one case that writes a multi-line `\r\n` frame plus a `\r\n\r\n` terminator, asserting the delivered frame is the `\n`-joined form. While there, add a frame containing a blank `data:` line to pin what the blank-line split does to it.
- **extractor/SwReview.AddIn.Tests/EventStreamPumpTests.cs:234** (events) - `EventsOpenStreamsFramesToThePageAndNeverPostsTheToken` does not actually exercise the redaction it names in its docstring. `PumpWorld` builds `UserSettings.Defaults()` with no `ApiKeyProtected` and no environment override, so `Secrets()` yields a single null and `Redaction.Redact` is a no-op; and the bearer token was never in a frame in the first place, it is a request header. `AssertNothingPostedContains(Token)` therefore passes byte-for-byte with `Redact(frame)` deleted - which is the FR-015 rule this test is the only guard for.
  - Fix: Configure a key on the settings the world writes (`SetApiKey`), have the fake server send a frame whose `data:` embeds that key the way a provider error message would, and assert the posted `events.frame` carries the mask and not the key. Do the same for one `events.closed` reason.
- **extractor/SwReview.AddIn.Tests/EventStreamPumpTests.cs:271** (events) - The brief names four stoppers - "`events.close`, a new `events.open`, a `ready` from a reloaded page, and `Dispose` all stop the pump". Three are covered (`AReadyFromAReloadedPageAndAnEventsCloseBothStopThePump`, `ASecondOpenReplacesTheFirstAndDropsItsLateFrames`). `Dispose` is only ever called in `PumpWorld.Dispose` with nothing asserted after it, so `ReviewHost.Dispose() -> _events.Dispose()` - the line that keeps a reader thread and a socket from outliving the pane - has no test. Deleting `_events.Dispose()` from ReviewHost.cs:405 leaves the suite green.
  - Fix: Add a case that opens a stream, waits for a frame, disposes the host, then sends another frame from the still-open connection and asserts nothing further is posted and the server sees the connection drop.
- **extractor/SwReview.AddIn/Review/ReviewHost.cs:524** (events) - `LastEventId` accepts a JSON number as well as a string ("A number as well as a string, because the page's own `seq` is a number"). The page does not send a number: app.js:274 sends `state.lastSeq > 0 ? String(state.lastSeq) : null`, and the contract row says `{chat_id, last_event_id | null}`. The numeric branch is unrequested flexibility for a caller that does not exist, and no test reaches it - every `events.open` in the suite passes `last_event_id = (string?)null`. Per the repo's "no configurability that wasn't requested", it is either a branch to delete or a branch to test.
  - Fix: Preferred: drop the `JsonValueKind.Number` branch and keep string-or-null, which is what the contract and the only caller say. If it is kept deliberately, add the two tests it needs - a numeric `last_event_id` arriving as the `Last-Event-ID` header, and `0` / a negative arriving as no header at all - since `Blank`, the `> 0` guard and the invariant-culture format are all untested today.
- **extractor/SwReview.AddIn/Review/EventStreamPump.cs:277** (events) - A narrow race lets a replaced pump still post `events.closed`. `Closed` checks and bumps `_generation` atomically, but calls `_onClosed` after releasing the lock. If a reader is ending naturally at the moment the page sends a new `events.open`, `Closed` can pass its generation check first, then `Open`'s `Close()`/`++_generation` runs and starts the replacement, and only then does the old thread post `events.closed`. The page's `payload.chat_id === state.chatId` guard does not help on the common case, which is a reconnect to the SAME chat: `scheduleReconnect` fires against a stream that was just opened, sets `state.stream = null` and schedules a duplicate `openStream()`. Combined with the backoff regression above this becomes a self-sustaining open/close loop. Frames are immune - `Deliver` re-checks under the lock on every frame - it is only the terminal message that escapes.
  - Fix: Capture the decision and the callback invocation under one guard: have `Closed` return the reason (or null) from inside the lock and invoke `_onClosed` only when the caller is still the current generation at invocation time, e.g. re-check `generation == _generation - 1` from the lock exit, or serialise `Open`/`Close`/`Closed` on the same gate with the callback dispatched by whoever won. A test that ends a connection and calls `Open` from another thread in a tight loop pins it.
- **extractor/SwReview.AddIn/Review/ReviewPage/app.js:278** (events) - `showStreamState('Streaming.')` now fires the instant `events.open` is posted, before anything has connected. With the backend down the engineer sees "Streaming." for up to a second, then "the review backend is not running. Reconnecting...", then "Streaming." again - a status line that alternates between a claim and its contradiction. The old code showed it only after `response.ok`.
  - Fix: Same fix as the backoff: move `showStreamState('Streaming.')` into the first `events.frame` for the current chat. Until then the honest text is "Connecting...".
- **extractor/SwReview.AddIn/Review/ReviewHost.cs:405** (events) - `Dispose` -> `_events.Dispose()` -> `Close()` joins the reader for up to `StopTimeout` (5s). `SwReviewAddIn.DisconnectFromSW` runs on the SOLIDWORKS UI thread and disposes the review host there (SwReviewAddIn.cs:~277). The join is bounded, which is what the brief asked for, but the bound is a 5-second SOLIDWORKS freeze on add-in unload in the case where the abort does not unblock the socket read promptly. `events.close` and `events.open` do not have this problem - they are handled on the dedicated page-message pump thread.
  - Fix: Either shorten the bound on the dispose path (the reader is a background thread; 500ms is enough to observe an abort that worked, and a wedged one dies with the process), or dispose the pump off the UI thread before the UI-thread teardown. Say which in the comment, because "bounded" alone does not tell the next reader that this bound lands on the application thread.
- **extractor/SwReview.AddIn.Tests/ReviewPageContractTests.cs:160** (events) - The forbidden-strings scan (`text/event-stream`, `getReader`, `ReadableStream`) is good but does not cover the shape most likely to come back: a page that builds the events URL again. `sessionPath('/events')` contains none of those three strings, and `fetch` itself cannot be banned because the page still calls `/messages`, `/stop`, `/evidence` and the disposition route through it. The test's own docstring says the second half "is the point of the round", so it should name the route.
  - Fix: Add `"/events"` to the forbidden list (after confirming no other page names it), or assert on the stripped source that no `fetch(` argument expression contains `events`. Cheap, and it fails the exact regression the test was written for.
- **extractor/SwReview.AddIn/Review/ReviewHost.cs:538** (events) - The numeric branch of `LastEventId` (`JsonValueKind.Number` -> `seq.ToString(InvariantCulture)`) is unreachable from the only caller: app.js line 274 sends `state.lastSeq > 0 ? String(state.lastSeq) : null`, always a string or null. The doc comment justifies it as "the page's own seq is a number and asking it to stringify one before sending it would be a rule to forget" - but the page does stringify, so this is flexibility that was not requested and that nothing tests.
  - Fix: Either delete the numeric branch (explicit over clever: the contract row says `{chat_id, last_event_id | null}` and the page sends a string), or keep it and pin it with the numeric test case named in must_fix #2. Do not leave it untested and unreachable.
- **extractor/SwReview.AddIn.Tests/EventStreamPumpTests.cs:193** (events) - `ASecondOpenReplacesTheFirstAndDropsItsLateFrames` is weaker than it reads, in two ways. (1) `Open` calls `Close` first, which aborts the first request, so the first connection's writer thread throws on the next write and the late frame usually never reaches the reader at all - the generation guard in `Deliver` is not really the thing under test, and the test would likely still pass with that guard removed. (2) The only closure assertion is `Assert.DoesNotContain("chat-8", ...)`, but the spurious close the abort would produce is for `ChatId`, not `chat-8`, so a regression that let the replaced reader post `events.closed` for the old chat passes here. (The guard is separately covered by `CloseStopsTheReaderAndSaysNothingFurther`, so this is about this test's strength, not a hole in the implementation.)
  - Fix: Add `Assert.Empty(recorded.Closed);` to this test, and rename or re-scope it to what it actually proves (a second open opens a second connection and the recorder only ever sees the new chat's frames). To exercise the guard properly, drive `Deliver` past a generation bump without an abort - e.g. a second `Open` for the same chat while the first server is mid-frame - or state plainly in the docstring that the abort is the primary mechanism and the generation counter is the backstop.
- **extractor/SwReview.AddIn.Tests/EventStreamPumpTests.cs:146** (events) - No test covers a non-200 answer on the event route. `GetResponse()` throws `WebException` for 401/404/500, which lands in the catch at EventStreamPump.cs:184 and posts `events.closed` with a raw .NET string ("The remote server returned an error: (404) Not Found."). That is the single most likely real-world failure of this route (unknown chat id, rotated token, backend restarted) and it is the path that feeds the reconnect loop in must_fix #1.
  - Fix: Add a case where the `SseBackend` answers the request with 404 (and one with 401) and assert `events.closed` arrives once, carries the chat id, and that no frame was delivered.
- **extractor/SwReview.AddIn/Review/ReviewHost.cs:405** (events) - `_events.Dispose()` in `ReviewHost.Dispose` is untested. `PumpWorld.Dispose` calls `_host.Dispose()` but asserts nothing about it, so deleting line 405 leaves the whole suite green while every pane close leaks a reader thread and an open socket against a backend that is being shut down. The brief lists `Dispose` alongside `events.close`, a new `events.open` and `ready` as one of the four things that must stop the pump; the other three are tested and this one is not.
  - Fix: Extend `AReadyFromAReloadedPageAndAnEventsCloseBothStopThePump` to a third stopper, or add a sibling test: open a stream, dispose the host, send another frame from the connection, sleep, and assert no further `events.frame` and no `events.closed` were posted.
- **extractor/SwReview.AddIn.Tests/EventStreamPumpTests.cs:81** (events) - `AMultiLineDataFrameIsNotSplit` writes the whole frame in a single `Write` + `Flush` (SseConnection.Pump), so it does not test what its name claims - a chunk-based reader that split on `\r?\n\r?\n` in a buffer would pass it too. What it genuinely proves is that the `ReadLine` lines are rejoined with `\n`, which is worth having but is not the brief's case ("a frame arrives in two reads as often as in one", per the Drain docstring).
  - Fix: Add a case that sends one frame in two writes with a flush between them, splitting mid-line (e.g. `"id: 9\ndata: {\"se"` then `"q\":9}\n\n"`), and assert one frame arrives, verbatim. That requires a small `SendRaw(string)` on `SseConnection` that writes without appending the frame terminator.
- **extractor/SwReview.AddIn.Tests/ReviewPageContractTests.cs:161** (events) - The "page no longer reads the stream itself" assertion bans `text/event-stream`, `getReader` and `ReadableStream`, but not the route. A regression that reintroduced `fetch(backendUrl(sessionPath('/events')))` - with `EventSource`, or reading `response.text()` in a loop - would pass all three. The route string appears nowhere in page code today (only in comments, which `Strip` removes), so the check is free.
  - Fix: Add `"/events"` to the `gone` array, with the message naming the route rather than the API.
- **extractor/SwReview.AddIn/Review/ReviewPage/app.js:167** (events) - The `events.frame` guard tests only `payload.chat_id === state.chatId`, while `events.closed` and `scheduleReconnect` correctly test `state.stream`. After `endSession` calls `closeStream()` (line 564), `state.stream` is null but `state.chatId` is unchanged, so any frame already in flight from the host still appends a card after "The session ended." It is a small, benign window, but it is the one place where the page's two stream guards disagree with each other.
  - Fix: Make the two symmetric: `if (state.stream && payload.chat_id === state.chatId)` in the `events.frame` branch. `openStream` sets `state.stream` synchronously before posting `events.open`, so no legitimate frame can be dropped by this.
- **extractor/SwReview.AddIn/Review/ReviewPage/app.js:278** (events) - `showStreamState('Streaming.')` is written the moment `events.open` is posted, before the host has opened a socket - the page says it is streaming when all it knows is that it asked. With must_fix #1 unfixed it also repaints "Streaming." once a second against a backend that is refusing, so the banner alternates between a failure reason and a claim that is false. This is the same 'unknown stays unknown' rule the round holds elsewhere.
  - Fix: Fold this into the must_fix #1 change: leave `showStreamState('Connecting...')` (or nothing) at the open, and set `Streaming.` together with the backoff reset when the first `events.frame` arrives.
- **extractor/SwReview.AddIn/ToolService/ToolServiceGate.cs:251** (tool-service) - A document change that arrives while a start is in flight is dropped permanently, and the comment that justifies dropping it is only conditionally true. The guard at line 251 returns when `_starting`, and line 253 explains 'A start already in flight will attach to whatever is active when it runs'. `StartCore` attaches on the application thread (`ToolServiceHost.Start` -> `OnApplicationThread(options.Invoker, ...)`, ToolServiceHost.cs:712) while `_starting` stays true until after the attach returns and the pipe is listening (ToolServiceGate.cs:364). `ActiveDocChangeNotify` is delivered on that same application thread, so if it lands *after* the attach delegate captured document A but *before* the worker reaches line 364 — the window covering `server.Start()` and the first log write — `FollowDocument` returns at 251 and nothing ever re-checks. The service stays attached to A until the engineer happens to switch documents again. Narrow, but it is the original bug re-created, and no test covers `FollowDocument` during `_starting` at all.
  - Fix: Record the dropped path instead of discarding it: add a `private string? _pendingFollow;` set under the lock at line 251 when the guard fires because of `_starting`, and at the end of `StartCore` (after `_started(service)` succeeds) read and clear it under the lock and, if set, call `FollowDocument(pending)` again. Add a test with the injected `schedule` seam: `EnsureStarted`, then `FollowDocument(other)` while the start is still pending, then run the pending action — assert a second start happens and the gate ends attached to `other`. Correct the comment at line 253 while you are there.
- **extractor/SwReview.AddIn/ToolService/ToolServiceGate.cs:279** (tool-service) - No de-duplication of the busy log line. SOLIDWORKS raises `ActiveDocChangeNotify` on every activation, including simply clicking between two already-open document windows, so during one long review turn the engineer flipping windows gets the identical 'not re-attaching to X: a turn is running against Y' line appended over and over. Each line is a separate `File.AppendAllText` open/write/close (ToolServiceLog.Write, ToolServiceHost.cs:470-483) and — until must_fix #1 lands — each one is preceded by a fresh round of HTTP busy questions on the application thread. The log the finding points the engineer at (`tool-service-*.log`) becomes harder to read, not easier, which is the opposite of the point.
  - Fix: Keep the last logged pair on the gate (`private string? _lastBusyLog;`) and write only when `(activePath, attached.DocumentPath)` differs from it, clearing it whenever a restart actually happens or the service is replaced. Assert in `ABusyBridgeIsNotRestartedAndTheLogSaysWhyNamingBothDocuments` that three `FollowDocument` calls for the same active path while busy still produce exactly one line (the test already uses `Assert.Single`, so it only needs the extra calls).
- **extractor/SwReview.AddIn/ToolService/ToolServiceGate.cs:273** (tool-service) - When the busy question throws, the engineer is told in the wrong place. The docstring at line 239 says 'A busy question that throws counts as busy', and the behaviour is right, but the reason goes only to `_report` — the add-in log and the pane — while the *tool-service* log, which is the file finding 1 sends the engineer to and the file that carries the 'attached to ...' line, gets nothing. The engineer who switches documents, sees stale-document errors and opens `tool-service-*.log` finds no explanation at all in the one case where the gate genuinely could not decide. The test `ABusyPredicateThatThrowsIsTreatedAsBusyAndReported` (ToolServiceWiringTests.cs) asserts only `world.Reports` and does not notice.
  - Fix: Call the same `Log(attached, ...)` on the throw path with a reason of its own — e.g. `"not re-attaching to " + activePath + ": could not tell whether a turn is running against " + attached.DocumentPath` — before returning, and extend the test to assert the line lands in `world.Services[0].LogLines` as well as in `Reports`.
- **extractor/SwReview.AddIn/SwReviewAddIn.cs:736** (tool-service) - The busy predicate does not cover everything that holds the bridge, and nothing re-points the consumers that cached the old one. (a) A live Terminal ('Ask') session is not counted: `CliProfileWriter` bakes `{pipe, secret}` from `GeneralChatBridge` into the generated CLI profile at terminal start (Terminal/CliProfileWriter.cs:378), so a restart mid-session leaves the running CLI pointed at a disposed pipe with a retired secret — its MCP tool calls fail with a connect/unauthorized error rather than the documented `document no longer open`. (b) The same is true of any backend chat created earlier: `POST /sessions` carries the bridge once, at creation (ReviewHost.cs:882), and only new sessions see the re-pointed `reviewOptions.Bridge`; an idle-but-open chat's next turn uses the dead pipe. (c) Secret hygiene: `TerminalHost.Secrets` reads the *current* general-chat secret late (`() => new[] { toolService.GeneralChatBridge?.Secret }`, Terminal/TerminalHost.cs:283), so after a restart the retired secret drops out of the redaction set used at TerminalHost.cs:1136 while a live CLI process still holds it in its profile — an error message that echoes the profile or env would now pass through unredacted. Before this change secrets never rotated inside one SOLIDWORKS session, so none of this could arise.
  - Fix: Three separable steps, in priority order: (1) add 'a terminal session is attached' to the `busy` lambda so the common case simply does not restart under a live Ask session (and log it, which the existing wording already covers); (2) have `TerminalHost` keep retired secrets in its redaction list — change `Secrets` to return an accumulating set rather than only the current bridge's secret — so a rotation can never un-redact something; (3) decide explicitly, with the owner, what should happen to an already-created chat's stale bridge: either re-write the profile / restart the terminal on the `_started` callback, or accept it and say so in the doc. (3) is a design call, not a code fix I would make unilaterally.
- **docs/pane-findings-2026-09-16.md:48** (tool-service) - Finding 1 has no closing '**Fixed.**' line, so the doc still reads as an open defect while the code that fixes it is in the tree. The brief's item 5 asks for one on each of the two findings; finding 2 has one at line 90, finding 1 ends at 'Worth considering.' (line 48) with nothing after it. The doc's own intro (line 7-8, 'Both are real and reproducible, and neither is fixed.') is now stale for both findings.
  - Fix: Append a '**Fixed.**' paragraph under finding 1 naming what landed — `ToolServiceGate.FollowDocument`, called from `SwReviewAddIn.OnActiveDocumentChanged` after `EnsureStarted`, skipped while a review turn or remodel run holds the bridge and logged into the tool-service log when it is, plus `DocumentPresenceDispatcher.DocumentClosedError(attachedDocumentPath)` now naming the attached document — and amend the intro sentence at line 7-8 so it no longer says neither is fixed.
- **extractor/SwReview.AddIn/ToolService/ToolServiceGate.cs:401** (tool-service) - `Canonical` is under-tested for the cases it exists to handle, and one of its behaviours is surprising. `Path.GetFullPath` resolves a relative path against the *process* working directory, which for an add-in hosted in SLDWORKS.exe is whatever SOLIDWORKS last set it to — so two relative spellings could canonicalise to two different absolute paths, or to the same wrong one. It also does not normalise 8.3 short names, `subst` drives, or a UNC path versus the mapped drive that points at the same share, all of which are ordinary in a PDM/vault setup and all of which would read as 'different document' and trigger a restart. The documented fallback (line 407-410: `GetFullPath` throws -> compare the trimmed raw strings, because 'differ' is the answer that restarts) is the right call, but no test drives it: `TheSameDocumentSpeltDifferentlyDoesNotRestartAnything` covers only case and a `..` segment.
  - Fix: Add two cases to `TheSameDocumentSpeltDifferentlyDoesNotRestartAnything`'s neighbourhood: a path containing a character `GetFullPath` rejects on both sides (asserting the raw-string fallback still says 'same', so no restart), and a trailing-separator pair (`C:\models\frame.SLDASM` vs `C:\models\frame.SLDASM\` is already handled by the `TrimEnd`, but nothing pins it). Then state the known limits in the `SameDocument` docstring — short names, substituted drives and UNC-vs-mapped spellings restart once and then settle, which is acceptable because the restart is idempotent — so the next reader does not have to rediscover them.
- **extractor/SwReview.AddIn/ToolService/ToolServiceGate.cs:251** (tool-service) - A document switch that arrives while the FIRST start is in flight is dropped permanently. FollowDocument returns at line 251 when _starting is true, and the comment at 252-253 ('A start already in flight will attach to whatever is active when it runs') is only true up to the moment SwSession.Attach reads the active document; SwScope.Open then walks the component tree, which docs/review-backlog.md:422 records as tens of seconds on a large assembly. A switch during that walk leaves the gate attached to the old document with no event left to correct it, which is exactly finding 1's symptom, until the engineer switches documents again.
  - Fix: Record the last requested active path when FollowDocument bails on _starting, and re-evaluate it at the end of StartCore (after _service is published): if the newly attached DocumentPath differs from the recorded request, call FollowDocument again. Add a test: schedule captured manually, EnsureStarted, FollowDocument('other') while the start is pending, run the pending start (which attaches to the old path), and assert a second start follows and the gate ends on 'other'.
- **extractor/SwReview.AddIn.Tests/ToolServiceWiringTests.cs:385** (tool-service) - FollowDocumentStartsNothingBeforeTheFirstService is near-tautological: it sets DocumentOpen = false, so Starts stays 0 even for an implementation that wrongly schedules StartCore from FollowDocument - StartCore would bail on _documentAvailable() and the assertions would still pass. The test's own docstring says the property under test is 'starting is EnsureStarted's job', which it does not actually pin.
  - Fix: Set DocumentOpen = true (leave the gate with no service) and assert Starts == 0 and gate.GeneralChatBridge is null. That version fails if FollowDocument ever schedules a start of its own.
- **extractor/SwReview.AddIn/ToolService/ToolServiceGate.cs:279** (tool-service) - The busy branch logs and probes on every call. SOLIDWORKS raises ActiveDocChangeNotify on each window/tab activation, so while one long review turn runs against document A and the engineer works in B, each activation writes another identical 'not re-attaching to B: a turn is running against A' line into the tool-service log and re-runs the HTTP busy probe per open chat. The test only exercises a single call (Assert.Single(LogLines)), so the repetition is unpinned.
  - Fix: Remember the last (attached path, active path) pair the line was written for and skip the log - and, if cheap, the probe - when it is unchanged; extend ABusyBridgeIsNotRestartedAndTheLogSaysWhyNamingBothDocuments to call FollowDocument three times with the same active path and still assert Assert.Single(LogLines).
- **extractor/SwReview.AddIn/Remodel/RemodelHost.cs:173** (tool-service) - RunInProgress is a new public member with no test of its own; every gate test uses GateWorld's Busy flag, so nothing asserts that the half of the add-in wiring that reads a remodel run actually reports one. If _busy's meaning drifts (it is also the plan/start refusal flag, set at 471/752 and cleared at 612/800), the tool service would restart underneath a running remodel and nothing would fail.
  - Fix: Add ~5 lines to RemodelHostTests beside ASecondStartWhileTheRunIsInFlightIsRefusedAsRunInProgress: assert host.RunInProgress is false before the start, true while the run is in flight, and false once it completes. Same for ToolServiceHost.WriteLog if it is reachable from a test seam.
- **extractor/SwReview.AddIn/SwReviewAddIn.cs:862** (tool-service) - Nothing pins the add-in-side wiring itself: that OnActiveDocumentChanged calls FollowDocument(CurrentDocument()?.Path) after EnsureStarted(), and that busy is the OR of the two hosts. ToolServiceWiringTests' header explains why SwReviewAddIn cannot be tested directly, so this matches existing precedent - but the busy lambda at 735-736 is new logic, and a null-guard or an inverted OR there would silently disable the whole feature with a green suite.
  - Fix: Either accept it explicitly (precedent), or lift the busy expression into a small static helper on the gate side (e.g. ToolServiceGate.Busy(Func<bool> review, Func<bool> remodel)) that a test can drive with each combination of null/true/false.
- **docs/pane-findings-2026-09-16.md:29** (tool-service) - The brief (item A5) requires both findings to gain a 'Fixed' line; only finding 2 has one (line 90). Finding 1 - the finding Part B implements - still reads as open, and lines 16 and 29 still quote the superseded error wording with no note that the message now names the attached document.
  - Fix: Add a 'Fixed.' paragraph under finding 1 naming ToolServiceGate.FollowDocument, the SwReviewAddIn.OnActiveDocumentChanged call site and DocumentPresenceDispatcher.DocumentClosedError(string), and quoting the new sentence. If the docs update is another agent's task in this stage, hand it the wording rather than editing the file.
- **reviewer/src/swreview/cli.py:925** (check-rms) - The command still says nothing about which exception store it graded, and after this change that omission can hide a real substitution: with `--out`, a carried-forward exceptions.json in the run folder wins over exceptions.json beside the package (checks/rms/run.py:583), so waivers an engineer accepted against the package can silently stop applying because an unrelated sibling run of the same design_id was copied in first. Neither the JSON payload nor the human lines carry run.exceptions_carried_forward, although check.json and the route's CheckResult both do. Before this change the two paths were always the same file, so the precedence had no observable consequence.
  - Fix: Add "exceptions_carried_forward": {"from_run": ..., "count": ..., "reason": ...} to the payload and one human line next to `session:`/`report:` naming the store that was graded ("carried forward N exception(s) from <run>", or the reason when nothing was carried). Two lines, matching what check.json already records, and it makes the new precedence visible instead of inferred.
- **reviewer/src/swreview/checks/rms/run.py:583** (check-rms) - _store_to_grade_against introduces a precedence rule (run folder first, package second) but no test exercises the case where both files exist. test_rms_run.py:657 covers carried-only and test_rms_run.py:680 covers package-only. The conflicting case - the one where an engineer's package waivers are shadowed by a carried copy - is the only case where the rule is a decision rather than a tautology, and it is unpinned.
  - Fix: Add a case in TestCarryForward: the package holds a store waiving rule A, an earlier sibling run holds a store waiving rule B, run with out_dir under that run root, then assert B is silenced, A is not, and the package's file is still byte-for-byte what it was. That is the sentence the docstring claims; pin it.
- **reviewer/tests/unit/test_cli.py:1925** (check-rms) - The move of the run root from the package's parent to `--out`'s parent is tested only in the positive direction. Nothing asserts what the finding actually asked for: that a folder sitting beside the PACKAGE, holding the same design_id and an exceptions.json, is no longer read as an earlier run. That is the hazard docs/pane-findings-2026-09-16.md and docs/review-backlog.md:773 named (a foreign store copied in from an arbitrary sibling of an arbitrary --package directory), and it is now closed by construction with no test holding it closed.
  - Fix: Add a case: build the package under tmp_path/pkgroot/pkg with a same-design sibling tmp_path/pkgroot/other holding an exceptions.json, run with --out tmp_path/runs/r1, and assert nothing was carried (check.json's exceptions_carried_forward.from_run is None, no exceptions.json in the run folder) and the finding is not waived.
- **reviewer/src/swreview/chat/server.py:639** (check-rms) - The CLI can now create folders that is_check_folder (checks/rms/run.py:777) accepts but check_result cannot read: read_rms_check needs only check.json plus the session it names, while check_result does load_package(check_dir), and a `--out` run folder holds no package.json. Since the new guidance encourages putting run folders under a run root whose siblings are earlier checks, an engineer who points --out under the pane server's --run-root makes GET /checks/{check_id} answer that folder with InvalidPackage instead of the check it holds, and POST /sessions treats it as a claimable check folder. POST /checks/rms itself is unaffected (verified).
  - Fix: Either record the package directory in check.json and have read_rms_check carry it on RmsCheckRun.package_dir so check_result loads the package the check actually graded (one field plus a back-compatible read), or state in specs/003-resilient-modeling/contracts/cli.md that --out must not name a folder under a running pane's run root. The first is the explicit fix; the second is free but leaves a trap.
- **benchmarks/README.md:67** (check-rms) - Two more documented invocations are now usage errors: benchmarks/README.md:67 (`swreview check rms --package ../benchmarks/packages/<name>/native --scope part`) and benchmarks/native/rms-part/RECIPE.md:257 and :262. These are the workstation recipes an engineer follows with SOLIDWORKS open, so they fail at the least convenient moment.
  - Fix: Add `--out <run dir>` to all three lines, matching the form specs/003-resilient-modeling/quickstart.md now uses for the native package (`--out $runs\native\run-1`). Outside the brief's named files, so raise it with the owner before editing.
- **README.md:80** (check-rms) - The quick-reference line still reads `swreview check rms --package <package dir>           # findings, coverage, session.json, report.md`. That invocation now exits 2 (missing required option), and the comment still describes files landing beside the package. The brief did not name README.md, so the implementer was right not to touch it, but it is now the most visible wrong instruction in the repo.
  - Fix: Owner call: either add README.md to this round's scope and make it `swreview check rms --package <package dir> --out <run dir>   # findings, coverage; run folder holds session.json, report.md, check.json`, or leave it and carry it as a known-stale line. Recommend fixing it - one line, zero risk, and it is the line an engineer copies.
- **specs/001-agentic-design-review/contracts/cli.md:42** (check-rms) - Feature 001's CLI contract still documents `swreview check rms` with no `--out` and says "it writes `session.json` and `report.md` into the package directory". It now directly contradicts specs/003-resilient-modeling/contracts/cli.md:7. The brief named only the 003 contract, so this is a scope question rather than an implementer error, but two contracts in the tree now describe the same command incompatibly.
  - Fix: Ask the owner: (A) update the 001 row to match 003 - one row, no code, keeps the contracts consistent; (B) leave it, on the rule that a feature's contract is frozen at its feature, and accept that 001 is superseded by 003 for this command; (C) replace the 001 row with a pointer to 003's. Recommend A - the contracts are read as current documentation here, and a frozen-but-wrong row costs more than the edit.
- **reviewer/src/swreview/cli.py:669** (check-rms) - `_context_for(package_dir, *, exceptions: bool = False)`: the `exceptions=True` branch now has no caller anywhere. All four in-tree call sites (cli.py:756, 779, 805, 826) use the default, and the only `exceptions=True` caller in the repo was `rms_run` in test_cli_exceptions_accept_rms.py, which this change deleted. Ten lines of docstring and two lines of body are now dead. CLAUDE.md section 3 asks for orphans created by a change to be removed.
  - Fix: Drop the parameter and the `load_exceptions`/`refresh` lines, simplifying to `return build_context(load_package(package_dir))`, and drop the now-unused `load_exceptions` import if nothing else in cli.py uses it. Alternative (do nothing): harmless but leaves a documented option nothing exercises, which is exactly the kind of thing that later gets wired up wrongly. Recommend removing it, but flagging it to the owner first since it is a src edit slightly outside the brief's letter.
- **reviewer/src/swreview/cli.py:914** (check-rms) - Nothing refuses `--out` pointing at the package directory itself. `swreview check rms --package pkg --out pkg` writes `session.json`, `report.md` and `check.json` into `pkg` and sets `run_root` to the package's parent - exactly the pre-fix behaviour, including against a golden fixture. The brief's guarantee ("the package directory is never written") is therefore conditional on the operator, and no test covers the case.
  - Fix: Three options. (A) Do nothing: it is an explicit request, and `--out` being required already removes the accidental path that caused the defect. (B) Refuse when `out_dir == Path(package).resolve()` with a one-line message, plus one test - three lines, explicit, and makes the contract sentence unconditional. (C) Refuse only when the package is under `tests/golden/fixtures` - narrow and magic, I would not. Recommend B if you want the contract to read as an absolute, A if you would rather not grow the command; either way add a test pinning the chosen behaviour, because right now it is unstated.
- **reviewer/tests/unit/test_cli_exceptions_accept_rms.py:366** (check-rms) - Two assertions were weakened from `assert not (run_dir / REPORT_FILE_NAME).exists()` to a byte-comparison against the report the check itself wrote (also at line 396). The change is forced - `rms_run` now goes through `run_rms_check`, which writes a report - but a byte-compare no longer detects a re-render that happens to produce identical bytes, which is the likely shape of "accept-rms accepted nothing and re-rendered anyway".
  - Fix: Keep the byte-compare and add `st_mtime_ns` (captured before the call) to the assertion, or assert on the report file's stat only. Both tests already assert `body["report_file"] is None`, which carries most of the weight, so this is a small tightening rather than a hole - but the lens asked whether each assertion would still fail for the behaviour it guards, and this pair would not.
- **reviewer/src/swreview/checks/rms/run.py:370** (check-rms) - The comment above `out.mkdir` claims the placement means "a run that is refused for its arguments leaves no folder behind". True for the two refusals above it, but `UnreadableExceptionsError` from the carry-forward and `RmsRunError` from `_dispatch` both fire after the mkdir, so a refused run can leave an empty `out` (and, for the dispatch failure, a carried-forward `exceptions.json`). Nothing asserts what `out` holds after a refusal; `test_check_rms_reports_a_rule_that_cannot_grade_as_an_error_not_a_traceback` now creates one and checks nothing about it.
  - Fix: Add one assertion to that existing test - after the exit-1, `out` either does not exist or holds no `session.json`/`report.md`/`check.json` - pinning whichever is the intended answer, and reword the comment to "after the two argument refusals" so it stops implying more than it delivers. No implementation change needed if a bare empty folder is acceptable; if it is not, move the mkdir down to just before `_carry_forward` - but the carry-forward needs it, so an empty folder after a dispatch failure is unavoidable and should simply be stated.
- **reviewer/src/swreview/cli.py:949** (check-rms) - The human output gained `session: <path>` and `report: <path>` but not `check: <path>`, while the JSON payload names all three (`session_file`, `report_file`, `check_file`). An engineer reading the terminal is told about two of the three files the run just wrote, and `check.json` is the one `read_rms_check`/`GET /checks/{id}` answers from.
  - Fix: Add `lines.append(f"check: {run.check_file}")` and extend `test_check_rms_human_output_names_the_run_folder` to assert it - two lines, keeps the two output shapes saying the same thing. Alternative: drop to a single `run folder: <out_dir>` line and let the payload carry the filenames, which is arguably cleaner but changes the line the quickstart now describes. Recommend adding the third line.

## Proxy option B round (2026-09-16)

- **extractor/SwReview.AddIn.Tests/ReviewPageInjectionTests.cs:428** (serving) - `ServeThePagesTheWayTheAddInDoes` is named for serving "the way the add-in does", and the constraint was that the helpers "serve the same way so the page tests exercise the real path". It shares the `PageFileServer` class and the `TaskPaneControl.PageResourceFilter` constant, but its handler body is a second, independent implementation: it does not call `BackendProxyHandler.TryServe` first, it does not take a deferral, it does not guard the empty-reason phrase, and it has no try/catch. So every page test in the assembly exercises the file server but not the handler ordering, the deferral discipline, or the UI-thread/off-thread split that `TaskPaneControl.ServePageResource` actually implements. A regression in the pane's ordering (proxy-first) would leave all ten page-test files green; only `BackendProxyPageTests.TheShippedPaneServesItsOwnPagesAndItsBackendCallsThroughOneHandler` would catch it. Two handler bodies for one documented rule is also a DRY violation of exactly the kind the decision text called out.
  - Fix: Extract the wiring from `TaskPaneControl.AttachPageAsync` + `ServePageResource` into one internal static entry point - e.g. `TaskPaneControl.ServeOrigin(CoreWebView2 core, CoreWebView2Environment environment, BackendProxyHandler? proxy, PageFileServer files)` that registers the filter and installs the real handler - and have both the pane and `ServeThePagesTheWayTheAddInDoes` call it, the helper passing a proxy over a null endpoint (which already returns null outside the prefix). One body, one filter, one ordering, and the page tests then genuinely exercise the shipped path. If that is judged too large a change now, at minimum wrap the helper's handler body in a try/catch so an exception cannot escape a WebView2 event on the STA loop and take the test host with it, and rename the method to say what it really does.
- **extractor/SwReview.AddIn/Remodel/RemodelHost.cs:386** (serving) - `RemodelHost.SendInit` still sends `{ origin: endpoint.Origin }` - i.e. the literal `http://127.0.0.1:<port>` (BackendProcess.cs:52) - together with `token`, while `ReviewHost.cs:533` and `ModelCheckHost.cs:284` now send `BackendProxy.PageOrigin`. The Remodel page cannot in fact reach loopback (its CSP is now `connect-src 'self'`, and `remodel.js` contains no fetch at all, both verified), so this is not a live escape. But it hands a renderer a loopback URL and a bearer token it has no use for, and it leaves one of the four hosts on the pre-proxy contract: the day someone adds a fetch to the Remodel page, or relaxes that one CSP, the failure is the exact web-filter interstitial this whole change exists to remove. docs/pane-backend-proxy.md:223 states the rule as "`init.backend.origin` is now `BackendProxy.PageOrigin`" and its Where column silently omits RemodelHost; section 2 does qualify it, so the two halves of the doc disagree about whether the rule is universal.
  - Fix: Send `BackendProxy.PageOrigin` from `RemodelHost.SendInit` as the other two hosts do, so all four pages are given the same, uniform, unusable-off-origin value, and add the assertion to `RemodelPageContractTests` alongside the ones in `ModelCheckHostTests`/`PageMessageTests`. If the Remodel page genuinely needs neither field, dropping `origin` (and `token`) from its init payload is the stronger version - explicit over clever - but that is a contract change, so confirm before doing it. Either way, make docs/pane-backend-proxy.md:223 say "all four hosts" or name the exception.
- **extractor/SwReview.AddIn/Review/PageFileServer.cs:146** (serving) - The XML doc on this class (lines 30-34) says "every segment is decoded first and then refused if it is empty, `.`, `..`, or carries a separator", but the code decodes the entire `AbsolutePath` and only then splits on '/'. The behaviour is actually safe - and arguably safer than the documented order, because `%2f` becomes a real boundary and the resulting `..` segment is then refused - but the consequence is that the "carries a separator" clause can only ever fire for a backslash, never for a forward slash, since a decoded '/' has already become a split point. A reader auditing this class against its own comment will conclude the forward-slash case is covered by the segment check when it is covered by the split. On the one line in the product that decides whether the pane can read the rest of the disk, the comment and the code should describe the same mechanism.
  - Fix: Reword the comment to state what the code does: the whole path is unescaped once, then split, so a decoded '/' becomes a segment boundary (and a '..' it produces is caught by the segment check), while a decoded '\\', ':' or wildcard is caught by `Path.GetInvalidFileNameChars()`. No code change; `TraversalOutOfTheWebFolderIsRefused` already pins both forms. Optionally add the one case the theory is missing - a double-encoded `%252e%252e%252f` - so the "decode exactly once" property is pinned rather than assumed.
- **extractor/SwReview.AddIn/Review/PageFileServer.cs:86** (serving) - `_root = Path.GetFullPath(webFolder).TrimEnd(Path.DirectorySeparatorChar)` degrades badly for a drive-root web folder. `PageFileServer("C:\\")` yields `_root == "C:"`, after which `Path.Combine("C:", "Windows", "win.ini")` produces the drive-relative path `C:Windows\win.ini`, `Path.GetFullPath` resolves it against the process's current directory on C:, and the containment check `candidate.StartsWith("C:" + '\\')` passes for every file on the volume. `TaskPaneOptions.WebFolder` defaults to `<assembly dir>\\web` so this is unreachable in the product today, but it is the one input that turns the containment gate into a no-op, and `WebFolder` is a public settable property. Separately, `TrimEnd` only strips `Path.DirectorySeparatorChar`, not `AltDirectorySeparatorChar`.
  - Fix: Guard the root in the constructor: after `Path.GetFullPath`, reject a path equal to its own `Path.GetPathRoot` (and a path that does not exist as a directory) with an `ArgumentException` naming the parameter, rather than trimming into a drive-relative spelling. Add two constructor tests - a drive root and a null/whitespace folder - since neither is covered today. Cheap, no effect on the shipping path, and it closes the only input that disarms the second traversal gate.
- **extractor/SwReview.AddIn/TaskPaneControl.cs:761** (serving) - `TaskPaneControl.ReadRequestContent` and `BackendProxyHandler.Read` (BackendProxyHandler.cs, the private `Read(Stream?)`) are the same nine lines implementing the same rule - copy the stream into a MemoryStream, and return null rather than an empty array when there are no bytes - and the second is applied to a MemoryStream that the first just built from the same bytes. So every proxied request with a body is copied twice and the "empty means null" rule is stated in two places that must agree for `ProxiedRequest.Body == null` to keep meaning "no body". `BackendProxyHandlerTests` pins the rule on one of them only.
  - Fix: Give `BackendProxyHandler.TryServe` a `byte[]?` body overload (the pane already has the bytes on the UI thread and must read them there), keep the `Stream?` overload delegating to it for the tests that exercise it that way, and delete `TaskPaneControl.ReadRequestContent`'s duplicate rule by making it the single definition. That removes one full copy of every POST body per request and leaves one place where "empty means no body" is decided.
- **extractor/SwReview.AddIn/TaskPaneControl.cs:323** (serving) - `_files = new PageFileServer(_options.WebFolder)` snapshots the web folder at construction, while `_proxy` is built over lazy callbacks (`() => _options.Backend()`, `request => _options.ProxySend(request)`) precisely so the options can change after the pane is built. `WebFolder` is a public `get; set;` on `TaskPaneOptions`, so a caller that sets it after constructing the control gets a file server pointed at the old folder with no error - the pane simply serves 404s and the tab never finishes loading. `BackendProxyPageTests.WithPane` happens to set it in the object initializer, so nothing catches this.
  - Fix: Pick one discipline and make it explicit. Either read the folder lazily like the other two (`new PageFileServer(() => _options.WebFolder)` or construct the server inside `ServePageResource`), or - better, since the folder genuinely never changes at runtime - move `WebFolder` to a constructor parameter of `TaskPaneOptions` / make it `{ get; }` with an init-time value, so "set after construction" stops being expressible. The second is smaller and matches how the folder is actually used.
- **extractor/SwReview.AddIn/Review/PageFileServer.cs:69** (serving) - Responses carry only `Content-Type` and `Cache-Control: no-store`. There is no `X-Content-Type-Options: nosniff`. The closed extension list makes MIME confusion unlikely today, but the pane now serves `.svg` (which renderers will treat as a document) and `.json`, and the folder-mapping sandbox that used to decide content types is gone - this class is the only thing that types a response now. One header is a smaller cost than reasoning about sniffing every time a new extension is proposed.
  - Fix: Append `\r\nX-Content-Type-Options: nosniff` to both the 200 and the 404 header strings, and extend `APageFileIsServedWithItsContentTypeAndNoStore` and `AssertNotFound` to assert it. Two lines, no behavioural risk. Do nothing is defensible given the closed list - flagging it because the contract in pane-host-messages.md now enumerates the response rules and this is the one it does not mention.
- **extractor/SwReview.AddIn/Review/PageFileServer.cs:181** (serving) - `Path.GetFullPath` canonicalizes `..`, `.` and short names but does not resolve NTFS reparse points, so a junction or symlink placed inside the web folder that points outside it passes the `StartsWith(_root + separator)` check and the file outside is served. The class doc calls the containment check "the line that decides whether the pane can read the rest of the disk", which slightly overstates what `GetFullPath` guarantees. Exploiting it requires write access to the add-in's install folder, at which point the attacker can replace the DLL - so the practical risk is near zero and "do nothing" is a reasonable answer.
  - Fix: Either (a) accept it and soften the class comment to say the check is textual containment of the canonical path, not reparse-point resolution, so the next reader is not misled about the guarantee; or (b) resolve the final path with `new FileInfo(candidate).Directory.FullName` plus a `FileAttributes.ReparsePoint` check on each resolved component before serving. My recommendation is (a): (b) adds a syscall per resource on the tab-load path to defend against an attacker who already owns the install directory, which is over-engineering for this threat model.
- **extractor/SwReview.AddIn/Review/BackendProxy.cs:19** (serving) - The class remarks still say WebView2 "raises `WebResourceRequested` for <see cref="ResourceFilter"/>", but `ResourceFilter` was deleted in this change - the filter is now `TaskPaneControl.PageResourceFilter`. The cref resolves to nothing (the build is at 0 warnings only because XML doc generation is off, so the compiler never checks it). This is the same failure mode the design doc gave as the reason for deleting the property: "a property documented as 'the filter WebView2 is given' that is not the filter WebView2 is given is a lie the next reader would act on". The property went; the sentence pointing at it stayed.
  - Fix: Change the cref to `<see cref="TaskPaneControl.PageResourceFilter"/>` and adjust the sentence: the filter is now the whole origin, not the prefix, and the same handler serves the page files. While there, consider turning on `<GenerateDocumentationFile>` for SwReview.AddIn so CS1574 catches the next one - the codebase leans heavily on these comments.
- **extractor/SwReview.AddIn/Review/PageFileServer.cs:107** (serving) - The `catch (IOException)` and `catch (UnauthorizedAccessException)` -> 404 branches have no test. The comment names a reachable scenario ("a build copying over it is the realistic one"), and this is the only place where a file that passed `File.Exists` still fails to be served - i.e. the one path where a live pane shows a blank tab for a file that is genuinely there. Deleting both catches would fail no test; the exception would instead escape into `ServePageResource`'s blanket catch and leave the request unanswered, which is a materially different outcome from a 404.
  - Fix: Two tests, both cheap and deterministic. (1) Hold `web/Review/ReviewPage/app.js` open with `new FileStream(path, FileMode.Open, FileAccess.Read, FileShare.None)`, then assert `Serve(Origin + "/Review/ReviewPage/app.js")` is the same 404 as every other refusal (reuse `AssertNotFound` plus the `EveryRefusalLooksTheSame` comparison). (2) While you are there, consider widening the catch - `File.ReadAllBytes` can also throw `SecurityException` and `OutOfMemoryException`, neither of which is currently answered, and the class's stated contract is that it "always answers".
- **extractor/SwReview.AddIn/TaskPaneControl.cs:323** (serving) - `TaskPaneOptions.WebFolder` is a public settable property (line 88) but `TaskPaneControl` captures it once in the constructor (`_files = new PageFileServer(_options.WebFolder)`), while the two options beside it, `Backend` and `ProxySend`, are read late through callbacks on line 322. Setting `WebFolder` after the control is constructed therefore has no effect at all, silently. That matters more than the usual mutable-option smell because the property is now documented as "the boundary a page request may not escape" (lines 85-87): a caller who set it would believe they had moved the security boundary. `BackendProxyPageTests.WithPane` happens to set it in the initializer, which is why nothing catches this.
  - Fix: Pick one and be explicit. Cheapest: make `WebFolder` get-only (constructor or initializer-only setter) so the compiler enforces what the code already assumes. Alternative, consistent with `Backend`/`ProxySend`: construct the `PageFileServer` lazily in `AttachPageAsync` from `_options.WebFolder`. If you keep it as is, at minimum document that it is read once, in the constructor. My recommendation is get-only: it is the explicit option and it costs one keyword.
- **extractor/SwReview.AddIn.Tests/ReviewPageInjectionTests.cs:441** (serving) - `ServeThePagesTheWayTheAddInDoes` claims in its own doc comment to serve "exactly as `TaskPaneControl.AttachPageAsync` does", and it diverges in two ways. It drops production's empty-reason normalization (`string.IsNullOrEmpty(answer.Reason) ? "OK" : answer.Reason`, TaskPaneControl.cs:722-723), and it omits `BackendProxyHandler.TryServe` entirely - so a page test that ever fetched `/__backend/...` would get `PageFileServer`'s plain-text 404 rather than the proxy's `{error_class, message, retryable}` 503, which is a different shape from the one the pages parse. Nothing currently does (ModelCheckPageTests stubs `window.fetch`), so this is a latent trap for the next page test rather than a live failure.
  - Fix: Either narrow the claim - say it serves the page FILES the way the add-in does, and that `/__backend` is not wired here because no page test fetches - or close the gap by giving the helper a `BackendProxyHandler` over a fake transport and the same `TryServe(...) ?? files.Serve(...)` chain. I would narrow the claim: the helper is a test harness and adding a second server to it buys nothing until a page test needs one. Also use the same empty-reason normalization, so the two sites cannot disagree about what an empty reason means.
- **extractor/SwReview.AddIn.Tests/PageFileServerTests.cs:167** (serving) - Two of the seven `TraversalOutOfTheWebFolderIsRefused` cases carry no signal. I confirmed on this machine's .NET Framework 4.8 that `Uri` collapses `/../secret.html` and `/Review/../../secret.html` to `/secret.html` before `PageFileServer` sees them, so those two pass because `web\secret.html` does not exist - they would still pass with the entire segment rule at lines 153-161 deleted. The docstring acknowledges the clamping, but the cases sit in a theory named "traversal ... is refused" and are counted toward the 58. The `Assert.DoesNotContain("outside", ...)` is likewise decorative: the 404 body is the constant "Not found.", so it can only fire if the 200 path is taken, which the status assertion already caught. (The other five cases - `%2e%2e`, `..%2f`, `%5C` - are genuinely load-bearing; I verified `Uri` leaves all three escaped.)
  - Fix: Keep the two clamped cases as regression guards but move them into their own small test named for what they actually assert - that a browser-normalized `..` lands on a path that is simply not there - so the traversal theory contains only cases that exercise the rule. And drop or strengthen the `DoesNotContain("outside")` assertion, which cannot fail as written.
- **extractor/SwReview.AddIn/Review/PageFileServer.cs:193** (serving) - The containment check and the segment rule are each individually redundant: I traced every refusal case and each one is caught by both. Delete `candidate.StartsWith(_root + Path.DirectorySeparatorChar, ...)` and the suite stays green (the `..` segment rule catches it); delete the `..` segment rule and the suite stays green (containment catches it). That is deliberate belt-and-braces and I would keep both - but it means a refactor that removes either one silently loses half the defence, and the `+ Path.DirectorySeparatorChar` in particular is the classic prefix off-by-one (a sibling folder `web2` next to `web`) that nothing pins.
  - Fix: One test that reaches the containment check with the segment rule intact: construct a `PageFileServer` over `<root>/web` with a real `<root>/web2/evil.html` beside it, and assert `Serve(Origin + "/%2e%2e/web2/evil.html")` is a 404 that does not contain the file's bytes. That case survives `Uri` canonicalization (verified), is refused by the segment rule today, and would be the test that fails if someone drops the separator from line 193. Cheap, and it converts "defence in depth" from a comment into something the suite enforces.
- **specs/002-task-pane-assistant/contracts/pane-host-messages.md:79** (origin-csp) - The contract never states what `init.backend.origin` now is. The Review row (line 79) and the Model check row (line 166) both still read `{backend: {port, origin}, token, ...}` with no value, and line 97 says only "using the token and origin from `init`". The single most consequential change of this round - `origin` stopped being `http://127.0.0.1:<port>` and became `https://swreview.invalid/__backend` - is stated only in docs/pane-backend-proxy.md and in two C# comments. A page author working from the contract alone (which is what the contract is for, and what ReviewPageContractTests reads as data) would rebuild a loopback URL and land back on the failure in section 1.
  - Fix: Spell the value out in both `ready` rows - `backend: {port, origin: "https://swreview.invalid/__backend"}` - and add one sentence under the new "Every page resource is served by the host" section saying the port is carried for the pane's own display and diagnostics only and that no page builds a URL from it. Then have ReviewPageContractTests assert the literal `https://swreview.invalid/__backend` appears in the contract and in the host's init, so the two cannot drift again.
- **extractor/SwReview.AddIn.Tests/ReviewPageEventStreamTests.cs:264** (origin-csp) - The Review page's event-stream tests still boot the page from `backend = new { port = 51999, origin = "http://127.0.0.1:51999" }` - an `init` ReviewHost can no longer produce. PageMessageTests.cs and ModelCheckPageTests.cs were both converted in this round; this fixture was missed. It is the largest remaining page-test corpus running against a shape that does not exist, so it would keep passing if `backendUrl()` regressed to building loopback URLs, and it is a copy-paste source for the next page test.
  - Fix: Change it to `origin = "https://swreview.invalid/__backend"` to match the host, and consider asserting once (as PageMessageTests now does) that no page fixture's init origin contains `127.0.0.1`.
- **extractor/SwReview.AddIn/TaskPaneControl.cs:730** (origin-csp) - `ServePageResource` leaves the request unanswered on an unexpected exception, and the comment's justification ("WebView2 then tries the request itself and fails") is weaker now than it was under the folder mapping. With no mapping behind the handler, "tries it itself" means WebView2 issues a real request for `https://swreview.invalid/...`: on the pilot workstation - the one whose filter answers browser HTTP with its own content, which is the premise of this entire document - anything that resolver or filter returns would be loaded on the page's own origin, under `script-src 'self'`. `.invalid` is reserved and will not resolve on a correct resolver, so this is narrow, but it is the one path where the round's "every resource on this origin is served by the host" claim does not hold.
  - Fix: In the catch, make one best-effort attempt to assign a plain 404 (the same body/headers PageFileServer.NotFound produces) inside its own try/catch, so a request that reaches this handler is never handed back to the network stack. Add a test that forces `Serve`/`TryServe` to throw and asserts a response was assigned.
- **extractor/SwReview.AddIn/Review/PageFileServer.cs:29** (origin-csp) - The stated path rule is not the implemented one. The XML summary (line 29) and docs/pane-backend-proxy.md:240-243 both say "every segment is decoded first and then refused if it ... carries a separator". The code at line 141 does the reverse: it decodes the whole `AbsolutePath` with `Uri.UnescapeDataString` and then splits on '/', so an encoded `%2f` becomes a real separator rather than being refused as one. The outcome is still safe - the segments a decoded traversal produces are `..` or empty and are refused, and PageFileServerTests pins `/..%2f..%2fsecret.html` - but the rule a future maintainer will reason from is not the rule the code enforces, and this is the line that decides whether the pane can read the rest of the disk.
  - Fix: Either make the code match the doc (split on '/' first, then `Uri.UnescapeDataString` each segment and refuse any that still contains a separator), which is the stricter and more explicit reading, or reword both the summary and the doc to describe what happens: the path is decoded whole, split, and each resulting segment refused if empty, `.`, `..`, or carrying an invalid file-name character. The first is preferable - it makes `%2f` a refusal rather than a separator - and PageFileServerTests already has the cases to prove it either way.
- **extractor/SwReview.AddIn.Tests/PageFileServerTests.cs:283** (origin-csp) - The "every file the four shipped pages load" theory is a hand-kept list of 13 InlineData rows and it is already incomplete: `Terminal/TerminalPage/term.css`, `Terminal/TerminalPage/vendor/addon-fit.js`, `Model/ModelCheckPage/check.css` and `Remodel/RemodelPage/remodel.css` are all loaded by their pages and none is on it. docs/pane-backend-proxy.md:220 describes this test as covering "every file the four shipped pages load", which it does not. The risk the closed extension list created is exactly this: a page that adds an asset with an extension outside the list (`.woff`, `.gif`, `.webmanifest`, a `.map`) gets a silent 404 and a half-loaded tab, which under the old folder mapping could not happen.
  - Fix: Replace the InlineData list with an enumeration of the built `web/` folder: every file on disk must either serve 200 with the expected content type or appear on a short, named 404 allow-list (`vendor/LICENSES.md` today). That makes adding an unserveable asset a red test instead of a broken tab, and removes a list that has to be kept by hand.
- **docs/pane-backend-proxy.md:143** (origin-csp) - Two stale claims in the doc that is the handoff's source of truth. Line 143 says `BackendProxyTests.cs` has "13 cases"; it now has 18 (6 `[Fact]` plus 12 `[InlineData]`), because this round removed the `ResourceFilter` cases and added `TheOneResourceFilterCoversThePrefixAndThePagesThemselves`. Line 224 says the four CSPs are "byte-identical (the page contract tests assert that)"; the four tests that check it (ReviewPageInjectionTests.cs:179, ModelCheckPageInjectionTests.cs:66, RemodelPageContractTests.cs:204, TerminalPageContractTests.cs:148) all compare `Normalize(expected)` to `Normalize(actual)`, which collapses whitespace and re-joins directives - so they assert equivalence, not byte-identity. (I verified independently that all four page CSPs are in fact byte-for-byte equal to the contract's line 69 today; the doc's claim is true, the test cited as proving it does not prove it.) Line 146's "58 cases" for PageFileServerTests is correct (7 + 51).
  - Fix: Correct the count on line 143, and either soften line 224 to "the page contract tests assert it up to whitespace" or - better, and what the decision's "byte-identical" constraint actually asks for - drop `Normalize` from the four assertions so they compare the raw strings.
- **extractor/SwReview.AddIn.Tests/RemodelPageContractTests.cs:939** (origin-csp) - `ContractCsp()` and `Normalize()` are duplicated verbatim in four test files - RemodelPageContractTests.cs:914/939, ReviewPageInjectionTests.cs:232/246, ModelCheckPageInjectionTests.cs:136 and TerminalPageContractTests.cs:604-609 - each re-parsing the same CSP out of the same contract file with its own regex (and the regexes already differ: some allow `/?>`, some do not). This round is what made that duplication load-bearing, because the CSP was changed in all four pages at once and one page silently keeping the old policy is precisely the failure these tests exist to catch. Pre-existing, so it is a cleanup rather than a regression.
  - Fix: Move `ContractCsp()` and the comparison into one shared helper next to `ReviewPageFiles.ReadContract` (e.g. `PageContracts.AssertShipsContractCsp(html, which)`), and have all four call it. One regex, one normalization decision, one place to change when the policy changes again.
- **extractor/SwReview.AddIn/Review/PageFileServer.cs:104** (origin-csp) - Page responses carry `Content-Type` and `Cache-Control: no-store` but no `X-Content-Type-Options: nosniff`. The folder mapping used to be the thing deciding content types; now the pane does, from a closed extension list, and a served file whose bytes disagree with its extension can still be sniffed by the renderer. Low severity - the extension list is closed, everything under `web/` is shipped by the add-in, and `default-src 'none'` constrains what a sniffed type could become - but it is one header, it is free, and it is the kind of thing the mapping used to make moot.
  - Fix: Append `\r\nX-Content-Type-Options: nosniff` to the 200 and the 404 header strings, and add it to the contract's file-server rules list (pane-host-messages.md, the paragraph that already names `Cache-Control: no-store`) so the two stay together. One assertion in PageFileServerTests pins it.
- **extractor/SwReview.AddIn.Tests/PageFileServerTests.cs:297** (origin-csp) - `EveryFileTheShippedPagesLoadIsServed` does not cover every file the shipped pages load, though its own docstring (line 277-280) and docs/pane-backend-proxy.md:220 both say it does. The four index.html files reference 17 resources; the theory has 13. Missing: `/Terminal/TerminalPage/term.css` (Terminal index.html:8), `/Terminal/TerminalPage/vendor/addon-fit.js` (:67), `/Model/ModelCheckPage/check.css` (:7), `/Remodel/RemodelPage/remodel.css` (:7). All four are .css/.js in directories the theory already reaches, so they pass today — but the folder mapping that used to answer them is gone and the test that is supposed to be the safety net for 'a single 404 here is a blank tab' has holes in exactly the two pages (Terminal, Remodel) that get the least end-to-end coverage.
  - Fix: Add the four `[InlineData]` rows. Cheap, and it makes the docstring and the doc true. Optionally derive the list by regexing `src=`/`href=` out of the four index.html files instead of hand-listing, which would make the test self-maintaining — but that is the more-engineered option and hand-listing 17 rows is defensible.
- **extractor/SwReview.AddIn.Tests/BackendProxyPageTests.cs:120** (origin-csp) - The confound that already bit this test once is removed by construction but never asserted. `TheFolderMappedVirtualHostRaisesNoWebResourceRequestedForAFetchTheCspAllows` exists because the original version fetched from the shipped Review page, whose CSP then lacked `'self'`, so the renderer refused the call before a request existed and the zero-event result proved the policy rather than the platform (docs/pane-backend-proxy.md:191-195). The fix was to serve a scratch page built with `WriteScratchPage("connect-src 'self'")` — but the test asserts only `Failed to fetch` and `seen.Count == 0`, both of which a CSP refusal produces just as happily. Nothing in the test body fails if `ScratchPageHtml` (line 395) is later edited and the confound returns; it would go on passing while proving nothing. This is the one test in the file whose conclusion the suite cannot re-derive.
  - Fix: Install the `securitypolicyviolation` listener before the fetch — the pattern is already written 180 lines down in `ThePagesCspRefusesLoopbackBeforeAnyNetworkCallIsMade` (line 285-300) — and assert `window.__violation === null` alongside the existing two assertions. Then the test states its own precondition instead of inheriting it from a helper.
- **extractor/SwReview.AddIn.Tests/BackendProxyPageTests.cs:309** (origin-csp) - Tautological assertion. `Assert.Empty(fake.Requests)` in `ThePagesCspRefusesLoopbackBeforeAnyNetworkCallIsMade` cannot fail for the reason its placement implies. `http://127.0.0.1:1/health` is off the page origin, so it falls outside `TaskPaneControl.PageResourceFilter` (`https://swreview.invalid/*`) and can never reach `ProxySend` — with `connect-src 'self'`, with `connect-src *`, or with no CSP at all. It reads as corroboration of the CSP claim and is independent of it. The real claim is carried entirely by the two `Assert.Contains` on the violation event above it.
  - Fix: Either drop the line, or keep it and say in the comment what it actually pins — that a loopback URL is outside the filter as well as outside the policy, which is a second independent fact and worth one sentence rather than an assertion that looks like the conclusion.
- **specs/002-task-pane-assistant/contracts/pane-host-messages.md:79** (origin-csp) - The contract never says what `init.backend.origin` holds — the single payload field this decision changed. Both `ready` rows (line 79 Review, line 166 Model check) and specs/004-resilient-remodeler/contracts/pane-remodel-messages.md:29 still print `{backend: {port, origin}, ...}` with no definition. The new 'Every page resource is served by the host' section documents the serving side thoroughly but stops short of the field the pages read. A reader coming to the contract fresh gets `origin` from its history — the loopback origin it used to be — and nothing catches that: ModelCheckHostTests.cs:62 and PageMessageTests.cs:57 assert a hardcoded literal, not a value read out of the contract, which is how RemodelHost.cs:386 came to disagree with the other two hosts without any test noticing.
  - Fix: One sentence in the new section: `init.backend.origin` is `https://swreview.invalid/__backend` — the page's own origin under the proxy prefix — and `port` is carried for the pane's display and for diagnostics only, with no page building a URL from it. Then decide whether the remodel contract row says the same thing (see must_fix #1).
- **docs/pane-backend-proxy.md:143** (origin-csp) - Two accuracy slips in a document that is otherwise exact. (a) Line 143 says `BackendProxyTests.cs` is '13 cases'; it is 18 — 6 `[Fact]` plus 12 `[InlineData]` across 3 `[Theory]`. The neighbouring counts are both exact (BackendProxyHandlerTests 9, PageFileServerTests 58, and I counted all three), so this is the one stale number and it is in the table describing the file this change edited. (b) Line 224 says the CSP is 'byte-identical (the page contract tests assert that)'. The tests assert normalized equality: ReviewPageInjectionTests.cs:180, ModelCheckPageInjectionTests.cs:78 and RemodelPageContractTests.cs:203 all compare through a `Normalize()` that trims each directive and collapses internal whitespace. Byte identity does hold today — I compared the raw bytes of the contract's tag and all four index.html tags and they match exactly — but the suite would not catch a page that drifted to `connect-src  'self'`.
  - Fix: Correct the count to 18, and either say 'identical directive-for-directive (the page contract tests normalize whitespace before comparing)' or tighten the three tests to `Assert.Equal(expected, actual)` on the raw capture. I'd take the doc edit: normalization is the right call for a policy string spread across markdown and HTML, and the doc is what is wrong.
- **extractor/SwReview.AddIn.Tests/ReviewPageInjectionTests.cs:233** (origin-csp) - DRY, pre-existing, reporting not recommending. `ContractCsp()` and `Normalize()` are copied verbatim into three files — ReviewPageInjectionTests.cs:233-244, ModelCheckPageInjectionTests.cs:132-148, RemodelPageContractTests.cs:915-944 — and TerminalPageContractTests.cs:604-648 carries a fourth spelling of the same idea (`CspMeta`/`CspContent`/`CspTag`), which is also why the Terminal page is checked against the Review page rather than against the contract. One rule, four implementations. The chain does close (Review/Model/Remodel each compare to the contract, Terminal compares to Review), so the CSP is correctly pinned everywhere — this is maintenance burden, not a hole. Only ReviewPageInjectionTests.cs is touched by this change, so under the surgical-changes rule I am flagging it rather than folding it into this task.
  - Fix: Separate change: one `PageCspContract` helper next to ReviewPageFiles, used by all four contract test classes, with Terminal comparing to the contract like the others instead of to the Review page. Worth doing when something next touches CSP; not worth widening this diff for.
- **extractor/SwReview.AddIn/Review/PageFileServer.cs:64** (origin-csp) - `.woff2` is provisioned for a policy that would refuse it. The owner's decision names woff2 in the closed content-type list, so it is there correctly — but no page ships or references a font (`@font-face` and `url(` return nothing across app.css, check.css, remodel.css, term.css and vendor/xterm.css), and the contract CSP is `default-src 'none'` with no `font-src`, so a woff2 served by this list is blocked by the page's own policy before it can be used. Same shape, less sharply, for `.json`, `.svg`, `.png` and `.ico`: all named by the decision, none currently referenced — though `img-src 'self' data:` at least permits three of them, which is what makes woff2 the odd one out.
  - Fix: Keep the entry (the decision is binding and a font is a plausible next asset), and add half a sentence to the list's doc comment or the contract's file-server paragraph saying woff2 is stocked ahead of a `font-src` the CSP does not yet grant — so the first person to ship a font finds the CSP change waiting for them instead of debugging a silent block. No code change.
- **extractor/SwReview.AddIn.Tests/BackendProxyPageTests.cs:392** (origin-csp) - Stale instruction in a failure message that is meant to be read years from now. `AssertNoEventsWereRaised` tells whoever trips it to 'finish the proxy as that document's section 2 describes and delete these two tests'. The proxy is finished — option B shipped, and section 4 is what a reader would need. The message is the only guidance a future engineer gets at the moment a WebView2 update starts raising the event for folder-mapped hosts, which is precisely when it matters that the message is current. ('These two tests' is correct: two tests call this helper.)
  - Fix: Reword to: the mapping no longer swallows the event, so `SetVirtualHostNameToFolderMapping` is a choice again rather than a trap — delete these two tests and read section 4 before reintroducing it, since the file server also pays for the sandbox the mapping provided.

## Feature 006 spec review should-fix (2026-09-17)

- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\spec.md** (Differences from the Macro, row (v) (line 265)) - Row (v) lists the data-card check among the checks this feature makes document-scoped and explains it by the macro's sub-assembly sequence dropping checks - but the macro's sub-assembly sequence does run the data-card check. The row states a difference that does not exist and thereby weakens the table SC-015 is judged against.
  - Fix: Remove "and the data-card check" from row (v). The real data-card differences are already rows (l) and (m) plus FR-005's library-skip rule.
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\spec.md** (FR-011 (line 193); Differences from the Macro table (lines 242-267)) - The transparency check is the one assembly check the macro deliberately does NOT skip suppressed components for, so under FR-004 a suppressed component carrying a transparency override becomes an unresolved row where the macro produced an error finding. FR-011 says nothing about suppression and no Differences row covers it (row (c) covers only the hidden check, in the opposite direction).
  - Fix: State the suppression rule in FR-011 explicitly and name check 6 in whichever Differences row records the suppressed-component decision taken for finding 4 above.
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\spec.md** (Edge case (line 130), FR-005 (line 184)) - "The longest matching prefix wins" is stated globally over "profile prefixes", but the profile carries four independent lists (skip, one-mate, two-mate, sketch-exempt). A single path routinely matches prefixes in two different lists - the macro's own constants do exactly this, with an electrical library folder on the skip list and an electrical wires sub-folder on the two-mate list - and a global longest-wins rule silently discards one of the two classifications.
  - Fix: State that precedence is resolved independently within each list, that skip membership and mate-requirement membership are orthogonal, and that the coverage reason names the winning prefix per list. Repeat the rule in FR-005 and in the profile schema requirement in FR-001.
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\spec.md** (FR-009 (191), FR-013 (195), FR-016 (198), FR-017 (199), FR-018 (200), FR-020 (202); Differences table (242-267)) - Six checks specify a *skipped* coverage row for the empty case (no child components, no sketches, no cut-list items, no display dimensions, no annotations, no notes) where the macro reported nothing and thereby implied a pass. The convention is defensible and stated once in US1, but it is a per-check reporting difference from the macro with no lettered row, so under SC-015 every such row is an unexplained disagreement.
  - Fix: Add one lettered Differences row covering the convention: "A check with no subjects is skipped coverage naming the reason | The macro printed nothing, which reads as a pass | A release gate must show what it did not look at | §1.10, §5.1", and cite it from the affected FRs in Appendix A.
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\spec.md** (FR-021 (line 203), FR-001 (line 177)) - Neither FR says whether the part-number pattern is matched case-insensitively, or whether the file name includes its extension. The macro upper-cases the title before matching and its pattern includes the extension, so a lower-case file name matches. FR-005 states case-insensitivity for path prefixes only, which invites the opposite reading for the pattern.
  - Fix: State in FR-021 (and in the profile schema requirement in FR-001) that the pattern is matched case-insensitively against the file name including its extension.
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\spec.md** (FR-016 (line 198); Differences table (lines 242-267)) - FR-016 identifies subjects structurally ("every cut-list item of a part document"), where the macro identifies them by a case-sensitive name prefix, so a renamed cut-list item escapes the macro and is caught here. The profile carries no item-name setting, so the macro's behaviour cannot be reproduced even deliberately. The difference is unlisted and will show up as an unexplained disagreement under SC-015.
  - Fix: Add a lettered Differences row: "Cut-list items are identified structurally from the package's cut-list records | The macro matched feature names beginning with the default cut-list item name, so a renamed item was never checked | A rename is not a waiver | Check 12, C34".
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\spec.md** (SC-015 (line 306)) - "Agree check by check" is undefined, and literal agreement is impossible by construction: Differences rows (a) and (n) guarantee different finding counts and different per-document shapes, and rows (b), (m), (o)-(u) guarantee different finding sets. The criterion also requires running the 2017 macro on the owner's real documents, which force-rebuilds every drawing - a mutation of the owner's files that the spec nowhere acknowledges while FR-038 forbids this feature from rebuilding anything.
  - Fix: Define the comparison unit: "for every (check, document) pair the two agree on pass/fail; counts are compared per the counting rule in row (n), not line for line". Add a sentence that the comparison run executes the legacy macro on copies of the owner's documents, or records explicitly that the macro's force rebuild is the legacy tool's behaviour, accepted once for this measurement and outside this feature's read-only guarantee.
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\spec.md** (User Story 4 Independent Test (line 101) against acceptance scenario 8 (line 112) and SC-008 (line 299)) - The Independent Test describes a three-id waiver fixture; the acceptance scenario and SC-008 describe a four-id fixture including a warning-level id, which is the case that exercises FR-036's "warning-level and unknown ids MUST be reported as invalid". The story's own test is weaker than its acceptance criterion.
  - Fix: Make the Independent Test use the same four-id fixture and the same four outcomes (accepted, unused, invalid, invalid), nothing written, exit non-zero.
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\spec.md** (FR-019 (201) and FR-001 (177)) - FR-019 hard-codes the revision cell location (last data row, first column), which the normative reverse-engineering marks as a company-specific drawing-template convention, and FR-001's profile schema does not carry it. That is a company-specific value living in a requirement (and therefore in code, tests and contracts) rather than in the profile, contrary to the feature's own rule that no company value exists outside the profile.
  - Fix: Add the revision cell location to FR-001's profile schema (a row selector and a column index, defaulting in the example profile to the last data row and column zero) and change FR-019 to read the cell the profile names. Record the macro's values in research.md with the other constants.
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\spec.md** (FR-001 (line 177) and Edge Cases (line 165)) - The vault root is mandated as a profile key but no requirement consumes it. Every path comparison in the spec is done against the library prefixes, which are absolute, and the one edge case that mentions the vault root says the match is textual and the root need not exist. It is configuration with no specified function, which the constitution's "Engineered Enough" principle forbids.
  - Fix: Either give the vault root a stated function (for example: library prefixes may be written relative to it and are resolved against it before matching, which is what would make the profile portable between machines) in FR-005, or remove it from FR-001 and from the Key Entities "Standards Profile" description at line 271.
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\spec.md** (Key Entities "Standards Check Run" (line 272)) - The requirement that the run record the profile by path and content hash and never by value appears only in the Key Entities list, which is descriptive. It is the control that keeps company values out of the run folder, the check record and anything a user might attach to a bug report, and no MUST states it.
  - Fix: Add the clause to FR-030: the check record MUST identify the profile by path and content hash and MUST NOT copy any profile value into the record, the session, the report or any message to the page.
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\spec.md** (Input (line 9), FR-001 (177), FR-011 (193), SC-014 (305), Assumptions (314-315)) - Five binding statements, including two MUSTs and a success criterion, discharge their obligation into `research.md`, which does not exist in the feature directory. The spec is not self-contained and nothing in it can be verified today: the constants record, the probe list and the polarity answer are all forward references to a plan-phase artifact.
  - Fix: Keep the forward references but state them as obligations on the plan phase rather than as satisfied facts, and make the Assumptions section list the probes by name inline (exploded-state interface, transparency polarity, visibility, revision-table read, note walk, drawing-view/annotation walk, non-active sheet read) so the spec stands alone. Write research.md as part of this package, as the design brief's Structure section requires.
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\spec.md** (SC-005 (line 296)) - SC-005 is not measurable as written. Running the same check suite against two profiles whose library folders, vault roots and part-number patterns differ cannot produce the same expected results over the same fixture packages - the classification of every component changes with the prefixes. The criterion needs matched profile-and-package pairs, which it does not say.
  - Fix: Restate as two matched pairs: "the same suite, run over two fixture packages whose paths, file names and property names are written for two different fictional profiles, produces the same sixteen coverage rows and the same findings by check id in both, proving no value is compiled in".
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\spec.md** (SC-015 (line 306)) - SC-015 requires the macro's report and the tab's result to "agree check by check", but the spec's own Differences table changes the granularity of the output for at least six checks - dedup, counting, per-subject naming, feature naming, dimension formatting. There is no stated comparison procedure, so "agree" is undefined and any disagreement can be argued either way. The gate that decides whether the feature is accepted is the least measurable criterion in the spec.
  - Fix: Define the comparison unit explicitly: for each check and each document, compare the set of failing subjects the macro names against the set the tab names, after applying rows a and n (dedup by document, count once per check per document). State what "agree" means for the checks whose macro output carries no subject identity (rows o and u) - that the tab is a superset.
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\spec.md** (US1 "Why this priority" (line 19) and US2 "Why this priority" (line 48)) - US1's cost claim understates its extraction work and US2's priority rationale rests on that understatement. US1 needs the cut-list evidence, which is a whole new dump phase and a new IR model, not a "small additive read" - yet US2's justification for being second is that it alone brings "a new extraction phase".
  - Fix: Change US1's clause to "seven small additive reads plus one new cut-list dump phase and IR model", and change US2's to "the more expensive half: a second new extraction phase over a document kind the extractor has never read, five new IR models and four workstation probes".
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\spec.md** (FR-006 (line 188)) - FR-006 invents a skipped-coverage state for an assembly that simply is not exploded, and the distinction it draws cannot be made from the package as FR-022 encodes it. `IsExploded` is a boolean: an assembly with no exploded view reads not-exploded, which is a pass. Because FR-022 makes the field optional and omitted-when-null, "records no exploded state" is byte-identical to "the dump could not read it", so the same package must be reported both skipped and unresolved.
  - Fix: Make an assembly that is not exploded checked coverage, and reserve unresolved for a missing field accompanied by a gap - stating that rule explicitly, since the absent field alone cannot distinguish the two. Drop the "holds no exploded view" skip, or define the skip by the presence of a recorded exploded-view count of zero.
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\spec.md** (FR-005 (line 184) and Edge Cases (line 131)) - The rule for a prefix without a trailing separator is stated as "matches exactly one file", which is exact-equality semantics, not the prefix semantics the macro uses. A macro prefix with no separator still prefix-matches, so `...\SCREW` would match `...\SCREWDRIVER.SLDPRT`. The spec changes the matching rule without listing it as a difference.
  - Fix: Either keep pure prefix semantics and say "a prefix without a trailing separator is still a prefix match, so it matches that file and any path that starts with it", or keep exact-file semantics and add a lettered Differences row explaining the change and why (it removes an accidental match class).
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\spec.md** (FR-024 (209) and FR-037 (231)) - FR-024 defines only the `Standards` profile as gaining the new phases, and says nothing about the existing `Full` profile. FR-037 then makes the command line refuse any package "whose extractor profile did not run the phases the standards checks read", which as written refuses a `Full` package - the richest one the extractor produces - and makes the refusal rule undecidable for a reader.
  - Fix: State in FR-024 whether `Full` also gains the cut-list and drawing phases, and rewrite FR-037's refusal to test the phases the package records rather than the profile name: refuse when the phases the requested checks read are absent, naming the profile and the missing phases.
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\spec.md** (Requirements, "Severity, verdict and report" block (FR-027, line 215)) - The spec never states that standards findings carry the feature 001 evidence fields and that the feature 001 finding contract does not change. FR-027 maps only the verdict status. Feature 003 needed an explicit requirement for this because the constitution enumerates the evidence every finding must carry; without it the drawing and cut-list findings have no stated obligation to name configuration, document version or revision.
  - Fix: Add a requirement in the severity block: every standards finding MUST name the rule id as its check, its documents and component instances, its subjects with their persistent references, the observed condition, the check statement as its requirement and the recommended action, and MUST carry the feature 001 evidence fields without changing the feature 001 finding contract.
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\spec.md** (US3 Acceptance Scenario 6 (line 82) and FR-032 (line 223)) - Show is delegated to the shared resolver "byte-for-byte the rows the Model check page already has", but the standards findings include subject kinds the shared resolver has never handled - drawing sheets, drawing views, display dimensions, annotations, revision-table rows, cut-list items. Nothing in the Assumptions, the probes or Out of Scope acknowledges that selecting a drawing entity is new work or that it may be unresolved.
  - Fix: Add an assumption or a requirement stating which subject kinds the shared resolver can select today and that a subject kind it cannot reach MUST reply `ok: false` naming the kind rather than being omitted, and name the drawing-entity selection surface as a workstation probe if it is one.
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\spec.md** (FR-013 (line 195), Differences from the Macro table) - FR-013 makes two deviations from the macro that are not in the Differences table, which the design brief requires to carry one row per deliberate deviation. It names both the hole-wizard feature and the sketch (the macro names only the parent feature), and it evaluates "every sketch of a part document" where the macro walks only top-level features and hole-wizard sub-features.
  - Fix: Add two lettered rows: one for naming the sketch as well as the hole-wizard parent (why: the engineer needs to find the sketch, macro source Check 9), and one for walking every sketch the package records rather than top-level and hole-wizard only (why: L12 says sub-feature content can hide defects; source Check 9, L12).
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\spec.md** (FR-005 (line 184), Differences from the Macro table) - FR-005 generalizes the library skip list to any document, but in the macro the skip list is applied to parts only - an assembly under a skipped library folder is still walked and still checked. This is a behavioural change against the normative source and is not listed as a difference.
  - Fix: Either state in FR-005 that the skip list applies to part documents only, matching the macro, or add a lettered Differences row recording that it now applies to any document kind and why.
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\spec.md** (FR-028 (line 216)) - FR-028 claims the verdict is computed from the counts alone and then, in the same sentence, computes it from the counts plus the presence of unresolved checks. Unresolved rows are coverage, not a count the requirement defines. The self-contradiction makes the third verdict state's input undefined.
  - Fix: Replace "from the counts alone" with "from the error count and the unresolved coverage alone, and from nothing else", and define the unresolved count alongside the error and warning counts so all three inputs are named.
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\spec.md** (US2 Acceptance Scenario 5 (line 58) and Edge Cases (line 155)) - Both places say "the drawing revision" is read as the initial-revision value when the revision table holds only its header row. What is read from the table is the *table's* revision; the drawing's own revision is a separate custom property that FR-019 compares against it. As written the two sentences say the drawing's revision property is overwritten, which would make every mismatch disappear.
  - Fix: Change both to "the revision read from that table is the profile's initial-revision value, and is compared against the drawing's revision property as normal".
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\spec.md** (FR-037 (line 231)) - The command-line signature omits JSON output, although the sibling command it must match offers it and feature 003's command-line requirement mandates it. "In the shape the other check commands print" does not say whether the machine-readable shape is included, so a reader cannot tell whether `--json` is required.
  - Fix: Write the signature as `swreview check standards --package <dir> --out <dir> [--profile <yaml>] [--json]` and say it prints findings and coverage in both the human and JSON shapes `check rms` prints.
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\spec.md** (Edge Cases (line 166)) - An edge case is answered by deferring to unspecified existing behaviour rather than by stating the outcome. The constitution requires edge cases to be named in the spec rather than discovered in production, and this one names the situation without resolving it.
  - Fix: State the outcome: whether the second run reuses, suffixes or refuses the colliding folder id, and what the page reads back when two records share an id prefix.
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\spec.md** (Appendix A (lines 336, 357)) - Two rows of the requirement-to-source map are wrong. The FR-001 row cites a constant range that includes two constants the source marks as not company-specific and omits one it does; the FR-022 to FR-024 row omits the drawing checks and the drawing traversal section that are the entire source for FR-023's new dump phase.
  - Fix: Change the FR-001 row to cite the company-specific constants only (C1-C26, C28-C33, C41) and add C33 to FR-001's profile schema, or drop it from the citation. Add "§1.5 Drawing to parent traversal; Checks 13, 14, 15, 16" to the FR-022 to FR-024 row, or split FR-023 into its own row.
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\spec.md** (FR-021 (L203), FR-001 (L177), overview paragraph (L11)) - The part-number gate is specified three different ways and never precisely enough to test: the overview calls it a "title pattern", FR-021 matches "the file name", and neither says whether the file name includes the extension or whether the match is anchored to the whole name. Since the macro's pattern ends in `.SLD???`, matching a name without an extension never fires and the check silently switches off - the exact failure mode Differences row l exists to prevent.
  - Fix: State in FR-021 that the pattern is matched, case-insensitively and as a whole-string match, against the file name including its extension, taken from the document's recorded path; fix the overview to say "part-number file-name pattern"; add an edge case for an absent or empty pattern in the profile (the check is skipped coverage naming the empty setting, as the other empty-list settings are).
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\spec.md** (FR-001 (L177); Key Entities "Standards Profile" (L271); Edge Cases (L165)) - The vault root is required in the profile but no functional requirement consumes it. FR-005 anchors prefix matching at the start of the path, which implies the library prefixes are already absolute, so the vault root is dead configuration with undefined semantics - a test could not tell a correct implementation from one that ignores the key.
  - Fix: Either delete the vault root from the profile, or define it: prefixes may be written relative to the vault root and are resolved against it before matching, with an edge case for a relative prefix and an absolute one in the same list.
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\spec.md** (FR-019 (L201); FR-001 (L177)) - FR-019 hard-codes the revision cell location (last data row, first column) although the normative source flags that convention as company-specific and FR-001's profile does not carry it. A different drawing template moves the cell and the check reads the wrong value while reporting a confident warning.
  - Fix: Add the revision-cell location (row-from-end and column index, defaulting to last row / first column) to the profile schema in FR-001 and cite it from FR-019, or state in FR-019 why the convention is treated as universal despite the source marking it company-specific.
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\spec.md** (SC-003 (L294)) - SC-003 asserts a verdict without constraining the condition the verdict depends on. `ready_coverage_incomplete` requires zero errors (FR-028), but the unknown-evidence fixture is described only by its unreadable signals; nothing says it is otherwise compliant, so the measured verdict could legitimately be `not_ready` and the criterion would fail for the wrong reason.
  - Fix: Add "and no other check fails" (zero error-severity findings) to the fixture description in SC-003, and assert the error count is 0 alongside the verdict.
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\spec.md** (SC-007 (L298); US2 Independent Test (L50)) - "The compliant drawing fixture yields five checked-coverage rows" is only attainable under conditions the criterion does not state: the drawing's file name must match the profile's part-number pattern (otherwise the data-card check is skipped, not checked) and the drawing's referenced model must be in the fixture with a matching revision property (otherwise `standards.drawing.revision_matches` is unresolved for the model comparison).
  - Fix: Specify the compliant drawing fixture in SC-007: a part-number-conforming file name, a populated revision table whose last data row agrees with the drawing revision property, and a referenced model present in the package with the same revision - then five checked rows is a determined outcome.
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\spec.md** (SC-015 (L306)) - The macro-parity criterion names neither a sample nor a pass threshold ("documents of the owner's choosing"), so it cannot be said to have been met or missed. It also requires running the 2017 macro, whose only mutation is a force rebuild of every drawing it checks - a side effect on the owner's real documents that the criterion should call out since this feature's whole read-only argument rests on not doing it.
  - Fix: Name a minimum sample (at least one part, one assembly and one multi-sheet drawing, on copies), state the pass threshold (every disagreement maps to a lettered Differences row), and note that the macro force-rebuilds drawings so the comparison is run on copies and the rebuild-error counts are expected to differ per row g.
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\spec.md** (US4 Acceptance Scenario 8 (L112); SC-008 (L299); FR-036 (L230)) - The waiver-import outcome is self-contradictory as stated: an id is "reported as accepted" in the same sentence that says nothing is written. Whether "accepted" means "applied" or "would have been applied" decides what the test asserts about the exception store after the run.
  - Fix: Say it explicitly: when any id is invalid the import is a dry run - each id is reported with the outcome it would have had, the exception store is unchanged, and the exit code is non-zero; when every id is valid the accepted ids are written. Align the Independent Test's fixture with Scenario 8's four ids.
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\spec.md** (FR-032 (L223); US3 Acceptance Scenario 10 (L86)) - The spec requires the tab to refuse with the profile's schema error before any run folder is written, but never says which side validates the profile. If the host must produce a schema error before the dump, the profile schema exists in both C# and Python, which the DRY rule forbids; if the backend validates, a run folder already exists by the time the error is known, contradicting Scenario 10.
  - Fix: State the division: the host checks only that a profile path is configured and the file is readable and refuses before the dump on that; the backend validates the schema and returns the named schema error, and the page renders it; a run folder created before that refusal is reported as an empty run rather than a result. Or require a single validator invoked over a small host-to-backend preflight call, and say so.
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\spec.md** (FR-022 (L207) and FR-024 (L209) against US1 "Why this priority" (L19)) - US1's cost claim understates the work it depends on. FR-022 names eight new signals, not seven, and the eighth (cut-list items) is a new dump phase with a new IR model rather than a "small additive read" - which is what makes US1 more expensive than the story implies and matters for story sequencing.
  - Fix: Rewrite the clause as "seven small additive reads plus one new cut-list dump phase and its IR model", and check the same claim against the brief's staging (design-brief.md:42-45) so US1's scope is stated once.
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\spec.md** (FR-023 (L208); SC-004 (L295)) - The extractor writes a standing gap on every dump stating that native drawing extraction does not exist. FR-023 makes that statement false, and the spec neither requires it to be removed or made conditional nor accounts for its effect on package-comparison tests, while SC-004 demands byte-identical goldens.
  - Fix: Add to FR-023 that the standing native-drawing gap is emitted only when the drawing phase did not run (and reworded to name the profile that skipped it), and add a clause to SC-004 asserting that a non-Standards-profile dump's gap set is unchanged.
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\spec.md** (FR-022 (L207) and FR-023 (L208); FR-024 (L209); FR-037 (L231)) - The additivity rules are stated only for the eight small signals; the drawing IR (sheets, views, dimensions, annotations, revision tables, notes, populated `drawing_document_ids`, drawing manifest entries) is introduced by FR-023 with no statement that it is optional, defaulted, omitted-when-null and inside the same single 1.4.0 bump. Separately, nothing says whether the `Full` profile also gains the cut-list and drawing phases, which decides whether FR-037's refusal rejects a full-profile package.
  - Fix: Extend FR-022's additivity sentence to cover every model FR-023 introduces, and state in FR-024 whether `Full` runs the cut-list and drawing phases; then define FR-037's refusal in terms of the recorded phase rows rather than the profile name, so any package that actually ran the phases is accepted.
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\spec.md** (Edge Cases, "Traversal and document state" (L126-127)) - The spec covers a document reached only through unresolved instances but not the mixed case - one resolved instance and one lightweight or suppressed instance of the same document - which decides whether the document is graded at all. Feature 003 states the rule explicitly; this spec's silence leaves FR-004 reading as though any unresolved instance poisons the document.
  - Fix: Add the edge case and a clause in FR-004: a document reached through at least one resolved instance is graded normally, and the unresolved instances are named in the finding's coverage limits rather than making the document unresolved.
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\spec.md** (US3 Acceptance Scenario 5 (L81); FR-029 (L217)) - The bucket vocabulary the tab renders omits the bucket a failing check occupies. Four buckets are enumerated (checked, skipped, unresolved, out of scope) but the existing coverage model has five, and the spec never says which row a check with findings shows in the sixteen-check list.
  - Fix: Add `failed` to the enumerated buckets in US3's story text and in FR-029, and state that a failing check shows as failed with its finding count and links to its findings.
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\spec.md** (Differences from the Macro, row c (L246)) - The rationale column makes a factual claim about the macro that the normative reverse-engineering contradicts: the transparency check also does not skip suppressed components, so "every other assembly check" is wrong. The table's stated purpose is macro traceability, so an error here undermines the only place deviations are justified.
  - Fix: Change to "while the other assembly checks that enumerate components, except the transparency check, skip them", or drop the comparative clause.
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\spec.md** (Differences from the Macro (L242-267)) - Three deliberate behavioural deviations from the macro are made in the requirements but have no row in the table the spec says lists them all, so they are not traceable and will read as defects during SC-015's macro comparison: the longest-prefix precedence rule (the macro used list order), the profile-named material configuration (the macro read the configuration literally named `Default`, and a missing one is now unresolved rather than a finding), and the sketch walk depth (the macro walked top-level features and hole-wizard sub-features only).
  - Fix: Add three rows (y, z, aa) with the macro behaviour, the new behaviour, the reason and the source sections, and cite them from FR-005, FR-013 and FR-015 so SC-015's disagreement-explains-to-a-row test can close.
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\spec.md** (FR-020 (L202); US2 Acceptance Scenario 6 (L59)) - The export-control check is specified as one finding per drawing that names "the sheet and the note" (singular) without saying what happens when the phrase appears in several notes or on several sheets - which sheet and note the single finding names is undetermined, so the scenario's exact outcome is not testable on a multi-sheet fixture, and SC-007 explicitly seeds a multi-sheet drawing.
  - Fix: Say that the single finding names every sheet and note that carried the phrase, as subjects, and adjust US2 Scenario 6 and SC-007 to assert all of them.
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\spec.md** (Requirements section (L171-236); Success Criteria (L290-306)) - Several requirements have no measurable outcome anywhere: the single no-language-model evaluation entry point is only a subordinate clause in the CLI requirement rather than its own FR (feature 003 makes it FR-024), and FR-031 (tabs and pinned lists), FR-032 (message set), FR-033 (routes) and FR-034 (escaping) have no SC at all. The spec also never says whether the feature 001 review checklist gains a standards item or is deliberately untouched, though FR-025 requires generalising 003's summary check id.
  - Fix: Promote the single-entry-point rule to its own FR; add success criteria for the pinned tab/tool lists (the existing tests pass unedited), for the page-host message set (parameterised over both hosts, per FR-032) and for the escaping rule; and state explicitly whether a standards summary check id and a review-checklist item exist or are out of scope.
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\spec.md** (Differences from the Macro, row h (L251)) - Row h's Source column cites macro sections for a claim that is not about the macro at all - the limitation is in this repository's mate extraction. The table's column is defined as citing the reverse-engineering, so the citation is unverifiable at the place it points.
  - Fix: Cite both: "Check 3, Check 5 (macro); coverage inventory A3 / `MateDumper` (the package's limit)", and say the same in Assumptions (L313) so the gap's owner is unambiguous.
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\spec.md** (SC-013 (L304)) - The completeness half of SC-013 names no ground truth and no counting method, so "records, or records a gap naming, every sheet, view, ... and note" cannot be judged pass or fail: nothing says what the expected inventory is compared against.
  - Fix: State the comparison: the owner writes down the expected counts per sheet from the SOLIDWORKS tree (sheets, views, display dimensions, annotations, revision-table rows, notes) before the run, and the criterion is that every recorded count matches or is accompanied by a gap naming the shortfall.
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\spec.md** (SC-001 (L292), SC-002 (L293), SC-007 (L298)) - Criteria listed as measurable offline require drawing evidence that only User Story 2 delivers, so none of them can be measured when User Story 1 ships alone. The spec does not map success criteria to the stories that make them measurable, which breaks the template's independent-delivery rule for the P1 increment.
  - Fix: Tag each success criterion with the story that first makes it measurable (SC-001/002 gain their drawing half at US2), or split SC-001 and SC-002 into a parts-and-assemblies criterion measurable at US1 and a whole-package criterion measurable at US2.
- **specs/006-standards-check/spec.md** (FR-032 (line 223), FR-034 (line 225)) - Both requirements describe a transport that no longer exists in this repository. The pages no longer reach the loopback backend and are no longer served from a folder-mapped virtual host: the mapping was removed and every page file and every backend call is served by the host through one `WebResourceRequested` handler at the page's own origin. A Standards page built to "call the backend itself" with a loopback origin, under a `connect-src http://127.0.0.1:*` policy, is the exact failure the handoff round fixed - every call answered by the web filter.
  - Fix: Say the page calls the backend at its own origin under the `/__backend` prefix through the host's proxy, with the token from `init`, and that its files are served by the host's page file server under `connect-src 'self'` - and note that the `/events` route is not proxyable, which this tab does not need.
- **specs/006-standards-check/spec.md** (FR-032 (line 223)) - The `standards.start` message has no payload specified, while the row it mirrors carries one, and the verb is renamed from the sibling's (`check.start`/`check.extracted`) without saying why. A host implementing this cannot tell whether the message carries a scope, a profile path, or nothing.
  - Fix: Give `standards.start` an explicit payload (`{}` if it needs none) and state whether the family keeps the sibling's verb with a family field or its own verb, and why.
- **specs/006-standards-check/spec.md** (FR-030 (line 221), Edge Cases (line 166)) - The run-folder name is left to "the run-folder helper's existing naming behaviour", but that name is the check id an engineer sees, types and reads back, and both families will now produce identically shaped ids under one run root. Nothing in the folder name tells the engineer, or the pane, which family a folder belongs to.
  - Fix: Decide and state the suffix (recommended `-standards`, so the run root is readable by eye and the family filter of the other tab is trivial), and say that the family field remains authoritative for the result shape.
- **specs/006-standards-check/spec.md** (FR-033 (line 224), FR-001 (line 177)) - The route set never says how the backend learns which profile to grade against. The command line has `--profile`; the route has nothing, and FR-001 makes the path a setting without naming which process reads it. The tab could therefore grade against a different profile than the command line does on the same package.
  - Fix: State that the start route's body carries the profile path (or that the backend resolves the same setting the command line's default resolves), and require the check record to store the profile path and content hash so a reader can tell which profile a result was graded against - Key Entities already promises that.
- **specs/006-standards-check/spec.md** (FR-001 (line 177), US3 AS-15 (line 91)) - The profile path "MUST be a setting", but no surface is named for editing it. The pane's only settings surface is the Review page's settings form and its `settings.save` message, which US3 AS-15 says is unchanged. An engineer whose profile lives elsewhere has no way to point the tab at it.
  - Fix: Say where the setting is edited - the settings file and the quickstart only (recommended, so no existing page changes), or the Review settings form, in which case AS-15's "unchanged" must be qualified - and say what the refusal tells the engineer to do (the quickstart path).
- **specs/006-standards-check/spec.md** (FR-011 (line 193)) - FR-011 contains two MUSTs that collide for the same component: a component with no appearance override "MUST be checked coverage", and the check "MUST be unresolved... for every component until the workstation probe... has settled" the polarity. A component with no override does not depend on the polarity, so an implementer cannot tell which rule wins, and SC-002's compliant fixture depends on the answer.
  - Fix: Scope the probe clause: "unresolved for every component that carries an override, until the probe has settled the polarity", leaving components with no override in checked coverage.
- **specs/006-standards-check/spec.md** (FR-021 (line 203), Differences row (l) (line 255)) - The data-card gate matches "the file name" against the profile's pattern, without saying whether the file name includes its extension. The macro's pattern carries the extension, and difference (l) exists precisely because a hidden extension switched the whole check off - so leaving it ambiguous re-opens the defect the row was written to close.
  - Fix: Say "the file name including its extension, taken from the document's path", and say so in the profile schema entry for the pattern.
- **specs/006-standards-check/spec.md** (US3 (line 69), US3 AS-2/AS-4 (lines 78, 80), FR-028 (line 216)) - The headline strings are specified for two of the three verdict states and the machine value for the third contains the word "ready" while its headline is supposed to say the opposite. A run with zero errors and eleven warnings renders "ready to release" with a warning count beside it, and the spec never says the headline must qualify itself. For the one sentence an engineer acts on, the exact wording should not be left to the page.
  - Fix: Fix the three headline strings in the spec (for example "ready to release", "not ready to release", "coverage incomplete - cannot say ready"), and require the warning count to appear in the headline sentence when it is non-zero rather than only beside it.
- **specs/006-standards-check/spec.md** (US3 (line 69)) - The story promises "a release verdict for that document", but the run grades the open document and every document it reaches, so the verdict covers dozens of documents and a finding on any of them flips it. An engineer reading "for that document" will misattribute a not-ready verdict to the assembly itself.
  - Fix: Say "a release verdict for that document and everything it reaches", and require the header to name how many documents were graded.
- **specs/006-standards-check/spec.md** (Success Criteria (lines 290-306)) - No success criterion covers the tab's interaction surface: Show, the refusals, or the escaping rule. Every acceptance scenario for those exists in US3, but the measurable-outcomes section - the section the feature is accepted against - measures only speed, read-back, counting and the no-language-model rule. Feature 003 carries an explicit Show criterion.
  - Fix: Add criteria: every selectable subject either selects or reports `ok: false` naming what it could not reach; each of the five refusal causes produces its named error class and no run folder; and the markup fixture renders as text on the Standards page as it does on the other pages.
- **specs/006-standards-check/spec.md** (SC-015 (line 306)) - The acceptance criterion against the macro names no sample: "documents of the owner's choosing" with no count and no kind mix. As written it is satisfied by one part, and it is the only criterion that proves the reimplementation actually reproduces the checklist.
  - Fix: Name a sample - for example at least one release-ready assembly, one assembly with known defects, one sheet-metal part and one multi-sheet drawing - and state that every one of the sixteen checks must be exercised at least once across the sample.
- **specs/006-standards-check/spec.md** (Appendix A (lines 330-332), Input (line 9)) - Every requirement's normative source is cited by a section numbering ("§1.8", "Check 11", "L1") of a document the appendix never names and that does not exist in the repository - it lives in a scratchpad. A reader of the repository cannot resolve a single citation, so the traceability the appendix exists to provide is unverifiable.
  - Fix: Reproduce the reverse-engineering's section structure inside `research.md` (which is the one file permitted to carry the macro's constants) and cite `research.md`'s own section ids in Appendix A and in the Differences table's Source column.
- **specs/006-standards-check/spec.md** (FR-019 (line 201), US2 AS-4 (line 57), Differences from the Macro table (lines 242-267)) - FR-019 requires all three revision comparisons to run whenever their inputs exist; the macro returns immediately after reporting a missing revision table, so the drawing-to-model comparison never runs in that case. That is a deliberate improvement, but the Differences table - which the brief requires to carry one row per deliberate deviation - does not record it, so on the test day a disagreement with the macro will look like a defect rather than a listed deviation.
  - Fix: Add a lettered row: "A missing revision table no longer suppresses the drawing-to-model comparison", with the macro's early return as the left column.
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\spec.md** (FR-016 (line 198)) - Two of check 12's documented conditions are neither adopted nor rejected: how a cut-list item is identified (the macro matches a name prefix, case-sensitively, so a renamed item is silently not checked), and the duplicate-folder skip that prevents the same cut-list folder being traversed twice. FR-016 covers only the body-count skip.
  - Fix: State in FR-016 that a cut-list item is whatever the cut-list phase records as one (folder kind plus body count), not a name match, and add a Differences row saying the macro's name-prefix gate is dropped so renamed items are still checked. State that each cut-list folder is a subject once, however many times it appears in the traversal, and name `SubWeldFolder`/`SubAtomFolder` handling.
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\spec.md** (FR-007 (line 189); Differences table (lines 242-267)) - FR-007 treats the document-level count as a count of rebuild errors, but the source records that it counts everything in the What's Wrong list including warnings and mate errors. The part-level equivalent gets an explicit Differences row (u); the assembly-level one gets none, so an engineer reading a `standards.assembly.rebuild_errors` finding cannot tell that warnings inflate the number.
  - Fix: Add to FR-007 that the recorded count is the document's What's Wrong count, which includes warnings and mate errors as well as feature errors, and that the finding says so; add a Differences row or extend (u) to cover the assembly case, citing check-table §Check 2.
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\spec.md** (FR-001 (line 177), Key Entities "Standards Profile" (line 271), Edge case (line 165)) - The profile carries a "vault root" value that no requirement reads. Every path rule in the spec is expressed as a full prefix (FR-005, FR-010, FR-013), and the edge case about a non-existent vault root says the match is textual, so the vault root is dead configuration - or the prefix lists are meant to be relative to it and nothing says so.
  - Fix: Either drop the vault root from FR-001 and the Key Entity, or say what it does - e.g. the library prefix lists are written relative to it and are joined to it before matching, which is what makes one profile portable between a vault root of `C:\...` and a mapped drive. Whichever is chosen, SC-005's "two fictional profile fixtures whose vault roots ... differ" must actually exercise it.
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\spec.md** (FR-017 (line 199), US2 Acceptance Scenario 1 (line 54), Key Entities "Display Dimension" (line 279)) - "Rendered with the unit it is reported in" leaves the rendering unspecified: no unit, no precision and no source for either. The macro's unit convention is flagged company-specific in the source (C41), so if a display unit or precision is chosen it belongs in the profile, and if it comes from the document the dump must record it.
  - Fix: Add the dimension unit (and, if the document does not carry it, the display unit and decimal places) to FR-023's list of values the drawing phase records, or add a display-unit/precision pair to the profile schema in FR-001 and have FR-017 cite it. State that the raw value is kept as extracted and formatted only at render time (Differences row (p) already says this, so the FR should match it).
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\spec.md** (FR-033 (line 224), FR-032 (line 223)) - The standards-specific refusals are specified for the command line only. The route and the page-to-host message set say nothing about a run directory whose package was produced by a profile that did not run the standards phases, an unreadable or missing `package.json`, an unreadable carry-forward exception store, or the dump itself failing mid-run - all of which the sibling contract enumerates.
  - Fix: Extend FR-033 with the standards-route refusals (missing/invalid package, package without the standards phases, unreadable carry-forward store, missing/invalid profile) and extend FR-032's refusal list with a failed dump, a run folder that cannot be created, and a package the dump produced without the required phases. Add an acceptance scenario for at least the failed-dump path.
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\spec.md** (FR-024 (line 209), FR-037 (line 231), US4 Acceptance Scenario 2 (line 106)) - The refusal criterion "the extractor profile did not run the phases the standards checks read" is not decidable as written. The eight added signals of FR-022 land in phases the existing `ModelCheck` and `full` profiles already run, so a `model_check` package carries most of the evidence; the cut-list and drawing phases are the only genuinely new ones, and a part-only package legitimately has no drawing phase. Without a rule, the command either refuses valid packages or accepts thin ones.
  - Fix: State the rule in terms of phases actually recorded rather than the profile name: refuse when the phases the applicable checks need are absent for the document kinds present (cut-list phase absent and part documents present; drawing phase absent and drawing documents present; feature phase absent), and name the missing phase in the refusal. Say explicitly whether a `full` or `model_check` package is refused, and whether an older package predating the new phases is refused or graded with those checks out-of-scope.
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\spec.md** (FR-025 (line 210); Success Criteria (lines 292-306)) - FR-025 requires generalizing three values feature 003 hard-codes, one of which feature 005's carry-forward key is built on, but the spec names no regression guarantee for feature 003 or 005 and no success criterion pins their outputs unchanged. SC-004 protects only the IR goldens.
  - Fix: Add to FR-025 that the generalization must leave the RMS family's emitted values byte-identical - same `rms.*` ids, same `modeling.resilience` summary id, same rules version string, so feature 005's carry-over key does not change - and add an SC: "the feature 003 and 005 test suites pass unedited and their session/report goldens are byte-identical after the generalization". Name feature 005 in the Assumptions section as a dependency.
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\spec.md** (FR-036 (line 230), FR-037 (line 231), US4 Independent Test (line 101)) - The command-line surface is under-specified against its stated sibling: the waiver-import command is never named and its arguments never given, and `check standards` has no `--json`, although FR-037 promises output "in the shape the other check commands print" and every other check command has one.
  - Fix: Add `--json` to FR-037's signature. In FR-036, name the generalized command and its arguments (run dir, `--package`, `--file`, `--by`, `--json`) and the waiver file's shape (`{"<check id>": "<reason>"}`), and say whether the existing `accept-rms` spelling is kept as an alias or replaced - a rename is a breaking change to feature 003's quickstart (003/quickstart.md:157).
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\spec.md** (Input paragraph (line 9) and Appendix A (lines 330-367)) - Appendix A - the spec's traceability spine - cites section numbers (§1.4, Check 11, L1, C22-C25) of a reverse-engineering document that does not exist anywhere in the repository, and that cannot be committed verbatim because it contains the very company values the hard rule bars (vault paths C1-C21, the export-control phrase C30, the data-card property name C22, the part-number pattern C29). Every citation is currently unresolvable.
  - Fix: Decide where the normative sectioning lives and say so in line 9: either research.md reproduces the section structure (§1.x, Check N, L-numbers) with the constants listed once as "the values the macro carried, to be entered into the profile" - which the brief permits for research.md only - and Appendix A cites research.md headings; or Appendix A cites the macro's own subroutine names. Do not leave Appendix A pointing at a file no reader of the repository can open.
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\spec.md** (FR-021 (line 203); Edge cases (lines 158-160)) - The data-card gate is under-specified in two ways the macro's own behaviour makes load-bearing: whether the file name matched against the pattern includes the extension (the profile pattern vocabulary implies it does, since the macro's pattern ended `.SLD???`), and whether matching is case-insensitive (the macro upper-cased before matching). Both decide whether the check fires at all.
  - Fix: State in FR-021 that the subject is the file name including its extension, and that matching is case-insensitive, and say in FR-001 whether the pattern vocabulary has any other metacharacters. Add an edge case for a document whose file name matches the pattern but whose extension is unexpected (e.g. `.SLDLFP`).
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\spec.md** (Assumptions (line 314); FR-017 (line 199), FR-018 (line 200)) - The probe list omits the two enum values the reverse-engineering explicitly marks unverified, both of which decide whether the drawing checks pick the right objects. The assumption paragraph names walks and interfaces but not the constants.
  - Fix: Add both enum values to the probe list in Assumptions (and to the research probe table when it is written), each with what it settles: the annotation-type value that identifies a display dimension, and the select-type value used to identify component features in a feature-tree walk. Note that if the drawing phase records a normalized annotation kind rather than the raw enum, FR-018's "any annotation type" becomes independent of it.
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\spec.md** (FR-030 (line 221); US4 Acceptance Scenario 1 (line 105)) - FR-030 requires only "a check record" in the run folder, while the acceptance scenario and the success criterion depend on a session and a report being there too. The report is also where FR-029's "no document was rebuilt" statement has to live, so its existence needs a requirement behind it.
  - Fix: Name all three artifacts in FR-030 - the session, the rendered report and the check record - and state that the tab path and the command-line path write the same three, so `report.open` has a path to open and the read-back returns the same content on both paths.
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\spec.md** (Requirements, evidence subsection (lines 205-211)) - No requirement says the checks reject a package whose IR major version they do not support, although the constitution makes that mandatory for every consumer and this feature introduces a minor bump that older readers must tolerate. Nor does any requirement or edge case cover a package that is missing, truncated or unparseable.
  - Fix: Add to FR-022 (or a new FR) that the standards family rejects a package whose IR major version it does not support, naming the version, and that a 1.3.0 package loads with the new optional fields absent, producing unresolved coverage for the checks that need them rather than passes. Add edge cases for a missing, truncated or unparseable `package.json` on both the route and the command line.
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\contracts\profile.md** (Prefix semantics, rule 6 (lines 145-147); mirrored in rules.md line 146 and data-model.md line 246) - The cross-list tie-break ("a component matching both mate-count lists takes the one-mate requirement") is stated in three contract files but in no requirement, and it conflicts with the specificity principle the same files use for within-list matching: a *longer*, more specific two-mate prefix loses to a shorter one-mate prefix.
  - Fix: Add the tie-break to FR-006 explicitly, and prefer "longest matching prefix across the two mate-count lists wins; on an exact tie the one-mate requirement wins", which keeps one rule instead of two. Add a fixture row where the two-mate prefix is the longer one.
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\contracts\standards-check.md** (Section 1 note "[D] There is a `profile_path`" (lines 51-54) and section 2 `ready`/`init` row (line 160)) - Two inconsistencies around `profile_path`: `init` gains a field FR-038 does not list, and the contract says the host sends the path on the route while the same file says the page calls the routes itself.
  - Fix: Add `profile_path` to FR-038's `init` enumeration (with the reason: the page shows which profile is in force and relays it on the route), and reword :51 to "the host supplies the configured path in `init`; the page sends it on the request".
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\contracts\standards-check.md** (`StandardsResult` example (lines 72-82) and the bullet list at line 122) - The worked example is internally impossible, and the bullet count is wrong - both in the file that is normative for two other features' contracts.
  - Fix: Set `"warning": 0` in the example (or drop the "no drawing graded" note and add a drawing to `documents_graded`), and correct "Six points" to "Seven points".
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\contracts\cli.md** (`swreview` table, `swreview exceptions accept-standards` row) - The contract adds a second command name while claiming, in the same sentence, that the family is chosen from the record - which is exactly the reason a second name is unnecessary. FR-042 requires reuse by generalization, not a new surface.
  - Fix: Rename the generalized command (e.g. `swreview exceptions accept-checks <run_dir>`) with `accept-rms` kept as a deprecated alias, or state explicitly in cli.md why a family-named alias exists when the family is auto-detected, and record the alias in FR-042.
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\research.md** (R3.2, the "Mass override" row (line 288); plan.md line 159) - The plan discards a read the extractor already performs, in favour of an UNVERIFIED runtime COM cast, and does not say what happens to the existing behaviour that read drives. If the cast fails, `standards.part.material_assigned` is unresolved for every part - and the existing override gap may now be emitted twice or removed, which touches SC-004's "unchanged gap set".
  - Fix: State in R3.2 and in the `PropertyDumper.cs` change note what happens to the existing `GetOverrideOptions` gap (kept, so the gap set is unchanged, or replaced, and then which goldens move), and add a fallback: when the `OverrideMass` cast fails, derive `mass_overridden` from `GetOverrideOptions` being non-zero rather than leaving every part's material check unresolved.
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\research.md** (R1 table row 1 (line 28), R3.1 preamble (line 267), and the Phase 0 preamble (line 8)) - Two factual misstatements about the normative source document, in the file whose whole job is to record what that source says.
  - Fix: Correct R1 to "twenty-two limitations - four the author stated and eighteen found by reading" (and line 8 likewise), and R3.1 to "two enum values ... now settled, with seven further values confirmed".
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\quickstart.md** (Scenario 5 (US4): Waivers, twice over (lines 152-189)) - The scenario is not runnable as written: it passes a waiver file that no step creates, and grades a package that no step produces, so the SC-008 evidence cannot be reproduced from the quickstart.
  - Fix: Add the two missing steps: a here-string/`Set-Content -Encoding utf8` block that writes `waivers.json` with the four ids (the BOM warning at :169-171 then has something to apply to), and the builder invocation (or `Copy-Item` plus edit) that produces `package-with-second-subject`.
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\plan.md** (Project Structure, Source Code list (lines 210-212) vs contracts/README.md "Where these contracts land in other features' packages") - The README commits to editing two other features' contract files that the plan's self-declared exhaustive file list does not name, so those edits have no task home.
  - Fix: Add `specs/003-resilient-modeling/contracts/model-check.md` and `specs/001-agentic-design-review/contracts/agent-tools.md` to the Source Code list with those one-line notes.
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\contracts\ir-additions.md** (Section 1, `DrawingSheet.source` row (line 51); plan.md lines 201-202; quickstart.md gate 4) - Stamping `source: "pdf_ingest"` on every PDF-ingested sheet changes the serialized bytes of any package that contains `DrawingSheet` rows, but the package's byte-identical claim and its gate cover only static golden files and the gap set. Which regenerated fixtures move is left to be discovered.
  - Fix: Say explicitly whether any feature 001/002/003 golden contains PDF-ingested `DrawingSheet` rows; if so, name those fixtures and the task that regenerates them, or stamp `source` only on newly ingested sheets so existing fixtures serialize unchanged.
- **specs/006-standards-check/quickstart.md** (## Regression gate, rows 2, 3 and 5) - The three gates that carry this feature's IR-additivity promise are the only gates in the document with no command, and gate 2 as written measures the wrong artefact. The golden *input* fixtures (`reviewer/tests/golden/fixtures/*/package.json`) are static files nothing rewrites, so "byte-identical" is vacuously true for them; the artefacts actually at risk are the pytest-regressions baselines (`reviewer/tests/golden/test_golden/*.yml`), which are regenerated from callable output and would move if a new IR field reached a summary or a coverage row. Neither those baselines nor the schema-regeneration command appear anywhere in the quickstart.
  - Fix: Add a short "Scenario 0: the IR bump" to quickstart.md with the three commands: `uv run python -m swreview.ir.schema --write ../specs/001-agentic-design-review/contracts/ir.schema.json`; `uv run pytest tests/golden tests/unit/test_schema_sync.py tests/unit/test_ir_*.py`; `git status --porcelain reviewer/tests/golden` expected empty. Reword gate 2 to name `reviewer/tests/golden/test_golden/*.yml` as the artefact that must not move, and add a test that loads every existing golden `package.json` under the 1.4.0 models unchanged.
- **specs/006-standards-check/contracts/ir-additions.md** (§5 New gap `entity_kind` values (same list in data-model.md §2.4)) - `drawing_persist_ref` is declared as a new gap kind but no row, rule or requirement anywhere in the package ever produces it, and §4 states the opposite design - that a missing persistent reference is expressed by a null `persist_ref`, not by a gap. Similarly `cut_list_folder` appears in the gap list but no table row, rule row or FR names the condition that emits it.
  - Fix: Either delete `drawing_persist_ref` and `cut_list_folder` from both gap lists, or add the row that emits each (e.g. `cut_list_folder` when a body-folder's `GetSpecificFeature2` cast fails, and `drawing_persist_ref` only if PROBE-10 shows a kind that *should* have a reference and threw) and cite it from the rule that reads it.
- **specs/006-standards-check/contracts/rules.md** (Part scope rows `standards.part.sketches_fully_defined` and `standards.part.rebuild_errors`, "Evidence read" column (`.parent_ids`)) - `Feature.parent_ids` is used as the feature-tree ancestry chain, but in the IR it is the *dependency* list from `IFeature.GetParents` - the sketch a boss was built from, not the folder or feature that contains it. Naming "the chain of parent features that reaches the subject" (difference z) from `parent_ids` would print dependencies, not ancestry; the tree ancestry is the `folder_id` chain, which the same rows also list.
  - Fix: In both rows, say the ancestry chain is walked from `folder_id` and state explicitly whether `parent_ids` is read at all; if the intent is to also name a hole-wizard consumer, cite `SketchInfo.consumer_ids` (models.py:322-325), which is the field that answers that.
- **specs/006-standards-check/contracts/ir-additions.md** (§1, last row: `ExtractorInfo` | `profile` (existing field) | `str`) - The row types the field `str` and describes the change as gaining a value. It is a closed enumeration in both serializers - a pydantic `Literal` and a C# enum deserialized with `allowIntegerValues: false` - so the change is an enum extension in two declarations plus a changed `enum` array in the regenerated contract, and a 1.3.0 reader rejects a `standards` package outright rather than reading it. None of that is stated, and the row's "Mark: n/a, Phase: n/a" hides it from the task that must make both edits.
  - Fix: Type the row `Literal["full","model_check","standards"]` / `DumpProfile`, and say the extension is made in `ir/models.py` and `Dump/DumpContracts.cs` together, that it changes the `enum` array in the regenerated `ir.schema.json`, and that a pre-1.4.0 reader refuses a `standards` package (which is the intended behaviour).
- **specs/006-standards-check/contracts/ir-additions.md** (§6 Phases and profiles, the "Stated consequence" paragraph) - The paragraph names `tests/unit/test_ir_phases.py` and the C# `IrSerializerTests` as the edits the two new phases force, but omits the `DumpPhase.name` field description, which enumerates the nine current phase names verbatim and feeds the generated contract. Leaving it stale makes the regenerated 1.4.0 schema describe a phase vocabulary that no longer matches `PhaseOrder`, and `test_ir_phases.py` already asserts against that description's text, so the omission is also a silent test dependency.
  - Fix: Add `DumpPhase.name`'s description (models.py:673-678) and the matching C# XML doc on `Ir/Manifest.cs` to the named edits in §6 and in plan.md's Phase 1 key point 7.
- **specs/006-standards-check/contracts/cli.md** (## `swreview-extract` (C#), the `dump` row; and contracts/ir-additions.md §6 profile table, `full` row) - Two small phase-vocabulary errors that a task would copy. cli.md names a "mesh" phase that does not exist in `PhaseOrder`, and ir-additions.md's `full` row says "all eleven" while its own parenthetical immediately makes one of the eleven conditional, so the row contradicts itself and understates that a `full` dump of a part or assembly runs ten phases and records the eleventh as `skipped`.
  - Fix: In cli.md drop "or mesh". In ir-additions.md §6 and data-model.md §2.5 write the `full` row as "document, manifest, mate, feature, equation, cutlist, hole, fastener, face, body, and drawing only for a drawing root (recorded `skipped` otherwise)".
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\spec.md** (FR-024 (line 225), phrase "each view's and sheet's annotations") - FR-024 requires sheet-level annotations, but `ISheet` exposes no annotation accessor on the 2024 interop; the contracts silently reinterpret this as "the type-1 pseudo-view's annotations". A requirement that names a read that does not exist will either be implemented wrongly or quietly dropped.
  - Fix: Reword FR-024 to "each view's annotations, including those of the sheet-format (type-1) pseudo-view", matching ir-additions.md §3.5, and do the same for the notes clause.
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\spec.md** (FR-025 (line 226)) - FR-025 names a "property" dump phase that does not exist and omits the `equation` phase, so the requirement's phase list does not match the profile definition the rest of the package uses.
  - Fix: Change FR-025's list to "the document, manifest, mate, feature, equation and cutlist phases", matching ir-additions.md §6 and cli.md.
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\contracts\profile.md** (Prefix semantics rule 6 (line 145); data-model.md `PrefixMatch` (line 246)) - The contracts add a cross-list tiebreak (one-mate beats two-mate) that the spec never states, and justify it by "the schema defines the one-mate list as the more specific" - which the schema does not define anywhere. FR-006's own rationale ("the four lists answer four different questions") does not hold for the one-mate and two-mate lists, which answer the same question.
  - Fix: Either add the tiebreak to FR-006 explicitly as a spec decision, or replace it with "across the two mate-count lists the longest matching prefix decides; a tie takes the one-mate requirement" and state that rule in the profile schema's field rules so the justification is真 rather than asserted.
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\research.md** (R8 table (lines 452-468)) - R8 applies its own rule ("the mutating members that sit beside the reads the new phases perform") unevenly: the write counterparts of several of the exact reads this feature adds are left off the denylist.
  - Fix: Add those members to R8's table (and to the plan's guard row and the guard test), or state explicitly why the display-dimension, dimension, note, annotation and feature write families are excluded.
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\data-model.md** (§2 "IR additions (schema 1.4.0)", lines 66-238) - data-model.md §2 restates contracts/ir-additions.md §1-§6 almost field for field - two normative copies of the same tables, null rules, gap lists, phase order and profile matrix. Any correction (for example the revision-table plurality above) has to be made twice and will drift.
  - Fix: Keep the field tables in one place - `contracts/ir-additions.md` is the one that also carries the interop member and verification mark - and reduce data-model.md §2 to the relationships, the additivity rule and a pointer, as data-model.md §1 already does for the profile ("`contracts/profile.md` is the normative document; this is the model").
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\spec.md** (SC-005 (line 323)) - SC-005's headline claim and its stated measurement do not match: scanning the repository for the *fixture* profiles' values cannot demonstrate that no *company* value is present. As written the criterion is not measurable for the thing it asserts.
  - Fix: Split the criterion: (a) a scan for the macro's values (the R5 table read as data) over every path except `research.md`, which is the assertion SC-005 actually makes; (b) the fixture-value scan, which proves no fixture value is compiled in; (c) the matched-pair run. Name the scan's allowlist (research.md only).
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\spec.md** (FR-032 (line 236) with FR-009/FR-011 (lines 211, 213) and SC-002 (line 320)) - Because the package walks only the root assembly's mate group, every assembly containing a sub-assembly gets at least one unresolved row, and FR-032 forbids `ready` whenever any check has an unresolved row. The release verdict the owner asked for is therefore unreachable for essentially every real assembly, and the only fixture that reaches `ready` is a synthetic single-level one. No mitigation is named.
  - Fix: Choose and record one: (a) bring sub-assembly mate extraction into US1 (the coverage audit places it in `MateDumper.cs:29,51-70`, a bounded change); (b) classify the known, permanent data gap as its own bucket (e.g. `known_limitation`) that does not suppress `ready` while still travelling on the headline; or (c) state in FR-032 and the tab copy that `ready` is not expected for multi-level assemblies in this version, so the owner is not surprised at the pilot.
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\contracts\standards-check.md** (`StandardsResult.verdict.counts` example (§1) and data-model.md `BucketCounts` (line 250)) - The unit of the bucket counts is never defined - checks, documents, or (check, document, bucket) rows - although the same document states that one check can land in several buckets. The example sums to more than sixteen, so a page or a test cannot be written against it, and SC-002/SC-003's "zero unresolved checks" is ambiguous.
  - Fix: State the unit explicitly in FR-032 and data-model.md - e.g. `error`/`warning` count findings, and the four coverage buckets count **distinct checks** that reached that bucket (a check in several buckets counts in each) - and make the example arithmetic consistent with it.
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\plan.md** (Constitution Check, "third-party reuse respects licenses" row (line 110); research.md R1 (lines 5-6, 26-27)) - The plan prints the company's macro file names in a public repository, outside research.md, while the spec deliberately avoids naming them - the same inconsistency the handling note in R5 warns about.
  - Fix: Refer to "the owner's release-checklist macro" in plan.md (and, if the owner's handling decision is to keep the company prefix out, in research.md R1), keeping the literal file names in the one place the owner has agreed to carry company-identifying data.
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\research.md** (R3.3 "Dangling annotations" row and R4 PROBE-6) - The dangling check moves from the macro's `GetFirstAnnotation3`/`GetNext3` walk to `IView.GetAnnotations()`, but no probe asks whether the two return the same set (in particular whether display-dimension annotations are included). SC-016's per-(check, document) parity with the macro depends on that answer, and a smaller set would produce a silent under-report rather than an unresolved row.
  - Fix: Add to PROBE-6 a printed comparison of `GetAnnotationCount()`/`GetAnnotations()` against the `GetFirstAnnotation3`/`GetNext3` walk, per view, with types listed; record the answer in R3.3 and, if the sets differ, name which enumeration the dumper uses.
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\plan.md** (Project Structure, `Ir/Enums.cs` row (line 147)) - The plan adds an `AnnotationOwner` enum that no field in the IR contract or the data model uses; `DrawingAnnotation` carries only `owner_id`. Either the enum is dead, or a field is missing from the contract.
  - Fix: Drop `AnnotationOwner` from the plan, or add the `owner_kind` field it types to ir-additions.md §3.5 and data-model.md §2.3 with its null rule.
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\contracts\ir-additions.md** (§5 "New gap `entity_kind` values" (line 203); data-model.md §2.4 (line 208)) - The gap kind `cut_list_folder` is declared in both gap lists but no null rule in either document ever produces it, so a test enumerating the new gap kinds will have one it cannot exercise.
  - Fix: Either give `cut_list_folder` a producing condition (for example a body-folder whose `GetTypeName2` or `GetSpecificFeature2` could not be read, which PROBE-8 makes likely enough to be worth naming), or remove it from both lists.
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\contracts\cli.md** (`swreview` table, row `swreview exceptions accept-standards`) - A second command name is introduced for a body that dispatches on the record's own family field, which is both redundant and undefined at the edges: nothing says what `accept-standards` does when pointed at an rms run folder (or `accept-rms` at a standards one). The status strings are also presented as the existing command's when they are not, and the existing command has four invalid kinds, not two.
  - Fix: Prefer one command (rename to `exceptions accept-checks`, keeping `accept-rms` as an alias) whose family comes from the run's `check.json`; or keep two names and specify the refusal when the named family does not match the record. Either way, state where the per-family status labels and the per-family waivability test live (a `CheckFamily` field or the family's registry), and either keep the existing strings or record the wording change as a deliberate break of FR-042's "the existing command's" wording.
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\contracts\standards-check.md** (Section 1, `StandardsResult.verdict.counts`; data-model.md section 3, `BucketCounts`) - The unit of the headline counts is never defined - checks, documents, coverage rows or findings - and the worked example is only consistent with a mixed unit. These counts are the product of the feature; an engineer cannot read "skipped: 4" without knowing what was counted.
  - Fix: Define each count in data-model section 3 - e.g. `error`/`warning`/`waived` are findings, the four coverage counts are (check, bucket) rows - and label them that way in the rendered headline and in the example, so SC-001 to SC-003 can assert numbers that mean one thing.
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\contracts\rules.md** (Assembly scope table, `standards.assembly.not_transparent`, Checked and Unresolved columns) - The row resolves a conflict inside FR-012 silently and in the direction the FR's own sentence forbids: it makes every component unresolved while the polarity is unsettled, including components the FR says must be checked coverage. The divergence is not recorded anywhere.
  - Fix: Pick the broad reading (a component with no override is still unresolved until PROBE-2, because the polarity question decides how the recorded value is read for every component) and record it as an explicit resolution in research R2.8, or narrow the row to match FR-012's first sentence and change SC-002's expectation. Do not leave the two halves of FR-012 contradicting each other in the contract that implements it.
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\plan.md** (Constitution Check row III, and Delivery order rows 1 and 3, against quickstart scenarios 1-6 and spec SC-006) - The fixture inventory does not cover the success criteria. The plan names five goldens and two profile fixtures; the quickstart grades seven packages, and SC-006 needs an eighth that is named nowhere.
  - Fix: List every fixture package the success criteria require in one place (plan Delivery order row 1 or a Testing table), including the matched pair, the multiplicity fixture for SC-006 and the second-subject package for SC-008, and say which builder call produces each - so the setup story delivers them rather than a later task discovering them.
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\research.md** (R7, `CheckFamily` paragraph; plan.md line 188) - The word `family` already means something else in the module being generalized, and the plan puts the two meanings in the same file. `checks/rms/run.py` calls its three rule scopes "families" throughout; `checks/rules/run.py` is to use `family` for rms-versus-standards.
  - Fix: Rename one of the two in the move task: keep `family` for the `CheckFamily` axis (it is what `check.json` records) and rename the RMS rule-scope local and parameter to `scope`, which is what `RmsScope`/`RULES.scope` already call it. Name the rename in the move task so the "every rms test passes unedited" gate still holds (the rename is internal to `run.py`).
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\tasks.md** (T069 (line 145)) - The `standards-drawings` golden is specified with only the three-sheet violating drawing and one compliant drawing, omitting four fixture contents the quickstart and four US2 acceptance scenarios require: a PDF-ingest-only drawing, a drawing referencing two models, a drawing with no views, and a document reached both from the root and from a drawing reference.
  - Fix: Extend T069's fixture list with the four missing drawings/relationships and add the matching acceptance bullets, so the US2 acceptance scenarios are exercised at the golden level and not only in T065's unit tests.
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\tasks.md** (T043 (line 105)) - The part-rule test task omits the library skip-prefix skip for two of the four part checks. FR-006 makes the skip apply to all four part checks plus the data card, and `contracts/rules.md` states it for `rebuild_errors` and `material_assigned`, but T043 only names it for `sketches_fully_defined` and `cut_list_excluded`.
  - Fix: Add the skip-prefix skipped-coverage case to T043's `rebuild_errors` and `material_assigned` classes, naming the matched prefix as the reason.
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\tasks.md** (Phase 3 tasks generally; FR-002 coverage row (line 271)) - FR-002's second half - that a run folder created before a schema refusal is reported as an empty run rather than as a result - has no task. T073 only asserts the 400 `ProfileInvalid` reply, and T075 covers the host-side refusal that writes no folder at all, which is the other case.
  - Fix: Add the empty-run assertion to T078 (the page contract test) or to T073, and cite it in the FR-002 coverage row.
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\tasks.md** (Phase 3 "Independent Test" (line 86)) - The phase's independent test says the compliant fixture produces "every one of the twelve checks in checked or skipped coverage with a stated reason", with no unresolved caveat and without saying which compliant fixture. That contradicts T054's own acceptance and SC-002, both of which require unresolved rows on `-compliant`.
  - Fix: Name `standards-compliant-flat` in the Phase 3 independent test and carry the `standards.assembly.not_transparent` unresolved caveat, matching spec.md:21 and T054.
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\tasks.md** (T014 (line 60)) - T014 is marked `[P]` alongside T012, but its own text requires validating against the regenerated 1.4.0 contract, which only exists after T013. The dependency section attaches that edge to T015 instead of T014.
  - Fix: Either state in T014 that the contract-validation half runs after T013 (and keep `[P]` only for the fixture-and-assertion authoring), or move the edge: "T014 after T013".
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\tasks.md** (T054 and T055 (lines 119-120)) - Both tasks carry their markers in the order `[US1] [P]`, contradicting the format the file declares two sections earlier. Every other task in the file uses `[P] [Story]`.
  - Fix: Rewrite as `T054 [P] [US1]` and `T055 [P] [US1]`.
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\tasks.md** (Phase 8 (lines 208-222)) - Every other workstation task names the quickstart scenario it runs, but quickstart Scenario 9 (the ten probes) is named by no task, while Scenarios 7, 8, 10, 11 and 12 each have one. The probe recording is spread across T101-T105 without the scenario reference, so a reader checking scenario coverage finds a hole.
  - Fix: Add "(quickstart Scenario 9)" to T101 or to the Phase 8 preamble, so every quickstart scenario maps to a task.
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\tasks.md** (T091 (line 189)) - The task description is not a task: "Implement whatever the flow needs across" three files gives no verifiable unit of work, which is the one implementation task in the file without a named deliverable. Its acceptance leans entirely on T090 and a quickstart scenario.
  - Fix: Name the three deliverables explicitly: the exception lookup and checked-within-scope re-rendering in `run.py`, the waived-count and exception-id rendering in `report.py`, and the accept-route re-render in `chat/server.py`.
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\checklists\requirements.md** (CHK009 (line 23)) - The item claims no two requirements conflict and that FR-012's checked reading is reconciled explicitly, but FR-012 contains two sentences that contradict each other while the polarity is unsettled, and the reconciliation lives in `contracts/rules.md` and `research.md`, not in the spec the checklist is validating.
  - Fix: Amend FR-012 so its checked condition is explicitly conditional on the settled polarity, then restate CHK009 to point at the amended FR rather than at a reconciliation held in the contract.
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\checklists\requirements.md** (CHK021 (line 38) and Notes (line 58)) - The checklist declares itself a pre-planning gate whose failures "would require spec updates before `/speckit-clarify` or `/speckit-plan`", yet CHK021 is validated against `tasks.md`'s requirement-coverage table - an artefact produced two phases later. The item can never have been satisfiable at the stated gate, and it leaves the checklist's scope undefined.
  - Fix: Either split CHK021 into a spec-only half (every FR has an acceptance scenario or success criterion) and record the tasks-table check as a separate post-tasks validation iteration, or update the Purpose and the Notes to say the checklist was re-run against the completed plan package.
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\tasks.md** (T069 (line 145)) - The drawing fixture task enumerates four of the eight elements the quickstart puts in `standards-drawings`, so four US2 acceptance scenarios have a unit test but no fixture, and quickstart Scenario 4 - which T097 must run - cannot pass on the fixture T069 builds.
  - Fix: Extend T069's fixture list with the PDF-ingest-only drawing, the two-model drawing, the no-views drawing and the document reached both from the root and from a drawing reference, and add the four matching acceptance clauses.
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\tasks.md** (T012 (line 58) and T105 (line 217)) - Both tasks assert persistent references and id prefixes for entity kinds that the IR contract does not give them. `DrawingRecord` and `RevisionTableRow` carry neither an `id` prefix nor a `persist_ref`; the persistent reference for a revision table is on `RevisionTable`, not on its rows. As written T012's assertion is unsatisfiable for two of the nine models and T105 probes a kind that has nowhere to record the answer.
  - Fix: In T012, name the seven records that carry an id and a persist_ref pair and assert separately that `DrawingRecord` and `RevisionTableRow` carry neither. In T105, replace "revision-table row" with "revision table". Spec FR-026 (spec.md:228) says "revision-table row" too and should be corrected to "revision table" in the same pass.
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\tasks.md** (T003 (line 38)) - The profile test task states a blanket rule - every key required, every value may be empty - that is false for three of the schema's fields, so a test written to that sentence would either assert something the schema forbids or quietly skip those fields.
  - Fix: Restate as "every key is required; every string and list value may be empty, while `version` must be `1` and `revision.cell.row_from_end` and `revision.cell.column` must be integers of zero or more - an empty or negative value for those three is `ProfileInvalid` naming the field".
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\tasks.md** (T002 (line 37)) - The fixture task requires the two profiles to differ in every field from each other, which `version` makes impossible, so the acceptance cannot be satisfied literally.
  - Fix: Change T002 to "differing in every value-bearing field (vault root, the four library lists, the data-card property names, the part-number pattern, the revision property, initial, header text and cell, the material configuration and the export-control phrase) from each other and from every R5 value; `version` is `1` in both".
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\tasks.md** (T077 (line 164)) - T077 carries the `[P]` marker while the dependency section states it is verified only after T076, so the marker contradicts its own declared meaning and would let a scheduler start it against an unfinished task.
  - Fix: Drop the `[P]` from T077 and add it to the Phase 5 line of "Parallel opportunities" as a task that follows T076, or keep `[P]` only for writing the parameterization and add a separate verification step after T076.
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\tasks.md** (T054 (line 119) and T055 (line 120)) - Two tasks reverse the declared marker order, which is the ordering every other task in the file follows and the one the Spec Kit format line fixes.
  - Fix: Rewrite as "T054 [P] [US1]" and "T055 [P] [US1]".
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\tasks.md** (Requirement coverage table, FR-023 row (line 292) and FR-006 row (line 275)) - Two rows of the coverage table omit tasks that the requirement's own text makes necessary, so the table overstates nothing but understates coverage in a way a reviewer checking "does this task's acceptance fail if the requirement is unmet" would flag.
  - Fix: Change the FR-023 row to "T012 to T018, T031 to T040" and the FR-006 row to "T005, T006, T041, T043, T045".
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\tasks.md** (T092 (line 197)) - The performance task excludes itself from CI runs while claiming to measure a threshold on the CI machine, so SC-011 has no run that ever executes it as a gate.
  - Fix: State who runs it and when - for example "run explicitly with `-m perf` on the CI machine as part of Polish (T097), with the measured number recorded" - or move the threshold to a nightly job the task names; either way, name the run that produces the SC-011 number.
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\tasks.md** (T091 (line 189)) - The task description does not say what is implemented, which is the one thing a reviewer must be able to check before the task is claimed done. "Whatever the flow needs" names three files but no change in any of them.
  - Fix: Replace with the three named changes the accept flow actually needs: the waived-outcome path in `report.py` that renders an accepted check as checked within scope carrying the exception id and note, the waived count reaching `ReleaseVerdict.notes` in `run.py`, and the accept route's re-render plus the `409 RuleNotAcceptable` guard in `chat/server.py` (contracts/standards-check.md section 5).
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\tasks.md** (T096 (line 201)) - The contract-update task has no acceptance clause and its last item names a file without naming what changes in it, so nothing decides whether the task is done.
  - Fix: Say which README section gains what sentence, and add an acceptance line such as "Acceptance: every contract row names a route, message or command that exists in the shipped code, and `test_provider_schema.py` passes with the bumped count".
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\tasks.md** (T011 (line 46) and T018 (line 64)) - Both tasks require a new test or assertion to be written but name no file to write it in, which is the one thing that makes a test task checkable in a package whose every other test task names its path.
  - Fix: Name the files: the rms regression assertion in `reviewer/tests/unit/test_rms_report.py` (or wherever `_split_by_reportability` is already covered), and the fixture-parse test as a new `reviewer/tests/unit/test_ir_golden_fixtures_load.py`.
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\tasks.md** (T041 (line 103)) - The assembly-rule test task drops one half of `mate_references`' unresolved condition and states no unresolved condition for `one_fixed`, so two contract columns have no named assertion even though the task's preamble claims all four columns are covered.
  - Fix: Add "or null" to the `mate_references` unresolved clause and "unresolved per component whose `is_fixed` could not be read" to the `one_fixed` clause.
- **C:\Users\Cole\source\repos\smart_SW\specs\006-standards-check\checklists\requirements.md** (CHK010 (line 24)) - The measurability item asserts a shape - fixture, count, assertion - that three of the eleven offline criteria do not have, so the item passes on a generalization rather than on what the criteria say.
  - Fix: Restate as "SC-001 to SC-011 each name the artefact they are measured on and an assertion that can be run offline; SC-001 to SC-007 and SC-011 additionally name a fixture and a count", and re-evaluate the mark against that wording.

## Feature 006 round A (2026-09-17)

- **reviewer/tests/unit/test_standards_no_company_values.py:8** (setup) - Documented deviation from T001 that is not surfaced anywhere but this module docstring. T001 requires (a) a field-by-field comparison of all thirteen profile fields against an R5 `{field -> macro value}` map and (b) a literal tree scan for R5's vault root, library folder names, part-number pattern, export-control phrase and qualified data-card property name. `research.md` R5 at HEAD carries only the *shape* of each value (the concrete values were handed to the owner separately), so only `part_number.pattern` and `revision.cell` can be rebuilt. The implementation substitutes a three-way disagreement test between the shipped profiles plus a `vault_root:` census and a shipped-source scan. The substitution is sound and the R5 parser does fail loudly on drift (`test_the_r5_table_parses_and_names_exactly_the_profile_fields`), but the consequence is that a leak of the real vault root or export-control phrase into `specs/006-standards-check/` prose would be caught by no test - only by T098's matched-pair run.
  - Fix: Report the deviation explicitly to the spec owner rather than leaving it in a docstring, and decide whether SC-005 measurement (2) should be restated in the contract. No code change needed if the deviation is accepted.
- **reviewer/tests/fixtures/standards/profile-a.yaml:39** (setup) - `revision.cell.row_from_end: 0` is exactly the row selector R5 records for the macro ("last row, column 0"). The leak test passes because it folds `revision.cell.row_from_end` and `.column` into one compound field (`r5_field`, line ~256) and compares the pair {0,1} against {0,0} - and T002 does list "cell" as one field, so this is not a contract breach. But `contracts/profile.md` makes a point that its example is "deliberately not the last row and not column 0 even though that is what most templates want", and profile-a re-introduces the half the contract calls out, in the one field where the macro's value is still literally derivable.
  - Fix: Change profile-a's `row_from_end` to a non-zero value (e.g. 3). Keep `row_from_end: 0` coverage of the "last row" semantics in `test_standards_profile.py`'s hand-built YAML instead of in a shipped fixture, so no committed profile carries a value R5 pins.
- **reviewer/tests/unit/test_check_family.py:184** (setup) - Three live copies of the four rms waiver labels with nothing tying them together: `RMS_FAMILY.waiver_labels` (checks/rms/registry.py:378-381), `RMS_WAIVER_UNKNOWN`/`RMS_WAIVER_INVALID` (reviewer/src/swreview/cli.py:1196-1202), and the literals in this test (187-190). `family.py`'s own docstring claims the labels are "a fact about the family and not a constant in the command that prints them", which is not true until T089 rewires cli.py. T088 requires the rms labels stay byte-identical; right now the two source copies can drift silently in either direction.
  - Fix: One line now: have `test_the_waiver_status_labels_are_byte_identical_to_today_s` assert `RMS_FAMILY.waiver_labels` against `swreview.cli.RMS_WAIVER_UNKNOWN`/`RMS_WAIVER_INVALID` instead of against re-typed literals, so the duplication is pinned until T089 removes it. Soften family.py's docstring claim to "will be" until then.
- **reviewer/tests/unit/test_support_standards.py:476** (setup) - T007's byte-for-byte clause is implemented as builder determinism, not as the drift gate the sentence asks for: `test_rebuilding_reproduces_a_written_fixture_byte_for_byte` builds the same specs twice in `tmp_path` and compares. That proves the builder is deterministic; it does not prove that a *committed* fixture still matches what its builder produces, which is what "a drifted fixture is a failure and not a rewrite" means. No standards fixture exists yet (T031+ write `standards-seeded/` and `standards-compliant-flat/`), so nothing currently guards them.
  - Fix: Keep the determinism test, rename it to say what it measures, and add the real gate when T031's fixtures land: rebuild each committed `reviewer/tests/golden/fixtures/standards-*/package.json` from its spec list and compare bytes against the checked-in file. Flag this to whoever owns T031 so the gate is not lost between tasks.
- **reviewer/src/swreview/checks/standards/library.py:142** (setup) - `_prepare` silently drops a blank or whitespace-only prefix entry (`if entry.strip()`). Neither `contracts/profile.md`'s prefix semantics nor T005 says what an empty *entry* in a list means - the contract only settles empty *lists*. This is behaviour the implementation invented (and did pin, at test_standards_library.py:179). It also sits oddly beside the schema's own principle that unknown keys are refused "so a typo in `skip_prefixes` cannot silently disable a skip list": a stray `- ""` is likewise a typo, and it is silently ignored rather than named.
  - Fix: Either refuse a blank list entry in `load_profile` as `ProfileInvalid` naming the list and the index (consistent with the schema's no-silent-typo rule), or have the contract state that a blank entry matches nothing. Pick one and record it in `contracts/profile.md` prefix semantics; do not leave the decision only in `library.py`.
- **reviewer/src/swreview/checks/rms/report.py:1** (setup) - Stale task ids in docstrings written during the move. `rms/report.py:1` says "(T022)" - T022 is this feature's standards registry, not the report move (T010). `rms/run.py:462` cites "(T072)" for the check record, a feature 003 task id carried through the move. Task ids in shipped source go stale by design and this one is already wrong on arrival.
  - Fix: Drop the bare task ids from these two docstrings, or replace them with the durable citation the rest of the module uses (`specs/006-standards-check/research.md` R7).
- **reviewer/tests/support/features.py:78** (setup) - Outside T001-T011, reported rather than fixed: `tests/support/features.py` gained `SketchSpec.text_segments`, a `sketch_feature(text_segments=...)` parameter and the `SketchInfo(text_segment_count=...)` wiring. T013 says it edits "exactly the Python version pins plan.md's Source Code block lists ... and nothing else", and this helper is not a version pin. The edit is additive and defaults to null, and the goldens are provably byte-identical, so nothing is broken - but it is an unlisted edit to shared feature 003 test support.
  - Fix: Confirm with the T012/T013 owner that `plan.md`'s Source Code block covers this file, or have `test_ir_standards.py` construct `SketchInfo` directly rather than widening the shared builder.
- **reviewer/tests/unit/test_check_family.py:48** (setup) - `standards_family()` builds a second, hand-written copy of the standards descriptor, and its docstring promises "T022 replaces it with the real `STANDARDS_FAMILY` and the values below are what that one has to match". T022 has since landed (`reviewer/src/swreview/checks/standards/registry.py:233`) and does NOT match: the test's copy has `tools={"assembly": "check_standards_assembly", "part": "check_standards_part", "drawing": "check_standards_drawing", "document": "check_standards_document"}`, the real one has `dict.fromkeys(("assembly","part","drawing","document"), CHECK_TOOL)` where `CHECK_TOOL == "check_standards"`. Nothing catches the divergence, and the three assertions in `TestTheDescriptorAdmitsASecondFamily` are tautological - each asserts the literal values constructed two lines above, so they prove only that a frozen dataclass stores its arguments. This is a live DRY violation: two definitions of one family's facts, already disagreeing.
  - Fix: Import `STANDARDS_FAMILY` from `swreview.checks.standards.registry` and assert against it in `TestTheDescriptorAdmitsASecondFamily`, deleting the local `standards_family()`. If you would rather keep T009's test free of a T022 import so the neutral-layer assertions stand alone, keep the local builder but add one assertion `standards_family() == STANDARDS_FAMILY`, which is the promise the docstring already makes. I recommend the first: the local copy exists only because T022 had not landed, and it has already drifted once.
- **reviewer/tests/unit/test_rms_report.py:284** (setup) - T011 requires "one assertion is added in `reviewer/tests/unit/test_rms_report.py` that an **rms** result whose document has no `ComponentInstance` still lands in unresolved coverage". The assertion actually added is `assert [row.kind for row in package.documents if row.document_id == GHOST] == ["part"]` - a statement about the fixture's document kind, not about the report layer's behaviour. The behavioural claim is carried entirely by the pre-existing feature 003 assertions above it (`findings == []`, the unresolved item, the reason text), so the gate adds no new pin. Concretely: if someone later adds `"part"` to `DOCUMENT_SCOPED_KINDS` in `checks/rules/report.py:76`, the added line still passes and the surrounding test breaks in a way that reads as a fixture problem rather than as the regression T011 exists to catch.
  - Fix: Import `DOCUMENT_SCOPED_KINDS` from `swreview.checks.rules.report` and assert `kind not in DOCUMENT_SCOPED_KINDS` instead of `== ["part"]`. That ties the regression to the constant whose widening is the risk, keeps the edit to one assertion in one feature 003 file as T011 allows, and makes the test fail loudly if the drawing-only carve-out is ever broadened. Alternative (do nothing): defensible, because `test_rules_report_document_scope.py::TestEveryOtherKindIsUnchanged::test_a_part_with_no_instance_is_still_unresolved_coverage` already pins the same property on the neutral layer with a fictional family - but that one would also pass if `DOCUMENT_SCOPED_KINDS` grew, since it names a part too. I recommend the import.
- **reviewer/tests/unit/test_support_standards.py:476** (setup) - T007 asks that "re-running the builder reproduces a **written fixture** byte for byte, so a drifted fixture is a failure and not a rewrite". `test_rebuilding_reproduces_a_written_fixture_byte_for_byte` writes the same specs twice into `tmp_path` and compares the two - a determinism test (real, and worth having: it would catch a timestamp, a random id or a set-ordered field), but it cannot catch what the task names, because no package fixture is committed for it to compare against. `reviewer/tests/fixtures/standards/` holds only `profile-a.yaml` and `profile-b.yaml`.
  - Fix: Two options. (a) Commit one small canonical package under `reviewer/tests/fixtures/standards/` built from `[an_assembly(), a_part(), drawing_fixture()]` and assert the freshly built bytes equal the committed bytes, keeping the tmp_path determinism assertion beside it. (b) Do nothing now and leave it to the later task that commits the first standards golden, adding the comparison there. I recommend (a): the fixture is a handful of KB, and the property T007 is buying - a fixture edited by hand rather than regenerated is a red test - only exists once something is committed. Tell me which, since (a) adds a file the task list does not name.
- **reviewer/src/swreview/checks/standards/library.py:142** (setup) - `_prepare` silently drops any entry whose `strip()` is empty (`if entry.strip()`), documented only in its own docstring ("A blank entry is dropped: it is not 'every path'"). `contracts/profile.md` says every list value may be empty but says nothing about a blank ELEMENT, so this is behaviour invented in the implementation. The direction is the safe one - without the drop, `""` normalizes to `""` and every subject `startswith("")`, which would make one stray blank line in `skip_prefixes` skip the entire vault - and `test_an_empty_entry_matches_nothing` pins it. But an engineer reading the contract cannot learn that a blank line is ignored rather than refused.
  - Fix: My recommendation: make it visible in the schema rather than in the matcher - reject a blank or whitespace-only element in `profile.py` as `ProfileInvalid` naming the list and the index, which is the same "a half-written file is named, not guessed at" rule the rest of the schema already follows, and delete the drop from `_prepare`. Option B: keep the drop and add one line to `contracts/profile.md` prefix semantics saying a blank entry matches nothing. Option C: do nothing - the behaviour is safe and tested. Both A and B need your say-so: A changes a refusal the contract does not currently list, and B edits a contract no task in T001-T011 authorizes editing. Which do you want?
- **reviewer/src/swreview/checks/rms/run.py:223** (setup) - T010 asked for `run.py`'s rule-scope local and parameter to be renamed from `family` to `scope`. `_dispatch`'s parameter and every loop local were renamed correctly (and `_dispatch`'s docstring explains why). But the public keyword argument `families: Sequence[RmsScope] | None` survives on `run_rms_check`, and its docstring at lines 238-246 still says "Which rule families to run" / "The families to actually run" - so the exact ambiguity T010 set out to remove is still in the file, on the one name a caller reads. No feature 003 test passes `families=`; the only callers are `swreview/chat/server.py:1169` and `swreview/remodel/artifacts.py:459`.
  - Fix: The blocker is `remodel/artifacts.py:470`, which writes a literal `"families"` key into an artifact record that feature 004/005 tests or goldens may pin. My recommendation is to do nothing here and leave it to whichever later task owns that record, rather than widen T010's blast radius into feature 004's artifacts - but say so explicitly as a deviation in the task report rather than leaving it to be found. If you would rather close it now: rename the parameter and `RMS_FAMILIES` to `scopes`/`RMS_SCOPES`, keep the serialized `"families"` key unchanged, and run the feature 004/005 suites. Your call.
- **reviewer/src/swreview/checks/rms/report.py:1** (setup) - Module docstring is tagged "(T022)" - `"""What the RMS family adds to `checks/rules/report.py`, and nothing else (T022)."""`. T022 is the standards registry task; this file is T010's half of the move. A reader tracing the move back to its task lands in the wrong place.
  - Fix: Change the tag to (T010). Trivial, no risk, no test touches it.
- **reviewer/tests/unit/test_standards_no_company_values.py:1** (setup) - Documented deviation from T001's literal text that needs your explicit sign-off. T001 says the field-by-field half must parse R5 into a `{profile field -> macro value}` map and assert every field differs, and the literal half must scan for "the vault root, the library folder names, the part-number pattern, the export-control phrase and the qualified data-card property name". `research.md` R5 records only the SHAPE of each value (the concrete values were handed to the owner separately), so only two fields - `part_number.pattern` and `revision.cell` - can be rebuilt and compared, and only the part-number pattern can be scanned for literally. The test substitutes a three-way disagreement between the example and the two fixtures for the other eleven fields, plus a census of populated `vault_root:` lines and a scan of `reviewer/src/` and the three C# projects for the three profiles' own distinctive values. This is a sound substitution and the module docstring argues it well. The residual gap: a company vault root or library folder name written into `specs/006-standards-check/` PROSE (not as a `vault_root:` line, and not equal to the macro's part-number pattern) is caught by no assertion in this file, even though T001 names catching a leak in this package's own prose as the reason to include `specs/006-standards-check/` in the scan.
  - Fix: Options. (a) Accept as written, recording the deviation in the task report - the third SC-005 measurement (the matched-pair run of quickstart scenario 6, T098) is the one that catches a value under a different spelling, and prose leaks stay a review responsibility. (b) Add a prose scan: for each of the three profiles, assert that no `library.*` entry, vault root or export-control phrase of ANY of them appears in `specs/006-standards-check/` outside the contract's own block - this catches a fixture value pasted into prose but still not a real value nobody in the repo knows. (c) Ask the owner for a redacted checksum list of R5's real values to scan against, which defeats the purpose. I recommend (a) plus a line in the report; the test is already doing more than a grep could. Confirm you agree before this ships as the SC-005 measurement.
- **extractor/SwReview.Extractor/Dump/PackageWriter.cs:367** (ir) - The conditional drawing gap always blames the profile: "the drawing phase did not run under the '<profile>' profile". That is true for `model_check`, but for `full` and `standards` over a part or assembly root the profile does run the drawing phase -- the reason it did not run is that the root document is not a drawing (`drawingRoot` at line 278), or that no `IDrawingSource` is wired (SwDump passes null). A reader told to blame the profile will re-run `--profile standards` and get the identical gap back. The contract only requires the gap to name the profile; it does not require it to assert the profile is the cause.
  - Fix: Keep the profile in the sentence but state the actual cause beside it: name the root document kind when `drawingRoot` is false, and say "the profile does not read drawings" only when `standardsEvidence` is false. Add the third case (a profile that would run it, with no source wired) so the message never claims a cause that is not the one that fired.
- **extractor/SwReview.Extractor/Ir/PackageSerializer.cs:69** (ir) - `Serialize` mutates the caller's object: `package.OmitEmptyAdditiveArrays()` rewrites `CutListItems`/`DrawingRecords` from `[]` to `null` in place. A caller that serializes a package and then reads `package.CutListItems.Count` gets a NullReferenceException on the second statement, and `PackageAppender`/`PackageReuse` hand the same instance around. It is harmless today only because `PackageWriter` never assigns an empty list, which makes the guard a no-op on the one path that is tested -- so the hazard is untested rather than absent.
  - Fix: Make the normalization non-mutating: compute the two members' serialized form locally (e.g. serialize a shallow copy, or null them, serialize, restore in a `finally`), or move the responsibility to a converter. If the mutation is kept deliberately, add a test that asserts what the caller's object looks like after `Serialize` returns, so the contract is pinned rather than implied by an XML comment.
- **extractor/SwReview.Extractor/Ir/DrawingRecord.cs:50** (ir) - The additivity rule's array half is applied asymmetrically. Python omits every empty list on the new records (`sheets`, `views`, `revision_tables`, `display_dimensions`, `annotations`, `notes`, `rows`, `cells` -- models.py `_omit_absent` calls), while the C# DTOs are non-nullable `List<T>` initialized to empty and therefore always write `"sheets": []`, `"views": []` and so on. Both sides load either spelling, so nothing breaks now, but the two serializers produce different bytes for identical evidence -- which matters the moment a standards golden built by the Python T008 builder is compared against, or reconciled with, a real dump (T098's matched pair, the quickstart bring-up).
  - Fix: Pick one rule and state it. Either drop the `empties=` arguments from the new records' Python serializers so both sides write `[]`, or extend `OmitEmptyAdditiveArrays` (or a converter) to null the nested lists on the C# side too. Whichever way, add a cross-language assertion so the choice is pinned rather than incidental.
- **reviewer/src/swreview/ir/models.py:115** (ir) - `omit_additive`'s `empties` branch drops a key on any falsy value (`if not data.get(name)`), not on an empty list. The docstring says `empties` names lists, but nothing enforces it: the day someone passes a scalar name -- an int count, a bool, a string -- a legitimate `0`, `False` or `""` is silently omitted and read back as the default. That is precisely the `""`-versus-null distinction `RevisionTableRow.cells` and `RevisionTable.current_revision_raw` exist to protect, implemented one argument away from breaking it.
  - Fix: Test the type explicitly: `value = data.get(name); if isinstance(value, list) and not value: data.pop(name, None)` (or `if value == []`). Add a unit test that an `empties` name holding `0`/`False`/`""` is still written.
- **extractor/SwReview.Extractor.Tests/PackageReuseTests.cs:1** (ir) - The file gained a UTF-8 BOM (`efbbbf`) that HEAD did not have; 78 of the 86 .cs files under SwReview.Extractor have no BOM, so no-BOM is the house convention and this is an unrequested byte change to a file the task only needed one substantive edit in. It pollutes the diff and will show up as a whole-file change in some tools.
  - Fix: Rewrite the file without the BOM, leaving only the phase-list edit at line 119.
- **extractor/SwReview.Extractor.Tests/PackageReuseTests.cs:119** (ir) - T017's acceptance says "the phase-list edits are the ones named here rather than discovered (RK-8)", and contracts/ir-additions.md section 6 names exactly three: `test_ir_phases.py`, the C# `IrSerializerTests` mirror, and `Ir/Manifest.cs`'s XML doc (T016 adds `PackageWriterTests.cs` and `CommandLineOptionsTests.cs`). `PackageReuseTests.cs` also pins the full phase list and had to be edited; it is named nowhere. The edit itself is correct and unavoidable -- the omission is that it was discovered rather than declared, which is the exact failure RK-8 is guarding against.
  - Fix: Record it as a deviation in the handback and, through the owning task, add `PackageReuseTests.cs` to section 6's list of edits the two new phases force, so the next phase addition finds it named.
- **extractor/SwReview.Extractor/Dump/SwDump.cs:46** (ir) - T017 names its edit sites exhaustively -- `Dump/DumpContracts.cs`, `Dump/PackageWriter.PhaseOrder` and `Build`, the gap block, `Console/Program.cs`, `Ir/Manifest.cs`. `SwDump.cs` is not among them, and it was edited to pass `cutList: null, drawings: null` into the widened constructor. The change is forced (the two parameters are positional and required) and correct for this stage, but it is a product-behaviour decision -- every real SOLIDWORKS `standards` dump now records `cutlist` as `skipped`, which is exactly the row `swreview check standards` refuses a package on (FR-043) -- made in a file the task did not scope.
  - Fix: Leave the wiring as it is, but report it as a named deviation and make sure the task that adds `CutListDumper`/`DrawingDumper` (T032-T040) knows this is the line it must replace, otherwise the standards tab ships refusing every package the pilot extractor produces.
- **reviewer/tests/support/features.py:78** (ir) - T013 says "edit **exactly** the Python version pins plan.md's Source Code block lists ... plus the Python side of the cross-language `ReuseKey` canonical form -- and nothing else", and `tests/support/features.py` is not on that list. It gained `SketchSpec.text_segments`, a `sketch_feature(text_segments=...)` parameter and the `text_segment_count=` wiring. The change is additive and default-None so no golden moves, and its only consumer is `tests/unit/test_support_standards.py:284` (T007/T008, a different agent working concurrently), so it may well belong to that task rather than this one.
  - Fix: Confirm ownership with the T007/T008 agent. If this task made the edit, report it as a deviation against T013's "and nothing else"; if T008 made it, nothing to change -- but say so in the handback, because T018's acceptance asks whether "the bump edited exactly the pins T013 and T015 name" and the answer is currently unattributed.
- **extractor/SwReview.Extractor/Ir/DrawingRecord.cs:35** (ir) - `DrawingRecord.Source` and `DrawingSheetRecord.Source` (line 70) are typed `DrawingEvidenceSource`, a two-valued enum, where both the contract (ir-additions.md 3.1 and 3.2, "Constant for this model") and the regenerated schema (`"const": "native"`) pin a single value. A C# caller can set `PdfIngest` on either record and produce a package that serializes cleanly but fails the contract -- the C# DTO is looser than the schema it is meant to mirror, whereas the Python `Literal["native"]` is exact.
  - Fix: Either drop the setter (get-only property returning `DrawingEvidenceSource.Native`, with a `[JsonInclude]`/private setter for the round-trip) or add a test that deserializing a record whose `source` is `pdf_ingest` is refused, so the C# side enforces the same constant the schema does.
- **specs/006-standards-check/contracts/ir-additions.md:116** (ir) - Contract/implementation divergence in the array name. Section 3's heading says the eight drawing models live in `EvidencePackage.drawings[]` "omitted when empty", and T015 repeats it ("gains `CutListItems` and `Drawings` as `List<T>?` nulled when empty"). The implementation correctly rejected that - `drawings` has held the PDF ingest's `DrawingSheet` since feature 001 and five goldens carry rows in it, so making it nullable-when-empty would move every package on disk - and put the native records in a new `drawing_records[]` on both sides (reviewer/src/swreview/ir/models.py:1329, extractor/SwReview.Extractor/Ir/EvidencePackage.cs:104). The decision is right and is documented in three docstrings and the test module header, but the normative contract still names the wrong member, so the next reader of ir-additions.md will implement `drawings[]`.
  - Fix: Ask the spec owner to amend section 3's heading (and section 3.1's prose) to `EvidencePackage.drawing_records[]`, in the task that is allowed to edit that contract. No code change.
- **reviewer/src/swreview/ir/models.py:1099** (ir) - The two serializers disagree on nested empty arrays inside the nine new models. Python omits them (`DrawingSheetRecord` drops `views`/`revision_tables`, `DrawingView` drops `display_dimensions`/`annotations`/`notes`, `DrawingRecord` drops `sheets`, `RevisionTable` drops `rows`, `RevisionTableRow` drops `cells`), while the C# DTOs always write `"views": []` etc. - only the two top-level arrays are nulled (EvidencePackage.cs:152). Both shapes validate and both deserialize, so nothing breaks today, but a C#-dumped package re-serialized by Python is not byte-identical, which is exactly the property the rest of this bump is built to preserve, and any future byte comparison across the two languages will fail for a reason nobody stated. The additivity rule's point 3 only demands this for fields added to models that already existed, so the Python omission is an extra rule that the C# side does not keep.
  - Fix: Pick one and state it: either drop the `empties=` argument from the seven new-record serializers (the models are wholly new, so an empty array in them moves no golden), or add the same emptiness resolution to the C# records and a test asserting the two languages write the same shape for an empty sheet.
- **extractor/SwReview.Extractor/Ir/PackageSerializer.cs:69** (ir) - `Serialize` mutates the package it was handed: `OmitEmptyAdditiveArrays()` rewrites `CutListItems`/`DrawingRecords` from an empty list to null on the caller's object. The method is documented as a pure conversion ("Serializes a package as indented JSON"), and a caller that serializes a package and then appends to `package.CutListItems` gets a NullReferenceException that points at the append, not at the serializer. No caller does that today (PackageWriter only assigns when Count > 0), so this is latent rather than live.
  - Fix: Resolve the emptiness where the package is built rather than on the way out, or have `Serialize` compute the omission without writing back (a converter, or a local copy of the two members restored in a finally).
- **extractor/SwReview.Extractor/Dump/PackageWriter.cs:367** (ir) - The conditional drawing gap blames the profile in the two cases where the profile is not the reason. The message reads "the drawing phase did not run under the 'full' profile" for a part or assembly root (the root kind is the reason) and for any root in this build (`SwDump` passes `drawings: null`, so no source is wired). A pilot reading it is told to re-run with a different profile, which changes nothing. The contract's wording ("naming the profile that skipped it") is what was implemented, so this is a contract wording problem as much as a code one, and the new test only asserts `Contains(spelling)`.
  - Fix: Name the actual reason alongside the profile - root document kind, or no drawing source wired - e.g. "the drawing phase did not run: the root document is not a drawing (profile 'full')". Extend Build_DrawingPhaseThatDidNotRun_IsAGapNamingTheProfileThatSkippedIt with a drawing-root-but-no-source case.
- **reviewer/tests/unit/test_ir_standards.py:915** (ir) - `test_every_new_gap_entity_kind_validates` is tautological. `Gap.entity_kind` is a free `str` (models.py:1181) and the generated contract types it `"type": "string"`, so all twenty-one parametrized cases pass against HEAD as well - the test would be green with none of T013 implemented, and it would stay green if a kind were misspelled. Its neighbour `test_the_twenty_one_new_kinds_are_distinct` asserts only that the test module's own constant has no duplicates.
  - Fix: Either close the vocabulary (make the twenty-one kinds a checked set somewhere one side owns) or, cheaper, assert the constant against the kinds parsed out of `contracts/ir-additions.md` section 5, so a kind renamed in the contract and not in the code fails here.
- **reviewer/tests/unit/test_ir_standards.py:885** (ir) - Nothing guards the eleven hand-written omit lists. Each new model repeats its own `nulls=(...)`/`empties=(...)` tuple in `_omit_absent`; all eleven are complete today (I checked each against its field set), but the only Python assertions are `test_the_new_records_omit_their_own_unknowns` (CutListItem alone) and `test_an_empty_list_on_a_new_record_is_left_out` (DrawingSheetRecord alone). A field added later to `DrawingView` or `RevisionTable` and left out of the tuple would serialize its null with no test failing, on the one rule this whole bump exists to keep.
  - Fix: Add one parametrized test over the nine new models: build each with every optional field unset and assert the serialized dict contains no key whose value is null and no key whose value is an empty list. That is the generic form of the C# `StandardsPackage_OmitsEveryUnreadAdditionRatherThanWritingItsNull` list, which has the same hand-maintenance problem.
- **extractor/SwReview.Extractor.Tests/PackageReuseTests.cs:119** (ir) - T018's last clause - "the bump edited exactly the pins T013 and T015 name" - is neither true nor measured. `PackageReuseTests.cs` was edited too (its pinned phase list, forced by T017's two new phases and anticipated by RK-8 as the risk of a discovered edit rather than a named one), and no test asserts the edit set. The same file also gained a UTF-8 BOM on line 1 (`-using System;` / `+﻿using System;`), an unintended byte change unrelated to the phase list.
  - Fix: Strip the BOM from PackageReuseTests.cs, and either add PackageReuseTests.cs to the named pin list in the task/plan record or state it as a deviation. If T018's clause is meant to be measured, it needs an actual assertion (a scripted diff check) rather than inspection.
- **extractor/SwReview.Extractor/Ir/DrawingRecord.cs:35** (ir) - `DrawingRecord.Source` and `DrawingSheetRecord.Source` are the full `DrawingEvidenceSource` enum, while the contract and the regenerated schema pin them to `const: "native"` (Python `Literal["native"]`). A C# caller can set `DrawingEvidenceSource.PdfIngest` on a native record; the DTO accepts it, and the failure surfaces only as a schema-validation error, or as the whole package failing to load on the Python side. No test pins either member to `native`.
  - Fix: Either introduce a one-value type for these two members, or add an assertion that a serialized `DrawingRecord`/`DrawingSheetRecord` always writes `"source": "native"` and that a package carrying `"pdf_ingest"` on either is rejected by the contract.
- **extractor/SwReview.Extractor.Tests/PackageWriterTests.cs:555** (ir) - T018's gap-set clause is only half measured. The requirement is that a `model_check` dump produces the same gap set as before the bump (same kinds, entity kinds, entity ids and count, the drawing message excepted); the edited test compares a `model_check` build against a `full` build in the *same* build tree, which is an internal-consistency check, not a regression against pre-1.4.0 behaviour. The pre-1.4.0 byte baseline (`Fixtures/package-pre-1.4.0.json`) is `BuildSamplePackage`, a hand-built package, so it carries no PackageWriter gap set to compare against. Related and smaller: `Build_StandardsProfile_*` only exercises an assembly root, though T016 names "a part or assembly".
  - Fix: Capture the `model_check` and `full` gap sets (kind|entity_kind|entity_id) from HEAD once into a small fixture and assert against it, the way the byte baseline works; add a part-root case to the standards-profile cut-list test.
- **reviewer/tests/unit/test_standards_registry.py:290** (family) - T021 requires the test to assert 'every rule carries a statement and a function'. TestEvaluators only proves that every id binds on a throwaway copy of the catalogue; nothing asserts that the real RULES entries end up with an fn. Feature 003's equivalent does exactly that (test_rms_registry.py:194 `assert callable(rule.fn)` and :268 the fn/severity/coverage invariant on the real catalogue), and no later task in tasks.md adds the assertion for this family, so the sentence would never be measured.
  - Fix: Add a test that imports `swreview.checks.standards` and asserts `callable(rule.fn)` and `(rule.fn is not None) == (rule.severity is not None)` for all sixteen, to be switched on by the task that lands the last evaluator module (T046); flag it to that owner now so it is not lost.
- **reviewer/src/swreview/checks/standards/registry.py:237** (family) - The four-scope vocabulary is written out three times in the same file: the RuleScope Literal (line 65), `scopes=frozenset({...})` (line 237) and `dict.fromkeys(('assembly','part','drawing','document'), CHECK_TOOL)` (line 238). A scope added in one place and not the others is only caught because the test happens to cross-check two of them. DRY.
  - Fix: Derive the other two from the Literal: `scopes=frozenset(get_args(RuleScope))` and `tools=dict.fromkeys(get_args(RuleScope), CHECK_TOOL)`. `get_args` is already imported in the test; import it here.
- **reviewer/src/swreview/checks/standards/verdict.py:56** (family) - Two problems in one declaration. (1) `CoverageBucketName` restates four of `swreview.report.session.CoverageBucket`'s five values with no guard, while its sibling `RuleCoverageBucket` in checks/rules/registry.py:53 carries an explicit `assert set(get_args(...)) <= set(get_args(CoverageBucket))` for exactly this reason - the established pattern is not followed here. (2) The docstring claims `failed` and `warned` are 'never session buckets', but report/session.py:149 defines the session's five as checked, skipped, unresolved, failed, out_of_scope - `failed` is a session bucket that this family derives rather than reads (contracts/standards-check.md), which is a different statement.
  - Fix: Add the same subset assertion against `CoverageBucket`, and reword the docstring to 'derived from findings by checks/standards/report.py rather than read from session.coverage (which has no warned bucket)'. While there, `buckets[pair.bucket]` at line 185 raises a bare KeyError for an off-vocabulary bucket; raise a named ValueError instead.
- **reviewer/src/swreview/checks/standards/verdict.py:61** (family) - `FindingSeverity = Literal['error', 'warning']` restates this family's severity vocabulary, which is already stated by `RuleSeverity` and `STANDARDS_FAMILY.severities` in checks/standards/registry.py. Three statements of the same closed set, and this one is not exported in __all__ even though it types a public dataclass field.
  - Fix: Import `RuleSeverity` from checks/standards/registry and use it for `FindingOutcome.severity` (a type-only import keeps verdict.py free of behaviour), or export the alias and assert it equals STANDARDS_FAMILY.severities in the test.
- **reviewer/src/swreview/checks/standards/verdict.py:146** (family) - `graded_kinds: Collection[str | None] = ()` defaults to empty, and `_notes` suppresses the 'no drawing graded' note when it is empty. A caller that forgets the argument silently loses one of FR-032's three notes, and the failure is invisible - a clean headline with nothing said about the missing drawing is exactly the outcome the note exists to prevent. T069a asserts the note's absence over a drawing-carrying fixture, so the reverse hole is real.
  - Fix: Make `graded_kinds` a required keyword argument (and `empty_setting_checks` too, if T047 always has it), so a caller cannot omit it; keep the current behaviour for a genuinely empty graded set.
- **reviewer/src/swreview/checks/standards/traversal.py:129** (family) - `CheckedDocument.unresolved_reason` is not in data-model.md:260's field list for CheckedDocument (document_id, kind, path, file_name, configuration, instances, reached_by, matches_part_number, unresolved_instances). The field is a reasonable addition - the report layer needs the reason for its unresolved coverage row - but the design artifact and the code now disagree, and no task in this stage authorised editing data-model.md.
  - Fix: Raise it with the feature owner: either have the report layer compose the reason from `kind is None` / `path is None` and drop the field, or get data-model.md's row amended in the task that is allowed to touch it. Do not edit data-model.md from T024.
- **reviewer/src/swreview/checks/standards/traversal.py:301** (family) - `matches_part_number` is a plain bool, so it cannot distinguish 'the profile's pattern is empty' from 'the file name does not follow the convention'. contracts/rules.md requires two different skipped reasons for standards.document.data_card_complete ('the file name does not match, naming the convention' versus 'the pattern is empty, naming the empty setting'), and the empty-setting case must also feed the verdict's empty-setting note. The report layer therefore has to re-read profile.part_number.pattern to tell them apart - a coupling that is easy to get wrong silently.
  - Fix: Either document on the field that the empty-pattern case is the report layer's to detect from the profile (and pin that in T047's test), or carry the distinction here, e.g. `matches_part_number: bool | None` with None meaning 'no convention configured'. Prefer the first; it keeps the traversal a pure set-builder.
- **reviewer/src/swreview/checks/standards/verdict.py:195** (family) - A waiver on a warning-severity finding is silently honoured. `FindingOutcome(check='standards.drawing.revision_matches', severity='warning', waived=True)` yields `counts.warning == 0`, `waived == 1` and the note '1 finding waived by an accepted exception' - the warning disappears from the headline. `contracts/rules.md` ('Waivable checks') says a waiver naming either of the two warning checks is reported `invalid (warning check)` and **changes nothing**, and `STANDARDS_FAMILY.waiver_labels` already carries that label. The verdict is the last place that invariant can hold, and it does not. No test covers the combination.
  - Fix: Refuse it explicitly rather than accommodate it: add a `__post_init__` to `FindingOutcome` raising `ValueError` when `severity == 'warning' and waived`, naming contracts/rules.md 'Waivable checks'. Add a test asserting the refusal, and one asserting a warning finding still counts in `counts.warning` whatever the caller passes.
- **reviewer/src/swreview/checks/standards/verdict.py:227** (family) - `graded_kinds` defaults to `()` and `_notes` guards on `if graded_kinds and ...`, so a caller that simply omits the argument gets no 'no drawing graded' note. `release_verdict(findings=[], coverage=[])` returns `notes == ()`. That is exactly the silent clean headline the module's own docstring says `notes` exists to prevent, and it is the one note whose absence is indistinguishable from its not applying. Untested - every note test passes `graded_kinds` explicitly, so nothing fails if a future caller forgets it.
  - Fix: Make `graded_kinds` a required keyword argument (explicit over clever - a run always knows what it graded), or treat an empty collection as 'no drawing graded' and say so in the docstring. Either way add the test: a verdict built without graded kinds carries the note.
- **reviewer/src/swreview/checks/standards/verdict.py:185** (family) - `_counted` indexes `buckets[pair.bucket]` over four keys while `swreview.report.session.CoverageBucket` has five ('failed' is the fifth). A `CoveragePair(bucket='failed')` raises a bare `KeyError: 'failed'` with no message saying why the verdict does not count that bucket. The `CoverageBucketName` Literal narrows it for a type checker, but `contracts/standards-check.md` says `failed` and `warned` are derived by the report layer from findings, so the report layer is precisely the caller most likely to hand one over by mistake. No test covers it.
  - Fix: Raise a named error - e.g. `ValueError(f"{pair.bucket} is a rendered bucket derived from findings, not a counted (check, document) pair")` - and add a test asserting it, so the failure names the rule instead of a dict key.
- **reviewer/src/swreview/checks/standards/traversal.py:259** (family) - `_walk` runs its breadth-first queue on a list with `queue.pop(0)`, which is O(n) per pop and makes the whole traversal O(n^2) in the number of component instances. The graded set is computed once per run over the entire component forest, and a release-checklist run over a real top-level assembly is thousands of instances - the exact input this feature exists for. Nothing in the test fixtures is large enough to notice.
  - Fix: `from collections import deque`, build `queue = deque(...)` and use `queue.popleft()`. One-line change, no behaviour difference, and it removes the only super-linear path in the graded-set computation.
- **reviewer/tests/unit/test_standards_registry.py:299** (family) - T021 asks that 'every rule carries a statement and a function', but `test_binding_every_id_completes_the_catalogue` proves it over a `replace()`d copy of the catalogue, so nothing asserts the real `RULES` is ever fully bound - today all sixteen have `fn is None`. `registry.py`'s own docstring says 'importing `swreview.checks.standards`, which imports all four of them, is what completes the `fn` half of the invariant', yet `checks/standards/__init__.py` imports no evaluator module at all. The copy-based test is the right call while T042/T044/T046 are unwritten, but as it stands an evaluator module that forgets to bind one of the sixteen fails nothing here - it fails later, at dispatch, in whichever check happens to run first.
  - Fix: Leave the copy test, and add the real assertion in the same file guarded on the evaluators existing - `import swreview.checks.standards` then `assert [r.id for r in RULES.values() if r.fn is None] == []` - marked xfail/skip until T042, T044 and T046 land, so the gate exists and is visibly owed rather than forgotten. Flag to whoever takes T042/T044/T046 that `checks/standards/__init__.py` must import all four evaluator modules for the docstring's claim to be true.
- **reviewer/src/swreview/checks/standards/traversal.py:109** (family) - `CheckedDocument` carries `file_name` (the recorded `Document.file_name`) beside `matches_part_number` (decided against the name in `path`). The docstring explains the difference, and `test_the_name_matched_is_the_one_in_the_path_not_the_recorded_file_name` pins it here - but the field left on the record is the one the contract says must never be matched against (difference l), and T045/T046 are about to read this record to report the data-card check's observed text. The trap is that `document.file_name` reads like the right thing to use.
  - Fix: Carry the graded name explicitly - add `path_file_name: str | None` set from `_name_in(path)` (the value `matches_part_number` was decided from) - or rename the recorded one to `recorded_file_name`. Either makes the wrong choice unavailable rather than merely documented, which is the 'explicit over clever' reading.
- **extractor/SwReview.Extractor.Tests/GuardTests.cs:321** (extractions) - T019 says the expected set is derived "from research.md R8's table membership rather than a hand-typed count", and R8 itself says "the guard test derives its expected set from this table's membership ... so a future row cannot silently leave a member ungated". The test derives from a hand-transcribed C# copy of the table, and its own doc admits the weaker guarantee: "a row added to R8 **and copied here** cannot leave a member ungated". A row added to research.md and not copied is invisible - exactly the failure R8 legislates against. The table now exists in four places (research.md R8, ReadOnlyGuard.cs, guard-allowlist.md, GuardTests.cs) plus RemodelGuardTests.ExpectedDeniedMembers, which is aggressive repetition by the project's own DRY rule. (I verified all four are currently in exact agreement: 31 distinct bare names, SetText once; the denied set is 46 = 15 pre-existing + 31, and RemodelGuardTests' ExpectedDeniedMembers matches it exactly.)
  - Fix: Parse R8's table out of specs/006-standards-check/research.md at test time (the repo root is reachable from the test's build output the same way ReviewPageFiles.WebFolder is), failing loudly if the table's shape changes - the same "parser fails loudly rather than silently matching nothing" rule T001 imposes on the Python R5 parser. If reaching the spec tree from this test project is judged too fragile, say so as an explicit deviation rather than letting the doc comment quietly restate the guarantee more weakly.
- **extractor/SwReview.AddIn.Tests/CheckPaneHostTests.cs:667** (extractions) - T027 says to write the tests "over the fakes ModelCheckHostTests already uses". `CollectingChannel` (667) and `FakeOpener` (676) are re-declared as private nested copies; the assembly now carries four CollectingChannel copies (CheckPaneHostTests, ModelCheckHostTests:586, RemodelHostTests:1618, and ReviewHostTests' opener) and three FakeOpener copies. They are private nested classes so they genuinely cannot be reused as-is - but that is an argument for extracting them, not for a fourth copy, and the project's DRY rule says flag repetition aggressively.
  - Fix: Extract the two fakes into one internal test-support file (e.g. PaneFakes.cs) that CheckPaneHostTests uses, leaving ModelCheckHostTests' private copies untouched so it stays unedited; later hosts then have one copy to reuse. Or record the copy as a deliberate deviation with the "ModelCheckHostTests must stay unedited" reason.
- **extractor/SwReview.AddIn/Model/ModelCheckHost.cs:110** (extractions) - The rebuild is not a pure move in one respect: the old code passed the option delegates to PaneActions by value (`LogFolder = options.LogFolder`), so a caller replacing `options.LogFolder` after construction got the stale delegate; every accessor is now wrapped (`LogFolder = () => options.LogFolder()`) and read fresh. The comment explains the intent, but T028 authorises a rebuild on the collaborator, not a semantic change to when option delegates are read. No current test appears to depend on the old capture-at-construction behaviour, so this is a latent rather than an actual break.
  - Fix: Either keep the wrapping and state it explicitly as a deviation in the task report (it is needed because CheckPaneHostOptions is a second options object), or pass the delegates through unwrapped so the semantics are identical to what ModelCheckHostTests was written against.
- **extractor/SwReview.Extractor/Guard/ReadOnlyGuard.cs:126** (extractions) - `"SetSystemValue3"` is already refused by the `SetSystemValue` entry in DeniedPrefixArray (line 147), so the member entry is dead weight in the lookup. The comment at lines 121-125 acknowledges and justifies it (R8's table is the membership, and a reader cross-checking should not need to know which rule catches it), which is a defensible explicit-over-clever call - but it does mean the denied-member set's cardinality is no longer the number of members only this list refuses, and RemodelGuardTests' set equality now pins a redundant entry.
  - Fix: No change required if the owner agrees with the stated rationale; otherwise drop the member entry and add a one-line test asserting SetSystemValue3 is refused by the prefix rule, so the prefix is still proved to cover it.
- **specs/004-resilient-remodeler/contracts/guard-allowlist.md:38** (extractions) - The new 'Denials added after stage 1' table is a third hand copy of the same thirty-one names (research.md R8, ReadOnlyGuard.cs, RemodelGuardTests.ExpectedDeniedMembers, and this file). ReadOnlyGuard.cs's new comment claims it is 'restated in 004-resilient-remodeler/contracts/guard-allowlist.md and asserted from both ends by StandardsDenylistTests and RemodelGuardTests', but no test reads this markdown, so the claim is false and the table can drift silently. (I verified all four agree today: 31 distinct names, identical sets.)
  - Fix: Either add an assertion that parses this table and compares it to ReadOnlyGuard.DeniedMembers (the repo already parses contract markdown in ModelCheckContract.Load and RemodelPageFiles' status-stage list), or reword the ReadOnlyGuard comment so it does not claim an assertion that does not exist.
- **extractor/SwReview.Extractor.Tests/GuardTests.cs:404** (extractions) - TheExpectedSetIsTheTablesDistinctMembershipWithSetTextCountedOnce opens with Assert.Equal(Table.SelectMany(r => r.Members).Distinct(OrdinalIgnoreCase).Count(), ExpectedMembers.Count), but ExpectedMembers is defined two members above as new HashSet<string>(Table.SelectMany(row => row.Members), StringComparer.OrdinalIgnoreCase). Both sides are the same computation over the same source, so that assertion cannot fail for any Table. The other two assertions in the test (SetText on two rows, one entry in the set) are real.
  - Fix: Drop the first assertion, or make it non-tautological by asserting the count against the guard's own surface - e.g. that ReadOnlyGuard.DeniedMembers contains every ExpectedMembers entry and that its size grew by exactly ExpectedMembers.Count over feature 003's sixteen.
- **extractor/SwReview.AddIn.Tests/CheckPaneHostTests.cs:234** (extractions) - TheFourSharedRowsAreAnsweredByPaneActions exercises only three of the four PaneActions rows: folder.open, log.open and entity.show. report.open (PaneActions.TryHandle case at Review/PaneActions.cs:160) is never sent anywhere in the file, so nothing proves a check pane's report.open reaches PaneActions at all - and report.open is the row that resolves a path from the host's own check record, which is the one thing CheckPaneHost's PaneRunLookup wires up.
  - Fix: Add a report.open with the tracked run_id to the same test (asserting the opened path) and rename nothing; that also covers the PaneRunLookup closure for both rows that use it.
- **extractor/SwReview.AddIn.Tests/CheckPaneHostTests.cs:15** (extractions) - T027 names SendInit and Dispatch as members the collaborator carries. Dispatch is private and is adequately exercised through Receive (ready, the owner's start type, an unknown type), but SendInit - which is public on CheckPaneHost precisely so an owner can re-send init when the backend comes up - is never called directly by any test; it is only reached via a 'ready' message. Nothing pins its behaviour when the owner drives it (e.g. SendInit(null) posting an unsolicited init).
  - Fix: Add one test that calls world.Host.SendInit(null) and asserts the posted init carries a null id and the same payload shape, so the member T027 names is covered as a member rather than only as a side effect of 'ready'.
- **extractor/SwReview.AddIn.Tests/RemodelPageFiles.cs:64** (extractions) - Two problems in one change. (a) Scope: RemodelPageFiles.cs is feature 004's test helper and is named nowhere in T029/T030 or in plan.md's Source Code block; it was edited to filter the shared folder by what index.html loads. The edit is in practice unavoidable (the Remodel page's contract test would otherwise scan check-page.js), but it is an undeclared file and should be reported as a deviation. (b) Asymmetry: ModelCheckPageFiles.Scripts() (ModelCheckPageFiles.cs:52) was NOT given the same filter and still sweeps the whole shared folder recursively, so the first shared file the Standards tab adds that the Model page does not load will fail ModelCheckPageTests' contract rules for a page that never loads it.
  - Fix: Report the RemodelPageFiles edit as a deviation against the task's file list, and apply the same index.html-driven filter to ModelCheckPageFiles.Scripts() (it is a no-op today - the Model page loads both shared scripts - and it removes the trap for T079's Standards page). Consider lifting IsUnder/Loads into one shared helper rather than a second copy.
- **extractor/SwReview.AddIn/web/shared/check-page.js:914** (extractions) - The header slot is looked up as byId(page.headerId) rather than with a literal, and ModelCheckPageTests' ElementLookup regex (ModelCheckPageTests.cs:687, matching getElementById|byId followed by a quoted literal) therefore no longer sees 'grade'. EveryElementIdTheScriptLooksUpExistsInTheHtml silently stopped checking that the Model check page's header slot exists in index.html; only the offscreen render test would catch a rename, and it would fail with a null-property symptom rather than a named id.
  - Fix: Have SharedCheckPageTests assert that each page's headerId string literal (check.js's headerId: 'grade') is present as an id in its own index.html - one line, and it restores the rule for every page built on the factory.
- **extractor/SwReview.AddIn/Model/ModelCheckHost.cs:113** (extractions) - The extraction quietly changed how options are read: the old host passed options.LogFolder / EntityResolver / Opener / Secrets straight into PaneActionsOptions (delegate captured at construction), while the new host wraps each as () => options.X(). The comment above it claims 'a caller that replaces one after construction still has it read fresh - which is what "a settings save moves the run root" and "the backend started after the pane opened" both depend on'. No test replaces a ModelCheckHostOptions property after construction, so the claim is untested; CheckPaneHostTests only proves the Func indirection inside CheckPaneHost (the world's own fields change, not the option delegates).
  - Fix: Either add one test that swaps ModelCheckHostOptions.Opener (or LogFolder) after the host is constructed and shows the new one is used, or soften the comment to describe only what is tested.
- **extractor/SwReview.AddIn.Tests/CheckPaneHostTests.cs:667** (extractions) - T027 says to write the tests 'over the fakes ModelCheckHostTests already uses'; the file instead defines its own private CollectingChannel and FakeOpener, a third and fourth copy of fakes that already exist in ModelCheckHostTests.cs:586 and PaneActionsTests.cs:667/696. Low priority: the AddIn test assembly already has eight private IPageChannel fakes, so this matches existing style rather than introducing the pattern, and CLAUDE.md section 3 says to match existing style.
  - Fix: Leave as is unless the team wants the fakes hoisted; if hoisted, do it as its own task across the whole assembly rather than for these two files only. Worth recording as a small deviation from T027's wording.

## Feature 006 round B (2026-09-17)

- **extractor/SwReview.Extractor/Dump/FeatureDumper.cs:529** (extractor) - `count = segments is object[] array ? array.Length : 0;` fabricates a definite `0` for any answer that is an array of some other element type (a typed `ISketchText[]`, a SafeArray marshalled as something other than `object[]`). `contracts/ir-additions.md` section 1 and T037 authorise `0` only 'for an empty or null array'; an answer the dumper cannot count is not empty, and recording it as 0 rules the sketch-text exemption OUT, which can raise a false finding on `standards.part.sketches_fully_defined`. Note the sibling read handles exactly this shape ambiguity: `ComponentTreeDumper.Slot` (line ~490) accepts both `double[]` and `object[]`, and writes a gap when the answer carries no readable slot.
  - Fix: Count any array and gap anything else: `count = segments switch { null => 0, Array a => a.Length, _ => null };` and, on the `_` branch, `gaps.Add(GapKind.NotExtracted, "sketch_text", id, $"GetSketchTextSegments answered for '{name}' with something that is not an array, so its text segment count is unknown.", null)`. Add the corresponding case to `FeatureDumperTests`.
- **extractor/SwReview.Extractor/Dump/CutListDumper.cs:190** (extractor) - T039 states 'a folder whose `GetTypeName2` or `GetSpecificFeature2` could not be read yields a `cut_list_folder` gap and the folder is **still recorded**'. That holds at the item level (ReadFolder records the child when its `BodyFolder` read threw, line 219) and for `GetTypeName2` (ReadText falls back to empty string, line 203), but at the top level `if (!answered || folder == null) continue;` records nothing when `GetSpecificFeature2` threw - the gap is written, but no row survives. `Dump_TopLevelSpecificFeatureThatFailed_IsACutListFolderGap` (CutListDumperTests.cs:222) pins the deviation with a justifying comment rather than the task sentence.
  - Fix: Defensible as written - a top-level feature that could not be identified is not known to be a folder, and inventing a CutListItem for it would fabricate evidence - but the deviation should be raised with the task owner rather than settled in a test comment. Either record a placeholder row (empty folder name, null body count, null exclusion, which Python then reports unresolved), or get the sentence in tasks.md read as applying only to the item level and say so in the XML doc.
- **extractor/SwReview.Extractor/Dump/PropertyDumper.cs:106** (extractor) - `MaterialConfiguration(kind, activeConfiguration)` keys off `SwSession.KindOf`, which has a `default: return DocumentKind.Part` branch (SwSession.cs:99-100) for any `GetType()` value that is not assembly or drawing, while `ReadMaterial` (line ~296) keys off `model is IPartDoc`. The two predicates can disagree, so the contract sentence 'Present whenever the material read was attempted, including when `material` came back null' is not structurally guaranteed - it is guaranteed only because the two predicates happen to agree on well-behaved documents. `PropertyDumperTests` covers Part, Assembly and Drawing but has no case pinning the tie between the material read and the recorded configuration.
  - Fix: Derive both from one predicate: have `ReadMaterial` return the configuration it used (or have `Read` compute `bool isPart = model is IPartDoc` once and pass it to both), so the field is written if and only if the read was attempted. Add a test that the configuration is recorded exactly when `GetMaterialPropertyName2` was gated.
- **extractor/SwReview.Extractor.Tests/PropertyDumperTests.cs:302** (extractor) - Nothing pins the wiring of the four new `Document` fields in `PropertyDumper.Read`. Every test exercises the static helpers in isolation, and `TheFourFields_AreOmittedWhenNull` only asserts the DTO's defaults on `new Document()`. Transposing `IsExploded` and `RebuildErrorCount` at PropertyDumper.cs:107-110, or dropping the `MaterialConfiguration` assignment entirely, would leave the whole file green. Contrast T034, whose acceptance explicitly required the four ComponentInstance fields to be 'pinned end to end through PackageWriterTests' fake IComponentTreeSource' - and `Build_CarriesTheFourSchema140ComponentReadsThroughToThePackage` does exactly that.
  - Fix: Add one end-to-end assertion the way T034 did. `PackageWriterTests` already builds documents through `FakeSources`; set the four fields on a fake document row and assert they survive into `package.Documents[n]`, or add a seam for `PropertyDumper.Read` equivalent to the `IFeatureReader` split so the four assignments can be exercised together.
- **extractor/SwReview.Extractor/Dump/PropertyDumper.cs:134** (extractor) - `ReadMassOverridden` runs for every document kind, so a drawing (and any document where `CreateMassProperty2` legitimately answers nothing) now carries a second `mass_override` gap on top of the existing `document` gap 'CreateMassProperty2 returned nothing; mass and volume are unknown.' at line ~326. That is a new unresolved-coverage row on every drawing in every `full` and `model_check` dump for a question that does not apply to a drawing - the same reasoning that made `is_exploded` silent for a part or drawing (ReadIsExploded, line 156-163) and that the contract states for `material_configuration`.
  - Fix: Gate the read the way `ReadIsExploded` is gated - `if (kind == DocumentKind.Drawing) return null;` with no gap - or, if the task owner wants it unconditional, at least suppress the duplicate row when `ReadMass` is about to write its own gap for the same null object.
- **extractor/SwReview.Extractor/Dump/SwCutListReader.cs:58** (extractor) - DRY. `SwCutListReader.Document` (lines 47-54), `ActiveConfiguration` (58-70) and the `FirstFeature`/`GetNextFeature` walk (72-85) are near-verbatim copies of `SwFeatureReader.Document` (48-57), `ActiveConfiguration` (58-73) and `Walk` (75-89) - the only differences are the tail of one error message and the added count cap. `SubFeatures` (87-100) likewise duplicates the `GetFirstSubFeature`/`GetNextSubFeature` loop at SwFeatureReader.cs:199-205, and `CutListDumper.Dump`'s per-document loop (lines 111-142) duplicates `FeatureDumper.Dump`'s (149-196) including the `walked` HashSet, the Part filter and the suppression filter. The binding rule is 'DRY ... never copy'; two readers now drift independently on the same interop members.
  - Fix: Extract the shared pieces - a `SwDocumentReader` (or static helpers in `SwReview.Extractor.Sw`) holding `Document(ScopedComponent)`, `ActiveConfiguration(object)` and the two gated feature walks, and a shared 'once per resolved part document' enumerator both dumpers drive. Keep the per-family gap kinds as a parameter. This is a refactor of code with no seat dependency, so both existing test suites pin it.
- **extractor/SwReview.Extractor/Dump/CutListDumper.cs:261** (extractor) - The comment above the persist-ref read says 'A read that THREW is a gap - that is the dump failing, not SOLIDWORKS answering', but `SwCutListReader.PersistRef` calls `PersistRefService.TryGet`, which swallows `PersistRefError` into null (PersistRefService.cs:129-136). A failure inside `GetPersistReference3` therefore reaches the dumper as an ordinary null and the `TryStep` never fires, so the two cases the comment distinguishes are in fact indistinguishable here - the same conflation T035 exists to remove on the mate side.
  - Fix: Either call `PersistRefService.Get` (which throws `PersistRefError` for the caller to record as a gap, which is what its own XML doc says it is for) and let the `TryStep` catch it, or correct the comment to say that a declined reference and a failed read are both a null pair, matching existing `TryGet` practice elsewhere in the dump.
- **extractor/SwReview.Extractor.Tests/CutListDumperTests.cs:340** (extractor) - Coverage gaps in the T039 suite. (a) The `Part(...)` helper always builds a child node with `ParentKey = "bracket-assy-1"` and `RootDocumentKind = Assembly`, so no test covers the part-opened-alone path, where `ComponentTreeDumper.PartRootNode` (line 223) is the only node and its `Handle` is an `IModelDoc2` rather than an `IComponent2` - the case a weldment engineer will hit most often. (b) No test covers `ActiveConfiguration` throwing, even though `ReadDocument` returns early on it (line 155) and drops the whole document's cut list. (c) No test covers `Features` throwing. (d) `SwCutListReader` itself has no test at all, so the gated member names it produces (`ConfigurationManager.ActiveConfiguration`, `Configuration.Name`, `GetPathName`, `FirstFeature`, `GetNextFeature`, `GetFirstSubFeature`, `GetNextSubFeature`) never reach `Dump_NoMutatingMemberEverPassesThroughTheGate`'s `ReadOnlyGuard.Assert` loop.
  - Fix: Add (a) a root-part case using a node shaped like `PartRootNode` (ParentKey null, Handle = the document), (b) an `ActiveConfigurationThrows` switch on `FakeDoc` asserting a `cut_list_folder` gap and no rows, (c) a `FeaturesThrow` switch, and (d) at minimum a static assertion that every member name `SwCutListReader` gates passes `ReadOnlyGuard.Assert`, so the read-only proof covers the production reader and not only the dumper.
- **extractor/SwReview.Extractor/Dump/CutListDumper.cs:190** (extractor) - T039 says "a folder whose GetTypeName2 or GetSpecificFeature2 could not be read yields a cut_list_folder gap and the folder is still recorded". That holds for the item-level GetSpecificFeature2 (ReadFolder, line 231, tested) and for GetTypeName2 (ReadText, tested), but not at the top level: a feature whose GetSpecificFeature2 threw is dropped entirely and only a gap survives. The test at CutListDumperTests.cs:222 documents the deviation in a comment rather than flagging it.
  - Fix: Either record the folder (and walk its sub-features anyway) so the task's wording holds at both levels, or get the deviation confirmed and noted in the handover so the parent decides.
- **extractor/SwReview.Extractor/Dump/SwCutListReader.cs:56** (extractor) - DRY: SwCutListReader.ActiveConfiguration (lines 56-71) is a near-verbatim copy of SwFeatureReader.ActiveConfiguration (lines 59-74) - same two gated calls, same null check, same GetPathName in the message, differing only in the trailing clause - and SwCutListReader.Document (line 46) is a verbatim copy of SwFeatureReader.Document (line 49). Features/SubFeatures (lines 73, 89) re-implement the FirstFeature/GetNextFeature and GetFirstSubFeature/GetNextSubFeature loops SwFeatureReader.Walk already runs, with a second, differently-named cap constant (MaxSubFeatureCount = 100000 vs MaxSubFeatureDepth = 64).
  - Fix: Hoist the shared document/configuration/feature-walk reads into one helper both readers call, or state in the file why two copies are preferred here.
- **extractor/SwReview.Extractor/Dump/SwCutListReader.cs:56** (extractor) - contracts/ir-additions.md section 2 and data-model.md section 2.2 give CutListItem.configuration the interop member "none" (it is the configuration the tree was read in), and research.md R3.2's cut-list row lists no configuration member. The implementation instead makes three fresh interop calls (ConfigurationManager.ActiveConfiguration, Configuration.Name, and GetPathName in the failure message), none of them in R3's cut-list row, and their failure is what drops the whole document's cut list at CutListDumper.cs:156. ComponentNode.ReferencedConfiguration is already in hand on the node the dumper is standing on.
  - Fix: Take the configuration from the ComponentNode the dumper already holds (or from the document phase's recorded active configuration) and delete the reader member, which also removes the failure path behind the must-fix.
- **extractor/SwReview.Extractor/Dump/PropertyDumper.cs:134** (extractor) - mass_overridden is read for every document kind, so a drawing - which has no mass properties - now collects a second gap (mass_override, NotExtracted, line 148) beside the pre-existing "document" gap at line 327 for the same single failure. The comment at line 322 claims "the gap below is unchanged, so the gap set of an existing dump does not move on this account", which is only true when CreateMassProperty2 answers; T018's goldens-gate exception list (drawing-gap message plus cutlist-phase gaps) does not cover a new mass_override row on a drawing root. is_exploded and material_configuration both gate on kind for exactly this reason ("an absent question is not a gap").
  - Fix: Gate the override read on the kinds where mass properties exist, or correct the comment and add the new row to T018's stated exception list.
- **extractor/SwReview.Extractor.Tests/PropertyDumperTests.cs:31** (extractor) - No test reaches PropertyDumper.Read, so the four assignments at lines 106-109 and 134 are unpinned: deleting them leaves the whole suite green. In particular the ordering T031 names - OverrideMass read off the CreateMassProperty2 object BEFORE ReadMass's volume gates - is asserted nowhere (the test at line 176 says so in its own comment), and MaterialConfiguration_ForAPartWithNoMaterial_IsStillRecorded (line 266) cannot test its own claim, because the helper is never told what the material read gave; it is the same assertion as the test above it with a different string. ComponentTreeDumper got an end-to-end pin through PackageWriterTests (Build_CarriesTheFourSchema140ComponentReadsThroughToThePackage); PropertyDumper has no equivalent.
  - Fix: Add a seam for the document read the way ICutListReader/IFeatureReader do (or a thin injectable for ISldWorks/IModelDoc2) and pin the four assignments plus the override-before-volume-gate ordering; failing that, state in the handover that the wiring is unverified offline.
- **extractor/SwReview.Extractor.Tests/CutListDumperTests.cs:391** (extractor) - Untested failure modes on a phase whose whole point is that a read failure is never a pass: the item's Feature.Name throwing (the FakeFeat.NameThrows switch at line 391 is wired at line 495 and never set by any test - dead test scaffolding), ActiveConfiguration throwing, Features throwing, SubFeatures throwing, GetPersistReference3 throwing, and GetModelDoc2 answering null. Each is one Fact given the fake already supports most of them.
  - Fix: Add one test per path, asserting the gap kind, the entity id and what survives on the package - which will also surface the must-fix above.
- **extractor/SwReview.Extractor/Dump/CutListDumper.cs:286** (extractor) - The persist-ref read failure is recorded as `cut_list_folder`, but data-model.md section 2.4 defines that kind narrowly - "emitted when a body-folder's GetTypeName2 or GetSpecificFeature2 could not be read" - and the same paragraph says a missing persistent reference is deliberately a null persist_ref and no gap at all. The same kind is also reused for the GetModelDoc2, configuration, feature-walk, sub-feature-walk and Feature.Name failures, so one entity kind now stands for seven different conditions and Python cannot tell them apart.
  - Fix: Keep cut_list_folder to the two conditions the contract names and give the others their own kinds (or record them against the existing document-level kinds), so the reason text a coverage row prints is the reason that actually happened.
- **extractor/SwReview.Extractor/Dump/FeatureDumper.cs:529** (extractor) - `count = segments is object[] array ? array.Length : 0` reads any non-null answer that is not an object[] - a typed value array, a scalar, anything a 2024 SP5 build might hand back for a hole-wizard-consumed sketch (PROBE-9 is open on exactly this) - as 0, which rules the text exemption OUT and lets standards.part.sketches_fully_defined grade the sketch. The transparency read next door took the opposite decision for the same situation (ComponentTreeDumper.ReadTransparency writes a gap and returns null when the answer carries no slot 7, "unknown is never a zero").
  - Fix: Treat a non-null answer of an unexpected shape as unknown (null plus a sketch_text gap naming what came back) and keep 0 for null and for a genuinely empty array; add the case to the T037 tests.
- **extractor/SwReview.Extractor/Dump/SwCutListReader.cs:73** (extractor) - A standards dump now walks every part document's feature tree twice: once through SwFeatureReader.Walk in the feature phase and again through SwCutListReader.Features/SubFeatures in the cutlist phase, doubling the FirstFeature/GetNextFeature interop traffic per part on an assembly with many unique parts. research.md R3.4 rejected IAssemblyDoc.GetComponents(false) on exactly this ground ("a second traversal that could disagree with the first").
  - Fix: Either let the cutlist phase consume the feature phase's already-walked tree (the handles are in hand) or record in the file why a second walk is worth it here - the phase needs GetSpecificFeature2 per feature, which the first walk does not make.
- **extractor/SwReview.Extractor/Dump/PropertyDumper.cs:300** (extractor) - "Is this a part" is decided twice in the same document read with two different predicates: ReadMaterial gates on `model is IPartDoc` (line 300) while MaterialConfiguration gates on `kind == DocumentKind.Part` (line 292 helper, called at line 106). The contract requires material_configuration to be present "whenever the material read was attempted", so the two predicates have to agree by construction, not by coincidence.
  - Fix: Decide once and pass the same answer to both, so the field cannot be written for a document whose material read was never attempted.
- **reviewer/src/swreview/checks/standards/results.py:96** (rules) - `Subject` here shadows the name of data-model.md section 3's `Subject` while having a different shape: this one is `id/name/type_name/persist_ref/persist_ref_scope/suppressed/configuration`; the contract's is `kind/id/name/persist_ref/persist_ref_scope/parent_chain/showable` with `kind` a CLOSED set (component, mate, feature, sketch, cut_list_item, sheet, view, dimension, annotation, note, revision_row, property). The `type_name` strings written here - `"cut list item"`, `"data card property"`, `"document"` - are not members of it, and `"document"` has no member at all, so T048 has to invent a second mapping table for the structured `subjects` array.
  - Fix: Either rename this one (e.g. `SubjectRow`) or align its `type_name` values with the contract's `kind` vocabulary now, and raise the missing `document` kind with the owner before T047/T048 lands.
- **reviewer/src/swreview/checks/standards/results.py:243** (rules) - `outcome` restates the body of `checks/rules/results.py::verdict` almost line for line; the only differences are one branch condition (`elif passing:` instead of `elif passing or not unknown:`) and the extra `skips` half. DRY is a binding hard rule for this feature and the brief names 'a copy instead of a reuse' explicitly.
  - Fix: Generalize `verdict` in the family-neutral `checks/rules/results.py` to take an optional `skips` sequence and a flag (or a strategy) for the vacuous-pass rule, and have both families call the one function.
- **reviewer/src/swreview/checks/standards/assembly.py:388** (rules) - `mate_references` emits the 'no mate data for the sub-assembly' unresolved row only when the package records ZERO mates scoped to that sub-assembly (`has_mate_data = is_root or bool(mates)`, line 241). contracts/rules.md's Unresolved column says the row is emitted 'once per sub-assembly' because the dump walks only the root assembly's mate group (difference h), unconditionally. If a dump ever records a partial set of sub-assembly mates, this check grades them and claims full coverage with no unresolved row - partial data read as complete. `fully_mated` carries the same condition at line 580.
  - Fix: Confirm the intended reading with the owner. If the contract means it unconditionally, emit the sub-assembly unresolved row for every non-root assembly document regardless of how many mates were recorded, and adjust `test_a_sub_assembly_whose_mates_the_package_records_is_graded`, which currently pins the looser reading.
- **reviewer/src/swreview/checks/standards/part.py:95** (rules) - Three small DRY violations against the feature's stated hard rule: `DOCUMENT_GAP = "document"` is declared twice with near-identical docstrings (part.py:95 and document.py:71); `document.py::_name_in_path` (line 240) re-implements `traversal.py::_name_in`, which already answers 'the file name a path ends in, separator-agnostic'; and `HOLE_WIZARD_TYPE_NAMES = frozenset({"HoleWzd"})` (part.py:89) restates a SOLIDWORKS type name already carried by `checks/rms_types.yaml`'s `hole: [HoleWzd, SimpleHole, Hole]` class, in a module that already imports `load_table`.
  - Fix: Put the gap-kind constant in `checks/standards/results.py` beside `find_gap`/`gap_note`; export `traversal._name_in` (or `file_name_of(path)`) and call it from document.py; and either read the hole-wizard type name from `rms_types.yaml` or add a comment at both sites saying why the table's broader `hole` class cannot be used.
- **reviewer/src/swreview/checks/standards/document.py:230** (rules) - `PrefixMatcher.from_profile(profile)` is rebuilt on every call: once per assembly document (assembly.py:245), once per part document (part.py:203) and once per data-card evaluation (document.py:230, inside the check function itself). data-model.md section 3 describes the matcher as holding the four normalized prefix lists, i.e. as a per-run object.
  - Fix: Build the matcher once per run and thread it through, or memoize `PrefixMatcher.from_profile` on the profile identity.
- **reviewer/src/swreview/checks/standards/results.py:1** (rules) - `checks/standards/results.py` is a new 313-line module that no task in the T041-T046 range names: T042 names `assembly.py`, T044 `part.py`, T046 `document.py`. The brief says 'do not touch files outside your task scope'. Its content is well justified by DRY (the `Subject` shape, the gap lookup and FR-005's document-evidence half really are shared by all four evaluator modules), but it also pre-empts naming decisions T047/T048 owns.
  - Fix: Flag it to the owner as a deviation and get it acknowledged, rather than leaving it as an unannounced extra file.
- **reviewer/src/swreview/checks/standards/assembly.py:621** (rules) - Rows contracts/rules.md does not state, chosen silently: `not_transparent` on an assembly with no immediate children returns a single `unresolved` while the polarity is unsettled (line 621) and a vacuous `pass` once settled; `not_hidden` on the same document returns a vacuous `pass` (line 700). The contract's Skipped column for both is 'never' and its Checked column is about components, so a childless assembly has no stated row, and a vacuous `pass` on a release gate is the shape the feature elsewhere refuses (difference aa).
  - Fix: Confirm with the owner whether a childless assembly should be `pass`, `skip` (as `one_fixed` and `fully_mated` already do with `NO_CHILDREN`) or nothing, and make all four instance-signal checks agree.
- **reviewer/src/swreview/checks/standards/part.py:508** (rules) - The cut-list census counts a folder whose `body_count` is NULL as non-displayable (`(items[0].body_count or 0) > 0`), so the coverage line 'N cut-list folder(s) seen, M displayable' understates the displayable count whenever a read failed. contracts/rules.md asks the reason to say 'how many folders were seen and how many were displayable'; an unread count is neither.
  - Fix: Count three states in the census - seen, displayable, and 'body count unread' - so an unread row is visible on the line rather than silently folded into 'not displayable'. Fold this into the fix for the grouping defect above.
- **reviewer/tests/unit/test_standards_document_rule.py:219** (rules) - `test_the_resolved_value_is_the_one_compared` is tautological: it constructs two `DataCardFieldResult` instances and asserts back the `state` and `resolved_value` it just passed in. It exercises neither `read_card` nor `data_card_complete`, and would still pass if the whole rule in `document.py` were deleted. Its docstring claims something about `GetAll3`'s resolved value that nothing in the body tests.
  - Fix: Replace the body with an assertion through the code under test: build a card whose wanted property carries a resolved value and assert `read_card(...)` returns `state == "present"` with that `resolved_value`, and that a property absent from the card comes back `("absent", None)` — or assert it end-to-end through `evaluate` so the finding's observed text is what is pinned.
- **reviewer/src/swreview/checks/standards/part.py:169** (rules) - `Part.hole_wizard` resolves the owning hole-wizard feature by two routes — the parent chain and `Feature.sketch.consumer_ids` — but no test sets `SketchSpec`/`sketch_spec` consumers (no occurrence of `consumer_names` anywhere in `test_standards_part_rules.py`), so the consumer route is never exercised. Statement coverage does not reveal it: the generator over an empty `consumer_ids` still executes. `contracts/rules.md` makes naming the hole-wizard feature part of the fail condition for `standards.part.sketches_fully_defined` (difference z), so half of that mechanism ships unverified.
  - Fix: Add a case to `TestSketchesFullyDefined` where the under-defined sketch is a sibling of a `HoleWzd` feature that lists it via `consumers=(...)`, and assert the feature name appears in the observed text. If the consumer route is not actually how the dump records hole-wizard sketches, delete the branch rather than ship an untested path.
- **reviewer/tests/unit/test_standards_assembly_rules.py:203** (rules) - FR-005's document-evidence half is asserted for `not_exploded` only. `standards.assembly.rebuild_errors` carries the same `document_evidence_unresolved` guard (assembly.py:355-357); the guard runs during that test because `evaluate_assembly` evaluates all seven checks, so it shows as covered, but nothing asserts it — deleting the guard from `rebuild_errors` breaks no test. The part-side `TestFr005` does loop over all four `PART_CHECKS`, which is the pattern to match.
  - Fix: Turn the sub-assembly-reached-only-through-unresolved-instances case into a loop over the document-evidence checks (`not_exploded`, `rebuild_errors`), asserting `outcomes(...) == {"unresolved"}` and the component id and state in the reason for each — the way `test_standards_part_rules.py:589` does it.
- **reviewer/tests/unit/test_standards_assembly_rules.py:512** (rules) - In `test_a_one_mate_prefix_component_below_its_minimum_fails_naming_both`, `assert "1" in result.result.observed` cannot fail: the component's `full_path` is `s1`, so the digit is in the observed text regardless of the mate requirement or the count found. The task text asks that the finding name "the requirement and the count found"; only the prefix assertion on the next line currently carries weight.
  - Fix: Assert the phrase rather than the digit, e.g. `"carries 0 unsuppressed mate(s)" in observed` and `"below the 1" in observed`, so a wrong requirement or a wrong count fails the test.
- **reviewer/src/swreview/checks/standards/assembly.py:710** (rules) - The same condition — an assembly with no immediate child components — is answered three different ways: `one_fixed` (474-475) and `fully_mated` (520-521) report `skip` with `NO_CHILDREN`, `not_hidden` (710-711) reports a vacuous `pass`, and `not_transparent` (633-636) reports a vacuous `pass` once the polarity is settled. The contract's Skipped column says "never" for the latter two, so a pass is contract-literal, but it books coverage as "checked" for a document where nothing was looked at, which inflates the headline the verdict is computed from. Neither vacuous-pass path is asserted by a test (assembly.py:635 is the one statement these tests never reach).
  - Fix: Either state the choice explicitly — a one-line comment on each `passed(...)` saying an assembly with no children trivially satisfies the statement, plus a test asserting `pass` for that shape — or make all four use the same `NO_CHILDREN` skip and record the divergence from the contract's "never" column in the task report. My preference is the former: the contract is explicit that these two are never skipped, so the fix is to pin the behaviour with a test rather than change it.
- **reviewer/src/swreview/checks/standards/assembly.py:562** (rules) - Four unresolved-direction branches ship with no test behind them (statement coverage of the three modules by their own test files is 98-99%, and these are the gaps): assembly.py:562-569 (an under-constrained component whose referenced document has no recorded path, so no mate-count prefix can be matched), assembly.py:726 (a `visibility_raw` outside {-1, 0, 1}), part.py:407 (a null `material_configuration`, which the contract's unresolved column names as "the configuration the material was read in was not recorded"), and document.py:243 (`_name_in_path` on a document with no path). All four fail safe — they report unresolved — but the contract names the third one explicitly, and the second is defensive code for a value SOLIDWORKS does not define.
  - Fix: Add a one-line case for each of the first three (the contract names them), and either test or delete assembly.py:723-729, which handles a visibility value no build emits and is the only one of the four that reads as speculative.
- **reviewer/src/swreview/checks/standards/report.py:112** (run) - `KIND_BY_TYPE_NAME` maps the `document` subject label to `kind: "document"`, and `document_subject` is what every document-subject check names, so findings ship a `Subject.kind` outside the closed union data-model.md section 3 states: `component | mate | feature | sketch | cut_list_item | sheet | view | dimension | annotation | note | revision_row | property`. "document" is not in it. The page (T078-T081) is written against that union.
  - Fix: Either amend the `Subject` row in `specs/006-standards-check/data-model.md` section 3 to include `document` - a contract edit no task in T047-T052 authorises, so raise it rather than doing it silently - or map a document subject onto an existing kind. Recommend raising it: "document" is the honest kind and the union is the thing that is incomplete.
- **reviewer/src/swreview/checks/standards/report.py:476** (run) - `_write_summary` re-implements the family-neutral "one item that moves between two buckets" placement copied out of `checks/rules/report.py::_write_summary` (the same clear-the-other-bucket loop, the comment even says "exactly as the neutral layer does it"). Worse, the neutral layer writes its own summary item on every call and this module then deletes it and writes another, so each run writes the `standards.release` row twice and `report_results` has to filter the discarded row out of `reported["coverage"]` (line 176). The hard rule is DRY: build on `checks/rules`, never copy it.
  - Fix: Give `checks/rules/report.py` one seam for the summary - e.g. an optional `summary_item: Callable[[ToolContext, ReviewSession], CoverageItem] | None` that `report_results` uses in place of its own default - and have the standards family supply the verdict-bearing item. The bucket-moving logic then exists once, and no throwaway row is written.
- **reviewer/src/swreview/checks/standards/run.py:326** (run) - Two documented behaviours of the phase-row refusal have no test. `_refuse_missing_phases` treats a phase the package records **no row for at all** as missing (`ran.get(name, NEVER_RAN)`), and deliberately accepts a phase recorded `failed` ("a `failed` phase **ran**"). `tests/unit/test_standards_run.py`'s `without_phase` helper only rewrites a row to `skipped`, so neither branch is exercised; a regression that made an absent row pass, or a failed row refuse, would be green.
  - Fix: Add two cases to `tests/unit/test_standards_run.py` section 2: a package with the `cutlist` row removed entirely is refused naming it, and a package whose `cutlist` row is `failed` is graded (its gaps becoming unresolved coverage, which is the honest answer).
- **reviewer/src/swreview/checks/standards/report.py:431** (run) - `_empty_setting_checks` recovers a structured fact - "this check skipped because a profile list is empty" - by running the regex `EMPTY_SETTING` over another module's prose (`checks/standards/document.py:149,158`). Nothing binds the two: if document.py rewords its skip reason, FR-032's "n checks skipped because a profile list is empty" note silently disappears from every verdict. `test_standards_verdict.py` passes `empty_setting_checks` in by hand, so no test crosses the seam.
  - Fix: Preferred (explicit over clever): carry the fact rather than parse it - e.g. an `empty_setting: str | None` on the skip constructor in `checks/standards/results.py`, read here. If the regex stays, add a test in `test_standards_report.py` that runs the real `evaluate_document` with a profile whose `part_number.pattern` and `data_card.properties` are empty and asserts the verdict note appears.
- **reviewer/src/swreview/checks/standards/report.py:540** (run) - `_derived_buckets` keys the rendered `failed`/`warned` reasons by `(check, bucket)` then by document, so two finding-shaped results for the same (check, document) pair collapse to one rendered row while `_finding_outcomes` counts both in the verdict's error count. The rendered sixteen rows and the headline counts would then disagree, and nothing in the report layer or its tests asserts the one-finding-per-(check, document) invariant T047 states.
  - Fix: Either accumulate the reasons per document (list, not overwrite) or assert the invariant where the results enter - e.g. refuse in `report_results` when two finding-shaped `RuleResult`s share a `(rule_id, document_id)` pair - and add a test for the refusal, so a future check that fans out per subject fails loudly instead of under-reporting.
- **reviewer/src/swreview/checks/standards/report.py:323** (run) - `Subject.parent_chain` is hard-coded `[]` for every subject. `checks/standards/part.py` computes the parent-feature chain (difference z, `scope.where()`) and spends it on the `observed` prose only, so the structured field the page is meant to render is permanently empty. It is a field in data-model.md section 3 with no producer.
  - Fix: Populate it for feature and sketch subjects from the chain part.py already builds (carry it on the subject row, or stamp it in `_kind_stamp` beside `kind`), or note explicitly in the contract that the chain lives in `observed` for this release so US3 does not build a renderer for an always-empty list.
- **reviewer/src/swreview/checks/standards/run.py:382** (run) - `_write_report` re-implements `checks/rules/run.py::write_report` (same `REPORT_FILE_NAME` join, same `write_text(..., encoding="utf-8")`, same return) only to prepend a header, and `write_report` is imported nowhere in this family as a result. Small, but it is the copy-instead-of-reuse pattern the hard rule names.
  - Fix: Add an optional `header: str = ""` parameter to `checks/rules/run.py::write_report` and call it from `_write_report`, leaving this module owning only `VERDICT_HEADER`.
- **reviewer/src/swreview/checks/standards/run.py:246** (run) - Nothing on this path refuses an `out_dir` inside `package_dir`. `contracts/cli.md` line 23 makes that a named refusal ("`--out` is unwritable, or is inside the package directory"), and `run_standards_check`'s own docstring says the pane deliberately passes the package directory as the run folder - so the two callers want opposite things and only the CLI half (T086/T087) is specified to enforce it. `test_it_writes_the_run_folder_and_not_one_byte_into_the_package` only covers the disjoint case.
  - Fix: Leave the refusal in the CLI as the contract says, but state in `run_standards_check`'s docstring that the entry point deliberately permits out_dir == package_dir for the pane and that the inside-the-package refusal is the command's, so T086/T087 does not assume it was already handled here.
- **reviewer/tests/unit/test_standards_run.py:211** (run) - test_a_package_whose_cutlist_phase_never_ran_is_refused_naming_it asserts `"standards" in message`, intending to pin T051's "naming the profile that produced it". But run.py's MISSING_PHASES template ends with the literal "Extract the design again with --profile standards", so that substring is present for every package whatever profile wrote it. The half of the requirement that matters is untested and would still pass if profile=package.extractor.profile were dropped from the format call.
  - Fix: Build the package with as_profile(exploded_package(), "model_check") (the helper already exists at line 120) and assert "model_check" in message. Keep the "--profile standards" assertion separately as the remediation sentence.
- **reviewer/tests/unit/test_standards_report.py:500** (run) - test_a_part_document_with_no_instance_is_unresolved_coverage_and_not_a_finding grades `top`, the root assembly, which does carry an instance (cmp:0001), and then asserts one finding and no unresolved row - the opposite of what its name states. _split_by_reportability's unreportable branch (a part or assembly document with no ComponentInstance becoming unresolved coverage) is never exercised anywhere in this file, so the rule T047 leans on is unverified for the standards family.
  - Fix: Either rename the test to what it checks (a document that is reached by instances still yields a finding), or build a package holding a part document that no ComponentInstance names and assert the unresolved coverage row with the "no component instance for <id>" reason. Prefer the latter, plus the rename.
- **reviewer/src/swreview/checks/standards/results.py:74** (run) - Two different `subject_input` functions are reachable from the same family under the same name. results.py imports the neutral feature-tree one from checks/rules/results.py (line 36) and re-exports it in __all__ (line 74), formatted `<id> <name> [<type>] persist_ref=... scope=...`; report.py defines its own (line 222) formatted `<kind> <id> <name> persist_ref=<ref|none> scope=...`. A check author importing from results.py - the natural place, since every subject constructor lives there - silently gets the wrong format. The re-exported one is also dead for this family, because _stamped (report.py:260) overwrites inputs wholesale.
  - Fix: Remove subject_input from checks/standards/results.py's import and __all__ so the family has exactly one. If a check genuinely needs the neutral form, import it explicitly from checks/rules/results.py at the call site.
- **reviewer/src/swreview/checks/standards/report.py:440** (run) - DRY: _write_summary re-implements the neutral layer's summary dance verbatim - hard-coding its own `for bucket in ("checked", "unresolved")` clear loop (line 481) that duplicates rules/report.py::_SUMMARY_BUCKETS (line 110) and the identical loop at rules/report.py:517. Two copies of one rule, free to disagree the day a third summary bucket appears. Related: the neutral _write_summary runs first and writes a standards.release item with the neutral reason, which this one then overwrites - so replace_coverage's event emission announces the wrong summary to the pane before the right one, and one coverage write per run is wasted.
  - Fix: Export a small write_summary_item(family, context, session, bucket, item) from checks/rules/report.py that owns the clear-then-replace rule and call it from both. Optionally let a family suppress the neutral summary (a summary= hook, or an extra_coverage step that returns it) so the item is written once with its final reason.
- **reviewer/src/swreview/checks/standards/report.py:143** (run) - EMPTY_SETTING = re.compile(r"the profile's (?P<setting>[A-Za-z0-9_.]+) is empty") decides a verdict note by regex-matching a check's human-readable skip prose. The only two producers are document.py:149 and document.py:158; a reword there silently drops the "n checks skipped because a profile list is empty" note (FR-032) with nothing going red, and no test in test_standards_report.py covers the path at all - the note is only exercised in test_standards_verdict.py, which passes empty_setting_checks in directly. This is prose as an interface, against the explicit-over-clever rule the docstring itself invokes.
  - Fix: Carry the fact rather than parse it: add a skipped_on_empty_setting field (or a skipped_empty_setting(rule, document_id, setting) constructor) to the standards RuleResult, set it in document.py, and read the attribute in _empty_setting_checks. Add one test that a skip on an empty data_card.properties reaches the verdict note.
- **reviewer/tests/unit/test_standards_report.py:646** (run) - Coverage gaps against the task text. (a) T047's worst_bucket order "unresolved, failed, warned, skipped, checked, out of scope" is only partly pinned - the tests show unresolved>failed and spot-check checked/warned/out_of_scope, but warned>skipped and skipped>checked are never asserted and BUCKET_ORDER itself is never compared to the contract's sequence. (b) Only two of the four HIGH_SEVERITY_CHECKS (registry.py:105-110) are parametrized; assembly.rebuild_errors and drawing.dimensions_not_overridden are not. (c) In test_standards_run.py, _refuse_missing_phases' documented rule that a phase with no row at all is missing too is never tested - without_phase only rewrites the status to "skipped".
  - Fix: Assert BUCKET_ORDER == ("unresolved","failed","warned","skipped","checked","out_of_scope") and add one case where a check lands in warned+skipped+checked together; extend the severity parametrize to all four high checks; add a without_phase_row helper that drops the row entirely and assert the same refusal.
- **reviewer/src/swreview/tools/standards_checks.py:205** (run) - _results_for's docstring says "Every check that applies to one graded document, in catalogue order", but the body emits the unavailable-check unresolved rows for the whole catalogue first and then appends each scope evaluator's results, so the order is not catalogue order. Harmless today (coverage aggregates), but a comment that does not describe the code is exactly what the behavioural guidelines call out.
  - Fix: Reword to what it does ("the unavailable checks first, then each scope's evaluator"), or interleave so the claim becomes true.
- **reviewer/src/swreview/checks/standards/report.py:122** (run) - KIND_BY_TYPE_NAME carries six labels - "sheet", "view", "dimension", "annotation", "note", "revision row" - for which checks/standards/results.py has no constructor yet; the drawing subject constructors do not land until T066. They are unreachable entries today, and if a T066 author picks a different label string the map fails open to "feature" with nothing going red.
  - Fix: Either keep them and add an assertion that every type_name any standards subject constructor emits is a key of this map (cheap, and it catches the fail-open), or trim to the five live labels and let T066 add its own.
- **reviewer/.pytest-tmp-reviewer006/probe.py:1** (run) - `uv run ruff check .` from reviewer/ reports 4 errors (I001 plus three E501), all inside .pytest-tmp-reviewer006/probe.py - a leftover exploratory script in a basetemp directory the environment rules say to delete after the run. Four such directories were still present when I reviewed (.pytest-tmp-review041, .pytest-tmp-reviewer, .pytest-tmp-reviewer006, .pytest-tmp-reviewer006b); 006 and 006b match this group's label. Ruff over src and tests is clean, so this is housekeeping rather than a code defect, but it fails the tree-wide ruff gate.
  - Fix: Delete reviewer/.pytest-tmp-reviewer006 and .pytest-tmp-reviewer006b. I removed only my own .pytest-tmp-review-run and left the others alone, since concurrent agents may still be using them.
- **reviewer/tests/golden/test_standards_goldens.py:200** (goldens) - T053 says "hand-check every expected check id against contracts/rules.md" and SC-001 requires "the right check id, the right severity and the right subject named". The module pins counts (`SEEDED_FINDINGS`) and derives the expected status from `RULES`/`STANDARDS_FAMILY` itself (line 236), which is self-referential, and inspects `inputs` only in `test_two_violations_are_seeded_at_depth` (293). Nothing ties a seeded violation to the subject it seeded: that `assembly.one_fixed` names exactly the three fixed components, `fully_mated` names `MR-10002-2` and not the library part `MR-10003-1` (which is also under-constrained but satisfies its one-mate requirement), `not_hidden` names `MR-10002-4`, `mate_references` names `mate:0002`, `cut_list_excluded` names the one non-excluded item. A seed that silently moved to a neighbouring component would still produce eleven findings and pass.
  - Fix: Extend `SEEDED_FINDINGS` from `{check: count}` to `{check: (count, expected subject ids)}` and assert the `inputs`/`subjects` ids of each seeded finding against the generator's own constants, so each of the twelve rows is hand-checked in code rather than in prose.
- **reviewer/tests/golden/test_standards_goldens.py:256** (goldens) - Of T053's four drawing seeds only the six overridden dimensions are asserted present in the package (`test_the_seeded_drawing_carries_six_overridden_dimensions`). The dangling annotation (`Datum1`), the revision-table/property disagreement (table cell `AC` vs the drawing's `MeridianRev` = `AB`) and the note carrying `PROFILE.export_control.phrase` are seeded in `standards-seeded/generate_package.py` and nowhere verified. Because their checks are unbound until T066, nothing else in the suite reads them either, so a regenerated fixture that dropped one would go unnoticed until T070 and would then look like a drawing-check bug.
  - Fix: Add package-level assertions beside the six-dimension one: exactly one annotation with `is_dangling`, a revision table whose `revision.cell` value differs from the drawing's revision property, and exactly one note containing `fixture_profile().export_control.phrase` - all read off the profile, never spelled out.
- **reviewer/tests/golden/test_standards_goldens.py:483** (goldens) - T055/SC-003 require that "every affected check is unresolved naming the **exact** missing signal", and specifically that `standards.assembly.fully_mated` be unresolved "only for its under-constrained library-prefix components". Eight checks land in `unresolved` on `standards-unknown` (assembly.not_transparent, mate_references, fully_mated, the four part checks, data_card_complete) but the reason text is asserted for only two of them (`constrained status is unavailable`, line 452; `whether the mass is overridden was not read`, line 469). `test_the_sub_assembly_is_what_the_mate_check_cannot_answer_for` covers `mate_references` only; nothing asserts that `fully_mated`'s unresolved row is about `MR-13006-1` (the under-constrained `catalog/screws` component) rather than about the sub-assembly as a whole, and nothing asserts the reason naming the suppressed/lightweight instance for the two blocked documents in `test_a_document_reached_only_through_unresolved_instances_is_never_graded` (471).
  - Fix: Assert the reason string of every unresolved row on `standards-unknown` names a concrete missing signal (non-empty is already checked elsewhere - here assert the specific phrase per check), and add the `fully_mated`-names-the-library-component assertion SC-003 spells out.
- **reviewer/tests/checks/golden_standards.py:52** (goldens) - `standards_case(package_or_dir: EvidencePackage | Path | str, ...)` carries a `Path | str` branch (lines 63-67) that nothing calls: the golden harness always hands over a loaded `EvidencePackage` (test_golden.py:110), and `graded()` in `test_standards_goldens.py` does the same. It is untested speculative flexibility in a file whose whole job is to be the one narrow adapter - CLAUDE.md "No 'flexibility' or 'configurability' that wasn't requested", and the branch also silently skips the scratch copy that the module's own docstring names as what makes SC-010 ("no golden run touches the fixture it grades") structurally true.
  - Fix: Narrow the signature to `EvidencePackage` and delete the `Path | str` branch plus its docstring sentence; reinstate it in the task that actually needs a directory-taking caller.
- **reviewer/tests/golden/fixtures/standards-compliant/generate_package.py:63** (goldens) - T054 and SC-002 describe `standards-compliant` as "parts, sub-assemblies and drawings" (both plural). The fixture carries exactly one sub-assembly (`MR-21002`) and one drawing (`MR-91001`, two sheets), and `test_the_multi_level_fixture_carries_parts_sub_assemblies_and_a_drawing` only asserts `sub_assemblies(package)` is truthy. With a single sub-assembly, a difference-h regression that emitted one unresolved row per PACKAGE rather than one per sub-assembly would be indistinguishable, and `test_the_two_known_gaps_name_each_sub_assembly` (line 366) would still pass.
  - Fix: Add a second sub-assembly (and, if the wording is to be met literally, a second drawing), so "one unresolved row per sub-assembly" is measured with a list longer than one; the multiplicity fixture proves the counting rule for a shared sub-assembly but not for two distinct ones under the compliant path.
- **reviewer/.pytest-tmp-reviewer006/probe.py:1** (goldens) - Leftover pytest basetemp directories are still in the working tree (`reviewer/.pytest-tmp-reviewer006/`, `.pytest-tmp-reviewer006b/`, `.pytest-tmp-review041/`), untracked and not covered by `.gitignore` (which only ignores `.pytest-tmp/`). One of them contains an ad-hoc `probe.py` that makes a repo-wide `uv run ruff check .` report 4 errors; `uv run ruff check src tests` is clean. The environment instructions say to delete the basetemp directory afterwards. This is another agent's leftover, not T053-T056's own output, and is reported rather than fixed.
  - Fix: Delete the three leftover `reviewer/.pytest-tmp-*` directories before the stage closes, and consider widening `.gitignore` from `.pytest-tmp/` to `.pytest-tmp-*/` so a forgotten basetemp cannot reach a commit or break the lint gate.
- **reviewer/tests/golden/fixtures/standards-seeded/generate_package.py:1** (goldens) - Nothing in the suite pins that a committed `package.json` still matches the `generate_package.py` beside it. tests/unit/test_support_standards.py:591 proves the *builder* is deterministic, but not that the five committed packages are what today's generators emit, so editing a generator (or a builder default) silently drifts the fixture from the intent its docstring table states. I verified by hand that all five currently MATCH byte-for-byte, so the test would pass today and costs nothing.
  - Fix: Add one parametrized test that imports each `standards-*/generate_package.py` by path, calls `build()`, `save_package`s to `tmp_path`, and byte-compares `package.json` (and `case.json`) with the committed one.
- **reviewer/tests/golden/test_standards_goldens.py:342** (goldens) - test_every_us1_check_is_checked_or_skipped_with_a_stated_reason promises more than it delivers. The `coverage` array carries no `failed` bucket (a failure is represented by a Finding, as standards-seeded's baseline shows: its six assembly checks have only out_of_scope coverage rows). So if a compliant document started FAILING a check, its coverage row simply disappears and `graded_buckets <= {"checked", "skipped"}` still holds -- the test cannot detect the regression it is named for. It is backstopped by test_the_multi_level_fixture_produces_zero_findings, but the coverage assertion itself is weaker than it reads.
  - Fix: Assert positively that every graded document id of a US1 check lands in exactly one of checked/skipped/unresolved -- i.e. that the union of the four buckets' document_ids equals the full graded-document set for that check -- so a document dropping out of coverage because it failed is caught here rather than only next door.
- **reviewer/tests/golden/test_standards_goldens.py:218** (goldens) - test_no_seventeenth_check_result asserts `rows == list(RULES)`, deriving the expectation from the registry it is checking. It catches a seventeenth row but not a missing one: if a rule were dropped from RULES the run would emit fifteen rows and the test would still pass. T021 pins the sixteen ids against contracts/rules.md, so this is a second line of defence that is one assertion short.
  - Fix: Add `assert len(rows) == 16` (and the same for `seeded["checks"]`) so the count is pinned independently of the registry the rows are compared against.
- **reviewer/tests/golden/test_standards_goldens.py:72** (goldens) - DRY: `graded()` re-implements the golden harness's case runner -- read case.json, resolve_callable, load_package, call with kwargs -- which is exactly test_golden.py:107-115. The module docstring says 'Every case runs exactly what the golden harness runs ... so the two cannot drift apart', but nothing enforces that; the two copies can drift the moment the harness changes how a case is invoked (extra kwargs, a different loader entry point).
  - Fix: Extract the four lines into a `run_case(fixture_dir)` helper in test_golden.py beside `resolve_callable` and have both `test_golden_case` and `graded()` call it.
- **reviewer/.gitignore:20** (goldens) - Outside T053-T056, reported per the brief: six leftover pytest basetemp directories are sitting untracked in the tree (.pytest-tmp-review-extractor, .pytest-tmp-review-goldens, .pytest-tmp-review-run, .pytest-tmp-reviewer, .pytest-tmp-reviewer006, .pytest-tmp-reviewer006b), and one of them holds a scratch `probe.py` that makes `uv run ruff check .` from reviewer/ report 4 errors (I001 plus three E501). `.gitignore:20` ignores `.pytest-tmp/` only, not `.pytest-tmp-*`, so a `git add -A` would sweep all six into the commit. I removed only my own .pytest-tmp-review-goldens.
  - Fix: Delete the five remaining directories and widen the ignore line to `.pytest-tmp*/`.
- **reviewer/src/swreview/checks/standards/run.py:1** (goldens) - Outside T053-T056, cosmetic, visible in every new baseline: the out-of-scope coverage reason reads 'doc:1 is a drawing document and this check grades a assembly one' -- 'a assembly' instead of 'an assembly'. It is now frozen into all five standards-* baselines, so fixing it later costs a --force-regen of five goldens.
  - Fix: Fix the article in the out-of-scope reason builder (wherever `grades a {scope} one` is formatted) before the wording is frozen further, and regenerate the five standards-* baselines in the same change.

## Feature 006 round C (2026-09-17)

- **extractor/SwReview.Extractor/Dump/SwDrawingReader.cs:198** (dumper) - `ReferencedModels()`'s own doc comment promises "A sheet or view that will not answer is skipped rather than thrown on: this runs inside ComponentTreeDumper.Traverse, which is outside a phase, so an escaping exception would cost the whole package" - but the method contains no try/catch. `SheetNames` throws by design when GetSheetNames returns null (line 62), and the Sheet/GetViews/GetReferencedModelName/ReferencedDocument calls can each throw. One unreadable sheet therefore loses the referenced-model list for the whole drawing; ComponentTreeDumper.TraverseDrawing catches it in a single TryStep whose gap names no model at all, so FR-025's "a drawing_referenced_document gap naming it" is not met and every referenced model's parts and assemblies come back ungraded under one anonymous gap.
  - Fix: Make the loop per-sheet and per-view fault tolerant (skip and continue, letting the drawing phase record the named gap when it re-reads the same sheets), or delete the comment's claim and state the all-or-nothing behaviour. Cover it with a case in which one of two sheets throws and the other's references still come back.
- **extractor/SwReview.Extractor/Dump/ComponentTreeDumper.cs:463** (dumper) - In `WalkReferencedModel`, `gate.Call("GetPathName", ...)` (463), `gate.Call("ConfigurationManager.ActiveConfiguration", ...)` (500), `gate.Call("Configuration.Name", ...)` (505) and `gate.Call("GetRootComponent3", ...)` (530) - plus `ReadPatternMembership`'s `FirstFeature` walk on the referenced document - are bare gate calls outside any `TryStep`. Every other failure in the same method degrades to a gap, and `Traverse` runs outside `RunPhase` (its own comment at line 97-99 says an escaping exception costs the whole package), so a single ill-behaved referenced model of a drawing turns a recoverable coverage gap into no package at all.
  - Fix: Wrap each of those reads in `gaps.TryStep` (or wrap the whole per-model walk in one TryStep that names the model path) so one bad referenced model costs its own subtree and not the dump. Assert it with a fake whose GetPathName throws.
- **extractor/SwReview.Extractor.Tests/ComponentTreeDumperTests.cs:291** (dumper) - T063 asks for tests that "for a drawing root the dumper produces one subtree per referenced model under a synthesized forest root, walking each referenced document's component tree exactly as it walks an assembly root". The new tests exercise only the two extracted statics - `DrawingRootNode` and `AddDrawingForest` with a delegate stub - so the instance half (`WalkReferencedModel`, `ReferencedModelNode`, `SubtreeScope`) is entirely unverified: the kind-unreadable refusal, the no-active-configuration branch, the null-root-component branch, the part-is-one-node-and-no-more rule, the `depth: 1` start, and the change from `_session.Document` to `scope.Document` for persistent-reference scoping (line 729) all ship with no test. That scoping change also affects the assembly-root path, where it is likewise untested.
  - Fix: Extract the per-model decision the way DrawingTraversal was extracted - a small seam taking (path, kind, configuration, rootComponent) rather than an IModelDoc2 - so the branch table and the scope choice can be asserted without a seat; at minimum add an assertion that a referenced part yields exactly one node and a referenced assembly's children carry PersistRefScopePath equal to that assembly's path, not the drawing's.
- **extractor/SwReview.Extractor/Dump/DrawingDumper.cs:378** (dumper) - `CrossCheckRevisionTable` (line 398) is reached only when the view enumeration succeeded and the non-active/empty-views early return at 378-392 did not fire. Those are exactly the sheets where `ISheet.RevisionTable` is the only remaining evidence that a revision table exists, and contracts/ir-additions.md section 3.2 says the single-valued property "is still read and compared, so a table the walk missed is a visible disagreement rather than an absence". On a non-active sheet whose views came back empty the property is never read, so a sheet that does carry a revision table is recorded as a sheet with none plus only the generic drawing_sheet_views gap.
  - Fix: Read the cross-check before the two early returns (or in a finally-shaped step) so a sheet whose views could not be enumerated still reports the disagreement, and add a DrawingDumperTests case: non-active sheet, empty views, SheetRevisionTable non-null.
- **extractor/SwReview.Extractor/Dump/DrawingDumper.cs:332** (dumper) - A sheet that `GetSheetNames()` listed but the `Sheet[name]` indexer would not return is dropped with no `DrawingSheetRecord` at all - only a document-scoped `drawing_sheet` gap. The four drawing checks grade per sheet and report unresolved per sheet (contracts/rules.md, T065), so with no record there is no sheet subject to mark unresolved, and `record.Sheets.Count` silently disagrees with the drawing's own `GetSheetCount()` that SwDrawingReader.SheetNames already read.
  - Fix: Record the sheet with its name, its true index and empty views, and attach the gap to that sheet's id (as the non-active/empty-views path already does), so the sheet is a visible unresolved subject rather than an absence. This shares a fix with the index defect above.
- **extractor/SwReview.Extractor/Dump/DrawingDumper.cs:757** (dumper) - `RevisionTableRow.IsHeader` is hard-wired to null for every row and DrawingDumperTests.Dump_ClassifiesNoRowAsAHeader pins that, so the field is dead in every package this phase writes. contracts/ir-additions.md section 3.7 defines it as "the extractor's reading of the table's own title-row structure (`TotalRowCount` versus `RowCount`)", both VERIFIED members, with "may be null" as the failure rule rather than the only behaviour. The contract row is not implemented, only refused.
  - Fix: Either read TotalRowCount/RowCount and record the derivation (null plus the existing revision_table_read gap when the cast fails), or raise a contract edit in the task that owns section 3.7 so the row states that the extractor records null until PROBE-4 answers. Do not leave the contract and the code disagreeing.
- **extractor/SwReview.Extractor/Dump/DrawingDumper.cs:202** (dumper) - The angle and length dimension-type sets are integer literals (`{3, 16}` and `{1,2,4,5,6,7,8,9,10,11,12,14,15}`) with the enum names only in a comment, while every other dumper in this tree (MateDumper, HoleDumper, FaceDumper, PropertyDumper, SwDrawingReader itself at line 152) uses the `SolidWorks.Interop.swconst` enum the project already references. I reflected the installed swconst and the numbers are correct today, and a swconst enum constant introduces no COM object into this pure class, so the literals buy nothing and cost the compiler's check.
  - Fix: Use `(int)swDimensionType_e.swAngularDimension` etc., or state in the doc why this file avoids the swconst reference the rest of Dump/ takes.
- **extractor/SwReview.Extractor/Dump/DrawingDumper.cs:698** (dumper) - `_gate.Call("ITableAnnotation.Type", ...)` names the member with an interface qualifier. research.md R8 states the convention explicitly — "ReadOnlyGuard.DeniedMemberSet stores unqualified names, matched case-insensitively, because SwGate.Call(\"<member>\") names them that way" — and ReadOnlyGuard.cs:78 repeats it ("The names are bare because SwGate.Call names them bare"). Every other call in this file is bare, including `_gate.Call("Type", ...)` for IView.Type three methods up. The gate log that SC-010 is audited against now carries one non-bare name, and a future denylist entry for a bare name would silently not match this call site.
  - Fix: Use the bare name "Type", as the view-type read already does. If distinguishing the two reads matters, raise it in R8 rather than diverging in one call site.
- **extractor/SwReview.Extractor/Dump/SwDrawingReader.cs:193** (dumper) - The XML doc on ReferencedModels claims "A sheet or view that will not answer is skipped rather than thrown on: this runs inside ComponentTreeDumper.Traverse, which is outside a phase, so an escaping exception would cost the whole package." The body (:200-225) contains no try/catch: SheetNames throws InvalidOperationException when GetSheetNames returns null, the AsSheet/AsView casts throw, and each Gate.Call propagates. ComponentTreeDumper.TraverseDrawing does wrap the whole call in gaps.TryStep, so the package survives — but the entire referenced-model list is lost, not one sheet or one view. One unreadable view on sheet 3 therefore costs every referenced model its subtree and every part- and assembly-scope check on the whole package.
  - Fix: Either implement the per-sheet and per-view skip the doc describes (continue past a sheet or view that throws so the readable ones still yield references), or correct the doc to say the caller turns any failure into one gap and the forest is then empty. The first matches the stated preference for handling more edge cases; add a test once the walk is testable.
- **extractor/SwReview.Extractor/Dump/SwDrawingReader.cs:145** (dumper) - DimensionValue calls AsDisplayDimension(dimension).GetDimension2(0) and value.GetSystemValue3(...) with neither read named to the gate; the dumper's wrapper names the whole thing "GetSystemValue3". Every sibling member that spans several calls gates each one individually — ActiveSheetName gates GetCurrentSheet and GetName, SheetNames gates GetSheetCount and GetSheetNames, DimensionName gates GetDimension2, FullName and Name. The guard still runs (the outer Call is entered), so this is not a read-only hole, but the SC-010 gate log under-reports what the phase touched and the inconsistency invites the next member to skip the gate entirely.
  - Fix: Gate both inner calls the way DimensionName does — Gate.Call("GetDimension2", ...) then Gate.Call("GetSystemValue3", ...) — and either drop the now-redundant outer wrapper in DrawingDumper.ReadNumber for this member or keep it and let the reader gate the inner ones.
- **extractor/SwReview.Extractor/Dump/DrawingDumper.cs:774** (dumper) - `cells.Cells.Add(read ? text : null)` records null for a cell whose read THREW (with a revision_table_read gap from TryStep) and also records null with NO gap when SOLIDWORKS answers null for the cell. Everywhere else in this file a null answer is a gap: ReadText (:823) explicitly gaps "when it threw or when SOLIDWORKS answered null", which is what makes view names, note text and sheet format names honest. A null cell with no gap is a signal the dump could not read that carries no reason, contrary to the feature's own rule that an unreadable cell is a gap and not an absence.
  - Fix: Emit a revision_table_read gap naming the table and the [row, column] when the cell read succeeds but returns null, matching ReadText's rule, and add the test case — the fake already supports it (set a Rows entry to null).
- **extractor/SwReview.Extractor.Tests/ComponentTreeDumperTests.cs:458** (dumper) - AddDrawingForest_HandsTheWalkTheDocumentItWasGivenAndNothingElse ends with Assert.Empty(_observer.Members) to prove "the forest never asks SOLIDWORKS to find, open or resolve a document". AddDrawingForest is a static method that is never handed the gate or the session, so it could not possibly record a gated member: the assertion cannot fail and proves nothing about the code path that actually talks to SOLIDWORKS (WalkReferencedModel, which is untested).
  - Fix: Move the open/load/resolve assertion onto the real walk once it is testable (assert against _observer.Members after driving TraverseDrawing/WalkReferencedModel through a fake session), and drop or re-scope the tautological one here.
- **extractor/SwReview.Extractor.Tests/DrawingDumperTests.cs:822** (dumper) - FakeTable.CurrentRevisionFailure is declared and wired into the fake at :1044 but no test ever sets it, so the CurrentRevision read failing — contracts/ir-additions.md section 3.7's null rule for current_revision_raw — is untested dead scaffolding. In the same vein FakeDimension has an OverrideFailure switch but none for the two value reads, so the dimension_override gap DrawingDumper.ReadNumber writes when GetSystemValue3 or GetOverrideValue throws (the value and override_value null rules in section 3.4) is never exercised either.
  - Fix: Add the two missing cases: a table whose CurrentRevision read throws (null current_revision_raw plus a revision_table_read gap, rows still read) and an overridden dimension whose GetOverrideValue/GetSystemValue3 read throws (null value plus a dimension_override gap naming ddm:NNNN); or delete the unused switch.
- **reviewer/tests/unit/test_ingest_package.py:249** (rules) - `test_a_sheet_written_before_the_stamp_existed_loads_with_no_source` globs `golden/fixtures/*/package.json` (one level) and asserts `all("source" not in sheet ...)` over EVERY fixture it finds. Two problems. (a) The glob misses the nested T069 fixtures at `golden/fixtures/standards-drawings/*/package.json`, so the gate quietly covers less than it reads as covering. (b) The assertion is stronger than T067 asks for ("no golden package under reviewer/tests/golden/fixtures/ moves"): it forbids ANY fixture from carrying a stamped sheet, which directly contradicts T069's required `standards-drawings-ingested` fixture - I confirmed that file carries `drawings[0].source == "pdf_ingest"` and escapes the assertion only by accident of nesting. Switching the glob to `rglob` to fix (a) would immediately fail on (b).
  - Fix: Pin the five pre-1.4.0 fixtures by name - `angle-not-length`, `cover-blind-tap`, `mixed-units`, `plate-stack`, `shaft-bore` - assert each still carries sheets with no `source` key and loads with `source is None`, and assert the set of named fixtures is exactly the five contracts/ir-additions.md section 4 lists. That makes the gate say what it means and leaves T069's stamped fixture legal.
- **reviewer/src/swreview/checks/standards/drawing.py:719** (rules) - `elif compared:` emits `passed(rule, document_id, [scope.itself()])` whenever any single comparison succeeded, so a drawing whose only revision table's cells could not be read still reports the DOCUMENT as checked for `standards.drawing.revision_matches` as long as one referenced model agreed. contracts/rules.md's Checked column is conjunctive: "A revision table exists, every table agrees with the drawing property, and every referenced document's property agrees with it." Unlike the per-subject checks (where `outcome()` passes on the subjects that were graded), this check's only subject is the whole document, so the pass row claims more than was established. `test_a_table_whose_cells_could_not_be_read_is_unresolved` asserts only that `"warn" not in outcomes` and so does not pin this.
  - Fix: Track which of the three comparisons actually completed and withhold the pass when a table was unreadable - e.g. keep `compared` but add `tables_all_read` and require both for `passed`. Alternatively keep the pass but say in its coverage text what was compared and what was not. Either way tighten the test to assert the exact outcome set.
- **reviewer/src/swreview/checks/standards/drawing.py:855** (rules) - The blind-sheet reason calls `scope.why(sheet.id, "drawing_sheet_views", "drawing_view")`, but per contracts/ir-additions.md 3.3 a `drawing_view` gap's `entity_id` is a DrawingView id (`dvw:NNNN`), never a sheet id, and `find_gap` matches on `gap.entity_id == entity_id`. The `drawing_view` kind can therefore never match here, so the reason silently falls back to `NO_GAP` ("the dump recorded no gap for it") even when the dump did explain itself per view. The hard rule is that an unresolved row names the reason; this one names the wrong absence.
  - Fix: Drop `"drawing_view"` from that lookup (a sheet-scoped gap is `drawing_sheet_views`), or walk the sheet's views and fold in their `drawing_view` gap notes explicitly.
- **reviewer/src/swreview/checks/standards/drawing.py:350** (rules) - `Drawing.ingested` is every `package.drawings` row whose `document_id` matches, with no filter on `source`. `DrawingSheet.source` is a three-valued Literal that admits `"native"` (contracts/ir-additions.md section 1, and T012 pins that it accepts it), so a row stamped `"native"` in `drawings[]` would be counted into the mixed-drawing unresolved row and rendered by `INGESTED_SHEETS` as "PDF-ingested evidence (Sheet1 (source 'native'))" - a sentence that contradicts itself. contracts/rules.md's per-sheet rule is that every sheet record "carries its own `source`"; this path infers the source from which array the row landed in instead of reading it.
  - Fix: Filter `ingested` on `sheet.source != "native"` (null and `"pdf_ingest"` both count as ingested, per section 4) and add a test for a `drawings[]` row stamped `"native"`, so the array membership is not the sole evidence of provenance.
- **reviewer/src/swreview/checks/standards/drawing.py:152** (rules) - DRY (CLAUDE.md asks that repetition be flagged aggressively). `dimension_subject`, `annotation_subject`, `note_subject` and `_sheet_subject` are four bodies identical but for `type_name` and how `name` is built, and `dimensions_not_overridden` (lines 424-509) and `annotations_not_dangling` (lines 531-582) are the same fifty-line offenders/observed/passing/unknown loop with two predicates swapped. The module's own header defends keeping these subjects out of `results.py` (correct, they are drawing-only), but that argument does not justify four copies inside one file.
  - Fix: One `_row_subject(row, type_name, sheet, view, configuration, document_id, *, name=None)` replacing the four constructors, and factor the shared loop into a helper taking the flag accessor, the gap kind and the observed-text formatter. Keep the two check functions as thin, explicitly-named wrappers so the catalogue mapping stays readable.
- **reviewer/tests/unit/test_pdf_parser.py:96** (rules) - Deviation from the task text, no action needed beyond recording it: T067 names `reviewer/tests/unit/test_pdf_drawing.py` and `test_package_builder.py` as the files to write the ingest-stamp tests in. Neither file exists; the tests landed in `test_pdf_parser.py` and `test_ingest_package.py`, which are the real test files for `ingest/pdf_drawing.py` and `ingest/package_builder.py`. Correct call - creating the two named files would have split each module's tests across two places - but it should be reported as a deviation rather than left to be discovered. Relatedly, T068 asks for "the stamp in pdf_drawing.py AND package_builder.py" and `package_builder.py` received only a docstring (lines 268-273); that is right, because `pdf_drawing.parse_drawing_pdf` is the only constructor of `DrawingSheet` under reviewer/src (verified by grep) and the builder merely carries native rows over, but it too is a deviation from the literal task text.
  - Fix: Record both in the round-C report so nobody re-opens them. No code change.
- **reviewer/tests/unit/test_standards_drawing_rules.py:821** (rules) - `test_an_unread_note_does_not_hide_a_phrase_that_was_read` asserts only `"warn" in outcomes(...)`. That is a subset assertion: it passes whether or not the contract-required unresolved row for the null-text note is emitted, so it pins neither the current behaviour nor the contract's. It is the only test that exercises 'phrase found beside an unread note', which is exactly the case the must-fix above gets wrong - the test is why the deviation went unnoticed.
  - Fix: Assert the whole bucket set (`outcomes(results, NO_ITAR_STATEMENT) == {"warn", "unresolved"}`) and that the unresolved reason names `dnt:0002`; add the twin case for a sheet with no type-1 view beside a carrying note.
- **reviewer/src/swreview/checks/standards/drawing.py:354** (rules) - `drawing_scope` classifies evidence by ARRAY MEMBERSHIP (`package.drawings` -> ingested, `drawing_records` -> native) and reads `DrawingSheet.source` only to word the message. `contracts/rules.md` says the decision is per sheet from the sheet's own `source`, and the 1.4.0 enum `DrawingEvidenceSource` admits `"native"` on `DrawingSheet` too. A `drawings[]` sheet stamped `"native"` (or any future value) would be reported as PDF-ingested evidence and produce a spurious 'not fully checked' unresolved row on an otherwise clean drawing.
  - Fix: Filter `ingested` on `sheet.source != "native"` (null still read as `pdf_ingest` per ir-additions section 4), and add a unit test for a `drawings[]` sheet whose source is `"native"` so the classification is decided by the field the contract names.
- **reviewer/tests/unit/test_standards_drawing_rules.py:527** (rules) - `revision_matches` is only ever tested against ONE referenced document. The contract's difference y is 'every sheet's revision tables AND every referenced document are compared', and T069's two-model case ('both revision disagreements are named in the one revision warning') is covered only by a golden, which is a much coarser instrument than a unit test and is owned by another task.
  - Fix: Add a test with two views referencing two different models at different revisions and assert one `warn` row whose observed text names both model file names and both revisions.
- **reviewer/src/swreview/ingest/package_builder.py:274** (rules) - T068's package_builder half is a docstring only, and the behaviour the docstring claims - 'a sheet carried over from the native package keeps the source it already recorded, because this function did not write it' - has no test. `_drawings` copies `native.drawings` verbatim, so a regression that re-stamped carried-over sheets would be caught by nothing; the existing native-wins tests (test_ingest_package.py:387+) never assert `source`.
  - Fix: In `test_ingest_package.py`, extend the native-package fixture with one `drawings[]` sheet carrying `source="native"` (or null) and assert the built package still records that value rather than `pdf_ingest`.
- **reviewer/src/swreview/checks/standards/drawing.py:360** (rules) - DRY: `evaluate_drawing` is the third verbatim copy of the scope dispatch loop (`for rule in RULES.values(): if rule.scope == SCOPE: assert rule.fn is not None; results.extend(rule.fn(scope))`) - see `checks/standards/assembly.py:316` and `part.py:238`. Three copies is where the registry should own it; `by_scope` already exists for exactly this partition. The bare `assert` for the registry invariant is also stripped under `-O` in all three.
  - Fix: Add one `evaluate_scope(scope_obj, scope_name)` (or `RULES.dispatch`) to `checks/standards/registry.py` raising a real error for an unbound `fn`, and make the three entry points one line each. Cross-file, so coordinate with the owners of assembly.py/part.py rather than editing them mid-stage.
- **reviewer/tests/unit/test_ingest_package.py:245** (rules) - `test_a_sheet_written_before_the_stamp_existed_loads_with_no_source` globs `golden/fixtures/*/package.json` only, so it does not reach the new nested `golden/fixtures/standards-drawings/*/package.json` fixtures despite its docstring's broad claim about 'the golden fixtures'; and `assert with_sheets >= 5` is an unexplained magic floor that will drift. It also hard-asserts that NO top-level fixture may ever carry a `source` key, which will fail the day a legitimately stamped fixture is added at that level.
  - Fix: Either restrict the docstring and the glob to the pre-1.4.0 feature 001/002 fixtures by name, or use `rglob` and assert per fixture that a sheet's source is null OR `pdf_ingest` (never `native`), and name the five fixtures the floor stands for.
- **reviewer/tests/unit/test_pdf_parser.py:96** (rules) - Naming deviation worth recording: T067 names `reviewer/tests/unit/test_pdf_drawing.py` and `test_package_builder.py`; the tests were added to the existing `test_pdf_parser.py` and `test_ingest_package.py` instead. Those are the repository's existing test files for `ingest/pdf_drawing.py` and `ingest/package_builder.py`, so the choice is right, but it is a deviation from the task text that the hand-off should state rather than leave for a reader to discover.
  - Fix: No code change; record the deviation and its reason in the T067/T068 completion note.
- **reviewer/tests/golden/test_standards_goldens.py:755** (goldens) - `test_no_drawing_check_passes_or_fails_on_the_unread_note` cannot see a drawing check that FAILS. `buckets_of()` (line 156) reads `result["coverage"]`, and coverage never carries `failed` or `warned` - verified against standards-seeded.yml: coverage buckets are {checked, out_of_scope, skipped, unresolved} while `result["checks"][].buckets` are {checked, failed, out_of_scope, skipped, unresolved, warned}. So `assert "failed" not in reached` (line 766) is dead. Worse, a check that fails on a document emits NO coverage row for it, so `reached` comes back empty and `assert reached <= {"checked", "skipped"}` (line 770) is vacuously true. If `standards.drawing.revision_matches` started failing on standards-unknown's drawing, this test would still pass. T070a's sentence "no drawing check passes or fails on it" is therefore only half measured. The sibling `test_every_drawing_check_grades_the_drawing_of_both_compliant_fixtures` (line 589) already got this right with its `assert reached, (case, check)` guard.
  - Fix: Read the buckets off `result["checks"]` (which does carry `failed`/`warned`) rather than `result["coverage"]`, and add the non-empty guard: `assert reached, check` before the `<=` comparison, so an empty bucket set is a failure rather than a pass.
- **reviewer/tests/unit/test_ir_golden_fixtures_load.py:351** (goldens) - The golden-tree gate was correctly EXTENDED by name for the new `standards-drawings` group (DRAWING_GOLDEN_GROUP/DRAWING_GOLDEN_CASES at lines 94-113), but the companion `REWRITTEN_BY_THE_DRAWING_CHECKS` allowance (line 114) is looser than it needs to be: it is unioned into `allowed` (line 351) and subtracted from `drifted` (line 358) with no constraint on the git status. `golden_tree_status()` strips the status to a bare token, so ANY status on those five baselines now passes - `M`, but also `D` (a deleted standards-seeded.yml) and `R` (a renamed one). Under the round-B gate a `D` on a standards-* baseline failed. The five paths genuinely had to move for T069a/T070/T070a, so the exception itself is justified and named; only its breadth is not.
  - Fix: Pin the allowed status: `stray = [... if path not in allowed_untracked and (path, status) not in {(p, "M") for p in REWRITTEN_BY_THE_DRAWING_CHECKS}]`, or simply add `and status == "M"` to the `REWRITTEN_BY_THE_DRAWING_CHECKS` branch in both comprehensions, so a deleted or renamed baseline of this feature's own still fails.
- **reviewer/tests/golden/fixtures/standards-drawings/generate_package.py:154** (goldens) - DRY: the type-1 sheet-format pseudo-view is hand-built in FIVE fixture generators - standards-drawings:154 (`sheet_format_view`), standards-unknown:157, standards-compliant:92, standards-compliant-flat:83, standards-seeded:136 - and the revision-table shape `(("ZONE", "REV", "DESCRIPTION"), ("A1", rev, ...))` in six places (standards-drawings:171 and :227, standards-compliant:103, standards-compliant-flat:94, standards-seeded:164, standards-unknown:166). Both are load-bearing: T069a and T070a each state in their own words that a sheet missing its type-1 view makes `standards.drawing.no_itar_statement` unresolved and silently changes what the fixture proves. Five independent copies of exactly the construct whose omission is a silent failure is the repetition `tests/checks/standards_fixtures.py` exists to prevent - its own docstring says "the three things all four of them do ... are here once (constitution Principle V)".
  - Fix: Move `sheet_format_view(sheet_name, *notes)` and `agreeing_table(revision)` from standards-drawings/generate_package.py into `reviewer/tests/checks/standards_fixtures.py` beside `card()` and `write_fixture()`, export them, and import them in all five generators. Regenerate all six packages and confirm byte-identical output (they are today: I re-ran every generator into a temp dir and all six package.json files matched).
- **reviewer/tests/golden/fixtures/standards-seeded/generate_package.py:240** (goldens) - The seeded revision violation is not isolated. T053 seeds exactly one `revision_matches` defect - "a revision table disagreeing with the drawing's revision property" - but the referenced assembly is built with `card(PROFILE)` (line 240) and therefore carries no revision property at all, so the one finding names TWO disagreements: standards-seeded.yml reports `... the revision table drv:0001 on sheet 'Sheet1' states 'AC' ... and the MeridianRev property of MR-90001.SLDDRW states 'AB'; the MeridianRev property of MR-90001.SLDDRW states 'AB', and MR-20001.SLDASM, which it references, states no MeridianRev property`. The finding count stays at one so T070's SC-001 assertions hold, but TestSeeded asserts nothing about the observed text, so the finding would still be produced (and every test would still pass) if the seeded table agreed with the property. The seeded defect is unpinned. Note the parallel test for the drawings group, `test_the_revision_warning_names_the_table_reading_and_the_property` (test_standards_goldens.py:834), is exactly the missing assertion.
  - Fix: Give the referenced assembly `card(PROFILE, revision="AB")` so the only disagreement is the seeded table-versus-property one (a no-op for the other fifteen checks), regenerate standards-seeded's package.json and baseline, and add to TestSeeded an assertion that the revision finding's `observed` names both `'AC'` and `'AB'` and the `row 0 from the end, column 1` cell.
- **reviewer/tests/golden/fixtures/standards-drawings/generate_package.py:455** (goldens) - `build_no_views` writes a sheet with `views=()`, so that sheet records no type-1 sheet-format view either - and standards-drawings-no-views.yml consequently carries a SECOND unresolved row, `standards.drawing.no_itar_statement ... it recorded no type-1 sheet-format view`. T069's acceptance for this case is only about the model comparison ("the model comparison is unresolved naming the missing referenced document, with nothing crashing"), so the extra unresolved row is confound noise from the same missing-pseudo-view mechanism that T069a and T070a both explicitly warn makes a fixture prove something other than what it exists to prove. The no-views case is the one place where that mechanism is left in.
  - Fix: Either keep the sheet truly view-less and add a golden test asserting that the no_itar row is the missing-pseudo-view one and is expected, or - cleaner and consistent with every other case here - give the sheet the `sheet_format_view("Sheet1", BENIGN_NOTE)` pseudo-view only (still no model view, so the drawing still references no document), regenerate, and confirm the only unresolved row left is `standards.drawing.revision_matches` naming `references no document`.
- **reviewer/tests/unit/test_ir_golden_fixtures_load.py:351** (goldens) - The gate was extended by name for the new golden (correct), but REWRITTEN_BY_THE_DRAWING_CHECKS (line 114) additionally exempts seven of this feature's own paths from BOTH the stray list and the drifted list, in ANY git status. That is broader than the tasks need: a deleted baseline (' D standards-seeded.yml'), a renamed one, or a conflicted one now passes the gate that previously failed on any status other than '??'. The exemption is also derived from all five NEW_GOLDEN_CASES, so standards-multiplicity.yml is excused under a task text (T069a/T070/T070a) that does not touch it.
  - Fix: Keep the named set but pin the status: build the exemption as a mapping of path -> expected status 'M' and require `status == "M"` for those paths, so a deletion or a rename still lands in `stray`. Optionally split the set into the four the drawing tasks rewrite and the one T066 moves, each with its own one-line reason, so the allowance still expires on its own at commit time.
- **reviewer/tests/golden/test_standards_goldens.py:1131** (goldens) - On standards-drawings-no-views the baseline shows standards.drawing.no_itar_statement unresolved with the reason 'dsh:0001 Sheet1: it recorded no type-1 sheet-format view ...', but no test pins it. test_the_no_views_drawing_still_runs_its_own_checks only asserts findings == [] and that dimensions/annotations are skipped. If the export-control evaluator regressed to 'skipped' (or 'checked') on a sheet with no notes and no type-1 view — which contracts/rules.md forbids: the skip condition requires every sheet to have recorded a type-1 view — this case would go on passing, and it is the only fixture in the suite that exercises that branch.
  - Fix: Add one assertion in the no-views block: `assert buckets_of(drawings_no_views, NO_ITAR).get("unresolved") == [drawing_id]` plus `assert "no type-1 sheet-format view" in reasons_for(drawings_no_views, NO_ITAR, "unresolved")`, mirroring the model-comparison assertion right below it.
- **reviewer/tests/golden/test_standards_goldens.py:490** (goldens) - In standards-seeded the revision warning F-003 observed carries TWO disagreements: the seeded table-vs-property one ('AC' vs 'AB') and an unseeded second one, 'the MeridianRev property of MR-90001.SLDDRW states AB, and MR-20001.SLDASM, which it references, states no MeridianRev property' (the referenced assembly's card has no revision property). T070's assertions on this fixture are count-only (SEEDED_FINDINGS[REVISION] == 1), so if the seeded table/property disagreement were removed the finding would still appear from the model comparison and every test would stay green — the seeded cause is not isolated, and T053's 'one violation of each check' is overshot inside the finding.
  - Fix: Add the observed-content assertion the drawings-seeded case already has, e.g. in TestSeeded: `observed = findings_by_check(seeded)[REVISION][0]["observed"]; assert "'AC'" in observed and "'AB'" in observed`. If the extra disagreement is unwanted rather than merely unasserted, that is a T053 fixture change (give MR-20001 the revision property) and should be raised with the fixture's owner before the round is committed, since it rewrites standards-seeded's package and baseline.

## Feature 006 round D (2026-09-17)

- **extractor/SwReview.AddIn/Review/SwReviewDump.cs:87** (host) - `result.Package.CutListItems?.Count` reports null ("the count is unknown") for a standards dump whose cutlist phase RAN and found nothing. `EvidencePackage.OmitEmptyAdditiveArrays()` (Ir/EvidencePackage.cs:153-163) nulls an EMPTY `CutListItems`/`DrawingRecords` on the way into `PackageSerializer.Serialize`, and `Write(options)` mutates the same instance the summary then reads - so a plain (non-weldment) part or assembly yields `counts.cut_list_items: null` on `standards.extracted`, which the page renders as absent rather than as zero. The comment on lines 84-86 asserts the opposite ("Null when the phase did not run"), and `StandardsHostTests.ACountTheDumpDidNotReportIsNullRatherThanZero` cannot catch it because it drives a fake `IReviewDump` and never goes through the serializer. Contract D7 says the counts "name what the two new phases produced"; unknown-instead-of-zero misnames it, and it is the constitution's gap rule inverted (a known fact reported as unread).
  - Fix: Option A (recommended): read the truth from the phase rows rather than from the array - pass `cutListItems: PhaseRan(result.Package, "cutlist") ? (result.Package.CutListItems?.Count ?? 0) : (int?)null` and the same shape for `drawing`, which is exactly the source `contracts/ir-additions.md` section 6 makes authoritative. Effort: ~10 lines plus one test that runs a real (headless) package through the serializer. Risk: low; no other caller reads these two fields. Option B: capture the counts before `Write` nulls them - cheaper but depends on mutation order, which is the trap that produced this. Option C: do nothing and correct the comment to say "null also when the phase ran and found none" - honest, but leaves the page unable to distinguish "no cut list" from "not read", which is the distinction the Standards tab exists to keep. I recommend A; it is the explicit-over-clever reading and matches your preference for handling the edge case rather than documenting it away.
- **extractor/SwReview.AddIn/Review/RunFolders.cs:213** (host) - `CarriesSuffix` decides from the folder name alone whether a folder is a check run, so a REVIEW run folder of a document whose own name ends in the suffix is answered as a check. `DocumentName("C:\\parts\\fit-check.SLDPRT")` is `fit-check`, the review folder is `20260916-101532-fit-check`, and `NewestCheckFolder(root, "-check")` returns it; the collision-digit branch extends this to `...-fit-check-2`. `init` then names a review folder as `latest_check`, `CheckPaneHost.NewestOnDisk` records it, and `report.open`/`folder.open` will open it. The T071 tests cover `-check` vs `-standards`, a bare review folder, `-remodel` and `-terminal`, but not a document name that itself ends in a reserved suffix. Impact is bounded (the page's `loadLatestCheck` swallows the 404 and clears the state), but the host has answered a question wrongly and the tab's run directory points at a review.
  - Fix: Option A (recommended): make the name unable to collide - in `DocumentName`, strip or fold a trailing reserved suffix (`-check`, `-standards`, `-remodel`) so no review folder can end in one, and add the T071 row that proves it. Effort: ~8 lines plus two tests. Risk: low (no golden reads a run-folder name); it does change the folder name for such documents, which is a behaviour change to feature 002/003 naming. Option B: probe `check.json` on the single winning candidate only (one file read, not one per sibling, so the contract's stated objection does not apply) - but this CONFLICTS with T078's requirement that "a run folder created before a ProfileInvalid refusal renders as an empty run", since such a folder has no check.json, so I recommend against it. Option C: do nothing and record the ambiguity in the XML doc; defensible, since the failure is a cleared tab rather than a wrong result. I lean A, but it touches shared naming - your call.
- **extractor/SwReview.AddIn.Tests/StandardsHostTests.cs:184** (host) - `CheckPaneHost.NewestOnDisk` (Review/CheckPaneHost.cs:334-361) makes three load-bearing promises in its doc comment and none is asserted anywhere: that the disk-read folder is NOT handed to `RegisterLatestRun` (otherwise `entity.show` and the Ask tab move off the engineer's current work), that it does NOT become `LatestCheck`, and that it IS recorded so `report.open`/`folder.open` answer for it. `InitFindsTheNewestStandardsRunOnDiskWhenThisSessionHasRunNone` only reads `latest_check.run_dir`. A future edit that swapped the record for a `TrackCheck` call would be silently green.
  - Fix: Add to that test: `Assert.Empty(world.Registered)`, `Assert.Null(world.Host.LatestCheck)`, and a `folder.open {run_id: <folder name>}` round trip asserting `folder.opened` succeeds for the disk-read id. Effort: ~12 lines in the existing world, no production change. Risk: none. Recommended - this is the invariant that keeps a restart from moving the pane's latest run.
- **extractor/SwReview.AddIn.Tests/StandardsHostTests.cs:750** (host) - FR-001/FR-002's C# half - "no profile value ever enters this process" - is proved only indirectly: `WriteProfile` defaults to a comment and one test writes unparseable YAML. Nothing asserts that no byte of the profile file reaches a posted message. The Python side has `test_standards_no_company_values.py` doing exactly this measurement; the add-in side, which is the process that holds the path, has no equivalent.
  - Fix: Add one test: write a profile containing a distinctive sentinel (e.g. `vault_root: \\\\SENTINEL-VAULT-VALUE`), run `ready` and `standards.start`, and assert no posted message contains the sentinel while `init.profile_path` does contain the path. Effort: ~15 lines. Risk: none. Recommended - it converts the design's headline claim from prose into a measurement, and it is the one hard rule whose violation would be unrecoverable (a public repo).
- **extractor/SwReview.AddIn/web/shared/check-page.js:256** (host) - `loadLatestCheck` sets `'The check from this session, as it stands now.'` - wording that was true when `latest_check` could only be in-process state. T072 makes `init` answer with the newest folder on disk from a PREVIOUS session (the whole point of SC-009/AS-17), so after a restart the shared page now states something false to the engineer, on both check tabs. The file is another agent's (T030/T079), but the sentence was made wrong by this change.
  - Fix: Option A (recommended): have `init` distinguish the two - either a `from_this_session: bool` beside `latest_check`, or let the page compare `latest_check.run_dir` against the run it started, and say "The most recent check in this run folder" for a read-back. Effort: ~6 lines host-side plus a page line. Option B: soften the shared sentence to something true in both cases ("The latest check, as it stands now.") - one word, zero risk, loses the distinction. Option C: do nothing. I recommend B now and A only if the page needs the distinction for anything else; either way, hand it to whoever owns check-page.js so two agents do not edit it at once.
- **specs/002-task-pane-assistant/contracts/settings.schema.json:24** (host) - The feature 002 settings contract was edited to add `standards_profile_path`, but no task authorizes it: T076 names only `StandardsHost.cs`, `UserSettings.cs` and `SwReviewAddIn.cs`, and T096 - which enumerates every other-feature contract this feature updates (chat-api.md, pane-host-messages.md, model-check.md, cli.md, README.md) - does not list settings.schema.json. Your binding rule is "edit contracts only where a task says so". The edit itself is correct and unavoidable (the schema has `additionalProperties: false` and `UserSettingsTests` runs `SettingsContract.AssertValid` over a saved file, so omitting it would fail), and the row is minimal, optional and correctly documented as absent in pre-006 files.
  - Fix: No code change. Record it as an explicit deviation in the round's report ("T076 also edited specs/002 settings.schema.json because SettingsContract.AssertValid refuses an unknown key; tasks.md assigns this to no task") and, if you want the paper trail to match, raise it so T096's list can pick it up. I recommend keeping the edit and reporting the deviation rather than reverting.
- **extractor/SwReview.AddIn/Standards/StandardsHost.cs:198** (host) - `Start` and `ModelCheckHost.StartCheck` (Model/ModelCheckHost.cs:169-266) are ~55 lines of the same body: the `CreateFor*` call with its `RunFolderFailed` try/catch, the "extracting" status, the `Dump.Run` call with its `ExtractionFailed` try/catch, `TrackCheck`, the gaps-aware "ready" status, and the `{run_dir, document, configuration, counts, gaps}` envelope. Only the profile, the nouns and the count keys differ. This is the second-largest copy in the two hosts after the plumbing `CheckPaneHost` was extracted to stop copying. Against it: plan.md Structure Decision 3 explicitly lets each host keep "its own start verb, refusals and record type", the class comment calls the order "not negotiable", and your own rule is explicit over clever.
  - Fix: Option A (recommended, small): move only the mechanical middle into `CheckPaneHost` - e.g. `RunDump(id, document, profile, Func<string,string> folderName, Action<DumpSummary> reply)` or a `CheckDumpStep` helper - leaving each host's refusal list, verb and count map where they are. Effort: ~40 lines moved, one new test; risk: touches the already-green ModelCheckHost path, which must still pass `ModelCheckHostTests` unedited. Option B: do nothing - the duplication is sanctioned by the plan, each host's order stays readable in one place, and a third check tab is not planned. Effort zero; cost is that a fix to the dump-failure path must be made twice. Given you asked me to flag repetition aggressively but also to respect the plan's recorded decision, I lean A but would not block on it - tell me which you want before anyone edits ModelCheckHost again.
- **extractor/SwReview.AddIn/Review/RunFolders.cs:126** (host) - The XML doc states "**Nothing here throws.** Every caller is a page message" four lines above a guard clause that throws `ArgumentNullException(nameof(suffix))` (line 134). The claim is what a reader relies on when calling this from `init`; it is only true for `runRoot`, which returns null for blank/missing/unlistable.
  - Fix: Either narrow the sentence ("Nothing here throws for anything on disk; a null suffix is a programming error and throws") or drop the guard and treat a null suffix as `return null`, which is what `CheckPaneHost` already does one level up (it returns early when `LatestCheckSuffix == null`). Effort: one line. I recommend narrowing the sentence - the throw is the right answer for a caller bug, and this codebase's comments are load-bearing.
- **extractor/SwReview.AddIn/Standards/StandardsHost.cs:239** (host) - The comment at lines 245-247 says the refusal names the document through `RunFolders.DocumentName` specifically because `Path.GetFileName` throws on invalid path characters on .NET Framework and "a strange path must not turn a refusal into a crash" - but the line above it evaluates `document.Kind`, which is `System.IO.Path.GetExtension(Path)` (Review/ReviewHost.cs:36) and throws `ArgumentException` on exactly the same input. The protection the comment claims is defeated one expression earlier. (The same exposure pre-exists in `ModelCheckHost` and in `DocumentPayload`, so this is inherited, not introduced - but the new comment asserts otherwise.)
  - Fix: Option A: guard once - have `PageDocument.Kind` return null on `ArgumentException` rather than throw, which fixes all three call sites and keeps the refusal a refusal. Effort: ~4 lines in ReviewHost.cs plus a test; risk: touches feature 002 code, so confirm nobody else is editing that file this stage. Option B: leave the behaviour (an escaping exception is answered as `HostError` by `CheckPaneHost.Receive`, so SOLIDWORKS never sees it) and delete the misleading half of the comment. Effort: one line. I recommend B for this round and A as a follow-up, since the refusal-naming rule is about naming refusals, and `HostError` is a named answer even if it is the wrong name.
- **extractor/SwReview.AddIn/Review/RunFolders.cs:175** (host) - The same-second tie-break is `string.CompareOrdinal`, so the collision suffixes sort lexically: with ten or more runs in one second, `...-standards-9` beats `...-standards-10`, and `init` reads back the wrong run. Unreachable by hand (ten presses inside one second) but it is a silent wrong answer rather than a refusal, and the ordering rule is documented as "what puts a collision-suffixed ...-standards-2 after the ...-standards of the same second", which is only true below ten.
  - Fix: Option A: parse the trailing collision digits and compare them numerically, falling back to ordinal on the rest of the name. Effort: ~10 lines plus one test with `-2`, `-9`, `-10`. Option B: do nothing and state the bound in the doc comment ("ties beyond nine runs in one second order lexically"). Given your preference for edge cases over speed I marginally favour A, but B is honest and free - your call.
- **extractor/SwReview.AddIn/Settings/UserSettings.cs:175** (host) - `StandardsProfilePath` is persisted and round-tripped correctly (and `ReviewHost.Clone` preserves it across a `settings.save`, so the pane cannot silently drop it), but nothing surfaces it: `SendSettings`/`View(_settings)` does not carry it and the Settings page has no control for it, so the only way to point at a non-default profile is hand-editing %APPDATA%\SwReview\settings.json. quickstart.md step 6 instructs the engineer to "open **Settings** and confirm `StandardsProfilePath` points at the file", which cannot be done as shipped. No US3 task asks for the control, so this may be deliberate scope.
  - Fix: Option A: add the field to `View`/`settings.save` and one row to the Settings page - effort ~30 lines across host, page and tests, and it touches feature 002 files two other agents are in this stage, so it is a separate task rather than a patch here. Option B (recommended for this round): leave it and record the gap, so either a later task or the T100 workstation step covers it and quickstart step 6 is corrected. Option C: do nothing silently - not recommended, because the quickstart is the bring-up script the owner will actually follow.
- **extractor/SwReview.AddIn/Review/RunFolders.cs:213** (host) - CarriesSuffix false-positives on an ordinary REVIEW folder whose document name happens to end in the suffix. A review of `bracket-check.SLDPRT` creates `20260916-101532-bracket-check`, which is byte-identical in shape to a model check of `bracket`; likewise a review of `gauge-standards.SLDASM` creates `...-gauge-standards`. NewestCheckFolder returns it, `init` reports it as `latest_check`, CheckPaneHost records it, and `report.open` would then open the REVIEW's report.md as though it were a check report. The guarding test `TheScanNeverAnswersOneSuffixWithTheOthersFolder` (RunFoldersStandardsTests.cs) only seeds `20260917-090000-bracket`, so the case is not covered. The ambiguity is inherent to a names-only scan, so this is a design limit that is currently neither pinned nor documented.
  - Fix: Pick one and say so: (a) accept it, add a test that pins the known behaviour and one sentence in contracts/standards-check.md section 4 naming the collision; or (b) open only the single winning candidate (not every sibling, so the 'no check.json read per sibling' rule still holds) and fall back to the next candidate when it holds neither check.json nor an empty directory — noting that T078 requires a ProfileInvalid-refused folder, which has no check.json, to still be reported as an empty run.
- **extractor/SwReview.AddIn/Standards/StandardsHost.cs:11** (host) - DRY: StandardsHostOptions is a near-verbatim copy of ModelCheckHostOptions — Channel, RunRoot, Backend, CurrentDocument, Dump, RegisterLatestRun, EntityResolver, Opener, LogFolder, Now, Secrets, all with the same defaults and the same doc comments (only ProfilePath is new). The nine-member delegating facade (Checks/LatestCheck/TrackCheck/FindCheck/Receive/Post/PostStatus/DocumentChanged/Dispose, StandardsHost.cs:166-190) is copied line for line from ModelCheckHost.cs:138-162, and Start's tail — create folder, RunFolderFailed refusal, PostStatus('extracting'), Dump.Run, ExtractionFailed refusal, TrackCheck, PostStatus('ready'), Send(<type>.extracted {run_dir, document, configuration, counts, gaps}) — is the same skeleton as StartCheck with three strings and one DumpProfile changed. That is roughly 180 duplicated lines in a feature whose stated rule is DRY; plan.md Structure Decision 3 asks for a separate start verb and separate refusals, not a second options class and a second copy of the dump-and-reply tail.
  - Fix: Either (a) leave it and record the duplication explicitly as an accepted deviation on Structure Decision 3, the way the CheckRecord deviation is already recorded in CheckPaneHost's summary, or (b) lift the shared half: give CheckPaneHostOptions a public base that both host options derive from (or have each host take CheckPaneHostOptions plus its own extras), and move the create/dump/register/reply skeleton onto CheckPaneHost as one method parameterized by folder factory, DumpProfile, reply type and counts dictionary. Option (b) touches ModelCheckHost, but ModelCheckHostTests and ModelCheckPageTests are the unedited regression proof for exactly that.
- **extractor/SwReview.AddIn/Review/CheckPaneHost.cs:193** (host) - Doc drift introduced by the change: `Checks` is documented 'the checks this pane ran, oldest first' (and StandardsHost.cs:166 says 'the runs this host made, oldest first'), but NewestOnDisk now appends a run from a PREVIOUS session, and appends it last regardless of its timestamp, so the list can both contain a run this pane did not make and be out of chronological order.
  - Fix: Reword both summaries to 'the runs this pane can answer for, in the order it learned of them — this session's runs plus, after a restart, the newest run found on disk', or insert the disk record in timestamp order if callers depend on the ordering.
- **extractor/SwReview.AddIn/Review/RunFolders.cs:175** (host) - The same-second tie-break compares folder names with string.CompareOrdinal, so the collision suffix orders lexically: '...-standards-10' sorts before '...-standards-9'. Reachable only with ten or more runs inside one second, but the test TheScanSeesTheCollisionSuffixedFolderAndPrefersTheLaterOfTheSameSecond only exercises the -2 case, so the rule reads as proven when it is not.
  - Fix: Either compare the trailing collision integer numerically (parse the digits after the last '-' when the stem matches, default 1), or add a comment and a test naming ten-in-one-second as out of scope.
- **extractor/SwReview.AddIn.Tests/RunFoldersStandardsTests.cs:141** (host) - T071 asks that the registered standards folder be the one 'entity.show resolves document ids through'. TrackingAStandardsRunMakesItsFolderTheLatestRunAndTheRecordCarriesNoChatId stops at `new RunPackageIndex(() => host.LatestSession?.RunDirectory).DocumentPath(...)`, which re-spells the add-in's wiring in the test rather than driving it; no test sends `entity.show` at a StandardsHost whose resolver reads through a RunPackageIndex over a standards run folder.
  - Fix: Either extend that test to post `entity.show` at a StandardsHost wired to a RunPackageIndex-backed resolver over the registered folder and assert the resolved `full_path`, or add one sentence to the test's summary saying the resolver leg is covered once, host-agnostically, in ReviewHostTests.
- **extractor/SwReview.AddIn/Review/RunFolders.cs:140** (host) - NewestCheckFolder's summary states '<b>Nothing here throws.</b>', but the catch filter admits only IOException, UnauthorizedAccessException and ArgumentException; Directory.GetDirectories on .NET Framework can also raise System.Security.SecurityException. The escape is contained (CheckPaneHost.Receive wraps Dispatch and answers HostError), so this is a comment that overstates, not a crash path.
  - Fix: Either add SecurityException to the filter, or soften the comment to 'every failure this can see is answered none; anything else is answered by Receive's envelope as HostError'.
- **extractor/SwReview.AddIn/Review/SwReviewDump.cs:82** (host) - Scope/coverage note, not a defect: T076 names StandardsHost.cs, UserSettings.cs and SwReviewAddIn.cs, but ReviewServices.cs (DumpSummary gains cutListItems/drawingSheets) and SwReviewDump.cs (`result.Package.DrawingRecords?.Sum(d => d.Sheets.Count)`) were also edited — necessary for the `counts` row the contract requires, and shared with the Review tab's full dumps. That new Sum line has no test at all (SwReviewDump needs a SOLIDWORKS seat); the counts are only exercised through FakeStandardsDump. Separately, specs/002-task-pane-assistant/contracts/settings.schema.json was edited to add `standards_profile_path`, which is required because that schema sets additionalProperties:false and is authorized by tasks.md's 'other features' contracts are edited in the task that changes their shape' convention — but T076 does not say so in words.
  - Fix: Report both as deviations in the handover. If you want the Sum line covered offline, the cheapest probe is a unit test over a hand-built EvidencePackage asserting DrawingRecords null gives a null drawing_sheets count and two records of 1 and 2 sheets give 3 — otherwise state explicitly that it is [W] workstation-verified only.
- **reviewer/tests/unit/test_runner_reduced_profile.py:134** (page-tab) - `test_the_model_check_sentence_is_byte_identical` and `test_a_package_that_timed_no_phase_still_names_the_four_geometry_phases` both call `package(profile)` with `phases=None`, which produces `extractor.phases == []`. No dump the extractor performs produces an empty phase list (`PhaseLog.Rows()` always emits all eleven names), so the only two tests that pin the byte-identical wording exercise a package shape that cannot exist. The phase-row tests below them (`test_the_skipped_phases_are_read_off_the_packages_own_rows`, `test_a_phase_row_that_ran_is_never_reported_as_skipped`) hand-pick subsets that are likewise not what `PhaseOrder` emits.
  - Fix: Build the fixtures from the extractor's own `PhaseOrder` (`document, manifest, mate, feature, equation, cutlist, drawing, hole, fastener, face, body`), marking `ok`/`skipped` the way each profile's gates in `PackageWriter.Build` would, and keep the empty-rows case only as the explicitly-labelled legacy-package fallback.
- **reviewer/src/swreview/agent/runner.py:258** (page-tab) - "Which profiles are reduced" is expressed by negation (`if package.extractor.profile == "full": return`), while the C# half of the same requirement expresses it as an explicit map (`StepStrip.ReducedEvidence` / `StepStrip.ReducedProfile`, extractor/SwReview.AddIn/TaskPaneControl.cs:1322). The two sides of one rule now disagree in shape, and a future non-reduced profile added to the `Literal` (e.g. a probe-only profile) would silently be annotated as partial evidence. This is the "explicit over clever" rule.
  - Fix: Name the reduced set positively - e.g. a module-level `REDUCED_PROFILES: frozenset[str] = frozenset({"model_check", "standards"})` guarded by an assertion against `ExtractorInfo.profile`'s `Literal` members, or a `profile != "full"` check with a comment pinning the closed literal - so the Python and C# sides answer the same question the same way.
- **extractor/SwReview.AddIn/Standards/StandardsPage/standards.js:123** (page-tab) - `startCheck` is a near-verbatim copy of `Model/ModelCheckPage/check.js:68-97`: the same guard, the same `setChecking(true)/hideBanner()/setCheckState(...)` preamble, the same `send(...).then(setRunDirectory + setCheckState + api.call(route, 'POST', body))`, the same `.then(renderResult + setCheckState)`, the same `.catch`, the same trailing `setChecking(false)`. Only five things differ: the start message type, the start payload, the two progress sentences, the request body, and the extra `api.renderResult(null)` in the catch. Thirty duplicated lines of the start-verb pipeline is exactly the repetition the DRY hard rule and the 'page loads check-page.js' instruction aim at, and the divergent catch is already a place the two pages behave differently for no reason a reader can see.
  - Fix: Hoist the pipeline into `shared.create` as a declarative start verb - e.g. `startVerb: {message: 'standards.start', payload: {}, route: CHECK_ROUTE, body: function (extracted) {...}, extracting: '...', checking: '...', clearOnFailure: true}` - leaving each page only the parts that differ. If the two catch behaviours are both wanted, make `clearOnFailure` the explicit flag rather than a silent divergence between two copies.
- **extractor/SwReview.AddIn/Standards/StandardsPage/standards.js:217** (page-tab) - `contracts/standards-check.md` point 7 says of `rebuilt`: "The page renders it as a sentence **beside those findings**" - i.e. beside the `standards.assembly.rebuild_errors` / `standards.part.rebuild_errors` rows whose counts it qualifies. The implementation renders it once in the verdict header instead, and no test asserts either placement, so the contract sentence is unmet and undetected.
  - Fix: Either render the no-rebuild sentence on the two rebuild-error findings as the contract says (and keep or drop the header copy deliberately), or, if the header is the better place, raise it with the contract owner before the round closes rather than leaving the contract and the page disagreeing.
- **extractor/SwReview.AddIn.Tests/WebViewFallbackTests.cs:219** (page-tab) - The new Standards-tab fallback test asserts the panel says "Review, Ask and Model check tabs cannot be shown". That sentence was written for five tabs and is now stale on two counts: it omits Remodel (tab 5) and Standards (tab 6), and it is the text an engineer reads *on the Standards tab* when WebView2 is missing - the panel tells them the Standards tab is fine while they are looking at its failure. T080 asks for "the documented fallback panel on the new tab"; pinning the stale sentence makes the staleness part of the contract.
  - Fix: Update the fallback sentence in `TaskPaneControl.BuildFallback` to name the pages that actually need a renderer (or to say it generically), and update the four assertions together. If that is judged out of T080's scope, say so explicitly in the handover so the Polish phase picks it up rather than inheriting a pinned stale string.
- **extractor/SwReview.AddIn.Tests/StandardsPageInjectionTests.cs:167** (page-tab) - T078 requires "a CSP meta tag **byte-identical** to `pane-host-messages.md`'s", but `IndexHtmlCarriesTheContractCspMetaTag` compares through `Normalize`, which splits on `;`, trims each directive and collapses internal whitespace. A page that shipped `default-src  'none';script-src 'self' ;` would pass. (The shipped bytes do match today - I compared the two `content="..."` strings and they hash identically - so this is a weakened assertion, not a live defect.)
  - Fix: Assert `Assert.Equal(ContractCsp(), meta.Groups[1].Value)` directly and drop `Normalize`, so the test measures what the task asks for.
- **extractor/SwReview.AddIn/TaskPaneControl.cs:1243** (page-tab) - `Dispose(bool)` explicitly nulls and disposes `_reviewView` and `_terminalView` but not `_modelCheckView`, `_remodelView` or the newly added `_standardsView`. The lazy views are disposed only as children of their `TabPage` by `base.Dispose`, and the fields are left pointing at disposed controls, so `StandardsPageReady` and `StandardsChannel`'s `() => _standardsView` can still hand out a disposed WebView2 after disposal. This follows the tabs 4 and 5 pattern, so it is pre-existing, but T081 adds a third instance of it.
  - Fix: Either add `_standardsView = null` (with the other two) to the `disposing` branch so the three lazy views are let go symmetrically with the two eager ones, or add a comment at the eager pair saying why only those two are disposed explicitly, so the asymmetry is a decision rather than an omission.
- **reviewer/tests/unit/test_runner_reduced_profile.py:134** (page-tab) - test_the_model_check_sentence_is_byte_identical builds its package with phases=None, so extractor.phases is empty and _skipped_phases (runner.py:286) falls back to GEOMETRY_PHASES. The test therefore pins the fallback, not the path a real package takes. A model_check package written by the product records all eleven names in PackageWriter.PhaseOrder (extractor/SwReview.Extractor/Dump/PackageWriter.cs:57), with cutlist and drawing also 'skipped', so on the workstation the sentence becomes "...the cut list, drawing, hole, fastener, face and body or mesh phases were never run, so cut lists, drawings, holes, fasteners, faces and bodies are empty..." — not byte-identical to feature 003's. Every existing model_check fixture has phases: [] (checked all of reviewer/tests/golden/fixtures), which is the only reason test_chat_server.py, test_chat_checks_routes.py, test_rms_run.py and test_prerun_digest.py still pass unedited.
  - Fix: Add a case that builds a model_check package with the eleven real PhaseOrder rows (document/manifest/mate/feature/equation ok, cutlist/drawing/hole/fastener/face/body skipped) and assert the sentence the product will actually ship. Then make the choice deliberately: either accept the wider sentence and say so in the docstring, or have _skipped_phases intersect the recorded skipped rows with the phases the profile could have run so model_check keeps its four.
- **extractor/SwReview.AddIn.Tests/StandardsPageTests.cs:763** (page-tab) - CallStub records `String(url).replace('https://swreview.invalid/__backend', '')`, so ThePageCallsTheStandardsRouteItselfWithTheRunDirAndTheProfilePathFromInit asserts "POST /checks/standards" identically whether the page prefixed the proxy origin from init.backend.origin or issued a bare relative '/checks/standards'. The origin half of the task's requirement is untested here, and the token-in-header half is only a static grep for 'Authorization' and "'Bearer '" in the shared file (StandardsPageTests.cs:188-189), which is pre-existing code and would pass with no standards page at all.
  - Fix: Have the stub also record the raw url and request.headers, and assert the url starts with 'https://swreview.invalid/__backend' and that headers.Authorization equals 'Bearer 0FAKEtoken' (the stub's own token). One extra push and two Assert lines; the same hole exists in ModelCheckPageTests.cs:552 if you want to close both.
- **extractor/SwReview.AddIn.Tests/TaskPaneControlTests.cs:130** (page-tab) - Nothing anywhere asserts that activating the Standards tab in a *working* environment creates its WebView2 and navigates to StandardsPageUrl. TaskPaneControlTests only asserts the pre-activation state (placeholder note, StandardsPageReady false, no WebView2), and WebViewFallbackTests exercises activation only with a factory that throws, so no WebView2 is ever created there either. LoadStandardsAsync (TaskPaneControl.cs:677) could pass _remodelTab or RemodelPageUrl to AttachPageAsync and every test in the suite would still pass. The Remodel tab has exactly this test (RemodelPageContractTests.WithRemodelTab, line 742), so the pattern already exists.
  - Fix: Add a WithStandardsTab helper modelled on RemodelPageContractTests.WithRemodelTab: real environment factory, InitializeAsync, ActivateStandardsAsync, assert control.StandardsPageReady and that the loaded CoreWebView2.Source starts with TaskPaneControl.StandardsPageUrl and that the view is a descendant of the Standards TabPage.
- **extractor/SwReview.AddIn.Tests/TaskPaneControlTests.cs:116** (page-tab) - TheStandardsPurposeSaysItNeedsNoAiAndNoKey opens with Assert.Contains("No AI, no key", StandardsPurpose) and Assert.Contains("read-only", StandardsPurpose) where StandardsPurpose is the test file's own private const (line 47) — both assert a substring of a literal declared eight lines above, and pass with the product deleted. Only the third assertion (the tooltip) touches TaskPaneControl. The same sentence is additionally duplicated as a third literal in TaskPaneGuidanceTests.cs:70, so one wording change now needs three edits.
  - Fix: Point the first two assertions at TaskPaneControl.StandardsPurpose (the product constant), keeping the local literal only for the exact-equality tab/tooltip check, and have TaskPaneGuidanceTests reference the same one copy instead of declaring its own.
- **extractor/SwReview.AddIn.Tests/StandardsPageInjectionTests.cs:83** (page-tab) - T078 asks for a CSP meta tag byte-identical to pane-host-messages.md's, but IndexHtmlCarriesTheContractCspMetaTag compares Normalize(contract) to Normalize(page), which collapses runs of whitespace inside each directive and drops empty directives. A page shipping 'default-src  'none';; script-src 'self'; ...' would pass. (The two strings are in fact byte-identical today — I diffed them — and this mirrors ModelCheckPageInjectionTests.cs:78, so it is a convention worth tightening in both rather than a new defect.)
  - Fix: Assert.Equal on the raw captured group against the raw contract group, and keep Normalize only for the failure message; or leave as-is deliberately and drop 'byte-identical' from the test's own doc comment.
- **reviewer/src/swreview/exceptions.py:423** (routes-cli) - The standards fingerprint hashes the document only; the profile is deliberately excluded (`_standards_rows(package, document_id)` takes no profile, and the docstring says "What the digest cannot see is a change to the profile itself"). data-model.md section 5 -- which T084 cites by name -- says the standards fingerprint hashes "every input the check read for that document" and enumerates "and for the data-card check, the profile property names and their three states". The document half is over-met (the whole `custom_properties` card is hashed, a strict superset of the three states), but the profile half is absent, and nothing anywhere compares `check.json`'s recorded `profile.sha256` against the profile in force. Consequence: an engineer tightens `data_card.required` or `part_number.pattern`, re-runs, and an exception accepted under the looser profile stays `active` and silently waives a check whose requirement changed -- the direction SC-008 forbids. The docstring's own claim that "`check.json` carries the profile's path and hash for exactly that comparison" is not realized by any code path. No leak risk in fixing it: only the SHA-256 is stored, never the values.
  - Fix: Either (a) thread the profile into the digest -- give `fingerprint(...)` an optional `profile:` argument and have `_standards_rows` fold in the profile-derived inputs each check read for this document (the qualified data-card property names, the library prefixes matched, the part-number pattern), which is what data-model section 5 asks for; or (b) if the exclusion is the intended design, record `profile_sha256` on the standards `ReviewException` and have `ExceptionStore.refresh` flag `needs_review` when the profile in force differs -- and correct the docstring, which currently promises a comparison nobody makes.
- **reviewer/src/swreview/chat/server.py:816** (routes-cli) - `_standards_finding_row` is a near-verbatim copy of `_finding_row` (server.py:730): same `finding`/`severity`/`statement`/`observed`/`acceptable` keys, same six-line exception block. The docstring defends the copy on two differences, and only one is real. The key name (`rule_id` vs `check`) is real. The other is not: `_finding_row`'s `acceptable = rule is not None and rule.severity == "fail"` is exactly `waiver_invalidity(RMS_FAMILY, RULES, check) is None` under the new `family.py` reader, because `RMS_FAMILY.status_by_severity["fail"][0] == "demonstrated" == WAIVABLE_STATUS` (rms/registry.py:368). So the two functions differ in one string literal, in a feature whose hard rule is DRY and whose whole `CheckFamily` descriptor exists to stop exactly this.
  - Fix: Collapse to one `_family_finding_row(finding, exceptions, *, family, rules, id_key)` and have both call sites pass their family, its catalogue and `"rule_id"`/`"check"`. That also removes the second definition of "is this waivable" from the route layer, which is the drift `waiver_invalidity` was introduced to prevent.
- **reviewer/src/swreview/chat/server.py:1314** (routes-cli) - `_family_of` reads the record and returns `str(record.get("family", RMS_FAMILY.check_file_family))`, and `_read_any_check`/`_accept_exception` then branch on `== STANDARDS_FAMILY.check_file_family`, falling through to the rms path for everything else. `read_standards_check` guards its own family (checks/standards/run.py:451, `NOT_THIS_FAMILY`), but `read_rms_check` has no such guard, so a `check.json` carrying any third `family` value is rendered in the feature 003 `CheckResult` shape and accepted against the rms catalogue instead of being refused. The contract's rule is two-sided -- "**Neither family answers for the other**" -- and the CLI's guard is two-sided (`_run_family` + `WRONG_FAMILY` refuses anything that is not the command's own family), so the route is the odd one out.
  - Fix: Make the route's dispatch an explicit lookup over the known families (the `CHECK_FAMILIES` map cli.py:1341 already builds) and raise `UnknownCheck` naming the recorded family for anything else, keeping the documented one exception: an absent `family` key means the feature 003 model check.
- **reviewer/src/swreview/chat/server.py:1224** (routes-cli) - The `scope` refusal on `POST /checks/standards` raises a bare `ChatError`, so it reaches the page as the generic `error_class: "InvalidRequest"`. Every other refusal this route can make is named (`InvalidRunDir`, `InvalidPackage`, `MissingStandardsPhases`, `ProfileUnreadable`, `ProfileInvalid`, `UnreadableExceptions`), and "refusals are named" is a binding rule of this feature. The message itself is excellent; only its class is anonymous. Secondly, the check runs *after* `resolve_run_dir`, so a body carrying both a bad `run_dir` and a `scope` is reported as `InvalidRunDir` and the engineer never learns the field does not exist -- a body-shape error is cheaper to decide than a path rule and belongs first.
  - Fix: Give it its own `ChatError` subclass (e.g. `error_class = "NoScopeField"`, status 400) and move the `if "scope" in body` test above `resolve_run_dir`. Add the class assertion to tests/unit/test_chat_standards_routes.py:662, which today only asserts the status and the message.
- **reviewer/src/swreview/checks/rules/report.py:128** (routes-cli) - T091 names three files: "in `checks/standards/report.py`, the **waived-outcome path** ...; in `checks/standards/run.py`, the **exception lookup** and the **waived count** ...; and in `chat/server.py`, the accept route's **re-render**". Only the third landed where the task says. The waived-outcome wording was implemented in the shared `checks/rules/report.py` (`BoundTo`/`BOUND_TO`/`DEFAULT_BOUND_TO` and the two `_waived`/`_needs_review` rewrites), and `checks/standards/report.py` and `checks/standards/run.py` are untouched in the working tree (the waived count and `ReleaseVerdict.notes` were already landed in round A/B -- verdict.py:79,156,218). The shared layer is the right home under the DRY rule and the feature 001/003 goldens are protected byte-for-byte by `DEFAULT_BOUND_TO` reproducing the old sentences exactly (verified: full suite green, no golden moved), so this is a defensible deviation -- but it is an undeclared one, and a later reader following T091 will look in the wrong file.
  - Fix: No code change needed. Declare the deviation in the round report: T091's waived-outcome path lives in `checks/rules/report.py` because writing it in `checks/standards/report.py` would have been a second copy of wording the rms family already renders, and `checks/standards/run.py` needed nothing because rounds A/B had already landed the lookup and the waived count.
- **reviewer/src/swreview/exceptions.py:532** (routes-cli) - `assert document_id is not None, "refused above when the standards kind has no document"` carries a load-bearing narrowing for a value that is then passed to `_standards_rows`. `assert` is removed entirely under `python -O`, so under that flag the guard disappears and `_standards_rows(package, None)` would raise a `LookupError` reading "package has no document None" instead of the named `ValueError` raised twenty-five lines above. The `ValueError` at line 515 already makes the case impossible, so the assert is pure type-checker appeasement in a module whose refusals are all named.
  - Fix: Restructure so the narrowing is free: hoist the standards branch into its own `if kind == "standards":` block that takes `document_id` as a local already proven non-None (e.g. bind `document = document_id` inside the raise-guard), or use `typing.assert_never`-free explicit control flow. Either way drop the bare `assert` from a refusal path.
- **reviewer/tests/unit/test_cli_check_standards.py:427** (routes-cli) - `contracts/cli.md` lists seven exit-1 conditions and T086 requires a test per condition. Six have one each; the fourth -- "The profile file is **absent or unreadable**" -- is covered only by `test_an_absent_profile_file_exits_1_naming_the_path`. `load_profile` has two distinct unreadable paths that no test exercises from the command line: a non-`FileNotFoundError` `OSError` (profile.py:198, e.g. a directory at the path) and a non-UTF-8 file (profile.py:208, which is a `ProfileUnreadable` rather than a `ProfileInvalid` and is the easy one to get backwards). Both reach `_errors_as_exit_1(ProfileError)` only because `ProfileError` was added to `also`; a regression that made either a bare `Exception` would surface as a traceback rather than exit 1 and nothing would catch it.
  - Fix: Add one test pointing `--profile` at a directory and one at a file of invalid UTF-8 bytes, asserting exit 1, the path in the message, and that no run folder was written -- mirroring `test_a_refused_profile_writes_nothing_into_the_run_folder` (line 636 of the routes test).
- **reviewer/tests/unit/test_cli_exceptions_accept_standards.py:513** (routes-cli) - `test_the_two_commands_share_one_body` is tautological. `assert cli._accept_waiver_file.__module__ == cli.__name__` is true for any function defined in cli.py, and `rms_command.__doc__ != standards_command.__doc__` is true for any two differently documented functions. The test would still pass if `exceptions_accept_standards` were a full second implementation - which is precisely the thing FR-042 and contracts/cli.md ("one command body, two names") forbid and this is the only test claiming to guard it.
  - Fix: Monkeypatch `cli._accept_waiver_file` with a recorder and invoke both commands, asserting each delegates to it exactly once and that the only difference in the recorded call is `(family, rules)` - i.e. `(RMS_FAMILY, RULES)` versus `(STANDARDS_FAMILY, STANDARDS_RULES)` with identical run_dir/package/file/by/json_output.
- **reviewer/src/swreview/exceptions.py:169** (routes-cli) - The `_omit_null_document_id` wrap serializer has no test. Its docstring claims "an `exceptions.json` the accept command rewrites keeps the bytes it had before this field existed", but nothing asserts `document_id` is absent from a serialized geometry or feature-tree exception: the round-trip tests (test_exceptions_standards.py:253, :273) and test_cli_exceptions_accept_rms.py all pass with the serializer deleted, because `document_id: null` loads fine. Untested implementation carrying a byte-stability claim is exactly what drifts.
  - Fix: Add one test to test_exceptions_standards.py section 7: accept a geometry exception, `store.save()`, and assert `"document_id" not in json.loads(path.read_text())["exceptions"][0]` - plus the mirror, that a standards record does carry it.
- **reviewer/src/swreview/chat/server.py:1289** (routes-cli) - Half-applied accept. `accept_check_exception` writes and saves the exception in `_accept_exception` first, then calls `_recheck`, which for the standards family re-runs `run_standards_check` against the profile path recorded in `check.json`. If that profile has been moved, deleted or edited into an invalid state since the check ran, `_recheck` raises ProfileUnreadable/ProfileInvalid and the route answers 400 - but `exceptions.json` has already been written and `report.md` still says what the run said before the waiver. The page reports a failure while the store on disk disagrees. No test covers a re-render that fails after a successful write.
  - Fix: Validate the recorded profile before writing (call `load_profile` on the recorded path at the top of the accept path and raise ProfileRefused there), or re-render inside a try that retires the just-written exception on failure. Either way add a test that moves the profile between the check and the accept and asserts the store and the report end up consistent.
- **reviewer/tests/unit/test_cli_check_standards.py:401** (routes-cli) - `assert "standards" in message` cannot fail independently: line 400 already asserts `"--profile standards" in message`, which contains the substring. contracts/cli.md requires the phases refusal to name "the profile that produced the package", and that requirement is therefore unmeasured - a message that named only the remedy flag and never the offending package's own `extractor.profile` would pass. The same weakness appears at test_cli_exceptions_accept_standards.py:466/481, where `"standards" in message` is satisfied by the literal `accept-standards` in the refusal template.
  - Fix: Build the fixture package with a distinguishable `extractor.profile` (e.g. `model_check`) and assert that value appears in the message, separately from the `--profile standards` remedy string. For the family guard, assert on the quoted forms the WRONG_FAMILY template produces (`'rms'` and `'standards'`) rather than on bare substrings.
- **reviewer/src/swreview/cli.py:1070** (routes-cli) - The two entry points that contracts/cli.md says are the same evaluation report the graded documents in two different shapes: the route returns `documents_graded: [{id, kind}]` (server.py:796) while `check standards --json` returns `documents: [ids]` plus a parallel `document_kinds: {id: kind}` map. A consumer that reads one cannot read the other, and the split map is a second place the same pairing is expressed.
  - Fix: Emit `documents_graded` in the CLI payload with the same row shape the route uses (keeping `documents` only if `check rms`'s id-list key must be preserved for compatibility), and build it from one helper shared by cli.py and chat/server.py.
- **reviewer/src/swreview/cli.py:1060** (routes-cli) - DRY: `check_standards_command`'s payload/lines block duplicates `check_rms_command`'s (cli.py:948-975) almost line for line - the same `package`/`out_dir`/`session_file`/`report_file`/`check_file` payload rows, the same `for finding in run.findings: lines.append(""); lines += _finding_lines(finding)` loop, the same `coverage: {_counts_line(_coverage_counts(...))}` line and the same trailing `session:`/`report:` rows. About 35 lines are copied with the verdict header and the documents wording as the only real differences, so the "same human and JSON shapes the other check commands print" requirement is held by copy rather than by construction.
  - Fix: Extract the common tail - the five file-path payload rows and the findings/coverage/session/report line block - into one helper both commands call, leaving each command only its own header lines and its family-specific payload keys.
- **reviewer/src/swreview/chat/server.py:1275** (routes-cli) - Redundant work on the accept path: `_family_of` reads `check.json` once, then `_recheck` -> `_recorded_standards_check` -> `read_standards_check` reads it again, and `_standards_check` -> `standards_result` loads `package.json` a second time after `run_standards_check` has already loaded it. Three re-reads of files that were just read, on the interactive route. Harmless today at fixture size, but a standards package over a drawing-rooted run is the largest payload this backend builds.
  - Fix: Thread the already-read record through (`_recheck(check_dir, record)` instead of `_recheck(check_dir, family)`), and have `run_standards_check` return the loaded package on `StandardsCheckRun` so `standards_result` takes it as an argument instead of calling `load_package` again.

## Feature 006 round E (2026-09-17)

- **reviewer/tests/perf/test_standards_perf.py:76** (polish-code) - T092 and SC-011 both say "a fixture package representing a 200-component assembly with three drawings" - one package, one evaluation, under 2 s. The test builds four packages and times four run_standards_check calls against a single 2 s budget (_packages(), design_dirs, test at line 278), walking the 200 components four times over. The docstring argues the case honestly and the result has 7x headroom (0.278 s), so nothing is broken today; but the assertion is not the one the success criterion states, and on a slow shared runner it can go red while SC-011 still holds.
  - Fix: Either keep it and say so in the report (my recommendation - it is the pessimistic reading and it passes comfortably), or assert per root (`assert each < BUDGET_SECONDS`) and print the total, which measures exactly what SC-011 states while still exercising all four roots. Do not silently raise the budget.
- **reviewer/tests/perf/test_standards_perf.py:278** (polish-code) - T092 ends "the measured number is recorded on this task", and the standing rule forbids editing tasks.md, so the number currently lives nowhere: not in the test, not in a contract, not in the quickstart. A number nobody wrote down is not a measurement anyone can check next round.
  - Fix: Report the measured line verbatim to the user - `check standards over 200 components and 3 drawings (4 roots): 0.278 s, budget 2.0 s` - and ask whether they want it recorded on T092 in tasks.md (their call, since the agent may not edit it) or added to quickstart Scenario 10's paragraph, which already points at this test for the CI-side number.
- **reviewer/tests/unit/test_standards_no_llm.py:127** (polish-code) - Nothing proves the guard bites. `refuse_provider` is installed on providers.get, providers.call_tool and chat_server.FakeProvider, and all three tests pass - but they would pass identically if the patched names were no longer the seams a construction goes through (a rename, a new adapter built directly, a `from .providers import get` inside a future module). The whole module's force rests on an unasserted assumption.
  - Fix: Add one assertion inside the guard that it fires, e.g. `with guarded(): with pytest.raises(AssertionError): providers.get(ProviderName.OPENAI)` and the same for chat_server.FakeProvider, plus a socket case (`with pytest.raises(AssertionError): socket.getaddrinfo("api.example.invalid", 443)`). Three lines, and the module then proves its own instrument before using it.
- **reviewer/tests/unit/test_standards_no_llm.py:213** (polish-code) - The tab path's TestClient - and therefore create_app plus the app's lifespan - is built by the `client` fixture outside `guarded()`, so anything the backend constructs or reads at start-up is outside the proof; only the POST and the GET are guarded. SC-010's claim is about the tab path, and on a workstation the pane's backend start-up is part of it.
  - Fix: Build the client inside the guard for at least one test (a `guarded_client` helper that enters `guarded()` and then the TestClient context), or add a small test that creates the app under the guard and asserts watch.read == [].
- **reviewer/src/swreview/checks/standards/verdict.py:62** (polish-code) - T099 is the DRY task, and this change adds a fifth hand-maintained copy of one vocabulary rather than removing copies. The four session coverage buckets are already spelled out in `swreview/report/session.py` as the canonical `CoverageBucket` Literal, again in `agent/checklist.py:21` (COVERAGE_BUCKETS, same name, same four values), again in `tools/session.py:35` (ModelCoverageBucket/MODEL_COVERAGE_BUCKETS, same derivation), and again here as `CoverageBucketName`. Deriving the tuple from the local Literal removed three typed-out copies inside standards/report.py - a real improvement - but the Literal it derives from is itself the duplicate, and the docstring's claim that "a fifth bucket is added in one place" is not true of the product.
  - Fix: Either alias it (`CoverageBucketName = CoverageBucket` imported from swreview.report.session, keeping verdict.py's docstring about which buckets are session buckets), or - if verdict.py's deliberate zero-product-imports property is worth keeping - add one test asserting `get_args(CoverageBucketName) == get_args(CoverageBucket) == tuple(agent.checklist.COVERAGE_BUCKETS)` so the copies cannot drift. Ask the user which; the alias is the DRY answer, the test is the cheap one.
- **extractor/SwReview.Extractor.Console/Program.cs:225** (polish-code) - `StandardsProbeInteropMembers` documents itself as "Every interop member `probe standards` names to the gate, in the order the run first names it", and the T094 test asserts that literal list. Both are under-statements: the run also gates members it reaches through the shipped readers - ComponentTreeDumper's whole traversal, SwSession.Attach's ActiveDoc/GetType, SwDrawingReader.TableAnnotations (GetTableAnnotations), PersistRefService's GetPersistReference3 - none of which are in the list, and the first-seen order the observer records starts with the traversal's members, not with GetOpenDocumentByName. The gate log itself is correct (it prints the observer, not this list); only the claim about the list is wrong, which matters because the next reader will trust the comment when adding a probe.
  - Fix: Reword to what it is: "every interop member this file names to the gate directly - the members the shipped readers gate are additionally recorded by the observer and printed in the log" - and drop "in the order the run first names it" or reorder the array to match ExecuteProbeStandards' actual call order.
- **reviewer/.pytest-tmp-rev:1** (polish-code) - Two pytest basetemp directories from this stage are still in the working tree as untracked paths - `reviewer/.pytest-tmp-rev/` and `reviewer/.pytest-tmp-rev3/` - which the environment rule says to delete after the run. They are not mine (mine, .pytest-tmp-rev092, is removed) and they will otherwise reach whoever commits this round.
  - Fix: Whichever agent created them deletes them before the stage closes; `git status --porcelain reviewer` should show only the intended new fixtures and tests.
- **specs/006-standards-check/quickstart.md:423** (polish-code) - Scenario 9 was not updated for the refusal T095 added. contracts/cli.md now says `probe standards` refuses a `--doc` naming a document that is not already open (exit 1), and Program.cs:1537-1546 enforces it — but Scenario 9 still says only "Run it on each of the documents the probes need (the assembly ..., the weldment ..., the six-sheet drawing)". An engineer following the quickstart on the workstation will get exit 1 on the first document and have no line telling them why. This also silently diverges from `probe rms`, which does attach-and-open.
  - Fix: Add one sentence to Scenario 9: open each document in SOLIDWORKS first, because probe standards refuses a --doc that is not already open (SwSession.Attach would otherwise open it, and a run that opened a document cannot be the proof that it opened none). One line, and the [W] scenario then matches the shipped command.
- **extractor/SwReview.Extractor.Console/Program.cs:1537** (polish-code) - T099 is the DRY task of this very round, and this round added a fresh copy of an existing pattern. The "document must already be open" guard in ExecuteProbeStandards (1537-1546: `gate.Call("GetOpenDocumentByName", ...) is IModelDoc2` -> throw) is a near-literal copy of RunSuppressTest's block at 2865-2877, and StandardsProbeDocumentNotOpenMessage (358-361) is a second near-identical message builder beside SuppressTest.DocumentNotOpenMessage. Two copies of a safety check whose whole point is that it must not be forgotten is exactly the shape T099 exists to remove.
  - Fix: Extract one helper on Program, e.g. `RequireAlreadyOpen(SwGate gate, ISldWorks swApp, string documentPath, string message)`, and have both call sites pass their own message (the two rationales genuinely differ). Keep the two message constants, share the check. Cover it with one option test that the helper throws for a document GetOpenDocumentByName does not return.
- **extractor/SwReview.AddIn/Review/RunFolders.cs:1** (polish-code) - T099 requires confirming that the six consumed seams are "each unchanged and now have a second consumer". Two of the six are not unchanged: RunFolders.cs gained 150 lines and reviewer/src/swreview/exceptions.py 354 added / 21 removed, both in round D (f30e2b2) — exceptions.py widened FingerprintKind to a third kind, added STANDARDS_CHECK_PREFIX, a model_serializer and seven new IR imports. PaneActions.cs, web/shared/dom.js, SwEntityResolver.cs and FeatureSelection.cs are genuinely untouched since ebf6b34. Nothing in the working tree records that T099's confirmation was performed or qualified, so the task's central claim is currently unsupported for two of its six items.
  - Fix: Either report the qualification explicitly (both changes are additive extensions of the shared mechanism — a third fingerprint kind and a third run-folder kind — not a re-creation, which is what the constitution actually forbids), or, if "unchanged" is meant literally, treat it as a discrepancy and say so in the round report. Do not silently claim six-for-six.
- **reviewer/tests/perf/test_standards_perf.py:76** (polish-code) - The budget measures more than SC-011 and T092 state. SC-011 is "all sixteen checks over a package representing a 200-component assembly with three drawings ... under 2 s"; the test times four separate run_standards_check calls (assembly root plus one per drawing root), i.e. 201 + 3x202 = 807 document gradings, against the same 2 s. The reasoning in the module docstring is sound (a run grades only the opened root, so the design is four roots) but it is a stricter budget than the success criterion, and a future 3x slowdown would fail this test while the product still met SC-011. Measured today: 0.284 s, ~7x headroom, so nothing is red now.
  - Fix: Keep the four-root workload (it is the honest reading of "the whole wait"), but say in one line beside BUDGET_SECONDS that the budget is deliberately applied to the aggregate and is therefore stricter than SC-011's literal wording — so a future failure is triaged as "which reading moved" rather than "the product regressed". Alternatively assert per-root and aggregate separately.
- **reviewer/tests/unit/test_standards_no_llm.py:213** (polish-code) - The tab-path proof guards the request but not the backend's construction. The `client` fixture builds create_app(...) and enters the TestClient context (running lifespan startup) OUTSIDE `guarded()`; only the POST and GET inside the `with guarded()` block are watched. A key read or provider construction that happened at app construction or in a startup hook would not be caught, although FR-045's claim ("works on a workstation with no API key") covers standing the backend up as well as answering the route. The module's own docstring claims the backend is built with the product's own defaults so "the product's own factory is never reached", which overstates what the test measures.
  - Fix: Build the client inside the guard (e.g. a `guarded_client` fixture, or move create_app + TestClient entry into the `with guarded()` block for one additional test) so that app construction and lifespan startup are watched too. Keep the existing un-guarded run as the comparison baseline.
- **extractor/SwReview.Extractor.Tests/CommandLineOptionsTests.cs:487** (polish-code) - StandardsProbe_NamesEveryInteropMemberItReadsToTheGuard asserts a hand-typed array equals Program.StandardsProbeInteropMembers — a change-detector. It proves the list is distinct and that every name passes ReadOnlyGuard, which is worth having, but it cannot catch the failure it is written against: a gate.Call in the probe that names a member NOT in the list (the observer would then record a member the list never vouched for, and the gate log would print a name the guard test never checked). Today the coupling is held only by the private StandardsProbeMember class and reviewer discipline.
  - Fix: Make StandardsProbeMember internal and add one test asserting that every public const string on it appears in StandardsProbeInteropMembers (reflection over the class' fields) and vice versa. That turns the pairing into a checked invariant instead of a copy, and keeps the hand-typed array as the readable contract-order assertion.
- **reviewer/tests/perf/test_standards_perf.py:1** (polish-code) - T092 says the measured number "is recorded on this task", but this round's rules forbid editing tasks.md, so the measurement (0.284 s for 200 components and 3 drawings over 4 roots, budget 2.0 s, on this machine) is recorded nowhere in the repository. The number exists only in the -s output of a run nobody is obliged to repeat.
  - Fix: Hand the number up in the round report for whoever may edit tasks.md, and consider one line in the module docstring recording the first measurement and the machine it was taken on, so the trend the docstring argues for has a starting point.
- **reviewer/src/swreview/chat/server.py:777** (polish-code) - Asymmetry left by the T099 dedup: the standards row goes through the named wrapper `_standards_waiver_invalidity(check)` (865-874) while the RMS row now calls `waiver_invalidity(RMS_FAMILY, RULES, check)` inline. Same question, two spellings, three lines apart. (The behaviour change itself is correct — only "fail" maps to WAIVABLE_STATUS in RMS_FAMILY.status_by_severity, so `severity == "fail"` and `waiver_invalidity(...) is None` agree — and it is covered by tests/unit/test_chat_checks_routes.py:277,291.)
  - Fix: Either add `_rms_waiver_invalidity` mirroring the standards wrapper, or call waiver_invalidity inline in both rows. One spelling for one question.
- **reviewer/tests/golden/test_standards_goldens.py:1** (polish-code) - Housekeeping, not this round's code: three basetemp directories left behind by other agents are untracked in the working tree — reviewer/.pytest-tmp-rev/, reviewer/.pytest-tmp-rev092/, reviewer/.pytest-tmp-review-polish-docs/. They do not break the golden gate (test_ir_golden_fixtures_load only watches reviewer/tests/golden) but they are junk in `git status` before a commit. My own .pytest-tmp-review-polish-{code,perf,spot} are removed.
  - Fix: Whichever agent owns those three should delete them at the end of its run; they must not reach a commit.
- **reviewer/tests/checks/matched_pair.py:22** (polish-docs) - The module docstring claims "`_checked_folders` asserts all five, so a fixture that quietly stopped exercising a list fails here rather than passing as an equality between two packages that test nothing." Nothing in the test suite ever calls `matched_package`, so `_checked_folders`, `_check_names` and `_revision_table`'s guards run only inside `generate_package.py` when a human regenerates. test_standards_goldens.py imports only `UNNUMBERED_PART` (line 32) and grades the committed package.json off disk. If someone edits profile-a.yaml's `skip_prefixes`, `sketch_exempt_prefixes` or `part_number.pattern`, the committed fixture silently stops exercising that list and both equality tests keep passing - the exact failure mode the docstring says is covered. I confirmed the fixtures are reproducible today: `matched_package(fixture_profile(p)).model_dump_json()` equals the committed package for both a and b.
  - Fix: Add one test to tests/golden/test_standards_goldens.py, parametrized over MATCHED_PAIR, asserting `matched_package(fixture_profile(profile))` round-trips to the committed `package.json`. It runs all five `_checked_folders` assertions plus `_check_names` on every suite run and pins each fixture to the profile it claims to be written for, which is what makes SC-005's third measurement hold over time. (Alternatively soften the docstring - but the test is cheap and strictly stronger.)
- **reviewer/tests/golden/test_standards_goldens.py:1192** (polish-docs) - `graded_against` is a copy of `graded` (line 139) with one line changed: both build the fixture directory, read case.json, resolve the callable and load the package; only the kwargs differ. It also hardcodes `profile=profile` instead of splatting `case["kwargs"]`, so a case that later grows a second kwarg would be graded with that kwarg silently dropped rather than failing.
  - Fix: Give `graded` an optional override, e.g. `def graded(case_name, **overrides)` returning `function(package, **{**case.get("kwargs", {}), **overrides})`, and have the cross-grading tests call `graded(PAIR_A, profile=MATCHED_PAIR[PAIR_B])`. One reader of case.json instead of two.
- **reviewer/tests/golden/test_standards_goldens.py:1187** (polish-docs) - `PAIR_A, PAIR_B = MATCHED_PAIR` unpacks a dict into its two keys by insertion order. It works, but the two most-used names in the new block are derived by an idiom that reads as a typo (the values, not the keys, are what a reader expects), and it is implicit where the repository's style everywhere else is explicit.
  - Fix: Write `PAIR_A = "standards-profile-a"` / `PAIR_B = "standards-profile-b"` and build MATCHED_PAIR from them, so the names and the mapping have one source and the binding is stated rather than inferred.
- **specs/002-task-pane-assistant/contracts/pane-host-messages.md:211** (polish-docs) - The Standards page's `entity.show` row, added this round, says "all three hosts delegate it to the one `PaneActions`". Four hosts now do: ReviewHost.cs:462, RemodelHost.cs:320 and CheckPaneHost.cs:387, the last serving both the Model check and Standards tabs. The row's substance (entity.show -> entity.shown through PaneActions) is true of the code; only the count is stale, and it is stale in the sentence this task wrote.
  - Fix: Say "all four hosts" (or "every check and review host"). Note that specs/004-resilient-remodeler/contracts/pane-remodel-messages.md:116 and the Model check row at line 168 ("both hosts") carry the same stale count; those two are outside T096's file list, so report rather than edit unless the owner wants them swept.
- **specs/006-standards-check/quickstart.md:179** (polish-docs) - Scenario 3's first bullet says the suppressed component appears in the skipped coverage of the four assembly checks with "suppressed in `<configuration>`" as the reason. The shipped reason names no configuration: "cmp:0005 MR-13002-1: the instance is suppressed in this configuration, so it is not part of the assembly being graded (FR-005)". The configuration is in the row's Scope column ("configuration: Default"), not in the reason, so a reader checking the scenario against the report finds the placeholder unfilled.
  - Fix: Either reword the quickstart to "with the instance's suppressed state as the reason and the configuration on the row's scope", or have the reason name the configuration. The quickstart edit is the smaller change and matches what the code deliberately does (one configuration per run, stated once on the scope).
- **reviewer/src/swreview/checks/standards/report.py:1** (polish-docs) - Every standards report renders the shared feature 001 skeleton's header, which includes a `- Model: gpt-5.6` line (seen at the top of every run folder's report.md from Scenarios 1-6, e.g. $runs/drawings/no-views/report.md line 13). No provider is constructed on any standards path - T093 proves it - so the artefact an engineer reads at a release gate names a language model that never ran. Pre-existing (round A/B), surfaced by the quickstart runs T097 is meant to validate, and it undercuts the sentence README.md and the pane banner both carry.
  - Fix: Suppress or blank the Model line for a family whose descriptor declares no provider (a family-neutral field on the check family, not a standards special case), or state in the report that the model line is the session skeleton's and no model was used. Worth raising with T099, which owns the one-schema-owner/one-report review.
- **reviewer/tests/golden/test_standards_goldens.py:1249** (polish-docs) - `test_the_two_own_profile_runs_produce_the_same_findings_by_check_id` (and the verdict test) compare `findings_shape(first) == findings_shape(second)` with no non-empty floor: two empty finding lists compare equal, so if the matched pair ever stopped seeding violations both assertions pass vacuously. `_checked_folders`, `_check_names` and `_revision_table` guard the folder roles, the name convention and the revision disagreement, but nothing guards the data-card, material, cut-list or mate seeds. Today the pair produces 5 error + 2 warning findings, so the test is currently meaningful - it just cannot stay that way on its own. `test_every_check_is_bound_and_all_sixteen_are_accounted_for` does not close this: a package with zero findings still satisfies coverage-union == RULES.
  - Fix: Pin the expected failing check ids as a named module constant and assert it on both packages, the way `SEEDED_FINDINGS` does for standards-seeded, before asserting the two shapes are equal.
- **reviewer/tests/golden/test_standards_goldens.py:1192** (polish-docs) - `graded_against` is a near-copy of `graded` (line 139) - same three lines of case.json load, resolve_callable and load_package - differing only in that it forces `profile=profile` instead of `**case.get("kwargs", {})`. DRY, and the forced kwarg silently drops any other kwarg a case.json might carry, so the cross-grading control would stop being the same run as the golden if a case ever gained a second argument.
  - Fix: Give `graded` an optional `profile: str | None = None` that overrides the case's own kwarg (`{**case.get("kwargs", {}), **({"profile": profile} if profile else {})}`) and delete `graded_against`, or have `graded_against` call `graded`.
- **reviewer/tests/golden/test_standards_goldens.py:1184** (polish-docs) - `PAIR_A, PAIR_B = MATCHED_PAIR` unpacks a dict by insertion order to get its two keys. It works, but it reads as a two-element sequence, depends on dict ordering for its meaning, and raises an opaque `ValueError: too many values to unpack` the moment a third profile is added - against the repository's stated explicit-over-clever preference.
  - Fix: `PAIR_A = "standards-profile-a"` / `PAIR_B = "standards-profile-b"` and build MATCHED_PAIR from them, so the names are the source and the dict is derived.
- **specs/006-standards-check/quickstart.md:260** (polish-docs) - T097 pinned Scenario 5's `<passing-id>` to `standards.assembly.not_transparent` and justified it as "the one error-severity check standards-seeded does not fail". It does not fail today only because it is UNRESOLVED pending SC-014 - the run prints `unresolved checks: standards.assembly.not_transparent`. Scenario 1 (line 138) states that after SC-014 lands "all sixteen produce exactly one finding each", so at T102 this id starts failing, the expected `unused` becomes `would accept 1`, and no error-severity check is left that could occupy the `<passing-id>` slot on standards-seeded at all. The scenario will be wrong for whoever runs T102.
  - Fix: Add a sentence to the Scenario 5 comment saying that `<passing-id>` is only unfailing while SC-014 is unsettled, and name what to substitute after T102 (a second fixture, or drop the passing-id case there and prove `unused` on a different package).
- **specs/006-standards-check/quickstart.md:322** (polish-docs) - Scenario 6 says "Also assert, in the same pass: ... every byte of both package directories is unchanged after the runs." Nothing in `TestTheMatchedPair` asserts it; SC-010 is asserted generically elsewhere (reviewer/tests/unit/test_cli_check_standards.py:322) over a different fixture, and the golden harness copies packages into a scratch dir rather than grading them in place, so the pair's own directories are never the subject of an immutability assertion. The quickstart states a measurement that the pass it names does not make.
  - Fix: Either add a digest-before/digest-after assertion over both fixture directories to TestTheMatchedPair, or reword the quickstart to point at the general SC-010 test as the place that measurement is made.


## Features 008 and 009: open follow-ups (2026-09-23)

The follow-ups research R5 of `specs/008-checks-first-review/` (008 T098) and of `specs/009-engineer-workspace/` (009 T075) leave open, each re-read against main on 2026-09-23; file and line references are as of that tree.

### 008 checks-first review (7)

- **reviewer/src/swreview/bridge/client.py:185** (follow-up) - The named-pipe transport reads a byte at a time with no read timeout: `NamedPipeTransport.timeout_s` is stored (:161) and never read, so a live detection that never answers blocks checks first's pre-run, and with it `POST /sessions`, for as long as SOLIDWORKS hangs (008 research R2.23). Every failure the pipe can report already becomes failed coverage and a not-evaluated line; a hang cannot be told from a slow answer.
  - Fix: an injectable transport with a real read timeout (the module docstring's optional `pywin32` transport, :33-36), or moving the pre-run into `ReviewRun.start()` so a hang delays the first turn rather than session creation. The setup latency measured at the next sitting (008 T104) decides which.
- **reviewer/src/swreview/mcp/server.py:294** (follow-up) - MCP general chat binds the dispatch with no model view, so a command-line model reads full payloads, persist references included, while `bridge_capture` and `bridge_measure` now take package entity ids (008 R2.27, R2.38) and answer a pasted reference with an error naming it.
  - Fix: give MCP the slimmed view (`tools/model_view.view_of`) without `get_finding`, which needs a review session, and pin the MCP payloads in `test_tool_payload.py`'s `mcp` rows.
- **reviewer/src/swreview/tools/session.py:502** (follow-up) - `request_capture` resolves its entity server-side for three kinds (component, hole, fastener) through `ToolContext`, while the bridge tools use the pure `resolve_entity_ref` over ten kinds (`tools/refs.py:103`; 008 R2.27). Two resolvers with different reach for one question.
  - Fix: move `request_capture` onto `resolve_entity_ref` in a change of its own: it widens the kinds a capture can be requested for, so it needs its own tests and a docstring change.
- **reviewer/src/swreview/agent/runner.py:618** (backlog) - `_verdict_key` is `(check, component_ids, drawing locations, configuration)` and ignores hole ids, so `_reconcile_reruns` (:635) can fold two `hole.coaxiality` findings on the same two components that differ only by their holes into one (008 R2.8; the replay's `finding_subject_key` in `findings.py` includes entity-id inputs for exactly this reason).
  - Fix: key `_verdict_key` on `finding_subject_key` (or add the entity-id inputs it uses), with a test of a resumed turn re-running one of two hole-pair findings on the same components.
- **reviewer/src/swreview/benchmark/compare.py:500** (follow-up) - `_check_one_study` refuses runs of one study that differ in commit, effort, set file, checklist, step budget or explanation mode, but never reads `session.model_view`, so two runs made with different model views (slimming, pruning age) would be placed in one comparison.
  - Fix: add the session's `model_view` to the refused differences, the refusal naming the field.
- **reviewer/src/swreview/tools/registry.py:391** (follow-up) - A reused command-line `--out` folder keeps the higher-numbered `tool-results/step-<n>.json` files of an earlier, longer run beside the new run's; each envelope's `session_id` (:381) identifies them, but nothing removes or refuses them, so a reader listing the folder sees steps the session does not have.
  - Fix: clear `tool-results/` when a review session starts in a folder (`agent/runner.py:1139`, where the folder is set), or refuse a folder whose files carry another `session_id`.
- **reviewer/src/swreview/report/explanations.py:123** (watch item) - Research R5 records that the explanation request sends the top rows' finding inputs, which can carry inline persist references (`<id> <name> [<type>] persist_ref=<ref>`, `checks/rules/results.py:158`, `checks/standards/report.py:238`); it is not a tool result, so FR-015's view does not cover it. Re-read 2026-09-23: `_finding_payload` sends no `inputs` - titles, the observed, requirement and action text, component ids and names, source ids - so no reference reaches the request today.
  - Fix: none needed now. If `inputs` or another field that can carry a reference is ever added, strip it with `tools/model_view.REF_IN_TEXT` and pin that no `persist_ref=` reaches `_prompt`'s output.

### 009 engineer workspace (6)

- **reviewer/src/swreview/chat/server.py:1232** (backlog) - The backend adds every chat to `ChatServer.chats` and never evicts one, so a night of kept reviews holds every run's package in memory (009 R5; 1.48 MB on disk for the recorded big assembly).
  - Fix: evict an ended chat once its snapshot is on disk (the disk route of 009 US6 already restores a folder read-only after a restart), keeping the live one and any with a turn in flight.
- **reviewer/src/swreview/agent/runner.py:959** (backlog) - A follow-up question is appended to `ReviewRun.messages` and emits no event, so the page's pinned follow-up answer does not survive a reload: the answer's text is on the stream, the question it answers is not.
  - Fix: an in-memory list of follow-ups on `ReviewRun` served with the snapshot, or a new event type (which moves `chat-events.schema.json`, a contract 009 kept still).
- **reviewer/src/swreview/checks/hole_alignment.py:104** (owner, later) - Hole-alignment titles keep hole ids ("The axes of hol:0012 and hol:0019 ...", also :136): the title is the first sentence of `observed` (`tools/recording.py:39`), and plain words need the hole-to-part map.
  - Fix: name each hole by its part and feature in the title helper (`report/names.py`) once the map reaches it, without changing `observed`, which is evidence.
- **reviewer/src/swreview/checks/rms/assembly.py:391** (owner, later) - SOLIDWORKS API tokens (`swMateCONCENTRIC`, `swSelFACES`) reach RMS titles from the checks' observed strings (`{mate.id} ({mate.type}) references ...`; the part rules' `name [type_name]` lists, `checks/rms/part.py:239`). Rewording them changes findings' evidence and every RMS golden.
  - Fix: a display table from API token to words applied in the title helper only, leaving `observed` as the evidence it is.
- **reviewer/src/swreview/report/markdown.py:66** (owner) - `report.md` does not lead with the summary the Review tab prints (one source, `report/summary.py`); left out so the report goldens hold (009 R5).
  - Fix: render `review_summary` at the top of `render_report` in a change that regenerates the `.md` goldens deliberately.
- **reviewer/src/swreview/report/review_words_v1.yaml:118** (owner) - Research R5 left open whether modelling practice belongs under the hygiene goal or is a ninth goal line; it landed as the ninth goal (`modelling_practice`, prefix `rms.`; 009 T010, T016), a data row awaiting the owner's confirmation.
  - Fix: the owner's decision. Moving it under hygiene is one row of the goal table plus the fixture counts of `test_review_summary_fixture.py`.

## Feature 011: open follow-ups (2026-09-23)

What `specs/011-drawing-context/` leaves open after its review round of 2026-09-23 (the findings fixed that day are in the history, and research R5 and `tasks.md` carry the owner's and the seat's). File and line references are as of that tree.

### 011 drawing context (6)

- **extractor/SwReview.AddIn/ToolService/ToolServiceHost.cs** (follow-up, T073-T074) - The add-in builds no `IConfirmedDrawingSource`, so `drawing.read` answers "This bridge cannot read a drawing" in a pane review and a confirmed candidate is never read, whatever `DrawingOpenScope.SeatValidated` says. `ReviewHost.ReviewRunDirectory` (T074's half) is in.
  - Fix: `ToolServiceOptions.ReviewRunDirectory` (null by default), set in `SwReviewAddIn.CreateToolServiceGate`, and `ToolServiceHost.Attach` building `ConfirmedDrawingRead` on it with the application-thread invoker; the tests T073 lists (the wiring, the review and general-chat secrets, the `gated=` line). **Resolved 2026-09-23** (T073, T074): as written, the source built by `ToolServiceHost.ConfirmedDrawingSource` with the seam's gate observed by the plain recorder and `DrawingOpenScope.SeatValidated` unchanged (false), so a closed candidate is still refused until T077; `ToolServiceWiringTests`' `drawing.read` section holds the wiring.
- **extractor/SwReview.AddIn/Review/ReviewPage/app.js:532** (follow-up, page lane) - The live Review page appends every `coverage` event to `state.coverage` (`render.coverageSummary`'s docstring says it is rebuilt, and a snapshot restore does replace it), so a check the backend restates - `check_drawings` after a confirmed read (`ReviewRun._restate_drawing_check`), or any check a model re-runs with checks first off - shows its first and restated items side by side until the page reloads the session. The session and `report.md` hold one of each.
  - Fix: key the page's coverage by `(check, scope)` and let a later event replace an earlier one for the same key, or have the backend send the bucket's whole state; either way with a page test driven by the pane fixture, which now carries a restated check. **Resolved 2026-09-23** (011 T082, T083): keyed by `(check, scope)` in `app.js` (`recordCoverage`, `coverageKey`), the later event replacing the earlier wherever it sat and going to the end of the arrival order, for the live stream and a restored snapshot alike; `ReviewPageDrawingQuestionsTests` over the pane fixture and `ReviewPageCoverageTests`. What remains: an item a restatement stops recording has no event to remove it, so the live page keeps it until the session is reloaded. **Superseded 2026-09-23** (011 T092, T093, on review: the key merged items the backend holds side by side - a drawing's conformance skipped and checked, a tool that failed twice - and live and restored views of one session differed): the backend announces each drop as `coverage.withdrawn` (`ToolContext.withdraw_coverage`, the one place an announced item leaves the session), the page appends every item as sent and drops what a withdrawal names, and a restore prints the snapshot as it came. Still not on the stream, so shown live only after a reload: the items finalization writes (`finalize_session`), the partial-profile item written before the first turn (`record_partial_evidence`) and `check_interference_group`'s per-row coverage (`tools/checks_interference.py`, appended without `record_coverage`).
- **reviewer/tests/unit/test_tool_payload.py:362** (owner, research R5 Q10) - `DRAWING_ARMS` does not model a standards run: with checks first off, the review and slim arrays with `check_standards` and the drawing family are 38,058 / 37,976 and 38,414 / 38,281 bytes (OpenAI / Gemini), over `ARRAY_CEILING`, and neither is pinned.
  - Fix: the owner's answer. "No" (as Q9): pin `review+standards+drawings` and `review+slim+standards+drawings` unasserted, with `--write` in a commit of their own. "Yes": something must leave those arrays first; the ceiling and the tool docstrings do not move.
  - **Resolved 2026-09-23 (owner decision 9A):** `ARRAY_CEILING` is asserted only on the arrays the pane sends by default, and every other array a review can send is pinned for both providers and not asserted, each classified in `ARRAY_KINDS` so a new array fails a test until it has a kind (011 T084, T085; research R5 Q10; 005 `contracts/levers.md`; 008 research R2.57).
- **reviewer/src/swreview/agent/runner.py** (T066) - The step restated after a confirmed read re-runs `check_drawings` only. While `DRAWING_BINDING_VALIDATED` is false no drawing dimension binds, so the stack-up cannot change; once T066 sets it, a confirmed candidate's tolerances would not reach `check_joints` in that review, which the re-call guard answers from the pre-run. T066's text now carries this.
  - Fix: in T066's commit, restate `check_joints` beside `check_drawings` and answer its repeats from it, with a test under checks first.
- **extractor/SwReview.Extractor/Guard/ReadOnlyGuard.cs** (watch item) - `OpenDoc6` and `CloseDoc` stay allowed on a read-only gate in either spelling (the documented exclusions of 011 `contracts/guard.md` section 3: the extractor's model open and the bare name of feature 004's stage-1 key), so outside `DrawingOpenScope` only call-site discipline keeps a read path from closing a document; the read audit would flag no such literal because neither is in the table.
  - Fix: none now. Feature 012's allowlist design, which will add drawing writers deliberately, is the moment to decide whether a close belongs to a guard of its own rather than an exclusion.
- **reviewer/src/swreview/tools/drawings.py:230** (seat, T077) - When the seam cannot close a drawing it opened, the coverage says "close it in SOLIDWORKS", but the drawing was opened hidden (`DocumentVisible(false, 3)`), so it has no window for the engineer to close.
  - Fix: probe D14 records how a hidden drawing appears (the Window menu, the document list); word the sentence from that record, or have the seam make a drawing it failed to close visible before giving up.

## Seat readiness: owner items (2026-09-23)

What the seat-readiness review of 2026-09-23 left for the owner; the rest of its findings were fixed that day (008 T122, T123; 010 T109; 011 T092 to T099).

### 008 checks first (2)

- **specs/008-checks-first-review/tasks.md** (T102, T103; owner) - The recorded 99 findings were graded with the example profile, and the sitting grades with the real one (T101 first), so the standards findings cannot be compared key for key. T102 and T103 now list them apart and compare every other recorded key, a contact counting for a recorded interference finding (owner decision 3A).
  - Fix: the owner's rule for the standards half: compared against a replay of the recording under the real profile (008 T105 can grade the sitting's folders either way), or left out of SC-010's count.
- **specs/008-checks-first-review/spec.md** (SC-003, SC-010; owner, watch) - T103's line is 300,000 input tokens, SC-003's 0.3M, which assumes the regrouped estimate's behaviour (no repeat of a check the digest reported); the replay's requested figure for `small-assembly-a` is 459,000. A paid review that makes the recorded calls fails T103 by design; the tasks say so and ask the seat to record the repeated calls.
  - Fix: none unless the owner reads SC-010's "the replay's estimate" as the requested figure, in which case T103's line is 688,500 and the 0.3M is SC-003's replay target only.
