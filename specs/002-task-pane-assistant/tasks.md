---

description: "Task list for the Task Pane Assistant"
---

# Tasks: Task Pane Assistant

**Input**: Design documents from `/specs/002-task-pane-assistant/`

**Prerequisites**: plan.md, spec.md, research.md, data-model.md, contracts/, quickstart.md; feature 001 complete through its wiring tasks.

**Tests**: REQUIRED (constitution Principle III). Test tasks precede implementation in every phase; feature 001 golden baselines must not change.

**Organization**: Foundational provider port first (it removes the Anthropic dependency and every story needs it), then US2 (settings), US1 (review chat), US4 (in-process tool service), US3 (terminal general chat), polish.

## Format: `[ID] [P?] [Story] Description`

- **[P]**: parallelizable (different files, no dependency on incomplete tasks)
- **[Story]**: US1 review chat, US2 provider settings, US3 general chat, US4 in-process tool service
- Paths are relative to the repository root

## Path Conventions

- Python: `reviewer/src/swreview/`, tests in `reviewer/tests/`
- C#: `extractor/SwReview.AddIn/`, `extractor/SwReview.Extractor/`, tests in `extractor/SwReview.Extractor.Tests/` and a new `extractor/SwReview.AddIn.Tests/`
- Vendored web assets: `extractor/SwReview.AddIn/Review/ReviewPage/`, `extractor/SwReview.AddIn/Terminal/TerminalPage/`

---

## Phase 1: Setup

- [X] T001 Update `reviewer/pyproject.toml`: add `openai`, `google-genai`, `mcp`, `starlette`, `sse-starlette`, `uvicorn`, `httpx`; dev `respx`; remove `anthropic`; add **no** new console entry points - both servers are subcommands of the existing `swreview` script (`swreview chat serve`, `swreview mcp`), which is the spelling `contracts/mcp-toolset.md`, `contracts/README.md`, `contracts/cli-profiles.md` and T059 all use; run `uv sync --all-extras`
- [X] T002 [P] Create `extractor/SwReview.AddIn.Tests/SwReview.AddIn.Tests.csproj` (xUnit, net48, x64, references `SwReview.AddIn` and `SwReview.Extractor`) and add it to `extractor/SwReview.sln`
- [X] T003 [P] Add `Microsoft.Web.WebView2` (pinned) to `extractor/SwReview.AddIn/SwReview.AddIn.csproj` with `WebView2Loader.dll` copied to output; add `<Content>` items for `Review/ReviewPage/**` and `Terminal/TerminalPage/**` copied to `web/`
- [X] T004 [P] Vendor xterm.js and `@xterm/addon-fit` (pinned versions, MIT) into `extractor/SwReview.AddIn/Terminal/TerminalPage/vendor/` with a `LICENSES.md` listing versions and licenses; no CDN references anywhere
- [X] T005 [P] Add `.gitignore` entries: `extractor/SwReview.AddIn/web/`, `**/.swreview-cli/`, `%APPDATA%`-style settings never in repo; add `benchmarks/**/chat-log.jsonl` and `**/events.jsonl` under `runs/` ignore

---

## Phase 2: Foundational (provider layer and runner port)

**Purpose**: One `AgentProvider` protocol with OpenAI, Gemini, and fake adapters; runner and registry free of the Anthropic SDK; uniform events; `--provider` on the CLI.

**⚠️ CRITICAL**: No user story work can begin until this phase is complete.

- [X] T005a Replace the two free-form map parameters before any schema work: `mark_coverage(check, bucket, scope: CoverageScope, reason)` in `reviewer/src/swreview/tools/session.py` and `bridge_interference(component_ids, configuration, settings: InterferenceSettings)` in `reviewer/src/swreview/tools/bridge.py`, with a new `InterferenceSettings` model (`treat_coincident_as_interference`, `treat_subassemblies_as_components`, `include_multibody`, `ignore_hidden`, `fastener_folder_treatment`) beside `CoverageScope`; update the call sites and their tests. `dict[str, Any]` generates `{"type": "object", "additionalProperties": true}` with no `properties`, which OpenAI strict mode cannot express: strictified it becomes an object that admits no keys, and `CoverageScope.model_validate({})` succeeds, so every coverage item would silently record an empty scope. The rows in `specs/001-agentic-design-review/contracts/agent-tools.md` are updated in T064
- [X] T006 Write `reviewer/tests/unit/test_provider_schema.py`: for every function in `tools/registry.py`'s registrations, `providers/schema.py:canonical_schema(fn)` yields `type: object`, properties with descriptions from the Google-style docstring, `required` matching non-default parameters, nested `SourceRef` definitions inlined; `strictify()` sets `additionalProperties: false` on every object, moves every property into `required`, rewrites optional parameters to `["T", "null"]`, and has no `$defs`; `gemini_adapt()` removes `additionalProperties` and `$defs` and keeps nullable unions; **no strictified tool schema contains an object with `additionalProperties: true`, and none contains an object with zero properties** (both are unrepresentable in strict mode and would silently accept nothing - see T005a); a golden comparison of every tool's canonical schema against the feature 001 tool table names in `contracts/agent-tools.md`
- [X] T007 Implement `reviewer/src/swreview/agent/providers/schema.py`: `canonical_schema(fn)` via `pydantic.TypeAdapter(fn).json_schema(ref_template=...)` plus a docstring `Args:` parser; `strictify(schema)`; `gemini_adapt(schema)`; `ToolSpec` dataclass builder `tool_spec(fn)`
- [X] T008 Write `reviewer/tests/unit/test_provider_protocol.py`: `AgentEvent` model validates against `specs/002-task-pane-assistant/contracts/chat-events.schema.json` for every event type, with the feature 001 schemas registered by `$id` in a `referencing.Registry` (the cross-feature `$ref`s are absolute `$id`s: `https://smart-sw.local/contracts/review-session.schema.json#/$defs/...`) - note that `jsonschema` resolves `$ref` lazily, so a test that only exercises event types without a `$ref` proves nothing and every ref'd type must be covered; `ProviderName` rejects unknown values; `EffortMapping` is recorded; the `providers.get(name)` registry returns the adapter class and raises for `anthropic`/`claude`
- [X] T009 Implement `reviewer/src/swreview/agent/providers/__init__.py`: `AgentProvider` protocol (`run(system, messages, tools, effort, max_steps, on_event) -> TurnResult`), `AgentEvent`, `ToolCallRequest`, `ToolCallResult`, `EffortMapping`, `ProviderName`, registry
- [X] T010 Write `reviewer/tests/unit/test_fake_provider.py`: scripted tool calls and text; emits `text.delta`, `tool.started/finished` through the registry, stops at `max_steps`; deterministic
- [X] T011 Implement `reviewer/src/swreview/agent/providers/fake.py` (script-driven provider used by tests and by the pane's development mode)
- [X] T012 Write `reviewer/tests/unit/test_registry_provider_neutral.py`: `RecordedTool.call(arguments) -> ToolCallResult` returns `(payload, is_error)` without raising, validates arguments with `pydantic.validate_call`, records the step through an injectable sink, converts exceptions to error payloads and `failed` coverage; a call naming a tool that is not registered returns an error `ToolCallResult` naming the tool and records `failed` coverage instead of raising (a hallucinated tool name is routine provider behaviour, and feature 001 relied on the SDK to dispatch); no `anthropic` import anywhere under `src/`, and no `*.py` file under `reviewer/src/` matches `(?i)anthropic|claude` (today that is `agent/runner.py`, `cli.py`, `tools/context.py` and `tools/registry.py`, all of which T013, T015 and T023 already rewrite) (an import scan misses a string constant such as the model id in `tools/context.py`; the scan is scoped to `reviewer/src/` because `spec.md`, `tasks.md` and the repo's `CLAUDE.md` legitimately contain the words)
- [X] T012a Port the five feature 001 test modules that import `anthropic.lib.tools` and would fail at collection once T001 removes the dependency - `reviewer/tests/unit/test_tools_bridge.py`, `test_tools_checks_fastener.py`, `test_tools_checks_fit.py`, `test_tools_checks_interference.py`, `test_tools_measure.py`: replace `pytest.raises(ToolError)` with assertions on `result.is_error is True`, replace the `json.loads(caught.value.content)["error"]` payload reads (test_tools_bridge.py, test_tools_checks_fit.py twice) with reads of `ToolCallResult.payload["error"]`, remove the `anthropic` import from each, and keep every existing failed-coverage assertion unchanged. Behaviour-preserving, so feature 001 goldens are untouched (SC-009)
- [X] T013 Port `reviewer/src/swreview/tools/registry.py`: drop `BetaFunctionTool`/`ToolError`; `RecordedTool` wraps a `ToolSpec`; `RecordedTool` takes a **pluggable recording sink** (review-session steps, or the general-chat chat log) and `ToolContext.session` becomes optional, so the MCP server in T059 reuses this one choke point for validation, recording, error conversion and the never-raise rule instead of reimplementing them; `ToolRegistry.build(ctx) -> list[RecordedTool]` plus a name lookup for dispatch; keep `REGISTRATIONS` and `fail_tool`. T012 and T012a must be green against it
- [X] T014 Write `reviewer/tests/unit/test_runner_provider.py` (replace the anthropic-based fake in `test_agent_runner.py`): runner drives an `AgentProvider`; multi-turn `continue_session(chat, text)` appends a user turn, including on a session that already ended (`ended → running`); `answer_evidence(request_id, answer)` marks the request answered and resumes; `fail_tool`; events written to `events.jsonl` in order with monotonic `seq`; `session.started` carries `session_id`, `package_id`, `provider`, `model` and `effort_mapping` **as flat fields per the event schema** (`provider_info` is the `ReviewSession` field, a different shape); final `session.json` validates against the (minor-bumped) review-session contract. Plus one case per resume rule in data-model section 3: (a) a second `finalize_session` produces the same coverage, not duplicates; (b) answering an evidence request removes or rewrites its `coverage.evidence_request` unresolved item; (c) a check re-run after its blocking request is answered yields one verdict - same finding id reused, or `supersedes` set and the superseded finding excluded from the report body and the severity counts; (d) `max_steps` is a per-turn budget and a resumed turn on a session near the cumulative total still runs, with the cumulative count recorded separately; (e) a turn the provider ends on its output ceiling emits `turn.ended {reason: "truncated"}` and an `unresolved` coverage item; (f) a provider exception mid-run still finalizes: `ended_at` non-null and `session.json` written
- [X] T015 Port `reviewer/src/swreview/agent/runner.py`: `run_review(..., provider: AgentProvider, model, effort)`; `ReviewRun` object with `continue_session` and `answer_evidence`; event emission via an `EventSink` that writes `events.jsonl` and fans out to callbacks; `provider_info` and `retry_of` on the session; `finalize_session` rebuilt to be idempotent and called on the failure path as well as the success path; per-turn step budget with the cumulative count kept separately; and a line in `reviewer/src/swreview/agent/prompts/system_v1.md` telling the model that an answered evidence request means re-running the check it named in `why` and recording one verdict (feature 001's prompt assumes requests are never answered mid-session)
- [X] T016 Bump `specs/001-agentic-design-review/contracts/review-session.schema.json` (minor) with optional `provider_info` (provider, model, `effort_mapping` whose `provider_value` is a string or an integer, key_source), optional `retry_of` (the failed session this one replaces, FR-028), and optional `supersedes` on `Finding` if T014 case (c) takes the supersession route rather than reusing the finding id; update `reviewer/src/swreview/report/session.py`, `report/markdown.py` (a superseded finding is excluded from the body and the severity counts) and their tests
- [X] T017 Write `reviewer/tests/unit/test_openai_provider.py` with `respx`: request shape (`tools` flat with `strict: true`, `instructions`, `reasoning.effort`, `max_output_tokens`, `parallel_tool_calls`), full-list history with `response.output` echoed including reasoning items, `function_call` → validated call → `function_call_output`, streaming events mapped to `text.delta`/`tool.started`/`tool.finished`, error mapping for `AuthenticationError`, `RateLimitError`, `APIConnectionError`, `APIStatusError`; a response with `status: "incomplete"` and `incomplete_details.reason == "max_output_tokens"` maps to `turn.ended {reason: "truncated"}` (it is neither an exception nor a tool error, and must not be reported as a normal end), as does a truncated `function_call_arguments` stream that will not parse; a requested effort level the chosen model does not offer fails fast with a named error rather than being silently downgraded; key redaction in error text; effort mapping recorded
- [X] T018 Implement `reviewer/src/swreview/agent/providers/openai_provider.py` (Responses API, streaming, strictified tools, `instructions`, `reasoning={"effort": ...}`). The output ceiling comes from `agent/settings.py` per provider and model, **not** the inherited `16000`: that number is documented in feature 001's runner as the Anthropic non-streaming ten-minute ceiling ("raise only together with stream=True") and this adapter streams. Reasoning tokens draw from the same budget, so the truncation path from T017 is reachable in normal use
- [X] T018a Prove the strict schemas against the real API once per tool group: a key-gated test (`@pytest.mark.live`, skipped without `OPENAI_API_KEY`, following the `conftest.py` skip convention) that posts each group's strictified tools and asserts a non-400 response. A `respx` exchange replays a body we authored and cannot retire this risk; run it here in Phase 2, not at Polish
- [X] T019 Write `reviewer/tests/unit/test_gemini_provider.py` with a stubbed `genai.Client`: `FunctionDeclaration(parameters_json_schema=gemini_adapt(...))`, `automatic_function_calling.disable=True`, `system_instruction`, `thinking_config.thinking_level` mapping (and the integer `thinking_budget` for 2.5 models, recorded as an integer `provider_value`), a requested level the model does not offer failing fast rather than downgrading silently, `function_call` parts → validated call → `Part.from_function_response` with `id`, streaming chunks synthesized into events, error mapping for `ClientError` 401/429 and `ServerError`, redaction
- [X] T020 Implement `reviewer/src/swreview/agent/providers/gemini_provider.py` (`generate_content_stream`, manual loop, `role="tool"` responses)
- [X] T021 Write `reviewer/tests/unit/test_provider_settings.py`: precedence settings-over-env with `key_source` reported; `SecretStr` never in `model_dump`, `repr`, or logs; `redact(text, secrets)` masks every configured key; default models per provider (`gpt-5.6`, `gemini-3.5-flash`, the scripted `fake` id) and the per-provider output ceiling used by T018; `enterprise` mapping for Gemini
- [X] T022 Implement `reviewer/src/swreview/agent/settings.py` (`ProviderSettings`, `from_env()`, `redact()`, `configure_logging_redaction()`)
- [X] T023 Update `reviewer/src/swreview/cli.py`: `--provider openai|gemini|fake` (default `openai`), `--model`, `--effort` on `review` and `benchmark run`; `REVIEW_CLIENT` hook replaced by a `provider_factory` hook; `benchmark/runner.py` passes provider and model. **Delete `DEFAULT_MODEL = "claude-opus-5"` from `reviewer/src/swreview/tools/context.py`** and take the default from `agent/settings.py` per provider at every use (`new_session`, both `build_context` overloads, `context_for`, `runner.run_review`, and both CLI commands): no task otherwise touches that module, and a review run without `--model` would write a Claude model id into `session.json`, violating FR-026 and sending an invalid model to the OpenAI adapter. Update `tests/unit/test_cli.py` (including the default-model assertion), `tests/unit/test_answer_key_isolation.py` **and** `tests/integration/test_answer_key_isolation.py` fakes
- [X] T024 Write `reviewer/tests/unit/test_events_schema.py`: every event the runner and tools can emit validates against `contracts/chat-events.schema.json`, and `events.jsonl` from a fake run round-trips. Extend `reviewer/tests/support/contracts.py`, whose `CONTRACTS_DIR` is hard-coded to the feature 001 directory and cannot load the 002 contracts at all, with a loader that registers `ir.schema.json`, `review-session.schema.json` and the feature 002 contracts in one `referencing.Registry` keyed by `$id`; the cross-feature `$ref`s are absolute `$id`s, so resolution is by `$id` and never by file path
- [X] T025 Run the full suite; confirm feature 001 goldens unchanged and `uv pip show anthropic` reports not installed

**Checkpoint**: `swreview review --provider fake` produces a session and `events.jsonl`; OpenAI and Gemini adapters pass recorded tests.

---

## Phase 3: User Story 2 - Provider Settings (Priority: P2)

**Goal**: Per-user settings with a protected key, settings UI in the Review page, backend started with the key in its environment.

**Independent Test**: quickstart Scenario 2 (fake and recorded providers).

- [X] T026 [P] [US2] Write `extractor/SwReview.AddIn.Tests/UserSettingsTests.cs`: round trip to a temp `settings.json` validating against `contracts/settings.schema.json`; key protected with DPAPI `CurrentUser` and unreadable when the ciphertext is copied to a different user context (simulate by tampering); missing file yields defaults (`openai`, default model, `high`, `codex`, run root); `provider: "fake"` round-trips and validates in a development build, and a release build refuses to load it, falls back to `openai` and surfaces a visible error (FR-027); `key_source` reported
- [X] T027 [P] [US2] Write `extractor/SwReview.AddIn.Tests/RedactionTests.cs`: every configured secret is masked in log lines and exception messages, including URL-encoded and JSON-escaped forms
- [X] T028 [US2] Implement `extractor/SwReview.AddIn/Settings/UserSettings.cs` and `Settings/Redaction.cs`
- [X] T029 [P] [US2] Write `reviewer/tests/unit/test_cli_audit_secrets.py`: `swreview audit-secrets <dir>...` exits 1 and names the file when any configured key (from env) appears in files under the directories; 0 otherwise; **provider-shaped detectors** (`sk-...`, `AIza...`) catch a leaked key when the invoking shell holds none - on the workstation the key lives DPAPI-protected in `%APPDATA%` and reaches only the backend child's environment, so an env-only scan run from a separate shell reports "none" no matter what is in the files; and the command exits non-zero when it has neither a key source nor detectors, so a vacuous run is loud rather than green
- [X] T030 [US2] Implement `audit-secrets` in `reviewer/src/swreview/cli.py` (env keys plus provider-shaped detectors, and the bridge secret when one is supplied)
- [X] T031 [US2] Write `extractor/SwReview.AddIn.Tests/PageMessageTests.cs`: `settings.get`, `settings.save`, `models.list` messages per `contracts/pane-host-messages.md`, including that `settings.save` never echoes the key, that `init` omits it and carries the backend `origin`, that `settings.save` is refused with `error {error_class: "TurnRunning"}` while a chat has a running turn instead of restarting the backend, and that a model id the provider rejects re-triggers `models.list`
- [X] T032 [US2] Implement the Review page skeleton `extractor/SwReview.AddIn/Review/ReviewPage/{index.html,app.js,app.css}` with the Settings section (provider, model list, key field, effort, base URL, enterprise fields, Save) and the `ready`/`init`/`settings.*`/`models.list` messages; and `extractor/SwReview.AddIn/Review/ReviewHost.cs` handling those messages. `index.html` carries the strict CSP meta tag from `contracts/pane-host-messages.md` and has no inline handlers; the provider list offers `fake` only in a development build
- [X] T033 [US2] Write `extractor/SwReview.AddIn.Tests/BackendProcessTests.cs` against a stub script that prints `{port, token}`: parses the first line, times out with a clear error, passes the key only through the environment (assert the command line has no key), stops the process on dispose, restarts on settings change only when no turn is running, and is assigned to the job object before its first thread runs
- [X] T034 [US2] Implement `extractor/SwReview.AddIn/Review/BackendProcess.cs` (locate `uv`/python from settings or PATH, start `swreview chat serve --port 0`, job object kill-on-close, health poll)

**Checkpoint**: Settings save and restore across restarts; the backend starts with the key in its environment only.

---

## Phase 4: User Story 1 - Review Chat (Priority: P1)

**Goal**: Press Review, watch the investigation, act on findings, answer evidence requests, ask follow-ups.

**Independent Test**: quickstart Scenario 1.

- [X] T035 [P] [US1] Write `reviewer/tests/unit/test_chat_sessions.py`: `ChatSession` state machine transitions from data-model section 3, including `ended → running` on a follow-up and `failed` as terminal; `events.jsonl` writer with monotonic `seq`; replay from a given `seq`
- [X] T036 [P] [US1] Write `reviewer/tests/unit/test_chat_server.py` with Starlette's test client: 401 without token, and a token supplied only in the query string is still 401; `OPTIONS` on every route is answered 204 **without** a token and echoes the exact configured origin with `Vary: Origin` and the private-network header, while a request whose `Origin` is anything else is 403; `POST /sessions` starts a fake-provider run and returns ids, records `retry_of` when given, and returns 400 for a `run_dir` that is not a descendant of the run root, is a UNC path, or contains `..`; `GET /events` streams replayed then live events with `id`; `POST /messages` 409 while running, then 202, and 202 on a session whose previous turn already ended; evidence answer 404/409/202; disposition 200/409 and `report.md` re-rendered; `/stop` emits `turn.ended {reason: "stopped"}` then writes `session.ended`; a shutdown signal during a running turn still finalizes the session with a non-null `ended_at`; `/models` proxies and maps provider errors to 502; error bodies redacted
- [X] T037 [US1] Implement `reviewer/src/swreview/chat/sessions.py` and `reviewer/src/swreview/chat/server.py` (Starlette app, SSE via `sse-starlette`, one worker thread per chat, credentials from environment only, CORS and origin checks per `contracts/chat-api.md`, the `run_dir` path rule, and shutdown finalization of every live chat)
- [X] T037a [US1] Write `reviewer/tests/unit/test_chat_main.py` driving the **real** entry point as a subprocess (T036 uses an in-process ASGI client and T033 uses a stub script, so nothing today exercises the program the pane actually launches): `swreview chat serve --port 0` prints a first stdout line that parses as `{port, token}` with a 32-byte base64url token, stdout receives nothing further after a request has been served, the bound address is `127.0.0.1`, and `--fail-bridge 3` makes the fourth bridge call return the circuit-open error (the breaker rule itself is already covered by `test_bridge_client.py`)
- [X] T038 [US1] Implement `reviewer/src/swreview/chat/__main__.py` (`--port 0`, `--allow-origin <origin>`, prints `{port, token}` once, binds 127.0.0.1, `--fail-bridge N` test hook) and wire `swreview chat serve` in `cli.py`. Pass a `log_config` whose `default` **and** `access` handlers write to `sys.stderr`: uvicorn's default sends the access log to stdout, which corrupts the handshake line and can block the child once the parent stops draining the pipe
- [X] T039 [P] [US1] Write `extractor/SwReview.AddIn.Tests/ReviewHostTests.cs`: `review.start` refuses without an open document; with a fake session and fake backend client it creates the run folder, runs the dump, posts the session, and replies `review.started`; `entity.show` resolves through a fake resolver and on failure reports the state code **and the component's full path** (`entity.shown {ok, state_code, message, full_path}`); `report.open`/`folder.open`/`log.open` shell out through an injectable opener that is never called with a path outside `run_root` or the log folder, and resolve their path from the host's own session record keyed by `chat_id` rather than from the page payload
- [X] T040 [US1] Implement `extractor/SwReview.AddIn/Review/ReviewHost.cs` review flow: run folder naming (`<run_root>/<yyyyMMdd-HHmmss>-<doc>`) in a **shared helper** that T060's terminal-first path also calls for `<run_root>/<yyyyMMdd-HHmmss>-terminal`, in-process `PackageWriter` dump with progress `status` messages, backend `POST /sessions`, `entity.show` via `PersistRefService.Resolve` + `SelectByID2` + `ViewZoomToSelection` on the application thread, `document.changed` notifications
- [X] T041 [US1] Implement the Review page chat view in `Review/ReviewPage/app.js`. Every string that came from the model or from a customer document (text deltas, `result_summary`, finding `title` and `recommended_action`, drawing `text_as_read`, evidence text, error `message`) is inserted with `textContent`/`createTextNode`, never `innerHTML`, and no inline handlers are used (FR-029). The event stream is read with `fetch` plus a `ReadableStream` reader, not `EventSource`, which cannot set `Authorization` or `Last-Event-ID`; the token never appears in a URL. Features: `Last-Event-ID` reconnect; transcript of text, tool cards (name, summary, status, elapsed), finding cards (status, severity, title, components, expand for full fields, Show in SOLIDWORKS, Accept/Reject/Defer with note), evidence request cards with an answer box, coverage summary, error cards with Retry (starts a new session carrying `retry_of`, FR-028) / Open Settings / View log, follow-up input disabled while a turn runs but enabled again once it ends; keep the last 500 events in memory
- [X] T042 [US1] Write `extractor/SwReview.AddIn.Tests/ReviewPageContractTests.cs`: load `app.js` in a headless check that every message type it sends and handles is in `contracts/pane-host-messages.md` (string scan against the contract table) and that it never references a key field after `init`
- [X] T042a [US1] Write `extractor/SwReview.AddIn.Tests/ReviewPageInjectionTests.cs`: render a finding whose `title` and `recommended_action` and a `tool.finished` whose `result_summary` each contain `<img src=x onerror=...>` and `</script>`, and assert the resulting DOM holds those characters as literal text with no injected element; assert `index.html` carries the CSP meta tag and contains no `innerHTML` and no inline `on*` attribute. The string scan in T042 cannot catch injection
- [X] T042b [US1] Write `extractor/SwReview.AddIn.Tests/WebViewFallbackTests.cs`: with an injectable environment factory that throws `WebView2RuntimeNotFoundException`, `TaskPaneControl` shows the fallback panel containing the evergreen runtime download URL and the run root path in plain text, the Actions tab still works, and no exception escapes to SOLIDWORKS; the environment is created once per process with user data folder `%LOCALAPPDATA%\SwReview\WebView2\<instance>` and a second tab reuses the same environment object
- [X] T043 [US1] Replace `extractor/SwReview.AddIn/DumpIrPanel.cs` with `TaskPaneControl.cs`: tabs Review (WebView2 hosting the page from the fixed virtual host `swreview.invalid` mapped with `CoreWebView2HostResourceAccessKind.Allow`), Terminal (placeholder until US3), Actions (the three existing buttons); one `CoreWebView2Environment` for the process with the explicit user data folder, shared by both tabs, and the runtime-missing fallback behind an injectable environment factory; `NavigationStarting` and `NewWindowRequested` cancel any URL outside the virtual host; `SwReviewAddIn.cs` hosts it behind a guard so a WebView2 failure cannot break add-in load, starts the backend after settings load, and stops it on disconnect
- [ ] T044 [US1] Run quickstart Scenario 1 with the fake provider on the bracket fixture (prepared by feature 001 task T061; it is not in the repository); record results in the metric table in `benchmarks/native/bracket-assy/notes.md`: component count, seconds to extraction complete and to the first finding card (SC-001), first-delta-to-screen seconds (SC-002), and the Show in SOLIDWORKS hit count over resolvable references (SC-007)

**Checkpoint**: A full review runs from the pane with the fake provider; findings can be shown, dispositioned, and followed up.

---

## Phase 5: User Story 4 - In-Process Tool Service (Priority: P4)

**Goal**: Captures, measurements, interference, and Show in SOLIDWORKS served by the add-in on the application thread, with a per-launch secret.

**Independent Test**: quickstart Scenario 4.

- [X] T045 [US4] Move `SwBridgeDispatcher`, `BridgeProtocol`, `BridgeServices`, and the command result types from `extractor/SwReview.Extractor.Console/Serve/` to `extractor/SwReview.Extractor/Bridge/`; keep `PipeServer` in the console; update namespaces and tests; add the `secret` field to `BridgeRequest` (optional; the console host ignores it, the in-process host requires it) and document it in `PROTOCOL.md`, including that the in-process host issues two secrets - a review secret accepting `ping|capture|measure|interference` and a general-chat secret accepting `ping|capture|measure`, with `interference` answered `unauthorized` for the latter (the CLI can read its own generated profile, so the secret must not be the only thing withholding a command the MCP allowlist excludes)
- [X] T046 [P] [US4] Write `extractor/SwReview.AddIn.Tests/InProcPipeServerTests.cs`: requests read on a background thread are executed through an injectable `Invoke` that records the calling thread; concurrent requests serialize in arrival order; wrong or missing secret returns `unauthorized` and is logged without the secret; the general-chat secret is refused on `interference` and accepted on `capture`; an `Invoke` that never completes (a modal dialog on the application thread) times out and answers the documented "the SOLIDWORKS thread did not answer" error while the late delegate cannot write a stale response; `Invoke` on a control whose handle is not created or already destroyed answers a clean `error` rather than throwing onto the reader thread; a request whose document is gone answers the `document no longer open` error and the Python side records it as failed coverage; the created pipe's DACL grants the current user only; circuit-open propagates; dispose stops the listener and pending requests get `error`
- [X] T047 [US4] Implement `extractor/SwReview.AddIn/ToolService/InProcPipeServer.cs` (pipe reader thread, explicit `PipeSecurity` granting `ReadWrite` to the current user SID only - .NET Framework 4.8 has no `PipeOptions.CurrentUserOnly` and a default descriptor is readable by any local process - `Control.BeginInvoke` marshalling to the Task Pane control guarded by `IsHandleCreated`/`IsDisposed`, a bounded wait with the documented timeout error, one-at-a-time execution) and `ToolService/ToolServiceHost.cs` (`SwScope.Open` on the application thread, `SwBridgeDispatcher`, pipe `swreview-<guid>`, the two scoped secrets, log under `%LOCALAPPDATA%\SwReview\logs` recording per request the command, elapsed time, the distinct set of interop member names `SwGate` gated, and any `MutatingCallError` refusal - this is the artifact SC-004 is audited against)
- [X] T048 [US4] Wire the tool service into `SwReviewAddIn.cs`: start on connect after the first document is available, pass `{pipe, secret}` with the **review** secret to the backend `POST /sessions` `bridge` field and the **general-chat** secret to the MCP profile writer, stop on disconnect; `entity.show` uses the same scope
- [X] T049 [P] [US4] Update `reviewer/src/swreview/bridge/client.py` and `tools/bridge.py` to send `secret` and to accept it from the session's `bridge` config, and to surface `unauthorized` and the `document no longer open` error as failed coverage rather than as a crash; tests in `tests/unit/test_bridge_client.py`
- [X] T050 [US4] Write `extractor/SwReview.AddIn.Tests/ProcessLifetimeTests.cs`: backend and CLI child processes are assigned to a job object with kill-on-close **before their initial thread is resumed** (the ConPTY child is started suspended by `CreateProcess`, so assigning after it runs leaves a window in which a fast-exiting child escapes the job); disposing the host ends them; two hosts in one process get distinct pipe names and ports
- [ ] T051 [US4] Run quickstart Scenario 4 on the workstation; record in `benchmarks/native/bracket-assy/notes.md`, including the SC-004 audit of the tool-service log (no `MutatingCallError`, no gated member outside the reader set) and the SC-003 check that every tool call appears in `session.json` steps or `chat-log.jsonl`

**Checkpoint**: No console `serve` needed for the pane; requests are served in-process with a secret.

---

## Phase 6: User Story 3 - General Chat Terminal (Priority: P3)

**Goal**: Codex running inside the pane (Gemini deferred, decision 2026-09-13) with our read-only tools and a regenerated restriction profile.

**Independent Test**: quickstart Scenario 3.

- [X] T052 [P] [US3] Write `extractor/SwReview.AddIn.Tests/ConPtyTests.cs`: start `cmd.exe /c echo hello` under a pseudo-console and receive `hello`; start a `.cmd` shim from a temp directory through the resolved launch command and read its output (`CreateProcess`, which ConPTY requires, cannot execute a `.cmd` directly and the Gemini CLI ships as `gemini.cmd`); resize changes `COLUMNS`/`LINES` as observed by a child `mode con`; closing the console ends the child; output chunks arrive as bytes without translation; a child producing 1 MB posts fewer than a stated bound of `terminal.output` messages and loses no bytes (the coalescing rule)
- [X] T053 [US3] Implement `extractor/SwReview.AddIn/Terminal/ConPty.cs` (P/Invoke `CreatePseudoConsole`, `ResizePseudoConsole`, `ClosePseudoConsole`, `PROC_THREAD_ATTRIBUTE_PSEUDOCONSOLE`, pipes) and `Terminal/TerminalSession.cs` (start the **resolved launch command** from `CliLocator` with cwd and environment, async read loop coalescing into at most one `terminal.output` per ~16 ms or 32 KB and marshalling the `PostWebMessageAsJson` onto the UI thread, `terminal.input`, resize, stop with exit command then process-tree termination, exit notification)
- [X] T054 [P] [US3] Write `extractor/SwReview.AddIn.Tests/CliLocatorTests.cs`: finds `codex`/`gemini` on PATH including `.cmd`/`.ps1` npm shims **and returns a resolved launch command** for each (`.exe` direct; `.cmd`/`.bat` as `cmd.exe /c "<path>"`; `.ps1` as `powershell.exe -NoProfile -ExecutionPolicy Bypass -File "<path>"`; anything else reported unsupported) that both the `--version` probe and `TerminalSession` use; parses `--version`, compares to minimums, reports not-found with install steps and the Node requirement for Gemini
- [X] T055 [US3] Implement `extractor/SwReview.AddIn/Terminal/CliLocator.cs`
- [ ] T055a [US3] *(deferred: Gemini terminal is not in v1; do on the workstation later)* Spike, on the workstation and before T056 asserts anything Gemini-specific: install the Gemini CLI if absent, point its settings home at a scratch directory with the generated files, and confirm with `/mcp` that our settings and policy were actually loaded; record the exact variable or flag and the CLI version in `contracts/cli-profiles.md`, delete the "unverified" paragraph there, and confirm or correct `mcp.allowed`, `security.disableYoloMode`, `general.defaultApprovalMode`, `context.fileName`, the per-server `trust` key and the `toolName = "*"` wildcard rule, plus whether the override also relocates the CLI's credential store (if it does, add the same auth passthrough Codex needs)
- [X] T056 [P] [US3] Write `extractor/SwReview.AddIn.Tests/CliProfileWriterTests.cs`: generated Codex `config.toml` and Gemini `settings.json` plus policy TOML match `contracts/cli-profiles.md` byte-for-byte after placeholder substitution; the MCP server command is the resolved `swreview mcp` form for both a `python.exe` and a `uv.exe` value of the settings `python` field (never `-m swreview.mcp`, which has no module entry point); the secret appears only in the `env` block and never on a command line; a previous file with a missing restriction key produces a warning; regeneration overwrites edits; the Codex launch sets `CODEX_HOME` to the generated home, copies `auth.json` into it (and refuses to start with an explanatory message when the source credential is absent), and passes exactly `--profile swreview`, the five `-c` overrides listed in the contract, and `-C <run_dir>`; the Gemini launch passes `--approval-mode plan` and the settings-home override **named by T055a** (this assertion may not be written until that spike lands)
- [X] T057 [US3] Implement `extractor/SwReview.AddIn/Terminal/CliProfileWriter.cs` (resolve the settings `python` field into the `swreview mcp` command line per the table in `contracts/cli-profiles.md`; write the complete Codex config home and copy `auth.json` into it; write the Gemini settings home) plus the persona texts `Terminal/Profiles/codex-instructions.md` and `Terminal/Profiles/GEMINI.md` (read-only rule, tool list, run folder layout; no numbers from memory)
- [X] T058 [P] [US3] Write `reviewer/tests/unit/test_mcp_server.py` with the `mcp` client over in-memory streams: the tool list equals `contracts/mcp-toolset.md` exactly (no check tools, no `record_drawing_finding`, no `bridge_interference`); schemas are the canonical schemas; a call is appended to `chat-log.jsonl`; a tool that raises and a tool that returns `{"error": ...}` both reach the client as the same error result and both appear in `chat-log.jsonl` with `status: "error"` (SC-003 admits no missing call, including failures); missing `package.json` makes every query return an error result; `bridge_*` tools appear only with `--bridge-pipe`; resources `swreview://package/summary` and `swreview://report`
- [X] T059 [US3] Implement `reviewer/src/swreview/mcp/server.py` and `mcp/chat_log.py` calling tools through the **same** `RecordedTool` from T013 with the chat-log sink and no review session, so validation, recording, error conversion and the never-raise rule are not reimplemented; wire `swreview mcp` in `cli.py`. No `swreview/mcp/__main__.py` is created: the profiles invoke the subcommand
- [X] T060 [US3] Implement the Terminal page `extractor/SwReview.AddIn/Terminal/TerminalPage/{index.html,term.js,term.css}` (xterm.js + fit addon, base64 output decode, input and resize messages, CLI dropdown, Start/Stop, install-steps view, chat-log count; the same CSP meta tag and the same text-not-markup rule as the Review page) and the terminal tab in `TaskPaneControl.cs`, with the terminal run folder resolved through the shared helper from T040
- [X] T061 [US3] Write `extractor/SwReview.AddIn.Tests/TerminalPageContractTests.cs`: message types match `contracts/pane-host-messages.md`
- [X] T061a [US3] Write `extractor/SwReview.AddIn.Tests/ToolListingCheckTests.cs` against a Codex startup listing fixture and a Gemini `/mcp` transcript fixture: exact match starts the session; a missing allowlisted tool, and a tool outside both the MCP allowlist and the CLI's intended built-in set, each block the start with the named difference. This is the parser the plan's Gemini-naming risk row relies on
- [X] T062 [US3] Implement the first-launch tool listing check in `TerminalSession` (Codex startup tool list parse; Gemini `/mcp` output parse) comparing against the MCP allowlist **and** the intended built-in set from `contracts/cli-profiles.md`, and **failing the terminal start** on any difference. It is a gate, not a warning: silently falling back to an unrestricted CLI is the exact failure the read-only guarantee exists to prevent
- [ ] T063 [US3] Run quickstart Scenario 3 with Codex (Gemini deferred until T055a), starting from the Terminal tab with no prior review, and run the scripted refusal probe (a write into the run folder and a shell command outside the sandbox) for both CLIs; record in `benchmarks/native/bracket-assy/notes.md`, including the refusal text and the seconds from opening the tab to a tool-backed answer (SC-008)

**Checkpoint**: General chat answers through our tools; writes are refused; no orphan processes.

---

## Phase 7: Polish & Cross-Cutting Concerns

- [X] T064 [P] Update `specs/001-agentic-design-review/contracts/cli.md` (new `chat serve`, `mcp`, `audit-secrets`, `--provider`/`--model`, and the `--model <id>` row whose default is still documented as `claude-opus-5` - it becomes the per-provider default from `agent/settings.py`), `contracts/agent-tools.md` (tool schemas are generated by `providers/schema.py` and are provider-neutral; replace the opening sentences that say the tools are exposed to Claude and decorated with the Anthropic SDK's `@beta_tool`; update the `mark_coverage` `scope` and `bridge_interference` `settings` rows to the explicit models from T005a), and `specs/001-agentic-design-review/contracts/README.md` (the row describing the Python tool layer as feeding Claude). FR-026 covers documentation as well as code
- [X] T065 [P] Update `README.md`, `reviewer/README.md`, `extractor/README.md`, and `NOTICE.md` (xterm.js, ConPTY sample, openai, google-genai, mcp, starlette licenses; SwpilotCLI reimplemented, not copied)
- [X] T066 [P] Add an `.github/workflows/reviewer.yml` step running `swreview audit-secrets` over the test output directory with a dummy key set
- [ ] T067 *(partial 2026-09-13: pytest, ruff, dotnet, chat serve smoke, mcp tool listing and console usage verified; SOLIDWORKS and credential halves remain)* Run every quickstart scenario end to end; fix discrepancies between quickstart, contracts, and behavior
- [X] T068 DRY and constitution review across `providers/`, `chat/`, `mcp/`, `tools/`, and `SwReview.AddIn/`; remove duplicated event or message shaping (the MCP server must be going through T013's `RecordedTool`, not a second copy of it); confirm no Anthropic references remain in code, config, or docs for this feature, including string constants such as a default model id, with the scan scoped to `reviewer/src/` plus the emitted `session.json` and `events.jsonl` artifacts

---

## Dependencies & Execution Order

- **Setup (Phase 1)** → **Foundational (Phase 2)** → **US2 (Phase 3)** → **US1 (Phase 4)** → **US4 (Phase 5)** → **US3 (Phase 6)** → **Polish**
- US1's Show in SOLIDWORKS works in-process from Phase 4 without US4; US4 adds the shared tool service that US3's MCP server needs for captures and measurements
- US3's Python MCP server (T058, T059) can be built in parallel with Phase 5

### Parallel Opportunities

- Phase 1: T002 through T005 after T001
- Phase 2: T006/T007, T008/T009, T010/T011 in parallel; T017/T018 and T019/T020 in parallel after T013; T012a before T013
- Phase 3: T026, T027, T029 in parallel
- Phase 4: T035, T036, T039 in parallel; T042, T042a, T042b before T043
- Phase 6: T052, T054, T058 in parallel; T055a before T056; T061a before T062

### Test-before-implementation ordering

Task numbers are file order, not always authoring order. These test tasks are written and
failing **before** the implementation they cover, even though their numbers come after it:
T024 before T015's event emission is trusted (it can be written from the schema alone),
T042/T042a before T041, T050 before T034 and T047, T061a before T060 and T062.

---

## Implementation Strategy

1. Finish Phase 2 and prove `swreview review --provider fake` and both recorded adapters (this alone removes the Anthropic dependency and unblocks the org's constraint).
2. Deliver US2 then US1 with the fake provider so the pane is demonstrable without keys.
3. Deliver US4 to make live actions in-process, then US3 for general chat.
4. Workstation steps: T044, T051 and T063 need SOLIDWORKS; T055a needs the Gemini CLI and must land before T056 freezes the Gemini profile assertions; T018a needs an OpenAI key.

## Notes

- Never pass a key or a bridge secret on a command line; environment blocks only.
- Regenerate CLI profiles on every launch into a config home we own; never trust the engineer's global CLI config for restrictions, and never require a profile to already exist there.
- Feature 001 golden baselines must remain unchanged through the provider port.
- `contracts/chat-events.schema.json` is authoritative for event bodies; `data-model.md` section 2 is a summary of it.
- Workstation tasks T044, T051 and T063 depend on feature 001 task T061 having prepared `benchmarks/native/bracket-assy/`; SOLIDWORKS files are not in the repository.

## Deferred decisions (raised in review, not resolved here)

- **`POST /stop` has no caller.** Decision 2026-09-13: add a Stop button to the Review page in T041 and an FR (FR-030) - a running turn must be interruptible from the pane. The endpoint and the `stopped` turn reason exist, and the contract now says `/stop` emits `turn.ended {reason: "stopped"}`, but no FR requires a Stop control and T041 builds none. Either add a Stop button plus an FR, or delete the route, the enum value and the `/stop` clause in T036. Product call.
- **The effort mapping table.** `contracts/chat-events.schema.json` now allows an integer `provider_value` and `data-model.md` requires an adapter to fail fast rather than downgrade, but the actual four-level-to-provider table (what `xhigh` means on each OpenAI model, and on Gemini `thinking_level` vs the 2.5 `thinking_budget`) still has to be chosen and written into a contract before T017/T019 can assert it.
- **Behavioural tests for the two pages.** T042/T061 remain string scans plus the injection test in T042a; driving `app.js`/`term.js` under a headless JS runner (reconnect sends the highest seen `seq`, event 501 evicts event 1, the follow-up input is disabled between `tool.started` and `turn.ended`) would need a JS toolchain the repository does not have. Decide between that and asserting the same behaviours from the host-side xUnit tests plus quickstart steps.
