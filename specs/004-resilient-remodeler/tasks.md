---

description: "Task list for the Resilient re-modeler, stage 1 (reorganize)"
---

# Tasks: Resilient Re-modeler (stage 1)

**Input**: Design documents from `/specs/004-resilient-remodeler/`

**Prerequisites**: plan.md, spec.md, research.md, data-model.md, contracts/, quickstart.md; features 001 and 002 complete; feature 003 complete **through US6**.

**Feature 003 US6 deliverables are consumed here, never re-created.** Each one is built in 003 and only referenced below:

| Consumed from 003 US6 | Path | Used by |
|---|---|---|
| part-root node in the component tree dumper | `extractor/SwReview.Extractor/Dump/ComponentTreeDumper.cs` | every dump of the copy |
| `DumpProfile.ModelCheck` (features and equations; no faces, holes, fasteners, meshes) | `extractor/SwReview.Extractor/Dump/` | `remodel.snapshot`, `package-before.json`, `package-after.json` |
| `run_rms_check` | `reviewer/src/swreview/checks/rms/run.py` | `rms-before.json`, `rms-after.json` |
| `RmsGrade` | `reviewer/src/swreview/checks/rms/grade.py` | `grades.json`, the report header |
| `PaneActions` (`entity.show`, `report.open`, `folder.open`, `log.open`, run-root containment, redaction) | `extractor/SwReview.AddIn/Review/PaneActions.cs` | the Remodel host delegates to it |
| `dom.js` (`el`, `write`, `clear`, `append`, `button`, `field`, `list`, `scalar`, `compact`, `seconds`) | `extractor/SwReview.AddIn/web/shared/dom.js` | the Remodel page loads it |
| `FeatureSelection` (pure selection strategy, no COM in the signature) | `extractor/SwReview.AddIn/Review/FeatureSelection.cs` | `remodel.show_change` |

A part opened alone dumps zero features until the part-root node lands, and the re-modeler's only subject is a part opened alone (the copy it just made), so **no task below can start before 003 US6 sub-phases 9a and 9b land**.

**Tests**: REQUIRED. Every implementation task is preceded by its own test task, and the test must fail before the implementation exists. Feature 001, 002 and 003 golden baselines stay byte-identical; the feature 001 finding contract is unchanged; `ReadOnlyGuard` is not widened by any task in this feature.

**API convention**: every interop member named below is marked `(VERIFIED)` when the member exists with that signature, or the enum constant has that integer value, on SOLIDWORKS 2024 SP5 / `SolidWorks.Interop.sldworks` 32.5.0.48, and `(UNVERIFIED)` when only its behavior is in question. VERIFIED never means the call behaves; that is what Phase 2 is for.

**Scope**: stage 1 (reorganize) only. Stage 2 (rebuild) does not ship in this increment; it is re-specified after stage 1 runs on real parts. The `GateResult` type is tri-state (`pass` / `fail` / `unresolved`) from day one so stage 2 adds a tier rather than changing a type. Parts only. The reviewer and the Model check tab stay read-only and no task below changes that.

**Organization**: phases 0 to 9. Phases 0, 2, 3, 4 and 9 are setup, probe, foundational and polish and carry no story tag, exactly as feature 003 does.

- **US1** (P1) Reorganize a part into the six groups with the geometry proven unchanged. Phases 5 (T074 to T089) and 6.
- **US2** (P2) The dry-run plan report. Phase 1.
- **US3** (P3) Descriptions and global variables proposed by the model, applied by the executor. Phase 7.
- **US4** (P4) The run-to-completion report. Phase 5 (T090 to T093) and Phase 8.
- **US5** (P5) The source is never touched. Phase 3.

## Format: `[ID] [P?] [Story] Description`

- **[P]**: can run in parallel (different files, no dependency on another unfinished task)
- **[Story]**: US1 to US5 exactly as spec.md numbers them, mapped to phases above; setup, probe, bridge and polish tasks carry none

## Path Conventions

- Python: `reviewer/src/swreview/`, tests in `reviewer/tests/`
- C# extractor and bridge: `extractor/SwReview.Extractor/`, `extractor/SwReview.Extractor.Console/`, tests in `extractor/SwReview.Extractor.Tests/`
- C# add-in and pane: `extractor/SwReview.AddIn/`, tests in `extractor/SwReview.AddIn.Tests/`
- Run artifacts: `<run_root>/<yyyyMMdd-HHmmss>-<doc>-remodel/`

---

## Phase 0: Decide

**Purpose**: write the governing rule down, then convert the single biggest unknown (RK-1: `ReorderFeature` cannot move a feature past a dependency, so stage 1 may change almost nothing on a real part) into a number, **before any C# write code exists**.

**Ordering**: T001 runs first. T002 to T004 consume Phase 1's planner and therefore execute after Phase 1 and before Phase 2. Phase 0 is numbered first because it is the gate on Phases 2 to 9, not because all of its tasks run first.

- [x] T001 Amend `.specify/memory/constitution.md` from **1.0.0 to 1.1.0**: add the subsection "Documents are read, not written" to **Technical Constraints**, after the "SolidWorks-side code" bullet, carrying the read-only rule and exactly two bounded exceptions (003's suppressibility test; 004's re-modeler, which may create and modify exactly one document, a copy it created this session, must not save over a file that already existed, may use only `IModelDoc2.Save3` (VERIFIED, takes no filename), records and re-checks the source file's size, last-write time and content hash, writes through a document-scoped allowlist guard that re-verifies path, session tag and COM identity before each write, records an inverse for every applied change, is bounded by a change count and a wall clock, keeps the model out of the mutation, and reports geometric equivalence as `pass`, `fail` or `unresolved` and never as a boolean); update the file's Sync Impact Report comment (version change 1.0.0 to 1.1.0; modified principles: none; added sections: the new Technical Constraints subsection; removed sections: none; templates: no edits required; migration: `specs/003-resilient-modeling/plan.md`'s Complexity Tracking row for `suppress-test` now cites this section); no principle is added, removed or redefined, and **no new Principle VII is created**, because the rule is a constraint on where code may write, not a principle about what evidence means
- [x] T002 Dry run: `swreview remodel plan <package.json> --json` (T029) over the `benchmarks/native/rms-part` packages A and B and the `rms-part` golden fixtures; record per part the count of features that reach their target group, the count pinned and the blocking dependency edge for each, the groups that come out non-contiguous and the interloper for each, and the rebuild list by reason from the reason taxonomy
- [ ] T003 Dry run: the same command over **3 to 5 real production parts** nominated by the owner, from `ModelCheck`-profile packages only; SOLIDWORKS is never asked to write and no copy is made; record the same five numbers per part plus the per-part reorganizable fraction
- [ ] T004 Write `specs/004-resilient-remodeler/phase0-decision.md`: the numbers from T002 and T003 in one table, the honest expectation stated (a run that moves 3 of 200 features says "this part needs rebuilding", not "success"), and an explicit written **go / no-go** on staged versus rebuild-only, signed by the owner; a no-go stops the feature here and Phases 2 to 9 are not started
- [ ] T148 Added 2026-09-25 (owner, decision 17A): a **provisional** T003. Run the read-only `swreview remodel plan` over the three real single-part `ModelCheck` packages the development machine holds (IR 1.3.0 and 1.4.0), once before T142 to T147 and once after, and record **counts only** in `phase0-decision.md` as a dated section beside T003's pending section: per package the feature rows, the content features, the target group reached, the moves and renames planned, the rebuild list by reason, and every refusal with its reason; no part name, feature name, file name or path. The section says in its first line that it does not fill T003's table and that the seat's current-IR packages (decision 18A) replace it; T003 stays open
- [ ] T149 Added 2026-09-25 (owner, decision 17A): correct `spec.md`'s back-burner paragraph (and any other place that says so) that FeatureWorks needs a Professional or Premium seat. FeatureWorks ships with SOLIDWORKS Standard, Professional and Premium; the open question for the import path is whether the add-in is enabled on the seat, which nothing in this feature has checked

**Checkpoint**: the constitution carries the re-modeler exception; the owner has a number for RK-1 and a written decision.

---

## Phase 1: Planner (US2)

**Purpose**: the whole planner, pure Python, no SOLIDWORKS, no LLM, no I/O beyond reading a package. Fully fakeable with builder fixtures. This is a deliverable in its own right (a "how re-modelable is this part" report) even if nothing after it ships.

- [x] T005 [P] [US2] Add `reviewer/tests/support/remodel.py`: builders on top of 003's `tests/support/features.py` for part trees with a declared dependency graph (parent and child ids), duplicate feature names, a shared sketch with two consumers, a variable-radius fillet with `default_radius is None`, an `unknown` type, a group folder holding the wrong members, a derived subfolder, and scope signals (multibody, weldment, sheet metal, mesh, 3D Interconnect, imported dumb solid, surface bodies, configuration count); with `reviewer/tests/unit/test_support_remodel.py` asserting every built package validates against IR **1.2.0** and round-trips, and that every builder stamps `extractor.profile: "model_check"`, because that is the shape feature 003 US6 actually produces and the shape `package-before.json` will carry; a fixture built at 1.1.0 is not a package the planner will ever receive, since both serializers forbid unknown members
- [x] T006 [P] [US2] Extend `reviewer/tests/unit/test_rms_type_table.py`: the new `default_group_by_class` key is loaded; every `FeatureClass` maps either to one of the six RMS group names or to `needs_judgement`; `unknown` and `ambiguous` map to `needs_judgement` and never to a group; the group names are exactly the six the checker already uses, so the checker and the planner cannot disagree about what "should" means
- [x] T007 [US2] Add `default_group_by_class` to `reviewer/src/swreview/checks/rms_types.yaml` and its accessor on `RmsTypeTable` in `reviewer/src/swreview/checks/rms_types.py`; one new key in the existing file, not a second table
- [x] T008 [P] [US2] Write `reviewer/tests/unit/test_remodel_target.py`: `target_group(row, tree, table)` returns `Resolved(group, basis)`, `NeedsJudgement(reason, candidates)` or `NotContent()`; one case per class; a sketch follows its single consumer; an unconsumed sketch; a sketch with two consumers is `NeedsJudgement(shared_sketch)`; `unknown` and `ICE` are `NeedsJudgement`, never a guess; folders and end-tags are `NotContent`
- [x] T009 [US2] Implement `reviewer/src/swreview/remodel/target.py`
- [x] T010 [P] [US2] Write `reviewer/tests/unit/test_remodel_rank.py`: the key is `(group_index, intra_rank, original_index)`; one case per intra-group rule already graded by `checks/rms/part.py` (`shell_last`, `holes_last`, `transform_before_replicate`, `chamfers_before_fillets`, `largest_fillet_first`); a fillet whose `default_radius is None` is **blocked**, never given an arbitrary position; ties fall back to the original index so the sort is total and deterministic
- [x] T011 [US2] Implement `reviewer/src/swreview/remodel/rank.py`
- [x] T012 [P] [US2] Write `reviewer/tests/unit/test_remodel_names.py`: duplicate feature names across the whole tree are detected (SOLIDWORKS permits the same name inside different folders, and `IModelDocExtension.ReorderFeature` (VERIFIED) is **name-addressed**, so a duplicate makes a reorder ambiguous with no error code to tell you); the rename plan produces a unique, recorded, reversible new name applied **before any reorder**; a feature that cannot be safely renamed goes on the rebuild list with reason `ambiguous_name`; the check is one pass over `features[]`
- [x] T013 [US2] Implement `reviewer/src/swreview/remodel/names.py`
- [x] T014 [P] [US2] Write `reviewer/tests/unit/test_remodel_order.py`: Kahn's algorithm with a priority queue keyed by desired rank yields the RMS-closest legal order; an already-ordered tree yields **0 moves**; a reversed tree with no dependencies yields a full reorder; the LIS of current positions under the achievable order yields the **minimal** edit script, asserted as minimality rather than as a fixed list on a 200-row fixture (tens of moves, not 200); every emitted `Move.location` is in the closed set `{"before", "after"}` and the planner can never emit `to_end`, `to_top` or `to_folder`, which is the same closed set `contracts/guard-allowlist.md`'s option-composition test forbids the guard from composing (`swMoveLocation_e.Before = 2`, `After = 3`, VERIFIED values); a cycle is refused with the cycle named and the function never loops; the achievable order is produced before any folder exists
- [x] T015 [US2] Implement `reviewer/src/swreview/remodel/order.py` (Kahn plus LIS)
- [x] T016 [P] [US2] Write `reviewer/tests/unit/test_remodel_feasibility.py`: one fixture per rebuild-list reason with the reason, the subject and the blocking edge asserted: `backward_reference`, `shared_sketch`, `splits_group`, `cycle`, `radius_unreadable`, `ambiguous_name`, `unclassified` (a `GetTypeName2` (VERIFIED) value absent from `rms_types.yaml` reports `unresolved` and is **never** reported movable), `graph_unreadable` (`child_ids` and `parent_ids` both null means never move). The taxonomy is asserted as **exactly these eight** (FR-014), with no ninth value reachable. Separately, and **not** as a rebuild reason, one case asserts that an existing folder already carrying one of the six RMS names but holding the wrong members produces the scope refusal `rms_named_folder_wrong_members` (the one token, data-model.md sections 1.6 and 4.2), which in v1 **refuses the whole part before anything is copied**, because `IModelDoc2.EditDelete` is not in the stage-1 allowlist and there is therefore no dissolve path
- [x] T017 [US2] Implement `reviewer/src/swreview/remodel/feasibility.py` (pins by edge, non-contiguous groups by interloper, the rebuild list with a named reason per entry)
- [x] T018 [P] [US2] Write `reviewer/tests/unit/test_remodel_folders.py`: the plan creates each of the six folders once, over a **contiguous** run proven contiguous before creation; an existing folder with exactly the right members is a no-op; an existing folder with a subset or a superset is `rms_named_folder_wrong_members` and refuses the part in v1, with the reason token asserted against the closed `Refusal` code set of data-model.md section 4.2 rather than spelled at the call site; a derived subfolder is preserved and never dissolved; membership equality is asserted on ids, not on names; no plan ever contains a move-into-an-existing-folder step
- [x] T019 [US2] Implement `reviewer/src/swreview/remodel/folders.py`
- [x] T020 [P] [US2] Write `reviewer/tests/unit/test_remodel_intent.py`: the description **gap** list distinguishes `None` (unreadable) from `""` (absent) and never conflates them; the equation inventory reports which equations exist, which are globals, and which are broken; a global proposal is admissible **only** where feature data justifies it (hole diameter, shell thickness, fillet radius), because IR **1.2.0** carries `equations[]` and **no sketch dimensions**; a hard-coded sketch value can never appear as a proposal because it is not in the IR
- [x] T021 [US2] Implement `reviewer/src/swreview/remodel/intent.py`
- [x] T022 [P] [US2] Write `reviewer/tests/unit/test_remodel_scope.py`: `ScopeGate.evaluate(signals) -> Ok | Refusal(reasons[])` with **no COM in the signature**; one case per signal (multibody solid count greater than 1, weldment, sheet-metal folder, mesh or graphics body, and 3D Interconnect all refuse; an imported dumb solid is allowed and the reorganize stage is reported as a no-op; surface bodies are allowed and the gate's surface coverage is reported **uncovered**, never passed; the configuration count is recorded and drives `which_configs` on every equation add); an RMS-named folder holding the wrong members refuses with `rms_named_folder_wrong_members`, the one token of the closed `Refusal` code set (data-model.md section 4.2), and **not** as a ninth rebuild-list reason; two signals at once and the message names **both**; a refusal is a reported coverage gap, never a silent skip. One property test over the whole table asserts that every reason in this gate is decidable from `ScopeSignals` alone, with no copy path and no document in the signature, which is what lets the host refuse before anything is copied (FR-001)
- [x] T023 [US2] Implement `reviewer/src/swreview/remodel/scope.py`
- [x] T024 [P] [US2] Write `reviewer/tests/unit/test_remodel_plan_model.py`: `RemodelPlan` (pydantic) with the field names of `data-model.md` section 1, which is normative: `plan_schema: "1.0"`, `plan_revision`, `state`, `targets[]`, `descriptions[]`, `globals[]`, **`rejected_proposals[]`**, `deviations[]`, `changes[]`, **`rebuild[]`** (not `rebuild_list`), `pins[]`, `folders` as a `FolderPlan{actions, refusals}` (not a bare list), the recorded source attestation, and for an EPDM source the **vault path and revision**; each accepted `DescriptionProposal` and `GlobalProposal` carries its `provider` and `model`, and each `PlanTarget` with `decided_by == "model"` does too, which is what FR-056 has the report print; each `RejectedProposal` carries the tool, the arguments sent, the rejection sentence, the rule id, the provider and the model; there is **no `revisions[]` array and no `stage` field**, because the plan is one object rewritten in place; every change is addressed by persistent reference and never by name or index; an unknown `plan_schema` raises; rev 1 (pure) and rev 2 (after the judgement phase) both validate
- [x] T025 [US2] Implement the models in `reviewer/src/swreview/remodel/plan.py`
- [x] T026 [P] [US2] Write `reviewer/tests/unit/test_remodel_plan_compose.py`: `plan_reorganize()` composes target, rank, names, order, feasibility, folders, intent and scope and emits the change list in the **fixed** order of `data-model.md` section 1.11: C1 rename duplicate names, C2 set descriptions, C3 reorder into the achievable order, C4 create folders, C5 repair existing globals in place (`equation.edit`), C6 add new globals; the order is asserted as an invariant of the emitted list, not left to a comment; a case asserts the plan can contain **no dimension rename and no dimension equation** at all, because FR-030 puts both out of this version; an empty change list is a legitimate plan and reports why
- [x] T027 [US2] Implement `plan_reorganize()` in `reviewer/src/swreview/remodel/plan.py`
- [x] T028 [P] [US2] Write `reviewer/tests/unit/test_cli_remodel_plan.py`: `swreview remodel plan --package --document --json` prints, per part, features reaching their target group, pins with their edge, non-contiguous groups with their interloper, and the rebuild list by reason; exit 1 with a named reason on a scope refusal; the command performs **no writes and constructs no provider**, asserted with the `agent.providers` factories monkeypatched to raise
- [x] T029 [US2] Implement `swreview remodel plan` in `reviewer/src/swreview/cli.py` reusing the existing `check` commands' output shape, and add its row to `specs/001-agentic-design-review/contracts/cli.md`
- [x] T030 [US2] Golden fixtures `reviewer/tests/golden/fixtures/remodel-plan/` from the T005 builder: **four golden plans** (a compliant part that yields 0 moves; a part that reorganizes fully; a part where half the tree is pinned; a part that refuses) plus **one fixture per refusal category** of T016, with `checks/golden_remodel.py` adapter `plan_case(package)`; generate the baselines once and hand-check every reason id against the taxonomy

**Added 2026-09-25 (owner, decision 17A)**: three defects the real packages showed, each test-first. Verified on the three real `ModelCheck` packages of T148; only counts and SOLIDWORKS type names (API vocabulary) are recorded.

- [x] T142 [P] [US2] Write `reviewer/tests/unit/test_remodel_nodes.py` and the builder `absorbed_twice` in `reviewer/tests/support/remodel.py` (with its cases in `test_support_remodel.py`): a real package lists every absorbed sketch **twice** - at depth 0, in traversal order just before the feature that consumes it, and again at depth 1 under that feature, with the same `persist_ref`, name, type, readings and edges, while every other row's edges name only the depth-1 id - and the planner planned both (two targets, a duplicate-name rename of one feature against itself, and two reorders naming one persist ref). The builder lays a fictional, code-built package out in exactly that shape. Cases: the depth-1 row under a non-folder feature whose `persist_ref` and `type_name` equal exactly one depth-0 row's is that row listed a second time and is merged into it; every `parent_ids`, `child_ids`, `sketch.consumer_ids` and `folder_id` naming the dropped id names the kept one, once; the kept row's index, depth and readings stand; the merge gives back the flat walk's tree edge for edge; a row with no depth-0 twin (the Hole Wizard's own profile sketch), a depth-1 row under a **folder**, a twin of another type, and a persist ref two depth-0 rows share (the seven system folders of the real packages) are each **not** merged; a sketch listed under two consumers is one node; an unreadable edge list stays `None`; rows of two documents are refused. Over `plan_reorganize`: one target per feature, no rename, no persist ref moved twice, the same targets, moves, pins, rebuild list and folders as the tree without the second listings, and a coverage item naming every merged row (written on every plan, "none" included). `test_remodel_plan_model.py`: a persist ref is the subject of at most one `rename` and at most one `reorder`, so a planner that reads one feature as two is refused by the plan type rather than handed to the executor
- [x] T143 [US2] Implement `reviewer/src/swreview/remodel/nodes.py` (`tree_nodes`, `MergedRow`), read by `plan_reorganize` before target, rank, names and order, the `second listings` coverage item, and the `RemodelPlan` invariant; state the rule in `contracts/run-artifacts.md` ("How the planner reads the tree") with a pointer from `data-model.md` section 1's `targets` row; add the golden fixture `remodel-absorbed-sketches` to `generate_packages.py` and regenerate the remodel-plan baselines (the only change to the thirteen existing ones is the new coverage item), feature 001, 002 and 003 goldens byte-identical
- [ ] T162 [P] [US2] Added 2026-09-25 (decision 17A, found on the same real packages while verifying T143): a row the walk finds **only** under the feature that owns it - the Hole Wizard's own profile sketch, which sits after its hole in the listing and is that hole's parent - was planned as a feature with a position of its own, so the larger real part's plan moved two such sketches on their own and anchored two moves to them, and a folder could have tried to select one. Extend `test_remodel_nodes.py` and add the builder `carried_under` (a sub-feature row with no top-level twin, at the owner's depth plus one, directly after the owner or after its second listings): a row at depth 1 or deeper whose enclosing row is a feature, not a folder, and which is not a second listing, is **carried** by the nearest kept feature above it; it gets no target, is never the subject or the anchor of a move and never a folder member, every edge naming it names the owner instead (an edge to the owner itself is dropped), and its own parents and children become the owner's, an unreadable list on either side leaving the owner's list unread. A row under a folder is never carried. Over `plan_reorganize`: no move names a carried row, the plan equals the plan of the same tree without it, and a `carried sub-features` coverage item names every carried row on every plan
- [ ] T163 [US2] Implement it in `remodel/nodes.py` (`CarriedRow`), read by `plan_reorganize`, with the coverage item; extend the rule in `contracts/run-artifacts.md` ("How the planner reads the tree"); extend the golden `remodel-absorbed-sketches` with the hole's own profile sketch, and regenerate the remodel-plan baselines (the only change to the others is the new coverage item)
- [ ] T144 [P] [US2] Extend `reviewer/tests/unit/test_rms_type_table.py` and `test_remodel_target.py`: the census of every `GetTypeName2` value the three real packages carry (35 names, recorded as SOLIDWORKS type names only) leaves **no content type unclassified** - each is in a class, the ambiguous set, `tolerated_loose`, the folder type, or the derived-base set of T146; the system types the planner counted as content and put on the rebuild list as `unclassified` - `NotesAreaFtrFolder`, `AnnotationViewFeat`, `AmbientLight`, `DirectionLight`, the derived part's body and reference sub-folders (`FeatSolidBodyFolder`, `FeatSurfaceBodyFolder`, `RefAxisFtrFolder`, `RefPlaneFtrFolder`, `ProfileFtrFolder`, `RefPointFtrFolder`) and `CosmeticThread` - are `NotContent` for the planner and not content for feature 003's rules, and never reach the rebuild list
- [ ] T145 [US2] Add those names to `tolerated_loose` in `reviewer/src/swreview/checks/rms_types.yaml` with their provenance (the 2026-09-25 census of 2024 SP5 dumps); record in `data-model.md` section 9 that this is feature 004's second edit to the feature 003 table and why; `calibrated_version` is unchanged (T137 calibrates on the seat). A type seen on no real package is not added
- [ ] T146 [P] [US2] Write the derived-part refusal cases: a part whose tree carries a derived or mirrored base feature (`MirrorStock` on the real packages; `Stock`, Insert Part's base feature, from the API's type-name list and not yet seen in a package) is **refused**, with the new closed code `derived_part` naming the feature and a reason that says what is true - the body is another part's geometry brought in by that feature, so the features the six groups organize do not build it; the plan is `failed`, carries no change, lists the refusal among `plan_refusals`, and `_why_changes` says why in its own sentence; the code set of `data-model.md` section 4.2 is partitioned into gate, bridge-only and tree codes; the type table's new `derived_base` key is required, loaded and disjoint from `tolerated_loose`; a golden fixture `remodel-refusal-derived-part`
- [ ] T147 [US2] Implement the refusal row: `derived_base` in `rms_types.yaml` and its accessor on `RmsTypeTable`, `derived_part` in `remodel/scope.py`'s `RefusalCode` (a tree code, decided from the package, never by `ScopeGate.evaluate`), `RemodelPlan.tree_refusals`, `summary.plan_refusals`, and the refusal table row in `scope.py`'s docstring; `data-model.md` sections 1 and 4.2 and `contracts/run-artifacts.md` updated. It is decided **after** the copy, like the cycle refusal; the probe half that would refuse **before** the copy is T161

**Checkpoint**: `swreview remodel plan --json` runs over the benchmark packages and real parts with no SOLIDWORKS and no model; Phase 0's T002 to T004 can now execute.

---

## Phase 2: Probe

**Purpose**: answer, on the workstation, the behavioral questions no reflection dump can answer, **before any of the write code in Phases 3 to 6 is trusted**. Every probe **builds its own throwaway part** in the run folder and never opens an engineer's file, so it stays inside the constitution's exception 2 (a document created this session). The probe restores the system toggles in a `finally`.

- [x] T031 [P] Write `extractor/SwReview.Extractor.Tests/RemodelProbeTests.cs` and extend `CommandLineOptionsTests`: `probe remodel` parses `--probe <id,...>`, `--out` and `--keep-part` and requires an explicit acknowledgement flag; the throwaway-part recipe builder is pure and testable (box, cut, fillet, chamfer, shell, one folder, one equation, and two features deliberately sharing a name); the ledger writer emits one row per probe with `{probe_id, question, method, raw_result, verdict: verified|refuted|unresolved, sw_version}`; a probe that throws records `unresolved` and never `verified`
- [x] T032 Implement the `probe remodel` command in `extractor/SwReview.Extractor.Console/Program.cs` and `extractor/SwReview.Extractor/Rms/RemodelProbe.cs`: builds the throwaway part into the run folder, runs the selected probes, writes `capabilities/remodel-<sw-version>.yaml`, and records and restores `swInputDimValOnCreate = 10` (VERIFIED), `swShowErrorsEveryRebuild = 77` (VERIFIED) and `swWarnSaveUpdateErrors = 329` (VERIFIED) in a `finally`
- [ ] T033 **PROBE-1 (blocking)**: does `ISldWorks.CommandInProgress = true` suppress the "Cannot reorder" message box? Deliberately request an illegal reorder with the flag set and with it clear, watched from a watchdog thread; record whether the call returns or the STA thread blocks. A refuted PROBE-1 means stage 1 cannot run unattended and Phases 5 and 8 stop until the owner decides
- [ ] T034 **PROBE-3 and PROBE-4 (blocking)**: does `IModelDocExtension.ReorderFeature(String, String, swMoveAfter = 3)` (VERIFIED signature, UNVERIFIED behavior) move a feature on 2024, and does it return a bare `false` rather than corrupting the tree when asked to move past a dependency? Do feature folders require **contiguous** members on 2024? Record the exact strings passed and the tree state after each attempt
- [ ] T035 **PROBE-2 (blocking)**: equation units. In a part whose document length unit is mm, add `"w" = 120` and measure the resulting dimension; read `IEquationMgr.get_Value(i)` (VERIFIED) and record whether it returns document units or metres; record `IDimension.SystemValue` (VERIFIED, always metres) beside it. A wrong answer here silently builds a 120 metre part that rebuilds cleanly and passes every non-geometric check
- [ ] T036 **PROBE-6, PROBE-7 and PROBE-21**: does `IEquationMgr.Add3` (VERIFIED) add on 2024 or silently return `-1`? Does `set_Equation(i, text)` (VERIFIED) edit in place from C#? Does `IEquationMgr` on a part with **no** equations report count 0 or throw? Record `GetCount()` before and after every call
- [ ] T037 **PROBE-12 (blocking) and PROBE-13**: does `ICustomPropertyManager.Add3(key, 30, value, 2)` (VERIFIED) tag the document and does `Get4` (VERIFIED) read it back on 2024, since the tag is one of `VerifyTarget`'s four checks? Does `File.Copy` succeed on a `.SLDPRT` SOLIDWORKS currently holds open, and does the `new FileStream(src, Open, Read, FileShare.ReadWrite)` stream fallback succeed when it does not?
- [ ] T038 **PROBE-5, PROBE-9, PROBE-10, PROBE-11 and PROBE-20** (all non-blocking, each with a documented fallback): does `ReorderFeature(f, folderName, swMoveToFolder = 5)` (VERIFIED constant) move a feature into an existing folder, does `IFeatureManager.MoveToFolder` (VERIFIED present) work or silently no-op, and does `IFeature.MakeSubFeature` (VERIFIED present) work (an optimisation only; the plan needs none of them); what element type does `GetWhatsWrong`'s out array carry on 2024, names or `Feature` objects, with `IFeature.GetErrorCode2` (VERIFIED) staying primary either way; does the `___EndTag___` marker appear on 2024 and does it keep the folder's **default** name after a rename; the `GetTypeName2` census against the 2026-calibrated table; does `IFeature.Description` (VERIFIED) survive a reorder and a folder wrap, and is it per-configuration?
- [ ] T039 **PROBE-8 (blocking for shipping the gate)**: tolerance calibration. Build a box and a cylinder of exactly known analytic volume, surface area, centre of mass and principal moments; measure each with `IModelDocExtension.CreateMassProperty2()` (VERIFIED) at `swMassPropertyAccuracyLevel_Higher = 2` (VERIFIED) with `Recalculate()`'s Boolean checked before anything is read; record the **attained** relative error per quantity into the capabilities ledger. A tolerance that has never been compared against a known answer does not ship

**Checkpoint**: the capabilities ledger names a verdict for PROBE-1, 2, 3, 4, 8, 12 and 13; every blocking probe is `verified` or the owner has decided what to do about it.

Code note (2026-09-19): all fifteen probe executors are implemented and fake-tested; the real verdicts, and T033 to T039's checkboxes, await the workstation run.

---

## Phase 3: Guard and scope (US5)

**Purpose**: make it structurally impossible to write to the engineer's file, and prove it without a SOLIDWORKS seat. Two layers, because a call guard sees only a member name (the document is captured inside the lambda at `SwGate.Call("GetChildren", () => component.GetChildren())`), so "document-scoped" cannot be a property of `ICallGuard` alone.

- [x] T040 [P] [US5] Write `extractor/SwReview.Extractor.Tests/CallKeyTests.cs`: `QualifiedKey(iface, member)` round-trips and `BareName(qualified)` returns the member; a bare key handed to a remodel call site is a programming error and throws; the reviewer's existing bare-name gate sites are untouched, proven by `GuardTests` and `SwGateTests` staying green **with no edits**
- [x] T041 [US5] Implement `extractor/SwReview.Extractor/Guard/CallKey.cs` and migrate **only** the remodel call sites to interface-qualified keys; the qualified form is what closes two real collisions, `ICustomPropertyManager.Delete2(String)` (VERIFIED) being refused today because the bare name `Delete2` means `IEntity.Delete2`, and an allowlist entry written as `"Add3"` silently permitting `ICustomPropertyManager.Add3` as well as `IEquationMgr.Add3`
- [x] T042 [P] [US5] Write `extractor/SwReview.Extractor.Tests/RemodelGuardTests.cs`: the stage-1 allowlist of `contracts/guard-allowlist.md` asserted as an exact table, `IModelDocExtension.ReorderFeature`, `IFeatureManager.InsertFeatureTreeFolder2`, `IFeatureManager.EditRollback`, `IFeature.set_Name`, `IFeature.set_Description`, `IFeature.Select2`, `IEquationMgr.Add2`, `Add3`, `Delete`, `set_Equation` and `SetEquationAndConfigurationOption` (the last two reached only by `SetEquationVerified`, the `op: "set"` repair path of FR-029, so no allowlist entry is without a call path), `IModelDoc2.ForceRebuild3`, `IModelDoc2.ClearSelection2`, `IModelDoc2.Save3`, `IModelDocExtension.SelectByID2`, `ICustomPropertyManager.Add3`, `ICustomPropertyManager.Delete2`, `ISldWorks.SetUserPreferenceToggle` (the three user-preference toggles 10, 77 and 329 only), `ISldWorks.set_CommandInProgress` (the run-scoped modal-suppression flag of PROBE-1, a property rather than a `swUserPreferenceToggle_e` value, whose call path is the `RemodelSystemToggles` set-and-restore of T052, so it is not an entry without a call path) and `ISldWorks.CloseDoc` (all VERIFIED); the interface collisions of T041 resolved in both directions; **`IModelDoc2.EditDelete` is refused** (owner decision: v1 refuses a mis-membered RMS-named folder instead of dissolving it); every member of the explicit exclusion list refused, one row each (`EditRebuild3`, `SaveAs3` on any path, `SetSaveFlag`, `ModifyDefinition`, `EditSuppress2`, `EditUnsuppress2`, `SetSuppression2`, `EditUndo2`, `EditRedo2`, `StartRecordingUndoObject`, `FinishRecordingUndoObject2`, `SetReadOnlyState`, **`IDimension.set_Name`** (v1 addresses no dimension at all, per FR-030, so the entry would have no call path, which is the same accidental widening `StartRecordingUndoObject` is excluded for), `IModelDoc2.SetSystemValue*`, and the whole `FeatureCut*` / `FeatureExtrusion*` / `InsertFeature*` creation family); anything not on the list delegates to `ReadOnlyGuard.Assert(BareName(key))`; and the allowlist asserted as a **set equality** against `ReadOnlyGuard.DeniedMembers` union `DeniedPrefixes`, read as data after the visibility-only change `contracts/guard-allowlist.md` permits (both collections become `public static readonly IReadOnlyCollection<string>`; no member added, removed or reworded, and nothing in the tree gives `SwReview.Extractor` an `InternalsVisibleTo`), so a denial added upstream cannot silently widen the remodel surface
- [x] T043 [US5] Implement `extractor/SwReview.Extractor/Guard/RemodelGuard.cs` as an `ICallGuard` allowlist over qualified keys
- [x] T044 [P] [US5] Write the `AssertSaveTarget` cases in `RemodelGuardTests`: a path outside the run folder, a path containing `..`, the source path, another run's copy, a `.sldasm`, a path with no extension, and the copy path spelled with a different case or as a short name are **all refused**; only this run's canonicalized copy passes; the test states in one line why `Save3` cannot reach the source (it takes no filename, VERIFIED, and the copy is opened at its own path)
- [x] T045 [US5] Implement `AssertSaveTarget(path)` in `extractor/SwReview.Extractor/Rms/RemodelScope.cs`
- [x] T046 [P] [US5] Write the `AssertFolderSelection` cases: the helper passes only when `ISelectionMgr.GetSelectedObjectCount2(-1) == 1` (VERIFIED) **and** `((IFeature)GetSelectedObject6(1, -1)).GetTypeName2() == "FtrFolder"` (VERIFIED); zero selected, two selected, one selected non-folder and a null selection each throw with the failing condition named; the assertion exists once as a named helper and is **never inlined at a call site**. State in the test file's docstring that v1 has exactly two consumers, neither of which is a write: the planner's `rms_named_folder_wrong_members` refusal, which is written against this same reading, and the stage-2 dissolve precondition. A companion assertion pins that `IModelDoc2.EditDelete` is absent from the stage-1 allowlist, so no v1 path can need it before a delete
- [x] T047 [US5] Implement `AssertFolderSelection()` as a named, tested **refusal predicate with no write call site in v1**, matching `contracts/guard-allowlist.md`. It is **not** a precondition to any folder write: folder creation selects a contiguous run of N content features, which this predicate refuses by construction (it requires `GetSelectedObjectCount2(-1) == 1` and a single `FtrFolder`), so calling it before the `set_Name` that follows `InsertFeatureTreeFolder2(swFeatureTreeFolder_Containing = 2)` (VERIFIED) would fail every folder creation. It is built now rather than in stage 2 so that stage 2 inherits a tested assertion beside its delete rather than writing one under time pressure
- [x] T048 [P] [US5] Write `extractor/SwReview.Extractor.Tests/RemodelScopeTests.cs` over an `IRemodelTarget` fake: `VerifyTarget()` runs **before every single write**, asserted by call count rather than by inspection, and checks (1) `GetPathName()` (VERIFIED) equals the copy path this run created, (2) the `SwReviewRemodelRun` custom property equals this run's id, (3) `ISldWorks.GetOpenDocumentByName(copyPath)` (VERIFIED) returns the **same** COM identity via `Marshal.GetIUnknownForObject`, catching a close-and-reopen underneath us, and (4) the copy path is a canonicalized descendant of this run's folder with `..` resolved; each check failing independently throws `RemodelTargetError` naming which one failed and the run aborts with the change log intact; the happy path asserts the exact member sequence, in order
- [x] T049 [US5] Implement `extractor/SwReview.Extractor/Rms/RemodelScope.cs`, `IRemodelTarget` and `RemodelTargetError`; `RemodelScope` is the only object that holds the copy's `IModelDoc2`, and every write method is `VerifyTarget()` then `gate.Call(qualifiedKey, ...)`
- [x] T050 [P] [US5] Write `extractor/SwReview.Extractor.Tests/RemodelCopyTests.cs`: `File.Copy(source, runDir/copy/<name>-RMS.SLDPRT, overwrite: false)` refuses to overwrite; the `FileShare.ReadWrite` stream fallback runs when the source is held open (RK-9); the copy lives **only** in the run folder and never beside the source; an **EPDM vault** source is copied out and is never refused for vault reasons, with the vault path and revision recorded into `plan.json`; the open options compose to the exact integer `Silent|LoadModel = 17` and never contain `ReadOnly(2)` or `ViewOnly(4)`; the save options compose to the exact integer `Silent = 1` and never contain `Copy(2)`, `SaveReferenced(4)` or `AvoidRebuildOnSave(8)` (all VERIFIED constants); the tag is written and read back; and `GetPathName()` is asserted equal to the copy path and **not equal** to the source path after open
- [x] T051 [US5] Implement `extractor/SwReview.Extractor/Rms/RemodelCopy.cs` and `RunFolders.CreateForRemodel` in `extractor/SwReview.AddIn/Review/RunFolders.cs` so the run-folder naming convention stays in one place; `SaveAs*` is used nowhere in stage 1
- [x] T052 [P] [US5] Write `extractor/SwReview.Extractor.Tests/RemodelSystemTogglesTests.cs`: the toggles are read, set and restored in a `finally` on the happy path, on a thrown exception, and on recovery of a previous run's leftovers; `swInputDimValOnCreate = 10`, `swShowErrorsEveryRebuild = 77` and `swWarnSaveUpdateErrors = 329` (all VERIFIED) plus the separately allowlisted `ISldWorks.set_CommandInProgress` per PROBE-1 (a property, not a fourth `swUserPreferenceToggle_e` value, which is why it carries its own allowlist key); a toggle whose original value could not be read refuses the start rather than guessing one
- [x] T053 [US5] Implement `extractor/SwReview.Extractor/Rms/RemodelSystemToggles.cs`
- [x] T054 [P] [US5] Write `extractor/SwReview.Extractor.Tests/RemodelInteropManifestTests.cs`, method `RemodelInteropManifestTests.CodeMatchesManifest` (pure, always runs): for every member the re-modeler calls, the code's argument builders match the checked-in fixture `extractor/SwReview.Extractor.Tests/Fixtures/InteropSurface/remodel-interop-manifest.json` (the one path `contracts/interop-manifest.md` and `plan.md` both name) per `contracts/interop-manifest.md` on `{interface, member, arity, ordered parameter names, return type}`; a builder for a member absent from the manifest fails the test; the manifest covers every allowlisted member of T042 plus the read members the bridge uses
- [x] T055 [US5] Implement the frozen interop-surface manifest fixture and the argument-builder table it pins; the fixture is generated once by reflection over `SolidWorks.Interop.sldworks` 32.5.0.48 and checked in
- [x] T056 [US5] Write the workstation half of the manifest test as `RemodelInteropManifestTests.InstalledAssemblyMatchesManifest`, in the same class as T054 because the contract puts both tests there, skipped when the interop DLL is absent: regenerate `{interface, member, arity, ordered parameter names, return type}` from the **installed** DLL and diff it against the fixture; a difference fails the build, which is what turns a SOLIDWORKS upgrade from a runtime surprise into a red build
- [x] T057 [P] [US5] Write the remodel observer cases in `extractor/SwReview.AddIn.Tests/ToolServiceWiringTests.cs`: `ToolServiceRequestLogger.Format` for a remodel request records an empty `refused=` and a `gated=` set in which every key on `ReadOnlyGuard`'s denied surface - every key that needed the allowlist to pass - is on the stage-1 allowlist (`gated ∩ (ReadOnlyGuard.DeniedMembers ∪ DeniedPrefixes) ⊆ stage-1 allowlist`). It is **not** asserted that the whole gated set is allowlisted: `SwGate.Guard` calls `observer.Gated` for every member before the guard judges it, so a mutating remodel request also gates `GetObjectByPersistReference3`, `IFeature.get_Name` and `GetWhatsWrongCount`, and the log records strings with no read/write classification in them. The test additionally records the **target path** of every mutating call, so the run report can state from the log rather than from intent that every write went to `<copyPath>`; the secret never appears in the log
- [x] T058 [US5] Implement the remodel `ISwGateObserver` (target path per mutating call) and wire it into `extractor/SwReview.AddIn/ToolService/ToolServiceHost.cs`
- [ ] T150 [P] [US5] Added 2026-09-25 (owner, decision 17A): `ReadOnlyGuard`'s denylist lets the FeatureWorks and import-repair members through as bare names (`RecognizeFeatureAutomatic`, `CreateFeatures`, `ImportDiagnosis` among them). Write `ImportRepairDenylistTests` in `extractor/SwReview.Extractor.Tests/GuardTests.cs`, reading its membership from a new "Decision 17A" table in `contracts/guard-allowlist.md` exactly as `MechanicalChecksDenylistTests` reads feature 010's: every member is refused by the static and the instance guard, whatever its case, bare or interface-qualified; it is refused under `SuppressTestGuard`, `RemodelProbeGuard` and `RemodelGuard` as well, because none of their exemptions or allowlist keys names one; no member is the bare name of a stage-1 allowlist key, so the five overriding keys are unchanged; no string literal of the product source is refused by the table (the scan `DrawingFamilyReadAuditTests` runs, shared rather than copied), which is what "breaks nothing the product calls" means; the reads beside them (`GetImportedFileName`, `GetImportedFeatureParameters`, `Diagnose`, `GetImportFileData`) stay allowed; and, where the interop is installed, every method of `IFeatureWorksApp` in `SolidWorks.Interop.fworks` 32.5.0.48 is either on the table or on the contract's "left open" table with its reason. `RemodelGuardTests.ExpectedDeniedMembers` reads the new table rather than retyping it
- [ ] T151 [US5] Add the members to `ReadOnlyGuard.DeniedMemberSet` under a decision-17A block; add the table and a "left open, and why" table to `contracts/guard-allowlist.md` under "Denials added after stage 1"; amend this phase's checkpoint sentence, because `ReadOnlyGuard` is now narrowed (never widened) by this feature. A member deliberately left open is listed with its reason; the wider creation family (`FeatureFillet*`, `FeatureRevolve*`, `InsertMirrorFeature*`, `InsertPart*`, `MirrorPart*` and the rest) is an owner decision, because a complete table has to be generated from the interop as feature 011's was, and closing `FeatureFillet*` needs a new `RemodelProbeGuard` exemption for the throwaway part's fillet

**Checkpoint**: the guard, the scope, the copy, the toggles and the interop manifest are green with fakes and no SOLIDWORKS; `ReadOnlyGuard` was unchanged by stage 1, and decision 17A (T151) only narrows it: it gains denials and loses none.

---

## Phase 4: Bridge and host

**Purpose**: twelve `remodel.*` **commands** (not tools), because the bridge already has a secret policy, a guard, a circuit breaker, a per-request log of gated members and a document-presence dispatcher. Everything is addressed by persistent reference, never by name and never by index, because names change and indices change on every reorder.

- [x] T059 [P] Write the `remodel.*` command-shape cases in `extractor/SwReview.Extractor.Tests/BridgeDispatcherTests.cs`: `remodel.probe_scope`, `open`, `snapshot`, `rename`, `reorder`, `folder`, `describe`, `equation`, `rebuild`, `geometry`, `save` and `close`; **no command that writes takes a document parameter**, asserted as a property over the whole command table rather than per command, so the target is unreachable rather than validated; exactly two commands name a path at all, `probe_scope` and `open`, both before the scope exists, and a test asserts that after `open` returns no command in the family accepts a path again; `remodel.rename` takes `{persist_ref, new_name}` and has **no dimension form** (FR-030); `remodel.reorder`'s `location` is the closed set `{before, after}`; `remodel.equation`'s `op` is the closed set `{add, set, delete}`; every addressing parameter is a persist ref; an unknown command is an error result and never an exception
- [x] T060 Implement the `remodel.*` command records in `extractor/SwReview.Extractor/Bridge/BridgeProtocol.cs`
- [x] T061 [P] Write the `remodel.probe_scope`, `remodel.open` and `remodel.snapshot` handler cases over a fake scope. `probe_scope` reaches the source through `ISldWorks.GetOpenDocumentByName` (VERIFIED), **opens nothing**, returns no `IModelDoc2`, reads one VERIFIED member per scope signal, mints a `probe_id` against the canonicalized source path, and answers `source_not_open` when SOLIDWORKS does not already have the source open rather than opening it; a test asserts its recorded `refused=` set is **empty** and its `gated=` set is a subset of the checked-in read-only probe surface (the members of `contracts/bridge-remodel.md`'s `scope_signals` table plus `GetOpenDocumentByName`, `GetType`, `GetSaveFlag` and `ListExternalFileReferencesCount2`) with no stage-1 allowlist key in it - the gated set is **not** empty, because `SwGate.Guard` gates reads too - which is the machine-checkable form of "the source is only ever read", and that no `File.Copy` and no `OpenDoc7` happened on this path. `open` refuses with `scope_not_probed` when the `probe_id` is unknown or was minted for a different path, **before** the copy, so FR-001's "before any copy is made" is structural and not conventional; it re-reads the same signals on the copy at step 12 and a field-for-field difference is `scope_changed`; its result shape is `contracts/bridge-remodel.md`'s table row for `remodel.open`, which is normative and is not restated here (it includes `document_length_unit`, the only stated source for FR-027's metres-to-document-unit conversion), the signals being raw measurements decided later by `remodel/scope.py` in Python and not in C#; `open` performs `EditRollback(swMoveRollbackBarToEnd = 1, "")` (VERIFIED) and asserts no feature reports `IsRolledBack()` (VERIFIED), then `ForceRebuild3(false)` (VERIFIED), and a non-zero `GetWhatsWrongCount()` (VERIFIED) **stops the run and deletes the copy and closes the document**, because the part was already broken and nothing after it could be attributed, and because this is the one refusal in the feature that can happen after a copy exists (reading it needs a rollback and a rebuild, both writes, and neither may touch the source) so it is the one refusal that has to clean up after itself; `snapshot` returns `contracts/bridge-remodel.md`'s table row for `remodel.snapshot`, normative and not restated here - note `names{}` is keyed by persist ref, not a list, and `feature_count` is part of it
- [x] T062 Implement `Rms/RemodelScopeProbe.cs` and the `remodel.probe_scope`, `remodel.open` and `remodel.snapshot` handlers in `extractor/SwReview.Extractor/Bridge/BridgeDispatcher.cs`
- [x] T063 [P] Write the mutating handler cases for `rename`, `reorder`, `folder`, `describe` and `equation` (`op` in `{add, set, delete}`, the `set` branch being the FR-029 repair of T089a): each resolves its persist ref through `IModelDocExtension.GetObjectByPersistReference3(Object, Int32&)` (VERIFIED, ByRef error code) and reads that error code on **every** resolve, reads `IFeature.get_Name` (VERIFIED) and calls the name-based API in one breath; a ref that stops resolving after a reorder is a failed change recording "could not be addressed after change N" (RK-13) and never a retry; `reorder` returning a bare `false` is a **contract violation** that stops the run, because legality was decided from the dependency graph before the call, so the run never retries and never searches; `folder` supports `create` and `rename` only and refuses `dissolve` in v1
- [x] T064 Implement those handlers
- [x] T065 [P] Write the `rebuild`, `save` and `close` handler cases: `rebuild` returns `contracts/bridge-remodel.md`'s table row for `remodel.rebuild`, which is normative and is not restated here, with the per-feature `IFeature.GetErrorCode2` (VERIFIED) walk carried in its `feature_errors[]` field as the **primary** reading - the field T104's verify phase reads - and `GetWhatsWrong` (VERIFIED) as corroborating only; `save` calls `IModelDoc2.Save3(swSaveAsOptions_Silent = 1, out errors, out warnings)` (VERIFIED), treats `errors != 0` as a failed run, treats `warnings & swFileSaveWarning_RebuildError` (VERIFIED) as a failed run **with a saved artifact** and says so rather than reporting success, and asserts `GetSaveFlag() == false` (VERIFIED) afterwards; `close` closes the tagged copy only
- [x] T066 Implement those handlers
- [x] T067 [P] Write `extractor/SwReview.Extractor.Tests/AddEquationVerifiedTests.cs`: the single helper `AddEquationVerified(text, whichConfigs)` calls `IEquationMgr.Add3` (VERIFIED) and asserts `GetCount()` incremented **and** `get_Equation(i)` round-trips; on failure it calls `Add2` (VERIFIED) and asserts again; a second failure fails the change; the assertion is never inlined at a call site, because it is the only evidence the add happened
- [x] T068 Implement `AddEquationVerified` and route every equation add through it
- [x] T069 [P] Write the secret-scope cases in `extractor/SwReview.AddIn.Tests/InProcPipeServerTests.cs` and `CliProfileWriterTests`: the third secret `RemodelSecret` authorizes `ping` and `remodel.*` (including `probe_scope`) and **nothing else**; the **negative** cases, one row per command, the general-chat secret refused for every `remodel.*`, the review secret refused for every `remodel.*`, and the remodel secret refused for `interference` and every other non-remodel command; the remodel commands appear in **neither** the MCP function list nor the terminal profile's `enabled_tools`, as a sibling of the existing test that asserts those two lists are equal; `ToolListingCheckTests` and `CliProfileWriterTests` stay green with **no edits**
- [x] T070 Implement `RemodelSecret` in `extractor/SwReview.AddIn/ToolService/ToolServiceHost.cs` and `ScopedSecretPolicy` in `extractor/SwReview.Extractor/Bridge/SecretPolicy.cs`, handing it only to the remodel backend session, and write `remodel.log` (one line per bridge request: command, elapsed, gated members, target path, redacted)
- [x] T071 [P] Write `reviewer/tests/unit/test_remodel_client.py`: `bridge/remodel_client.py` maps each of the twelve commands, sends **no document argument** for any of them after `remodel.open` has returned (asserted over the whole table), surfaces `error_class` unchanged, and returns `circuit_open` when the existing `CircuitBreaker` has tripped after 3 consecutive failures rather than raising
- [x] T072 Implement `reviewer/src/swreview/bridge/remodel_client.py` on top of the existing `bridge/client.py`
- [x] T073 Reconcile `specs/004-resilient-remodeler/contracts/bridge-remodel.md` with what shipped (the twelve commands, their payloads, their replies, their error classes, and the normative statement that **no `remodel.*` command that writes names a document**, only `probe_scope` and `open` naming a path and both before the scope exists), bump the bridge protocol from 1.0 to **1.1** additively in `extractor/SwReview.Extractor.Console/Serve/PROTOCOL.md`, and confirm the four existing commands and the envelope are untouched

**Checkpoint**: every bridge command is exercised against fakes; the third secret is proven to authorize only `remodel.*`, and the other two secrets are proven to authorize none of it.

---

## Phase 5: Executor and artifacts (US1 and US4)

**Purpose**: the deterministic executor. It walks `plan.changes`, one bridge call per change, rebuilds and compares the error count after each, applies the recorded inverse on failure, and enforces the limits. No model is in this loop.

- [x] T074 [P] [US1] Write `reviewer/tests/unit/test_remodel_apply_log.py`: the `changes.jsonl` line format of `contracts/run-artifacts.md`: `ChangeRecord` carries exactly the fields of `data-model.md` section 2, which is normative and which `contracts/run-artifacts.md` restates verbatim: `seq`, `at`, `kind`, `subject{feature_id, name, persist_ref}`, `before`, `after`, `undo{command, params}`, `rebuild_errors_before`, `rebuild_errors_after`, `status`, `error_code`, `error`, **`target_path`** and `elapsed_ms`, asserted as a set equality so a field in one document and not the others fails; `kind` is the closed set `rename | describe | reorder | folder.create | folder.rename | equation.add | equation.edit | save`; `status` is the closed set of **five**, `attempting | applied | failed | rolled_back | rollback_failed`, with `failed` (the change never landed) and `rollback_failed` (it landed, raised the error count, and could not be undone) asserted as distinct outcomes because only the second ends the run (T080); `target_path` is the path `VerifyTarget` confirmed, and one case asserts it agrees with the target `remodel.log` recorded for the same request, which is what makes FR-041 checkable per change rather than per run; `changes.jsonl` is append-only with monotonic `seq`; **the attempting and applied line pair**, one line written **before** the call with `status: "attempting"` and a second after it with a terminal status, so a hard crash mid-change leaves exactly one `attempting` line naming what was in flight; a reader recovering a crashed run finds that line and never treats it as applied
- [x] T075 [US1] Implement the records and the writer in `reviewer/src/swreview/remodel/apply_log.py`
- [x] T076 [P] [US1] Write `reviewer/tests/unit/test_remodel_derive_undo.py`, one test per change kind: `rename` inverts to the recorded previous name; `reorder` inverts to the recorded anchor and location; `folder.rename` inverts to the previous name; `describe` writes the recorded previous text, with `""` for "there was none" and a `before` of `null` (unreadable) **refused up front** so the change is never attempted; `equation.add` inverts to `IEquationMgr.Delete(index)` (VERIFIED) applied in **reverse** order, and only ever for an add this run made; `equation.edit` inverts to `remodel.equation` with `op: "set"` and the recorded `before.equation_text`, so an in-place repair is its own inverse and a referenced global is never deleted in order to change it (FR-029); `save` has no inverse, because the copy is saved once, at the end, only after the gate passes; `folder.create` has **no forward inverse in v1** (dissolving needs `EditDelete`, which the owner removed from the stage-1 allowlist) and therefore escalates to the catastrophic fallback, asserted explicitly rather than left implicit; `EditUndo2` (VERIFIED, returns **void**) is never produced by any inverse
- [x] T077 [US1] Implement `derive_undo(change)` in `reviewer/src/swreview/remodel/apply_log.py` as a pure function
- [x] T078 [P] [US1] Write the happy path in `reviewer/tests/unit/test_remodel_apply.py` over a fake bridge: one bridge call per change; `ForceRebuild3(false)` (VERIFIED) and a `GetWhatsWrongCount()` (VERIFIED) comparison against the run **baseline** after every change; the fixed C1 to C6 order honoured (renames, descriptions, reorders, folders, `equation.edit`, `equation.add`; **no dimension step**, per FR-030); every reorder call carries a `location` from the closed set `{"before", "after"}` and the fake bridge rejects any other value, so the planner and the guard agree from both ends; every folder created by a contiguous `IFeature.Select2` (VERIFIED) run plus `InsertFeatureTreeFolder2(2)` (VERIFIED) plus `set_Name`, each verified with `IFeatureManager.FeatureFolderLocation` (VERIFIED); the run goes **to completion** and never asks for per-change approval
- [x] T079 [US1] Implement the executor core in `reviewer/src/swreview/remodel/apply.py`
- [x] T080 [P] [US1] Write the failure cases in `test_remodel_apply.py` with the fake bridge **scripting a failure at change N**: a change that raises the rebuild-error count above the baseline has its recorded inverse applied, the inverse is confirmed by a second rebuild, the record is written `rolled_back`, and the run continues past it; an inverse that itself fails stops the run with the log intact and names the change that could not be undone; a `RemodelTargetError` from `VerifyTarget` aborts immediately; `circuit_open` aborts and the run **never auto-resumes**, because a resumed run on a tree we did not re-verify is exactly the wrong risk, asserted by a "resume is refused" test
- [x] T081 [US1] Implement the inverse and abort paths
- [x] T082 [P] [US1] Write the limit cases: `max_changes` 250, `max_minutes` 20 wall clock and `max_rebuild_seconds` 120 each produce status `truncated`, finalize cleanly, and report how many planned changes were applied and which were not; **never a silent partial success**; the limits are enforced by the executor and are neither readable nor settable by the model
- [x] T083 [US1] Implement the three limits in `apply.py`
- [x] T084 [P] [US1] Write `reviewer/tests/unit/test_remodel_replay.py`: the catastrophic fallback deletes the copy, re-copies the source and replays `changes.jsonl` up to the last `applied` line; replay is deterministic because every change is persist-ref addressed; a replay whose source attestation no longer matches is refused rather than attempted; a replay that hits the same failure twice stops instead of looping; `EditUndo2` is never the mechanism at any tier
- [x] T085 [US1] Implement the replay fallback
- [x] T086 [P] [US1] Write `reviewer/tests/unit/test_remodel_attestation.py` (the C# side records the three values at copy time in T050; this pins the artifact and the re-check): `source-attestation.json` records the source path, size, `LastWriteTimeUtc` and content hash at plan time and re-checks all three at report time; any difference is a **hard failure** of the run and is reported as such, not as a warning; the attestation is written before any document handle to the source could exist, and the source is never opened for writing
- [x] T087 [US1] Implement the source attestation
- [x] T088 [P] [US1] Write `reviewer/tests/unit/test_remodel_units.py` as a pure regression over the **global's own literal**, because FR-030 puts dimension equations out of scope and the IR carries no dimensions: read the justifying value from the package's feature data (`GlobalEvidence.value_m`, metres, as every length in the IR is); convert it to the document's length unit and seed the literal with that exact value; add the global through `AddEquationVerified` and assert it landed and evaluated; rebuild and assert the equation's text and value round-trip at the document's stored precision rather than at a loose epsilon; assert the geometry snapshot is **identical** to the pre-change one, because a global that drives nothing cannot move geometry, so any delta here is a defect and not a tolerance question; **a 120 mm value never produces `"w" = 0.12`**; a document unit that cannot be read refuses the change instead of assuming metres; angles are handled in degrees per `swAngularEquationUnits_e` (VERIFIED); and one case asserts that no code path on this file reads or writes any `IDimension` member, since `IDimension.set_Name` is off the stage-1 allowlist
- [x] T089 [US1] Implement the global seeding step in `apply.py`: new globals in `order_index` order, each added after every global its expression names and removed in reverse order. There is no dimension rename step and no dimension equation step in v1
- [x] T089a [P] [US1] Write `reviewer/tests/unit/test_remodel_equation_edit.py`, the FR-029 repair path that currently has no call path anywhere: a `PlannedChange` of kind `equation.edit` reads `previous_text` from `get_Equation(index)` (VERIFIED) **before** writing, tries `IEquationMgr.set_Equation(index, text)` (VERIFIED) and asserts `GetCount()` is **unchanged** and the text round-trips, falls back to `SetEquationAndConfigurationOption` (VERIFIED) and asserts again, and fails the change with `equation_unverified` on a second failure; **delete-and-re-add is unreachable from this path**, asserted against a fake bridge that fails any `op: "delete"` during a repair, because while a referenced global is missing every dependent equation enters an error state that does not clear when it returns (research.md R3.5); a previous text that reads back as unreadable refuses the change up front, since there would be no inverse; a rename-only repair carries the existing numeric text across **unchanged** rather than recomputing it; the change is ordered at C5, before every `equation.add`, so a repair never competes with an add for the same name
- [x] T089b [US1] Implement `SetEquationVerified(index, text, whichConfigs)` in C# beside `AddEquationVerified` (one helper, never inlined), the `op: "set"` branch of the `remodel.equation` handler, and the `equation.edit` branch of `apply.py` and `derive_undo`
- [x] T090 [P] [US4] Write `reviewer/tests/unit/test_remodel_grades.py`: `package-before.json` and `package-after.json` are `ModelCheck`-profile dumps of the copy; `rms-before.json` and `rms-after.json` come from 003's `run_rms_check` with no provider constructed (factories monkeypatched to raise); `grades.json` carries 003's `RmsGrade` before and after plus the per-rule delta, with counts per bucket as the headline, the fraction secondary, the unresolved rule ids always named beside it, and **no letter grade**; exceptions are **copied forward** into this run folder from the newest run whose **source** `design_id` matches - read from `package.json`'s `design.design_id` for a `-check` candidate and from `source-attestation.json`'s `source_design_id` for a `-remodel` one, because this run's own packages are dumps of the copy and carry the copy's path-derived id - using 003's carry-forward helper, with no per-design store; the carried exceptions' `persist_ref_scope` values are rebound from the source `document_id` to the copy's before the rules run, and `ExceptionStore.refresh` runs **once**, against `package-before.json`, with the resulting store applied unchanged to both grades, so a rename or a reorder cannot re-open a `feature_tree`-fingerprinted waiver and move the delta for the wrong reason (`contracts/run-artifacts.md`)
- [x] T091 [US4] Implement the grade and dump artifacts in `reviewer/src/swreview/remodel/artifacts.py`
- [x] T092 [P] [US4] Write `reviewer/tests/unit/test_remodel_report.py`: `report.md` carries the change list, the before-and-after grade delta, the geometry comparison, and the rebuild list with a **named reason and a blocking dependency edge per entry**; a run that moved 3 of 200 features says "this part needs rebuilding" in its first line and not "success"; every fillet defaulted to `3-Core` is listed as **reviewed as structural; move to Quarantine if cosmetic**; the coverage section names what the gate cannot detect and every scope refusal; a `truncated` run says so in the first line; the report names the copy path and states, from `remodel.log` and from each `ChangeRecord.target_path` rather than from intent, that every write went to it (FR-041). Then the three FR-056 lines, each its own assertion because each is a distinct claim: the **Resilient Modeling Strategy is credited** as the source of the rules and the group vocabulary; every judgement item names **the provider and the model** that produced it, read from the proposal's own fields and never from a run-level default; and the report states that **the copy is a proposal the engineer accepts or discards, not an engineering acceptance result**. Plus: every `rejected_proposal` is listed with its rejection reason rather than omitted (US4 scenario 7), and the sentence that the added globals drive nothing yet (FR-030). The section order is the nine-row table of `contracts/run-artifacts.md`, asserted as an order and not as a set, so the acceptance sentence cannot be dropped from a long report
- [x] T093 [US4] Implement `reviewer/src/swreview/remodel/report.py` in the nine-section order of `contracts/run-artifacts.md`, including the judgement section (accepted proposals with their provider and model, then the rejected proposals with their rules) and the closing credit and proposal-not-acceptance statement

**Checkpoint**: every rollback path, every limit, the replay fallback and the attestation are green against a fake bridge that scripts failures at change N; no SOLIDWORKS involved.

---

## Phase 6: Geometry gate (US1)

**Purpose**: measure in C#, decide in Python. `remodel/geometry.py` is listed among Phase 1's modules in plan.md; the decision function and its case table are this phase's deliverable and are written here, not there. The decision function has **no COM in its signature** and is table-tested. This suite is what decides whether the product is trustworthy.

- [x] T094 [P] [US1] Write `extractor/SwReview.Extractor.Tests/RemodelGeometryTests.cs` over a fake: `remodel.geometry` builds `IModelDocExtension.CreateMassProperty2()` (VERIFIED) with `AccuracyLevel = swMassPropertyAccuracyLevel_Higher (2)` (VERIFIED), sets `SelectedItems` to the body and **checks `Recalculate()`'s Boolean before reading anything**; it uses the typed `IMassProperty2` interface and **never** `GetMassProperties2`'s raw `Object`, asserted on the recorded member names, because that flat `double[]` index layout is not discoverable by reflection and has changed across API generations; it returns a `GeometryReading` with volume, surface area, centre of mass, principal moments **sorted ascending**, solid body count, face count and edge count, plus `Mass` and the material name from `IPartDoc.GetMaterialPropertyName2` (VERIFIED) recorded **separately**; a `swMassPropertiesStatus_e` other than `OK` (VERIFIED) is reported, never defaulted
- [x] T095 [US1] Implement the `remodel.geometry` handler and the `GeometryReading` record
- [x] T096 [P] [US1] Write `reviewer/tests/unit/test_remodel_tolerances.py`: the two named profiles, `IDENTITY` (volume, area, centre of mass and moments all relative 1e-9, face count exact, body count exact) for stage 1 where nothing geometric is supposed to change, and `EQUIVALENCE` (volume 1e-6, area 1e-5, centre of mass 1e-6, moments 1e-5, face count warn, body count exact) reserved for stage 2; **every tolerance constant is in metres, every angle is in radians, and no comparison mixes an absolute with a relative bound**; loading a profile raises until the PROBE-8 calibration ledger is present, because a tolerance that has never been compared against a known answer does not ship
- [x] T097 [US1] Implement `reviewer/src/swreview/remodel/tolerances.py`
- [x] T098 [P] [US1] Write the tier-1 half of the case table in `reviewer/tests/unit/test_remodel_geometry.py`, one named case each: `identical`; `delta_volume_just_inside`; `delta_volume_just_outside`; `translated_0_1_mm`; `uniformly_scaled_1_0001`; `principal_moments_permuted` (pass **after sorting**, because two bodies differing by a symmetry-degenerate rotation can return the same three numbers permuted); `zero_volume_baseline` (unresolved, **not** a divide-by-zero); `baseline_body_count_2` (unresolved); `mass_properties_status_not_ok` (unresolved); `mass_only_delta` (reports `material_changed`, **never** `geometry_changed`, because mass is volume times density and density is not geometry); `face_count_differs` (fail under `IDENTITY`); `mirrored_tier1_only` (tier 1 alone passes, and the result carries the documented coverage limitation naming reflection, a rigid rotation about a symmetry axis, compensating add and remove pairs, differences occupying no volume, and surface or wire bodies as undetectable)
- [x] T099 [US1] Implement `evaluate(before, after, tolerances) -> GateResult{pass|fail|unresolved}` in `reviewer/src/swreview/remodel/geometry.py`, tri-state and never a boolean
- [x] T100 [P] [US1] Write the residual half of the case table against the optional residual reading (the **decision** is testable now with fixture readings; the tier-2 **measurement** is stage 2), one named case each: `mirrored_with_residual` (tier 1 passes, the residual equals the full volume, the result is `fail` with the mirrored diagnosis spelled out); `one_sliver_residual` (10 mm by 10 mm by 0.2 um); `many_slivers_over_threshold`; `residual_null_bounding_box`; `error_code_1058_boolean_fail` (unresolved, the gate did not run); `error_code_6_empty_body` (pass signal); `error_code_1067_no_intersect` (read **with** the residual volume, never alone); `error_code_1040_partial_coincidence` (inspect the residual, not fail); `error_code_minus_1_unknown` (unresolved); `error_code_1_non_api_body` (unresolved); `tier1_fail_tier2_pass` (**unresolved**, a bug in the tolerances, not silently resolved either way); the three residual signals are read separately and never collapsed into one scalar (total residual volume at or below `max(1e-12 m3, 1e-6 x V)`; residual body count recorded and **not** gated; the largest residual's bounding-box **minimum** dimension at or above 1e-5 m to count as real); all `swBodyOperationError_e` values VERIFIED
- [x] T101 [US1] Implement the residual branch of `evaluate()` behind an optional argument, so stage 2 adds a tier rather than changing the type
- [x] T102 [P] [US1] Write `reviewer/tests/unit/test_remodel_geometry_artifact.py`: `geometry.json` carries both `GeometryReading`s under the field names of `data-model.md` section 3 (`before`, `after`, `gate`, `tolerances`), the `GateResult` with `diagnosis`, `coverage_limits`, `deltas`, `material_changed`, `tier_1` and `tier_2`, and the tolerance profile with its `calibrated` flag and `calibration_ref`; **both readings are of the copy**, `subject: "copy_at_open"` and `subject: "copy_at_end"` (FR-037), each carrying the attested `source_sha256` that makes the baseline stand for the source; the source file is **never opened for the comparison in any mode**, asserted against a fake that fails any open of a path other than the copy's, and no reference body enters any tree, so `IPartDoc.InsertPart3` (VERIFIED) is used nowhere in stage 1; `coverage_limits` names, on every run including a passing one, the difference produced by the baseline rollback and rebuild themselves, alongside reflection, rigid rotation about a symmetry axis, compensating add and remove pairs, differences occupying no volume, and surface and wire bodies
- [x] T103 [US1] Implement the `geometry.json` writer
- [x] T104 [P] [US1] Write `reviewer/tests/unit/test_remodel_verify.py`: the VERIFY phase passes only when `GetWhatsWrongCount() == 0` **and** every `IFeature.GetErrorCode2` equals `swFeatureErrorNone = 0` (VERIFIED) **and** the gate returns `pass` under `IDENTITY`; any one of the three failing discards the copy, writes the report and **does not save**; a `pass` from the gate is never inferred from a successful rebuild
- [x] T105 [US1] Implement the VERIFY phase in `apply.py` and gate the SAVE phase behind it

**Checkpoint**: the full case table is green; the gate's verdict is decided by a pure function no COM object reaches.

---

## Phase 7: Judgement phase, bounded LLM (US3)

**Purpose**: the model proposes intent into the plan and mutates nothing. Nothing on its tool list decides an order, a move, a rollback or a verdict.

- [x] T106 [P] [US3] Write `reviewer/tests/unit/test_agent_events.py`: the `EventSink` that stamps `seq` and `at`, appends to `events.jsonl` and fans out is **extracted** from `agent/runner.py` to `agent/events.py` with no behavior change; the review run's `events.jsonl` stays byte-identical and the feature 001 and 002 goldens stay green **with no edits**, which is the regression proof; the per-turn step budget, `TurnEndReason` handling, `build_system_prompt` and the redactor hand-off (`no_redaction`) are extracted the same way and shared rather than copied
- [x] T107 [US3] Extract `reviewer/src/swreview/agent/events.py` and re-point `agent/runner.py` at it; `ReviewRun` itself is **not** reused by the re-modeler, because it is review-shaped (checklist, findings, evidence requests, coverage buckets, finalization) and none of that fits a remodel run
- [x] T108 [P] [US3] Write the `propose_description(feature_id, text)` cases in `reviewer/tests/unit/test_tools_remodel_plan.py`: per `contracts/tools.md`, the feature exists, is content, the text is 1 to 200 characters, is not equal to the feature's `name` or `type_name`, and contains no newline; each rejection returns `{"error": ...}`, which `RecordedTool.call` already turns into `is_error=True` with no exception, so no new error path is added; an accepted proposal lands in `plan.descriptions[]` and nowhere else
- [x] T109 [US3] Implement `propose_description` in `reviewer/src/swreview/tools/remodel_plan.py`
- [x] T110 [P] [US3] Write the `propose_global(name, expression, rationale, evidence)` cases: the name matches `^[a-z][a-z0-9_]{1,31}$`, is unique and is not an existing global; the expression parses over the declared globals and the documented function set (`sin`, `cos`, `tan`, `atn`, `arcsin`, `sqr`, in **degrees**); a proposal is admissible only where feature data justifies it (hole diameter, shell thickness, fillet radius), and a proposal that would turn a literal sketch dimension into a global is rejected because IR **1.2.0** carries no sketch dimensions; `rationale` is required and an empty one is a rejection; `evidence` names features that exist and carry the justifying parameter in the package, and a name for which the package holds no such parameter is rejected with `no parameter data for this feature in the package`; the argument is `evidence` and not `drives` because FR-030 means nothing is rewired to the new global, which the report states in those words; every rejection is appended to `plan.rejected_proposals[]` as well as returned as `{"error": ...}`
- [x] T111 [US3] Implement `propose_global`
- [x] T112 [P] [US3] Write the `decide_fillet(feature_id, group, rationale)` and `classify_unknown(feature_id, group, rationale)` cases: `decide_fillet` refuses a feature that is not a fillet, accepts only `3-Core` or `6-Quarantine`, and refuses `6-Quarantine` when the feature has dependents; an absent or refused decision **defaults to `3-Core`** (Core never violates `rms.quarantine.has_no_children`) and the feature is listed in the report as reviewed as structural; `classify_unknown` accepts only `unknown` or `ambiguous` features, requires one of the six groups, and writes `plan.targets[]` **and** `plan.deviations[]` so the deviation is always visible
- [x] T113 [US3] Implement `decide_fillet` and `classify_unknown`
- [x] T114 [P] [US3] Write the registration and read-back cases: `get_remodel_plan(section)` reads back without writing; the remodel tools are added through a `Registration` in `reviewer/src/swreview/tools/registry.py` **only when `ToolContext.remodel` is set**, exactly the way `bridge_tools()` is added only when the context carries a bridge; with `remodel` unset the registry is byte-identical to today's, asserted against the existing registry test; none of the four tools appears in the MCP list or the terminal profile
- [x] T115 [US3] Implement the registration and `get_remodel_plan`
- [x] T116 [P] [US3] Write `reviewer/tests/unit/test_remodel_runner.py` over `FakeProvider` scripts: a script that proposes nothing (the run completes and plan rev 2 equals rev 1 plus the recorded absence); a script that proposes valid intent; and **a script that proposes garbage**, a 4000-character description, a description equal to the feature name, an upper-case global name, a global colliding with an existing one, an expression referencing an undeclared global, a `6-Quarantine` fillet that has dependents, a group that is not one of the six, a feature id that does not exist, and a tool call trying to name a document; every one is rejected, the run still completes, and plan rev 2 contains only valid proposals **and one `RejectedProposal` per rejection**, each carrying the tool, the arguments sent, the rejection sentence, the rule id, the provider and the model, so the report can list them (FR-016, US4 scenario 7); each accepted proposal carries its own `provider` and `model` (FR-056); the judgement phase performs **no writes to SOLIDWORKS**, asserted against a bridge fake that fails any call
- [x] T117 [US3] Implement `reviewer/src/swreview/remodel/runner.py` (phases A plan, B judge, C apply, D verify; the only place a provider is constructed)
- [x] T118 [P] [US3] Write the prompt and provider-policy cases: `agent/prompts/remodel_v1.md` loads and states that the model proposes intent and never an order, a move, a rollback or a verdict; the provider is constructed in exactly one place; **OpenAI is the default and Gemini is the only alternative, and no Claude provider is reachable** from this feature, asserted against the provider registry; a run with no judgement slots completes with the `agent.providers` factories monkeypatched to raise, so the apply loop provably runs with no model in it
- [x] T119 [US3] Implement `reviewer/src/swreview/agent/prompts/remodel_v1.md` and the single provider construction site
- [x] T119a [P] [US3] Amended 2026-09-23 (owner, decision 4A; `contracts/tools.md` "Providers", research R7): rewrite `reviewer/tests/unit/test_remodel_prompt_policy.py::test_the_remodel_runner_is_the_one_construction_site` so it asserts that `remodel/runner.py::build_provider` hands its settings, and nothing else, to `cli.provider_factory` and returns what the factory built, for OpenAI and for Gemini; that it refuses the scripted provider before the factory is reached; that `runner.py` no longer reaches the registry itself; and that no other `remodel/` module reaches the registry or the factory. The remodel run's OpenAI adapter is built with lever 6 off, as before. The default-provider and retired-vendor cases cover the factory's body as well as the runner. `tests/unit/test_gemini_provider_construction.py` stays green unchanged. *Amended 2026-09-23 on review:* renamed `test_the_remodel_runner_is_the_one_entry_point` (it asserts that `runner.py` constructs nothing); its detectors now resolve relative imports, aliases and dotted calls, and count `chat.server.build_provider` - the pane's entry point, which builds with the pane's levers - as a way to the factory, so a `remodel/` module reaching either is refused
- [x] T119b [US3] Implement T119a: `build_provider` keeps its scripted-provider refusal and delegates to `cli.provider_factory` through a deferred import (as `chat.server.build_provider` does, because `cli` imports the `remodel` package), with the imports this orphans removed; resolve the `docs/review-backlog.md` entry on `build_provider` being a near-verbatim copy of `cli.provider_factory`

**Checkpoint**: the judgement phase runs entirely on fakes, rejects garbage, and cannot reach SOLIDWORKS.

---

## Phase 8: Pane, tab 5 (US4)

**Purpose**: the Remodel tab. Five tabs (Review, Ask, Extract, Model check, Remodel) with the step strip above them, sharing one `CoreWebView2Environment`.

- [x] T120 [P] [US4] Write the additions to `extractor/SwReview.AddIn.Tests/WebViewFallbackTests.cs`: the Remodel WebView is created **lazily on first tab activation** and not at add-in load (RK-11: five tabs otherwise cost four renderer processes inside the SOLIDWORKS process); the shared `CoreWebView2Environment` is reused and no second environment is created; the tab order is Review, Ask, Extract, Model check, Remodel; activating the tab twice creates one WebView; the WebView2 fallback path still renders the existing message
- [x] T121 [US4] Implement the fifth `TabPage` and the fourth `PageChannel` in `extractor/SwReview.AddIn/TaskPaneControl.cs` with lazy creation
- [x] T122 [P] [US4] Write `extractor/SwReview.AddIn.Tests/RemodelHostTests.cs` for `ready`: the host replies `init {backend:{port,origin}, token, run_root, document:{path,configuration,kind}|null, limits}`; the limits are the executor's and are reported, never settable from the page; the token is never logged
- [x] T123 [US4] Implement `ready` and `init` in `extractor/SwReview.AddIn/Remodel/RemodelHost.cs`; the host owns only `ready` and `remodel.*` and delegates everything else to 003's `PaneActions`
- [x] T124 [P] [US4] Write the `remodel.plan` refusal cases, one per reason and each naming its reason in the reply: no document; not a part; the document is dirty (`GetSaveFlag()` (VERIFIED) is true); read-only; `ListExternalFileReferencesCount2() != 0` (VERIFIED); a scope-gate refusal, with a two-signal case that names **both**; pre-existing rebuild errors; and a second run while one is in flight refused with `error {error_class: "RunInProgress"}`, the same shape `settings.save` already uses for `TurnRunning` (RK-10); on success the host creates the run folder, copies, opens, tags, dumps and plans, then replies `remodel.planned {run_dir, plan_summary}`; **no document handle to the source is ever taken**
- [x] T125 [US4] Implement `remodel.plan`
- [x] T126 [P] [US4] Write the run-control cases: `remodel.start {run_dir}` runs phases B to D **to completion** and replies `remodel.started {chat_id}`; progress arrives as unsolicited `status {stage: copying|dumping|planning|judging|applying|verifying|ready|error, message}`, `remodel.progress {applied, total, current:{seq, kind, subject_name}}` and `remodel.change` carrying one `ChangeRecord` as it is written so the list grows live; `remodel.stop` sets the stop flag, the executor finishes the change in flight, rolls it back if it failed, finalizes, and replies `remodel.stopped {changes_applied}`; `document.changed` naming the **copy** aborts the run; a killed run does not auto-resume and resuming is refused
- [x] T127 [US4] Implement `remodel.start`, `remodel.stop` and the unsolicited message pump
- [x] T128 [P] [US4] Write the result and artifact cases: `remodel.result {run_dir}` replies `{changes[], grade_before, grade_after, geometry, rebuild_list[]}` read **from the run folder** and not from memory; `remodel.open_copy` activates or re-opens the copy; `remodel.discard_copy` closes without saving and deletes `copy/` only, **keeping every other artifact** (`plan.json`, `changes.jsonl`, `report.md`, the grades, the geometry and the logs), so "what did it propose?" stays answerable after the engineer says no; `report.open`, `folder.open` and `log.open` delegate to 003's `PaneActions` with the path canonicalized and asserted a descendant of `run_root`
- [x] T129 [US4] Implement those handlers
- [x] T130 [P] [US4] Write the `remodel.show_change {run_dir, change_seq}` cases: the change's persist ref resolves through 003's `FeatureSelection` plus the existing `SwEntityResolver`, and the reply payload is **identical** to the one `entity.show` already returns, `entity.shown {ok, state_code, message}`, so one resolver serves three tabs; an unresolvable ref replies `ok:false` with the reason named and never silently selects nothing; a folder subject is selected without a zoom
- [x] T131 [US4] Implement `remodel.show_change`
- [x] T132 [P] [US4] Write `extractor/SwReview.AddIn.Tests/RemodelPageContractTests.cs`: every message type of the page-to-host and host-to-page tables, in both directions, asserted against `specs/004-resilient-remodeler/contracts/pane-remodel-messages.md`; the CSP meta tag byte-identical to `specs/002-task-pane-assistant/contracts/pane-host-messages.md`; `NavigationStarting` and `NewWindowRequested` cancelled; the page loads `web/shared/dom.js` from the shared virtual host rather than copying its helpers, because copying them would duplicate the `textContent` **security** rule
- [x] T133 [US4] Implement `extractor/SwReview.AddIn/Remodel/RemodelPage/{index.html, remodel.js, remodel.css}` (three stacked regions: a run header with the before-and-after grade and the geometry verdict; the live change list; the rebuild list with a reason per entry and a Show button per subject) against `specs/004-resilient-remodeler/contracts/pane-remodel-messages.md`
- [x] T134 [P] [US4] Write `extractor/SwReview.AddIn.Tests/RemodelPageInjectionTests.cs`: a feature named `<img src=x onerror=alert(1)>`, a description containing `</script>`, a rebuild-list reason carrying markup, and a run path containing quotes all render as **text** and execute nothing; every untrusted string reaches the DOM only through `dom.js`; the test asserts the rendered node type, not merely the absence of an alert
- [x] T134a [P] [US4] Write `reviewer/tests/unit/test_chat_remodel_routes.py` against a NEW `contracts/backend-remodel.md`: the nine loopback routes the Remodel tab's pipeline calls (`POST /remodel/probe`, `/remodel/open`, `/remodel/plan`, `/remodel/runs`, `GET /remodel/runs/{job_id}`, `GET /remodel/runs/{job_id}/events`, `POST /remodel/runs/{job_id}/package-after`, `/runs/{job_id}/stop`, `POST /remodel/close`), driven through Starlette's in-process client over `FakeRemodelBridge`; every bridge `error_code` maps to a named pane refusal class; `preexisting_rebuild_errors` answers `copy_present: false` with the count; `package-after` refuses any path but `<run_dir>/package-after.json`; `RunNotPlanned`, `ResumeRefused`, `RunInProgress`; the bridge secret is never echoed; no provider is constructed on any route but `/remodel/runs`, and there only through `remodel/runner.py`; the source path is named in no bridge call after `remodel.open`
- [x] T134b [US4] Implement the routes in `reviewer/src/swreview/chat/remodel.py`, registered in `chat/server.py::create_app` with an injectable `remodel_bridge_factory`; `RemodelJob` (worker thread, event stream, stop event, package-after rendezvous with a timeout that finalizes the run as `failed` with the change log intact); `open.json` written at open so `POST /remodel/runs` rebuilds the runner's inputs from the folder alone; `plan_summary` built by `remodel/summary.py`, moved out of `cli.py` with the remodel-plan goldens byte-identical
- [x] T134c [US4] The engineer stop: `stop_requested: Callable[[], bool]` on `run_remodel` and `apply_changes`, read between changes, `StopReason` `"stopped"` joining `TRUNCATING` so the run finalizes `truncated` (contract `remodel.stop`); tests in `test_remodel_apply.py` and `test_remodel_runner.py`
- [x] T134d [P] [US4] Write `extractor/SwReview.AddIn.Tests/BackendRemodelPipelineTests.cs` (over a fake `IRemodelBackend`, `FakeDump` and a fake seat: call order, every stage on the contract's closed list, change lines relayed verbatim once each, one stop posted, one package-after dump at the exact path, the source named nowhere after `OpenCopy`) and `RemodelBackendClientTests.cs` (an in-process `HttpListener`: method, path, token header and body per route; the refusal mapping)
- [x] T134e [US4] Implement `extractor/SwReview.AddIn/Remodel/{IRemodelBackend,RemodelBackendClient,IRemodelSeat,SwRemodelSeat,BackendRemodelPipeline}.cs` per the wiring brief: the two in-process `SwReviewDump` ModelCheck dumps renamed to `package-before.json` and `package-after.json`, the poll loop mapping `plan_state` to `status` stages, `RemodelRefusal` answered by the host as `error {error_class, message}`, `RemodelBridge` exposed on `IToolServiceAccess`
- [x] T134f [US4] Wire `StartRemodelHost` in `extractor/SwReview.AddIn/SwReviewAddIn.cs` mirroring `StartModelCheckHost`: options, pipeline, pump thread, subscribe and unsubscribe `RemodelPageMessageReceived`, dispose, the Remodel page included in `PostStatusToPages`, and the latest-run registration Show needs
- [x] T134g [US4] Extend `RemodelHostTests.cs` for the `RemodelRefusal` mapping, `DeleteCopyFolder` tolerating a copy the bridge already deleted, and the latest-run hook; `specs/004-resilient-remodeler/contracts/backend-remodel.md` written and parsed by T134a the way `RemodelPageContractTests` parses the pane contract

**Checkpoint**: the whole host-message layer and both page test suites are green with fakes; the tab is exercised manually on the workstation in Phase 9.

---

## Phase 9: Workstation bring-up

**Purpose**: everything that cannot be faked. Nothing here changes a design decision; it calibrates and records.

**Added 2026-09-25 (owner, decision 17A): the production seat adapter and its wiring, untracked until now.** Everything in Phases 3 to 8 runs against fakes, and the add-in still hands the bridge no seat: `BridgeServices.RemodelSeat` is never set by `ToolServiceHost.Attach`, so every `remodel.*` command on the add-in answers that no seat is attached. **T152 to T160 are prerequisites of T135**; none of them is implemented by decision 17A, which only tracks them.

- [ ] T152 [P] Write the fake-testable halves first: extend `RemodelInteropManifestTests.CodeMatchesManifest`'s argument-builder table with a row for every interop member T153 to T155 call; a reflection case asserting that each adapter class implements exactly the interfaces it is named for (below) and nothing that would take a document path it did not create; and the host cases of T157 to T160 in `ToolServiceWiringTests`, `RemodelHostTests` and `BackendRemodelPipelineTests`
- [ ] T153 Implement the copy's adapter in `extractor/SwReview.AddIn/Remodel/`: a SOLIDWORKS-facing class implementing `SwReview.Extractor.Rms.IRemodelDocument` with its inherited `IRemodelCopyTarget` (and `IRemodelTarget`), `IScopeSignalSource` and `IGeometrySource`, over the copy's `IModelDoc2`, and one implementing `IEquationTarget` over its `IEquationMgr`; one VERIFIED interop member per interface member, every write reached only through `RemodelScope` and a `SwGate` built with `RemodelGuard`, and the reads under bare keys as today
- [ ] T154 Implement the source's read-only adapter: a class implementing `IRemodelProbeSource` over `ISldWorks.GetOpenDocumentByName`, reading only the members of `contracts/bridge-remodel.md`'s `scope_signals` table plus `GetType`, `GetSaveFlag` and `ListExternalFileReferencesCount2`; it opens nothing and keeps no `IModelDoc2` past the probe
- [ ] T155 Implement the extractor-side seat: a class implementing `SwReview.Extractor.Rms.IRemodelSeat` with `IRemodelToggleHost` - `OpenDocument` through `OpenDoc7` with `Silent | LoadModel = 17` exactly, `CloseDocument` on the tagged copy only, `GetVault`, the three toggles and `CommandInProgress` - named apart from the add-in's existing `SwRemodelSeat`, which implements the add-in's own `IRemodelSeat` for `remodel.open_copy`
- [ ] T156 The frozen interop-manifest additions: every member T153 to T155 call that `extractor/SwReview.Extractor.Tests/Fixtures/InteropSurface/remodel-interop-manifest.json` lacks gets its row, generated by reflection over `SolidWorks.Interop.sldworks` 32.5.0.48 exactly as T055 generated the fixture; `CodeMatchesManifest` and `InstalledAssemblyMatchesManifest` stay green and `contracts/interop-manifest.md` names the additions
- [ ] T157 `ToolServiceHost.Attach` sets `BridgeServices.RemodelSeat` (with `RemodelGate` built with `RemodelGuard` and the remodel observer of T058) on the add-in's in-process host, so `RemodelSeatAvailable` is true there; the console host and every review session keep a null seat
- [ ] T158 The host-owned, per-run `RemodelRunRoot` handoff: the host makes the run folder with `RunFolders.CreateForRemodel` and hands exactly that folder - never one taken from a request - to `BridgeServices.RemodelRunRoot` for that run, before `remodel.open`, and clears it at `remodel.close`; the backend's `run_dir` and the bridge's run root are one folder by construction, and a second run gets a new one
- [ ] T159 Integration gap: activate the copy before the `ModelCheck` dump. `package-before.json` is dumped in-process from the active document, and after `remodel.open` the engineer's source can still be the active one; the pipeline activates the copy and checks `GetPathName()` equals the copy path before `SwReviewDump` runs, and refuses the dump otherwise, so `package-before.json` can never be a dump of the source
- [ ] T160 Integration gap: the tool service re-attaching between Plan and Start. Opening or activating the copy changes the active document, the tool service re-attaches, and the bridge session holding the `RemodelSession` (probe id, copy, toggles) is replaced, so `Start` finds no session and answers `target_mismatch`. Either the remodel session survives a re-attach (it belongs to the run, not to the attached document) or `Start` refuses with a named error that sends the engineer back to Plan; the owner's choice is written here before the code, with tests for it
- [ ] T161 The probe half of the derived-part refusal of T147, **not** a prerequisite of T135: `RemodelScopeProbe` reads the source's derived or mirrored base features (the feature walk and `GetTypeName2`, reads only) into a new `ScopeSignals` row, `ScopeGate` refuses on it with `derived_part` **before** the copy, and `contracts/bridge-remodel.md`'s `scope_signals` table and `data-model.md` section 4.1 gain the row, the Python and C# halves landing together. Until then the refusal is the planner's, after the copy; a derived part whose link is live is expected to refuse earlier on `external_refs` (UNVERIFIED)
- [ ] T135 Workstation: stage 1 end to end on benchmark part A (the RMS-compliant part); expect a near-empty change list, a `pass` gate under `IDENTITY`, and a grade delta of zero; record the wall-clock time of each phase and the bridge call count from `remodel.log`
- [ ] T136 Workstation: stage 1 end to end on benchmark part B and on at least one real part from Phase 0's set; record the change list, the grade delta, the geometry verdict, the rebuild list by reason, and whether the reorganizable fraction matches the Phase 0 prediction; a mismatch is recorded, not explained away
- [ ] T137 Workstation: calibrate `default_group_by_class` and `reviewer/src/swreview/checks/rms_types.yaml` against the `GetTypeName2` values 2024 actually returns (PROBE-11), update `calibrated_version`, and confirm every still-unknown type reports `unresolved` and never "movable"
- [ ] T138 Workstation: re-run PROBE-8 on the pilot workstation, freeze the `IDENTITY` profile against the attained error in the capabilities ledger, and remove the "profile raises until calibrated" guard only once that ledger is checked in; the numbers live in the ledger, not in a comment
- [ ] T139 Workstation, EPDM: a part on a vault path, a part checked out, and a part checked in and read-only are each **copied out** into the run folder and none is refused for vault reasons; the vault path and revision are recorded in `plan.json`; the copy is confirmed to exist only in the run folder
- [ ] T140 Workstation: regenerate the frozen interop manifest from the installed `SolidWorks.Interop.sldworks` 32.5.0.48 (the workstation half of T054 and T056) and confirm the diff is empty; run the Remodel tab manually and confirm lazy WebView creation and the measured renderer-process cost of five tabs
- [ ] T141 Record the results in `benchmarks/native/rms-part/notes.md` (probe verdicts, timings, type census), `specs/004-resilient-remodeler/quickstart.md` (the manual recovery procedure after a mid-run crash, since the run never auto-resumes) and `NOTICE.md` if any attribution changed; then run the DRY and constitution review across `reviewer/src/swreview/remodel/`, `tools/remodel_plan.py`, `Guard/RemodelGuard.cs`, `Rms/RemodelScope.cs`, `Bridge/` and `AddIn/Remodel/`, confirming that the `EventSink`, `PaneActions`, `dom.js`, `FeatureSelection`, `run_rms_check`, `RmsGrade` and `RunFolders` are **shared, not copied**, that features 001, 002 and 003 goldens are byte-identical, and that `ReadOnlyGuard` was not widened

**Checkpoint**: stage 1 has run on a real part, every blocking probe has a recorded verdict, and the owner has the numbers needed to re-spec stage 2.

---

## Dependencies and Execution Order

- **003 US6 sub-phases 9a and 9b (the part-root node, `DumpProfile.ModelCheck`, `run_rms_check`, `RmsGrade`) block everything below.** 003 US6 sub-phases 9d and 9e (`PaneActions`, `FeatureSelection`, `dom.js`) block Phase 8 only.
- T001 first. Then **Phase 1 -> Phase 0 (T002 to T004) -> Phase 2 -> Phases 3 and 4 -> Phase 5 -> Phase 6 -> Phase 7 -> Phase 8 -> Phase 9.** Phase 0's dry run consumes Phase 1's planner, which is why the planner is built before any C# write code exists; a no-go at T004 stops the feature.
- Phase 2's blocking probes (PROBE-1, 2, 3, 4, 12) gate running Phase 5's executor against SOLIDWORKS; the executor is written against fakes either way. PROBE-8 gates shipping the gate, not writing it.
- Phase 3 blocks Phase 4 (the dispatcher calls through `RemodelScope`). Phase 4 blocks Phase 5 (the executor calls the bridge) and Phase 6's C# half.
- Phase 6's `evaluate()` blocks Phase 5's T104 and T105; the rest of Phase 5 does not wait on it.
- Phase 7 depends on Phase 1 (the plan it writes into) and on 003's tool registry; it does not depend on Phases 3 to 6.
- Phase 8 depends on Phases 4, 5, 6 and 7 for the results it displays and on 003 US6 for `PaneActions`, `FeatureSelection` and `dom.js`.
- Phase 9 and Phase 2 are the only phases that need a SOLIDWORKS seat. Every C# task in Phases 3 to 6 and 8 is compiled and fake-tested without one.
- Decision 17A (2026-09-25): T142/T143, T144/T145 and T146/T147 are test-and-implementation pairs on the planner and run before T148, whose "after" counts they produce; T150/T151 narrow the guard and block nothing. **T152 to T160 (the production seat adapter and its wiring) block T135**, and therefore T136 to T140; T152 first, then T153 to T156 together, then T157 to T160. T161 blocks nothing.

### Parallel Opportunities

- Phase 1: T005 and T006 together; then each test task T008, T010, T012, T014, T016, T018, T020, T022 and T024 in parallel, with its implementation following it; T026 after T024; T028 after T026; T030 last.
- Phase 2: T033 to T039 run in one workstation sitting; T033, T034, T035 and T037 first because they are blocking.
- Phase 3: T040/T041, T042/T043, T044/T045, T046/T047, T048/T049, T050/T051, T052/T053, T054/T055 and T057/T058 as parallel pairs; T056 after T055.
- Phase 4: T059/T060 first; then T061/T062, T063/T064, T065/T066 and T067/T068 in parallel; T069/T070 and T071/T072 in parallel with those; T073 last.
- Phase 5: T074/T075 first; then T076/T077, T086/T087, T088/T089, T089a/T089b and T090/T091 in parallel; T078 to T085 are sequential on `apply.py`; T092/T093 last.
- Phase 6: T094/T095 (C#) in parallel with T096/T097 (Python); then T098/T099, then T100/T101; T102/T103 and T104/T105 in parallel.
- Phase 7: T106/T107 first; then T108/T109, T110/T111 and T112/T113 in parallel; then T114/T115, then T116/T117, then T118/T119.
- Phase 8: T120/T121 first; then T122/T123, T124/T125, T126/T127, T128/T129 and T130/T131 as pairs; T132, T133 and T134 last.
- Phases 3 and 4 can run in parallel with Phase 7 (different languages, different files).

## Notes

- The re-modeler works on a **copy it creates in the run folder** and never saves over an existing file. Nothing in stage 1 uses `SaveAs*`; `Save3` takes no filename (VERIFIED) and the copy is opened at its own path, so the source is unreachable rather than merely un-targeted. That property is structural, and every test that pins it (T044, T048, T050, T086) is load-bearing.
- **No `remodel.*` bridge command and no model-facing tool names a document.** Do not add a document parameter "for validation"; the absence is the guarantee.
- The allowlist is closed and interface-qualified. Adding a member to it is a spec change, not an implementation detail. Stage 2 gets a **second additive allowlist**, reviewed on its own.
- `IModelDoc2.EditUndo2` (VERIFIED, returns void) is never used at any tier, and `StartRecordingUndoObject` and `FinishRecordingUndoObject2` are not allowlisted, because an unused allowlist entry is exactly the accidental widening the allowlist exists to prevent.
- The run goes **to completion** and then presents the change list, the before-and-after grade and the geometry comparison. There is no approve-each-change mode.
- An unknown feature type, an unreadable dependency graph, an unreadable fillet radius and an unresolvable persist ref are all `unresolved`, never "movable" and never "pass".
- Success is measured on the **quality of the partition**, not on the size of the reorganized fraction. A run that moves 3 of 200 features is a correct run that says the part needs rebuilding.
- Stage 2 (rebuild) is out of scope for this increment and is re-specified after stage 1 has run on real parts. The tri-state `GateResult` and the optional residual argument exist so that stage 2 adds a tier without a type change.
